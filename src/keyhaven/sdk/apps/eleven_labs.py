# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `eleven_labs` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ElevenLabsEditVoiceSettingsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice_id: str


class ElevenLabsEditVoiceSettingsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ElevenLabsEditVoiceSettingsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    stability: float | None = None
    similarity_boost: float | None = None
    style: float | None = None
    use_speaker_boost: bool | None = None
    speed: float = 1.0


class ElevenLabsEditVoiceSettingsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ElevenLabsEditVoiceSettingsPath
    header: ElevenLabsEditVoiceSettingsHeader = Field(
        default_factory=ElevenLabsEditVoiceSettingsHeader
    )
    body: ElevenLabsEditVoiceSettingsBody = Field(default_factory=ElevenLabsEditVoiceSettingsBody)


class ElevenLabsGetVoiceSettingsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice_id: str


class ElevenLabsGetVoiceSettingsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ElevenLabsGetVoiceSettingsPath


class ElevenLabsListVoicesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_total_count: bool = True
    next_page_token: str | None = None
    page_size: int | None = None
    search: str | None = None
    sort: str | None = None
    sort_direction: str | None = None
    voice_type: str | None = None
    category: str | None = None
    fine_tuning_state: str | None = None


class ElevenLabsListVoicesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ElevenLabsListVoicesQuery | None = None


class ElevenLabsSpeechToSpeechConvertPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice_id: str


class ElevenLabsSpeechToSpeechConvertQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    enable_logging: bool = True
    optimize_streaming_latency: int | None = None
    output_format: Literal[
        "mp3_44100_128", "mp3_22050_32", "mp3_44100_192", "pcm_44100", "mulaw_8000"
    ] = "mp3_44100_128"


class ElevenLabsSpeechToSpeechConvertHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="multipart/form-data", alias="Content-Type")


class ElevenLabsSpeechToSpeechConvertBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    audio: str
    model_id: str = "eleven_english_sts_v2"
    voice_settings: str | None = None
    seed: int | None = None
    remove_background_noise: bool = False


class ElevenLabsSpeechToSpeechConvertRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ElevenLabsSpeechToSpeechConvertPath
    query: ElevenLabsSpeechToSpeechConvertQuery | None = None
    header: ElevenLabsSpeechToSpeechConvertHeader = Field(
        default_factory=ElevenLabsSpeechToSpeechConvertHeader
    )
    body: ElevenLabsSpeechToSpeechConvertBody


class ElevenLabsStreamTextToSpeechPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice_id: str


class ElevenLabsStreamTextToSpeechQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    enable_logging: bool | None = None
    optimize_streaming_latency: int | None = None
    output_format: str | None = None


class ElevenLabsStreamTextToSpeechHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ElevenLabsStreamTextToSpeechBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    text: str
    model_id: str | None = None
    language_code: str | None = None
    voice_settings: dict[str, Any] | None = None
    pronunciation_dictionary_locators: list[Any] | None = None
    seed: int | None = None
    previous_text: str | None = None
    next_text: str | None = None
    previous_request_ids: list[Any] | None = None
    next_request_ids: list[Any] | None = None
    apply_text_normalization: Literal["auto", "on", "off"] | None = None
    apply_language_text_normalization: bool | None = None
    use_pvc_as_ivc: bool | None = None


class ElevenLabsStreamTextToSpeechRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ElevenLabsStreamTextToSpeechPath
    query: ElevenLabsStreamTextToSpeechQuery | None = None
    header: ElevenLabsStreamTextToSpeechHeader = Field(
        default_factory=ElevenLabsStreamTextToSpeechHeader
    )
    body: ElevenLabsStreamTextToSpeechBody


class ElevenLabsNamespace:
    """Typed accessor for the `eleven_labs` app. Reach it via `client.apps.eleven_labs`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def edit_voice_settings(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenLabsEditVoiceSettingsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the configuration for a specific voice adjusting stability, similarity to the original speaker, style exaggeration, speaker boost, and speech speed. Changes apply immediately to this voice and persist across all subsequent speech generation requests until modified again. Use this when you need to fine-tune a voice's output quality, emotional range, or speaking pace for your use case.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "eleven_labs.edit_voice_settings", owner_id, args, account_id=account_id
        )

    async def get_voice_settings(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenLabsGetVoiceSettingsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the current configuration for a specific voice, including stability, similarity boost, style exaggeration, speaker boost, and speed parameters. These settings determine how the voice sounds during speech generation. Use this to review how a voice is configured before generating audio, or to capture current settings before making adjustments.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "eleven_labs.get_voice_settings", owner_id, args, account_id=account_id
        )

    async def list_voices(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenLabsListVoicesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all available voices for the authenticated account with optional search, filtering by category or voice type, and pagination. Each voice result includes its name, unique ID, description, preview URL, and configuration details. Use this to browse available voices, find a specific voice ID for use with text-to-speech endpoints, or discover new voice options for your application.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "eleven_labs.list_voices", owner_id, args, account_id=account_id
        )

    async def speech_to_speech_convert(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenLabsSpeechToSpeechConvertRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Transforms an uploaded audio recording into speech using a different target voice while preserving the original emotion, timing, intonation, and delivery style. Supports background noise removal and seed-based deterministic output. Use this when you need voice dubbing, content localization with consistent delivery, or creative voice replacement while keeping the original performance intact.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "eleven_labs.speech_to_speech_convert", owner_id, args, account_id=account_id
        )

    async def stream_text_to_speech(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenLabsStreamTextToSpeechRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Converts text to speech using a specified voice and streams the audio output in real time with configurable latency optimization and output format. Supports advanced features such as pronunciation dictionaries, context continuity via previous/next text, seed-based deterministic generation, and text normalization control. Use this when you need low-latency audio for real-time applications, or prefer streaming delivery over a single response for longer content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "eleven_labs.stream_text_to_speech", owner_id, args, account_id=account_id
        )
