# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `vimeo` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class VimeoCategoriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    direction: str | None = None
    page: float | None = None
    per_page: float | None = None
    sort: str | None = None


class VimeoCategoriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: VimeoCategoriesListQuery | None = None


class VimeoChannelsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel_id: float


class VimeoChannelsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VimeoChannelsGetPath


class VimeoChannelsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    direction: str | None = None
    filter: str | None = None
    page: float | None = None
    per_page: float | None = None
    query: str | None = None
    sort: str | None = None


class VimeoChannelsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: VimeoChannelsListQuery | None = None


class VimeoGroupsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    group_id: float


class VimeoGroupsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VimeoGroupsGetPath


class VimeoGroupsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    direction: str | None = None
    filter: str | None = None
    page: float | None = None
    per_page: float | None = None
    query: str | None = None
    sort: str | None = None


class VimeoGroupsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: VimeoGroupsListQuery | None = None


class VimeoNamespace:
    """Typed accessor for the `vimeo` app. Reach it via `client.apps.vimeo`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def categories_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VimeoCategoriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all Vimeo content categories, with optional sorting and pagination for browsing. Categories are top-level content classifications (e.g., Music, Sports, Education) used for organizing and discovering videos across the platform. Use when an agent needs to reference valid categories for video tagging, content discovery, or metadata enrichment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vimeo.categories_list", owner_id, args, account_id=account_id
        )

    async def channels_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VimeoChannelsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a specific Vimeo channel identified by its numeric ID, including its name, description, privacy settings, and creation metadata. This allows an agent to inspect a single channel's complete profile. Use when you need to look up the full details of a known channel for reference or content planning.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vimeo.channels_get", owner_id, args, account_id=account_id
        )

    async def channels_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VimeoChannelsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all Vimeo channels available on the platform, with optional filtering by name query, sort direction, and pagination controls. Channels are curated collections of videos organized around a specific theme or topic. Use this when an agent needs to discover existing channels for content organization, reference, or video distribution planning.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vimeo.channels_list", owner_id, args, account_id=account_id
        )

    async def groups_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VimeoGroupsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a specific Vimeo group by its numeric ID, including its name, description, member count, privacy settings, and creation metadata. This allows an agent to inspect a single group's complete profile. Use when you need to review the details of a known group for moderation, reference, or community management purposes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("vimeo.groups_get", owner_id, args, account_id=account_id)

    async def groups_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VimeoGroupsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all Vimeo groups, with optional text query filtering, sorting, and pagination parameters. Groups are community spaces where members can share and discuss videos around common interests. Use when an agent needs to discover public groups for content sharing, community engagement, or audience research.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vimeo.groups_list", owner_id, args, account_id=account_id
        )
