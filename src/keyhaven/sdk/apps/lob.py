# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `lob` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class LobAddressCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    address_line1: str
    address_city: str
    address_state: str
    address_zip: str
    address_country: str | None = None
    address_line2: str | None = None
    company: str | None = None
    phone: str | None = None
    email: str | None = None


class LobAddressCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LobAddressCreateBody


class LobLetterCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to: dict[str, Any]
    from_: dict[str, Any] = Field(alias="from")
    file: str
    description: str | None = None
    color: bool | None = None
    double_sided: bool | None = None
    merge_variables: dict[str, Any] | None = None
    metadata: dict[str, Any] | None = None


class LobLetterCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LobLetterCreateBody


class LobLettersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None


class LobLettersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LobLettersListQuery | None = None


class LobPostcardCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to: dict[str, Any]
    from_: dict[str, Any] = Field(alias="from")
    front: str
    back: str
    description: str | None = None
    size: str | None = None
    merge_variables: dict[str, Any] | None = None
    metadata: dict[str, Any] | None = None


class LobPostcardCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LobPostcardCreateBody


class LobPostcardsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    include: list[Any] | None = None


class LobPostcardsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LobPostcardsListQuery | None = None


class LobUsVerificationCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    primary_line: str
    city: str | None = None
    state: str | None = None
    zip_code: str | None = None
    secondary_line: str | None = None


class LobUsVerificationCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LobUsVerificationCreateBody


class LobNamespace:
    """Typed accessor for the `lob` app. Reach it via `client.apps.lob`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def address_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LobAddressCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Saves a new address to Lob's address book with name, company, street, city, state, and ZIP details. Use this to store frequently-used addresses for later postcard or letter creation. The address is persisted in your Lob account and can be reused across mailings. This is a reversible internal operation — addresses can be deleted without affecting past mailings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lob.address_create", owner_id, args, account_id=account_id
        )

    async def letter_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LobLetterCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates and mails a physical letter via Lob's print-and-send service, with options for color, double-sided printing, and merge variables for personalization. The letter is printed on paper, stuffed in an envelope, and sent through USPS at a real per-piece cost. This action is financially consequential and cannot be recalled once submitted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lob.letter_create", owner_id, args, account_id=account_id
        )

    async def letters_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LobLettersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists sent and pending letters with pagination, showing status, recipient, and creation timestamp. Use this to monitor mailed letters, check delivery status, or locate a specific letter by ID. Read-only — no letters are created or modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("lob.letters_list", owner_id, args, account_id=account_id)

    async def postcard_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LobPostcardCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Designs and mails a physical postcard via Lob's print-and-send service, using custom HTML or a template URL for the front and back. The postcard is printed, addressed, and sent through USPS — it incurs a real monetary cost per piece and physically reaches the recipient's mailbox. This action is not reversible once submitted; cancellation is only possible during a brief pre-print window.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lob.postcard_create", owner_id, args, account_id=account_id
        )

    async def postcards_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LobPostcardsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists sent and pending postcards with pagination, showing their status, recipient, and creation date. Use this to track mailings, review delivery status, or find a specific postcard ID. Read-only — no postcards are created or modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lob.postcards_list", owner_id, args, account_id=account_id
        )

    async def us_verification_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LobUsVerificationCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submits a US address to Lob's CASS-certified verification engine, which validates, corrects, and standardizes it to USPS format. Use this before creating a postcard or letter to ensure deliverability and avoid wasted postage. The operation is a validation lookup — it does not store the address or send any mail, and no persistent data is created.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lob.us_verification_create", owner_id, args, account_id=account_id
        )
