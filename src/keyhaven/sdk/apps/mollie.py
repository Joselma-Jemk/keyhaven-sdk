# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `mollie` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MollieCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    email: str | None = None
    locale: str | None = None
    metadata: dict[str, Any] | None = None


class MollieCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MollieCustomerCreateBody = Field(default_factory=MollieCustomerCreateBody)


class MollieCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: str | None = Field(default=None, alias="from")
    limit: int = 50
    testmode: bool | None = None


class MollieCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MollieCustomersListQuery | None = None


class MolliePaymentCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    amount: dict[str, Any]
    description: str
    redirectUrl: str
    webhookUrl: str | None = None
    method: str | None = None
    metadata: dict[str, Any] | None = None
    locale: str | None = None


class MolliePaymentCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MolliePaymentCreateBody


class MolliePaymentGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class MolliePaymentGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    embed: str | None = None
    testmode: bool | None = None


class MolliePaymentGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MolliePaymentGetPath
    query: MolliePaymentGetQuery | None = None


class MolliePaymentRefundCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    paymentId: str


class MolliePaymentRefundCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    amount: dict[str, Any]
    description: str | None = None
    metadata: dict[str, Any] | None = None


class MolliePaymentRefundCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MolliePaymentRefundCreatePath
    body: MolliePaymentRefundCreateBody


class MolliePaymentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: str | None = Field(default=None, alias="from")
    limit: int = 50
    profileId: str | None = None
    testmode: bool | None = None


class MolliePaymentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MolliePaymentsListQuery | None = None


class MollieNamespace:
    """Typed accessor for the `mollie` app. Reach it via `client.apps.mollie`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MollieCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new customer record in Mollie with name, email, locale, and optional metadata. Customers are used as the basis for recurring payments, subscriptions, and saved payment methods. Use this when an agent needs to register a new buyer before setting up a recurring billing relationship. This action is reversible by deleting the customer if no payments are linked.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mollie.customer_create", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MollieCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of all customer records stored on the Mollie account, with optional cursor-based pagination and test-mode filtering. Each customer record includes name, email, locale, and metadata. Use this to discover existing customers before creating recurring payments or subscriptions. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mollie.customers_list", owner_id, args, account_id=account_id
        )

    async def payment_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MolliePaymentCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new payment in Mollie and obtain a checkout URL to redirect the customer to the hosted payment page. Requires an amount (currency + value), a description that appears on the customer's bank statement, and a redirect URL for post-payment return. Optionally restrict the payment method (e.g. iDEAL, credit card) or provide a webhook for status updates. Use this when an agent needs to initiate a financial transaction. This action moves real money when the payment completes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mollie.payment_create", owner_id, args, account_id=account_id
        )

    async def payment_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MolliePaymentGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve full details of a single Mollie payment by its unique identifier (e.g. tr_7UhSN1zuXS), including amount, status, method, refunds, chargebacks, and timestamps. Use this to check whether a payment succeeded, inspect amounts before issuing a refund, or debug a transaction. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mollie.payment_get", owner_id, args, account_id=account_id
        )

    async def payment_refund_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MolliePaymentRefundCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Issue a refund against an existing Mollie payment, for the full amount or a partial value. The refund amount must be in the same currency as the original payment and cannot exceed the remaining refundable balance. Refunds reverse money from your account back to the customer and may take several business days to process depending on the payment method. Use this when an agent needs to return funds to a customer. This action is irreversible once submitted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mollie.payment_refund_create", owner_id, args, account_id=account_id
        )

    async def payments_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MolliePaymentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of payments from the connected Mollie account, optionally filtered by profile or test-mode. Each payment record includes status, amount, method, timestamps, and related links. Use this to review recent transactions, reconcile payments, or find a specific payment ID before performing follow-up operations like refunds. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mollie.payments_list", owner_id, args, account_id=account_id
        )
