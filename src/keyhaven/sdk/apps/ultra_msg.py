# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `ultra_msg` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class UltraMsgGetAllGroupsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class UltraMsgGetAllGroupsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UltraMsgGetAllGroupsPath


class UltraMsgGetContactsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class UltraMsgGetContactsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UltraMsgGetContactsPath


class UltraMsgGetGroupIdsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class UltraMsgGetGroupIdsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UltraMsgGetGroupIdsPath


class UltraMsgGetMessagesByStatusPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class UltraMsgGetMessagesByStatusQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str | None = None
    page: int | None = None
    limit: int | None = None
    sort: str | None = None


class UltraMsgGetMessagesByStatusRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UltraMsgGetMessagesByStatusPath
    query: UltraMsgGetMessagesByStatusQuery | None = None


class UltraMsgSendChatMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class UltraMsgSendChatMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to: str
    body: str


class UltraMsgSendChatMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UltraMsgSendChatMessagePath
    body: UltraMsgSendChatMessageBody


class UltraMsgNamespace:
    """Typed accessor for the `ultra_msg` app. Reach it via `client.apps.ultra_msg`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_all_groups(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UltraMsgGetAllGroupsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all WhatsApp groups that the UltraMsg instance is a member of, returning each group's ID, name, description, and participant list. This enables an agent to identify which groups the instance can broadcast to or monitor. Use when you need to discover available groups for sending group-targeted messages or auditing group membership.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ultra_msg.get_all_groups", owner_id, args, account_id=account_id
        )

    async def get_contacts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UltraMsgGetContactsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the list of WhatsApp contacts associated with the configured UltraMsg instance, including phone numbers and display names known to WhatsApp. This allows an agent to discover available recipients and their identifiers for targeting messages. Use when you need to enumerate known contacts before sending outgoing messages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ultra_msg.get_contacts", owner_id, args, account_id=account_id
        )

    async def get_group_ids(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UltraMsgGetGroupIdsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a compact list of all WhatsApp group IDs associated with the UltraMsg instance, without full group metadata such as descriptions or participant lists. This is a lighter alternative to GET_ALL_GROUPS when only group identifiers are needed for targeting or reference. Use when you need to quickly enumerate group targets before sending a group message.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ultra_msg.get_group_ids", owner_id, args, account_id=account_id
        )

    async def get_messages_by_status(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UltraMsgGetMessagesByStatusRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves previously sent WhatsApp messages for a given UltraMsg instance, filtered by delivery status (sent, queued, unsent, invalid) with pagination support. This provides visibility into outbound message delivery outcomes for auditing or troubleshooting. Use when an agent needs to verify delivery success, investigate failures, or monitor message throughput.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ultra_msg.get_messages_by_status", owner_id, args, account_id=account_id
        )

    async def send_chat_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UltraMsgSendChatMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends an outbound WhatsApp text message to a specified phone number or group via the UltraMsg API. The message is delivered to the recipient's WhatsApp account in real time, appearing as a standard chat message from the instance's WhatsApp number. Use this when an agent needs to notify a contact, respond to an inquiry, or send alerts programmatically through WhatsApp.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ultra_msg.send_chat_message", owner_id, args, account_id=account_id
        )
