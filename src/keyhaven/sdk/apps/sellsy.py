# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `sellsy` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SellsyCompaniesSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 25
    offset: str | None = None
    order: str | None = None
    direction: str | None = None
    field: str | None = None


class SellsyCompaniesSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filters: dict[str, Any] | None = None


class SellsyCompaniesSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SellsyCompaniesSearchQuery | None = None
    body: SellsyCompaniesSearchBody | None = None


class SellsyContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    civility: str | None = None
    first_name: str | None = None
    last_name: str
    email: str | None = None
    phone_number: str | None = None
    mobile_number: str | None = None
    position: str | None = None
    note: str | None = None


class SellsyContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SellsyContactCreateBody


class SellsyContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class SellsyContactGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    field: str | None = None
    embed: str | None = None


class SellsyContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SellsyContactGetPath
    query: SellsyContactGetQuery | None = None


class SellsyContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 25
    offset: str | None = None
    order: str | None = None
    direction: str | None = None
    field: str | None = None
    embed: str | None = None


class SellsyContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SellsyContactsListQuery | None = None


class SellsyInvoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 25
    offset: str | None = None
    order: str | None = None
    direction: str | None = None
    field: str | None = None
    embed: str | None = None


class SellsyInvoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SellsyInvoicesListQuery | None = None


class SellsyOpportunityCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    number: str | None = None
    amount: float | None = None
    currency: str | None = None
    due_date: str | None = None
    owner_id: int | None = None
    step_id: int | None = None
    funnel_id: int | None = None
    related: list[Any] | None = None


class SellsyOpportunityCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SellsyOpportunityCreateBody


class SellsyNamespace:
    """Typed accessor for the `sellsy` app. Reach it via `client.apps.sellsy`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def companies_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SellsyCompaniesSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches companies in the Sellsy CRM using structured filters (name, email, smart tags, owner) with pagination and sorting. Despite using POST, this is a read-only search operation that returns matching company records without modifying any data. Agents should use this to find existing companies during data enrichment, lead qualification, or before creating opportunities. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sellsy.companies_search", owner_id, args, account_id=account_id
        )

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SellsyContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact record in the Sellsy CRM with personal details (name, email, phone), job position, and optional notes. The contact appears immediately in the CRM and can be linked to companies, opportunities, and invoices. Agents should use this when adding a new individual lead or contact to the system. This action is reversible by deleting the contact.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sellsy.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SellsyContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single CRM contact by its numeric identifier, including personal info, communication details, and notes. Agents should use this when they need specific contact information for a known contact, such as email or phone number, to use in further operations. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sellsy.contact_get", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SellsyContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of CRM contacts with sorting, field selection, and embedding of related resources. Contacts are individuals linked to companies or standalone leads in the Sellsy CRM. Agents should use this to browse contacts for lookup, deduplication, or before creating new contacts to avoid duplicates. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sellsy.contacts_list", owner_id, args, account_id=account_id
        )

    async def invoices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SellsyInvoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of invoices with sorting, field selection, and embedding of related resources. Invoices represent billed amounts sent to customers with their statuses (draft, sent, paid, overdue). Agents should use this to check invoice history, verify payment statuses, or prepare financial summaries. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sellsy.invoices_list", owner_id, args, account_id=account_id
        )

    async def opportunity_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SellsyOpportunityCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new sales opportunity in the Sellsy CRM pipeline with amount, currency, expected close date, owner, and pipeline stage. The opportunity is linked to a company or individual and appears immediately in the CRM sales funnel, affecting pipeline reporting and forecasting. Agents should use this when recording a potential deal. This action is reversible by moving the opportunity to a lost stage or deleting it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sellsy.opportunity_create", owner_id, args, account_id=account_id
        )
