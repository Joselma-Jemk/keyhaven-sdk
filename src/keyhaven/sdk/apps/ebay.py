# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `ebay` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class EbayItemGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    item_id: str


class EbayItemGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldgroups: str | None = None


class EbayItemGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EbayItemGetPath
    query: EbayItemGetQuery | None = None


class EbayItemGetItemByLegacyIdListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldgroups: str | None = None
    legacy_item_id: str
    legacy_variation_id: str | None = None
    legacy_variation_sku: str | None = None


class EbayItemGetItemByLegacyIdListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EbayItemGetItemByLegacyIdListQuery


class EbayItemListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    item_ids: str | None = None
    item_group_ids: str | None = None


class EbayItemListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EbayItemListQuery | None = None


class EbayItemSummarySearchListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    aspect_filter: str | None = None
    auto_correct: str | None = None
    category_ids: str | None = None
    charity_ids: str | None = None
    compatibility_filter: str | None = None
    epid: str | None = None
    fieldgroups: str | None = None
    filter: str | None = None
    gtin: str | None = None
    limit: str | None = None
    offset: str | None = None
    q: str | None = None
    sort: str | None = None


class EbayItemSummarySearchListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EbayItemSummarySearchListQuery | None = None


class EbayShoppingCartListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class EbayNamespace:
    """Typed accessor for the `ebay` app. Reach it via `client.apps.ebay`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def item_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EbayItemGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch comprehensive details for a single eBay item by its unique item ID, with optional field groups to control response depth (e.g., include product identifiers, shipping options, or seller legal info). Returns full listing data including title, price, condition, seller, shipping, and return policy. Use this for deep-dive lookups on a specific listing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("ebay.item_get", owner_id, args, account_id=account_id)

    async def item_get_item_by_legacy_id_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EbayItemGetItemByLegacyIdListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Look up an eBay item using its legacy item ID from the classic eBay API, with optional legacy variation ID or SKU. Returns the modern item details including the current item ID and full listing data. Use this when migrating from an older system or when only legacy identifiers are available.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ebay.item_get_item_by_legacy_id_list", owner_id, args, account_id=account_id
        )

    async def item_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EbayItemListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve detailed information about one or more eBay items using their item IDs or group IDs. Returns pricing, availability, seller details, condition, and shipping options for each item. Use this when an agent needs to look up active eBay listings by known identifiers for comparison, display, or checkout preparation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("ebay.item_list", owner_id, args, account_id=account_id)

    async def item_summary_search_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EbayItemSummarySearchListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search eBay's active listings catalog using keywords, category filters, aspect filters, charity IDs, and other criteria. Supports pagination (limit/offset), sorting by price or date, auto-correction, and GTIN/EPID lookup. Returns summarised results with title, price, image, shipping cost, and seller name. Use this when an agent needs to find items for comparison, market research, or purchase decisions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ebay.item_summary_search_list", owner_id, args, account_id=account_id
        )

    async def shopping_cart_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EbayShoppingCartListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the current contents of the authenticated user's eBay shopping cart, including item titles, quantities, prices, and seller information. This is personal account data tied to the user's session. Use this to inspect cart contents before checkout or to gather item details for downstream processing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ebay.shopping_cart_list", owner_id, args, account_id=account_id
        )
