# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `maxio` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MaxioCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: dict[str, Any]


class MaxioCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MaxioCustomerCreateBody


class MaxioCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    q: str | None = None
    date_field: str | None = None
    start_date: str | None = None
    end_date: str | None = None
    direction: str | None = None


class MaxioCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MaxioCustomersListQuery | None = None


class MaxioInvoiceGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    uid: str


class MaxioInvoiceGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MaxioInvoiceGetPath


class MaxioSubscriptionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    subscription: dict[str, Any]


class MaxioSubscriptionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MaxioSubscriptionCreateBody


class MaxioSubscriptionUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    subscription_id: int


class MaxioSubscriptionUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    subscription: dict[str, Any]


class MaxioSubscriptionUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MaxioSubscriptionUpdatePath
    body: MaxioSubscriptionUpdateBody


class MaxioSubscriptionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    state: str | None = None
    date_field: str | None = None
    start_date: str | None = None
    end_date: str | None = None


class MaxioSubscriptionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MaxioSubscriptionsListQuery | None = None


class MaxioNamespace:
    """Typed accessor for the `maxio` app. Reach it via `client.apps.maxio`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MaxioCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer record in the Maxio (Chargify) billing system with required first name, last name, and email, plus optional address, phone, tax, and reference fields. The customer is immediately available for subscription assignment. Use this when onboarding a new client who will be assigned a subscription and billing profile.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "maxio.customer_create", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MaxioCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of customers from the Maxio (Chargify) billing system, with optional search by email, ID, reference, or organization, and date-range filtering. Results include customer IDs, names, email, organization, and billing addresses. Use this to find a customer by reference or email before creating a subscription, or to audit the customer base.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "maxio.customers_list", owner_id, args, account_id=account_id
        )

    async def invoice_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MaxioInvoiceGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single invoice by its unique UID from the Maxio (Chargify) billing system, including line items, totals, taxes, discounts, payment status, and customer details. Use this to view or verify an invoice after creation, check payment status, or extract line items for accounting reconciliation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "maxio.invoice_get", owner_id, args, account_id=account_id
        )

    async def subscription_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MaxioSubscriptionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new subscription for a customer in the Maxio (Chargify) billing system, associating them with a product and optional payment method, coupon codes, and component quantities. If a credit card is provided the first charge may be processed immediately depending on the product's billing schedule. Use this when a customer purchases a product — this action triggers the billing lifecycle and may generate an invoice and payment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "maxio.subscription_create", owner_id, args, account_id=account_id
        )

    async def subscription_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MaxioSubscriptionUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing subscription's product, billing day, payment collection method, or next billing date. Changes take effect on the next billing cycle and may alter the customer's invoice amount or schedule. Use this when a customer upgrades, downgrades, or changes their payment method — but note that changes involving a new product may trigger prorated charges.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "maxio.subscription_update", owner_id, args, account_id=account_id
        )

    async def subscriptions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MaxioSubscriptionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of subscriptions from the Maxio (Chargify) billing system, with optional filtering by state (active, canceled, etc.) and date range. Each result includes the subscription ID, customer info, product, state, and current billing period. Use this to review active subscriptions, find a subscription by customer or state, or export billing data for reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "maxio.subscriptions_list", owner_id, args, account_id=account_id
        )
