# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `breezy` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BreezyGetCompanyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: str


class BreezyGetCompanyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class BreezyGetCompanyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BreezyGetCompanyPath
    header: BreezyGetCompanyHeader = Field(default_factory=BreezyGetCompanyHeader)


class BreezyGetPositionByidPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: str
    position_id: str


class BreezyGetPositionByidHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class BreezyGetPositionByidRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BreezyGetPositionByidPath
    header: BreezyGetPositionByidHeader = Field(default_factory=BreezyGetPositionByidHeader)


class BreezyGetPositionsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: str


class BreezyGetPositionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    state: Literal["draft", "archived", "published", "closed", "pending"] = "published"


class BreezyGetPositionsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class BreezyGetPositionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BreezyGetPositionsPath
    query: BreezyGetPositionsQuery | None = None
    header: BreezyGetPositionsHeader = Field(default_factory=BreezyGetPositionsHeader)


class BreezyNamespace:
    """Typed accessor for the `breezy` app. Reach it via `client.apps.breezy`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_company(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BreezyGetCompanyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full profile and configuration details for a specific company in your Breezy HR account using its unique company ID. Returns the company name, branding settings, subscription level, and organizational metadata. Use this to verify which company context is active before performing other operations tied to that company, such as listing positions or managing candidates.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "breezy.get_company", owner_id, args, account_id=account_id
        )

    async def get_position_byid(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BreezyGetPositionByidRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the complete details of a single job position using its unique position ID and parent company ID. Returns comprehensive information including the full job description, requirements, hiring team members, creation and update timestamps, and current workflow stage. Use this when you need to inspect a role's full configuration — for example, before updating the position, reviewing its candidate pipeline, or checking its publication status before sharing a link.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "breezy.get_position_byid", owner_id, args, account_id=account_id
        )

    async def get_positions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BreezyGetPositionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all job positions for a specified company, optionally filtered by their current lifecycle state (draft, published, archived, closed, or pending). Returns key metadata for each position including title, department, location, creation date, and status — without exposing candidate-level data. Use this to discover open roles, monitor the hiring pipeline, or obtain a specific position ID needed for downstream Breezy operations like fetching candidates or updating the position.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "breezy.get_positions", owner_id, args, account_id=account_id
        )
