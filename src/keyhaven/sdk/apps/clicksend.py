# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `clicksend` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ClicksendEmailAddressesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email_address_id: float


class ClicksendEmailAddressesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClicksendEmailAddressesGetPath


class ClicksendEmailCampaignsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class ClicksendEmailHistoryListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class ClicksendFaxInboundListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class ClicksendFaxReceiptsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    rule_id: float


class ClicksendFaxReceiptsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClicksendFaxReceiptsGetPath


class ClicksendNamespace:
    """Typed accessor for the `clicksend` app. Reach it via `client.apps.clicksend`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def email_addresses_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClicksendEmailAddressesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves details about a specific allowed email address configured in your ClickSend account, identified by its email address ID. Allowed addresses are the sender addresses approved for outgoing email. Use this to verify a sender address before using it in a campaign.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clicksend.email_addresses_get", owner_id, args, account_id=account_id
        )

    async def email_campaigns_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClicksendEmailCampaignsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all email campaigns in your ClickSend account, including their status, name, and metadata. Campaigns represent bulk email sends to contact lists. Use this to browse existing campaigns, check their status, or find a specific campaign ID for further actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clicksend.email_campaigns_list", owner_id, args, account_id=account_id
        )

    async def email_history_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClicksendEmailHistoryListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the email send history for your ClickSend account, including sent emails, delivery status, timestamps, and recipient addresses. This log contains email addresses and delivery outcomes. Use this to audit email campaigns, check delivery failures, or verify that specific emails were sent.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clicksend.email_history_list", owner_id, args, account_id=account_id
        )

    async def fax_inbound_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClicksendFaxInboundListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all inbound fax automation rules configured in your ClickSend account. Each rule defines how incoming faxes are processed, routed, or forwarded. Use this to review existing fax handling rules before modifying or adding new ones.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clicksend.fax_inbound_list", owner_id, args, account_id=account_id
        )

    async def fax_receipts_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClicksendFaxReceiptsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves details of a specific fax receipt automation rule by its rule ID. The rule defines how fax delivery receipts are handled, such as forwarding to email or webhook. Use this to inspect a single rule's configuration without modifying it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clicksend.fax_receipts_get", owner_id, args, account_id=account_id
        )
