# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `dexscreener` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DexscreenerCheckTokenOrdersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainId: str
    tokenAddress: str


class DexscreenerCheckTokenOrdersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DexscreenerCheckTokenOrdersPath


class DexscreenerGetPairsByAddressPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainId: str
    pairId: str


class DexscreenerGetPairsByAddressRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DexscreenerGetPairsByAddressPath


class DexscreenerGetPairsByTokenPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainId: str
    tokenAddresses: str


class DexscreenerGetPairsByTokenRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DexscreenerGetPairsByTokenPath


class DexscreenerGetTokenBoostsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class DexscreenerGetTokenPoolsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainId: str
    tokenAddress: str


class DexscreenerGetTokenPoolsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DexscreenerGetTokenPoolsPath


class DexscreenerGetTokenProfilesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class DexscreenerGetTopTokenBoostsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class DexscreenerSearchPairsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    q: str


class DexscreenerSearchPairsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DexscreenerSearchPairsQuery


class DexscreenerNamespace:
    """Typed accessor for the `dexscreener` app. Reach it via `client.apps.dexscreener`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def check_token_orders(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerCheckTokenOrdersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Checks the paid promotion orders placed for a specific token on a given blockchain, returning order details. Use this when an agent needs to verify whether a token has active promotion orders and analyse the order history. This is read-only public data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.check_token_orders", owner_id, args, account_id=account_id
        )

    async def get_pairs_by_address(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerGetPairsByAddressRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed trading pair information on a specific blockchain by pair address, including price, liquidity, volume, and base/quote token details. Use this when an agent needs current market data for known pairs. This is read-only public data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.get_pairs_by_address", owner_id, args, account_id=account_id
        )

    async def get_pairs_by_token(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerGetPairsByTokenRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves trading pairs for one or more token addresses on a specific blockchain (up to 30 comma-separated addresses). Returns consolidated market data for all pairs involving those tokens. Use this for batch lookups when analysing multiple tokens at once. This is read-only public data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.get_pairs_by_token", owner_id, args, account_id=account_id
        )

    async def get_token_boosts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerGetTokenBoostsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the most recently boosted tokens on DexScreener, showing which tokens have received paid promotion orders. Use this to identify tokens currently being marketed on the platform. This is public market data with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.get_token_boosts", owner_id, args, account_id=account_id
        )

    async def get_token_pools(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerGetTokenPoolsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all trading pools (liquidity pairs) that include a specific token on a given blockchain, with current market data for each pool. Use this when an agent needs to see where a token is traded and compare liquidity across different pairs. This is read-only public data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.get_token_pools", owner_id, args, account_id=account_id
        )

    async def get_token_profiles(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerGetTokenProfilesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the latest token profiles listed on DexScreener, providing metadata such as token name, symbol, chain, and social links. Use this when an agent needs to discover newly listed tokens or monitor recent additions across multiple chains. This is public market data with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.get_token_profiles", owner_id, args, account_id=account_id
        )

    async def get_top_token_boosts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerGetTopTokenBoostsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the tokens with the highest number of active boost orders on DexScreener, ranked by boost count. Use this when an agent needs to identify the most promoted tokens as a signal of market attention. This is public market data with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.get_top_token_boosts", owner_id, args, account_id=account_id
        )

    async def search_pairs(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DexscreenerSearchPairsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for trading pairs across all supported blockchains using a query string such as a token name, symbol, or pair address. Returns matching pairs with current price, liquidity, and volume data. Use this when an agent needs to find a pair by name rather than by a known address. This is read-only public data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dexscreener.search_pairs", owner_id, args, account_id=account_id
        )
