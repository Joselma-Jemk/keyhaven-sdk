# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `deel` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DeelContractCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    type_: str = Field(alias="type")
    country_code: str | None = None
    client: dict[str, Any] | None = None
    worker: dict[str, Any] | None = None
    scope_of_work: str | None = None
    job_title: str | None = None
    start_date: str | None = None
    end_date: str | None = None
    compensation_details: dict[str, Any] | None = None


class DeelContractCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DeelContractCreateBody


class DeelContractGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contract_id: str


class DeelContractGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DeelContractGetPath


class DeelContractsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    after_cursor: str | None = None
    types: str | None = None
    statuses: str | None = None


class DeelContractsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DeelContractsListQuery | None = None


class DeelPeopleListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    hiring_status: str | None = None
    hiring_type: str | None = None
    search: str | None = None


class DeelPeopleListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DeelPeopleListQuery | None = None


class DeelPersonGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    worker_id: str


class DeelPersonGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DeelPersonGetPath


class DeelTimeOffCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    worker_id: str
    policy_id: str | None = None
    start_date: str
    end_date: str
    is_half_day: bool | None = None
    with_pay: bool | None = None
    reason: str | None = None


class DeelTimeOffCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DeelTimeOffCreateBody


class DeelNamespace:
    """Typed accessor for the `deel` app. Reach it via `client.apps.deel`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contract_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DeelContractCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new formal contract between the company and a worker. The payload includes the contract type, title, parties, compensation details, and scope of work. This action produces a legally binding engagement that commits the organisation to payment and employment terms; it cannot be undone without a formal termination process. Use only when you have explicit approval to onboard a worker.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "deel.contract_create", owner_id, args, account_id=account_id
        )

    async def contract_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DeelContractGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full details of a single contract by its unique identifier, including terms, compensation, dates, and current status. Use this to review a contract's specifics before making decisions such as termination or amendment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "deel.contract_get", owner_id, args, account_id=account_id
        )

    async def contracts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DeelContractsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Return a paginated list of contracts in the organisation, filtered by type, status, or cursor-based pagination. Contracts represent the formal engagement between the company and a worker. Use this to browse active or past contracts before inspecting or creating new ones.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "deel.contracts_list", owner_id, args, account_id=account_id
        )

    async def people_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DeelPeopleListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Return a paginated list of workers (contractors and employees) registered in the Deel organisation. Supports filtering by hiring status, hiring type, and free-text search across name or email. Use this to browse or search the workforce directory before performing actions on individual people.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("deel.people_list", owner_id, args, account_id=account_id)

    async def person_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DeelPersonGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full profile of a single worker by their unique identifier. Returns personal details, role, hiring type, status, and related metadata. Use this to inspect a specific worker before acting on their contract or time-off records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("deel.person_get", owner_id, args, account_id=account_id)

    async def time_off_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DeelTimeOffCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submit a paid or unpaid time-off request for a worker under a specific policy. Records the absence in the system and may trigger a manager approval workflow depending on the policy configuration. Use when a worker needs to record planned leave; the request remains cancellable or modifiable until approved.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "deel.time_off_create", owner_id, args, account_id=account_id
        )
