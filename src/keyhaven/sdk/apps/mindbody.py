# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `mindbody` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MindbodyAppointmentAddBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ClientId: str
    LocationId: int
    StaffId: int
    SessionTypeId: int
    StartDateTime: str
    EndDateTime: str | None = None
    Notes: str | None = None


class MindbodyAppointmentAddRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MindbodyAppointmentAddBody


class MindbodyAppointmentUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    AppointmentId: int
    StartDateTime: str | None = None
    EndDateTime: str | None = None
    StaffId: int | None = None
    SessionTypeId: int | None = None
    Notes: str | None = None


class MindbodyAppointmentUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MindbodyAppointmentUpdateBody


class MindbodyClassAddClientBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ClientId: str
    ClassId: int
    LocationId: int | None = None
    Waitlist: bool | None = None
    CrossRegionalBooking: bool | None = None
    ClientServiceId: int | None = None


class MindbodyClassAddClientRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MindbodyClassAddClientBody


class MindbodyClientVisitUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    VisitId: int
    SignedIn: bool | None = None
    MakeUp: bool | None = None


class MindbodyClientVisitUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MindbodyClientVisitUpdateBody


class MindbodyNamespace:
    """Typed accessor for the `mindbody` app. Reach it via `client.apps.mindbody`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def appointment_add(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MindbodyAppointmentAddRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new appointment booking for a client in the MINDBODY system. The appointment is visible on both the client's and staff member's schedules within the business's calendar. This action is reversible — the appointment can later be updated or cancelled. Use when a client requests a new service booking at a specific time with a specific staff member.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mindbody.appointment_add", owner_id, args, account_id=account_id
        )

    async def appointment_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MindbodyAppointmentUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing appointment's time, assigned staff, session type, or notes. The updated information replaces the old values in the MINDBODY schedule, freeing the original time slot and showing the new one to both client and staff. This is reversible — another update can revert or change the appointment further. Use when a client needs to reschedule, change providers, or adjust the service type of an existing booking.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mindbody.appointment_update", owner_id, args, account_id=account_id
        )

    async def class_add_client(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MindbodyClassAddClientRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Enrolls a client into a group class session (or its waitlist) in the MINDBODY system. If the class has available spots, the client is added to the class roster and a spot is consumed; if the class is full and `Waitlist` is true, the client is placed on the waiting list. The studio's attendance records are updated immediately. This is reversible — a client can later be removed from the class. Use when a client wants to reserve a spot in a scheduled class.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mindbody.class_add_client", owner_id, args, account_id=account_id
        )

    async def client_visit_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MindbodyClientVisitUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies a client visit record — typically to mark the client as signed in (arrived) for an appointment or class, or to flag the visit as a make-up session. This updates attendance tracking within the MINDBODY system. The change is internal to the business's records and is reversible (the flags can be cleared with another update). Use when a client checks in at the front desk or when adjusting how a past visit is categorized.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mindbody.client_visit_update", owner_id, args, account_id=account_id
        )
