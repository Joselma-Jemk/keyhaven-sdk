# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `oura` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class OuraDailyActivityListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    start_date: str | None = None
    end_date: str | None = None
    next_token: str | None = None


class OuraDailyActivityListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OuraDailyActivityListQuery | None = None


class OuraDailyReadinessListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    start_date: str | None = None
    end_date: str | None = None
    next_token: str | None = None


class OuraDailyReadinessListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OuraDailyReadinessListQuery | None = None


class OuraDailySleepListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    start_date: str | None = None
    end_date: str | None = None
    next_token: str | None = None


class OuraDailySleepListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OuraDailySleepListQuery | None = None


class OuraHeartRateListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    start_datetime: str | None = None
    end_datetime: str | None = None
    next_token: str | None = None


class OuraHeartRateListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OuraHeartRateListQuery | None = None


class OuraSleepTimeListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    start_date: str | None = None
    end_date: str | None = None
    next_token: str | None = None


class OuraSleepTimeListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OuraSleepTimeListQuery | None = None


class OuraNamespace:
    """Typed accessor for the `oura` app. Reach it via `client.apps.oura`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def daily_activity_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OuraDailyActivityListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches daily activity summaries for the authenticated Oura user within a date range, including total calories burned, step count, active calories, inactivity periods, and an overall activity score. Use this to monitor physical activity levels, verify daily movement goals are being met, or correlate activity patterns with sleep quality and readiness data for a holistic health picture.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "oura.daily_activity_list", owner_id, args, account_id=account_id
        )

    async def daily_readiness_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OuraDailyReadinessListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches daily readiness scores for the authenticated Oura user within a date range, reflecting how prepared the body is for activity based on sleep quality, HRV, resting heart rate, and previous-day activity levels. A higher score indicates better recovery and readiness for physical exertion. Use this to guide training decisions, rest recommendations, or to correlate readiness with lifestyle and sleep patterns.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "oura.daily_readiness_list", owner_id, args, account_id=account_id
        )

    async def daily_sleep_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OuraDailySleepListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches daily sleep summaries for the authenticated Oura user within a date range, including total sleep duration, sleep efficiency, latency, and a composite sleep score. Each day's entry provides high-level sleep metrics derived from the ring's sensors. Use this to track sleep trends over time or to identify nights with poor sleep quality. Supports pagination via next_token for large date ranges.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "oura.daily_sleep_list", owner_id, args, account_id=account_id
        )

    async def heart_rate_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OuraHeartRateListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves heart rate time-series data in 5-minute intervals for the authenticated Oura user within a datetime range, using ISO 8601 format for start and end boundaries. Each data point includes the average heart rate for that interval. Use this for detailed cardiovascular analysis, detecting resting heart rate trends, or correlating heart rate fluctuations with activity, sleep stages, and readiness throughout the day.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "oura.heart_rate_list", owner_id, args, account_id=account_id
        )

    async def sleep_time_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OuraSleepTimeListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed sleep session data for the authenticated Oura user within a date range, including sleep stage durations (light, deep, REM), heart rate curves, HRV readings, and movement during each sleep session. Each entry represents one complete sleep session with its own timeline. Use this for in-depth sleep quality analysis beyond daily summaries, such as stage distribution, nighttime physiological metrics, and sleep disruption patterns.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "oura.sleep_time_list", owner_id, args, account_id=account_id
        )
