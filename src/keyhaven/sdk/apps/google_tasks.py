# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `google_tasks` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GoogleTasksTasklistGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tasklist: str


class GoogleTasksTasklistGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleTasksTasklistGetPath


class GoogleTasksTasklistListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    maxResults: int = 1000
    pageToken: str | None = None


class GoogleTasksTasklistListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleTasksTasklistListQuery | None = None


class GoogleTasksTasksDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tasklist: str
    task: str


class GoogleTasksTasksDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleTasksTasksDeletePath


class GoogleTasksTasksInsertPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tasklist: str


class GoogleTasksTasksInsertQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str | None = None
    previous: str | None = None


class GoogleTasksTasksInsertBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    notes: str | None = None
    status: Literal["needsAction", "completed"] | None = None
    due: str | None = None
    completed: str | None = None
    deleted: bool | None = None
    hidden: bool | None = None


class GoogleTasksTasksInsertRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleTasksTasksInsertPath
    query: GoogleTasksTasksInsertQuery | None = None
    body: GoogleTasksTasksInsertBody = Field(default_factory=GoogleTasksTasksInsertBody)


class GoogleTasksTasksListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tasklist: str


class GoogleTasksTasksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    completedMax: str | None = None
    completedMin: str | None = None
    dueMax: str | None = None
    dueMin: str | None = None
    maxResults: int = 20
    pageToken: str | None = None
    showCompleted: bool = True
    showDeleted: bool = False
    showHidden: bool = False
    updatedMin: str | None = None
    showAssigned: bool = False


class GoogleTasksTasksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleTasksTasksListPath
    query: GoogleTasksTasksListQuery | None = None


class GoogleTasksNamespace:
    """Typed accessor for the `google_tasks` app. Reach it via `client.apps.google_tasks`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def tasklist_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleTasksTasklistGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a specific task list by its ID, returning its title and metadata. Use this when the agent needs to verify a task list exists or fetch its details before performing task operations on it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_tasks.tasklist_get", owner_id, args, account_id=account_id
        )

    async def tasklist_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleTasksTasklistListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all task lists owned by the authenticated user, with optional pagination up to 1000 lists per page. Returns each list's ID, title, and update timestamp. Use this when the agent needs to discover available task lists or obtain a list ID for subsequent task operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_tasks.tasklist_list", owner_id, args, account_id=account_id
        )

    async def tasks_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleTasksTasksDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a task from the specified task list. For assigned tasks originating from Google Docs or Chat Spaces, both the assigned copy and the original task are deleted. This operation is irreversible. Use this when the agent needs to clean up completed, obsolete, or incorrectly created tasks.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_tasks.tasks_delete", owner_id, args, account_id=account_id
        )

    async def tasks_insert(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleTasksTasksInsertRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new task in the specified task list with a title, optional notes, due date, and position relative to siblings. The task immediately appears in the user's Google Tasks UI across all connected devices. Use this when the agent needs to add a to-do item programmatically, set a due date, or organize tasks under a parent.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_tasks.tasks_insert", owner_id, args, account_id=account_id
        )

    async def tasks_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleTasksTasksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all tasks in a given task list with extensive filtering options including completion date range, due date range, hidden/deleted status, and last modification time. Supports pagination up to 100 tasks per page. Use this when the agent needs to browse, search, or audit tasks within a specific list.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_tasks.tasks_list", owner_id, args, account_id=account_id
        )
