# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `pipedrive` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PipedriveDealCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    owner_id: int | None = None
    person_id: int | None = None
    org_id: int | None = None
    pipeline_id: int | None = None
    stage_id: int | None = None
    value: float | None = None
    currency: str | None = None
    status: str | None = None
    probability: float | None = None
    expected_close_date: str | None = None


class PipedriveDealCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: PipedriveDealCreateBody


class PipedriveDealGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class PipedriveDealGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PipedriveDealGetPath


class PipedriveDealsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter_id: int | None = None
    ids: str | None = None
    owner_id: int | None = None
    person_id: int | None = None
    org_id: int | None = None
    pipeline_id: int | None = None
    stage_id: int | None = None
    status: str | None = None
    updated_since: str | None = None
    updated_until: str | None = None
    sort_by: str | None = None
    sort_direction: str | None = None
    limit: int | None = None
    cursor: str | None = None


class PipedriveDealsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PipedriveDealsListQuery | None = None


class PipedriveItemSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    term: str
    item_types: str | None = None
    fields: str | None = None
    exact_match: bool | None = None
    search_for_related_items: bool | None = None
    include_fields: str | None = None
    limit: int | None = None
    cursor: str | None = None


class PipedriveItemSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PipedriveItemSearchQuery


class PipedrivePersonsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter_id: int | None = None
    ids: str | None = None
    owner_id: int | None = None
    org_id: int | None = None
    updated_since: str | None = None
    updated_until: str | None = None
    sort_by: str | None = None
    sort_direction: str | None = None
    limit: int | None = None
    cursor: str | None = None


class PipedrivePersonsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PipedrivePersonsListQuery | None = None


class PipedriveNamespace:
    """Typed accessor for the `pipedrive` app. Reach it via `client.apps.pipedrive`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def deal_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PipedriveDealCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new deal in the Pipedrive CRM with a required title and optional owner, person, organization, pipeline, stage, monetary value, currency, probability, status, and expected close date. The deal appears immediately in the specified pipeline and is visible to the assigned owner and their team. Deals can be moved, edited, or deleted later. Use this when the agent needs to log a new sales opportunity from a lead, email, or manual entry.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pipedrive.deal_create", owner_id, args, account_id=account_id
        )

    async def deal_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PipedriveDealGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full details of a single Pipedrive deal by its numeric ID, including title, value, currency, stage, pipeline, owner, linked person and organization, custom fields, and activity summaries. This is read-only. Use this when the agent needs a complete picture of a specific deal before updating it, moving it through the pipeline, or generating a report.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pipedrive.deal_get", owner_id, args, account_id=account_id
        )

    async def deals_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PipedriveDealsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search and paginate through deals in the Pipedrive CRM with filters by owner, person, organization, pipeline, stage, or status, as well as update-date ranges and user-defined filters. The returned deals include their title, value, stage, pipeline, expected close date, and owner. This is read-only and does not affect any data. Use this when the agent needs to find deals matching specific criteria for reporting, inspection, or bulk operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pipedrive.deals_list", owner_id, args, account_id=account_id
        )

    async def item_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PipedriveItemSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search across multiple Pipedrive item types — deals, persons, organizations, products, leads, files, mail attachments, and projects — using a free-text term. Results include matched items with their type, ID, and a preview of the matching field. Optionally return related leads and deals. This is read-only. Use this when the agent needs to quickly locate any record across the entire CRM without knowing which entity type it belongs to.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pipedrive.item_search", owner_id, args, account_id=account_id
        )

    async def persons_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PipedrivePersonsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search and paginate through person records (contacts) in the Pipedrive CRM with filters by owner, organization, user-defined filter, or update-date ranges. Each record includes name, email, phone, organization, and custom fields. This is read-only. Use this when the agent needs to find a contact by name, email, or criteria before linking them to a deal or updating their details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pipedrive.persons_list", owner_id, args, account_id=account_id
        )
