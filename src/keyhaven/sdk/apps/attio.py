# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `attio` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AttioListEntriesQueryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list: str


class AttioListEntriesQueryBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: dict[str, Any] | None = None
    sorts: list[Any] | None = None
    limit: int | None = None
    offset: int | None = None


class AttioListEntriesQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AttioListEntriesQueryPath
    body: AttioListEntriesQueryBody | None = None


class AttioListsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class AttioRecordCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    object: str


class AttioRecordCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any]


class AttioRecordCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AttioRecordCreatePath
    body: AttioRecordCreateBody


class AttioRecordGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    object: str
    record_id: str


class AttioRecordGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AttioRecordGetPath


class AttioRecordUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    object: str
    record_id: str


class AttioRecordUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any]


class AttioRecordUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AttioRecordUpdatePath
    body: AttioRecordUpdateBody


class AttioRecordsQueryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    object: str


class AttioRecordsQueryBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: dict[str, Any] | None = None
    sorts: list[Any] | None = None
    limit: int | None = None
    offset: int | None = None


class AttioRecordsQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AttioRecordsQueryPath
    body: AttioRecordsQueryBody | None = None


class AttioNamespace:
    """Typed accessor for the `attio` app. Reach it via `client.apps.attio`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def list_entries_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AttioListEntriesQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches the entries of a specific Attio list using optional filters, sort orders, and pagination parameters. Returns matching entries with their associated record data without modifying any data. Use this function to retrieve a targeted subset of list members for analysis, export to external systems, or as the basis for bulk operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "attio.list_entries_query", owner_id, args, account_id=account_id
        )

    async def lists_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AttioListsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all lists configured in the Attio workspace, which are groupings of records used for segmentation, campaigns, and CRM organisation. Each list entry includes its name, identifier, and metadata. Use this function to discover available lists before querying their entries or performing list-based operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("attio.lists_list", owner_id, args, account_id=account_id)

    async def record_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AttioRecordCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new record inside a specified object with the provided attribute values. The record appears immediately in the Attio CRM and is visible to all workspace members with appropriate access. Use this function to add new contacts, companies, deals, or custom object entries to the CRM from automated workflows or data imports.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "attio.record_create", owner_id, args, account_id=account_id
        )

    async def record_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AttioRecordGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single CRM record by its parent object type and record identifier, returning all attribute values and metadata. The record may contain contact details, deal information, or other business data depending on the object schema. Use this function when you need the full details of a known record for review or as input to another operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("attio.record_get", owner_id, args, account_id=account_id)

    async def record_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AttioRecordUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Performs a partial update (PATCH) on an existing CRM record, modifying only the specified attribute values while leaving others unchanged. Changes take effect immediately and propagate to all workspace views and reports. Use this function to correct data, advance deal stages, update contact information, or sync changes from external systems into Attio.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "attio.record_update", owner_id, args, account_id=account_id
        )

    async def records_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AttioRecordsQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches records of a specified Attio object (e.g. people, companies, deals) using optional attribute filters, sort orders, and pagination controls. Returns matching records with all their attribute values without modifying any data. Use this function to find and retrieve CRM records matching specific criteria for analysis, enrichment, or subsequent actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "attio.records_query", owner_id, args, account_id=account_id
        )
