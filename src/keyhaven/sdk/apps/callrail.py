# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `callrail` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CallrailCallsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class CallrailCallsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 100


class CallrailCallsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CallrailCallsListPath
    query: CallrailCallsListQuery | None = None


class CallrailCompaniesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class CallrailCompaniesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 100


class CallrailCompaniesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CallrailCompaniesListPath
    query: CallrailCompaniesListQuery | None = None


class CallrailFormSubmissionCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class CallrailFormSubmissionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    form_submission: dict[str, Any]


class CallrailFormSubmissionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CallrailFormSubmissionCreatePath
    body: CallrailFormSubmissionCreateBody


class CallrailFormSubmissionsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class CallrailFormSubmissionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 100
    fields: str | None = None


class CallrailFormSubmissionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CallrailFormSubmissionsListPath
    query: CallrailFormSubmissionsListQuery | None = None


class CallrailTagsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class CallrailTagsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 100


class CallrailTagsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CallrailTagsListPath
    query: CallrailTagsListQuery | None = None


class CallrailNamespace:
    """Typed accessor for the `callrail` app. Reach it via `client.apps.callrail`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def calls_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CallrailCallsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of calls for a CallRail account, including caller and callee details, duration, timestamps, and recording status. This is read-only and does not alter any call data. Use this to audit call activity, retrieve records for analysis, or find a specific call by browsing pages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "callrail.calls_list", owner_id, args, account_id=account_id
        )

    async def companies_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CallrailCompaniesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of companies associated with a CallRail account. Companies represent the businesses whose calls are being tracked. Use this to retrieve company metadata or to find a specific company ID for use in other API calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "callrail.companies_list", owner_id, args, account_id=account_id
        )

    async def form_submission_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CallrailFormSubmissionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new form submission record in CallRail with visitor data, referrer, landing page, and submitted form fields. This writes a new lead record into the account but does not send any external communication. Use this to manually log a web form submission that was not captured automatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "callrail.form_submission_create", owner_id, args, account_id=account_id
        )

    async def form_submissions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CallrailFormSubmissionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of web form submissions recorded on a CallRail account, including visitor data, referrer info, and submitted field values. Form submissions often contain personally identifiable information (PII) submitted by website visitors. Use this to review lead data or export submissions for analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "callrail.form_submissions_list", owner_id, args, account_id=account_id
        )

    async def tags_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CallrailTagsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of tags configured on a CallRail account. Tags are used to label and categorize calls and form submissions. Use this to discover available tag names before applying them to records or filtering reports.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "callrail.tags_list", owner_id, args, account_id=account_id
        )
