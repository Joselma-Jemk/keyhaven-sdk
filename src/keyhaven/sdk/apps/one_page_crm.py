# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `one_page_crm` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class OnePageCrmChangeAuthKeyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class OnePageCrmChangeAuthKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: OnePageCrmChangeAuthKeyHeader = Field(default_factory=OnePageCrmChangeAuthKeyHeader)


class OnePageCrmGetBootstrapHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class OnePageCrmGetBootstrapRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: OnePageCrmGetBootstrapHeader = Field(default_factory=OnePageCrmGetBootstrapHeader)


class OnePageCrmGetUserPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class OnePageCrmGetUserHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class OnePageCrmGetUserRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: OnePageCrmGetUserPath
    header: OnePageCrmGetUserHeader = Field(default_factory=OnePageCrmGetUserHeader)


class OnePageCrmListUsersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class OnePageCrmListUsersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: OnePageCrmListUsersHeader = Field(default_factory=OnePageCrmListUsersHeader)


class OnePageCrmUpdateUserPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class OnePageCrmUpdateUserHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class OnePageCrmUpdateUserBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str | None = None
    last_name: str | None = None
    company_name: str | None = None
    bcc_email: str | None = None


class OnePageCrmUpdateUserRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: OnePageCrmUpdateUserPath
    header: OnePageCrmUpdateUserHeader = Field(default_factory=OnePageCrmUpdateUserHeader)
    body: OnePageCrmUpdateUserBody = Field(default_factory=OnePageCrmUpdateUserBody)


class OnePageCrmNamespace:
    """Typed accessor for the `one_page_crm` app. Reach it via `client.apps.one_page_crm`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def change_auth_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OnePageCrmChangeAuthKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Invalidates the current API authentication key for the OnePageCRM account and immediately generates a new one. After this operation, all integrations and scripts using the old key will stop working until updated with the new key. Use this only when you need to rotate credentials due to a security concern or as part of a routine key renewal process.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "one_page_crm.change_auth_key", owner_id, args, account_id=account_id
        )

    async def get_bootstrap(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OnePageCrmGetBootstrapRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns bootstrap data about the authenticated OnePageCRM user's account, including user details, team information, pipeline stages, custom fields, and configuration settings needed to initialize the CRM client. This is typically the first call an integration makes to understand the account structure. Use this to load initial account context before performing other CRM operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "one_page_crm.get_bootstrap", owner_id, args, account_id=account_id
        )

    async def get_user(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OnePageCrmGetUserRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns detailed information about a specific OnePageCRM user identified by their user ID, including their full name, email, role, and account settings. Use this to look up individual user details before performing user-specific actions like updates, or to verify a user's current role and permissions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "one_page_crm.get_user", owner_id, args, account_id=account_id
        )

    async def list_users(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OnePageCrmListUsersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of all users associated with the authenticated OnePageCRM account, including their names, roles, email addresses, and account status. This provides a directory of team members who have access to the CRM. Use this to discover active users, audit team composition, or get user IDs needed for user-specific operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "one_page_crm.list_users", owner_id, args, account_id=account_id
        )

    async def update_user(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OnePageCrmUpdateUserRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates a specific OnePageCRM user's profile information including first name, last name, company name, and BCC email setting. Changes take effect immediately across the CRM and affect how the user appears in the team directory and routed emails. Use this to correct user details, update company affiliations, or change email routing preferences without creating a new user record.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "one_page_crm.update_user", owner_id, args, account_id=account_id
        )
