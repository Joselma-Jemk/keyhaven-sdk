# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `recharge` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class RechargeCustomerCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    X_Recharge_Version: str = Field(default="2021-11", alias="X-Recharge-Version")


class RechargeCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str
    first_name: str
    last_name: str
    phone: str | None = None
    external_customer_id: dict[str, Any] | None = None


class RechargeCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RechargeCustomerCreateHeader | None = None
    body: RechargeCustomerCreateBody


class RechargeCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    updated_at_min: str | None = None
    limit: int = 50
    cursor: str | None = None


class RechargeCustomersListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Recharge_Version: str = Field(default="2021-11", alias="X-Recharge-Version")


class RechargeCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RechargeCustomersListQuery | None = None
    header: RechargeCustomersListHeader | None = None


class RechargeSubscriptionCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    X_Recharge_Version: str = Field(default="2021-11", alias="X-Recharge-Version")


class RechargeSubscriptionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address_id: int
    next_charge_scheduled_at: str
    charge_interval_frequency: int
    order_interval_frequency: int
    order_interval_unit: Literal["day", "week", "month"]
    quantity: int
    shopify_variant_id: int | None = None
    external_variant_id: dict[str, Any] | None = None
    product_title: str | None = None
    price: float | None = None
    properties: list[Any] | None = None


class RechargeSubscriptionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RechargeSubscriptionCreateHeader | None = None
    body: RechargeSubscriptionCreateBody


class RechargeSubscriptionGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    subscription_id: str


class RechargeSubscriptionGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Recharge_Version: str = Field(default="2021-11", alias="X-Recharge-Version")


class RechargeSubscriptionGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RechargeSubscriptionGetPath
    header: RechargeSubscriptionGetHeader | None = None


class RechargeSubscriptionUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    subscription_id: str


class RechargeSubscriptionUpdateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    X_Recharge_Version: str = Field(default="2021-11", alias="X-Recharge-Version")


class RechargeSubscriptionUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    quantity: int | None = None
    order_interval_frequency: int | None = None
    order_interval_unit: Literal["day", "week", "month"] | None = None
    charge_interval_frequency: int | None = None
    price: float | None = None
    use_shopify_variant_defaults: bool | None = None
    commit_update: bool | None = None


class RechargeSubscriptionUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RechargeSubscriptionUpdatePath
    header: RechargeSubscriptionUpdateHeader | None = None
    body: RechargeSubscriptionUpdateBody = Field(default_factory=RechargeSubscriptionUpdateBody)


class RechargeSubscriptionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer_id: str | None = None
    address_id: str | None = None
    status: Literal["active", "cancelled", "expired"] | None = None
    updated_at_min: str | None = None
    limit: int = 50
    cursor: str | None = None


class RechargeSubscriptionsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Recharge_Version: str = Field(default="2021-11", alias="X-Recharge-Version")


class RechargeSubscriptionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RechargeSubscriptionsListQuery | None = None
    header: RechargeSubscriptionsListHeader | None = None


class RechargeNamespace:
    """Typed accessor for the `recharge` app. Reach it via `client.apps.recharge`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RechargeCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer record in the Recharge store with their email, name, phone, and optional external platform identifier. The customer is immediately available for subscription creation and order management. Use this when you need to register a new customer from a signup form, import customers from another platform, or create a customer record before setting up their first subscription.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recharge.customer_create", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RechargeCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of customers on the Recharge store, with optional filtering by email and last-update time. Supports cursor-based pagination. Use this when you need to find a customer ID, browse the customer base, or locate a customer for subscription management or support actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recharge.customers_list", owner_id, args, account_id=account_id
        )

    async def subscription_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RechargeSubscriptionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new recurring subscription on an existing customer address, defining the product variant, quantity, frequency, and next charge date. Once created, the subscription will automatically generate orders and charges on the schedule set. Use this when you need to enroll a customer in a subscription program, migrate from a one-time purchase model, or set up a recurring delivery for a product.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recharge.subscription_create", owner_id, args, account_id=account_id
        )

    async def subscription_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RechargeSubscriptionGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single subscription record by its unique identifier, returning all details including customer, address, product variant, frequency, quantity, price, and next charge date. No data is modified — this is a read-only lookup. Use this when you need the full details of a subscription for customer support, before updating it, or to verify recurring billing settings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recharge.subscription_get", owner_id, args, account_id=account_id
        )

    async def subscription_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RechargeSubscriptionUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more fields of an existing subscription — such as quantity, order frequency, charge frequency, or unit price — and regenerates upcoming charges accordingly. Changes affect the customer's next billing cycle. Use this when you need to adjust a customer's subscription after a plan change, upgrade or downgrade their product quantity, or modify pricing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recharge.subscription_update", owner_id, args, account_id=account_id
        )

    async def subscriptions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RechargeSubscriptionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of subscriptions from the Recharge store, with optional filtering by customer, address, status, and update time. Supports cursor-based pagination. Use this when you need to browse active subscriptions, find subscriptions expiring soon, or look up a subscription ID for modification or cancellation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recharge.subscriptions_list", owner_id, args, account_id=account_id
        )
