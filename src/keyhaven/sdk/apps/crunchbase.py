# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `crunchbase` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CrunchbaseCompanyFinancialsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company: str


class CrunchbaseCompanyFinancialsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CrunchbaseCompanyFinancialsQuery


class CrunchbaseCompanySearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    category: str | None = None
    funding_stage: str | None = None
    per_page: int = 10


class CrunchbaseCompanySearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CrunchbaseCompanySearchQuery | None = None


class CrunchbaseFundingRoundSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    funded_organization: str | None = None
    investor: str | None = None
    lead_investor: str | None = None
    investment_type: str | None = None
    announced_on_after: str | None = None
    announced_on_before: str | None = None
    min_money_raised: int | None = None
    max_money_raised: int | None = None
    per_page: int = 5


class CrunchbaseFundingRoundSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CrunchbaseFundingRoundSearchQuery | None = None


class CrunchbaseInvestmentSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company: str | None = None
    investor: str | None = None
    is_lead: bool | None = None
    investment_type: str | None = None
    per_page: int = 5


class CrunchbaseInvestmentSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CrunchbaseInvestmentSearchQuery | None = None


class CrunchbasePersonSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    q: str | None = None
    company: str | None = None
    is_investor: str | None = None
    job_title_contains: str | None = None
    location: str | None = None
    per_page: int = 5
    page: int = 1


class CrunchbasePersonSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CrunchbasePersonSearchQuery | None = None


class CrunchbaseNamespace:
    """Typed accessor for the `crunchbase` app. Reach it via `client.apps.crunchbase`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def company_financials(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CrunchbaseCompanyFinancialsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed financial history for a specific company, including funding rounds, investments, acquisitions, and IPO data. Returns structured records with amounts, dates, valuation, and investor participation. Use when an agent needs to analyze a company's fundraising trajectory, funding milestones, or financial background.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "crunchbase.company_financials", owner_id, args, account_id=account_id
        )

    async def company_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CrunchbaseCompanySearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches Crunchbase for companies by industry category, funding stage, and pagination settings. Returns matching companies with their Crunchbase profiles including funding status, location, and description. Use when an agent needs to discover companies for market research, lead generation, or competitive analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "crunchbase.company_search", owner_id, args, account_id=account_id
        )

    async def funding_round_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CrunchbaseFundingRoundSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for funding rounds by investment type (seed, series A, series B, etc.), date range, amount range, or participating investors. Returns round details including amount raised, announcement date, investor composition, and lead investor. Use when an agent needs to find recent funding activity or track investment patterns in the market.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "crunchbase.funding_round_search", owner_id, args, account_id=account_id
        )

    async def investment_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CrunchbaseInvestmentSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches individual investment records by company or investor, with optional filtering by lead investor status and investment type. Returns detailed investment records with amounts, dates, and participant roles. Use when an agent needs to trace which investors backed which companies or analyze investment patterns and relationships.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "crunchbase.investment_search", owner_id, args, account_id=account_id
        )

    async def person_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CrunchbasePersonSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for notable individuals in Crunchbase such as founders, executives, and investors by name, company affiliation, job title, location, or investor status. Returns professional profiles with current and past roles, locations, and affiliations. Use when an agent needs to research people for networking, hiring, partnership scouting, or due diligence.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "crunchbase.person_search", owner_id, args, account_id=account_id
        )
