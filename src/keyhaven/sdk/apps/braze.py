# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `braze` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BrazeCampaignsListListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: str | None = None
    include_archived: str | None = None
    sort_direction: str | None = None
    last_edit_time_gt_: str | None = Field(default=None, alias="last_edit.time[gt]")


class BrazeCampaignsListListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: BrazeCampaignsListListQuery | None = None


class BrazeCanvasDataSeriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    canvas_id: str | None = None
    ending_at: str | None = None
    starting_at: str | None = None
    length: str | None = None
    include_variant_breakdown: str | None = None
    include_step_breakdown: str | None = None
    include_deleted_step_data: str | None = None


class BrazeCanvasDataSeriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: BrazeCanvasDataSeriesListQuery | None = None


class BrazeCanvasDetailsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    canvas_id: str | None = None


class BrazeCanvasDetailsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: BrazeCanvasDetailsListQuery | None = None


class BrazeCanvasListListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: str | None = None
    include_archived: str | None = None
    sort_direction: str | None = None
    last_edit_time_gt_: str | None = Field(default=None, alias="last_edit.time[gt]")


class BrazeCanvasListListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: BrazeCanvasListListQuery | None = None


class BrazeDauDataSeriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    length: str | None = None
    ending_at: str | None = None
    app_id: str | None = None


class BrazeDauDataSeriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: BrazeDauDataSeriesListQuery | None = None


class BrazeNamespace:
    """Typed accessor for the `braze` app. Reach it via `client.apps.braze`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def campaigns_list_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrazeCampaignsListListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of Braze Campaigns (targeted one-shot messaging campaigns). Supports filtering by archival status, sort direction, and last edit time. Use this to browse active campaigns, verify campaign existence, or select a campaign ID for further analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braze.campaigns_list_list", owner_id, args, account_id=account_id
        )

    async def canvas_data_series_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrazeCanvasDataSeriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves time-series analytics data for a specific Braze Canvas over a date range, with optional variant and step breakdowns. Can include data from deleted steps. Use this to measure Canvas performance, compare A/B variants, and generate engagement reports.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braze.canvas_data_series_list", owner_id, args, account_id=account_id
        )

    async def canvas_details_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrazeCanvasDetailsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full metadata for a single Braze Canvas, including its name, tags, steps, and variant configuration, identified by its Canvas ID. This is read-only and does not modify any Canvas. Use this after listing Canvases to inspect a specific Canvas before making edits or analyzing its performance.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braze.canvas_details_list", owner_id, args, account_id=account_id
        )

    async def canvas_list_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrazeCanvasListListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of Braze Canvases (multi-step user engagement campaigns). Supports filtering by archival status, sort direction, and last edit time. Use this to discover existing Canvases or to locate a specific Canvas before inspecting its details or analytics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braze.canvas_list_list", owner_id, args, account_id=account_id
        )

    async def dau_data_series_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrazeDauDataSeriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves daily active user (DAU) metrics over a specified date range, with optional filtering by app ID. This read-only analytics query returns time-series data for monitoring user engagement trends. Use this to populate dashboards, track growth, or diagnose engagement drops.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braze.dau_data_series_list", owner_id, args, account_id=account_id
        )
