# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `cloudbeds` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CloudbedsGuestGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    guestId: str


class CloudbedsGuestGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    includeGuestRequirements: bool | None = None


class CloudbedsGuestGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloudbedsGuestGetPath
    query: CloudbedsGuestGetQuery | None = None


class CloudbedsGuestsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reservationID: str | None = None
    guestID: str | None = None


class CloudbedsGuestsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CloudbedsGuestsListQuery | None = None


class CloudbedsReservationCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    startDate: str
    endDate: str
    guestFirstName: str
    guestLastName: str
    guestEmail: str
    guestCountry: str
    guestZip: str
    guestPhone: str | None = None
    rooms: list[Any] | None = None


class CloudbedsReservationCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CloudbedsReservationCreateBody


class CloudbedsReservationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reservationStatus: str | None = None
    limit: int = 50
    offset: int = 0
    includeGuestsDetails: bool | None = None
    includeCustomFields: bool | None = None


class CloudbedsReservationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CloudbedsReservationsListQuery | None = None


class CloudbedsNamespace:
    """Typed accessor for the `cloudbeds` app. Reach it via `client.apps.cloudbeds`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def guest_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudbedsGuestGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches comprehensive details for a single guest identified by their unique guest ID, including personal information, contact details, and optionally guest requirements data. Returns the full guest profile stored in the PMS. An agent should use this when it needs the complete guest context for lookups, verification, or personalization.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudbeds.guest_get", owner_id, args, account_id=account_id
        )

    async def guests_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudbedsGuestsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of guest records from the Cloudbeds PMS, optionally filtered by reservation ID or guest ID. Returns basic guest information such as names and contact details for matching records. An agent should use this to find guests in the system or verify guest identities against existing data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudbeds.guests_list", owner_id, args, account_id=account_id
        )

    async def reservation_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudbedsReservationCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new booking reservation in the Cloudbeds PMS with guest details, dates, and room selection. This commits the property to hold inventory for the guest's stay, making those rooms unavailable to other potential guests during that period. An agent should use this when a confirmed booking needs to be entered into the system, typically after payment or a booking request has been approved.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudbeds.reservation_create", owner_id, args, account_id=account_id
        )

    async def reservations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudbedsReservationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of reservations from the Cloudbeds property management system, with optional filters for status, guest details, and custom fields. The response includes booking dates, room assignments, and optional guest information. An agent should use this to search or browse reservations when it needs to find specific bookings or audit the current reservation state.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudbeds.reservations_list", owner_id, args, account_id=account_id
        )
