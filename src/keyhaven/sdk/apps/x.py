# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `x` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class XGetListByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetListByIdQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list_fields: str = Field(
        default="id,name,description,member_count,private,owner_id", alias="list.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "owner_id"


class XGetListByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetListByIdPath
    query: XGetListByIdQuery | None = None


class XGetListMembersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetListMembersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int = 100
    user_fields: str = Field(
        default="id,name,username,description,public_metrics", alias="user.fields"
    )


class XGetListMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetListMembersPath
    query: XGetListMembersQuery | None = None


class XGetListPostsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetListPostsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int = 100
    tweet_fields: str = Field(
        default="id,text,author_id,created_at,public_metrics", alias="tweet.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "author_id"


class XGetListPostsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetListPostsPath
    query: XGetListPostsQuery | None = None


class XGetPostByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetPostByIdQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tweet_fields: str = Field(
        default="id,text,author_id,created_at,public_metrics,context_annotations",
        alias="tweet.fields",
    )
    user_fields: str = Field(default="id,name,username,verified", alias="user.fields")
    expansions: str = "author_id"


class XGetPostByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetPostByIdPath
    query: XGetPostByIdQuery | None = None


class XGetPostRetweetsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetPostRetweetsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int = 100
    user_fields: str = Field(default="id,name,username,public_metrics", alias="user.fields")


class XGetPostRetweetsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetPostRetweetsPath
    query: XGetPostRetweetsQuery | None = None


class XGetPostsByIdsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: str
    tweet_fields: str = Field(
        default="id,text,author_id,created_at,public_metrics", alias="tweet.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "author_id"


class XGetPostsByIdsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: XGetPostsByIdsQuery


class XGetSpaceByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetSpaceByIdQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    space_fields: str = Field(
        default="id,state,created_at,title,host_ids,speaker_ids,participant_count",
        alias="space.fields",
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "host_ids,speaker_ids"


class XGetSpaceByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetSpaceByIdPath
    query: XGetSpaceByIdQuery | None = None


class XGetSpacesByCreatorIdsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_ids: str
    space_fields: str = Field(
        default="id,state,created_at,title,host_ids,participant_count", alias="space.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "host_ids,speaker_ids"


class XGetSpacesByCreatorIdsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: XGetSpacesByCreatorIdsQuery


class XGetSpacesByIdsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: str
    space_fields: str = Field(
        default="id,state,created_at,title,host_ids,participant_count", alias="space.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "host_ids,speaker_ids"


class XGetSpacesByIdsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: XGetSpacesByIdsQuery


class XGetUserByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetUserByIdQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_fields: str = Field(
        default="id,name,username,description,public_metrics,verified,created_at",
        alias="user.fields",
    )


class XGetUserByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetUserByIdPath
    query: XGetUserByIdQuery | None = None


class XGetUserByUsernamePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    username: str


class XGetUserByUsernameQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_fields: str = Field(
        default="id,name,username,description,public_metrics,verified,created_at",
        alias="user.fields",
    )


class XGetUserByUsernameRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetUserByUsernamePath
    query: XGetUserByUsernameQuery | None = None


class XGetUserFollowedListsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetUserFollowedListsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int = 100
    list_fields: str = Field(
        default="id,name,description,member_count,private,owner_id", alias="list.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "owner_id"


class XGetUserFollowedListsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetUserFollowedListsPath
    query: XGetUserFollowedListsQuery | None = None


class XGetUserListMembershipsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetUserListMembershipsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int = 100
    list_fields: str = Field(
        default="id,name,description,member_count,private,owner_id", alias="list.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "owner_id"


class XGetUserListMembershipsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetUserListMembershipsPath
    query: XGetUserListMembershipsQuery | None = None


class XGetUserOwnedListsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetUserOwnedListsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int = 100
    list_fields: str = Field(
        default="id,name,description,member_count,private", alias="list.fields"
    )


class XGetUserOwnedListsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetUserOwnedListsPath
    query: XGetUserOwnedListsQuery | None = None


class XGetUserPostsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class XGetUserPostsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int = 10
    tweet_fields: str = Field(
        default="id,text,created_at,public_metrics,context_annotations", alias="tweet.fields"
    )


class XGetUserPostsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: XGetUserPostsPath
    query: XGetUserPostsQuery | None = None


class XGetUsersByIdsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: str
    user_fields: str = Field(
        default="id,name,username,description,public_metrics,verified", alias="user.fields"
    )


class XGetUsersByIdsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: XGetUsersByIdsQuery


class XGetUsersByUsernamesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    usernames: str
    user_fields: str = Field(
        default="id,name,username,description,public_metrics,verified", alias="user.fields"
    )


class XGetUsersByUsernamesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: XGetUsersByUsernamesQuery


class XPublishPostHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class XPublishPostBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    text: str | None = None
    card_uri: str | None = None
    community_id: str | None = None
    direct_message_deep_link: str | None = None
    for_super_followers_only: bool = False
    geo: dict[str, Any] | None = None
    media: dict[str, Any] | None = None
    nullcast: bool = False
    poll: dict[str, Any] | None = None
    quote_tweet_id: str | None = None
    reply: dict[str, Any] | None = None
    reply_settings: Literal["following", "mentionedUsers", "subscribers", "verified"] | None = None
    share_with_followers: bool = False


class XPublishPostRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: XPublishPostHeader = Field(default_factory=XPublishPostHeader)
    body: XPublishPostBody = Field(default_factory=XPublishPostBody)


class XSearchPostsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    max_results: int = 10
    tweet_fields: str = Field(
        default="id,text,author_id,created_at,public_metrics", alias="tweet.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "author_id"


class XSearchPostsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: XSearchPostsQuery


class XSearchSpacesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    max_results: int = 10
    space_fields: str = Field(
        default="id,state,created_at,title,host_ids,participant_count", alias="space.fields"
    )
    user_fields: str = Field(default="id,name,username", alias="user.fields")
    expansions: str = "host_ids,speaker_ids"


class XSearchSpacesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: XSearchSpacesQuery


class XNamespace:
    """Typed accessor for the `x` app. Reach it via `client.apps.x`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_list_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetListByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed metadata about a specific X List by its numeric ID, including the list name, description, member count, privacy setting (public/private), and owner information. This is read-only and does not modify the list or create notifications. Use this when an agent needs to inspect list configuration, verify a list's privacy status, or retrieve the owner ID for subsequent list membership queries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.get_list_by_id", owner_id, args, account_id=account_id)

    async def get_list_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetListMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the members of a specific X List, identified by the list's numeric ID, returning user profile information (display name, username, description, public metrics) for up to 100 members. This is read-only and does not create any notifications. Use this when an agent needs to audit who is included in a list, verify that a specific user is a member, or analyse the composition of a curated group.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_list_members", owner_id, args, account_id=account_id
        )

    async def get_list_posts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetListPostsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves recent posts from an X List's curated timeline, showing content from all members of that list. Returns up to 100 posts with author data, engagement metrics, and timestamps. This is read-only and does not create any interactions. Use this when an agent needs to monitor a curated list of accounts for a specific topic, get a combined feed from relevant sources, or analyse conversation within a defined group of users.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.get_list_posts", owner_id, args, account_id=account_id)

    async def get_post_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetPostByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single X post (tweet) by its unique numeric ID, returning its text content, author, creation time, public engagement metrics (likes, retweets, replies, quotes), and context annotations. This is a read-only operation that does not create any notifications or alter any data. Use this when an agent needs to inspect a specific post's content and metrics, verify a post exists, or retrieve the author ID for follow-up queries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.get_post_by_id", owner_id, args, account_id=account_id)

    async def get_post_retweets(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetPostRetweetsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the list of users who have retweeted a specific X post, up to 100 users. Returns user profile information including display name, username, and public metrics. This is read-only and does not create any notifications. Use this when an agent needs to measure post amplification, identify who is sharing a particular piece of content, or analyse the reach of a post within the community.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_post_retweets", owner_id, args, account_id=account_id
        )

    async def get_posts_by_ids(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetPostsByIdsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves up to 100 X posts in a single request by providing a comma-separated list of numeric IDs. Returns the same detailed information as the single post lookup, including text, author, metrics, and context annotations. This is read-only and does not interact with any user timelines. Use this when an agent needs to batch-inspect multiple posts for analysis, comparison, or moderation workflows without making individual API calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_posts_by_ids", owner_id, args, account_id=account_id
        )

    async def get_space_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetSpaceByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific X Space (live audio conversation) by its unique ID, including its title, state (live, scheduled, ended), creator, speakers, participant count, and scheduled time. This is read-only and does not join the Space or create notifications. Use this when an agent needs to check whether a Space is currently live, get details about an upcoming Space, or analyse past Space participation metrics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_space_by_id", owner_id, args, account_id=account_id
        )

    async def get_spaces_by_creator_ids(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetSpacesByCreatorIdsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves Spaces created by specific X users, identified by their numeric user IDs (up to 100 creators). Returns each Space's title, state, host/speaker IDs, and participant counts. This is read-only and does not create notifications. Use this when an agent needs to find all Spaces hosted by a particular user or set of users, monitor when a specific creator goes live, or analyse an individual's Space hosting activity.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_spaces_by_creator_ids", owner_id, args, account_id=account_id
        )

    async def get_spaces_by_ids(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetSpacesByIdsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves up to 100 X Spaces in a single request by providing a comma-separated list of Space IDs. Returns each Space's title, state, host and speaker IDs, and participant counts. This is read-only and does not alter any state. Use this when an agent needs to batch-check the status of multiple Spaces simultaneously, monitor several scheduled events, or collect data on active audio conversations for analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_spaces_by_ids", owner_id, args, account_id=account_id
        )

    async def get_user_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUserByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed profile information for an X user by their numeric user ID, including display name, username, description, public metrics (followers, following, post count), verification status, and account creation date. This is read-only and does not create any notifications. Use this when an agent needs to look up a user's public profile, verify their identity, or gather follower/following metrics for analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.get_user_by_id", owner_id, args, account_id=account_id)

    async def get_user_by_username(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUserByUsernameRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Looks up an X user by their handle (username without the @ symbol), returning detailed profile information including display name, description, public metrics, verification status, and account creation date. This is read-only and does not create any notifications. Use this when an agent has a human-readable handle (e.g., 'elonmusk') and needs the numeric user ID for subsequent API calls, or wants to verify that a username exists before attempting interactions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_user_by_username", owner_id, args, account_id=account_id
        )

    async def get_user_followed_lists(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUserFollowedListsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all X Lists that a specific user follows (but may not own or be a member of), identified by their numeric user ID. Returns list metadata including name, description, member count, privacy status, and owner information for up to 100 lists. This is read-only and does not create any notifications. Use this when an agent needs to understand which lists a user actively monitors, differentiate between owned and followed lists, or analyse content consumption patterns.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_user_followed_lists", owner_id, args, account_id=account_id
        )

    async def get_user_list_memberships(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUserListMembershipsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all X Lists that a specific user is a member of, identified by their numeric user ID. Returns list metadata including name, description, member count, privacy status, and owner information for up to 100 lists. This is read-only and does not create any notifications. Use this when an agent needs to understand which curated groups a user belongs to, discover communities relevant to a user, or analyse a user's content ecosystems.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_user_list_memberships", owner_id, args, account_id=account_id
        )

    async def get_user_owned_lists(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUserOwnedListsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all X Lists owned by a specific user, identified by their numeric user ID. Returns list metadata including name, description, member count, and privacy status for up to 100 lists. This is read-only and does not create any notifications. Use this when an agent needs to discover which lists a user has created, find list IDs for subsequent timeline or member queries, or audit list ownership.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_user_owned_lists", owner_id, args, account_id=account_id
        )

    async def get_user_posts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUserPostsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves recent posts from a specific X user's timeline (up to 100 posts), identified by their numeric user ID. Returns each post's content, creation time, public metrics, and context annotations. This is read-only and does not affect the user's timeline or create any notifications. Use this when an agent needs to review a user's recent activity, analyse their posting patterns, or monitor content from a specific account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.get_user_posts", owner_id, args, account_id=account_id)

    async def get_users_by_ids(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUsersByIdsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves up to 100 X user profiles in a single request by providing a comma-separated list of numeric user IDs. Returns display name, username, description, public metrics, and verification status for each user. This is read-only and does not create any notifications. Use this when an agent needs to batch-lookup multiple users for audience analysis, follower list verification, or research without making individual API calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_users_by_ids", owner_id, args, account_id=account_id
        )

    async def get_users_by_usernames(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XGetUsersByUsernamesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves up to 100 X user profiles by their handles (usernames without @ symbol), returning display name, username, description, public metrics, and verification status for each. This is read-only and does not create any notifications. Use this when an agent needs to resolve a list of human-readable handles to numeric user IDs, or batch-lookup multiple accounts for research without individual API calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "x.get_users_by_usernames", owner_id, args, account_id=account_id
        )

    async def publish_post(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XPublishPostRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates and publishes a new post (tweet) on X.com. Supports text content, polls (2-4 options, 5 min to 7 days), media attachments, quote tweets, replies, community posts, geo-tagging, and reply permission settings. The post appears on the author's timeline and in the timelines of followers. This action is permanent and publicly visible — deletion is possible but retractions cannot be guaranteed once content is distributed. Use this when an agent needs to share content, respond to a conversation, conduct a poll, or engage with the X community on behalf of a user.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.publish_post", owner_id, args, account_id=account_id)

    async def search_posts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XSearchPostsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for X posts published within the last 7 days using a query string that supports boolean operators, hashtags, mentions, and advanced search syntax. Returns up to 100 matching posts with author data, engagement metrics, and timestamps. This is read-only and does not create any posts or notifications. Use this when an agent needs to find recent public conversation about a topic, monitor brand mentions, track hashtag usage, or discover relevant content for analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.search_posts", owner_id, args, account_id=account_id)

    async def search_spaces(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: XSearchSpacesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for live or scheduled X Spaces matching a query string, returning up to 100 matching Spaces with their title, state (live/scheduled), creator, speakers, and participant counts. This is read-only and does not interact with any Spaces. Use this when an agent needs to discover active or upcoming audio conversations about a specific topic, find Spaces related to a keyword, or monitor the live audio landscape for brand mentions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("x.search_spaces", owner_id, args, account_id=account_id)
