# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `gumroad` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GumroadOfferCodeCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    product_id: str


class GumroadOfferCodeCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    amount_cents: int | None = None
    amount_percentage: int | None = None
    max_purchase_count: int | None = None
    universal: bool | None = None


class GumroadOfferCodeCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GumroadOfferCodeCreatePath
    body: GumroadOfferCodeCreateBody


class GumroadOfferCodesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    product_id: str


class GumroadOfferCodesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GumroadOfferCodesListPath


class GumroadProductGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GumroadProductGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GumroadProductGetPath


class GumroadProductUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GumroadProductUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None
    price: int | None = None
    custom_permalink: str | None = None
    custom_summary: str | None = None
    tags: list[Any] | None = None


class GumroadProductUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GumroadProductUpdatePath
    body: GumroadProductUpdateBody | None = None


class GumroadProductsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    before: str | None = None
    after: str | None = None


class GumroadProductsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GumroadProductsListQuery | None = None


class GumroadSalesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    before: str | None = None
    after: str | None = None
    product_id: str | None = None
    email: str | None = None


class GumroadSalesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GumroadSalesListQuery | None = None


class GumroadNamespace:
    """Typed accessor for the `gumroad` app. Reach it via `client.apps.gumroad`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def offer_code_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GumroadOfferCodeCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new discount offer code for a specific Gumroad product, accepting either a fixed amount in cents or a percentage-based discount. The code becomes immediately usable by customers during checkout. Use this to launch a promotion or a limited-time sale.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gumroad.offer_code_create", owner_id, args, account_id=account_id
        )

    async def offer_codes_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GumroadOfferCodesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all offer codes (discount coupons) configured for a specific product, showing their names, discount amounts, usage limits, and activation status. Use this to review active promotions before creating or modifying discount codes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gumroad.offer_codes_list", owner_id, args, account_id=account_id
        )

    async def product_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GumroadProductGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a single digital product identified by its external ID or unique permalink. The response includes pricing, description, sales data, and product settings. Use this when you need full product details for a specific item.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gumroad.product_get", owner_id, args, account_id=account_id
        )

    async def product_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GumroadProductUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the metadata and configuration of an existing Gumroad product including its name, description, price (in cents), custom permalink, summary, and tags. The changes take immediate effect on the product listing and storefront. Use this to edit a product's details after creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gumroad.product_update", owner_id, args, account_id=account_id
        )

    async def products_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GumroadProductsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of digital products belonging to the authenticated user. Results can be filtered using cursor-based or page-based pagination. Use this to browse or discover which products are available in your Gumroad catalog.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gumroad.products_list", owner_id, args, account_id=account_id
        )

    async def sales_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GumroadSalesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all sales transactions for the authenticated user, with optional filtering by product ID or customer email. This reveals revenue data, customer information, and purchase history. Use this to audit or analyze your sales performance.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gumroad.sales_list", owner_id, args, account_id=account_id
        )
