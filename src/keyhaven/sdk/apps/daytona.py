# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `daytona` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DaytonaCreateWorkspaceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Daytona_Organization_ID: str | None = Field(default=None, alias="X-Daytona-Organization-ID")


class DaytonaCreateWorkspaceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    imageId: str
    size: str | None = None
    labels: dict[str, Any] | None = None
    autoStop: int | None = None


class DaytonaCreateWorkspaceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: DaytonaCreateWorkspaceHeader | None = None
    body: DaytonaCreateWorkspaceBody


class DaytonaDeleteWorkspacePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspaceId: str


class DaytonaDeleteWorkspaceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    force: bool


class DaytonaDeleteWorkspaceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Daytona_Organization_ID: str | None = Field(default=None, alias="X-Daytona-Organization-ID")


class DaytonaDeleteWorkspaceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DaytonaDeleteWorkspacePath
    query: DaytonaDeleteWorkspaceQuery
    header: DaytonaDeleteWorkspaceHeader | None = None


class DaytonaExecuteCommandPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspaceId: str


class DaytonaExecuteCommandHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Daytona_Organization_ID: str | None = Field(default=None, alias="X-Daytona-Organization-ID")


class DaytonaExecuteCommandBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    command: str
    cwd: str | None = None
    env: dict[str, Any] | None = None
    timeout: int | None = None


class DaytonaExecuteCommandRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DaytonaExecuteCommandPath
    header: DaytonaExecuteCommandHeader | None = None
    body: DaytonaExecuteCommandBody


class DaytonaGetWorkspacePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspaceId: str


class DaytonaGetWorkspaceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    verbose: bool | None = None


class DaytonaGetWorkspaceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Daytona_Organization_ID: str | None = Field(default=None, alias="X-Daytona-Organization-ID")


class DaytonaGetWorkspaceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DaytonaGetWorkspacePath
    query: DaytonaGetWorkspaceQuery | None = None
    header: DaytonaGetWorkspaceHeader | None = None


class DaytonaListWorkspacesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    verbose: bool | None = None
    labels: str | None = None


class DaytonaListWorkspacesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Daytona_Organization_ID: str | None = Field(default=None, alias="X-Daytona-Organization-ID")


class DaytonaListWorkspacesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DaytonaListWorkspacesQuery | None = None
    header: DaytonaListWorkspacesHeader | None = None


class DaytonaStartWorkspacePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspaceId: str


class DaytonaStartWorkspaceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Daytona_Organization_ID: str | None = Field(default=None, alias="X-Daytona-Organization-ID")


class DaytonaStartWorkspaceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DaytonaStartWorkspacePath
    header: DaytonaStartWorkspaceHeader | None = None


class DaytonaStopWorkspacePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspaceId: str


class DaytonaStopWorkspaceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Daytona_Organization_ID: str | None = Field(default=None, alias="X-Daytona-Organization-ID")


class DaytonaStopWorkspaceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DaytonaStopWorkspacePath
    header: DaytonaStopWorkspaceHeader | None = None


class DaytonaNamespace:
    """Typed accessor for the `daytona` app. Reach it via `client.apps.daytona`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_workspace(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DaytonaCreateWorkspaceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new workspace in Daytona with a specified image, size, name, labels, and auto-stop interval. This creates an isolated cloud development environment that consumes compute resources and may incur costs until it is stopped or deleted. Use this when an agent needs a fresh environment to run code, test changes, or isolate a development task from other workspaces.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "daytona.create_workspace", owner_id, args, account_id=account_id
        )

    async def delete_workspace(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DaytonaDeleteWorkspaceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a workspace and all its associated data, configuration, and resources by its workspace ID, with an optional force flag to override shutdown blockers. This action is irreversible: all files, environment state, and history within the workspace are lost. Use this when an agent needs to clean up a workspace that is no longer needed to free resources and avoid ongoing costs.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "daytona.delete_workspace", owner_id, args, account_id=account_id
        )

    async def execute_command(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DaytonaExecuteCommandRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes a shell command synchronously inside a running workspace, with optional working directory, environment variables, and timeout. The command runs within the workspace's file system and returns its output, including stdout and stderr. Use this when an agent needs to run code, install dependencies, modify files, or inspect the state of a workspace programmatically as part of a development or testing workflow.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "daytona.execute_command", owner_id, args, account_id=account_id
        )

    async def get_workspace(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DaytonaGetWorkspaceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific workspace by its ID, including its configuration, current status, resource usage, and metadata. Optionally includes verbose output for deeper inspection. Use this when an agent needs the full status and configuration of a workspace before deciding to start, stop, or delete it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "daytona.get_workspace", owner_id, args, account_id=account_id
        )

    async def list_workspaces(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DaytonaListWorkspacesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all workspaces in the Daytona account, optionally with verbose details and label-based filtering. Returns workspace summaries including ID, name, status (running, stopped, etc.), and configuration. Use this when an agent needs to discover available workspaces, check the status of existing environments, or find a specific workspace by label before performing lifecycle operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "daytona.list_workspaces", owner_id, args, account_id=account_id
        )

    async def start_workspace(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DaytonaStartWorkspaceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Starts a stopped workspace by its ID, initializing its environment and making it ready for command execution and development. The workspace transitions from a stopped to a running state and begins consuming compute resources. Use this when an agent needs to resume work in an existing workspace that is currently stopped or to bring a workspace online before executing commands.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "daytona.start_workspace", owner_id, args, account_id=account_id
        )

    async def stop_workspace(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DaytonaStopWorkspaceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Stops a running workspace by its ID, gracefully shutting down the environment to save compute resources. The workspace is preserved in its current state and can be started again later. Use this when an agent determines that a workspace is idle and should be deallocated to reduce resource consumption and costs.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "daytona.stop_workspace", owner_id, args, account_id=account_id
        )
