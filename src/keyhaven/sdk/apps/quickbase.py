# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `quickbase` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class QuickbaseRecordDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    table_id: str
    record_id: int


class QuickbaseRecordDeleteHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class QuickbaseRecordDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbaseRecordDeletePath
    header: QuickbaseRecordDeleteHeader = Field(default_factory=QuickbaseRecordDeleteHeader)


class QuickbaseRecordGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    table_id: str
    record_id: int


class QuickbaseRecordGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class QuickbaseRecordGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbaseRecordGetPath
    header: QuickbaseRecordGetHeader = Field(default_factory=QuickbaseRecordGetHeader)


class QuickbaseRecordsQueryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class QuickbaseRecordsQueryBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: str = Field(alias="from")
    where: str | None = None
    select: list[Any] | None = None
    sortBy: list[Any] | None = None
    limit: int | None = None


class QuickbaseRecordsQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: QuickbaseRecordsQueryHeader = Field(default_factory=QuickbaseRecordsQueryHeader)
    body: QuickbaseRecordsQueryBody


class QuickbaseRecordsUpsertHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class QuickbaseRecordsUpsertBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to: str
    data: list[Any]
    fieldsToReturn: list[Any] | None = None


class QuickbaseRecordsUpsertRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: QuickbaseRecordsUpsertHeader = Field(default_factory=QuickbaseRecordsUpsertHeader)
    body: QuickbaseRecordsUpsertBody


class QuickbaseNamespace:
    """Typed accessor for the `quickbase` app. Reach it via `client.apps.quickbase`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def record_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbaseRecordDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a single record from a specified Quickbase table using its record ID. The record is moved to the app's Recycle Bin and can be restored by an administrator, but it is no longer visible in the active table or included in queries and reports. Use this when an agent needs to remove stale, duplicate, or erroneous data from a Quickbase table. This action is destructive and should be used with caution.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbase.record_delete", owner_id, args, account_id=account_id
        )

    async def record_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbaseRecordGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single record from a specified Quickbase table using its unique record ID. Returns all field values for that record as defined by the table schema. This is a read-only operation with no side effects. Use this when an agent needs to look up the full details of a specific record by its known identifier.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbase.record_get", owner_id, args, account_id=account_id
        )

    async def records_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbaseRecordsQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for records in a specified Quickbase table using a structured query string with field-based filters (e.g. {6.EX.'value'}). You can select which fields to return, apply sorting, and limit the result count. This is a read-only operation — no records are created, modified, or deleted. Use this when an agent needs to find, filter, or inspect existing records for reporting, validation, or downstream processing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbase.records_query", owner_id, args, account_id=account_id
        )

    async def records_upsert(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbaseRecordsUpsertRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates new records or updates existing ones in a specified Quickbase table by sending field-value mappings. If a record with matching key fields already exists it is updated, otherwise a new record is inserted. The response can include specified field values from the affected records. Use this when an agent needs to synchronize data into Quickbase, batch-import records, or programmatically update existing entries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbase.records_upsert", owner_id, args, account_id=account_id
        )
