# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `bitly` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BitlyBitlinkCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    long_url: str
    domain: str | None = None
    group_guid: str | None = None
    title: str | None = None
    tags: list[Any] | None = None
    deeplinks: list[Any] | None = None


class BitlyBitlinkCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: BitlyBitlinkCreateBody


class BitlyBitlinkExpandBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    bitlink_id: str


class BitlyBitlinkExpandRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: BitlyBitlinkExpandBody


class BitlyBitlinkGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    bitlink: str


class BitlyBitlinkGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitlyBitlinkGetPath


class BitlyBitlinkShortenBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    long_url: str
    domain: str | None = None
    group_guid: str | None = None


class BitlyBitlinkShortenRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: BitlyBitlinkShortenBody


class BitlyBitlinkUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    bitlink: str


class BitlyBitlinkUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    tags: list[Any] | None = None
    archived: bool | None = None
    deeplinks: list[Any] | None = None
    long_url: str | None = None


class BitlyBitlinkUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitlyBitlinkUpdatePath
    body: BitlyBitlinkUpdateBody = Field(default_factory=BitlyBitlinkUpdateBody)


class BitlyGroupBitlinksListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    group_guid: str


class BitlyGroupBitlinksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    size: int | None = None
    page: int | None = None
    keyword: str | None = None
    query: str | None = None
    created_before: int | None = None
    created_after: int | None = None
    archived: str | None = None
    tags: str | None = None


class BitlyGroupBitlinksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitlyGroupBitlinksListPath
    query: BitlyGroupBitlinksListQuery | None = None


class BitlyNamespace:
    """Typed accessor for the `bitly` app. Reach it via `client.apps.bitly`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def bitlink_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitlyBitlinkCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a fully-configured Bitlink with title, tags, mobile deeplinks, and an optional custom back-half. The generated short link is immediately live, public, and redirects to the specified long URL. Use this when you need granular control over how the Bitlink appears and behaves from the moment of creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitly.bitlink_create", owner_id, args, account_id=account_id
        )

    async def bitlink_expand(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitlyBitlinkExpandRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Resolves a Bitlink to its full destination long URL without requiring group context or ownership authentication. Despite using a POST method, this is a pure lookup operation with no modifying side effects. Use this when you have a short link and need to reveal its target URL for verification or analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitly.bitlink_expand", owner_id, args, account_id=account_id
        )

    async def bitlink_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitlyBitlinkGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches metadata for a specific Bitlink including its destination URL, title, tags, creation date, and aggregate click count. No data is modified. Use this to inspect the current configuration or analytics summary of an existing short link before deciding to update or archive it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitly.bitlink_get", owner_id, args, account_id=account_id
        )

    async def bitlink_shorten(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitlyBitlinkShortenRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Takes a long URL and returns a short Bitly link owned by the specified group. The resulting short URL is immediately usable and redirects to the original URL when accessed. Use this when you need a quick short link without configuring title, tags, or deeplinks.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitly.bitlink_shorten", owner_id, args, account_id=account_id
        )

    async def bitlink_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitlyBitlinkUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing Bitlink's properties such as title, tags, destination URL, deeplink settings, or archived status. Changes take effect immediately for every future visitor who clicks the short link. Use this to rebrand, correct, or disable a previously published short link without changing its short URL.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitly.bitlink_update", owner_id, args, account_id=account_id
        )

    async def group_bitlinks_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitlyGroupBitlinksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all Bitlinks owned by the specified group, with optional filters for tags, keywords, creation date range, and archived status. No data is modified. Use this to discover, audit, or browse the short links within a team group and to gather Bitlink IDs for subsequent update or expand operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitly.group_bitlinks_list", owner_id, args, account_id=account_id
        )
