# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `vercel` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class VercelAddDomainToProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str


class VercelAddDomainToProjectQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelAddDomainToProjectBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class VercelAddDomainToProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelAddDomainToProjectPath
    query: VercelAddDomainToProjectQuery | None = None
    body: VercelAddDomainToProjectBody


class VercelCreateEnvironmentVariablesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str


class VercelCreateEnvironmentVariablesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    upsert: str | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelCreateEnvironmentVariablesBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key: str
    value: str
    type_: Literal["system", "secret", "encrypted", "plain", "sensitive"] = Field(alias="type")
    target: list[Any]


class VercelCreateEnvironmentVariablesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelCreateEnvironmentVariablesPath
    query: VercelCreateEnvironmentVariablesQuery | None = None
    body: VercelCreateEnvironmentVariablesBody


class VercelCreateProjectQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelCreateProjectBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    framework: str | None = None
    gitRepository: dict[str, Any] | None = None
    rootDirectory: str | None = None
    environmentVariables: list[Any] | None = None


class VercelCreateProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: VercelCreateProjectQuery | None = None
    body: VercelCreateProjectBody


class VercelDeleteProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str


class VercelDeleteProjectQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelDeleteProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelDeleteProjectPath
    query: VercelDeleteProjectQuery | None = None


class VercelDeployQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    forceNew: Literal["0", "1"] | None = None
    skipAutoDetectionConfirmation: Literal["0", "1"] | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelDeployBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    customEnvironmentSlugOrId: str | None = None
    deploymentId: str | None = None
    gitSource: dict[str, Any]
    project: str
    target: str | None = None
    withLatestCommit: bool | None = None


class VercelDeployRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: VercelDeployQuery | None = None
    body: VercelDeployBody


class VercelEditEnvironmentVariablePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str
    id: str


class VercelEditEnvironmentVariableQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelEditEnvironmentVariableBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key: str | None = None
    target: list[Any] | None = None
    gitBranch: str | None = None
    type_: Literal["plain", "sensitive"] | None = Field(default=None, alias="type")
    value: str | None = None
    customEnvironmentIds: list[Any] | None = None
    comment: str | None = None


class VercelEditEnvironmentVariableRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelEditEnvironmentVariablePath
    query: VercelEditEnvironmentVariableQuery | None = None
    body: VercelEditEnvironmentVariableBody = Field(
        default_factory=VercelEditEnvironmentVariableBody
    )


class VercelFindProjectByIdOrNamePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str


class VercelFindProjectByIdOrNameQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelFindProjectByIdOrNameRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelFindProjectByIdOrNamePath
    query: VercelFindProjectByIdOrNameQuery | None = None


class VercelGetAProjectDomainPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str
    domain: str


class VercelGetAProjectDomainQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelGetAProjectDomainRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelGetAProjectDomainPath
    query: VercelGetAProjectDomainQuery | None = None


class VercelGetDeploymentPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrUrl: str


class VercelGetDeploymentQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    withGitRepoInfo: str | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelGetDeploymentRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelGetDeploymentPath
    query: VercelGetDeploymentQuery | None = None


class VercelGetDeploymentEventsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrUrl: str


class VercelGetDeploymentEventsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    direction: Literal["backward", "forward"] = "forward"
    follow: float | None = None
    limit: float | None = None
    name: str | None = None
    since: float | None = None
    until: float | None = None
    statusCode: float | None = None
    delimiter: float | None = None
    builds: float | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelGetDeploymentEventsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelGetDeploymentEventsPath
    query: VercelGetDeploymentEventsQuery | None = None


class VercelListDeploymentsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    app: str | None = None
    limit: float | None = None
    projectId: str | None = None
    target: str | None = None
    users: str | None = None
    since: float | None = None
    until: float | None = None
    state: str | None = None
    rollbackCandidate: bool | None = None
    branch: str | None = None
    sha: str | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelListDeploymentsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: VercelListDeploymentsQuery | None = None


class VercelListProjectsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: str | None = None
    search: str | None = None
    repo: str | None = None
    repoId: str | None = None
    repoUrl: str | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelListProjectsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: VercelListProjectsQuery | None = None


class VercelRemoveDomainFromProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str
    domain: str


class VercelRemoveDomainFromProjectQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelRemoveDomainFromProjectBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    removeRedirects: bool | None = None


class VercelRemoveDomainFromProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelRemoveDomainFromProjectPath
    query: VercelRemoveDomainFromProjectQuery | None = None
    body: VercelRemoveDomainFromProjectBody | None = None


class VercelRemoveEnvironmentVariablePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str
    id: str


class VercelRemoveEnvironmentVariableQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customEnvironmentId: str | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelRemoveEnvironmentVariableRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelRemoveEnvironmentVariablePath
    query: VercelRemoveEnvironmentVariableQuery | None = None


class VercelRetrieveDecryptedEnvironmentVariablePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str
    id: str


class VercelRetrieveDecryptedEnvironmentVariableQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelRetrieveDecryptedEnvironmentVariableRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelRetrieveDecryptedEnvironmentVariablePath
    query: VercelRetrieveDecryptedEnvironmentVariableQuery | None = None


class VercelRetrieveEnvironmentVariablesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str


class VercelRetrieveEnvironmentVariablesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    gitBranch: str | None = None
    decrypt: Literal["true", "false"] | None = None
    source: str | None = None
    customEnvironmentId: str | None = None
    customEnvironmentSlug: str | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelRetrieveEnvironmentVariablesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelRetrieveEnvironmentVariablesPath
    query: VercelRetrieveEnvironmentVariablesQuery | None = None


class VercelRetrieveProjectDomainsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str


class VercelRetrieveProjectDomainsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    production: Literal["true", "false"] | None = None
    target: Literal["production", "preview"] | None = None
    gitBranch: str | None = None
    redirects: Literal["true", "false"] | None = None
    verified: Literal["true", "false"] | None = None
    limit: float | None = None
    teamId: str | None = None
    slug: str | None = None


class VercelRetrieveProjectDomainsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelRetrieveProjectDomainsPath
    query: VercelRetrieveProjectDomainsQuery | None = None


class VercelUpdateProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idOrName: str


class VercelUpdateProjectQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    slug: str | None = None


class VercelUpdateProjectBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    buildCommand: str | None = None
    devCommand: str | None = None
    framework: str | None = None
    installCommand: str | None = None
    name: str | None = None
    rootDirectory: str | None = None


class VercelUpdateProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: VercelUpdateProjectPath
    query: VercelUpdateProjectQuery | None = None
    body: VercelUpdateProjectBody | None = None


class VercelNamespace:
    """Typed accessor for the `vercel` app. Reach it via `client.apps.vercel`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def add_domain_to_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelAddDomainToProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Assigns a custom domain name to a Vercel project identified by project ID or name. The domain must exist and be configured to point to Vercel's nameservers or DNS records for the site to become accessible. After adding, Vercel will attempt to provision an SSL certificate. This action affects the live site's URL and can cause downtime if misconfigured. Use this when an agent needs to associate a custom domain with a project, replacing the auto-generated vercel.app URL.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.add_domain_to_project", owner_id, args, account_id=account_id
        )

    async def create_environment_variables(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelCreateEnvironmentVariablesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates one or more environment variables for a Vercel project by setting a key, value, type (plain, secret, encrypted, system, sensitive), and target environments (production, preview, development). When the upsert query parameter is true, existing variables are updated instead of creating duplicates. Environment variables affect all subsequent deployments and can contain sensitive credentials. Use this when an agent needs to configure secrets, API keys, or configuration values for a deployment environment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.create_environment_variables", owner_id, args, account_id=account_id
        )

    async def create_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelCreateProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new project on Vercel with a name, optional framework (e.g., Next.js, SvelteKit), root directory, linked Git repository, and environment variables. Once created, the project appears in the Vercel dashboard and can receive deployments. Linking a Git repository requires the Vercel app to be installed first. This action is reversible by deleting the project but will affect any ongoing deployments. Use this when an agent needs to bootstrap a new deployment target for a web application.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.create_project", owner_id, args, account_id=account_id
        )

    async def delete_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelDeleteProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a Vercel project and all associated resources, including deployments, domains, environment variables, and build logs. The project's URLs will stop resolving and all deployment history is lost. This action is irreversible. Use this only when an agent has explicit authorisation to decommission a project, clean up test resources, or remove abandoned projects from a Vercel team account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.delete_project", owner_id, args, account_id=account_id
        )

    async def deploy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelDeployRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new deployment on Vercel for a specified project, targeting a Git source (GitHub only, with org, repo, branch/ref required) or redeploying an existing deployment. You can target production, preview (default), or a custom environment. When forceNew=1 is set, a new deployment is created even if the code has not changed. This action triggers a build that consumes Vercel build minutes. Use this when an agent needs to deploy a specific Git branch or commit, or trigger a fresh deployment without a Git push event.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("vercel.deploy", owner_id, args, account_id=account_id)

    async def edit_environment_variable(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelEditEnvironmentVariableRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing environment variable for a Vercel project, identified by the project ID/name and the environment variable ID. You can update the key, value, type, target environments, Git branch scoping, custom environment assignments, and add a comment. Changes affect subsequent deployments that use the modified environment. This action is reversible by providing the previous values. Use this when an agent needs to rotate secrets, update configuration values, or change environment variable scoping.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.edit_environment_variable", owner_id, args, account_id=account_id
        )

    async def find_project_by_id_or_name(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelFindProjectByIdOrNameRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details about a specific Vercel project by its unique ID or name, including its framework, Git repository link, environment variables reference, and latest deployments. This is read-only and does not alter the project. Use this when an agent needs to inspect a project's configuration, verify its settings before a deployment, or retrieve the project ID for use in other API calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.find_project_by_id_or_name", owner_id, args, account_id=account_id
        )

    async def get_a_project_domain(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelGetAProjectDomainRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves information about a specific domain associated with a Vercel project, identified by both the project and the domain name. Returns the domain's verification status, SSL certificate status, target environment, and configuration. This is read-only and does not modify the domain. Use this when an agent needs to check whether a domain is properly verified, inspect its SSL status, or confirm domain settings before removing or reconfiguring it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.get_a_project_domain", owner_id, args, account_id=account_id
        )

    async def get_deployment(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelGetDeploymentRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves comprehensive information about a specific Vercel deployment by its unique ID or hostname (URL), including its status, Git commit metadata, build logs summary, environment variables used, and alias domains. Additional details are included when the authenticated user owns the deployment. This is read-only and does not affect the deployment. Use this when an agent needs to inspect a deployment's status, investigate failures, or verify that the correct commit was deployed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.get_deployment", owner_id, args, account_id=account_id
        )

    async def get_deployment_events(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelGetDeploymentEventsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches build logs and deployment events for a specific Vercel deployment, identified by deployment ID or hostname and build ID (name parameter). Supports streaming live logs (follow=1), directional pagination (since/until timestamps), and filtering by HTTP status code. This is a read-only operation that does not affect the deployment. Use this when an agent needs to debug build failures, monitor ongoing deployments in real time, or retrieve specific log entries for error analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.get_deployment_events", owner_id, args, account_id=account_id
        )

    async def list_deployments(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelListDeploymentsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists deployments under the authenticated user or team with extensive filtering options including project ID, target environment, branch name, commit SHA, state (BUILDING, READY, ERROR, etc.), date range, and rollback candidate status. Incomplete deployments have a null url property. This is read-only and does not modify any deployments. Use this when an agent needs to audit deployment history, find a specific deployment by branch or state, or monitor the status of recent deploys.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.list_deployments", owner_id, args, account_id=account_id
        )

    async def list_projects(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelListProjectsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Vercel projects accessible to the authenticated user or team, with optional filtering by name search, repository, or repository ID. Returns each project's ID, name, framework, latest deployment status, and connected Git repository. This is read-only and does not modify any state. Use this when an agent needs to discover available projects, find a project ID by name, or enumerate projects before performing management operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.list_projects", owner_id, args, account_id=account_id
        )

    async def remove_domain_from_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelRemoveDomainFromProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Detaches a custom domain from a Vercel project and optionally removes all redirects pointing to that domain. After removal, the site becomes inaccessible via that domain and any SSL certificates associated with it are invalidated. This action is reversible only by re-adding the domain, but may cause permanent traffic loss in the interim. Use this when an agent needs to unlink a domain from a project, migrate a domain to a different project, or clean up unused domain associations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.remove_domain_from_project", owner_id, args, account_id=account_id
        )

    async def remove_environment_variable(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelRemoveEnvironmentVariableRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes an environment variable from a Vercel project, identified by the project ID/name and the environment variable ID. Optionally scoped to a custom environment. After deletion, subsequent deployments will not have access to this variable, which could cause build failures or runtime errors if the application depends on it. This action is not reversible — the variable would need to be recreated. Use this when an agent needs to clean up unused or outdated configuration, or rotate secrets by removing the old variable before creating a new one.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.remove_environment_variable", owner_id, args, account_id=account_id
        )

    async def retrieve_decrypted_environment_variable(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelRetrieveDecryptedEnvironmentVariableRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the plaintext (decrypted) value of a specific environment variable for a Vercel project, identified by project ID/name and the variable ID. This exposes the actual secret value (API keys, database passwords, tokens) in the response. While technically a GET/read operation, this reveals the most sensitive data of all read endpoints. Use this when an agent explicitly needs to read a secret value for use in deployment or configuration — handle the returned data with extreme care and never log it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.retrieve_decrypted_environment_variable", owner_id, args, account_id=account_id
        )

    async def retrieve_environment_variables(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelRetrieveEnvironmentVariablesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all environment variables for a Vercel project, filtered optionally by Git branch, custom environment, or decrypted. The response includes variable keys, types, targets, and values. Sensitive variables may have their values masked depending on the decrypt parameter. While this is a read-only operation, it may expose secrets and configuration values. Use this when an agent needs to audit environment configuration, verify that required variables exist, or inspect current settings before making changes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.retrieve_environment_variables", owner_id, args, account_id=account_id
        )

    async def retrieve_project_domains(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelRetrieveProjectDomainsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all domains associated with a Vercel project, including custom domains and auto-generated vercel.app domains. Supports filtering by production status, target environment, Git branch, redirects, and verification status. This is read-only and does not modify any domain configuration. Use this when an agent needs to inspect which domains are attached to a project, check verification status, or audit domain configuration before making changes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.retrieve_project_domains", owner_id, args, account_id=account_id
        )

    async def update_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: VercelUpdateProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing Vercel project's configuration, including its name, framework, build command, dev command, install command, and root directory. Changes take effect for the next deployment. This action is reversible by providing the previous values. Use this when an agent needs to reconfigure a project's build settings, change the framework detection, or update deployment commands without creating a new project.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "vercel.update_project", owner_id, args, account_id=account_id
        )
