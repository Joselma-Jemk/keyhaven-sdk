# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `signnow` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SignnowDocumentDownloadPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    documentId: str


class SignnowDocumentDownloadQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str | None = Field(default=None, alias="type")
    with_history: int | None = None


class SignnowDocumentDownloadRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SignnowDocumentDownloadPath
    query: SignnowDocumentDownloadQuery | None = None


class SignnowDocumentGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    documentId: str


class SignnowDocumentGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SignnowDocumentGetPath


class SignnowDocumentInviteCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    documentId: str


class SignnowDocumentInviteCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to: list[Any]
    from_: str = Field(alias="from")
    cc: list[Any] | None = None
    subject: str | None = None
    message: str | None = None
    language: str | None = None


class SignnowDocumentInviteCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SignnowDocumentInviteCreatePath
    body: SignnowDocumentInviteCreateBody


class SignnowDocumentUploadBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    file: str
    check_fields: bool | None = None


class SignnowDocumentUploadRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SignnowDocumentUploadBody


class SignnowDocumentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 20
    offset: int = 0
    sortby: str | None = None
    order: str | None = None


class SignnowDocumentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SignnowDocumentsListQuery | None = None


class SignnowTemplateDocumentCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    templateId: str


class SignnowTemplateDocumentCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    document_name: str | None = None


class SignnowTemplateDocumentCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SignnowTemplateDocumentCreatePath
    body: SignnowTemplateDocumentCreateBody | None = None


class SignnowNamespace:
    """Typed accessor for the `signnow` app. Reach it via `client.apps.signnow`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def document_download(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SignnowDocumentDownloadRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Download the PDF file of a document from SignNow, optionally including the audit history. The audit trail records every action taken on the document (views, signatures, timestamps and IP addresses). Use this to retrieve a signed document for record-keeping, sharing, or archival; the document may contain sensitive personal or business information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "signnow.document_download", owner_id, args, account_id=account_id
        )

    async def document_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SignnowDocumentGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch the full metadata, fields, and roles of a single document using its unique SignNow identifier. The response includes signature fields, text fields, and role assignments that have been placed on the document. Use this to inspect a document's structure before sending it for signature or after receiving signed copies.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "signnow.document_get", owner_id, args, account_id=account_id
        )

    async def document_invite_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SignnowDocumentInviteCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Send a signing invitation email for a document to one or more recipients with defined roles, subject, and message. The recipients will receive an email requesting their signature and can sign electronically. Use this when a document is fully prepared with fields and roles assigned; this action triggers real email notifications to external signers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "signnow.document_invite_create", owner_id, args, account_id=account_id
        )

    async def document_upload(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SignnowDocumentUploadRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Upload a file (PDF, DOCX, or image) as a new document in the SignNow account. Optionally parse simple text tags embedded in the document into fillable fields. Use this to bring a new document into SignNow before adding signature fields or sending it out for signing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "signnow.document_upload", owner_id, args, account_id=account_id
        )

    async def documents_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SignnowDocumentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of documents stored in the authenticated SignNow account, with optional sorting by name, creation date, or last update. The response includes metadata such as document status, owner, and thumbnail URL. Use this to browse documents before reading, downloading, or sending one for signature.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "signnow.documents_list", owner_id, args, account_id=account_id
        )

    async def template_document_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SignnowTemplateDocumentCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new document by copying an existing SignNow template and assigning it a name. The new document is a standalone instance that can be further edited, have fields added, or be sent for signature. Use this when you need to generate a document from a pre-defined template without uploading a new file.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "signnow.template_document_create", owner_id, args, account_id=account_id
        )
