# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `airtable` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AirtableCreateTablePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    base_id: str


class AirtableCreateTableHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AirtableCreateTableBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    fields: list[Any]
    primaryFieldId: str | None = None


class AirtableCreateTableRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AirtableCreateTablePath
    header: AirtableCreateTableHeader = Field(default_factory=AirtableCreateTableHeader)
    body: AirtableCreateTableBody


class AirtableGetBaseSchemaPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    base_id: str


class AirtableGetBaseSchemaQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include: list[Any] | None = None


class AirtableGetBaseSchemaHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AirtableGetBaseSchemaRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AirtableGetBaseSchemaPath
    query: AirtableGetBaseSchemaQuery | None = None
    header: AirtableGetBaseSchemaHeader = Field(default_factory=AirtableGetBaseSchemaHeader)


class AirtableListBasesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    offset: str | None = None


class AirtableListBasesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AirtableListBasesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AirtableListBasesQuery | None = None
    header: AirtableListBasesHeader = Field(default_factory=AirtableListBasesHeader)


class AirtableListWebhooksPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    base_id: str


class AirtableListWebhooksHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AirtableListWebhooksRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AirtableListWebhooksPath
    header: AirtableListWebhooksHeader = Field(default_factory=AirtableListWebhooksHeader)


class AirtableUpdateTablePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    base_id: str
    table_id: str


class AirtableUpdateTableHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AirtableUpdateTableBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None


class AirtableUpdateTableRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AirtableUpdateTablePath
    header: AirtableUpdateTableHeader = Field(default_factory=AirtableUpdateTableHeader)
    body: AirtableUpdateTableBody = Field(default_factory=AirtableUpdateTableBody)


class AirtableNamespace:
    """Typed accessor for the `airtable` app. Reach it via `client.apps.airtable`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_table(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AirtableCreateTableRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new table within a specified base with a given name, description, and field definitions. Each field requires a name and type (e.g., singleLineText, number, date, multipleSelects), with optional configuration like precision, choices, or linked record settings. The new table is immediately visible to all collaborators of the base. Use this to extend a base's schema with new data collections.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "airtable.create_table", owner_id, args, account_id=account_id
        )

    async def get_base_schema(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AirtableGetBaseSchemaRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full schema definition for a specified base, including all tables, their fields, field types, and view configurations. This provides the structural blueprint of the base — which tables exist, what columns each has, and how data relationships are set up. Use this to understand a base's structure before reading, writing, or updating records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "airtable.get_base_schema", owner_id, args, account_id=account_id
        )

    async def list_bases(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AirtableListBasesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all Airtable bases accessible to the authenticated user. Each base corresponds to a project workspace containing its own set of tables, records, and collaborators. Use this function to discover available bases before performing operations on a specific base's tables or records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "airtable.list_bases", owner_id, args, account_id=account_id
        )

    async def list_webhooks(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AirtableListWebhooksRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Gets all configured webhooks for a specified base, returning their unique IDs, notification URLs, current cursor positions, and enabled status. Webhooks enable external services to receive real-time notifications when records in the base change. Use this to audit existing integrations or find a webhook ID before managing its payload specifications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "airtable.list_webhooks", owner_id, args, account_id=account_id
        )

    async def update_table(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AirtableUpdateTableRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the name and/or description of an existing table in a specified base using a PATCH request. Only the provided fields are modified; omitted fields remain unchanged. Changes propagate immediately to all views and collaborators of the base. Use this to rename a table or clarify its purpose without altering its field structure or existing data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "airtable.update_table", owner_id, args, account_id=account_id
        )
