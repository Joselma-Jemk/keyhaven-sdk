# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `perplexity` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PerplexityChatCompletionsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PerplexityChatCompletionsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    model: str
    messages: list[Any]
    max_tokens: int | None = None
    temperature: float = 0.7
    return_citations: bool = True


class PerplexityChatCompletionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PerplexityChatCompletionsHeader = Field(default_factory=PerplexityChatCompletionsHeader)
    body: PerplexityChatCompletionsBody


class PerplexityCreateEmbeddingsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PerplexityCreateEmbeddingsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    model: str
    input: list[Any]


class PerplexityCreateEmbeddingsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PerplexityCreateEmbeddingsHeader = Field(
        default_factory=PerplexityCreateEmbeddingsHeader
    )
    body: PerplexityCreateEmbeddingsBody


class PerplexityCreateSearchHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PerplexityCreateSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    max_results: int | None = None


class PerplexityCreateSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PerplexityCreateSearchHeader = Field(default_factory=PerplexityCreateSearchHeader)
    body: PerplexityCreateSearchBody


class PerplexityGetUsageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PerplexityGetUsageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PerplexityGetUsageHeader = Field(default_factory=PerplexityGetUsageHeader)


class PerplexityListModelsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PerplexityListModelsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PerplexityListModelsHeader = Field(default_factory=PerplexityListModelsHeader)


class PerplexityNamespace:
    """Typed accessor for the `perplexity` app. Reach it via `client.apps.perplexity`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def chat_completions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PerplexityChatCompletionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Send a conversation to Perplexity AI and receive a generated response augmented with real-time web search results and citations. The model selected (e.g. sonar-pro) determines capability, speed, and cost. The response includes inline citations when return_citations is enabled. Use this when the agent needs an AI-powered answer grounded in current web information, such as research, fact-checking, or summarisation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "perplexity.chat_completions", owner_id, args, account_id=account_id
        )

    async def create_embeddings(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PerplexityCreateEmbeddingsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generate vector embeddings for one or more input text strings using a Perplexity embedding model. The resulting embeddings are numerical vectors that capture semantic meaning, suitable for downstream tasks like similarity search, clustering, or RAG pipelines. This does not persist any data on Perplexity's servers. Use this when the agent needs to convert text into a machine-readable vector representation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "perplexity.create_embeddings", owner_id, args, account_id=account_id
        )

    async def create_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PerplexityCreateSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Perform a direct web search through Perplexity AI and receive an AI-summarised response with results. Unlike chat completions, this endpoint is optimised for standalone search queries without a conversation history. The response draws from live web data. Use this when the agent needs a quick, citation-backed answer to a specific factual question without maintaining a chat context.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "perplexity.create_search", owner_id, args, account_id=account_id
        )

    async def get_usage(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PerplexityGetUsageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve API usage statistics for the authenticated Perplexity account, including token consumption, request counts, and billing-period data. This is a read-only operation. Use this when the agent needs to monitor remaining quota, audit recent API consumption, or decide whether to proceed with large batch operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "perplexity.get_usage", owner_id, args, account_id=account_id
        )

    async def list_models(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PerplexityListModelsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the list of AI models available on the Perplexity API, including chat, search, and embedding variants with their capabilities and identifiers. This is a read-only operation that does not consume tokens or alter any state. Use this when the agent needs to discover which model to use for a subsequent chat completion, search, or embedding call.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "perplexity.list_models", owner_id, args, account_id=account_id
        )
