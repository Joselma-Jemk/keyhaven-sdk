# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `basecamp` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BasecampProjectCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class BasecampProjectCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None


class BasecampProjectCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BasecampProjectCreatePath
    body: BasecampProjectCreateBody


class BasecampProjectGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str
    project_id: str


class BasecampProjectGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BasecampProjectGetPath


class BasecampProjectUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str
    project_id: str


class BasecampProjectUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None


class BasecampProjectUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BasecampProjectUpdatePath
    body: BasecampProjectUpdateBody = Field(default_factory=BasecampProjectUpdateBody)


class BasecampProjectsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class BasecampProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str | None = None
    page: int | None = None


class BasecampProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BasecampProjectsListPath
    query: BasecampProjectsListQuery | None = None


class BasecampTodoCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str
    project_id: str
    todolist_id: str


class BasecampTodoCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content: str
    description: str | None = None
    assignee_ids: list[Any] | None = None
    completion_subscriber_ids: list[Any] | None = None
    notify: bool | None = None
    due_on: str | None = None
    starts_on: str | None = None


class BasecampTodoCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BasecampTodoCreatePath
    body: BasecampTodoCreateBody


class BasecampTodosListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str
    project_id: str
    todolist_id: str


class BasecampTodosListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str | None = None
    completed: bool | None = None
    page: int | None = None


class BasecampTodosListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BasecampTodosListPath
    query: BasecampTodosListQuery | None = None


class BasecampNamespace:
    """Typed accessor for the `basecamp` app. Reach it via `client.apps.basecamp`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def project_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BasecampProjectCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new project in the specified Basecamp account with a given name and optional description. The project starts empty and becomes immediately visible to all account members, appearing in the project list and dashboards. Use this function to spin up new collaborative workspaces for teams, initiatives, or client engagements.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "basecamp.project_create", owner_id, args, account_id=account_id
        )

    async def project_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BasecampProjectGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details of a single Basecamp project by its account and project identifiers, including its description, creator, creation date, dock configuration (pinned tools), and member access settings. This is a read-only operation with no side effects. Use this function to inspect a specific project's configuration before taking actions within its scope.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "basecamp.project_get", owner_id, args, account_id=account_id
        )

    async def project_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BasecampProjectUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies the name and/or description of an existing Basecamp project. Changes take effect immediately and are visible to all project members across the account. Use this function to rename a project or update its description to reflect current goals, scope changes, or status updates.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "basecamp.project_update", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BasecampProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all projects visible to the authenticated user in the specified Basecamp account, with optional filtering by status (active, archived, or trashed). Each project object includes its name, description, URL, and member roster. This is a read-only operation with no side effects. Use this function to discover accessible projects before working with their to-do lists, messages, or other resources.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "basecamp.projects_list", owner_id, args, account_id=account_id
        )

    async def todo_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BasecampTodoCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new to-do item to a specified to-do list in a Basecamp project, with optional assignees, HTML description, due date, start date, and notification preferences. When notify is true, assigned people will receive immediate notifications. Use this function to create tasks, delegate work to team members, and set deadlines within a project's workflow.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "basecamp.todo_create", owner_id, args, account_id=account_id
        )

    async def todos_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BasecampTodosListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all to-dos in a specified to-do list within a Basecamp project, with optional filtering by completion status. Each to-do includes its title, assignees, due date, completion state, and creation details. This is a read-only operation with no side effects. Use this function to review task assignments, track project progress, or find specific tasks before updating them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "basecamp.todos_list", owner_id, args, account_id=account_id
        )
