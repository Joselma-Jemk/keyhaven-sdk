# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `lmnt` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class LmntDeleteVoicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class LmntDeleteVoiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LmntDeleteVoicePath


class LmntListVoicesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    starred: str = "false"
    owner: Literal["system", "me", "all"] = "all"


class LmntListVoicesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LmntListVoicesQuery = Field(default_factory=LmntListVoicesQuery)


class LmntSpeechAdvancedBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice: str
    text: str
    model: Literal["aurora", "blizzard"] = "aurora"
    language: Literal[
        "auto", "en", "es", "pt", "fr", "de", "zh", "ko", "hi", "ja", "ru", "it", "tr"
    ] = "auto"
    format: Literal["aac", "mp3", "mulaw", "raw", "wav"] = "mp3"
    sample_rate: int = 24000
    speed: float = 1
    seed: int | None = None
    conversational: bool = False
    length: float | None = None
    top_p: float = 1
    temperature: float = 1
    return_durations: bool = False


class LmntSpeechAdvancedRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LmntSpeechAdvancedBody


class LmntSpeechBytesBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice: str
    text: str
    model: Literal["aurora", "blizzard"] = "aurora"
    language: Literal[
        "auto", "en", "es", "pt", "fr", "de", "zh", "ko", "hi", "ja", "ru", "it", "tr"
    ] = "auto"
    format: Literal["aac", "mp3", "mulaw", "raw", "wav"] = "mp3"
    sample_rate: int = 24000
    speed: float = 1
    seed: int | None = None
    conversational: bool = False
    length: float | None = None
    top_p: float = 1
    temperature: float = 1


class LmntSpeechBytesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LmntSpeechBytesBody


class LmntUpdateVoicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class LmntUpdateVoiceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    description: str | None = None
    gender: str | None = None
    name: str | None = None
    starred: bool | None = None
    unfreeze: bool | None = None


class LmntUpdateVoiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LmntUpdateVoicePath
    body: LmntUpdateVoiceBody = Field(default_factory=LmntUpdateVoiceBody)


class LmntVoiceInfoPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class LmntVoiceInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LmntVoiceInfoPath


class LmntNamespace:
    """Typed accessor for the `lmnt` app. Reach it via `client.apps.lmnt`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def delete_voice(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LmntDeleteVoiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a custom voice from the account, including all its training data, and cancels any in-progress operations on it. The voice ID becomes invalid for future synthesis requests. This action is irreversible — the voice cannot be recovered and would need to be recreated and retrained from scratch.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lmnt.delete_voice", owner_id, args, account_id=account_id
        )

    async def list_voices(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LmntListVoicesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all available voices for the authenticated account, filterable by ownership (system-provided, custom, or both) and starred status. Use this to discover available voice IDs and their properties before generating speech. Read-only — no voices are created, modified, or deleted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("lmnt.list_voices", owner_id, args, account_id=account_id)

    async def speech_advanced(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LmntSpeechAdvancedRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Synthesizes text into speech audio with the same voice and model parameters as SPEECH_BYTES, but additionally returns metadata such as phoneme durations for fine-grained alignment. Use this when you need timing data for lip-syncing, subtitle generation, or audio-visual synchronization. Consumes API credits and returns the audio file plus advanced synthesis metadata.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lmnt.speech_advanced", owner_id, args, account_id=account_id
        )

    async def speech_bytes(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LmntSpeechBytesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Converts text into spoken audio using a selected voice and model, returning the raw audio bytes in the requested format (MP3, WAV, AAC, raw PCM, or mu-law). Use this to generate voiceovers, automated announcements, or any audio content from text up to 5,000 characters. The operation consumes API credits and returns the audio directly in the response — no file is persisted on the LMNT platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lmnt.speech_bytes", owner_id, args, account_id=account_id
        )

    async def update_voice(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LmntUpdateVoiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies a custom voice's metadata — name, description, gender tag, starred status, or model unfreeze. Use this to organize and manage your custom voice library. The changes are immediately persisted and affect all future synthesis using this voice. Reversible by setting the values back.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lmnt.update_voice", owner_id, args, account_id=account_id
        )

    async def voice_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LmntVoiceInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves metadata for a single voice by its ID, including name, gender, description, and ownership information. Use this to inspect a voice's characteristics before selecting it for speech synthesis. Read-only — no voice properties are altered.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("lmnt.voice_info", owner_id, args, account_id=account_id)
