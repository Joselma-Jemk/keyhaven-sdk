# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `slack` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SlackBookmarksAddHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackBookmarksAddBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel_id: str
    title: str
    type_: str = Field(default="link", alias="type")
    access_level: Literal["read", "write"] | None = None
    emoji: str | None = None
    entity_id: str | None = None
    link: str | None = None
    parent_id: str | None = None


class SlackBookmarksAddRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackBookmarksAddHeader = Field(default_factory=SlackBookmarksAddHeader)
    body: SlackBookmarksAddBody


class SlackBookmarksEditHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackBookmarksEditBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    bookmark_id: str
    channel_id: str
    emoji: str | None = None
    link: str | None = None
    title: str | None = None


class SlackBookmarksEditRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackBookmarksEditHeader = Field(default_factory=SlackBookmarksEditHeader)
    body: SlackBookmarksEditBody


class SlackBookmarksListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackBookmarksListBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel_id: str


class SlackBookmarksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackBookmarksListHeader = Field(default_factory=SlackBookmarksListHeader)
    body: SlackBookmarksListBody


class SlackBookmarksRemoveHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackBookmarksRemoveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    bookmark_id: str
    channel_id: str
    quip_section_id: str | None = None


class SlackBookmarksRemoveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackBookmarksRemoveHeader = Field(default_factory=SlackBookmarksRemoveHeader)
    body: SlackBookmarksRemoveBody


class SlackChatDeleteHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackChatDeleteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    ts: str
    as_user: bool = True


class SlackChatDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackChatDeleteHeader = Field(default_factory=SlackChatDeleteHeader)
    body: SlackChatDeleteBody


class SlackChatDeleteScheduledMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackChatDeleteScheduledMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    scheduled_message_id: str
    as_user: bool = True


class SlackChatDeleteScheduledMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackChatDeleteScheduledMessageHeader = Field(
        default_factory=SlackChatDeleteScheduledMessageHeader
    )
    body: SlackChatDeleteScheduledMessageBody


class SlackChatPostEphemeralHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackChatPostEphemeralBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    user: str
    text: str | None = None
    attachments: str | None = None
    blocks: str | None = None
    as_user: bool = True
    icon_emoji: str | None = None
    icon_url: str | None = None
    link_names: bool | None = None
    markdown_text: str | None = None
    parse: str | None = None
    thread_ts: str | None = None
    username: str | None = None


class SlackChatPostEphemeralRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackChatPostEphemeralHeader = Field(default_factory=SlackChatPostEphemeralHeader)
    body: SlackChatPostEphemeralBody


class SlackChatPostMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackChatPostMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    text: str | None = None
    blocks: list[Any] | None = None
    attachments: str | None = None
    markdown_text: str | None = None
    thread_ts: str | None = None
    reply_broadcast: bool = False
    mrkdwn: bool = True
    parse: Literal["none", "full"] | None = None
    unfurl_links: bool | None = None
    unfurl_media: bool | None = None
    username: str | None = None
    icon_emoji: str | None = None
    icon_url: str | None = None
    link_names: bool | None = None


class SlackChatPostMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackChatPostMessageHeader = Field(default_factory=SlackChatPostMessageHeader)
    body: SlackChatPostMessageBody


class SlackChatScheduleMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackChatScheduleMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    post_at: int
    text: str | None = None
    blocks: str | None = None
    attachments: str | None = None
    markdown_text: str | None = None
    as_user: bool = True
    link_names: bool | None = None
    metadata: str | None = None
    parse: Literal["none", "full"] | None = None
    reply_broadcast: bool | None = None
    thread_ts: str | None = None
    unfurl_links: bool | None = None
    unfurl_media: bool | None = None


class SlackChatScheduleMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackChatScheduleMessageHeader = Field(default_factory=SlackChatScheduleMessageHeader)
    body: SlackChatScheduleMessageBody


class SlackChatScheduledMessagesListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackChatScheduledMessagesListBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str | None = None
    cursor: str | None = None
    latest: str | None = None
    limit: int = 100
    oldest: str | None = None
    team_id: str | None = None


class SlackChatScheduledMessagesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackChatScheduledMessagesListHeader = Field(
        default_factory=SlackChatScheduledMessagesListHeader
    )
    body: SlackChatScheduledMessagesListBody | None = None


class SlackChatUpdateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackChatUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    ts: str
    text: str | None = None
    blocks: str | None = None
    attachments: str | None = None
    as_user: bool = True
    file_ids: list[Any] | None = None
    link_names: bool | None = None
    markdown_text: str | None = None
    metadata: str | None = None
    parse: Literal["none", "full"] | None = None
    reply_broadcast: bool = False


class SlackChatUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackChatUpdateHeader = Field(default_factory=SlackChatUpdateHeader)
    body: SlackChatUpdateBody


class SlackConversationsArchiveHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsArchiveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str


class SlackConversationsArchiveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsArchiveHeader = Field(default_factory=SlackConversationsArchiveHeader)
    body: SlackConversationsArchiveBody


class SlackConversationsCloseHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsCloseBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str


class SlackConversationsCloseRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsCloseHeader = Field(default_factory=SlackConversationsCloseHeader)
    body: SlackConversationsCloseBody


class SlackConversationsCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    is_private: bool | None = None
    team_id: str | None = None


class SlackConversationsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsCreateHeader = Field(default_factory=SlackConversationsCreateHeader)
    body: SlackConversationsCreateBody


class SlackConversationsHistoryQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    cursor: str | None = None
    include_all_metadata: bool = False
    inclusive: bool = False
    latest: str = "now"
    limit: int = 100
    oldest: str = "0"


class SlackConversationsHistoryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackConversationsHistoryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackConversationsHistoryQuery
    header: SlackConversationsHistoryHeader = Field(default_factory=SlackConversationsHistoryHeader)


class SlackConversationsInfoQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    include_locale: bool = True
    include_num_members: bool = False


class SlackConversationsInfoHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackConversationsInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackConversationsInfoQuery
    header: SlackConversationsInfoHeader = Field(default_factory=SlackConversationsInfoHeader)


class SlackConversationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    exclude_archived: bool = False
    limit: int = 100
    team_id: str | None = None
    types: list[Any] | None = None


class SlackConversationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackConversationsListQuery | None = None


class SlackConversationsMembersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    cursor: str | None = None
    limit: int = 100


class SlackConversationsMembersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackConversationsMembersQuery
    header: SlackConversationsMembersHeader = Field(default_factory=SlackConversationsMembersHeader)


class SlackConversationsOpenHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsOpenBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str | None = None
    return_im: bool | None = None
    users: str | None = None
    prevent_creation: bool = True


class SlackConversationsOpenRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsOpenHeader = Field(default_factory=SlackConversationsOpenHeader)
    body: SlackConversationsOpenBody = Field(default_factory=SlackConversationsOpenBody)


class SlackConversationsRenameHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsRenameBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    name: str


class SlackConversationsRenameRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsRenameHeader = Field(default_factory=SlackConversationsRenameHeader)
    body: SlackConversationsRenameBody


class SlackConversationsRepliesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    token: str | None = None
    channel: str
    ts: str
    cursor: str | None = None
    include_all_metadata: bool = False
    inclusive: bool = False
    latest: str = "now"
    limit: int = 100
    oldest: str = "0"


class SlackConversationsRepliesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackConversationsRepliesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackConversationsRepliesQuery
    header: SlackConversationsRepliesHeader = Field(default_factory=SlackConversationsRepliesHeader)


class SlackConversationsSetPurposeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsSetPurposeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    purpose: str


class SlackConversationsSetPurposeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsSetPurposeHeader = Field(
        default_factory=SlackConversationsSetPurposeHeader
    )
    body: SlackConversationsSetPurposeBody


class SlackConversationsSetTopicHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsSetTopicBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    topic: str


class SlackConversationsSetTopicRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsSetTopicHeader = Field(
        default_factory=SlackConversationsSetTopicHeader
    )
    body: SlackConversationsSetTopicBody


class SlackConversationsUnarchiveHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackConversationsUnarchiveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str


class SlackConversationsUnarchiveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackConversationsUnarchiveHeader = Field(
        default_factory=SlackConversationsUnarchiveHeader
    )
    body: SlackConversationsUnarchiveBody


class SlackEmojiListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_categories: bool = False


class SlackEmojiListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackEmojiListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackEmojiListQuery | None = None
    header: SlackEmojiListHeader = Field(default_factory=SlackEmojiListHeader)


class SlackPinsAddHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackPinsAddBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    timestamp: str | None = None


class SlackPinsAddRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackPinsAddHeader = Field(default_factory=SlackPinsAddHeader)
    body: SlackPinsAddBody


class SlackPinsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    token: str | None = None
    channel: str


class SlackPinsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackPinsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackPinsListQuery
    header: SlackPinsListHeader = Field(default_factory=SlackPinsListHeader)


class SlackPinsRemoveHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackPinsRemoveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    timestamp: str | None = None


class SlackPinsRemoveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackPinsRemoveHeader = Field(default_factory=SlackPinsRemoveHeader)
    body: SlackPinsRemoveBody


class SlackReactionsAddHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackReactionsAddBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str
    name: str
    timestamp: str


class SlackReactionsAddRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackReactionsAddHeader = Field(default_factory=SlackReactionsAddHeader)
    body: SlackReactionsAddBody


class SlackReactionsGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    channel: str | None = None
    file: str | None = None
    file_comment: str | None = None
    full: bool | None = None
    timestamp: str | None = None


class SlackReactionsGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackReactionsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackReactionsGetQuery = Field(default_factory=SlackReactionsGetQuery)
    header: SlackReactionsGetHeader = Field(default_factory=SlackReactionsGetHeader)


class SlackReactionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    count: int = 100
    cursor: str | None = None
    full: bool | None = None
    limit: int = 0
    page: int = 1
    team_id: str | None = None
    user: str | None = None


class SlackReactionsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackReactionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackReactionsListQuery = Field(default_factory=SlackReactionsListQuery)
    header: SlackReactionsListHeader = Field(default_factory=SlackReactionsListHeader)


class SlackReactionsRemoveHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackReactionsRemoveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    channel: str | None = None
    file: str | None = None
    file_comment: str | None = None
    timestamp: str | None = None


class SlackReactionsRemoveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackReactionsRemoveHeader = Field(default_factory=SlackReactionsRemoveHeader)
    body: SlackReactionsRemoveBody


class SlackRemindersInfoQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reminder: str
    team_id: str | None = None


class SlackRemindersInfoHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackRemindersInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackRemindersInfoQuery
    header: SlackRemindersInfoHeader = Field(default_factory=SlackRemindersInfoHeader)


class SlackRemindersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str | None = None


class SlackRemindersListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackRemindersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackRemindersListQuery | None = None
    header: SlackRemindersListHeader = Field(default_factory=SlackRemindersListHeader)


class SlackSearchAllQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    count: int = 20
    highlight: bool = False
    page: int = 1
    sort: Literal["score", "timestamp"] = "score"
    sort_dir: Literal["asc", "desc"] = "desc"
    team_id: str | None = None


class SlackSearchAllHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackSearchAllRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackSearchAllQuery
    header: SlackSearchAllHeader = Field(default_factory=SlackSearchAllHeader)


class SlackSearchFilesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    count: int = 20
    highlight: bool | None = None
    page: int = 1
    sort: Literal["score", "timestamp"] = "score"
    sort_dir: Literal["asc", "desc"] = "desc"
    team_id: str | None = None


class SlackSearchFilesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackSearchFilesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackSearchFilesQuery
    header: SlackSearchFilesHeader = Field(default_factory=SlackSearchFilesHeader)


class SlackSearchMessagesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    count: int = 20
    cursor: str | None = None
    highlight: bool | None = None
    page: int = 1
    sort: Literal["score", "timestamp"] = "score"
    sort_dir: Literal["asc", "desc"] = "desc"
    team_id: str | None = None


class SlackSearchMessagesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackSearchMessagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackSearchMessagesQuery
    header: SlackSearchMessagesHeader = Field(default_factory=SlackSearchMessagesHeader)


class SlackUsersConversationsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    exclude_archived: bool = False
    limit: int = 100
    team_id: str | None = None
    types: str = "public_channel"
    user: str | None = None


class SlackUsersConversationsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackUsersConversationsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackUsersConversationsQuery | None = None
    header: SlackUsersConversationsHeader = Field(default_factory=SlackUsersConversationsHeader)


class SlackUsersGetPresenceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str | None = None


class SlackUsersGetPresenceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackUsersGetPresenceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackUsersGetPresenceQuery | None = None
    header: SlackUsersGetPresenceHeader = Field(default_factory=SlackUsersGetPresenceHeader)


class SlackUsersInfoQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str


class SlackUsersInfoHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackUsersInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackUsersInfoQuery
    header: SlackUsersInfoHeader = Field(default_factory=SlackUsersInfoHeader)


class SlackUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    include_locale: bool = False
    limit: int = 0
    team_id: str | None = None


class SlackUsersListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class SlackUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SlackUsersListQuery = Field(default_factory=SlackUsersListQuery)
    header: SlackUsersListHeader = Field(default_factory=SlackUsersListHeader)


class SlackUsersSetPresenceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class SlackUsersSetPresenceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    presence: Literal["auto", "away"]


class SlackUsersSetPresenceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SlackUsersSetPresenceHeader = Field(default_factory=SlackUsersSetPresenceHeader)
    body: SlackUsersSetPresenceBody


class SlackNamespace:
    """Typed accessor for the `slack` app. Reach it via `client.apps.slack`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def bookmarks_add(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackBookmarksAddRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new bookmark (link) to a Slack channel, making it visible to all channel members. Use this to pin important links, documents, or resources for team reference. The bookmark is immediately visible.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.bookmarks_add", owner_id, args, account_id=account_id
        )

    async def bookmarks_edit(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackBookmarksEditRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing bookmark's title, link, or emoji in a Slack channel. Changes are immediately visible to all channel members. Use this to correct or update pinned resources.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.bookmarks_edit", owner_id, args, account_id=account_id
        )

    async def bookmarks_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackBookmarksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all bookmarks in a specific Slack channel, including their titles, URLs, and types. Use this to review pinned resources or navigate channel bookmarks. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.bookmarks_list", owner_id, args, account_id=account_id
        )

    async def bookmarks_remove(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackBookmarksRemoveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Removes a bookmark from a Slack channel, making it no longer visible to members. Use this to clean up outdated or irrelevant pinned items. The bookmark is permanently removed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.bookmarks_remove", owner_id, args, account_id=account_id
        )

    async def chat_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackChatDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a message from a Slack conversation. The message is removed from the channel history for all members. Use this to remove inappropriate, outdated, or incorrect messages. This operation is irreversible.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.chat_delete", owner_id, args, account_id=account_id
        )

    async def chat_delete_scheduled_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackChatDeleteScheduledMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Cancels and removes a previously scheduled message from the queue before it is sent. Use this to prevent a scheduled message from being posted. This operation is only effective before the scheduled time.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.chat_delete_scheduled_message", owner_id, args, account_id=account_id
        )

    async def chat_post_ephemeral(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackChatPostEphemeralRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends an ephemeral message to a specific user in a channel, visible only to that user. Use this for personal notifications, command responses, or context that should not persist in the channel history. No other channel members can see this message.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.chat_post_ephemeral", owner_id, args, account_id=account_id
        )

    async def chat_post_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackChatPostMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Posts a message to a Slack channel, private group, or DM conversation. The message can include text, blocks, attachments, and be sent as a threaded reply. Use this to send notifications, alerts, or any communication to Slack users. The message is immediately visible to all channel members.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.chat_post_message", owner_id, args, account_id=account_id
        )

    async def chat_schedule_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackChatScheduleMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Schedules a message to be automatically posted to a Slack channel at a specified future time (up to 120 days ahead). Use this for delayed notifications, reminders, or timed announcements. The message can be deleted before sending using the delete scheduled message function.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.chat_schedule_message", owner_id, args, account_id=account_id
        )

    async def chat_scheduled_messages_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackChatScheduledMessagesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all currently scheduled messages, optionally filtered by channel. Use this to review pending scheduled messages before deleting or modifying them. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.chat_scheduled_messages_list", owner_id, args, account_id=account_id
        )

    async def chat_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackChatUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the text, blocks, or attachments of an existing message in a Slack channel. The original timestamp is preserved but the content is replaced. Use this to correct errors or update dynamic content in previously sent messages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.chat_update", owner_id, args, account_id=account_id
        )

    async def conversations_archive(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsArchiveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Archives a conversation, removing it from the active channel list for all members. Archived conversations can be unarchived later, restoring full history and membership. Use this to clean up inactive channels without permanent data loss.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_archive", owner_id, args, account_id=account_id
        )

    async def conversations_close(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsCloseRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Closes a direct message or multi-person direct message conversation. This removes the DM from the user's sidebar and marks it as closed. Use this to clean up DM channels that are no longer needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_close", owner_id, args, account_id=account_id
        )

    async def conversations_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new public or private channel in the Slack workspace. Once created, the channel is immediately visible to all members and ready for use. Use this when setting up a new workspace structure or organizing topics. Channel creation is reversible via archiving.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_create", owner_id, args, account_id=account_id
        )

    async def conversations_history(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsHistoryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the message history of a conversation, with optional time range, pagination, and metadata inclusion. Use this to review past messages, search for context, or audit channel activity. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_history", owner_id, args, account_id=account_id
        )

    async def conversations_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific conversation, including its name, purpose, topic, member count, and locale settings. Use this to inspect channel metadata before performing channel operations. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_info", owner_id, args, account_id=account_id
        )

    async def conversations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all conversations (public channels, private channels, DMs, MPIMs) accessible to the authenticated bot or user. Use this to discover available channels before sending messages or searching history. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_list", owner_id, args, account_id=account_id
        )

    async def conversations_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of member user IDs for a given conversation. Use this to determine who is in a channel before sending targeted messages or checking membership. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_members", owner_id, args, account_id=account_id
        )

    async def conversations_open(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsOpenRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Opens or resumes a direct message or multi-person direct message channel. If the DM does not exist, it will be created. Use this to initiate a DM conversation with one or more users before posting messages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_open", owner_id, args, account_id=account_id
        )

    async def conversations_rename(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsRenameRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Renames an existing conversation to a new name. The new name is immediately visible to all members and updates in all channel references. Use this to reorganize channel naming conventions. The operation is reversible by renaming again.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_rename", owner_id, args, account_id=account_id
        )

    async def conversations_replies(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsRepliesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all messages in a thread (replies to a parent message) from a conversation. Use this to read the full context of a threaded discussion. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_replies", owner_id, args, account_id=account_id
        )

    async def conversations_set_purpose(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsSetPurposeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the purpose (description) of a conversation, which appears in the channel header and member information. Use this to communicate the channel's intended use to all members. This is a visible but reversible operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_set_purpose", owner_id, args, account_id=account_id
        )

    async def conversations_set_topic(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsSetTopicRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sets the topic for a conversation, which is displayed prominently in the channel header. Use this to highlight current topics or important announcements. This is a visible but easily reversible operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_set_topic", owner_id, args, account_id=account_id
        )

    async def conversations_unarchive(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackConversationsUnarchiveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously archived conversation, making it visible and active again for all former members. Use this to reactivate a channel that was archived but is needed again. This reverses an archive operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.conversations_unarchive", owner_id, args, account_id=account_id
        )

    async def emoji_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackEmojiListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all custom emoji registered for the Slack team, with optional Unicode emoji category breakdowns. Use this to discover available custom emoji before using them in reactions or messages. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("slack.emoji_list", owner_id, args, account_id=account_id)

    async def pins_add(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackPinsAddRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Pins a specific message to a Slack channel, making it prominently visible to all members at the top of the channel. Use this to highlight important announcements or reference material.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("slack.pins_add", owner_id, args, account_id=account_id)

    async def pins_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackPinsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all items currently pinned to a specific Slack channel, including messages and files. Use this to review important pinned content. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("slack.pins_list", owner_id, args, account_id=account_id)

    async def pins_remove(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackPinsRemoveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Unpins a previously pinned message from a Slack channel, removing it from the pinned items area. Use this to clean up outdated pins. Only the pinned indicator is removed; the original message remains in the channel.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.pins_remove", owner_id, args, account_id=account_id
        )

    async def reactions_add(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackReactionsAddRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a reaction emoji to a message in a Slack channel. The reaction is immediately visible to all channel members. Use this to acknowledge, react to, or vote on messages without posting a reply.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.reactions_add", owner_id, args, account_id=account_id
        )

    async def reactions_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackReactionsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Gets all reactions (emoji) on a specific item such as a channel message, file, or file comment. Use this to check engagement on a message or review reactions. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.reactions_get", owner_id, args, account_id=account_id
        )

    async def reactions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackReactionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all reactions made by a user (defaults to the authenticated user), with pagination support. Use this to audit a user's reaction history across the workspace. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.reactions_list", owner_id, args, account_id=account_id
        )

    async def reactions_remove(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackReactionsRemoveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Removes a reaction emoji from a message, file, or file comment. Use this to undo a reaction that was added in error. Only the specified reaction is removed; other reactions remain.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.reactions_remove", owner_id, args, account_id=account_id
        )

    async def reminders_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackRemindersInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Gets detailed information about a specific reminder, including its time, text, and recurrence. Use this to inspect a reminder before modifying or deleting it. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.reminders_info", owner_id, args, account_id=account_id
        )

    async def reminders_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackRemindersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all reminders created by or for the authenticated user. Use this to review upcoming reminders before creating or deleting them. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.reminders_list", owner_id, args, account_id=account_id
        )

    async def search_all(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackSearchAllRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches across all Slack content including messages and files using a query string with boolean operators. Use this for comprehensive workspace search when you need both messages and file results together. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("slack.search_all", owner_id, args, account_id=account_id)

    async def search_files(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackSearchFilesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for files in Slack matching a query string, with sorting and pagination support. Use this to find documents, images, or other files shared in the workspace. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.search_files", owner_id, args, account_id=account_id
        )

    async def search_messages(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackSearchMessagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for messages in Slack matching a query string, supporting boolean operators and cursor pagination. Use this to find conversations, decisions, or information shared in messages. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.search_messages", owner_id, args, account_id=account_id
        )

    async def users_conversations(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackUsersConversationsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all conversations that the calling user is a member of, with optional filtering by conversation type. Use this to discover which channels a specific user can access. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.users_conversations", owner_id, args, account_id=account_id
        )

    async def users_get_presence(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackUsersGetPresenceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the current presence status (active, away, or auto) for a Slack user. Use this to check if a user is online before sending time-sensitive messages. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.users_get_presence", owner_id, args, account_id=account_id
        )

    async def users_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackUsersInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Gets detailed profile information about a specific Slack user, including name, email, timezone, and status. Use this to look up user details by user ID. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("slack.users_info", owner_id, args, account_id=account_id)

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all users in the Slack workspace with their profile information, including name, email, and display name. Use this to discover workspace members, resolve user IDs, or build user directories. This is a read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("slack.users_list", owner_id, args, account_id=account_id)

    async def users_set_presence(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SlackUsersSetPresenceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Manually sets the authenticated user's presence to either 'auto' (automatic based on activity) or 'away' (manually marked away). Use this to indicate availability status. This only affects the calling user's presence.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "slack.users_set_presence", owner_id, args, account_id=account_id
        )
