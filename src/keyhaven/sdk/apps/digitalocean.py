# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `digitalocean` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DigitaloceanAppsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class DigitaloceanAppsGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None


class DigitaloceanAppsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DigitaloceanAppsGetPath
    query: DigitaloceanAppsGetQuery | None = None


class DigitaloceanAppsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    with_projects: bool | None = None


class DigitaloceanAppsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DigitaloceanAppsListQuery | None = None


class DigitaloceanDatabasesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tag_name: str | None = None


class DigitaloceanDatabasesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DigitaloceanDatabasesListQuery | None = None


class DigitaloceanMyBalanceListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class DigitaloceanMyInvoicesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    invoice_uuid: str


class DigitaloceanMyInvoicesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DigitaloceanMyInvoicesGetPath


class DigitaloceanNamespace:
    """Typed accessor for the `digitalocean` app. Reach it via `client.apps.digitalocean`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def apps_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DigitaloceanAppsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch the full configuration, deployment history, service specs, and health status of a single App Platform application by its unique ID. Use this when an agent needs to inspect a specific app's settings, debug a failed deployment, or retrieve connection details for its services.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "digitalocean.apps_get", owner_id, args, account_id=account_id
        )

    async def apps_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DigitaloceanAppsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a list of all App Platform applications deployed on the DigitalOcean account, optionally filtered by associated project. Returns each app's ID, name, region, tier, and last deployment status. Use this when an agent needs to discover, inventory, or select an app for further inspection or management.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "digitalocean.apps_list", owner_id, args, account_id=account_id
        )

    async def databases_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DigitaloceanDatabasesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all managed database clusters on the DigitalOcean account, optionally filtered by tag. Returns each cluster's name, engine type (PostgreSQL, MySQL, Redis, MongoDB), region, node size, number of nodes, and status. Use this to inventory databases or locate a specific cluster before performing further operations on it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "digitalocean.databases_list", owner_id, args, account_id=account_id
        )

    async def my_balance_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DigitaloceanMyBalanceListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the current account billing balance, including month-to-date usage charges, credits, promo codes, and the net balance owed. This exposes sensitive financial data tied to the billing account. Use it to monitor spending, check remaining credit, or verify billing status before provisioning new resources.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "digitalocean.my_balance_list", owner_id, args, account_id=account_id
        )

    async def my_invoices_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DigitaloceanMyInvoicesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a specific DigitalOcean invoice by its UUID, including itemised line items, total amount charged, discounts, taxes, and payment status. This exposes historical billing and cost breakdowns. Use it when an agent needs to audit charges, download invoice data for accounting, or verify payment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "digitalocean.my_invoices_get", owner_id, args, account_id=account_id
        )
