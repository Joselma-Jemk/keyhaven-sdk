# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `customer_io` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CustomerIoCampaignTriggerPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    campaign_id: str


class CustomerIoCampaignTriggerBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any] | None = None
    recipients: list[Any] | None = None


class CustomerIoCampaignTriggerRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CustomerIoCampaignTriggerPath
    body: CustomerIoCampaignTriggerBody | None = None


class CustomerIoCampaignsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    start: str | None = None


class CustomerIoCampaignsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CustomerIoCampaignsListQuery | None = None


class CustomerIoCustomerCreateOrUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class CustomerIoCustomerCreateOrUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str
    created_at: int | None = None
    first_name: str | None = None
    last_name: str | None = None


class CustomerIoCustomerCreateOrUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CustomerIoCustomerCreateOrUpdatePath
    body: CustomerIoCustomerCreateOrUpdateBody


class CustomerIoCustomerEventTrackPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class CustomerIoCustomerEventTrackBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    data: dict[str, Any] | None = None


class CustomerIoCustomerEventTrackRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CustomerIoCustomerEventTrackPath
    body: CustomerIoCustomerEventTrackBody


class CustomerIoSegmentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None


class CustomerIoSegmentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CustomerIoSegmentsListQuery | None = None


class CustomerIoTransactionalEmailSendBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    transactional_message_id: str
    to: str
    identifiers: dict[str, Any]
    message_data: dict[str, Any] | None = None


class CustomerIoTransactionalEmailSendRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CustomerIoTransactionalEmailSendBody


class CustomerIoNamespace:
    """Typed accessor for the `customer_io` app. Reach it via `client.apps.customer_io`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def campaign_trigger(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CustomerIoCampaignTriggerRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Manually trigger a Customer.io campaign for one or more specific customer recipients immediately, bypassing normal automation schedules. Accepts a campaign ID and a list of recipient identifiers, plus optional custom data for message personalization. This causes the campaign to execute its flow (email, push, webhook) for those recipients right away. Use for one-off broadcasts, re-sending to customers who missed the original, or when you need to force a campaign outside its regular trigger conditions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "customer_io.campaign_trigger", owner_id, args, account_id=account_id
        )

    async def campaigns_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CustomerIoCampaignsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all campaigns configured in the Customer.io workspace, including their names, statuses (draft, sent, stopped), types (email, push, webhook), and creation timestamps, with optional pagination. Returns metadata needed to identify and reference specific campaigns. Use to discover available campaigns, check whether a campaign is active, or find a campaign ID to use with the campaign trigger action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "customer_io.campaigns_list", owner_id, args, account_id=account_id
        )

    async def customer_create_or_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CustomerIoCustomerCreateOrUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new customer profile or update an existing one in Customer.io by their unique identifier. Accepts email, first name, last name, and a created_at timestamp. This profile is the foundation for all segmentation, campaigns, and event tracking in the workspace. Use when onboarding new users from your application, syncing profile changes from your data source, or ensuring a customer exists before tracking events against them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "customer_io.customer_create_or_update", owner_id, args, account_id=account_id
        )

    async def customer_event_track(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CustomerIoCustomerEventTrackRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Record a custom event for a specific customer in Customer.io, such as page_visited, email_opened, or purchase_completed, with an optional data payload. Events trigger automated campaign workflows, segmentation recalculations, and can immediately cause email sends, push notifications, or webhooks based on workspace rules. Use when you need to log user activity that should drive marketing automation or real-time customer engagement.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "customer_io.customer_event_track", owner_id, args, account_id=account_id
        )

    async def segments_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CustomerIoSegmentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all customer segments in the Customer.io workspace with their names and membership counts. Segments are dynamic groups of customers defined by attributes, events, or behaviors, and are used as targeting criteria for campaigns. Use to understand your audience structure, verify which segments have active members, or find a segment ID for campaign targeting or analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "customer_io.segments_list", owner_id, args, account_id=account_id
        )

    async def transactional_email_send(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CustomerIoTransactionalEmailSendRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Send a transactional email to a single recipient using a predefined message template, with template variable data for personalization. Transactional emails bypass unsubscribe rules and are intended for operational messages such as order confirmations, password resets, receipts, and account notifications. Use when you need to deliver a critical one-to-one email immediately, independent of campaign schedules or automation workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "customer_io.transactional_email_send", owner_id, args, account_id=account_id
        )
