# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `lattice` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class LatticeGoalsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    startingAfter: str | None = None


class LatticeGoalsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LatticeGoalsListQuery | None = None


class LatticeReviewCycleReviewsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class LatticeReviewCycleReviewsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    startingAfter: str | None = None


class LatticeReviewCycleReviewsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LatticeReviewCycleReviewsListPath
    query: LatticeReviewCycleReviewsListQuery | None = None


class LatticeReviewCyclesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    startingAfter: str | None = None


class LatticeReviewCyclesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LatticeReviewCyclesListQuery | None = None


class LatticeUserGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class LatticeUserGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LatticeUserGetPath


class LatticeUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    startingAfter: str | None = None
    status: str | None = None


class LatticeUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LatticeUsersListQuery | None = None


class LatticeNamespace:
    """Typed accessor for the `lattice` app. Reach it via `client.apps.lattice`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def goals_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LatticeGoalsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of goals tracked in Lattice, with cursor-based pagination. Each goal includes the title, description, owner, due date, status, and progress percentage. This operation is read-only and does not modify any data. Use this to review team or company-wide objectives and track progress toward key results across the organization.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lattice.goals_list", owner_id, args, account_id=account_id
        )

    async def review_cycle_reviews_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LatticeReviewCycleReviewsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all individual reviews within a specific review cycle, identified by the cycle ID. Each review includes the reviewer, reviewee, status, due date, and response timestamps. This operation is read-only and does not modify any data. Use this to see who has completed their reviews and who still needs to submit within a given cycle.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lattice.review_cycle_reviews_list", owner_id, args, account_id=account_id
        )

    async def review_cycles_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LatticeReviewCyclesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of performance review cycles in your Lattice organization, with cursor-based pagination. Each cycle includes its name, status (open, closed, upcoming), start and end dates, and submission deadlines. This operation is read-only and does not modify any data. Use this to identify active or historical review periods for performance management analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lattice.review_cycles_list", owner_id, args, account_id=account_id
        )

    async def user_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LatticeUserGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full profile of a single Lattice user by their unique identifier, including their role, manager chain, department, start date, and custom fields. This provides the most complete view of an employee's Lattice profile. This operation is read-only and does not modify any data. Use this to inspect detailed information about a specific team member after obtaining their ID.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("lattice.user_get", owner_id, args, account_id=account_id)

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LatticeUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of users in your Lattice organization, with optional filtering by active or inactive status and cursor-based pagination. Each user record includes name, email, department, manager, and job title. This operation is read-only and does not modify any data. Use this to browse the company directory or find a specific user ID for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lattice.users_list", owner_id, args, account_id=account_id
        )
