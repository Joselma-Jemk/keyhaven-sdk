# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `bitbucket` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BitbucketPullrequestCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace: str
    repo_slug: str


class BitbucketPullrequestCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    description: str | None = None
    source: dict[str, Any]
    destination: dict[str, Any] | None = None
    close_source_branch: bool | None = None
    draft: bool | None = None
    reviewers: list[Any] | None = None


class BitbucketPullrequestCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitbucketPullrequestCreatePath
    body: BitbucketPullrequestCreateBody


class BitbucketPullrequestsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace: str
    repo_slug: str


class BitbucketPullrequestsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    state: str | None = None
    page: int | None = None
    pagelen: int | None = None


class BitbucketPullrequestsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitbucketPullrequestsListPath
    query: BitbucketPullrequestsListQuery | None = None


class BitbucketRepositoriesWorkspaceListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace: str


class BitbucketRepositoriesWorkspaceListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    role: str | None = None
    q: str | None = None
    sort: str | None = None
    page: int | None = None
    pagelen: int | None = None


class BitbucketRepositoriesWorkspaceListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitbucketRepositoriesWorkspaceListPath
    query: BitbucketRepositoriesWorkspaceListQuery | None = None


class BitbucketRepositoryCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace: str
    repo_slug: str


class BitbucketRepositoryCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    scm: str
    is_private: bool | None = None
    name: str | None = None
    description: str | None = None
    fork_policy: str | None = None
    language: str | None = None
    mainbranch: str | None = None
    project: dict[str, Any] | None = None


class BitbucketRepositoryCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitbucketRepositoryCreatePath
    body: BitbucketRepositoryCreateBody


class BitbucketRepositoryGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace: str
    repo_slug: str


class BitbucketRepositoryGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BitbucketRepositoryGetPath


class BitbucketNamespace:
    """Typed accessor for the `bitbucket` app. Reach it via `client.apps.bitbucket`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def pullrequest_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitbucketPullrequestCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new pull request against a destination branch in the specified repository, with a required title and source branch (or commit). Supports optional draft mode, reviewer assignment, description, and automatic source branch cleanup on merge. The PR is immediately visible to repository members and sends notifications to assigned reviewers. An agent should use this to submit code for review, automate release or dependency-update PRs, but should verify the source and target branches before calling.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitbucket.pullrequest_create", owner_id, args, account_id=account_id
        )

    async def pullrequests_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitbucketPullrequestsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists pull requests for a given repository, optionally filtered by state (OPEN, MERGED, DECLINED, SUPERSEDED). Returns pull request metadata including title, author, source and destination branches, reviewers, and timestamps. An agent should use this to find open PRs needing review, check the status of existing changes, or gather history for reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitbucket.pullrequests_list", owner_id, args, account_id=account_id
        )

    async def repositories_workspace_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitbucketRepositoriesWorkspaceListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all repositories in a given Bitbucket workspace. Results can be filtered by the authenticated user's role (member, contributor, admin, owner) or by a BBQL search query matching name, description, and language. An agent should use this to discover available repositories, find a specific project by name, or inventory code assets before taking further actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitbucket.repositories_workspace_list", owner_id, args, account_id=account_id
        )

    async def repository_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitbucketRepositoryCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Git repository inside the specified Bitbucket workspace with a given slug and optional configuration (visibility, description, fork policy, language, main branch, and project assignment). The repository becomes immediately visible to workspace members and, if set to public, to anyone on the internet. An agent should use this to scaffold a new project or automate provisioning, but should verify the workspace and privacy settings before calling this action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitbucket.repository_create", owner_id, args, account_id=account_id
        )

    async def repository_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BitbucketRepositoryGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single Bitbucket repository identified by its workspace and repo slug. Returns metadata including the repository name, description, visibility (public/private), fork policy, main branch, and project assignment. An agent should use this to inspect a specific repository's configuration before deciding to clone, fork, or modify it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bitbucket.repository_get", owner_id, args, account_id=account_id
        )
