# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `linkup` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class LinkupGetCreditsBalanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class LinkupSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    q: str
    depth: Literal["standard", "deep"] = "standard"
    outputType: Literal["sourcedAnswer", "searchResults", "structured"] = "sourcedAnswer"
    structuredOutputSchema: str | None = None
    includeImages: bool = False
    fromDate: str | None = None
    toDate: str | None = None
    excludeDomains: list[Any] | None = None
    includeDomains: list[Any] | None = None


class LinkupSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LinkupSearchBody


class LinkupNamespace:
    """Typed accessor for the `linkup` app. Reach it via `client.apps.linkup`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_credits_balance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkupGetCreditsBalanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Queries the /v1/credits/balance endpoint to retrieve the remaining API credit balance on the user's Linkup account. The response contains a numeric value representing how many credits are available for future search requests — each search consumes a certain number of credits depending on depth and options. An agent should use this before performing searches to verify sufficient credits exist, or to report account usage to the user. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "linkup.get_credits_balance", owner_id, args, account_id=account_id
        )

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkupSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submits a natural-language query to Linkup's web search API and returns a synthesized answer grounded in real-time online sources with citations. It can also return raw search results or structured data conforming to a user-provided JSON schema. Depth, date range, image inclusion, and domain allow/block lists are configurable. An agent should use this when it needs current web information, fact-checking, or research beyond the LLM's training data — the result is a read-only retrieval with no side effects on the system or external services.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("linkup.search", owner_id, args, account_id=account_id)
