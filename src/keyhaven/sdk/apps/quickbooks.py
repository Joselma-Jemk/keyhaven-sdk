# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `quickbooks` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class QuickbooksCustomerCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    companyId: str


class QuickbooksCustomerCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    minorversion: int | None = None


class QuickbooksCustomerCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class QuickbooksCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    DisplayName: str | None = None
    GivenName: str | None = None
    FamilyName: str | None = None
    CompanyName: str | None = None
    PrimaryEmailAddr: dict[str, Any] | None = None
    PrimaryPhone: dict[str, Any] | None = None
    BillAddr: dict[str, Any] | None = None


class QuickbooksCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbooksCustomerCreatePath
    query: QuickbooksCustomerCreateQuery | None = None
    header: QuickbooksCustomerCreateHeader | None = None
    body: QuickbooksCustomerCreateBody = Field(default_factory=QuickbooksCustomerCreateBody)


class QuickbooksCustomerGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    companyId: str
    customerId: str


class QuickbooksCustomerGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    minorversion: int | None = None


class QuickbooksCustomerGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class QuickbooksCustomerGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbooksCustomerGetPath
    query: QuickbooksCustomerGetQuery | None = None
    header: QuickbooksCustomerGetHeader | None = None


class QuickbooksInvoiceCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    companyId: str


class QuickbooksInvoiceCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    minorversion: int | None = None


class QuickbooksInvoiceCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class QuickbooksInvoiceCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    CustomerRef: dict[str, Any]
    Line: list[Any]
    DueDate: str | None = None
    TxnDate: str | None = None
    CurrencyRef: dict[str, Any] | None = None


class QuickbooksInvoiceCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbooksInvoiceCreatePath
    query: QuickbooksInvoiceCreateQuery | None = None
    header: QuickbooksInvoiceCreateHeader | None = None
    body: QuickbooksInvoiceCreateBody


class QuickbooksInvoiceGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    companyId: str
    invoiceId: str


class QuickbooksInvoiceGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    minorversion: int | None = None


class QuickbooksInvoiceGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class QuickbooksInvoiceGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbooksInvoiceGetPath
    query: QuickbooksInvoiceGetQuery | None = None
    header: QuickbooksInvoiceGetHeader | None = None


class QuickbooksPaymentCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    companyId: str


class QuickbooksPaymentCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    minorversion: int | None = None


class QuickbooksPaymentCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class QuickbooksPaymentCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    CustomerRef: dict[str, Any]
    TotalAmt: float
    Line: list[Any] | None = None
    PaymentMethodRef: dict[str, Any] | None = None
    TxnDate: str | None = None


class QuickbooksPaymentCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbooksPaymentCreatePath
    query: QuickbooksPaymentCreateQuery | None = None
    header: QuickbooksPaymentCreateHeader | None = None
    body: QuickbooksPaymentCreateBody


class QuickbooksQueryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    companyId: str


class QuickbooksQueryQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    minorversion: int | None = None


class QuickbooksQueryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class QuickbooksQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: QuickbooksQueryPath
    query: QuickbooksQueryQuery
    header: QuickbooksQueryHeader | None = None


class QuickbooksNamespace:
    """Typed accessor for the `quickbooks` app. Reach it via `client.apps.quickbooks`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbooksCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new customer record to the QuickBooks company file with provided details such as display name, contact info, and billing address. The new customer becomes immediately available for use on invoices, estimates, and sales receipts. Use this when you need to register a customer from an external form, sync contacts into QuickBooks, or create a customer on the fly before issuing an invoice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbooks.customer_create", owner_id, args, account_id=account_id
        )

    async def customer_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbooksCustomerGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full customer record — including contact information, billing address, and metadata — for a given customer ID. This is a pure lookup that does not modify any data. Use this when you need specific customer details to verify information, prepare an invoice, or reconcile a payment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbooks.customer_get", owner_id, args, account_id=account_id
        )

    async def invoice_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbooksInvoiceCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generates a new invoice for a specified customer with line items, due date, and currency. Once created, the invoice is reflected in accounts receivable and becomes visible to the customer if online invoicing is enabled. Use this to bill a customer from an order, send invoices programmatically from an external system, or automate recurring billing workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbooks.invoice_create", owner_id, args, account_id=account_id
        )

    async def invoice_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbooksInvoiceGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the complete invoice record including line items, totals, tax details, customer reference, and payment status for a given invoice ID. No data is modified — this is a read-only lookup. Use this when you need to verify invoice details, check whether an invoice has been paid, or retrieve information for customer support.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbooks.invoice_get", owner_id, args, account_id=account_id
        )

    async def payment_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbooksPaymentCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Records a payment received from a customer against one or more invoices, updating the customer's balance and marking the linked invoices as paid. This directly affects accounts receivable and cannot be undone without a reversing journal entry. Use this when processing an incoming customer payment to reconcile invoices against bank deposits or payment gateway data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "quickbooks.payment_create", owner_id, args, account_id=account_id
        )

    async def query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: QuickbooksQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes a SELECT-style QBQL (QuickBooks Query Language) statement against the company file to read any entity type — customers, invoices, items, accounts, and more. Results can be filtered, sorted, and limited using QBQL syntax in the query parameter. Use this when you need a flexible, read-only lookup across any QuickBooks entity, for example to find all active customers or retrieve unpaid invoices by a date range.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("quickbooks.query", owner_id, args, account_id=account_id)
