# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `envoy` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class EnvoyEmployeesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    status: str | None = None


class EnvoyEmployeesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EnvoyEmployeesListQuery | None = None


class EnvoyInviteCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str
    name: str
    host_id: str
    location_id: str
    expected_arrival_at: str | None = None
    expected_departure_at: str | None = None


class EnvoyInviteCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: EnvoyInviteCreateBody


class EnvoyLocationGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class EnvoyLocationGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EnvoyLocationGetPath


class EnvoyLocationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None


class EnvoyLocationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EnvoyLocationsListQuery | None = None


class EnvoyReservationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    location_id: str | None = None
    employee_email: str | None = None
    date: str | None = None


class EnvoyReservationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EnvoyReservationsListQuery | None = None


class EnvoyVisitorsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    status: str | None = None
    location_id: str | None = None


class EnvoyVisitorsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EnvoyVisitorsListQuery | None = None


class EnvoyNamespace:
    """Typed accessor for the `envoy` app. Reach it via `client.apps.envoy`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def employees_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EnvoyEmployeesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all employees in the Envoy organization with optional filtering by status (active, inactive). Each record includes the employee's name, email, and other profile details. Use this to find employee IDs for assigning hosts to visitor invitations, verify the employee roster, or look up contact information across the organization.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "envoy.employees_list", owner_id, args, account_id=account_id
        )

    async def invite_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EnvoyInviteCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a visitor invitation at a specified location with an assigned host and scheduled arrival time. The invitation notifies both the visitor and host, and the visit appears on the Envoy arrival dashboard and signage. Use this when an employee is expecting an external guest and needs to register them for building access, host notification, and front desk preparation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "envoy.invite_create", owner_id, args, account_id=account_id
        )

    async def location_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EnvoyLocationGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details of a single location by its unique identifier, including the address, time zone, operating hours, and other configuration settings. Use this to get specific location information for display purposes, validation checks, or when preparing visitor invitations that reference a particular office or site.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "envoy.location_get", owner_id, args, account_id=account_id
        )

    async def locations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EnvoyLocationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all physical locations configured in the Envoy organization, such as office buildings, campuses, and branch offices, with pagination support. Each location includes its name, address, and operational configuration. Use this to discover available locations when creating visitor invitations or filtering visitor and reservation records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "envoy.locations_list", owner_id, args, account_id=account_id
        )

    async def reservations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EnvoyReservationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists desk or room reservations in the Envoy workplace management system with optional filters for location, employee email, and date. Each reservation shows the resource booked, the employee who booked it, and the time slot. Use this to check workspace utilization, find available desks or rooms, or review an employee's booking history across locations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "envoy.reservations_list", owner_id, args, account_id=account_id
        )

    async def visitors_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EnvoyVisitorsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists visitor records with optional filtering by status (expected, checked_in, completed) and location. Each record includes the visitor's name, email, host information, and visit timestamps. Use this to review current on-site visitors, check expected arrivals for the day, or audit historical visit data across the organization.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "envoy.visitors_list", owner_id, args, account_id=account_id
        )
