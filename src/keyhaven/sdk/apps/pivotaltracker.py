# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `pivotaltracker` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PivotaltrackerProjectGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: int


class PivotaltrackerProjectGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PivotaltrackerProjectGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PivotaltrackerProjectGetPath
    header: PivotaltrackerProjectGetHeader = Field(default_factory=PivotaltrackerProjectGetHeader)


class PivotaltrackerProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 25
    offset: int = 0


class PivotaltrackerProjectsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PivotaltrackerProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PivotaltrackerProjectsListQuery | None = None
    header: PivotaltrackerProjectsListHeader = Field(
        default_factory=PivotaltrackerProjectsListHeader
    )


class PivotaltrackerStoriesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: int


class PivotaltrackerStoriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 25
    offset: int = 0
    filter: str | None = None


class PivotaltrackerStoriesListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PivotaltrackerStoriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PivotaltrackerStoriesListPath
    query: PivotaltrackerStoriesListQuery | None = None
    header: PivotaltrackerStoriesListHeader = Field(default_factory=PivotaltrackerStoriesListHeader)


class PivotaltrackerStoryCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: int


class PivotaltrackerStoryCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PivotaltrackerStoryCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    story_type: str | None = None
    estimate: int | None = None
    labels: list[Any] | None = None
    current_state: str | None = None
    owned_by_ids: list[Any] | None = None


class PivotaltrackerStoryCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PivotaltrackerStoryCreatePath
    header: PivotaltrackerStoryCreateHeader = Field(default_factory=PivotaltrackerStoryCreateHeader)
    body: PivotaltrackerStoryCreateBody


class PivotaltrackerStoryGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    story_id: int


class PivotaltrackerStoryGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PivotaltrackerStoryGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PivotaltrackerStoryGetPath
    header: PivotaltrackerStoryGetHeader = Field(default_factory=PivotaltrackerStoryGetHeader)


class PivotaltrackerStoryUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    story_id: int


class PivotaltrackerStoryUpdateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PivotaltrackerStoryUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None
    current_state: str | None = None
    estimate: int | None = None
    labels: list[Any] | None = None


class PivotaltrackerStoryUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PivotaltrackerStoryUpdatePath
    header: PivotaltrackerStoryUpdateHeader = Field(default_factory=PivotaltrackerStoryUpdateHeader)
    body: PivotaltrackerStoryUpdateBody | None = None


class PivotaltrackerNamespace:
    """Typed accessor for the `pivotaltracker` app. Reach it via `client.apps.pivotaltracker`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def project_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PivotaltrackerProjectGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details of a single Pivotal Tracker project by its ID, including its name, description, member counts, and configuration settings. Use this to inspect a project's metadata before operating on its stories or members. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pivotaltracker.project_get", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PivotaltrackerProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of all Pivotal Tracker projects the authenticated user can access, including both owned and shared projects. Use this to discover available project IDs before performing project-scoped actions. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pivotaltracker.projects_list", owner_id, args, account_id=account_id
        )

    async def stories_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PivotaltrackerStoriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of stories (features, bugs, chores, releases) within a specific Pivotal Tracker project, with optional filtering by state. Use this to browse the backlog, review sprint progress, or locate a story ID for a subsequent update. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pivotaltracker.stories_list", owner_id, args, account_id=account_id
        )

    async def story_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PivotaltrackerStoryCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new story in a specified Pivotal Tracker project with a name, type, estimate, labels, owners, and initial state. The story immediately appears in the project backlog and is visible to all project members. Use this to capture work items during sprint planning or triage. This action is reversible by updating the story state or deleting the story.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pivotaltracker.story_create", owner_id, args, account_id=account_id
        )

    async def story_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PivotaltrackerStoryGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single Pivotal Tracker story by its unique ID, including its description, current state, estimate, labels, and ownership. Use this to inspect a story's current status or content before making changes. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pivotaltracker.story_get", owner_id, args, account_id=account_id
        )

    async def story_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PivotaltrackerStoryUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing Pivotal Tracker story's attributes such as name, description, state, estimate, labels, or ownership. Changes are immediately visible to all project members and affect the project's workflow and reports. Use this to progress a story through its lifecycle or correct its metadata. This action is reversible by issuing another update.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pivotaltracker.story_update", owner_id, args, account_id=account_id
        )
