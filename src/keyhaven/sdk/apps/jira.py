# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `jira` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class JiraGetAccessibleResourcesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class JiraGetAccessibleResourcesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class JiraGetAccessibleResourcesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: JiraGetAccessibleResourcesQuery | None = None
    header: JiraGetAccessibleResourcesHeader | None = None


class JiraGetProjectsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cloudid: str


class JiraGetProjectsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    expand: str = "description,lead"


class JiraGetProjectsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class JiraGetProjectsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: JiraGetProjectsPath
    query: JiraGetProjectsQuery | None = None
    header: JiraGetProjectsHeader | None = None


class JiraSearchIssuesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cloudid: str


class JiraSearchIssuesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    jql: str = ""
    startAt: int = 0
    maxResults: int = 50
    fields: str = "summary,description,status,assignee,priority,issuetype"


class JiraSearchIssuesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class JiraSearchIssuesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: JiraSearchIssuesPath
    query: JiraSearchIssuesQuery | None = None
    header: JiraSearchIssuesHeader | None = None


class JiraNamespace:
    """Typed accessor for the `jira` app. Reach it via `client.apps.jira`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_accessible_resources(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JiraGetAccessibleResourcesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Atlassian sites and products (Jira, Confluence, etc.) that the authenticated user has access to, returning each site's cloudId, name, URL, and avatar. This is a prerequisite discovery call — the returned cloudId values are required as path parameters for all subsequent site-specific API calls. Use this function when you need to determine which Jira site to operate on or to enumerate available Atlassian instances.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "jira.get_accessible_resources", owner_id, args, account_id=account_id
        )

    async def get_projects(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JiraGetProjectsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all projects in the specified Jira Cloud site, with optional expanded details such as project description, lead, and avatar URLs. This is a read-only listing that returns project keys, names, and metadata — it does not create or modify anything. Use this function when you need to discover available projects, map project names to their keys, or obtain project IDs for subsequent issue operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "jira.get_projects", owner_id, args, account_id=account_id
        )

    async def search_issues(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JiraSearchIssuesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for issues in Jira using JQL (Jira Query Language), with configurable pagination and field filtering. Returns matching issues with their keys, summaries, statuses, and any other requested fields — it is a read-only query and does not modify any data. Use this function when you need to find issues by project, status, assignee, or any JQL-supported criterion, or when you need to retrieve structured issue data for analysis or reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "jira.search_issues", owner_id, args, account_id=account_id
        )
