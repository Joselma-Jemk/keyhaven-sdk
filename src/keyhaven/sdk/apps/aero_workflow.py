# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `aero_workflow` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AeroWorkflowCreateContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class AeroWorkflowCreateContactBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    firstName: str
    lastName: str
    defaultEmailAddress: str | None = None
    officePhone: str | None = None
    homePhone: str | None = None
    mobilePhone: str | None = None
    facebookUrl: str | None = None
    linkedInUrl: str | None = None
    title: str | None = None
    twitterHandle: str | None = None
    notes: str | None = None


class AeroWorkflowCreateContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AeroWorkflowCreateContactPath
    body: AeroWorkflowCreateContactBody


class AeroWorkflowGetContactsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class AeroWorkflowGetContactsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AeroWorkflowGetContactsPath


class AeroWorkflowGetEmailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class AeroWorkflowGetEmailsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageNumber: int = 1


class AeroWorkflowGetEmailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AeroWorkflowGetEmailsPath
    query: AeroWorkflowGetEmailsQuery | None = None


class AeroWorkflowGetOthersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class AeroWorkflowGetOthersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AeroWorkflowGetOthersPath


class AeroWorkflowGetTasksPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class AeroWorkflowGetTasksRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AeroWorkflowGetTasksPath


class AeroWorkflowNamespace:
    """Typed accessor for the `aero_workflow` app. Reach it via `client.apps.aero_workflow`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AeroWorkflowCreateContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new contact record in the AeroWorkflow practice management system with the provided details such as name, email, phone numbers, social profiles, job title, and notes. This permanently adds a new person or organization to the account's contact database, making them available for tasks, emails, and other workflow operations. Use this when onboarding a new client, prospect, or referral source into the practice management system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aero_workflow.create_contact", owner_id, args, account_id=account_id
        )

    async def get_contacts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AeroWorkflowGetContactsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full contact database from the AeroWorkflow practice management system, including names, phone numbers, email addresses, social media profiles, job titles, and notes for each contact. This read-only query returns all contact records stored in the specified account without modifying any data. Use this when you need to look up client, prospect, or referral source contact information for your accounting practice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aero_workflow.get_contacts", owner_id, args, account_id=account_id
        )

    async def get_emails(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AeroWorkflowGetEmailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of email records from the AeroWorkflow practice management system for the specified account. Each item represents an email message tracked or stored within the account, and the optional pageNumber query parameter allows pagination through large result sets. Use this when you need to review email history, audit client correspondence, or locate specific email records linked to the practice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aero_workflow.get_emails", owner_id, args, account_id=account_id
        )

    async def get_others(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AeroWorkflowGetOthersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve miscellaneous auxiliary records from the AeroWorkflow practice management system that do not belong to the dedicated contacts, tasks, or emails endpoints. This read-only operation returns uncategorized data items stored in the account and has no side effects on any records. Use this when you need to access supplemental data that falls outside the standard entity types within the AeroWorkflow account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aero_workflow.get_others", owner_id, args, account_id=account_id
        )

    async def get_tasks(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AeroWorkflowGetTasksRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve all task records from the AeroWorkflow practice management system for the specified account. This read-only operation returns the full collection of workflow tasks including their current status, assignments, and other details without modifying any data. Use this when you need to list, review, or audit open or completed tasks across the accounting practice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aero_workflow.get_tasks", owner_id, args, account_id=account_id
        )
