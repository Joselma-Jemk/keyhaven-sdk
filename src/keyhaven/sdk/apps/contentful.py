# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `contentful` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ContentfulAssetsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    space_id: str
    environment_id: str


class ContentfulAssetsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    mimetype_group: str | None = None
    order: str | None = None
    limit: int | None = None
    skip: int | None = None


class ContentfulAssetsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ContentfulAssetsListPath
    query: ContentfulAssetsListQuery | None = None


class ContentfulEntriesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    space_id: str
    environment_id: str


class ContentfulEntriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content_type: str | None = None
    query: str | None = None
    order: str | None = None
    limit: int | None = None
    skip: int | None = None
    include: int | None = None
    select: str | None = None


class ContentfulEntriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ContentfulEntriesListPath
    query: ContentfulEntriesListQuery | None = None


class ContentfulEntryCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    space_id: str
    environment_id: str


class ContentfulEntryCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Contentful_Content_Type: str = Field(alias="X-Contentful-Content-Type")


class ContentfulEntryCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: dict[str, Any]
    metadata: dict[str, Any] | None = None


class ContentfulEntryCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ContentfulEntryCreatePath
    header: ContentfulEntryCreateHeader
    body: ContentfulEntryCreateBody


class ContentfulEntryGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    space_id: str
    environment_id: str
    entry_id: str


class ContentfulEntryGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ContentfulEntryGetPath


class ContentfulEntryPublishPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    space_id: str
    environment_id: str
    entry_id: str


class ContentfulEntryPublishHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Contentful_Version: int = Field(alias="X-Contentful-Version")


class ContentfulEntryPublishRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ContentfulEntryPublishPath
    header: ContentfulEntryPublishHeader


class ContentfulEntryUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    space_id: str
    environment_id: str
    entry_id: str


class ContentfulEntryUpdateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    X_Contentful_Version: int = Field(alias="X-Contentful-Version")


class ContentfulEntryUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: dict[str, Any]
    metadata: dict[str, Any] | None = None


class ContentfulEntryUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ContentfulEntryUpdatePath
    header: ContentfulEntryUpdateHeader
    body: ContentfulEntryUpdateBody


class ContentfulNamespace:
    """Typed accessor for the `contentful` app. Reach it via `client.apps.contentful`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def assets_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ContentfulAssetsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch all media assets (images, videos, audio files, documents) from a Contentful space environment, with optional full-text search, MIME type group filtering (image, video, audio, archive), ordering, and pagination. Returns asset metadata including titles, descriptions, file URLs, file sizes, and upload/processing status. Use when you need to browse the media library, find existing assets to link to entries, or verify upload completion.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "contentful.assets_list", owner_id, args, account_id=account_id
        )

    async def entries_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ContentfulEntriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch all content entries from a specified Contentful space environment, with filtering by content type, full-text search across fields, ordering, pagination, and linked entry resolution depth. Returns the raw entry data including all locale-specific field values, metadata, and version numbers. Use when you need to browse content inventory, export entries for migration, or find specific records to update or publish.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "contentful.entries_list", owner_id, args, account_id=account_id
        )

    async def entry_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ContentfulEntryCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new content entry of a specified content type in a Contentful space environment. The content type is set via the X-Contentful-Content-Type header, and field values are provided as a locale-keyed map (e.g. title: { 'en-US': 'Hello' }). The entry is created in draft state only, not yet visible to the public through the Delivery API. Use when adding new content programmatically, such as importing articles from an external source or seeding content for a new section.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "contentful.entry_create", owner_id, args, account_id=account_id
        )

    async def entry_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ContentfulEntryGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a single content entry by its unique ID from a given Contentful space and environment. Returns the entry's complete field data across all locales, its metadata including version number, creation and update timestamps, and references to linked content. Use when you need to inspect one specific entry before updating it, verify its current draft state, or read its field values for display or transformation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "contentful.entry_get", owner_id, args, account_id=account_id
        )

    async def entry_publish(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ContentfulEntryPublishRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Publish an entry's current draft so it replaces the live version served through the Contentful Delivery API and CDN to websites and apps. Requires the current version number via the X-Contentful-Version header. The previous published state is overwritten but retained in version history for rollback. Use when content has been reviewed, approved, and is ready to go live to end users.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "contentful.entry_publish", owner_id, args, account_id=account_id
        )

    async def entry_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ContentfulEntryUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Update an existing content entry's field values by its ID in a Contentful space environment. Requires the current version number via the X-Contentful-Version header to prevent concurrent edit conflicts. This replaces the entry's draft fields only; the published version remains unchanged until a separate publish action is performed. Use when editing existing content such as correcting text, updating media references, or modifying metadata before review.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "contentful.entry_update", owner_id, args, account_id=account_id
        )
