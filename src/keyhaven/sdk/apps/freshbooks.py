# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `freshbooks` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FreshbooksClientCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accountId: str


class FreshbooksClientCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    client: dict[str, Any]


class FreshbooksClientCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshbooksClientCreatePath
    body: FreshbooksClientCreateBody


class FreshbooksClientGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accountId: str
    clientId: str


class FreshbooksClientGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshbooksClientGetPath


class FreshbooksClientsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accountId: str


class FreshbooksClientsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 15


class FreshbooksClientsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshbooksClientsListPath
    query: FreshbooksClientsListQuery | None = None


class FreshbooksInvoiceGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accountId: str
    invoiceId: str


class FreshbooksInvoiceGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshbooksInvoiceGetPath


class FreshbooksInvoicesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accountId: str


class FreshbooksInvoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 15


class FreshbooksInvoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshbooksInvoicesListPath
    query: FreshbooksInvoicesListQuery | None = None


class FreshbooksPaymentCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accountId: str


class FreshbooksPaymentCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    payment: dict[str, Any]


class FreshbooksPaymentCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshbooksPaymentCreatePath
    body: FreshbooksPaymentCreateBody


class FreshbooksNamespace:
    """Typed accessor for the `freshbooks` app. Reach it via `client.apps.freshbooks`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def client_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshbooksClientCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new client record in the specified FreshBooks account with fields such as first and last name, email, organization, currency, language, and primary address. The new client becomes immediately available for invoicing, expense tracking, and payment collection. This action is reversible by archiving the client. Use it when onboarding a new customer, importing contacts, or setting up a client for one-time billing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshbooks.client_create", owner_id, args, account_id=account_id
        )

    async def client_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshbooksClientGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single FreshBooks client by its unique client ID, including billing address, contacts, currency, language, VAT info, and internal notes. This read-only operation does not alter any data in FreshBooks. Use it when you need to inspect a client's complete profile before creating an invoice, verifying billing information, or displaying client details in a user interface.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshbooks.client_get", owner_id, args, account_id=account_id
        )

    async def clients_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshbooksClientsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of clients for the specified FreshBooks account. Each client record includes names, email, organization, currency, language, and primary address. This is a read-only query that does not create or modify any FreshBooks data. Use it when searching for existing clients, populating a client dropdown, or exporting client data for analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshbooks.clients_list", owner_id, args, account_id=account_id
        )

    async def invoice_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshbooksInvoiceGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single FreshBooks invoice by its unique invoice ID, including line items, taxes, discounts, payment history, and current outstanding balance. This read-only operation does not alter any FreshBooks data. Use it when reviewing a specific invoice before sending a reminder, checking payment status, or displaying invoice details to a customer.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshbooks.invoice_get", owner_id, args, account_id=account_id
        )

    async def invoices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshbooksInvoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of invoices for the specified FreshBooks account. Each invoice includes status, client, dates, line items, totals, taxes, and outstanding balance. This is a read-only query that does not create or modify any FreshBooks data. Use it when reviewing accounts receivable, auditing billing history, or syncing invoice data to external reporting tools.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshbooks.invoices_list", owner_id, args, account_id=account_id
        )

    async def payment_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshbooksPaymentCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Records a new payment against an invoice in FreshBooks, specifying the invoice ID, amount, currency, date, payment type (Check, Credit, Cash), and an optional note. Once recorded, the invoice's paid amount and outstanding balance are updated, and the payment appears in the FreshBooks payment history and financial reports. Use it when logging received payments, recording offline payments, or reconciling bank deposits. This action affects financial records and should be used with accurate data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshbooks.payment_create", owner_id, args, account_id=account_id
        )
