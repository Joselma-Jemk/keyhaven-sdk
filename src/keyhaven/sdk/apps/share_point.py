# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `share_point` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SharePointCreateFolderInSiteDrivePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str
    parent_item_id: str


class SharePointCreateFolderInSiteDriveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    folder: dict[str, Any] = {}
    microsoft_graph_conflictBehavior: Literal["rename", "fail", "replace"] | None = Field(
        default=None, alias="@microsoft.graph.conflictBehavior"
    )


class SharePointCreateFolderInSiteDriveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointCreateFolderInSiteDrivePath
    body: SharePointCreateFolderInSiteDriveBody


class SharePointCreateListInASitePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class SharePointCreateListInASiteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    displayName: str
    columns: list[Any] | None = None
    list: dict[str, Any]


class SharePointCreateListInASiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointCreateListInASitePath
    body: SharePointCreateListInASiteBody


class SharePointDeleteItemInSiteDrivePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str
    item_id: str


class SharePointDeleteItemInSiteDriveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointDeleteItemInSiteDrivePath


class SharePointGetRootSiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SharePointGetSiteByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class SharePointGetSiteByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointGetSiteByIdPath


class SharePointGetSiteByPathPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    hostname: str
    relative_path: str


class SharePointGetSiteByPathRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointGetSiteByPathPath


class SharePointListDrivesForASitePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class SharePointListDrivesForASiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointListDrivesForASitePath


class SharePointListSubsitesForASitePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class SharePointListSubsitesForASiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointListSubsitesForASitePath


class SharePointSearchForSitesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    search_text: str


class SharePointSearchForSitesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointSearchForSitesPath


class SharePointSerachForItemsInASiteDrivePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str
    search_text: str


class SharePointSerachForItemsInASiteDriveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SharePointSerachForItemsInASiteDrivePath


class SharePointNamespace:
    """Typed accessor for the `share_point` app. Reach it via `client.apps.share_point`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_folder_in_site_drive(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointCreateFolderInSiteDriveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new folder inside a specified parent folder (or root) of a SharePoint site's default document library. You can control conflict behavior by choosing to rename, fail, or replace if a folder with the same name already exists. Use this to organise documents into a structured folder hierarchy on the site.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.create_folder_in_site_drive", owner_id, args, account_id=account_id
        )

    async def create_list_in_a_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointCreateListInASiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new SharePoint list on a specified site with a given display name and list template (e.g., genericList, task, issue). Optionally accepts column definitions. The new list is immediately visible to site members. Use this to set up structured data storage like issue trackers, task boards, or custom lists.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.create_list_in_a_site", owner_id, args, account_id=account_id
        )

    async def delete_item_in_site_drive(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointDeleteItemInSiteDriveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a file or folder from a SharePoint site's document library by its item ID. Deleted items bypass the recycle bin if the library policy is configured accordingly. This action is irreversible — use with caution and verify the item ID before calling.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.delete_item_in_site_drive", owner_id, args, account_id=account_id
        )

    async def get_root_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointGetRootSiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the root-level SharePoint site collection for the tenant. Returns site metadata including ID, name, description, and web URL. Use this as a starting point to navigate to subsites or document libraries within the default site collection.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.get_root_site", owner_id, args, account_id=account_id
        )

    async def get_site_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointGetSiteByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a SharePoint site's metadata using its unique site ID (the encoded SharePoint ID format combining hostname, site ID, and web ID). Returns the site's name, description, and web URL. Use this when you have stored a site ID from a previous search or listing operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.get_site_by_id", owner_id, args, account_id=account_id
        )

    async def get_site_by_path(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointGetSiteByPathRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a SharePoint site by specifying its hostname and server-relative path (e.g., '/sites/mySite'). Returns the site's metadata including ID, name, and web URL. Use this when you know the human-readable URL of a site rather than its encoded ID.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.get_site_by_path", owner_id, args, account_id=account_id
        )

    async def list_drives_for_a_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointListDrivesForASiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all document libraries (drives) configured on a SharePoint site. Each drive represents a container for files and folders, such as 'Shared Documents'. Returns drive metadata including name, ID, and quota information. Use this to discover available document libraries before listing or searching their contents.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.list_drives_for_a_site", owner_id, args, account_id=account_id
        )

    async def list_subsites_for_a_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointListSubsitesForASiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all subsites (child sites) within a specified SharePoint site. Returns their IDs, names, and web URLs. Use this to discover the site hierarchy when navigating from a parent site to its children.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.list_subsites_for_a_site", owner_id, args, account_id=account_id
        )

    async def search_for_sites(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointSearchForSitesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches across the SharePoint tenant for sites matching the given keywords, using free-text search against site properties such as name and description. Returns a list of matching sites with their IDs, names, and web URLs. Use this to locate a site when you only know part of its name or purpose.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.search_for_sites", owner_id, args, account_id=account_id
        )

    async def serach_for_items_in_a_site_drive(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SharePointSerachForItemsInASiteDriveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches the default document library of a SharePoint site for files and folders matching a query string. The search covers file names, folder names, and content metadata. Returns matching items with their IDs, names, sizes, and download URLs. Use this to find specific documents across the site's default drive.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "share_point.serach_for_items_in_a_site_drive", owner_id, args, account_id=account_id
        )
