# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `zendesk_sell` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ZendeskSellContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    sort_by: str | None = None
    sort_mode: Literal["asc", "desc"] | None = None
    q: str | None = None


class ZendeskSellContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZendeskSellContactsListQuery | None = None


class ZendeskSellDealGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class ZendeskSellDealGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZendeskSellDealGetPath


class ZendeskSellDealsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    stage_id: int | None = None
    pipeline_id: int | None = None
    contact_id: int | None = None


class ZendeskSellDealsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZendeskSellDealsListQuery | None = None


class ZendeskSellLeadCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any] | None = None


class ZendeskSellLeadCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ZendeskSellLeadCreateBody = Field(default_factory=ZendeskSellLeadCreateBody)


class ZendeskSellLeadGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class ZendeskSellLeadGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZendeskSellLeadGetPath


class ZendeskSellLeadsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    sort_by: str | None = None
    sort_mode: Literal["asc", "desc"] | None = None
    q: str | None = None


class ZendeskSellLeadsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZendeskSellLeadsListQuery | None = None


class ZendeskSellNamespace:
    """Typed accessor for the `zendesk_sell` app. Reach it via `client.apps.zendesk_sell`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZendeskSellContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of contacts from Zendesk Sell with optional full-text search, field-based sorting, and pagination controls. Returns contact profiles including names, emails, phone numbers, and organization. Use this when an agent needs to locate a specific contact or enumerate matching contacts for outreach, deal association, or reporting tasks.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zendesk_sell.contacts_list", owner_id, args, account_id=account_id
        )

    async def deal_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZendeskSellDealGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single deal by its numeric identifier from Zendesk Sell. Returns comprehensive deal data including monetary value, pipeline stage assignment, associated contacts and products, and custom fields. Use this when an agent needs the full context of a specific deal before updating its stage, value, or ownership.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zendesk_sell.deal_get", owner_id, args, account_id=account_id
        )

    async def deals_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZendeskSellDealsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of deals from Zendesk Sell, filterable by pipeline stage, pipeline, or associated contact. Returns deal summaries including value, stage, and close date. Use this when an agent needs to audit the sales pipeline, find deals at a specific stage, or identify deals linked to a particular contact or account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zendesk_sell.deals_list", owner_id, args, account_id=account_id
        )

    async def lead_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZendeskSellLeadCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new lead record in the Zendesk Sell CRM. Requires at minimum a last name, and can include contact details, organization, status, tags, and custom fields. This adds a visible entry to the sales pipeline that the team will see and may act upon. Use this when an agent has gathered enough lead information from a form, import, or conversation and needs to persist it into the CRM.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zendesk_sell.lead_create", owner_id, args, account_id=account_id
        )

    async def lead_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZendeskSellLeadGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single lead by its unique numeric identifier from Zendesk Sell. Returns comprehensive lead data including custom fields, associated contacts, and communication history. Use this when an agent needs the complete picture of a specific lead before updating, qualifying, or contacting them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zendesk_sell.lead_get", owner_id, args, account_id=account_id
        )

    async def leads_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZendeskSellLeadsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of leads from Zendesk Sell, with optional full-text search, field-based sorting, and pagination controls. Returns lead summaries including names, contact info, status, and tags. Use this when an agent needs to find, filter, or count leads matching specific criteria before acting on a particular record.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zendesk_sell.leads_list", owner_id, args, account_id=account_id
        )
