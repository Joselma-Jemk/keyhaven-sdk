# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `etsy` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class EtsyListingCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shop_id: str


class EtsyListingCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    description: str
    price: dict[str, Any]
    quantity: int
    who_made: str
    when_made: str
    taxonomy_id: int
    listing_type: str | None = None
    state: str | None = None
    tags: list[Any] | None = None
    materials: list[Any] | None = None
    shipping_profile_id: str | None = None


class EtsyListingCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EtsyListingCreatePath
    body: EtsyListingCreateBody


class EtsyListingDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shop_id: str
    listing_id: str


class EtsyListingDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EtsyListingDeletePath


class EtsyListingGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shop_id: str
    listing_id: str


class EtsyListingGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EtsyListingGetPath


class EtsyListingUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shop_id: str
    listing_id: str


class EtsyListingUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    description: str | None = None
    state: str | None = None
    quantity: int | None = None
    price: dict[str, Any] | None = None
    tags: list[Any] | None = None


class EtsyListingUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EtsyListingUpdatePath
    body: EtsyListingUpdateBody = Field(default_factory=EtsyListingUpdateBody)


class EtsyListingsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shop_id: str


class EtsyListingsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    state: str = "active"
    limit: int = 25
    offset: int = 0
    sort_on: str | None = None
    sort_order: str | None = None


class EtsyListingsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EtsyListingsListPath
    query: EtsyListingsListQuery | None = None


class EtsyShopGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shop_id: str


class EtsyShopGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EtsyShopGetPath


class EtsyNamespace:
    """Typed accessor for the `etsy` app. Reach it via `client.apps.etsy`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def listing_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtsyListingCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new product listing in an Etsy shop with the specified title, description, price, quantity, and metadata such as taxonomy category, tags, materials, and shipping profile. The listing can be created as a draft or published as active, making it immediately visible to shoppers. Use it when an agent needs to add a new product to a shop, but confirm before publishing live.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etsy.listing_create", owner_id, args, account_id=account_id
        )

    async def listing_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtsyListingDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a listing from an Etsy shop, making it inaccessible to buyers and unpublishing it immediately. This action is irreversible: any associated inventory, sales history, or reviews linked to the listing remain but the listing itself cannot be restored. Use it only when an agent is certain a product should be completely removed from the shop.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etsy.listing_delete", owner_id, args, account_id=account_id
        )

    async def listing_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtsyListingGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the full details of a single Etsy listing within a specific shop, including its description, price, quantity, images, variations, and shipping information. This is a read-only operation that provides all listing attributes needed for display or analysis. Use it when an agent needs to inspect a specific product before updating it or presenting its details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("etsy.listing_get", owner_id, args, account_id=account_id)

    async def listing_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtsyListingUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies attributes of an existing Etsy listing such as title, description, price, quantity, state (active/draft/inactive), and tags. Changes take effect immediately on the shop's public listing page, potentially impacting customer views and purchases. Use it when an agent needs to adjust a listing's details or availability.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etsy.listing_update", owner_id, args, account_id=account_id
        )

    async def listings_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtsyListingsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of listings belonging to a specific Etsy shop, with optional filters by state (active, draft, inactive, expired, sold_out), sorting, and pagination. This returns listing summaries including titles, prices, images, and URLs but not full details. Use it when an agent needs to browse or search a shop's product catalog.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etsy.listings_list", owner_id, args, account_id=account_id
        )

    async def shop_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtsyShopGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full profile of an Etsy shop by its numeric ID, including its title, announcement, currency, icon, and vacation settings. This is a read-only operation that returns shop metadata visible to the application. Use it when an agent needs to inspect shop configuration before performing other operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("etsy.shop_get", owner_id, args, account_id=account_id)
