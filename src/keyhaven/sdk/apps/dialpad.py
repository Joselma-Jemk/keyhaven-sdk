# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `dialpad` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DialpadCallbackRequestBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    phone_number: str
    call_center_id: int


class DialpadCallbackRequestRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DialpadCallbackRequestBody


class DialpadContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str
    last_name: str
    emails: list[Any] | None = None
    phones: list[Any] | None = None
    company_name: str | None = None
    job_title: str | None = None
    owner_id: str | None = None
    trunk_group: str | None = None
    urls: list[Any] | None = None


class DialpadContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DialpadContactCreateBody


class DialpadContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    include_local: bool | None = None
    owner_id: str | None = None


class DialpadContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DialpadContactsListQuery | None = None


class DialpadSmsSendBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: int
    text: str
    to_numbers: list[Any]
    from_number: str | None = None
    infer_country_code: bool | None = None
    sender_group_id: int | None = None
    sender_group_type: str | None = None
    media: str | None = None


class DialpadSmsSendRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DialpadSmsSendBody


class DialpadUserGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class DialpadUserGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DialpadUserGetPath


class DialpadUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    state: str | None = None
    email: str | None = None
    company_admin: bool | None = None
    number_search: str | None = None


class DialpadUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DialpadUsersListQuery | None = None


class DialpadNamespace:
    """Typed accessor for the `dialpad` app. Reach it via `client.apps.dialpad`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def callback_request(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DialpadCallbackRequestRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Add a phone number to a call center callback queue so that Dialpad will call the person back when an agent becomes available. The callback is placed into the queue immediately and the recipient will receive a phone call, potentially at any time of day. Use to offer a callback to a customer waiting in queue rather than having them hold.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dialpad.callback_request", owner_id, args, account_id=account_id
        )

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DialpadContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new shared or user-owned contact in the Dialpad directory. Requires first and last name; optionally accepts phone numbers, emails, company info, and routing settings. The contact becomes immediately visible and usable for calls and messages. Use to add a new person or vendor to the organisation's address book.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dialpad.contact_create", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DialpadContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Return a paginated list of contacts in the Dialpad directory, optionally filtered by owner or whether they include locally-created contacts. Use this to look up contact details before sending a message or creating a new contact.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dialpad.contacts_list", owner_id, args, account_id=account_id
        )

    async def sms_send(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DialpadSmsSendRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Send an SMS or MMS message from a Dialpad user or sender group to one or more recipients. Supports text, media attachments (MMS), and optional sender number selection. The message is delivered to the recipients' phones immediately and cannot be recalled. Use when you need to send an outbound text message; verify content carefully before sending.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("dialpad.sms_send", owner_id, args, account_id=account_id)

    async def user_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DialpadUserGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full profile of a single Dialpad user by their unique identifier, including their extension, phone number, role, and settings. Use this to inspect a specific user before modifying their configuration or sending a message on their behalf.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("dialpad.user_get", owner_id, args, account_id=account_id)

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DialpadUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Return a paginated list of users in the Dialpad company account. Supports filtering by state (active, suspended, deleted), email, admin status, and assigned phone number. Use this to browse the user directory or find a specific user's ID before performing user-level actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dialpad.users_list", owner_id, args, account_id=account_id
        )
