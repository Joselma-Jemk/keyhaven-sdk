# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `solscan` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SolscanGetAccountBalanceChangeQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str


class SolscanGetAccountBalanceChangeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SolscanGetAccountBalanceChangeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SolscanGetAccountBalanceChangeQuery
    header: SolscanGetAccountBalanceChangeHeader = Field(
        default_factory=SolscanGetAccountBalanceChangeHeader
    )


class SolscanGetAccountDetailQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str


class SolscanGetAccountDetailHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SolscanGetAccountDetailRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SolscanGetAccountDetailQuery
    header: SolscanGetAccountDetailHeader = Field(default_factory=SolscanGetAccountDetailHeader)


class SolscanGetAccountPortfolioQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str


class SolscanGetAccountPortfolioHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SolscanGetAccountPortfolioRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SolscanGetAccountPortfolioQuery
    header: SolscanGetAccountPortfolioHeader = Field(
        default_factory=SolscanGetAccountPortfolioHeader
    )


class SolscanGetAccountTokenAccountsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str
    type_: Literal["token", "nft"] = Field(alias="type")
    page: float | None = None
    page_size: float | None = None
    hide_zero: bool | None = None


class SolscanGetAccountTokenAccountsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SolscanGetAccountTokenAccountsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SolscanGetAccountTokenAccountsQuery
    header: SolscanGetAccountTokenAccountsHeader = Field(
        default_factory=SolscanGetAccountTokenAccountsHeader
    )


class SolscanGetAccountTransactionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str
    before: str | None = None
    limit: float | None = None


class SolscanGetAccountTransactionsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SolscanGetAccountTransactionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SolscanGetAccountTransactionsQuery
    header: SolscanGetAccountTransactionsHeader = Field(
        default_factory=SolscanGetAccountTransactionsHeader
    )


class SolscanGetChainInfoHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SolscanGetChainInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: SolscanGetChainInfoHeader = Field(default_factory=SolscanGetChainInfoHeader)


class SolscanNamespace:
    """Typed accessor for the `solscan` app. Reach it via `client.apps.solscan`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_account_balance_change(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SolscanGetAccountBalanceChangeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the balance change history for a Solana account, showing incoming and outgoing SOL movements over time. Each entry includes the amount, transaction signature, and timestamp. Use this to audit an account's financial activity or to trace when and how its balance fluctuated.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "solscan.get_account_balance_change", owner_id, args, account_id=account_id
        )

    async def get_account_detail(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SolscanGetAccountDetailRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch detailed information about a Solana account by its public address, including its lamport balance, owner program, rent state, and data size. This is the primary way to inspect any on-chain account. Use this when you need to verify an account's existence, check its balance, or identify which program owns it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "solscan.get_account_detail", owner_id, args, account_id=account_id
        )

    async def get_account_portfolio(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SolscanGetAccountPortfolioRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full portfolio of token holdings for a Solana wallet address, including SOL balance and all SPL token balances with their current estimated USD values. This provides a financial snapshot of everything the wallet holds. Use this to assess the total value and asset composition of a wallet without making any on-chain changes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "solscan.get_account_portfolio", owner_id, args, account_id=account_id
        )

    async def get_account_token_accounts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SolscanGetAccountTokenAccountsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all SPL token and NFT accounts owned by a Solana wallet address, with optional pagination and filtering by token type (fungible vs NFT) or zero-balance hiding. Each result includes the mint address, balance, and token metadata. Use this to discover what tokens a wallet holds and drill into specific token details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "solscan.get_account_token_accounts", owner_id, args, account_id=account_id
        )

    async def get_account_transactions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SolscanGetAccountTransactionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of recent transactions for a Solana account, with optional cursor-based pagination using the last signature of the previous page. Each transaction includes its signature, block time, status, fee, and involved accounts. Use this to review an account's transaction history or to find a specific transfer involving the address.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "solscan.get_account_transactions", owner_id, args, account_id=account_id
        )

    async def get_chain_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SolscanGetChainInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the current status and metadata of the Solana blockchain, including slot height, epoch, block time, and validator count. This data reflects the live state of the network and is useful for monitoring health or alignment. Use this to obtain a high-level snapshot of the Solana chain before performing account or transaction lookups.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "solscan.get_chain_info", owner_id, args, account_id=account_id
        )
