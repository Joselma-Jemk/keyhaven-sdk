# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `netlify` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class NetlifyCreateBuildHookPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class NetlifyCreateBuildHookBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    branch: str = "main"


class NetlifyCreateBuildHookRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyCreateBuildHookPath
    body: NetlifyCreateBuildHookBody


class NetlifyCreateDeployKeyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None


class NetlifyCreateDeployKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: NetlifyCreateDeployKeyBody = Field(default_factory=NetlifyCreateDeployKeyBody)


class NetlifyCreateSiteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    custom_domain: str | None = None
    repo: dict[str, Any] | None = None


class NetlifyCreateSiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: NetlifyCreateSiteBody


class NetlifyDeleteDeployKeyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key_id: str


class NetlifyDeleteDeployKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyDeleteDeployKeyPath


class NetlifyDeleteSitePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class NetlifyDeleteSiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyDeleteSitePath


class NetlifyGetAccountByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class NetlifyGetAccountByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyGetAccountByIdPath


class NetlifyGetAccountsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class NetlifyGetDeployPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    deploy_id: str


class NetlifyGetDeployRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyGetDeployPath


class NetlifyGetDeployKeyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key_id: str


class NetlifyGetDeployKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyGetDeployKeyPath


class NetlifyGetEnvVariablesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_id: str


class NetlifyGetEnvVariablesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str | None = None


class NetlifyGetEnvVariablesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyGetEnvVariablesPath
    query: NetlifyGetEnvVariablesQuery | None = None


class NetlifyGetSitePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class NetlifyGetSiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyGetSitePath


class NetlifyGetSiteDeployPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str
    deploy_id: str


class NetlifyGetSiteDeployRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyGetSiteDeployPath


class NetlifyGetSitesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class NetlifyListDeployKeysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class NetlifyListDeploysPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class NetlifyListDeploysQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 20


class NetlifyListDeploysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyListDeploysPath
    query: NetlifyListDeploysQuery | None = None


class NetlifyTriggerBuildHookPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    hook_id: str


class NetlifyTriggerBuildHookQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    trigger_branch: str | None = None
    trigger_title: str | None = None
    clear_cache: bool = False


class NetlifyTriggerBuildHookBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    message: str | None = None


class NetlifyTriggerBuildHookRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyTriggerBuildHookPath
    query: NetlifyTriggerBuildHookQuery | None = None
    body: NetlifyTriggerBuildHookBody | None = None


class NetlifyUpdateSitePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site_id: str


class NetlifyUpdateSiteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    custom_domain: str | None = None
    password: str | None = None
    processing_settings: dict[str, Any] | None = None


class NetlifyUpdateSiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NetlifyUpdateSitePath
    body: NetlifyUpdateSiteBody = Field(default_factory=NetlifyUpdateSiteBody)


class NetlifyNamespace:
    """Typed accessor for the `netlify` app. Reach it via `client.apps.netlify`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_build_hook(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyCreateBuildHookRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new build hook URL for a Netlify site, which provides a unique endpoint that external services or scripts can call to trigger a new deployment. The hook is configured with a descriptive title and an optional branch to build. Once created, anyone with the URL can trigger a build without authentication. Use this when an agent needs to set up continuous deployment triggers from external CI/CD systems, webhook integrations, or third-party services that cannot use Git-based triggers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.create_build_hook", owner_id, args, account_id=account_id
        )

    async def create_deploy_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyCreateDeployKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generates a new SSH deploy key pair that allows Netlify to access a private Git repository for automatic deployments. After creation, retrieve the public key via NETLIFY__GET_DEPLOY_KEY or NETLIFY__LIST_DEPLOY_KEYS, then add it to your Git provider (e.g., using GITHUB__CREATE_DEPLOY_KEY) so the repository can be linked as a private deployment source. The key can be deleted later if no longer needed. Use this when setting up continuous deployment from a private repository.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.create_deploy_key", owner_id, args, account_id=account_id
        )

    async def create_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyCreateSiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Netlify site/project under the authenticated account, with optional Git repository connection for continuous deployment. You can configure the site name, custom domain, repository provider, branch, build command, and deploy directory. For private repositories, first generate a deploy key via NETLIFY__CREATE_DEPLOY_KEY and add it to the Git provider. This action makes the site accessible at its Netlify subdomain immediately. Use this when an agent needs to provision a new website or web application deployment target.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.create_site", owner_id, args, account_id=account_id
        )

    async def delete_deploy_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyDeleteDeployKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a deploy key from the Netlify account. If this key was registered on a Git repository and is the only access mechanism, it will immediately break the automated deployment pipeline for any site using it. This action is irreversible. Use this only when an agent has confirmed the key is no longer needed, such as after switching a site from private to public, rotating credentials, or decommissioning infrastructure.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.delete_deploy_key", owner_id, args, account_id=account_id
        )

    async def delete_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyDeleteSiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a Netlify site and all its associated resources, including deploy history, forms, functions, and environment variables. The site's URL becomes unavailable immediately and this action is irreversible. Use this only when an agent has explicit authorisation to decommission a website, clean up test/staging sites, or remove abandoned projects. Consider archiving or disabling instead when there is any doubt.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.delete_site", owner_id, args, account_id=account_id
        )

    async def get_account_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetAccountByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific Netlify account/team by its ID or slug, including its name, billing plan, member count, and associated metadata. This is read-only and modifies nothing. Use this when an agent needs to inspect a specific team's configuration, verify account ownership, or check plan limits before deploying or creating sites under that account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.get_account_by_id", owner_id, args, account_id=account_id
        )

    async def get_accounts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetAccountsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Netlify accounts (teams) that the authenticated user has access to. Each account contains its own sites, deploys, billing plans, and team members. This is a read-only operation with no side effects. Use this when an agent needs to discover available team IDs for scoping subsequent operations, or to list which organisations a user belongs to.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.get_accounts", owner_id, args, account_id=account_id
        )

    async def get_deploy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetDeployRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific Netlify deployment by its unique ID, including its status, commit reference, branch, deploy time, SSL certificate status, and any error messages. This is read-only and does not affect the deployment. Use this when an agent needs to check whether a deployment succeeded, investigate build failures, or retrieve deployment metadata for reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.get_deploy", owner_id, args, account_id=account_id
        )

    async def get_deploy_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetDeployKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves details about a specific Netlify deploy key by its ID, including the public key value, title, fingerprint, and creation date. The public key is needed to register it on a Git provider for private repository access. This is read-only and does not affect the key's validity. Use this when an agent needs to read the generated public key after NETLIFY__CREATE_DEPLOY_KEY, before adding it to a GitHub/GitLab repository.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.get_deploy_key", owner_id, args, account_id=account_id
        )

    async def get_env_variables(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetEnvVariablesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves environment variables configured for a Netlify account/team, optionally filtered by site ID. The response includes variable keys, values, scopes, and encryption metadata. While this is a read-only operation, the returned values may contain sensitive secrets (API keys, database credentials). Use this when an agent needs to audit environment configuration, debug build issues, or verify that required variables are set for a deployment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.get_env_variables", owner_id, args, account_id=account_id
        )

    async def get_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetSiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific Netlify site by its ID or custom domain, including its name, URL, deploy settings, build configuration, SSL certificate status, and processing settings. This is read-only and does not alter the site. Use this when an agent needs to inspect site configuration, verify deployment settings, or retrieve a site's metadata for reporting purposes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("netlify.get_site", owner_id, args, account_id=account_id)

    async def get_site_deploy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetSiteDeployRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific deployment within a specific Netlify site, identified by both the site ID and deploy ID. Returns the same information as NETLIFY__GET_DEPLOY but scoped within a site context. This is read-only and does not modify any state. Use this when an agent needs to inspect a particular deployment that belongs to a known site, especially when verifying deployment results after a build hook trigger.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.get_site_deploy", owner_id, args, account_id=account_id
        )

    async def get_sites(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyGetSitesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Netlify sites accessible to the authenticated user across all teams, returning each site's ID, name, URL, SSL status, and configuration metadata. This is a read-only query that does not modify any resources. Use this when an agent needs to enumerate deployed sites for inventory, monitoring, or to find a specific site's ID for subsequent management operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.get_sites", owner_id, args, account_id=account_id
        )

    async def list_deploy_keys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyListDeployKeysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all deploy keys registered for the Netlify account, returning each key's ID, title, fingerprint, and creation date. This is read-only and does not modify any keys. Use this when an agent needs to audit existing deploy keys, find a specific key for inspection or deletion, or check which keys are active and their associated titles.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.list_deploy_keys", owner_id, args, account_id=account_id
        )

    async def list_deploys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyListDeploysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all deployments for a specific Netlify site in reverse chronological order, with pagination support. Each entry includes the deploy ID, status, branch, commit SHA, and timestamps. This is read-only and does not alter any state. Use this when an agent needs to review deploy history, find a specific deployment ID for inspection, or audit which commits were deployed and when.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.list_deploys", owner_id, args, account_id=account_id
        )

    async def trigger_build_hook(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyTriggerBuildHookRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Triggers a new build and deployment on Netlify by calling an existing build hook URL. The hook pulls the latest code from the connected Git repository (or uses the specified trigger branch), optionally clears the cache, and starts a fresh build. The resulting deployment will appear in the site's deploy log with the optional custom title. This is the standard method for triggering Git-based deployments from external tools. Use this when an agent needs to deploy the latest code, clear the build cache for a fresh build, or deploy a specific branch without Git push.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.trigger_build_hook", owner_id, args, account_id=account_id
        )

    async def update_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NetlifyUpdateSiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing Netlify site's configuration, including its name, custom domain, password protection, and HTML processing settings (pretty URLs). Changes take effect immediately and may affect the live site's URL, accessibility, or behaviour. This action is reversible by restoring the previous settings. Use this when an agent needs to reconfigure a deployed site, update its domain name, enable password protection, or adjust processing options.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "netlify.update_site", owner_id, args, account_id=account_id
        )
