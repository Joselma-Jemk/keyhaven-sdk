# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `dropbox_sign` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DropboxSignSignatureRequestCancelPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    signature_request_id: str


class DropboxSignSignatureRequestCancelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DropboxSignSignatureRequestCancelPath


class DropboxSignSignatureRequestGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    signature_request_id: str


class DropboxSignSignatureRequestGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DropboxSignSignatureRequestGetPath


class DropboxSignSignatureRequestListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    page_size: int = 20
    query: str | None = None


class DropboxSignSignatureRequestListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DropboxSignSignatureRequestListQuery | None = None


class DropboxSignSignatureRequestSendBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    subject: str | None = None
    message: str | None = None
    signers: list[Any]
    cc_email_addresses: list[Any] | None = None
    file_urls: list[Any] | None = None
    test_mode: bool = False
    metadata: dict[str, Any] | None = None


class DropboxSignSignatureRequestSendRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxSignSignatureRequestSendBody


class DropboxSignSignatureRequestSendWithTemplateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    template_id: str
    title: str
    subject: str | None = None
    message: str | None = None
    signers: list[Any]
    cc_email_addresses: list[Any] | None = None
    custom_fields: list[Any] | None = None
    test_mode: bool = False


class DropboxSignSignatureRequestSendWithTemplateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxSignSignatureRequestSendWithTemplateBody


class DropboxSignTemplateListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    page_size: int = 20
    query: str | None = None


class DropboxSignTemplateListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DropboxSignTemplateListQuery | None = None


class DropboxSignNamespace:
    """Typed accessor for the `dropbox_sign` app. Reach it via `client.apps.dropbox_sign`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def signature_request_cancel(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxSignSignatureRequestCancelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Immediately cancels an active signature request so that no additional signers can access or sign the document. Signers who have already signed retain their signatures but the request is marked as cancelled and cannot proceed further. Use this when a document was sent in error, terms have changed, or the request is no longer needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox_sign.signature_request_cancel", owner_id, args, account_id=account_id
        )

    async def signature_request_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxSignSignatureRequestGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single signature request by its unique identifier, including the current status, signer progress, document metadata, and signing URLs. Use this to check whether a specific request has been completed, review signer activity, or obtain the signed document after all parties have signed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox_sign.signature_request_get", owner_id, args, account_id=account_id
        )

    async def signature_request_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxSignSignatureRequestListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all signature requests on the authenticated account, with optional Lucene-style search and field filters. Each entry includes the current status (awaiting_signature, signed, cancelled), signer details, and creation timestamp. Use this to audit sent documents, find pending requests, or locate a specific request by title or metadata.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox_sign.signature_request_list", owner_id, args, account_id=account_id
        )

    async def signature_request_send(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxSignSignatureRequestSendRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a signature request with uploaded documents and immediately dispatches it to the listed signers via email, so each recipient receives a secure link to review and sign. Signers can view the document, fill any required fields, and apply their signature in the order you specify. Use this when you need to collect legally binding signatures on contracts, agreements, or forms and want the signing workflow fully automated.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox_sign.signature_request_send", owner_id, args, account_id=account_id
        )

    async def signature_request_send_with_template(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxSignSignatureRequestSendWithTemplateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates and dispatches a signature request from a pre-designed template, mapping signers to template roles and filling custom fields. This enables consistent document generation with predefined layouts, field positions, and branding across repeated use cases. Use this when you regularly send the same type of document, such as NDAs, offer letters, onboarding forms, or service agreements.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox_sign.signature_request_send_with_template",
            owner_id,
            args,
            account_id=account_id,
        )

    async def template_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxSignTemplateListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all reusable signature templates owned by the authenticated account with optional search and pagination. Templates contain pre-defined roles, fields, and document layouts that can be reused to send consistent signature requests. Use this to browse available templates, retrieve a specific template ID for use with send_with_template, or audit your template library.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox_sign.template_list", owner_id, args, account_id=account_id
        )
