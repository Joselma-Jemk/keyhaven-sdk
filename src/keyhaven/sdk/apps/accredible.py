# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `accredible` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AccredibleCreateAttributeKeyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"
    content_type: Literal["application/json"] = Field(
        default="application/json", alias="content-type"
    )


class AccredibleCreateAttributeKeyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    attribute_key: dict[str, Any]


class AccredibleCreateAttributeKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AccredibleCreateAttributeKeyHeader = Field(
        default_factory=AccredibleCreateAttributeKeyHeader
    )
    body: AccredibleCreateAttributeKeyBody


class AccredibleCreateCredentialHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"
    content_type: Literal["application/json"] = Field(
        default="application/json", alias="content-type"
    )


class AccredibleCreateCredentialBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    credential: dict[str, Any]


class AccredibleCreateCredentialRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AccredibleCreateCredentialHeader = Field(
        default_factory=AccredibleCreateCredentialHeader
    )
    body: AccredibleCreateCredentialBody


class AccredibleCreateGroupHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"
    content_type: Literal["application/json"] = Field(
        default="application/json", alias="content-type"
    )


class AccredibleCreateGroupBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    group: dict[str, Any]


class AccredibleCreateGroupRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AccredibleCreateGroupHeader = Field(default_factory=AccredibleCreateGroupHeader)
    body: AccredibleCreateGroupBody


class AccredibleSearchAttributeKeysHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"
    content_type: Literal["application/json"] = Field(
        default="application/json", alias="content-type"
    )


class AccredibleSearchAttributeKeysBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    kind: Literal["text", "date", "email", "image"] | None = None
    department_id: int | None = None
    page: int = 1
    page_size: int = 50


class AccredibleSearchAttributeKeysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AccredibleSearchAttributeKeysHeader = Field(
        default_factory=AccredibleSearchAttributeKeysHeader
    )
    body: AccredibleSearchAttributeKeysBody = Field(
        default_factory=AccredibleSearchAttributeKeysBody
    )


class AccredibleSearchCredentialsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"
    content_type: Literal["application/json"] = Field(
        default="application/json", alias="content-type"
    )


class AccredibleSearchCredentialsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: dict[str, Any]
    page: int = 1
    per_page: int = 20


class AccredibleSearchCredentialsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AccredibleSearchCredentialsHeader = Field(
        default_factory=AccredibleSearchCredentialsHeader
    )
    body: AccredibleSearchCredentialsBody


class AccredibleNamespace:
    """Typed accessor for the `accredible` app. Reach it via `client.apps.accredible`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_attribute_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AccredibleCreateAttributeKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Registers a new custom attribute key definition — a named metadata field (text, date, number, or boolean) that can be attached to credentials and templates. This defines the schema rather than setting values. Use this when you need to extend credential metadata with organization-specific fields beyond the standard ones.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "accredible.create_attribute_key", owner_id, args, account_id=account_id
        )

    async def create_credential(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AccredibleCreateCredentialRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Issues a new digital credential (certificate or badge) to a recipient, creating an official record of an achievement. The recipient receives a verifiable credential that can be shared, displayed, and validated by third parties. Use this when awarding a certificate, badge, or micro-credential to a learner or recipient after they have completed a course or met a requirement.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "accredible.create_credential", owner_id, args, account_id=account_id
        )

    async def create_group(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AccredibleCreateGroupRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new group — a course or category container — that organizes credentials and defines their shared display settings (design templates, language, blockchain recording, PDF attachment, expiry rules). The group's course name and description are visible to recipients when they view their credentials. Use this to set up a course, program, or category before issuing credentials under it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "accredible.create_group", owner_id, args, account_id=account_id
        )

    async def search_attribute_keys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AccredibleSearchAttributeKeysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of custom attribute key definitions, with optional filters by name, kind (text/date/email/image), or department ID. Results are paginated. No data is created, modified, or deleted. Use this to discover available custom metadata fields before creating or updating credentials that reference them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "accredible.search_attribute_keys", owner_id, args, account_id=account_id
        )

    async def search_credentials(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AccredibleSearchCredentialsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Queries the credential database with complex filters (group ID, recipient email, issue/expiry dates, custom fields) and returns matching credential records. Results are paginated for efficient browsing. No data is created, modified, or deleted. Use this to find specific credentials, verify issuance status, or audit credential records before performing other actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "accredible.search_credentials", owner_id, args, account_id=account_id
        )
