# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `segment` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SegmentDestinationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pagination_count: int = Field(default=10, alias="pagination.count")
    pagination_cursor: str | None = Field(default=None, alias="pagination.cursor")


class SegmentDestinationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SegmentDestinationsListQuery | None = None


class SegmentSourceCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    slug: str
    name: str | None = None
    enabled: bool = True
    metadataId: str
    settings: dict[str, Any] | None = None


class SegmentSourceCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SegmentSourceCreateBody


class SegmentSourceGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sourceId: str


class SegmentSourceGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SegmentSourceGetPath


class SegmentSourcesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pagination_count: int = Field(default=10, alias="pagination.count")
    pagination_cursor: str | None = Field(default=None, alias="pagination.cursor")


class SegmentSourcesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SegmentSourcesListQuery | None = None


class SegmentTrackingPlansListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pagination_count: int = Field(default=10, alias="pagination.count")
    pagination_cursor: str | None = Field(default=None, alias="pagination.cursor")


class SegmentTrackingPlansListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SegmentTrackingPlansListQuery | None = None


class SegmentWorkspaceGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SegmentNamespace:
    """Typed accessor for the `segment` app. Reach it via `client.apps.segment`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def destinations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SegmentDestinationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all destinations configured in the Segment workspace with cursor-based pagination. Destinations are the downstream tools (analytics platforms, marketing tools, data warehouses) that receive data from sources. Agents should use this to review which integrations are active and their connection status. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "segment.destinations_list", owner_id, args, account_id=account_id
        )

    async def source_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SegmentSourceCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new data source in the Segment workspace with a specified slug, name, metadata template, and optional settings. The source becomes available for data collection immediately if enabled, and appears in the workspace UI. Agents should use this when setting up a new tracking implementation for a website, app, or server. This action is reversible by disabling or deleting the source.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "segment.source_create", owner_id, args, account_id=account_id
        )

    async def source_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SegmentSourceGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full configuration of a single data source, including its metadata slug, name, enabled state, and settings. Agents should use this to inspect a specific source's setup, verify its current state, or retrieve its identifiers before connecting a destination. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "segment.source_get", owner_id, args, account_id=account_id
        )

    async def sources_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SegmentSourcesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all data sources configured in the Segment workspace with cursor-based pagination. Sources are the entry points for data collection (websites, mobile apps, servers). Agents should use this to discover available sources before modifying them, inspecting their settings, or creating destinations. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "segment.sources_list", owner_id, args, account_id=account_id
        )

    async def tracking_plans_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SegmentTrackingPlansListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all tracking plans defined in the Segment workspace with cursor-based pagination. Tracking plans define the expected event schemas and properties for data governance and validation. Agents should use this to review data schemas before implementing new tracking or to verify plan contents. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "segment.tracking_plans_list", owner_id, args, account_id=account_id
        )

    async def workspace_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SegmentWorkspaceGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns metadata about the authenticated Segment workspace, including its name, display name, and basic settings. Agents should use this to verify which workspace is currently connected or to retrieve workspace-level identifiers before performing other operations. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "segment.workspace_get", owner_id, args, account_id=account_id
        )
