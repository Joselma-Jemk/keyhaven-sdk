# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `factorial` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FactorialEmployeeCreateWithContractBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employee: dict[str, Any]
    contract_version: dict[str, Any]


class FactorialEmployeeCreateWithContractRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: FactorialEmployeeCreateWithContractBody


class FactorialEmployeeGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employee_id: str


class FactorialEmployeeGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FactorialEmployeeGetPath


class FactorialEmployeeUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employee_id: str


class FactorialEmployeeUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str | None = None
    last_name: str | None = None
    email: str | None = None
    phone_number: str | None = None
    gender: str | None = None
    birthday: str | None = None
    country: str | None = None


class FactorialEmployeeUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FactorialEmployeeUpdatePath
    body: FactorialEmployeeUpdateBody = Field(default_factory=FactorialEmployeeUpdateBody)


class FactorialEmployeesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: str | None = None
    emails: str | None = None
    names: str | None = None
    page: int = 1
    limit: int | None = None


class FactorialEmployeesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FactorialEmployeesListQuery | None = None


class FactorialLeavesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employee_ids: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None
    state: str | None = None
    page: int = 1


class FactorialLeavesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FactorialLeavesListQuery | None = None


class FactorialShiftsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employee_ids: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None
    page: int = 1


class FactorialShiftsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FactorialShiftsListQuery | None = None


class FactorialNamespace:
    """Typed accessor for the `factorial` app. Reach it via `client.apps.factorial`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def employee_create_with_contract(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialEmployeeCreateWithContractRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new employee record in Factorial along with an initial contract version in a single API call, specifying personal details, role, compensation, start date, and working hours. This action adds the person to the company's HR system and establishes legally-relevant employment terms. Use it when an agent needs to onboard a new hire, but confirm before creating binding employment records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial.employee_create_with_contract", owner_id, args, account_id=account_id
        )

    async def employee_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialEmployeeGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full profile of a single Factorial employee by their numeric ID, including personal details, job information, and employment metadata. This read-only operation returns sensitive HR data such as contact info and role. Use it when an agent needs detailed employee information for verification or reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial.employee_get", owner_id, args, account_id=account_id
        )

    async def employee_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialEmployeeUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing Factorial employee's personal information such as name, email, phone, gender, birthday, or country. Changes are persisted immediately in the company's HR records and may affect payroll, communications, and compliance. Use it when an agent needs to correct or update an employee's profile data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial.employee_update", owner_id, args, account_id=account_id
        )

    async def employees_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialEmployeesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of employees for the authenticated Factorial company, filterable by IDs, emails, or name substrings. Each entry includes personal details such as name, email, role, department, and employment status. Use it when an agent needs to browse the company directory or search for specific employees.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial.employees_list", owner_id, args, account_id=account_id
        )

    async def leaves_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialLeavesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of time-off requests across the company, filterable by employee, date range, and approval state (requested, approved, denied, cancelled). Each record shows the employee, leave type, dates, and status. Use it when an agent needs to check leave balances, pending requests, or attendance patterns.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial.leaves_list", owner_id, args, account_id=account_id
        )

    async def shifts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialShiftsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches attendance shifts recorded in Factorial, filterable by employee and date range, with pagination support. Shift records include clock-in/out times and total hours worked. Use it when an agent needs to review attendance data or verify worked hours for payroll preparation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial.shifts_list", owner_id, args, account_id=account_id
        )
