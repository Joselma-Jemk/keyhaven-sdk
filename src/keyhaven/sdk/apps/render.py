# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `render` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class RenderCancelDeployPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str
    deployId: str


class RenderCancelDeployRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderCancelDeployPath


class RenderCreateKeyValueInstanceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    ownerId: str
    region: Literal["oregon", "ohio", "virginia", "ireland", "frankfurt", "singapore"]
    plan: Literal["starter", "standard"]


class RenderCreateKeyValueInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: RenderCreateKeyValueInstanceBody


class RenderCreatePostgresInstanceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    ownerId: str
    plan: Literal["free", "starter", "standard", "pro", "pro_plus"] = "free"
    region: Literal["oregon", "ohio", "virginia", "iowa", "frankfurt", "singapore"] = "oregon"
    version: Literal["11", "12", "13", "14", "15", "16"] = "16"
    databaseName: str | None = None
    databaseUser: str | None = None


class RenderCreatePostgresInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: RenderCreatePostgresInstanceBody


class RenderCreateStaticSiteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(default="static_site", alias="type")
    name: str
    ownerId: str
    repo: str
    autoDeploy: Literal["yes", "no"] = "yes"
    branch: str | None = None
    image: dict[str, Any] | None = None
    buildFilter: dict[str, Any] | None = None
    rootDir: str | None = None
    envVars: list[Any] | None = None
    environmentId: str | None = None
    serviceDetails: dict[str, Any] | None = None


class RenderCreateStaticSiteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: RenderCreateStaticSiteBody


class RenderCreateWebServiceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(default="web_service", alias="type")
    name: str
    ownerId: str
    repo: str
    branch: str = "main"
    rootDir: str | None = None
    serviceDetails: dict[str, Any]
    envVars: list[Any] | None = None


class RenderCreateWebServiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: RenderCreateWebServiceBody


class RenderDeleteEnvVarPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str
    key: str


class RenderDeleteEnvVarRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderDeleteEnvVarPath


class RenderGetDeployPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str
    deployId: str


class RenderGetDeployRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderGetDeployPath


class RenderGetEnvVarPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str
    key: str


class RenderGetEnvVarRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderGetEnvVarPath


class RenderGetKeyValueInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    keyValueId: str


class RenderGetKeyValueInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderGetKeyValueInstancePath


class RenderGetPostgresConnectionInfoPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    postgresId: str


class RenderGetPostgresConnectionInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderGetPostgresConnectionInfoPath


class RenderGetPostgresInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    postgresId: str


class RenderGetPostgresInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderGetPostgresInstancePath


class RenderGetServicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderGetServiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderGetServicePath


class RenderGetWorkspacePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ownerId: str


class RenderGetWorkspaceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderGetWorkspacePath


class RenderListDeploysPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderListDeploysQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 20
    cursor: str | None = None


class RenderListDeploysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderListDeploysPath
    query: RenderListDeploysQuery | None = None


class RenderListEnvVarsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderListEnvVarsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderListEnvVarsPath


class RenderListKeyValueInstancesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ownerId: str
    limit: int = 20
    cursor: str | None = None


class RenderListKeyValueInstancesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RenderListKeyValueInstancesQuery


class RenderListLogLabelValuesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ownerId: str
    label: Literal["host", "instance", "statusCode", "method", "level", "type"]
    startTime: str | None = None
    endTime: str | None = None
    direction: Literal["backward", "forward"] = "backward"
    resource: list[Any]
    instance: list[Any] | None = None
    host: list[Any] | None = None
    statusCode: list[Any] | None = None
    method: list[Any] | None = None
    level: list[Any] | None = None
    type_: list[Any] | None = Field(default=None, alias="type")
    text: list[Any] | None = None
    path: list[Any] | None = None
    limit: int = 20


class RenderListLogLabelValuesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RenderListLogLabelValuesQuery


class RenderListLogsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ownerId: str
    startTime: str | None = None
    endTime: str | None = None
    direction: Literal["backward", "forward"] = "backward"
    resource: list[Any]
    instance: list[Any] | None = None
    host: list[Any] | None = None
    statusCode: list[Any] | None = None
    method: list[Any] | None = None
    level: list[Any] | None = None
    type_: list[Any] | None = Field(default=None, alias="type")
    text: list[Any] | None = None
    path: list[Any] | None = None
    limit: int = 20


class RenderListLogsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RenderListLogsQuery


class RenderListPostgresInstancesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ownerId: str | None = None
    limit: int = 20
    cursor: str | None = None


class RenderListPostgresInstancesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RenderListPostgresInstancesQuery | None = None


class RenderListServicesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ownerId: str | None = None
    limit: int = 20
    cursor: str | None = None


class RenderListServicesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RenderListServicesQuery | None = None


class RenderListWorkspaceMembersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ownerId: str


class RenderListWorkspaceMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderListWorkspaceMembersPath


class RenderListWorkspacesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class RenderRestartPostgresInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    postgresId: str


class RenderRestartPostgresInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderRestartPostgresInstancePath


class RenderRestartServicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderRestartServiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderRestartServicePath


class RenderResumePostgresInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    postgresId: str


class RenderResumePostgresInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderResumePostgresInstancePath


class RenderResumeServicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderResumeServiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderResumeServicePath


class RenderRollbackDeployPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str
    deployId: str


class RenderRollbackDeployRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderRollbackDeployPath


class RenderSetEnvVarPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str
    key: str


class RenderSetEnvVarBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    value: str


class RenderSetEnvVarRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderSetEnvVarPath
    body: RenderSetEnvVarBody


class RenderSuspendPostgresInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    postgresId: str


class RenderSuspendPostgresInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderSuspendPostgresInstancePath


class RenderSuspendServicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderSuspendServiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderSuspendServicePath


class RenderTriggerDeployPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderTriggerDeployRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderTriggerDeployPath


class RenderUpdateEnvVarsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: dict[str, Any]
    body: list[Any]


class RenderUpdatePostgresInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    postgresId: str


class RenderUpdatePostgresInstanceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    plan: Literal["free", "starter", "standard", "pro", "pro_plus"] | None = None
    autoSuspend: bool | None = None


class RenderUpdatePostgresInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderUpdatePostgresInstancePath
    body: RenderUpdatePostgresInstanceBody = Field(default_factory=RenderUpdatePostgresInstanceBody)


class RenderUpdateServicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceId: str


class RenderUpdateServiceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    serviceDetails: dict[str, Any] | None = None


class RenderUpdateServiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RenderUpdateServicePath
    body: RenderUpdateServiceBody | None = None


class RenderNamespace:
    """Typed accessor for the `render` app. Reach it via `client.apps.render`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def cancel_deploy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderCancelDeployRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Aborts a deployment that is currently in progress (building or deploying stage). The service continues to run the previous deployment version without interruption. Use this to stop a deploy that was triggered by mistake, that is stuck, or that contains unwanted changes; only actively running deploys can be cancelled.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.cancel_deploy", owner_id, args, account_id=account_id
        )

    async def create_key_value_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderCreateKeyValueInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new Redis-compatible Key Value instance with the specified name, plan, and region. This creates a billable resource with ongoing costs based on the chosen plan. Use this to add a caching or session store layer to your application; the instance will be accessible via its connection endpoint once provisioned.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.create_key_value_instance", owner_id, args, account_id=account_id
        )

    async def create_postgres_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderCreatePostgresInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new PostgreSQL database instance with the specified name, plan, region, and Postgres version. This creates a billable resource with ongoing costs based on the chosen plan. Use this to provision a database for a new or existing application; the instance will be ready for connections once provisioned.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.create_postgres_instance", owner_id, args, account_id=account_id
        )

    async def create_static_site(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderCreateStaticSiteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new static site on Render from a Git repository, configured with build command, publish path, and optional preview deployments or custom domains. This creates a billable resource on non-free plans and the site will be served from the specified publish directory. Use this to host a frontend application or static website; the site can be deleted or modified later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.create_static_site", owner_id, args, account_id=account_id
        )

    async def create_web_service(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderCreateWebServiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new web service on Render from a Git repository with the specified build and runtime configuration, including environment variables and plan. This creates a billable resource if the plan is not free, and the service will be accessible at its Render URL once the first deploy succeeds. Use this to deploy a new web application; the service can be suspended or deleted later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.create_web_service", owner_id, args, account_id=account_id
        )

    async def delete_env_var(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderDeleteEnvVarRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a single environment variable from a service by its key. If the service depends on this variable at runtime, it may fail to start or behave unexpectedly on the next deploy or restart. Use this to clean up unused or sensitive configuration values.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.delete_env_var", owner_id, args, account_id=account_id
        )

    async def get_deploy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderGetDeployRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a specific deployment, including its status (live, building, failed), commit SHA, commit message, and any error messages from the build process. No data is modified. Use this to inspect a deploy progress, troubleshoot build failures, or verify a rollback target before rolling back.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.get_deploy", owner_id, args, account_id=account_id
        )

    async def get_env_var(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderGetEnvVarRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the value of a single environment variable for a service by its key name, including potentially sensitive values like API keys or database credentials. No data is modified. Use this to look up a specific configuration value without fetching the entire env var list.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.get_env_var", owner_id, args, account_id=account_id
        )

    async def get_key_value_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderGetKeyValueInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a specific Key Value instance, including its plan, region, status, and connection details. No data is modified. Use this to inspect an instance configuration or verify it is healthy before connecting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.get_key_value_instance", owner_id, args, account_id=account_id
        )

    async def get_postgres_connection_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderGetPostgresConnectionInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the connection details for a PostgreSQL instance, including the internal and external connection strings, host, port, database name, and user credentials. This exposes sensitive authentication information. No data is modified. Use this to obtain the details needed to connect an application or tool to the database.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.get_postgres_connection_info", owner_id, args, account_id=account_id
        )

    async def get_postgres_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderGetPostgresInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a specific PostgreSQL instance, including its plan, region, version, status, disk usage, and connection limits. No data is modified. Use this to inspect an instance configuration before making changes or to check its health.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.get_postgres_instance", owner_id, args, account_id=account_id
        )

    async def get_service(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderGetServiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details of a specific service by its ID, including its current state, URL, plan, region, and Git repository configuration. No data is modified. Use this to inspect a service before performing management actions like suspension, restart, or deployment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.get_service", owner_id, args, account_id=account_id
        )

    async def get_workspace(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderGetWorkspaceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a single workspace identified by its owner ID, including its name, billing plan, and creation date. No data is modified. Use this to inspect a specific workspace configuration or to verify the workspace exists before creating resources in it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.get_workspace", owner_id, args, account_id=account_id
        )

    async def list_deploys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListDeploysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of deployment records for a service, including each deploy status (live, building, failed, etc.), commit message, and creation time. No data is modified. Use this to review deployment history, find a specific deploy ID for rollback, or monitor recent deployment activity.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_deploys", owner_id, args, account_id=account_id
        )

    async def list_env_vars(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListEnvVarsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all environment variables defined for a specific service as key-value pairs, including their current values. No data is modified. Use this to audit the current environment configuration before making changes or to troubleshoot service behaviour.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_env_vars", owner_id, args, account_id=account_id
        )

    async def list_key_value_instances(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListKeyValueInstancesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all Redis-compatible Key Value instances across your workspaces, optionally filtered by ownerId. Each entry includes the instance name, plan, region, and status. No data is modified. Use this to discover available KV instances for connection or management.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_key_value_instances", owner_id, args, account_id=account_id
        )

    async def list_log_label_values(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListLogLabelValuesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Discovers the distinct values that exist for a given log label (such as host, statusCode, method, or level) within the logs matching the provided filters. No data is modified. Use this to explore available filter values before running detailed log queries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_log_label_values", owner_id, args, account_id=account_id
        )

    async def list_logs(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListLogsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves log entries matching the specified filters, such as resource, time range, severity level, or search text. Supports filtering by service, instance, host, status code, HTTP method, and more. No data is modified. Use this to investigate application errors, monitor request traffic, or debug build failures.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("render.list_logs", owner_id, args, account_id=account_id)

    async def list_postgres_instances(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListPostgresInstancesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all PostgreSQL database instances across your workspaces, optionally filtered by ownerId. Each entry includes the instance name, plan, region, and status. No data is modified. Use this to discover available databases for connection or management operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_postgres_instances", owner_id, args, account_id=account_id
        )

    async def list_services(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListServicesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all services across your workspaces, optionally filtered by ownerId. Each entry includes the service name, type (web service, static site, cron job, etc.), status, and environment. No data is modified. Use this to discover and select services for subsequent operations like deploys or env var management.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_services", owner_id, args, account_id=account_id
        )

    async def list_workspace_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListWorkspaceMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all users who are members of a specific workspace, showing their roles (owner, admin, developer, etc.) and associated email addresses. No data is modified. Use this to audit workspace access or to find user IDs for member management.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_workspace_members", owner_id, args, account_id=account_id
        )

    async def list_workspaces(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderListWorkspacesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all workspaces (also called teams/owners) associated with your Render account. Each workspace contains services, databases, and members with shared billing. No data is modified. Use this to discover available workspace IDs before performing workspace-scoped operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.list_workspaces", owner_id, args, account_id=account_id
        )

    async def restart_postgres_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderRestartPostgresInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restarts a PostgreSQL instance, which briefly interrupts all database connections while the process reloads. Pending configuration changes may take effect. Use this to recover from an unresponsive state or to apply certain settings that require a restart.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.restart_postgres_instance", owner_id, args, account_id=account_id
        )

    async def restart_service(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderRestartServiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Triggers a restart of a running service, causing a brief downtime while the service process is terminated and recreated with the current configuration and latest deploy image. No code is rebuilt or redeployed. Use this to apply configuration changes or recover from an unhealthy state without triggering a new deployment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.restart_service", owner_id, args, account_id=account_id
        )

    async def resume_postgres_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderResumePostgresInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously suspended PostgreSQL instance to active status, making it available for connections again. The database resumes with all data intact. Use this to bring a suspended database back online for applications that need to access it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.resume_postgres_instance", owner_id, args, account_id=account_id
        )

    async def resume_service(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderResumeServiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously suspended service to its running state, making it accessible again at its URL. The service resumes with its last successful deploy and configuration. Use this to bring a service back online after it was suspended for cost savings or maintenance.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.resume_service", owner_id, args, account_id=account_id
        )

    async def rollback_deploy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderRollbackDeployRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Reverts a service to a previous deployment by redeploying that specific version as the new live deployment. This creates a new deploy record that mirrors the targeted one. Use this to quickly recover from a bad deploy by restoring a known-good version; the rollback appears in the deployment history.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.rollback_deploy", owner_id, args, account_id=account_id
        )

    async def set_env_var(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderSetEnvVarRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new environment variable or updates the value of an existing one for a service. If the key already exists, its value is overwritten; if it does not, it is added without affecting other variables. Use this for targeted env var changes; the new value takes effect on the next deploy or restart.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.set_env_var", owner_id, args, account_id=account_id
        )

    async def suspend_postgres_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderSuspendPostgresInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Suspends a PostgreSQL instance, making it unavailable for connections while preserving all data and configuration on disk. No data is lost. Use this to stop billing for idle databases (if the plan supports it) or to temporarily disable access; the operation is reversible via RESUME_POSTGRES_INSTANCE.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.suspend_postgres_instance", owner_id, args, account_id=account_id
        )

    async def suspend_service(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderSuspendServiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Stops a running service and frees its compute resources, making it inaccessible at its URL until resumed. The service configuration, data, and deploy history are preserved. Use this to temporarily disable a service to save costs or during maintenance; the operation is fully reversible via RESUME_SERVICE.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.suspend_service", owner_id, args, account_id=account_id
        )

    async def trigger_deploy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderTriggerDeployRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Starts a new deployment of the service using the latest code from its connected Git repository branch. The service may experience transient states during the build and deploy process, and the new version replaces the current live version once successful. Use this to push new code to production or staging; the service URL will serve the new version once the deploy completes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.trigger_deploy", owner_id, args, account_id=account_id
        )

    async def update_env_vars(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderUpdateEnvVarsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Replaces the entire set of environment variables for a service with a new set of key-value pairs. Any existing env vars not included in the request are deleted, and new ones are added. Use this to bulk-configure or reset a service environment; be aware that this removes any unlisted variables, which could affect service behaviour on the next deploy or restart.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.update_env_vars", owner_id, args, account_id=account_id
        )

    async def update_postgres_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderUpdatePostgresInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies the configuration of an existing PostgreSQL instance, such as changing its plan (which affects cost and performance) or toggling auto-suspend behaviour. The change takes effect without data loss. Use this to scale a database up or down or to adjust its idle behaviour.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.update_postgres_instance", owner_id, args, account_id=account_id
        )

    async def update_service(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RenderUpdateServiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies the configuration of an existing service, such as its build command, start command, plan, or region. Changes take effect on the next deploy or restart. Use this to adjust a service runtime behaviour or resource tier without creating a new service.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "render.update_service", owner_id, args, account_id=account_id
        )
