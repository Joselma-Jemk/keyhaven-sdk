# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `pendo` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PendoAccountsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class PendoAccountsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PendoAccountsGetPath


class PendoAccountsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order_by: str | None = None


class PendoAccountsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PendoAccountsListQuery | None = None


class PendoFeaturesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    is_private: bool | None = None
    wanted_by: int | None = None
    order_by: str | None = None
    tags: str | None = None
    products: str | None = None


class PendoFeaturesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PendoFeaturesListQuery | None = None


class PendoUsersGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: float


class PendoUsersGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PendoUsersGetPath


class PendoUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    role: str
    account: int | None = None
    start: int | None = None
    limit: int | None = None


class PendoUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PendoUsersListQuery


class PendoNamespace:
    """Typed accessor for the `pendo` app. Reach it via `client.apps.pendo`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def accounts_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PendoAccountsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a single Pendo Feedback account record by its string ID, including full account details and associated metadata. This is read-only. Use this when the agent needs the complete profile of a specific account, for example to inspect its settings or linked users before performing an account-scoped operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pendo.accounts_get", owner_id, args, account_id=account_id
        )

    async def accounts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PendoAccountsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a list of Pendo Feedback accounts, with optional sorting by a specified field. Each account entry includes its name, ID, and associated metadata. This is a read-only lookup. Use this when the agent needs to find an account ID or browse available accounts before querying users or features by account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pendo.accounts_list", owner_id, args, account_id=account_id
        )

    async def features_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PendoFeaturesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of feature requests from Pendo Feedback, filterable by privacy status, minimum number of upvotes (wanted_by), tags, and associated products. Each feature includes its title, description, status, vote count, and assignee. This is read-only. Use this when the agent needs to browse submitted feature requests to gauge demand or find a feature to prioritize.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pendo.features_list", owner_id, args, account_id=account_id
        )

    async def users_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PendoUsersGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a single Pendo Feedback user record by its numeric ID, including profile details, role, and account association. This is a read-only lookup. Use this when the agent needs detailed information about a specific user, such as verifying their role or account membership.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("pendo.users_get", owner_id, args, account_id=account_id)

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PendoUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of Pendo Feedback users, optionally filtered by role or account ID. Each user record includes profile details, role, and associated account. This is a read-only query. Use this when the agent needs to find a user ID, verify user existence, or audit who has access to the Pendo Feedback portal.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("pendo.users_list", owner_id, args, account_id=account_id)
