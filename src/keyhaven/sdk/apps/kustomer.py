# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `kustomer` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class KustomerConversationGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    conversation_id: str


class KustomerConversationGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: KustomerConversationGetPath


class KustomerConversationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    pageSize: int | None = None
    status: str | None = None


class KustomerConversationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KustomerConversationsListQuery | None = None


class KustomerCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    emails: list[Any] | None = None
    phones: list[Any] | None = None
    externalId: str | None = None


class KustomerCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: KustomerCustomerCreateBody = Field(default_factory=KustomerCustomerCreateBody)


class KustomerCustomerGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer_id: str


class KustomerCustomerGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: KustomerCustomerGetPath


class KustomerCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    pageSize: int | None = None
    sort: str | None = None


class KustomerCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KustomerCustomersListQuery | None = None


class KustomerNamespace:
    """Typed accessor for the `kustomer` app. Reach it via `client.apps.kustomer`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def conversation_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KustomerConversationGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single Kustomer conversation by its unique identifier, including all messages, participants, timestamps, and associated customer and ticket data. This provides the complete transcript and metadata of a support interaction. This operation is read-only and does not modify any data. Use this to review a specific customer conversation in depth after locating its ID.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "kustomer.conversation_get", owner_id, args, account_id=account_id
        )

    async def conversations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KustomerConversationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of conversations in your Kustomer account, with optional filtering by status and configurable page size. Each conversation includes the participants, channel, and latest message preview. This operation is read-only and does not modify any data. Use this to monitor active support interactions or find a specific conversation ID for detailed retrieval.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "kustomer.conversations_list", owner_id, args, account_id=account_id
        )

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KustomerCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer profile in Kustomer with details such as name, email, phone, and external system ID. Once created, the customer record becomes available for conversation routing, reporting, and CRM workflows across the support organization. This operation creates a persistent record that can later be updated or merged. Use this to onboard new customers into your CRM from external systems or manual data entry.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "kustomer.customer_create", owner_id, args, account_id=account_id
        )

    async def customer_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KustomerCustomerGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full profile of a single Kustomer customer by their unique identifier, including all associated metadata, custom fields, and linked conversations. This provides the complete customer context for support and CRM workflows. This operation is read-only and does not modify any data. Use this when you need the full customer record after finding their ID via a list operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "kustomer.customer_get", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KustomerCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all customer records in your Kustomer account, with optional sorting and configurable page size. Each record contains profile information such as name, email, phone, and external identifiers. This operation is read-only and does not modify any data. Use this to browse the customer base or locate a specific customer ID for detailed inspection.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "kustomer.customers_list", owner_id, args, account_id=account_id
        )
