# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `linkedin` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class LinkedinMeGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Linkedin_Version: str = Field(default="202605", alias="Linkedin-Version")


class LinkedinMeGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: LinkedinMeGetHeader | None = None


class LinkedinOrganizationFindQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    q: str = "roleAssignee"
    projection: str | None = None
    count: int | None = None
    start: int | None = None


class LinkedinOrganizationFindHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Linkedin_Version: str = Field(default="202605", alias="Linkedin-Version")
    X_Restli_Protocol_Version: str = Field(default="2.0.0", alias="X-Restli-Protocol-Version")


class LinkedinOrganizationFindRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LinkedinOrganizationFindQuery = Field(default_factory=LinkedinOrganizationFindQuery)
    header: LinkedinOrganizationFindHeader | None = None


class LinkedinPostCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Linkedin_Version: str = Field(default="202605", alias="Linkedin-Version")
    X_Restli_Protocol_Version: str = Field(default="2.0.0", alias="X-Restli-Protocol-Version")
    Content_Type: str = Field(default="application/json", alias="Content-Type")


class LinkedinPostCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    author: str
    commentary: str
    visibility: str
    lifecycleState: str
    distribution: dict[str, Any]
    content: dict[str, Any] | None = None
    isReshareDisabledByAuthor: bool | None = None


class LinkedinPostCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: LinkedinPostCreateHeader = Field(default_factory=LinkedinPostCreateHeader)
    body: LinkedinPostCreateBody


class LinkedinPostDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    post_urn: str


class LinkedinPostDeleteHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Linkedin_Version: str = Field(default="202605", alias="Linkedin-Version")
    X_Restli_Protocol_Version: str = Field(default="2.0.0", alias="X-Restli-Protocol-Version")
    X_RestLi_Method: str = Field(default="DELETE", alias="X-RestLi-Method")


class LinkedinPostDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LinkedinPostDeletePath
    header: LinkedinPostDeleteHeader | None = None


class LinkedinPostGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    post_urn: str


class LinkedinPostGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    viewContext: str | None = None


class LinkedinPostGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Linkedin_Version: str = Field(default="202605", alias="Linkedin-Version")
    X_Restli_Protocol_Version: str = Field(default="2.0.0", alias="X-Restli-Protocol-Version")


class LinkedinPostGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LinkedinPostGetPath
    query: LinkedinPostGetQuery | None = None
    header: LinkedinPostGetHeader | None = None


class LinkedinPostsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    author: str
    q: str
    viewContext: str | None = None
    count: int | None = None
    start: int | None = None
    sortBy: str | None = None


class LinkedinPostsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Linkedin_Version: str = Field(default="202605", alias="Linkedin-Version")
    X_Restli_Protocol_Version: str = Field(default="2.0.0", alias="X-Restli-Protocol-Version")


class LinkedinPostsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LinkedinPostsListQuery
    header: LinkedinPostsListHeader | None = None


class LinkedinNamespace:
    """Typed accessor for the `linkedin` app. Reach it via `client.apps.linkedin`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def me_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkedinMeGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the authenticated user's LinkedIn profile information (sub, name, and email from the OpenID Connect endpoint). Use this to confirm the current identity, verify OAuth scopes, or obtain the person URN needed for other API calls. Read-only — no profile data is changed and no trace is left on the account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("linkedin.me_get", owner_id, args, account_id=account_id)

    async def organization_find(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkedinOrganizationFindRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists the LinkedIn organizations that the authenticated member can administer, along with their role for each. Use this to discover which organization IDs are available before posting or managing content as an organization page. Read-only — no organizations or permissions are modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "linkedin.organization_find", owner_id, args, account_id=account_id
        )

    async def post_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkedinPostCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Publishes an organic post (text, image, video, or article) to a LinkedIn member's or organization's feed, or saves it as a draft. The post becomes visible to the selected audience (public, connections, or logged-in) immediately upon publishing and can be engaged with (liked, commented, reshared). This action is externally visible and cannot be automatically reverted — deletion removes the post but any engagement is lost.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "linkedin.post_create", owner_id, args, account_id=account_id
        )

    async def post_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkedinPostDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a LinkedIn post by its URN, making it inaccessible to all viewers. Any existing likes, comments, and shares are lost along with the post. This operation is idempotent (deleting an already-deleted post succeeds) but cannot be undone — the content and all engagement data are gone.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "linkedin.post_delete", owner_id, args, account_id=account_id
        )

    async def post_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkedinPostGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single LinkedIn post (text, media, engagement metrics) by its unique URN identifier. Use this to inspect post content and performance after publishing. The operation is read-only and leaves no trace on the platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "linkedin.post_get", owner_id, args, account_id=account_id
        )

    async def posts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LinkedinPostsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of posts from a specific LinkedIn member or organization, with optional draft inclusion and sorting. Use this to audit published content, find a post by its author, or prepare for analytics. Read-only — no content is created or modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "linkedin.posts_list", owner_id, args, account_id=account_id
        )
