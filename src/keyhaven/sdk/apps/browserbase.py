# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `browserbase` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BrowserbaseCreateContextBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    projectId: str


class BrowserbaseCreateContextRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: BrowserbaseCreateContextBody


class BrowserbaseCreateSessionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    projectId: str
    extensionId: str | None = None
    browserSettings: dict[str, Any] | None = None
    timeout: int | None = None
    keepAlive: bool | None = None
    proxies: bool | None = None
    region: Literal["us-west-2", "us-east-1", "eu-central-1", "ap-southeast-1"] | None = None
    userMetadata: dict[str, Any] | None = None


class BrowserbaseCreateSessionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: BrowserbaseCreateSessionBody


class BrowserbaseDeleteExtensionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseDeleteExtensionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseDeleteExtensionPath


class BrowserbaseGetContextPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetContextRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetContextPath


class BrowserbaseGetExtensionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetExtensionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetExtensionPath


class BrowserbaseGetProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetProjectPath


class BrowserbaseGetProjectUsagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetProjectUsageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetProjectUsagePath


class BrowserbaseGetSessionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetSessionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetSessionPath


class BrowserbaseGetSessionDebugPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetSessionDebugRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetSessionDebugPath


class BrowserbaseGetSessionDownloadsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetSessionDownloadsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetSessionDownloadsPath


class BrowserbaseGetSessionLogsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetSessionLogsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetSessionLogsPath


class BrowserbaseGetSessionRecordingPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseGetSessionRecordingRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseGetSessionRecordingPath


class BrowserbaseListProjectsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class BrowserbaseListSessionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: Literal["RUNNING", "ERROR", "TIMED_OUT", "COMPLETED"] | None = None
    q: str | None = None


class BrowserbaseListSessionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: BrowserbaseListSessionsQuery | None = None


class BrowserbaseUpdateContextPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseUpdateContextRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseUpdateContextPath


class BrowserbaseUpdateSessionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class BrowserbaseUpdateSessionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    projectId: str
    status: Literal["REQUEST_RELEASE"]


class BrowserbaseUpdateSessionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BrowserbaseUpdateSessionPath
    body: BrowserbaseUpdateSessionBody


class BrowserbaseNamespace:
    """Typed accessor for the `browserbase` app. Reach it via `client.apps.browserbase`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_context(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseCreateContextRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a persistent browser profile (context) that can be reused across multiple sessions. Contexts store cookies, localStorage, and other browsing data so that sessions starting from the same context share a consistent state. This is reversible — contexts can be deleted later. Use this when an agent needs to maintain authenticated sessions or persistent state across multiple browser automation runs.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.create_context", owner_id, args, account_id=account_id
        )

    async def create_session(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseCreateSessionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new remote browser session on Browserbase's cloud infrastructure, provisioning a real browser instance with configurable fingerprinting, proxy, viewport, stealth, and region settings. This action consumes compute credits associated with the project and can be used for web automation, scraping, or testing tasks. Use this when you need a live browser environment that mimics real user behavior, including ad blocking, captcha solving, and session recording.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.create_session", owner_id, args, account_id=account_id
        )

    async def delete_extension(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseDeleteExtensionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a previously uploaded browser extension from the Browserbase account. Once deleted, the extension cannot be used in new sessions and its ID becomes invalid. This is irreversible. Use this only when an agent needs to clean up unused extensions, remove outdated versions, or enforce organisational compliance by retiring custom extensions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.delete_extension", owner_id, args, account_id=account_id
        )

    async def get_context(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetContextRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches metadata about a specific browser context (persistent profile) by its unique identifier, including creation date, associated project, and usage information. This is read-only and does not modify the context or any sessions using it. Use this to verify context existence, inspect its properties, or retrieve the context ID for use when creating new sessions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_context", owner_id, args, account_id=account_id
        )

    async def get_extension(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetExtensionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves metadata about a specific browser extension that was uploaded to Browserbase, including its ID, name, and configuration. This is read-only and does not modify any extension data. Use this to verify extension details, check if a particular extension is available, or retrieve the extension ID needed for attaching it to a new session.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_extension", owner_id, args, account_id=account_id
        )

    async def get_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific Browserbase project by its unique ID, including its name, settings, and associated metadata. This is a read-only query that does not modify any state. Use this to verify project configuration, retrieve project-level defaults, or confirm the project ID before creating sessions or contexts within it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_project", owner_id, args, account_id=account_id
        )

    async def get_project_usage(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetProjectUsageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns resource consumption statistics for a specific Browserbase project, including session minutes, compute time, and other billing-related metrics. This is a read-only query with no side effects. Use this to monitor usage against plan limits, estimate costs, or decide whether to adjust session creation frequency to stay within budget.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_project_usage", owner_id, args, account_id=account_id
        )

    async def get_session(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetSessionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed metadata about a specific browser session by its unique identifier, including its current status, configuration settings, geolocation region, resource usage, and timestamps. No session state is altered. Use this to inspect a session's health, check whether automation tasks completed, or retrieve the session's configuration for debugging.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_session", owner_id, args, account_id=account_id
        )

    async def get_session_debug(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetSessionDebugRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves debugging URLs for a specific browser session, including Chrome DevTools Protocol (CDP) endpoints and page inspector links that allow live inspection of the remote browser. No session state is modified. Use this for troubleshooting automation scripts, inspecting page state in real time, or capturing performance data during active browser sessions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_session_debug", owner_id, args, account_id=account_id
        )

    async def get_session_downloads(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetSessionDownloadsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists files that were downloaded during a specific browser session, providing metadata such as file names and download timestamps. The session must have completed or still be active. This is a read-only query that does not download or delete the files. Use this to retrieve the output of automated downloads (e.g., generated PDFs, exported CSVs) after completing a browser automation workflow.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_session_downloads", owner_id, args, account_id=account_id
        )

    async def get_session_logs(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetSessionLogsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the console and browser-level log output produced during a browser session, useful for debugging what happened inside the remote browser. The logs include page errors, network requests, and console messages captured while the session was running. No state is modified. Use this when investigating automation failures, unexpected page behaviour, or JavaScript errors during a browser task.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_session_logs", owner_id, args, account_id=account_id
        )

    async def get_session_recording(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseGetSessionRecordingRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the recorded event log of a browser session, containing a time-ordered sequence of actions and state changes that occurred during the session (page navigations, clicks, network events). This is a read-only operation that does not affect the session or its data. Use this to replay or audit what happened in a session, generate reports of automated workflows, or analyse user-like behaviour for debugging.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.get_session_recording", owner_id, args, account_id=account_id
        )

    async def list_projects(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseListProjectsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Browserbase projects associated with the authenticated account. Each project contains its own sessions, contexts, and billing. This is a read-only operation. Use this when an agent needs to discover available project IDs before creating sessions or to navigate between different projects for multi-tenant automation workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.list_projects", owner_id, args, account_id=account_id
        )

    async def list_sessions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseListSessionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all browser sessions for the authenticated project, including each session's ID, status (RUNNING, ERROR, TIMED_OUT, COMPLETED), configuration, and resource usage. Optionally filter by status or query user metadata. This is a read-only operation that does not affect any running sessions. Use this to audit active sessions, check billing consumption, or find a session ID for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.list_sessions", owner_id, args, account_id=account_id
        )

    async def update_context(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseUpdateContextRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Refreshes the security parameters (encryption keys) of an existing browser context, rotating internal credentials without deleting the context or its stored data. The context remains usable after this operation. Use this when an agent suspects context compromise, needs to rotate secrets for compliance, or wants to mitigate session hijacking risks while preserving the browsing profile.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.update_context", owner_id, args, account_id=account_id
        )

    async def update_session(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BrowserbaseUpdateSessionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Requests early release of a running browser session by setting its status to REQUEST_RELEASE. This stops the session before its configured timeout, preventing additional compute charges while preserving any recorded data. Use this for cost optimisation when an agent has finished its work and no longer needs the session, or to proactively clean up idle sessions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "browserbase.update_session", owner_id, args, account_id=account_id
        )
