# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `amplitude` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AmplitudeEventsBatchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    api_key: str
    events: list[Any]
    options: dict[str, Any] | None = None


class AmplitudeEventsBatchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AmplitudeEventsBatchBody


class AmplitudeEventsSendBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    api_key: str
    events: list[Any]


class AmplitudeEventsSendRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AmplitudeEventsSendBody


class AmplitudeIdentifyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    api_key: str
    identification: list[Any]


class AmplitudeIdentifyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AmplitudeIdentifyBody


class AmplitudeNamespace:
    """Typed accessor for the `amplitude` app. Reach it via `client.apps.amplitude`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def events_batch(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AmplitudeEventsBatchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Bulk-uploads a large volume of analytics events to Amplitude's dedicated batch ingest endpoint, with no hard per-request event cap. Supports optional ingestion options such as time offset, ingestion metadata, and compression configuration. Use this for backfilling historical data, migrating event archives, or high-throughput pipelines where the standard real-time endpoint is not suitable.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "amplitude.events_batch", owner_id, args, account_id=account_id
        )

    async def events_send(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AmplitudeEventsSendRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends up to 2,000 analytics events to Amplitude via the HTTP API v2 real-time ingest endpoint. Events are processed within seconds and become visible in Amplitude dashboards, user sessions, funnel analyses, and cohort definitions. Use this to instrument user actions — page views, clicks, form submissions, purchases, or any custom event — as they happen.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "amplitude.events_send", owner_id, args, account_id=account_id
        )

    async def identify(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AmplitudeIdentifyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sets or modifies user properties on an existing Amplitude user profile without recording a separate analytics event. Changes apply immediately and affect how the user appears in Amplitude dashboards, cohort definitions, behavioral reports, and user lookup results. Use this to enrich profiles with attributes such as plan tier, signup date, subscription status, or any custom user trait.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "amplitude.identify", owner_id, args, account_id=account_id
        )
