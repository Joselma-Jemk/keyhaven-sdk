# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `serpapi` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SerpapiGoogleImagesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    engine: str = "google_images"
    q: str
    num: int = 10
    safe: str = "active"
    device: str = "desktop"
    no_cache: bool = False
    async_: bool = Field(default=False, alias="async")
    zero_trace: bool = False
    output: str = "json"
    gl: str = "us"
    hl: str = "en"
    google_domain: str = "google.com"
    location: str = "United States"
    uule: str | None = None
    ijn: int = 0
    start: int = 0
    tbm: str = "isch"
    tbs: str | None = None
    filter: int = 0
    nfpr: int = 1
    imgar: str = "s"
    imgcolor: str | None = None
    imgtype: str = "photo"
    imgc: str = "color"
    imgdims: str | None = None


class SerpapiGoogleImagesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SerpapiGoogleImagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SerpapiGoogleImagesQuery
    header: SerpapiGoogleImagesHeader = Field(default_factory=SerpapiGoogleImagesHeader)


class SerpapiGoogleNewsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    engine: str = "google_news"
    q: str
    tbm: str = "d"
    num: int = 10
    gl: str = "us"
    hl: str = "en"
    google_domain: str = "google.com"
    location: str = "United States"
    uule: str | None = None
    device: str = "desktop"
    no_cache: bool = False
    async_: bool = Field(default=False, alias="async")
    output: str = "json"
    filter: int = 0
    nfpr: int = 1
    topic_token: str | None = None
    story_token: str | None = None
    section_token: str | None = None
    publication_token: str | None = None


class SerpapiGoogleNewsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SerpapiGoogleNewsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SerpapiGoogleNewsQuery
    header: SerpapiGoogleNewsHeader = Field(default_factory=SerpapiGoogleNewsHeader)


class SerpapiGoogleScholarQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    engine: str = "google_scholar"
    q: str
    as_ylo: int = 2020
    as_yhi: int = 2024
    scisbd: int = 0
    hl: str = "en"
    as_sdt: str = "0,5"
    as_vis: int = 0
    as_rss: int = 0
    start: int = 0
    num: int = 10
    as_sdtp: str = "0"
    as_sdts: str = "5"
    as_qdr: str = "y"
    as_occt: str = "any"
    as_rights: str = ""
    as_publication: str | None = None
    as_author: str | None = None
    as_subj: str | None = None
    cluster: str | None = None
    cites: str | None = None


class SerpapiGoogleScholarHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SerpapiGoogleScholarRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SerpapiGoogleScholarQuery
    header: SerpapiGoogleScholarHeader = Field(default_factory=SerpapiGoogleScholarHeader)


class SerpapiGoogleSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    engine: str = "google"
    q: str
    num: int = 10
    gl: str = "us"
    hl: str = "en"
    safe: str = "active"
    start: int = 0
    device: str = "desktop"
    no_cache: bool = False
    async_: bool = Field(default=False, alias="async")
    output: str = "json"
    google_domain: str = "google.com"
    location: str = "United States"
    uule: str | None = None
    tbm: str | None = None
    tbs: str | None = None
    filter: int = 0
    nfpr: int = 1


class SerpapiGoogleSearchHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SerpapiGoogleSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SerpapiGoogleSearchQuery
    header: SerpapiGoogleSearchHeader = Field(default_factory=SerpapiGoogleSearchHeader)


class SerpapiGoogleTrendsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    engine: str = "google_trends"
    q: str
    geo: str = "US"
    date: str = "today 12-m"
    hl: str = "en"
    tz: str = "-420"
    data_type: str = "TIMESERIES"
    category: int = 0
    property: str = ""
    include_low_search_volume: bool = False
    include_geo_restrictions: bool = False


class SerpapiGoogleTrendsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SerpapiGoogleTrendsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SerpapiGoogleTrendsQuery
    header: SerpapiGoogleTrendsHeader = Field(default_factory=SerpapiGoogleTrendsHeader)


class SerpapiNamespace:
    """Typed accessor for the `serpapi` app. Reach it via `client.apps.serpapi`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def google_images(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SerpapiGoogleImagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search Google Images for pictures matching a query and return results with thumbnails, source URLs, dimensions, and file metadata. This is a read-only operation that retrieves publicly available image search data without making any changes. Use this function when you need to find visual content for research, content creation inspiration, or asset sourcing based on a descriptive query.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "serpapi.google_images", owner_id, args, account_id=account_id
        )

    async def google_news(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SerpapiGoogleNewsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search Google News for current articles matching a query, with filtering by time range, location, language, or specific publication and story tokens. This is a read-only operation that returns headlines, snippets, source names, and publication dates without altering any data. Use this function when an agent needs up-to-date news coverage, media monitoring, or research on recent events and developments.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "serpapi.google_news", owner_id, args, account_id=account_id
        )

    async def google_scholar(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SerpapiGoogleScholarRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search Google Scholar for academic papers, theses, books, and citations matching a query, with advanced filters for author, publication, date range, and subject area. This is a read-only operation that returns publication metadata, citation counts, and linked versions without modifying any resources. Use this function for academic research, literature reviews, citation analysis, or finding scholarly sources on a specific topic.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "serpapi.google_scholar", owner_id, args, account_id=account_id
        )

    async def google_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SerpapiGoogleSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Execute a Google web search and return organic results, ads, related queries, and knowledge graph data as structured JSON. This is a read-only operation — it fetches public web data without modifying any resources or triggering side effects. Use this function when an agent needs real-time web information, fact-checking, competitive research, or any scenario requiring access to Google's search results.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "serpapi.google_search", owner_id, args, account_id=account_id
        )

    async def google_trends(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SerpapiGoogleTrendsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve Google Trends data for a search term, including interest over time, geographic breakdown, related topics, and related queries over a configurable date range and category. This is a read-only operation that returns analytics data about search volume patterns without affecting any Google services. Use this function for market research, content strategy, SEO analysis, or understanding what topics are gaining popularity in a specific region or timeframe.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "serpapi.google_trends", owner_id, args, account_id=account_id
        )
