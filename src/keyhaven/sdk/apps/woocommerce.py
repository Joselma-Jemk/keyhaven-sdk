# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `woocommerce` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class WoocommerceOrderCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str = "pending"
    customer_id: int = 0
    payment_method: str | None = None
    payment_method_title: str | None = None
    set_paid: bool | None = None
    billing: dict[str, Any] | None = None
    shipping: dict[str, Any] | None = None
    line_items: list[Any] | None = None
    shipping_lines: list[Any] | None = None
    coupon_lines: list[Any] | None = None


class WoocommerceOrderCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: WoocommerceOrderCreateBody = Field(default_factory=WoocommerceOrderCreateBody)


class WoocommerceOrdersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 10
    search: str | None = None
    status: (
        Literal[
            "any",
            "pending",
            "processing",
            "on-hold",
            "completed",
            "cancelled",
            "refunded",
            "failed",
            "trash",
        ]
        | None
    ) = None
    customer: int | None = None
    after: str | None = None
    before: str | None = None


class WoocommerceOrdersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WoocommerceOrdersListQuery | None = None


class WoocommerceProductCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    type_: Literal["simple", "grouped", "external", "variable"] = Field(
        default="simple", alias="type"
    )
    status: Literal["draft", "pending", "private", "publish"] = "publish"
    sku: str | None = None
    regular_price: str | None = None
    sale_price: str | None = None
    description: str | None = None
    short_description: str | None = None
    categories: list[Any] | None = None
    images: list[Any] | None = None
    manage_stock: bool | None = None
    stock_quantity: int | None = None


class WoocommerceProductCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: WoocommerceProductCreateBody


class WoocommerceProductGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class WoocommerceProductGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WoocommerceProductGetPath


class WoocommerceProductUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class WoocommerceProductUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    status: str | None = None
    regular_price: str | None = None
    sale_price: str | None = None
    description: str | None = None
    short_description: str | None = None
    stock_quantity: int | None = None
    categories: list[Any] | None = None


class WoocommerceProductUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WoocommerceProductUpdatePath
    body: WoocommerceProductUpdateBody = Field(default_factory=WoocommerceProductUpdateBody)


class WoocommerceProductsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 10
    search: str | None = None
    status: Literal["any", "draft", "pending", "private", "publish"] | None = None
    category: str | None = None
    sku: str | None = None
    orderby: (
        Literal["date", "id", "include", "title", "slug", "price", "popularity", "rating"] | None
    ) = None
    order: Literal["asc", "desc"] = "desc"


class WoocommerceProductsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WoocommerceProductsListQuery | None = None


class WoocommerceNamespace:
    """Typed accessor for the `woocommerce` app. Reach it via `client.apps.woocommerce`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def order_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WoocommerceOrderCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new order with a customer, line items, shipping and billing addresses, payment method, and optional coupon lines. The set_paid flag can mark the order as paid immediately upon creation, triggering financial and inventory records. Creating an order reserves stock, generates a financial transaction record, and may initiate payment processing. Use this function when an agent needs to place an order on behalf of a customer or create manual administration orders — the operation has direct financial and inventory consequences.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "woocommerce.order_create", owner_id, args, account_id=account_id
        )

    async def orders_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WoocommerceOrdersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of orders from the store, filterable by status (pending, processing, completed, refunded, etc.), customer ID, and date range. This exposes order data including line items, totals, billing and shipping addresses, payment method, and customer notes. Use this function when an agent needs to review sales, check order statuses, or find specific orders for fulfillment — the data includes customer personal and financial details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "woocommerce.orders_list", owner_id, args, account_id=account_id
        )

    async def product_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WoocommerceProductCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new product in the WooCommerce store with a name, type (simple, grouped, external, variable), price, description, categories, images, and stock settings. The product can be published immediately or saved as a draft. Once created with status publish, it appears on the storefront and becomes available for purchase. Use this function when an agent needs to add inventory items programmatically — the new product is immediately visible to customers if published.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "woocommerce.product_create", owner_id, args, account_id=account_id
        )

    async def product_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WoocommerceProductGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single product by its numeric ID, including name, description, price, stock status, categories, images, attributes, and related metadata. This read-only operation returns the complete product object without modifying anything. Use this function when an agent needs to inspect a specific product's configuration or check stock levels before processing an order or updating inventory.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "woocommerce.product_get", owner_id, args, account_id=account_id
        )

    async def product_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WoocommerceProductUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the attributes of an existing product including name, status, regular price, sale price, description, short description, stock quantity, and category assignments. Changes are applied immediately and the updated product is visible on the storefront if its status is publish. Use this function when an agent needs to correct product information, adjust prices for a promotion, or update stock levels after inventory changes — all modifications are live immediately.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "woocommerce.product_update", owner_id, args, account_id=account_id
        )

    async def products_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WoocommerceProductsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of products from the WooCommerce store, filterable by search term, status (draft, publish, etc.), category ID, SKU, and sortable by date, price, popularity, or rating. This is a read-only catalog operation that returns product names, prices, stock levels, and metadata. Use this function to browse inventory, check pricing, or find products by attributes before performing downstream operations like order creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "woocommerce.products_list", owner_id, args, account_id=account_id
        )
