# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `open_weather_map` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class OpenWeatherMapCurrentAndForecastQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    lat: float
    lon: float
    exclude: str = "minutely,hourly,alerts"
    units: Literal["standard", "metric", "imperial"] = "metric"
    lang: Literal[
        "af",
        "al",
        "ar",
        "az",
        "bg",
        "ca",
        "cz",
        "da",
        "de",
        "el",
        "en",
        "eu",
        "fa",
        "fi",
        "fr",
        "gl",
        "he",
        "hi",
        "hr",
        "hu",
        "id",
        "it",
        "ja",
        "kr",
        "la",
        "lt",
        "mk",
        "no",
        "nl",
        "pl",
        "pt",
        "pt_br",
        "ro",
        "ru",
        "sv",
        "sk",
        "sl",
        "sp",
        "sr",
        "th",
        "tr",
        "ua",
        "vi",
        "zh_cn",
        "zh_tw",
        "zu",
    ] = "en"


class OpenWeatherMapCurrentAndForecastRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OpenWeatherMapCurrentAndForecastQuery


class OpenWeatherMapCurrentWeatherQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    lat: float
    lon: float
    units: Literal["standard", "metric", "imperial"] | None = None
    lang: (
        Literal[
            "af",
            "al",
            "ar",
            "az",
            "bg",
            "ca",
            "cz",
            "da",
            "de",
            "el",
            "en",
            "eu",
            "fa",
            "fi",
            "fr",
            "gl",
            "he",
            "hi",
            "hr",
            "hu",
            "id",
            "it",
            "ja",
            "kr",
            "la",
            "lt",
            "mk",
            "no",
            "nl",
            "pl",
            "pt",
            "pt_br",
            "ro",
            "ru",
            "sv",
            "sk",
            "sl",
            "sp",
            "sr",
            "th",
            "tr",
            "ua",
            "vi",
            "zh_cn",
            "zh_tw",
            "zu",
        ]
        | None
    ) = None


class OpenWeatherMapCurrentWeatherRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OpenWeatherMapCurrentWeatherQuery


class OpenWeatherMapForecastQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    lat: float
    lon: float
    units: Literal["standard", "metric", "imperial"] | None = None
    lang: (
        Literal[
            "af",
            "al",
            "ar",
            "az",
            "bg",
            "ca",
            "cz",
            "da",
            "de",
            "el",
            "en",
            "eu",
            "fa",
            "fi",
            "fr",
            "gl",
            "he",
            "hi",
            "hr",
            "hu",
            "id",
            "it",
            "ja",
            "kr",
            "la",
            "lt",
            "mk",
            "no",
            "nl",
            "pl",
            "pt",
            "pt_br",
            "ro",
            "ru",
            "sv",
            "sk",
            "sl",
            "sp",
            "sr",
            "th",
            "tr",
            "ua",
            "vi",
            "zh_cn",
            "zh_tw",
            "zu",
        ]
        | None
    ) = None
    cnt: int | None = None


class OpenWeatherMapForecastRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OpenWeatherMapForecastQuery


class OpenWeatherMapNamespace:
    """Typed accessor for the `open_weather_map` app. Reach it via `client.apps.open_weather_map`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def current_and_forecast(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenWeatherMapCurrentAndForecastRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves current weather conditions and optional forecast data blocks for a geographic location using latitude and longitude. The exclude parameter controls which data blocks are returned: by default minutely, hourly, and alerts are excluded so only current conditions and the daily 7-day forecast are included. Returns temperature, humidity, wind, pressure, UV index, sunrise/sunset times, and daily weather summaries. Use this function when an agent needs both current conditions and the daily outlook in a single call, such as for comprehensive weather briefings. The call is read-only and does not modify any state on the server.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "open_weather_map.current_and_forecast", owner_id, args, account_id=account_id
        )

    async def current_weather(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenWeatherMapCurrentWeatherRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the current weather conditions for a specific geographic location using latitude and longitude coordinates. Returns real-time data including temperature, feels-like temperature, humidity, wind speed and direction, atmospheric pressure, cloud cover, visibility, and weather condition descriptions. Use this function when an agent needs the present weather state at a location (e.g., 'is it raining in Paris now?') without forecast data. The call is read-only and does not modify any state on the server.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "open_weather_map.current_weather", owner_id, args, account_id=account_id
        )

    async def forecast(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenWeatherMapForecastRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a 5-day weather forecast with 3-hour granularity for a specific geographic location by latitude and longitude. Returns predicted data including temperature, feels-like temperature, humidity, wind speed and direction, probability of precipitation, atmospheric pressure, and weather condition descriptions for each 3-hour time step. The optional cnt parameter limits how many timestamps are returned (up to 40). Use this function when an agent needs future weather predictions for trip planning, event scheduling, or any scenario where upcoming conditions matter. The call is read-only and does not modify any state on the server.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "open_weather_map.forecast", owner_id, args, account_id=account_id
        )
