# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `coin_gecko` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CoinGeckoGetCoinMarketChartPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class CoinGeckoGetCoinMarketChartQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    vs_currency: str
    days: str
    interval: str | None = None


class CoinGeckoGetCoinMarketChartRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CoinGeckoGetCoinMarketChartPath
    query: CoinGeckoGetCoinMarketChartQuery


class CoinGeckoGetCoinOhlcPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class CoinGeckoGetCoinOhlcQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    vs_currency: str
    days: str
    interval: str | None = None


class CoinGeckoGetCoinOhlcRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CoinGeckoGetCoinOhlcPath
    query: CoinGeckoGetCoinOhlcQuery


class CoinGeckoGetCoinsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_platform: bool | None = None


class CoinGeckoGetCoinsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CoinGeckoGetCoinsListQuery | None = None


class CoinGeckoGetSimplePriceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: str
    vs_currencies: str
    include_market_cap: bool | None = None
    include_24hr_vol: bool | None = None
    include_24hr_change: bool | None = None
    include_last_updated_at: bool | None = None


class CoinGeckoGetSimplePriceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CoinGeckoGetSimplePriceQuery


class CoinGeckoSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str


class CoinGeckoSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CoinGeckoSearchQuery


class CoinGeckoNamespace:
    """Typed accessor for the `coin_gecko` app. Reach it via `client.apps.coin_gecko`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_coin_market_chart(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CoinGeckoGetCoinMarketChartRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves historical market data (price, market cap, and 24h volume) for a specific coin over a configurable time range. Each data point includes a UNIX timestamp for charting and analysis. Use when an agent needs historical trends for visualization, financial reporting, or backtesting strategies.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "coin_gecko.get_coin_market_chart", owner_id, args, account_id=account_id
        )

    async def get_coin_ohlc(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CoinGeckoGetCoinOhlcRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves candlestick (Open-High-Low-Close) data for a specific coin over a given number of days, suitable for technical analysis charting. Each data point includes the open, high, low, and close prices for the time interval. Use when an agent needs to perform or display technical analysis on historical price action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "coin_gecko.get_coin_ohlc", owner_id, args, account_id=account_id
        )

    async def get_coins_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CoinGeckoGetCoinsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the complete list of all coins supported by CoinGecko, including each coin's ID, name, and symbol. When include_platform is true, contract addresses and platform details are also returned. Use this to obtain the canonical coin ID required as input for other CoinGecko endpoints such as price and market chart queries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "coin_gecko.get_coins_list", owner_id, args, account_id=account_id
        )

    async def get_simple_price(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CoinGeckoGetSimplePriceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches real-time spot prices for one or more cryptocurrencies in one or more fiat or crypto currencies. Optionally includes market capitalization, 24-hour trading volume, 24-hour price change percentage, and last-updated timestamps. Use when an agent needs current market quotes for portfolio tracking, alerts, or financial analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "coin_gecko.get_simple_price", owner_id, args, account_id=account_id
        )

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CoinGeckoSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches CoinGecko's entire index for coins, categories, exchanges, and NFTs matching a text query. Results are grouped by type with relevant metadata such as coin IDs, exchange URLs, and category names. Use when an agent needs to find a coin ID, discover related assets, or explore what CoinGecko tracks for a given term.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "coin_gecko.search", owner_id, args, account_id=account_id
        )
