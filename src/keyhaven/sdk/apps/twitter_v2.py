# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `twitter_v2` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TwitterV2ListsTweetsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class TwitterV2ListsTweetsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    max_results: int | None = None
    pagination_token: str | None = None


class TwitterV2ListsTweetsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwitterV2ListsTweetsListPath
    query: TwitterV2ListsTweetsListQuery | None = None


class TwitterV2TweetsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class TwitterV2TweetsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwitterV2TweetsGetPath


class TwitterV2TweetsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: list[Any]


class TwitterV2TweetsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwitterV2TweetsListQuery


class TwitterV2UsersGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class TwitterV2UsersGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwitterV2UsersGetPath


class TwitterV2UsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: list[Any]


class TwitterV2UsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwitterV2UsersListQuery


class TwitterV2Namespace:
    """Typed accessor for the `twitter_v2` app. Reach it via `client.apps.twitter_v2`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def lists_tweets_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitterV2ListsTweetsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a timeline of recent tweets from a specified Twitter List, which is a curated group of Twitter accounts. Results are paginated and include full tweet objects with author information and engagement metrics. Use this to consume a list's feed programmatically, for example to monitor a curated set of news sources, industry voices, or topic-specific accounts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitter_v2.lists_tweets_list", owner_id, args, account_id=account_id
        )

    async def tweets_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitterV2TweetsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single tweet object by its unique ID, including its text content, author information, engagement metrics (likes, retweets, replies), and any attached media, links, or poll data. The response is read-only and reflects the tweet's current public state. Use this to inspect a specific tweet's full details when you have its ID, for example to verify content or analyze engagement.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitter_v2.tweets_get", owner_id, args, account_id=account_id
        )

    async def tweets_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitterV2TweetsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full tweet objects for up to 100 tweet IDs in a single request, including each tweet's text content, author information, engagement metrics (like count, retweet count, reply count), and attached media or links. The data is read-only and reflects the tweets' current public state. Use this to fetch details of specific tweets by their IDs without needing to perform a search.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitter_v2.tweets_list", owner_id, args, account_id=account_id
        )

    async def users_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitterV2UsersGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the public profile of a single Twitter user by their user ID, including their display name, username, bio description, profile image URL, follower and following counts, and account creation date. The data is read-only and reflects publicly available information. Use this to look up details about a specific user when you have their ID, for example to verify identity or gather profile data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitter_v2.users_get", owner_id, args, account_id=account_id
        )

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitterV2UsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves public profile information for up to 100 Twitter users by their user IDs in a single request. Each result includes the user's display name, username, bio description, profile image URL, follower count, following count, and account creation date. Use this to batch-fetch user profiles for enrichment, analysis, or to verify account details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitter_v2.users_list", owner_id, args, account_id=account_id
        )
