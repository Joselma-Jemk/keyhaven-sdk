# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `printful` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PrintfulOrderCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PrintfulOrderCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    recipient: dict[str, Any]
    items: list[Any]
    shipping: str | None = None
    currency: str | None = None


class PrintfulOrderCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PrintfulOrderCreateHeader = Field(default_factory=PrintfulOrderCreateHeader)
    body: PrintfulOrderCreateBody


class PrintfulOrderGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order_id: int


class PrintfulOrderGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PrintfulOrderGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PrintfulOrderGetPath
    header: PrintfulOrderGetHeader = Field(default_factory=PrintfulOrderGetHeader)


class PrintfulOrdersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 50
    offset: int = 0
    status: str | None = None


class PrintfulOrdersListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PrintfulOrdersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PrintfulOrdersListQuery | None = None
    header: PrintfulOrdersListHeader = Field(default_factory=PrintfulOrdersListHeader)


class PrintfulProductsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 50
    offset: int = 0


class PrintfulProductsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PrintfulProductsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PrintfulProductsListQuery | None = None
    header: PrintfulProductsListHeader = Field(default_factory=PrintfulProductsListHeader)


class PrintfulShippingRatesGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PrintfulShippingRatesGetBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    recipient: dict[str, Any]
    items: list[Any]
    currency: str | None = None


class PrintfulShippingRatesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PrintfulShippingRatesGetHeader = Field(default_factory=PrintfulShippingRatesGetHeader)
    body: PrintfulShippingRatesGetBody


class PrintfulStoresListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PrintfulStoresListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PrintfulStoresListHeader = Field(default_factory=PrintfulStoresListHeader)


class PrintfulNamespace:
    """Typed accessor for the `printful` app. Reach it via `client.apps.printful`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def order_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PrintfulOrderCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new fulfillment order in Printful with a recipient address, items (variant IDs and quantities), shipping method, and currency. This commits to a real production and shipping process that incurs actual costs to the account and triggers payment processing. Use this to fulfill a customer purchase after payment is confirmed. This action can be canceled only before production begins.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "printful.order_create", owner_id, args, account_id=account_id
        )

    async def order_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PrintfulOrderGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details of a specific Printful order by its ID, including line items, production status, shipping details, costs, and tracking information. Use this to check fulfillment progress, verify order details, or provide customer support updates. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "printful.order_get", owner_id, args, account_id=account_id
        )

    async def orders_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PrintfulOrdersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all orders placed through the Printful account, optionally filtered by status (draft, pending, fulfilled, canceled). Each order includes customer details, line items, costs, and shipping information. Use this to review order history, monitor fulfillment, or identify orders needing attention. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "printful.orders_list", owner_id, args, account_id=account_id
        )

    async def products_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PrintfulProductsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of all products available in the Printful catalog, including apparel, accessories, and home goods with variant IDs, prices, and images. Use this to browse the catalog or find a specific product and variant ID before creating an order. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "printful.products_list", owner_id, args, account_id=account_id
        )

    async def shipping_rates_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PrintfulShippingRatesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Calculates available shipping rates for a given set of items and destination address, returning carrier options with estimated costs and delivery times. This is a quote operation and does not create or modify any orders or charges. Use this to present shipping choices to a customer during checkout or to compare costs between carriers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "printful.shipping_rates_get", owner_id, args, account_id=account_id
        )

    async def stores_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PrintfulStoresListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns basic information about all e-commerce stores connected to the Printful account, including the store name, platform type, and connection status. Use this to discover available store IDs before managing products or orders for a specific store. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "printful.stores_list", owner_id, args, account_id=account_id
        )
