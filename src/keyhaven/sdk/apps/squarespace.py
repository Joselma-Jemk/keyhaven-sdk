# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `squarespace` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SquarespaceInventoryListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None


class SquarespaceInventoryListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SquarespaceInventoryListQuery | None = None


class SquarespaceOrderFulfillmentCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SquarespaceOrderFulfillmentCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shouldSendNotification: bool = True
    shipments: list[Any]


class SquarespaceOrderFulfillmentCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SquarespaceOrderFulfillmentCreatePath
    body: SquarespaceOrderFulfillmentCreateBody


class SquarespaceOrderGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SquarespaceOrderGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SquarespaceOrderGetPath


class SquarespaceOrdersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    modifiedAfter: str | None = None
    modifiedBefore: str | None = None
    fulfillmentStatus: str | None = None


class SquarespaceOrdersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SquarespaceOrdersListQuery | None = None


class SquarespaceProductCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(alias="type")
    storePageId: str
    name: str
    description: str | None = None
    urlSlug: str | None = None
    tags: list[Any] | None = None
    isVisible: bool | None = None
    variantAttributes: list[Any] | None = None
    variants: list[Any] | None = None


class SquarespaceProductCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SquarespaceProductCreateBody


class SquarespaceProductsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    type_: str | None = Field(default=None, alias="type")
    modifiedAfter: str | None = None
    modifiedBefore: str | None = None


class SquarespaceProductsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SquarespaceProductsListQuery | None = None


class SquarespaceNamespace:
    """Typed accessor for the `squarespace` app. Reach it via `client.apps.squarespace`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def inventory_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquarespaceInventoryListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves current stock levels for all product variants across the store, with cursor-based pagination for large inventories. Use this to check availability before fulfilling orders or to identify low-stock items that need reordering. Read-only operation that does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squarespace.inventory_list", owner_id, args, account_id=account_id
        )

    async def order_fulfillment_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquarespaceOrderFulfillmentCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a fulfillment (shipment) to an existing order, marking items as shipped. Optionally sends a shipping notification email to the customer when shouldSendNotification is true. Use this after an order has been packed and handed to a carrier. This action is visible to the customer and cannot be fully undone; reversing it requires a separate manual process.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squarespace.order_fulfillment_create", owner_id, args, account_id=account_id
        )

    async def order_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquarespaceOrderGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single order by its unique identifier, including line items, customer information, pricing, and fulfillment status. Use this to inspect a specific order before taking action such as creating a fulfillment. The response contains customer name, email, shipping address and payment details, which are sensitive.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squarespace.order_get", owner_id, args, account_id=account_id
        )

    async def orders_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquarespaceOrdersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of orders from the Squarespace commerce store. Supports filtering by modification date range and fulfillment status (PENDING, FULFILLED, CANCELED). Use this to browse recent orders, monitor fulfillment progress, or find specific orders for further processing. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squarespace.orders_list", owner_id, args, account_id=account_id
        )

    async def product_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquarespaceProductCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new product in the Squarespace store with its variants, pricing, and visibility settings. The product appears in the specified store page immediately and can be set as visible or hidden. Use this to add new inventory items, digital downloads, services, or gift cards. The operation is reversible by deleting or hiding the product afterward.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squarespace.product_create", owner_id, args, account_id=account_id
        )

    async def products_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquarespaceProductsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of products in the Squarespace store, with optional filtering by product type (PHYSICAL, DIGITAL, SERVICE, GIFT_CARD) and modification date. Use this to browse the product catalog, find products by type, or get product IDs for subsequent operations. Read-only and safe to call at any time.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squarespace.products_list", owner_id, args, account_id=account_id
        )
