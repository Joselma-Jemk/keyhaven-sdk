# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `shippo` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ShippoAddressCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    company: str | None = None
    street1: str
    street2: str | None = None
    city: str
    state: str | None = None
    zip: str
    country: str
    phone: str | None = None
    email: str | None = None
    validate_: bool = Field(default=False, alias="validate")


class ShippoAddressCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ShippoAddressCreateBody


class ShippoShipmentCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address_from: dict[str, Any]
    address_to: dict[str, Any]
    parcels: list[Any]
    async_: bool = Field(default=False, alias="async")
    extra: dict[str, Any] | None = None
    customs_declaration: str | None = None
    metadata: str | None = None


class ShippoShipmentCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ShippoShipmentCreateBody


class ShippoShipmentGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shipmentId: str


class ShippoShipmentGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ShippoShipmentGetPath


class ShippoShipmentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    results: int = 25


class ShippoShipmentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ShippoShipmentsListQuery | None = None


class ShippoTrackingGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    carrier: str
    trackingNumber: str


class ShippoTrackingGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ShippoTrackingGetPath


class ShippoTransactionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    rate: str
    label_file_type: str | None = None
    async_: bool = Field(default=False, alias="async")
    metadata: str | None = None


class ShippoTransactionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ShippoTransactionCreateBody


class ShippoNamespace:
    """Typed accessor for the `shippo` app. Reach it via `client.apps.shippo`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def address_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShippoAddressCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new address entry in the Shippo address book with optional USPS validation. The address can be reused later when creating shipments, saving repeated data entry. Use this to pre-store commonly-used sender or recipient addresses and optionally verify that the address is deliverable.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shippo.address_create", owner_id, args, account_id=account_id
        )

    async def shipment_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShippoShipmentCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new shipment record by providing sender and recipient addresses alongside parcel dimensions and weight. Shippo responds with available shipping rates from multiple carriers, but no label is purchased at this stage. Use this as the first step before buying a label; it is a draft operation that does not incur charges.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shippo.shipment_create", owner_id, args, account_id=account_id
        )

    async def shipment_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShippoShipmentGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch detailed information about a single shipment using its unique Shippo object ID. The response includes full address details, parcel dimensions, the list of available carrier rates, and any attached metadata. Use this when you already have the shipment ID and need its complete data for display or label purchase.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shippo.shipment_get", owner_id, args, account_id=account_id
        )

    async def shipments_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShippoShipmentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of all shipments on the authenticated Shippo account. Each shipment entry includes sender and recipient addresses, parcel details, and available shipping rates. Use this to browse or search existing shipments when you need to locate a specific shipment by scanning pages of results.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shippo.shipments_list", owner_id, args, account_id=account_id
        )

    async def tracking_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShippoTrackingGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the real-time tracking status and full event history for a shipment using its carrier code and tracking number. The response includes location, timestamp, and status description for each tracking event along with the overall delivery status. Use this to monitor shipment progress or confirm delivery without modifying any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shippo.tracking_get", owner_id, args, account_id=account_id
        )

    async def transaction_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShippoTransactionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Purchase a shipping label by submitting a rate object ID obtained from a previous shipment creation. This generates a printable label and commits the account to the carrier's postage cost, which is non-refundable in most cases. Use this only when you are ready to pay for shipping; the purchase is irreversible.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shippo.transaction_create", owner_id, args, account_id=account_id
        )
