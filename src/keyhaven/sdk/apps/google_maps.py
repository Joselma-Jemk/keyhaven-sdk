# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `google_maps` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GoogleMapsGetDirectionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    origin: str
    destination: str
    mode: Literal["walking", "driving", "bicycling", "transit"] = "walking"


class GoogleMapsGetDirectionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleMapsGetDirectionsQuery


class GoogleMapsGetElevationQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    locations: str


class GoogleMapsGetElevationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleMapsGetElevationQuery


class GoogleMapsGetStaticMapQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    center: str
    zoom: int = 13
    size: str
    markers: str | None = None
    maptype: Literal["roadmap", "satellite", "hybrid", "terrain"] = "roadmap"
    scale: int = 1


class GoogleMapsGetStaticMapRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleMapsGetStaticMapQuery


class GoogleMapsReverseGeocodeQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    latlng: str
    language: str = "en"
    result_type: str | None = None


class GoogleMapsReverseGeocodeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleMapsReverseGeocodeQuery


class GoogleMapsTextSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    language: str = "en"
    location: str | None = None
    maxprice: int | None = None
    minprice: int | None = None
    opennow: bool | None = None
    pagetoken: str | None = None
    radius: int | None = None
    region: str | None = None
    type_: (
        Literal[
            "accounting",
            "airport",
            "amusement_park",
            "aquarium",
            "art_gallery",
            "atm",
            "bakery",
            "bank",
            "bar",
            "beauty_salon",
            "bicycle_store",
            "book_store",
            "bowling_alley",
            "bus_station",
            "cafe",
            "campground",
            "car_dealer",
            "car_rental",
            "car_repair",
            "car_wash",
            "casino",
            "cemetery",
            "church",
            "city_hall",
            "clothing_store",
            "convenience_store",
            "courthouse",
            "dentist",
            "department_store",
            "doctor",
            "drugstore",
            "electrician",
            "electronics_store",
            "embassy",
            "fire_station",
            "florist",
            "funeral_home",
            "furniture_store",
            "gas_station",
            "gym",
            "hair_care",
            "hardware_store",
            "hindu_temple",
            "home_goods_store",
            "hospital",
            "insurance_agency",
            "jewelry_store",
            "laundry",
            "lawyer",
            "library",
            "light_rail_station",
            "liquor_store",
            "local_government_office",
            "locksmith",
            "lodging",
            "meal_delivery",
            "meal_takeaway",
            "mosque",
            "movie_rental",
            "movie_theater",
            "moving_company",
            "museum",
            "night_club",
            "painter",
            "park",
            "parking",
            "pet_store",
            "pharmacy",
            "physiotherapist",
            "plumber",
            "police",
            "post_office",
            "primary_school",
            "real_estate_agency",
            "restaurant",
            "roofing_contractor",
            "rv_park",
            "school",
            "secondary_school",
            "shoe_store",
            "shopping_mall",
            "spa",
            "stadium",
            "storage",
            "store",
            "subway_station",
            "supermarket",
            "synagogue",
            "taxi_stand",
            "tourist_attraction",
            "train_station",
            "transit_station",
            "travel_agency",
            "university",
            "veterinary_care",
            "zoo",
        ]
        | None
    ) = Field(default=None, alias="type")


class GoogleMapsTextSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleMapsTextSearchQuery


class GoogleMapsNamespace:
    """Typed accessor for the `google_maps` app. Reach it via `client.apps.google_maps`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_directions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMapsGetDirectionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Computes travel directions between an origin and a destination using one of four modes: walking, driving, bicycling, or transit. Returns step-by-step navigation instructions, distance, duration, polyline path, and waypoints along the route. Use this when the agent needs to plan a route, calculate travel time, or generate navigation instructions between two locations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_maps.get_directions", owner_id, args, account_id=account_id
        )

    async def get_elevation(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMapsGetElevationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves elevation data in meters for one or more geographic coordinate locations. Returns the elevation value and data resolution for each requested point, enabling terrain profiling across multiple locations. Use this when the agent needs to determine terrain height, generate elevation profiles along a route, or assess topographic characteristics of a geographic area.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_maps.get_elevation", owner_id, args, account_id=account_id
        )

    async def get_static_map(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMapsGetStaticMapRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generates a non-interactive static map image centered at specified coordinates with configurable zoom, image dimensions, map type (roadmap, satellite, hybrid, terrain), and optional markers. Returns raw image bytes suitable for embedding in reports, emails, or documents. Use this when the agent needs to include a map visual in a static context without interactive controls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_maps.get_static_map", owner_id, args, account_id=account_id
        )

    async def reverse_geocode(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMapsReverseGeocodeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Converts latitude/longitude coordinates into a human-readable street address using Google's Geocoding API. Returns the full formatted address along with address components (street, city, postal code, country, administrative area). Use this when the agent needs to find the address for GPS coordinates, determine what is at a given location, or reverse-map a set of coordinates.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_maps.reverse_geocode", owner_id, args, account_id=account_id
        )

    async def text_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMapsTextSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for places using a free-form text query (e.g. 'pizza in Sydney' or 'coffee shops near Central Station'). Returns matching places with names, addresses, coordinates, ratings, price levels, opening hours, and types. Use this when the agent needs to find businesses, landmarks, or points of interest by a descriptive text query rather than by coordinates.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_maps.text_search", owner_id, args, account_id=account_id
        )
