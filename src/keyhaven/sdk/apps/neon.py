# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `neon` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class NeonCreateApiKeyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key_name: str


class NeonCreateApiKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: NeonCreateApiKeyBody


class NeonCreateProjectBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project: dict[str, Any]


class NeonCreateProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: NeonCreateProjectBody


class NeonDeleteProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class NeonDeleteProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NeonDeleteProjectPath


class NeonGetProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class NeonGetProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NeonGetProjectPath


class NeonListApiKeysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class NeonListOperationsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class NeonListOperationsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    limit: int | None = None


class NeonListOperationsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NeonListOperationsPath
    query: NeonListOperationsQuery | None = None


class NeonListProjectsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    limit: int = 10
    search: str | None = None
    org_id: str | None = None
    timeout: int | None = None


class NeonListProjectsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: NeonListProjectsQuery | None = None


class NeonListSharedProjectsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    limit: int = 10
    search: str | None = None
    timeout: int | None = None


class NeonListSharedProjectsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: NeonListSharedProjectsQuery | None = None


class NeonRetrieveOperationDetailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str
    operation_id: str


class NeonRetrieveOperationDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NeonRetrieveOperationDetailsPath


class NeonRevokeApiKeyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key_id: int


class NeonRevokeApiKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NeonRevokeApiKeyPath


class NeonNamespace:
    """Typed accessor for the `neon` app. Reach it via `client.apps.neon`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_api_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonCreateApiKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generates a new API key for programmatic access to the Neon API. The key is returned only once in the response and cannot be retrieved later. Use this to set up automated deployments or CI/CD pipelines that interact with Neon projects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.create_api_key", owner_id, args, account_id=account_id
        )

    async def create_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonCreateProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new Neon serverless Postgres project with configurable settings including region, Postgres version, compute autoscaling limits, and IP allow-lists. This creates a billable resource subject to your plan's project limits. Use this to spin up a new database environment for development, staging, or production.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.create_project", owner_id, args, account_id=account_id
        )

    async def delete_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonDeleteProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a Neon project and all of its resources: compute endpoints, branches, databases, roles, and data. This action is irreversible and data cannot be recovered. Use with extreme caution — consider taking a backup or snapshot first.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.delete_project", owner_id, args, account_id=account_id
        )

    async def get_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonGetProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns detailed information about a specific Neon project, including its compute endpoints, branches, region, Postgres version, and connection URIs. Use this to inspect project configuration or to gather connection details needed for database clients.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("neon.get_project", owner_id, args, account_id=account_id)

    async def list_api_keys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonListApiKeysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all API keys associated with the authenticated Neon account. Each key includes metadata such as name, creation date, and last used timestamp. Use this to audit existing credentials or to find a key_id for a subsequent revoke operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.list_api_keys", owner_id, args, account_id=account_id
        )

    async def list_operations(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonListOperationsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all operations for a given Neon project, ordered by recency. Operations older than 6 months may be automatically deleted. Use this to review the activity history or find an operation_id for the retrieve endpoint.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.list_operations", owner_id, args, account_id=account_id
        )

    async def list_projects(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonListProjectsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all Neon projects for the authenticated account with optional search and pagination. A project is the top-level container for branches, databases, and compute endpoints. Use this to discover existing projects or to find a project_id for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.list_projects", owner_id, args, account_id=account_id
        )

    async def list_shared_projects(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonListSharedProjectsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all projects that have been shared with the authenticated account by other Neon users. Includes pagination and search by name or ID. Use this to discover projects from team members or organisations that you have access to but did not create yourself.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.list_shared_projects", owner_id, args, account_id=account_id
        )

    async def retrieve_operation_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonRetrieveOperationDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the current status and metadata of a specific Neon operation (e.g., creating a branch, resetting a password). An operation represents an asynchronous action on a project. Use this to poll the outcome after triggering a long-running task.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.retrieve_operation_details", owner_id, args, account_id=account_id
        )

    async def revoke_api_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NeonRevokeApiKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes the specified API key, immediately invalidating it. Any service or script using this key will lose access to the Neon API. This action is irreversible — use with caution.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "neon.revoke_api_key", owner_id, args, account_id=account_id
        )
