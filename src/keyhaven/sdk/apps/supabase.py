# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `supabase` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SupabaseCancelProjectRestorationPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseCancelProjectRestorationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseCancelProjectRestorationPath


class SupabaseCreateEdgeFunctionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseCreateEdgeFunctionQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    import_map_path: str | None = None
    entrypoint_path: str | None = None
    import_map: bool | None = None
    verify_jwt: bool | None = None
    name: str | None = None
    slug: str | None = None


class SupabaseCreateEdgeFunctionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    slug: str
    name: str
    body: str
    verify_jwt: bool | None = None


class SupabaseCreateEdgeFunctionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseCreateEdgeFunctionPath
    query: SupabaseCreateEdgeFunctionQuery | None = None
    body: SupabaseCreateEdgeFunctionBody


class SupabaseCreateOrganizationBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class SupabaseCreateOrganizationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SupabaseCreateOrganizationBody


class SupabaseCreateProjectBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    db_pass: str
    name: str
    organization_id: str
    region: Literal[
        "us-east-1",
        "us-east-2",
        "us-west-1",
        "us-west-2",
        "ap-east-1",
        "ap-southeast-1",
        "ap-northeast-1",
        "ap-northeast-2",
        "ap-southeast-2",
        "eu-west-1",
        "eu-west-2",
        "eu-west-3",
        "eu-north-1",
        "eu-central-1",
        "eu-central-2",
        "ca-central-1",
        "ap-south-1",
        "sa-east-1",
    ]
    desired_instance_size: (
        Literal[
            "micro",
            "small",
            "medium",
            "large",
            "xlarge",
            "2xlarge",
            "4xlarge",
            "8xlarge",
            "12xlarge",
            "16xlarge",
        ]
        | None
    ) = None
    template_url: str | None = None


class SupabaseCreateProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SupabaseCreateProjectBody


class SupabaseCreateSsoProviderPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseCreateSsoProviderBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: Literal["saml"] = Field(alias="type")
    metadata_xml: str | None = None
    metadata_url: str | None = None
    domains: list[Any] | None = None
    attribute_mapping: dict[str, Any] | None = None


class SupabaseCreateSsoProviderRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseCreateSsoProviderPath
    body: SupabaseCreateSsoProviderBody


class SupabaseDeleteEdgeFunctionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str
    function_slug: str


class SupabaseDeleteEdgeFunctionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseDeleteEdgeFunctionPath


class SupabaseDeleteProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseDeleteProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseDeleteProjectPath


class SupabaseDisableReadonlyModePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseDisableReadonlyModeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseDisableReadonlyModePath


class SupabaseGenerateTypescriptTypesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseGenerateTypescriptTypesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    included_schemas: str | None = None


class SupabaseGenerateTypescriptTypesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGenerateTypescriptTypesPath
    query: SupabaseGenerateTypescriptTypesQuery | None = None


class SupabaseGetAuthConfigPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseGetAuthConfigRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetAuthConfigPath


class SupabaseGetEdgeFunctionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str
    function_slug: str


class SupabaseGetEdgeFunctionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetEdgeFunctionPath


class SupabaseGetEdgeFunctionBodyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str
    function_slug: str


class SupabaseGetEdgeFunctionBodyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetEdgeFunctionBodyPath


class SupabaseGetOrganizationPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    slug: str


class SupabaseGetOrganizationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetOrganizationPath


class SupabaseGetProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseGetProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetProjectPath


class SupabaseGetProjectApiKeysPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseGetProjectApiKeysQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reveal: bool | None = None


class SupabaseGetProjectApiKeysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetProjectApiKeysPath
    query: SupabaseGetProjectApiKeysQuery | None = None


class SupabaseGetProjectHealthStatusPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseGetProjectHealthStatusQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    services: str
    timeout_ms: int | None = None


class SupabaseGetProjectHealthStatusRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetProjectHealthStatusPath
    query: SupabaseGetProjectHealthStatusQuery


class SupabaseGetSsoProviderPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str
    provider_id: str


class SupabaseGetSsoProviderRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseGetSsoProviderPath


class SupabaseListAllEdgeFunctionsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseListAllEdgeFunctionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseListAllEdgeFunctionsPath


class SupabaseListOrganizationMembersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    slug: str


class SupabaseListOrganizationMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseListOrganizationMembersPath


class SupabaseListOrganizationsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SupabaseListProjectsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SupabaseListSsoProvidersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseListSsoProvidersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseListSsoProvidersPath


class SupabasePauseProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabasePauseProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabasePauseProjectPath


class SupabaseRemoveSsoProviderPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str
    provider_id: str


class SupabaseRemoveSsoProviderRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseRemoveSsoProviderPath


class SupabaseRestoreProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseRestoreProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseRestoreProjectPath


class SupabaseRunSqlQueryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str


class SupabaseRunSqlQueryBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str


class SupabaseRunSqlQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseRunSqlQueryPath
    body: SupabaseRunSqlQueryBody


class SupabaseUpdateEdgeFunctionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str
    function_slug: str


class SupabaseUpdateEdgeFunctionQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    import_map_path: str | None = None
    entrypoint_path: str | None = None
    import_map: bool | None = None
    verify_jwt: bool | None = None
    name: str | None = None


class SupabaseUpdateEdgeFunctionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    body: str | None = None
    verify_jwt: bool | None = None


class SupabaseUpdateEdgeFunctionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SupabaseUpdateEdgeFunctionPath
    query: SupabaseUpdateEdgeFunctionQuery | None = None
    body: SupabaseUpdateEdgeFunctionBody | None = None


class SupabaseNamespace:
    """Typed accessor for the `supabase` app. Reach it via `client.apps.supabase`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def cancel_project_restoration(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseCancelProjectRestorationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Cancels an in-progress project restoration that was previously triggered, stopping the recovery process before it completes. This does not affect the deleted project itself — it only aborts the current restoration attempt. Use this if you initiated a restore by mistake or if the restore is taking too long and you need to retry.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.cancel_project_restoration", owner_id, args, account_id=account_id
        )

    async def create_edge_function(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseCreateEdgeFunctionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Edge Function for a Supabase project by providing a name, slug, and code body (JavaScript/TypeScript). This deploys serverless code that responds to HTTP requests at the function's URL; once created, it is immediately callable if not protected by JWT verification. Use this to add new serverless endpoints to your project; the function can be updated or deleted later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.create_edge_function", owner_id, args, account_id=account_id
        )

    async def create_organization(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseCreateOrganizationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Supabase organization under the authenticated user's account, which becomes a billing entity that can own projects. This has real billing implications — the organization may be tied to a payment plan and incur costs. Use this when setting up a new team workspace; the action is reversible only by deleting the org through the dashboard.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.create_organization", owner_id, args, account_id=account_id
        )

    async def create_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseCreateProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new Supabase project under the specified organization, choosing region, database password, and optional instance size. This spins up real cloud infrastructure and incurs billing costs from the moment of creation. Use this only when you are ready to commit to a new database environment — the resulting project can be paused but deletion requires a separate destructive action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.create_project", owner_id, args, account_id=account_id
        )

    async def create_sso_provider(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseCreateSsoProviderRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new SAML SSO provider for a Supabase project, enabling users to authenticate via an external identity provider (IdP) using SAML 2.0. Once created, the provider is available for login flows — users signing in with the configured domains will be redirected to the IdP. Use this when you need to add SAML-based single sign-on to a project; it is a one-time setup action that affects authentication for all users.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.create_sso_provider", owner_id, args, account_id=account_id
        )

    async def delete_edge_function(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseDeleteEdgeFunctionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes an Edge Function from a Supabase project by its slug, removing its code and making its endpoint immediately unreachable. This is irreversible — the function must be recreated from scratch. Use this only when the function is confirmed obsolete and no callers depend on it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.delete_edge_function", owner_id, args, account_id=account_id
        )

    async def delete_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseDeleteProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a Supabase project and all of its data, including the database, storage, and authentication records. This is irreversible and causes complete data loss — there is no undo endpoint. Use this only when you are certain the project and all its contents are no longer needed and have been backed up externally.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.delete_project", owner_id, args, account_id=account_id
        )

    async def disable_readonly_mode(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseDisableReadonlyModeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Temporarily disables readonly mode on a Supabase project for 15 minutes, allowing write operations when the project would otherwise reject them. The auto-reversion means this is safe for short maintenance windows but could expose the project to unintended writes if triggered carelessly. Use this when you need a brief write window to apply a migration or fix data — and only after understanding why the project is in readonly mode.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.disable_readonly_mode", owner_id, args, account_id=account_id
        )

    async def generate_typescript_types(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGenerateTypescriptTypesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generates TypeScript type definitions from your project's database schema, intended for use with the supabase-js client library. Optionally filter by included schemas via query parameter. This is a read-only code-generation endpoint — no project data is modified. Use this when setting up a new project or after schema changes to keep your frontend types in sync with the database.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.generate_typescript_types", owner_id, args, account_id=account_id
        )

    async def get_auth_config(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetAuthConfigRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full authentication configuration for a Supabase project, including settings for providers, sessions, and security rules. This is a read-only snapshot of the project's auth settings — no state is changed. Use this when you need to audit current auth settings or verify configuration before making changes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_auth_config", owner_id, args, account_id=account_id
        )

    async def get_edge_function(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetEdgeFunctionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves metadata about a specific Edge Function by its slug, including its name, status, version, and deployment timestamps. This is read-only — no state is changed. Use this to inspect a function's configuration or verify its deployment status before performing updates or checking its body.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_edge_function", owner_id, args, account_id=account_id
        )

    async def get_edge_function_body(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetEdgeFunctionBodyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the source code body of a deployed Edge Function by its slug, returning the raw JavaScript/TypeScript code as a string. This is read-only — no state is changed. Use this when you need to inspect or back up a function's current code before making updates, or to debug what is currently deployed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_edge_function_body", owner_id, args, account_id=account_id
        )

    async def get_organization(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetOrganizationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific Supabase organization by its slug, including name, billing plan, and subscription status. This is a read-only operation — no state is changed. Use this when you need to inspect an organization's settings before creating projects under it or checking plan limits.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_organization", owner_id, args, account_id=account_id
        )

    async def get_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific Supabase project by its ref, including its name, region, creation date, status, and organization. This is a read-only operation — no state is changed. Use this to inspect a project's configuration or verify its current state before performing management actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_project", owner_id, args, account_id=account_id
        )

    async def get_project_api_keys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetProjectApiKeysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the API keys (anon and service_role) for a Supabase project. When the `reveal` query parameter is true, the actual key values are returned in plain text — these are sensitive credentials that grant access to your project's data and services. This is a read-only GET but revealing keys introduces security exposure. Use this when you need to configure a client SDK or debug authentication issues.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_project_api_keys", owner_id, args, account_id=account_id
        )

    async def get_project_health_status(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetProjectHealthStatusRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Checks the health status of a Supabase project's individual services (e.g. auth, database, pooler, realtime, REST, storage), returning whether each is healthy or degraded. The timeout parameter controls how long to wait per service. This is read-only — no state is changed. Use this when diagnosing connectivity issues or verifying that a project is fully operational after a deployment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_project_health_status", owner_id, args, account_id=account_id
        )

    async def get_sso_provider(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseGetSsoProviderRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full configuration details of a specific SAML SSO provider by its UUID, including its metadata, domain bindings, and attribute mappings. It does not modify any state — purely informative. Use this when you need to inspect a provider's settings or verify that a provider was created correctly.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.get_sso_provider", owner_id, args, account_id=account_id
        )

    async def list_all_edge_functions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseListAllEdgeFunctionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Edge Functions deployed to a Supabase project, returning their slugs, names, statuses, and versions. This is read-only — no state is changed. Use this to discover which functions are deployed before targeting one for inspection, update, or deletion.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.list_all_edge_functions", owner_id, args, account_id=account_id
        )

    async def list_organization_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseListOrganizationMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all members of a Supabase organization identified by its slug, returning their user IDs, roles, and join dates. This is a read-only operation — no state is changed. Use this when you need to audit who has access to an organization's projects or verify team membership.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.list_organization_members", owner_id, args, account_id=account_id
        )

    async def list_organizations(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseListOrganizationsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Supabase organizations that the authenticated API user belongs to, returning each organization's ID, name, and slug. This is a read-only operation — no state is changed. Use this as a discovery step to find organization IDs or slugs needed for project creation or membership lookups.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.list_organizations", owner_id, args, account_id=account_id
        )

    async def list_projects(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseListProjectsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Supabase projects accessible by the authenticated user across all organizations, returning their refs, names, regions, and statuses. This is a read-only operation — no state is changed. Use this as a first step to discover project refs needed for project-specific operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.list_projects", owner_id, args, account_id=account_id
        )

    async def list_sso_providers(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseListSsoProvidersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all SAML SSO providers currently configured for a Supabase project, returning their UUIDs, types, and basic metadata. This is a read-only enumeration — no project state is changed. Use this when you need to discover which SSO providers exist, for example before targeting one for inspection or removal.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.list_sso_providers", owner_id, args, account_id=account_id
        )

    async def pause_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabasePauseProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Pauses a Supabase project, making its services (database, API, storage, auth) unavailable until the project is resumed via the dashboard or unpause API. Compute billing stops while paused, but storage costs continue. Use this to temporarily suspend a non-critical project to reduce costs; the action is reversible by unpausing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.pause_project", owner_id, args, account_id=account_id
        )

    async def remove_sso_provider(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseRemoveSsoProviderRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a SAML SSO provider from a Supabase project by its UUID, removing that authentication path for all users who relied on it. Users who previously authenticated through this provider will no longer be able to sign in, and the configuration is gone without a recovery endpoint. Use this only when you are certain the provider is no longer needed and there are no active sessions depending on it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.remove_sso_provider", owner_id, args, account_id=account_id
        )

    async def restore_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseRestoreProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Initiates restoration of a previously deleted Supabase project from backup, recovering its database, storage, and configuration. This is a potentially long-running operation that can be monitored and cancelled. Use this to recover a project that was deleted within the backup retention window; note that restored projects resume billing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.restore_project", owner_id, args, account_id=account_id
        )

    async def run_sql_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseRunSqlQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        [Beta] Executes an arbitrary SQL query directly against a Supabase project's database, including SELECT, INSERT, UPDATE, DELETE, DDL, and other statements. This can read, modify, or destroy data, create or drop tables, and alter schemas — its effect is exactly what the query text dictates. Use this with extreme caution; any SQL error or destructive statement executes immediately and is not transactional by default.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.run_sql_query", owner_id, args, account_id=account_id
        )

    async def update_edge_function(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SupabaseUpdateEdgeFunctionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing Edge Function's code, name, or JWT verification setting without changing its slug or URL. This deploys new code to the same endpoint, so any changes take effect immediately for all callers. Use this to patch bugs or update logic in a deployed function; the update can be overwritten by another update but the previous version is not preserved.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "supabase.update_edge_function", owner_id, args, account_id=account_id
        )
