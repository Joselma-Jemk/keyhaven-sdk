# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `highlevel` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class HighlevelContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    locationId: str
    firstName: str | None = None
    lastName: str | None = None
    email: str | None = None
    phone: str | None = None
    tags: list[Any] | None = None
    customField: list[Any] | None = None


class HighlevelContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: HighlevelContactCreateBody


class HighlevelContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contactId: str


class HighlevelContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HighlevelContactGetPath


class HighlevelContactUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contactId: str


class HighlevelContactUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    firstName: str | None = None
    lastName: str | None = None
    email: str | None = None
    phone: str | None = None
    tags: list[Any] | None = None
    customField: list[Any] | None = None


class HighlevelContactUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HighlevelContactUpdatePath
    body: HighlevelContactUpdateBody | None = None


class HighlevelContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    locationId: str
    query: str | None = None
    limit: int | None = None
    offset: int | None = None
    order: str | None = None
    sortBy: str | None = None


class HighlevelContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HighlevelContactsListQuery


class HighlevelContactsSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    locationId: str
    filters: list[Any] | None = None
    limit: int | None = None
    offset: int | None = None
    sortBy: str | None = None
    sortOrder: str | None = None


class HighlevelContactsSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: HighlevelContactsSearchBody


class HighlevelOpportunitiesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    locationId: str
    pipelineId: str | None = None
    pipelineStageId: str | None = None
    contactId: str | None = None
    status: str | None = None
    limit: int | None = None
    offset: int | None = None


class HighlevelOpportunitiesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HighlevelOpportunitiesListQuery


class HighlevelNamespace:
    """Typed accessor for the `highlevel` app. Reach it via `client.apps.highlevel`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HighlevelContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new contact record to a specified High-Level location with optional custom fields and tags. The contact becomes immediately visible in the CRM and can be used in workflows, campaigns, and pipeline stages. Use this when you need to import or manually register a new lead or customer.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "highlevel.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HighlevelContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full profile of a single contact by its unique identifier. Returns all standard and custom fields, tags, and associated metadata. Use this when you need to inspect a specific contact's complete record before taking further action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "highlevel.contact_get", owner_id, args, account_id=account_id
        )

    async def contact_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HighlevelContactUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing contact's properties including name, email, phone, tags, and custom field values. Changes are reflected instantly across the CRM, campaigns, and linked records. Use this when a contact's information needs to be corrected or enriched.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "highlevel.contact_update", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HighlevelContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of contacts from a specific High-Level location. The response includes basic contact fields and can be refined using query parameters for name, email, or phone search, sorting, and pagination. Use this function when you need to browse or export contacts within a location without modifying any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "highlevel.contacts_list", owner_id, args, account_id=account_id
        )

    async def contacts_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HighlevelContactsSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Performs an advanced search across contacts using structured filter groups, supporting complex logical conditions (AND within groups, OR between groups). Unlike the simple list endpoint, this enables precise queries like "contacts tagged X AND created after date Y". Use this when you need to find contacts matching multi-condition criteria.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "highlevel.contacts_search", owner_id, args, account_id=account_id
        )

    async def opportunities_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HighlevelOpportunitiesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of opportunities (deals) with optional filtering by pipeline, stage, associated contact, or status (open/won/lost). Each opportunity record includes its monetary value, pipeline stage, and assigned user. Use this to analyze sales pipeline health or find deals matching specific criteria.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "highlevel.opportunities_list", owner_id, args, account_id=account_id
        )
