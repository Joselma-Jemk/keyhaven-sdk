# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `tines` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TinesCreateTeamHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TinesCreateTeamBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class TinesCreateTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: TinesCreateTeamHeader = Field(default_factory=TinesCreateTeamHeader)
    body: TinesCreateTeamBody


class TinesGetTeamPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class TinesGetTeamHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TinesGetTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TinesGetTeamPath
    header: TinesGetTeamHeader = Field(default_factory=TinesGetTeamHeader)


class TinesListTeamMembersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class TinesListTeamMembersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    per_page: int | None = None
    page: int | None = None


class TinesListTeamMembersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TinesListTeamMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TinesListTeamMembersPath
    query: TinesListTeamMembersQuery | None = None
    header: TinesListTeamMembersHeader = Field(default_factory=TinesListTeamMembersHeader)


class TinesListTeamsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_personal_teams: bool = False
    per_page: int | None = None
    page: int | None = None


class TinesListTeamsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TinesListTeamsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TinesListTeamsQuery | None = None
    header: TinesListTeamsHeader = Field(default_factory=TinesListTeamsHeader)


class TinesUpdateTeamPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class TinesUpdateTeamHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TinesUpdateTeamBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class TinesUpdateTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TinesUpdateTeamPath
    header: TinesUpdateTeamHeader = Field(default_factory=TinesUpdateTeamHeader)
    body: TinesUpdateTeamBody


class TinesNamespace:
    """Typed accessor for the `tines` app. Reach it via `client.apps.tines`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TinesCreateTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new team within the Tines tenant. Teams are organizational units used to group users, stories, and resources. The new team is created with the specified name and is initially empty. Use when you need to provision a new team for project isolation or organizational structure. This is an internal administrative action that can be reversed by deleting the team.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "tines.create_team", owner_id, args, account_id=account_id
        )

    async def get_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TinesGetTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch the details of a specific team in the Tines tenant by its ID. Returns the team name, membership count, and other metadata. Use when you need to verify a team's existence or retrieve its properties before performing team-scoped operations like listing members or updating settings. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("tines.get_team", owner_id, args, account_id=account_id)

    async def list_team_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TinesListTeamMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of users who are members of a specific team in the Tines tenant. Returns user identifiers and metadata. Use to audit team membership, verify user access, or find members before performing actions like assigning resources to the team. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "tines.list_team_members", owner_id, args, account_id=account_id
        )

    async def list_teams(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TinesListTeamsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of all teams in the Tines tenant, with the option to include personal teams. Returns team names and IDs that can be used in subsequent operations. Use to discover available teams before creating resources, managing members, or inspecting team structure. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("tines.list_teams", owner_id, args, account_id=account_id)

    async def update_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TinesUpdateTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Update the name of an existing team in the Tines tenant. This is an administrative action that changes how the team appears throughout the Tines UI and API. Use when you need to rename a team following organizational changes. The team's resources, stories, and memberships are preserved. Reversible by updating the name again.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "tines.update_team", owner_id, args, account_id=account_id
        )
