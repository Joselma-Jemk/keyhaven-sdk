# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `fireflies` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FirefliesGetAiAppsOutputsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirefliesGetAiAppsOutputsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str = "query GetAIAppsOutputs($appId: String, $transcriptId: String, $skip: Float, $limit: Float) { apps(app_id: $appId, transcript_id: $transcriptId, skip: $skip, limit: $limit) { outputs { transcript_id user_id app_id created_at title prompt response } } }"
    variables: dict[str, Any] | None = None


class FirefliesGetAiAppsOutputsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirefliesGetAiAppsOutputsHeader = Field(default_factory=FirefliesGetAiAppsOutputsHeader)
    body: FirefliesGetAiAppsOutputsBody = Field(default_factory=FirefliesGetAiAppsOutputsBody)


class FirefliesGetTranscriptHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirefliesGetTranscriptBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str = "query Transcript($transcriptId: String!) { transcript(id: $transcriptId) { id title date duration sentences { speaker_id text start_time end_time } summary { keywords action_items outline } } }"
    variables: dict[str, Any]


class FirefliesGetTranscriptRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirefliesGetTranscriptHeader = Field(default_factory=FirefliesGetTranscriptHeader)
    body: FirefliesGetTranscriptBody


class FirefliesGetUserHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirefliesGetUserBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str = "query GetUser($userId: String) { user(id: $userId) { user_id name email integrations minutes_consumed is_admin recent_transcript recent_meeting num_transcripts } }"
    variables: dict[str, Any] | None = None


class FirefliesGetUserRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirefliesGetUserHeader = Field(default_factory=FirefliesGetUserHeader)
    body: FirefliesGetUserBody = Field(default_factory=FirefliesGetUserBody)


class FirefliesListTranscriptsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirefliesListTranscriptsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str = "query Transcripts($title: String, $fromDate: DateTime, $toDate: DateTime, $limit: Int, $skip: Int, $host_email: String, $organizer_email: String, $participant_email: String, $user_id: String, $mine: Boolean) { transcripts(title: $title, fromDate: $fromDate, toDate: $toDate, limit: $limit, skip: $skip, host_email: $host_email, organizer_email: $organizer_email, participant_email: $participant_email, user_id: $user_id, mine: $mine) { id title date duration } }"
    variables: dict[str, Any] | None = None


class FirefliesListTranscriptsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirefliesListTranscriptsHeader = Field(default_factory=FirefliesListTranscriptsHeader)
    body: FirefliesListTranscriptsBody = Field(default_factory=FirefliesListTranscriptsBody)


class FirefliesListUsersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirefliesListUsersBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str = "{ users { user_id name email integrations } }"


class FirefliesListUsersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirefliesListUsersHeader = Field(default_factory=FirefliesListUsersHeader)
    body: FirefliesListUsersBody = Field(default_factory=FirefliesListUsersBody)


class FirefliesSetUserRoleHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirefliesSetUserRoleBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str = "mutation SetUserRole($user_id: String!, $role: Role!) { setUserRole(user_id: $user_id, role: $role) { name is_admin } }"
    variables: dict[str, Any]


class FirefliesSetUserRoleRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirefliesSetUserRoleHeader = Field(default_factory=FirefliesSetUserRoleHeader)
    body: FirefliesSetUserRoleBody


class FirefliesNamespace:
    """Typed accessor for the `fireflies` app. Reach it via `client.apps.fireflies`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_ai_apps_outputs(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirefliesGetAiAppsOutputsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Queries Fireflies AI App outputs generated for meetings, filterable by app ID and transcript ID with pagination. Each output contains the app's prompt, the AI-generated response, and metadata such as creation timestamp and user. Use it when an agent needs to retrieve results from AI Apps like summaries, action items, or custom integrations that ran on meeting transcripts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fireflies.get_ai_apps_outputs", owner_id, args, account_id=account_id
        )

    async def get_transcript(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirefliesGetTranscriptRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full transcript of a specific Fireflies meeting by transcript ID, including speaker-labeled sentences with timestamps, key topics, action items, and an outline. This returns the complete conversation record as captured by the Fireflies assistant. Use it when an agent needs to analyze meeting content, extract decisions, or review what was said.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fireflies.get_transcript", owner_id, args, account_id=account_id
        )

    async def get_user(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirefliesGetUserRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves profile information for a specific Fireflies user by their user ID, or returns the API key owner's details if no ID is provided. Includes name, email, linked integrations, minutes consumed, admin status, and recent transcript activity. Use it when an agent needs to verify a user's identity or check their usage and permissions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fireflies.get_user", owner_id, args, account_id=account_id
        )

    async def list_transcripts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirefliesListTranscriptsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of meeting transcripts with filtering by title, date range, host/organizer/participant email, or user ID. Each result includes the meeting ID, title, date, and duration. Use it when an agent needs to browse available recordings before fetching a specific transcript.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fireflies.list_transcripts", owner_id, args, account_id=account_id
        )

    async def list_users(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirefliesListUsersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a list of all users within the Fireflies team or organization, including each user's name, email, and linked integrations. This provides a directory of who has access to the team's meeting recordings and transcripts. Use it when an agent needs to enumerate team members or verify user accounts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fireflies.list_users", owner_id, args, account_id=account_id
        )

    async def set_user_role(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirefliesSetUserRoleRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates a Fireflies user's role within the organization to either admin or user, granting or revoking administrative privileges. Admins can manage team settings, billing, and user access while regular users can only view their own content. Use it when an agent needs to change permissions, but confirm before granting admin access.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fireflies.set_user_role", owner_id, args, account_id=account_id
        )
