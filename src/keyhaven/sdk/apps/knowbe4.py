# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `knowbe4` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class Knowbe4GroupsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    status: str | None = None


class Knowbe4GroupsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: Knowbe4GroupsListQuery | None = None


class Knowbe4PhishingCampaignsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None


class Knowbe4PhishingCampaignsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: Knowbe4PhishingCampaignsListQuery | None = None


class Knowbe4TrainingCampaignsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None


class Knowbe4TrainingCampaignsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: Knowbe4TrainingCampaignsListQuery | None = None


class Knowbe4UserGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: int


class Knowbe4UserGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: Knowbe4UserGetPath


class Knowbe4UsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    status: str | None = None


class Knowbe4UsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: Knowbe4UsersListQuery | None = None


class Knowbe4Namespace:
    """Typed accessor for the `knowbe4` app. Reach it via `client.apps.knowbe4`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def groups_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Knowbe4GroupsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all user groups defined in your KnowBe4 account, with optional filtering by status. Groups are used to organize users for targeted training campaigns and phishing simulations. This operation is read-only and does not modify any data. Use this to discover existing group structures before assigning training or running reports.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "knowbe4.groups_list", owner_id, args, account_id=account_id
        )

    async def phishing_campaigns_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Knowbe4PhishingCampaignsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all simulated phishing campaigns configured in your KnowBe4 account, including their status, template, and targeting configuration. Phishing campaigns test employee susceptibility to social engineering attacks. This operation is read-only and does not modify any data. Use this to review active and historical phishing simulations before analyzing results.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "knowbe4.phishing_campaigns_list", owner_id, args, account_id=account_id
        )

    async def training_campaigns_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Knowbe4TrainingCampaignsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all security training campaigns in your KnowBe4 account, including their status, assigned content, and target audience. Training campaigns are scheduled assignments of security awareness courses to specific user groups. This operation is read-only and does not modify any data. Use this to review active and past training initiatives and assess organizational coverage.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "knowbe4.training_campaigns_list", owner_id, args, account_id=account_id
        )

    async def user_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Knowbe4UserGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the complete profile of a single KnowBe4 user by their numeric user ID, including their phishing and training history, risk score, and enrollment status. This provides the most detailed view of one user's security awareness posture. This operation is read-only and does not modify any data. Use this to investigate a specific employee's training progress or phishing test results.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("knowbe4.user_get", owner_id, args, account_id=account_id)

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Knowbe4UsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all users in your KnowBe4 account, with optional filtering by status (active, archived) and configurable page size. Each user record includes name, email, role, and security training status. This operation is read-only and does not modify any data. Use this to enumerate the workforce for security awareness reporting or to find a specific user ID for subsequent lookups.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "knowbe4.users_list", owner_id, args, account_id=account_id
        )
