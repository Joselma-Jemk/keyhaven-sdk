# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `jotform` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class JotformFormGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    form_id: str


class JotformFormGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: JotformFormGetPath


class JotformFormSubmissionsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    form_id: str


class JotformFormSubmissionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    filter: str | None = None
    orderby: str | None = None


class JotformFormSubmissionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: JotformFormSubmissionsListPath
    query: JotformFormSubmissionsListQuery | None = None


class JotformFormsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    filter: str | None = None
    orderby: str | None = None


class JotformFormsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: JotformFormsListQuery | None = None


class JotformSubmissionCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    form_id: str


class JotformSubmissionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    submission_1_: str | None = Field(default=None, alias="submission[1]")
    submission_2_: str | None = Field(default=None, alias="submission[2]")
    submission_3_first_: str | None = Field(default=None, alias="submission[3_first]")
    submission_3_last_: str | None = Field(default=None, alias="submission[3_last]")


class JotformSubmissionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: JotformSubmissionCreatePath
    body: JotformSubmissionCreateBody | None = None


class JotformUserGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class JotformUserSubmissionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    filter: str | None = None
    orderby: str | None = None


class JotformUserSubmissionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: JotformUserSubmissionsListQuery | None = None


class JotformNamespace:
    """Typed accessor for the `jotform` app. Reach it via `client.apps.jotform`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def form_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JotformFormGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full structure and settings of a single Jotform form by its form ID, including all question fields, their types, options, validation rules, and form-level configuration (emails, webhooks, thank-you pages). Use this to inspect a form's questions before reading submissions or to understand what data a form collects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("jotform.form_get", owner_id, args, account_id=account_id)

    async def form_submissions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JotformFormSubmissionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all submissions for a specific form with optional filtering (e.g., by status), ordering, and pagination. Each submission contains the answer values for every form question at the time of submission. Use this to export form responses, audit submitted data, or find specific submissions matching filter criteria.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "jotform.form_submissions_list", owner_id, args, account_id=account_id
        )

    async def forms_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JotformFormsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all forms owned by the authenticated user, with optional pagination, filtering, and sorting. Each form entry includes its ID, title, creation date, submission count, and current status. Use this to discover available forms, find a form by title pattern, or monitor form activity without making any changes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "jotform.forms_list", owner_id, args, account_id=account_id
        )

    async def submission_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JotformSubmissionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submits answer data to a live Jotform form by form ID, populating the specified question fields with provided values. This creates a new submission record identical to what a user would produce through the web form, potentially triggering emails, webhooks, payment processing, and other form automation. Use this to programmatically submit data on behalf of a user or system, but be aware that the submission may have real downstream effects depending on form configuration.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "jotform.submission_create", owner_id, args, account_id=account_id
        )

    async def user_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JotformUserGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the authenticated Jotform user's account details including usage limits, storage usage, plan type, and account creation date. This is a metadata endpoint that does not expose sensitive credentials. Use this to verify account identity, check remaining API rate limits, or confirm access before performing form operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("jotform.user_get", owner_id, args, account_id=account_id)

    async def user_submissions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: JotformUserSubmissionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all submissions across every form owned by the authenticated user, with optional filters, ordering, and pagination. Unlike the form-specific endpoint, this aggregates data from all forms into a single result set. Use this for cross-form reporting, compliance audit trails, or when you need to search submissions without knowing which form they belong to.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "jotform.user_submissions_list", owner_id, args, account_id=account_id
        )
