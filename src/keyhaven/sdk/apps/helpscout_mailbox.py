# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `helpscout_mailbox` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class HelpscoutMailboxConversationCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    subject: str
    mailboxId: int
    folderId: int | None = None
    type_: str = Field(alias="type")
    status: str | None = None
    customer: dict[str, Any]
    threads: list[Any]
    tags: list[Any] | None = None
    assignTo: int | None = None


class HelpscoutMailboxConversationCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: HelpscoutMailboxConversationCreateBody


class HelpscoutMailboxConversationGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    conversation_id: str


class HelpscoutMailboxConversationGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    embed: str | None = None


class HelpscoutMailboxConversationGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HelpscoutMailboxConversationGetPath
    query: HelpscoutMailboxConversationGetQuery | None = None


class HelpscoutMailboxConversationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str | None = None
    mailbox: str | None = None
    folder: str | None = None
    tag: str | None = None
    assigned_to: str | None = None
    modifiedSince: str | None = None
    number: str | None = None
    sortField: str | None = None
    sortOrder: str | None = None
    query: str | None = None
    page: int | None = None


class HelpscoutMailboxConversationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HelpscoutMailboxConversationsListQuery | None = None


class HelpscoutMailboxCustomerGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer_id: str


class HelpscoutMailboxCustomerGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HelpscoutMailboxCustomerGetPath


class HelpscoutMailboxCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    mailbox: str | None = None
    sortField: str | None = None
    sortOrder: str | None = None
    page: int | None = None


class HelpscoutMailboxCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HelpscoutMailboxCustomersListQuery | None = None


class HelpscoutMailboxMailboxesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None


class HelpscoutMailboxMailboxesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HelpscoutMailboxMailboxesListQuery | None = None


class HelpscoutMailboxNamespace:
    """Typed accessor for the `helpscout_mailbox` app. Reach it via `client.apps.helpscout_mailbox`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def conversation_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HelpscoutMailboxConversationCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new support conversation in a specified mailbox and folder, with customer details, subject, initial message threads, tags, and assignment. This sends a customer-facing communication and creates a trackable ticket. Use this to log incoming support requests programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "helpscout_mailbox.conversation_create", owner_id, args, account_id=account_id
        )

    async def conversation_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HelpscoutMailboxConversationGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single support conversation with full details including subject, status, assigned user, and optionally embedded thread messages. This reveals the complete customer interaction history. Use this to review a specific conversation before replying or changing its status.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "helpscout_mailbox.conversation_get", owner_id, args, account_id=account_id
        )

    async def conversations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HelpscoutMailboxConversationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches and filters support conversations across mailboxes by status, tags, assignee, date range, or custom query. Returns conversation summaries with subject, customer, status, and mailbox info. Use this to triage support requests or find specific customer interactions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "helpscout_mailbox.conversations_list", owner_id, args, account_id=account_id
        )

    async def customer_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HelpscoutMailboxCustomerGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single customer profile including name, email, phone, and associated conversations. This accesses personally identifiable contact data. Use this to review customer details before engaging with them in a support context.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "helpscout_mailbox.customer_get", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HelpscoutMailboxCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists customer profiles across the Help Scout account, with optional mailbox filtering and sorting. Returns names, email addresses, and contact information. Use this to find existing customers before creating a conversation or viewing their history.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "helpscout_mailbox.customers_list", owner_id, args, account_id=account_id
        )

    async def mailboxes_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HelpscoutMailboxMailboxesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all mailboxes configured in the Help Scout account, showing their names, email addresses, and settings. Mailboxes represent different support channels. Use this to discover available mailboxes before creating conversations or filtering by mailbox.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "helpscout_mailbox.mailboxes_list", owner_id, args, account_id=account_id
        )
