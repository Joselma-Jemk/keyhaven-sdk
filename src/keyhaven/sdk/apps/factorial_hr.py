# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `factorial_hr` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FactorialHrCreatesAWebhookSubscriptionHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FactorialHrCreatesAWebhookSubscriptionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    subscription_type: str
    target_url: str
    name: str | None = None
    company_id: int
    challenge: str | None = None
    enabled: bool | None = None
    api_version: str | None = None


class FactorialHrCreatesAWebhookSubscriptionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FactorialHrCreatesAWebhookSubscriptionHeader = Field(
        default_factory=FactorialHrCreatesAWebhookSubscriptionHeader
    )
    body: FactorialHrCreatesAWebhookSubscriptionBody


class FactorialHrReadsAllCredentialsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class FactorialHrReadsAllCredentialsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FactorialHrReadsAllCredentialsQuery | None = None


class FactorialHrReadsAllWebhookSubscriptionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int | None = None
    company_id: int | None = None
    type_: str | None = Field(default=None, alias="type")
    enabled: bool | None = None


class FactorialHrReadsAllWebhookSubscriptionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FactorialHrReadsAllWebhookSubscriptionsQuery | None = None


class FactorialHrNamespace:
    """Typed accessor for the `factorial_hr` app. Reach it via `client.apps.factorial_hr`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def creates_a_webhook_subscription(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialHrCreatesAWebhookSubscriptionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new webhook subscription that sends FactorialHR events (such as candidate creation or employee updates) to a specified external URL in real time. Requires a subscription type, target URL, and company ID; optional fields include name, challenge verification string, and API version. Once created, the webhook begins delivering events to the target URL immediately. Use when you need to set up a new data integration or real-time notification feed from FactorialHR.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial_hr.creates_a_webhook_subscription", owner_id, args, account_id=account_id
        )

    async def reads_all_credentials(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialHrReadsAllCredentialsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all API credentials configured for the FactorialHR account. Each credential entry includes metadata such as name, status, and scope of an API key used for third-party authentication. This endpoint only returns metadata, not the secret values themselves. Use when you need to list or audit existing API credentials.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial_hr.reads_all_credentials", owner_id, args, account_id=account_id
        )

    async def reads_all_webhook_subscriptions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FactorialHrReadsAllWebhookSubscriptionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all webhook subscriptions registered in the FactorialHR account, with optional filtering by ID, company, type, or enabled status. Webhooks push real-time event notifications (e.g., candidate creation, employee changes) to external URLs. Use when you need to review, monitor, or audit existing webhook integrations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "factorial_hr.reads_all_webhook_subscriptions", owner_id, args, account_id=account_id
        )
