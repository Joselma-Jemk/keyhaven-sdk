# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `keap` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class KeapContactCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    duplicate_option: str | None = None


class KeapContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact: dict[str, Any]


class KeapContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KeapContactCreateQuery | None = None
    body: KeapContactCreateBody


class KeapContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class KeapContactGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    optional_properties: list[Any] | None = None


class KeapContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: KeapContactGetPath
    query: KeapContactGetQuery | None = None


class KeapContactUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contactId: int


class KeapContactUpdateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    update_mask: list[Any] | None = None


class KeapContactUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact: dict[str, Any]


class KeapContactUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: KeapContactUpdatePath
    query: KeapContactUpdateQuery | None = None
    body: KeapContactUpdateBody


class KeapContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    email: str | None = None
    given_name: str | None = None
    family_name: str | None = None
    order: str | None = None
    order_direction: str | None = None
    since: str | None = None
    until: str | None = None
    optional_properties: list[Any] | None = None


class KeapContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KeapContactsListQuery | None = None


class KeapOrderCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact_id: int
    order_date: str
    order_items: list[Any]
    order_title: str
    order_type: str | None = None
    promo_codes: list[Any] | None = None
    sales_affiliate_id: int | None = None
    lead_affiliate_id: int | None = None
    shipping_address: dict[str, Any] | None = None


class KeapOrderCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: KeapOrderCreateBody


class KeapOrdersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact_id: int | None = None
    limit: int | None = None
    offset: int | None = None
    order: str | None = None
    paid: bool | None = None
    product_id: int | None = None
    since: str | None = None
    until: str | None = None


class KeapOrdersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KeapOrdersListQuery | None = None


class KeapNamespace:
    """Typed accessor for the `keap` app. Reach it via `client.apps.keap`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KeapContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new person to the CRM or updates a matching existing contact via duplicate-detection logic (by email or email+name). The contact becomes visible immediately in the Keap UI and is available for campaigns, orders, and automation. This action is reversible by deleting the contact but affects CRM records and any linked automation flows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "keap.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KeapContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full profile of a single CRM contact by its numeric ID, including optional custom properties. Use this when you need to inspect detailed contact information before performing an update or creating an order. The operation is read-only and does not change any data in the CRM.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("keap.contact_get", owner_id, args, account_id=account_id)

    async def contact_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KeapContactUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies specific fields of an existing CRM contact by ID, using an update mask to control which properties change. The updated contact data is immediately reflected in the Keap UI, campaigns, and automation flows. The previous values are overwritten and must be manually restored if needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "keap.contact_update", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KeapContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists contact records from the Keap CRM with pagination and optional filtering by email, name, and last-updated date range. Use this to search, browse, or audit contacts before performing actions like creating an order or updating a profile. This is a read-only operation — no data is created, modified, or deleted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "keap.contacts_list", owner_id, args, account_id=account_id
        )

    async def order_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KeapOrderCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new order record in the CRM with line items, pricing, and optional affiliate and shipping details. The order is immediately recorded in the contact's purchase history and may trigger automation rules, invoicing, or fulfillment workflows. This action impacts financial records and is not automatically reversible — a refund or credit would need to be issued separately.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "keap.order_create", owner_id, args, account_id=account_id
        )

    async def orders_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KeapOrdersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches order records from the CRM with optional filters by contact, product, paid status, and date range. Use this to review order history, check payment status, or find orders linked to a specific contact. No data is modified by this read-only operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("keap.orders_list", owner_id, args, account_id=account_id)
