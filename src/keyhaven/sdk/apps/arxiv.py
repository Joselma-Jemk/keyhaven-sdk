# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `arxiv` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ArxivDownloadPaperPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    paper_id: str


class ArxivDownloadPaperHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/pdf"


class ArxivDownloadPaperRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ArxivDownloadPaperPath
    header: ArxivDownloadPaperHeader | None = None


class ArxivGetCategoriesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "text/html"


class ArxivGetCategoriesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ArxivGetCategoriesHeader | None = None


class ArxivGetDailyUpdatesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    search_query: str
    max_results: int = 10
    sortBy: Literal["lastUpdatedDate", "submittedDate"] = "submittedDate"
    sortOrder: Literal["ascending", "descending"] = "descending"


class ArxivGetDailyUpdatesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class ArxivGetDailyUpdatesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ArxivGetDailyUpdatesQuery
    header: ArxivGetDailyUpdatesHeader | None = None


class ArxivGetPaperMetadataQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id_list: str


class ArxivGetPaperMetadataHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class ArxivGetPaperMetadataRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ArxivGetPaperMetadataQuery
    header: ArxivGetPaperMetadataHeader | None = None


class ArxivSearchPapersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    search_query: str
    id_list: str | None = None
    start: int = 0
    max_results: int = 10
    sortBy: Literal["relevance", "lastUpdatedDate", "submittedDate"] = "relevance"
    sortOrder: Literal["ascending", "descending"] = "descending"


class ArxivSearchPapersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/atom+xml"


class ArxivSearchPapersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ArxivSearchPapersQuery
    header: ArxivSearchPapersHeader | None = None


class ArxivNamespace:
    """Typed accessor for the `arxiv` app. Reach it via `client.apps.arxiv`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def download_paper(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ArxivDownloadPaperRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Downloads the full PDF document of an arXiv paper by its ID directly from arXiv's PDF endpoint. The response is a binary PDF file that can be saved, parsed, or processed further. Use this when an agent needs to read the full paper content, extract text, or analyze figures and tables.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "arxiv.download_paper", owner_id, args, account_id=account_id
        )

    async def get_categories(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ArxivGetCategoriesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the complete arXiv subject category taxonomy from the arXiv website. Returns a hierarchical listing of all categories and subcategories (e.g., cs.AI for Artificial Intelligence, math.NT for Number Theory). Use this to discover valid category codes for constructing targeted search queries in other arXiv functions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "arxiv.get_categories", owner_id, args, account_id=account_id
        )

    async def get_daily_updates(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ArxivGetDailyUpdatesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves papers submitted or updated within the last day for specified arXiv categories. Filters by category prefix (e.g., 'cat:cs.AI,cat:math.NT') and returns the most recent submissions sorted by date. Use this to monitor daily research output in specific fields or to discover newly published papers in areas of interest.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "arxiv.get_daily_updates", owner_id, args, account_id=account_id
        )

    async def get_paper_metadata(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ArxivGetPaperMetadataRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full metadata record for a specific arXiv paper using its unique ID (e.g., '2108.07258' or 'cs/0703041'). Returns title, authors, abstract, submission date, subject categories, and links to the PDF and other formats. Use this to fetch citation information or verify the existence and details of a known paper.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "arxiv.get_paper_metadata", owner_id, args, account_id=account_id
        )

    async def search_papers(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ArxivSearchPapersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches arXiv's open-access repository for scientific papers matching a query string. Supports Boolean operators, field-specific searches, pagination, and sorting by relevance or submission date. Returns Atom XML results containing paper metadata (title, authors, abstract, categories, links). Use this to find academic literature across physics, mathematics, computer science, and related fields.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "arxiv.search_papers", owner_id, args, account_id=account_id
        )
