# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `ramp` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class RampCardsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cardholder_id: str | None = None
    card_program_id: str | None = None
    entity_id: str | None = None
    page_size: int | None = None
    start: str | None = None


class RampCardsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RampCardsListQuery | None = None


class RampTransactionGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    transaction_id: str


class RampTransactionGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RampTransactionGetPath


class RampTransactionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_date: str | None = None
    to_date: str | None = None
    state: (
        Literal[
            "CLEARED", "COMPLETION", "DECLINED", "ERROR", "PENDING", "PENDING_INITIATION", "ALL"
        ]
        | None
    ) = None
    card_id: str | None = None
    user_id: str | None = None
    merchant_id: str | None = None
    page_size: int | None = None
    start: str | None = None


class RampTransactionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RampTransactionsListQuery | None = None


class RampUserInviteCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RampUserInviteCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idempotency_key: str
    email: str
    first_name: str
    last_name: str
    role: Literal[
        "AUDITOR",
        "BUSINESS_ADMIN",
        "BUSINESS_BOOKKEEPER",
        "BUSINESS_OWNER",
        "BUSINESS_USER",
        "GUEST_USER",
        "IT_ADMIN",
    ]
    department_id: str | None = None
    location_id: str | None = None
    direct_manager_id: str | None = None
    is_manager: bool | None = None


class RampUserInviteCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RampUserInviteCreateHeader | None = None
    body: RampUserInviteCreateBody


class RampUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    department_id: str | None = None
    location_id: str | None = None
    role: (
        Literal[
            "AUDITOR",
            "BUSINESS_ADMIN",
            "BUSINESS_BOOKKEEPER",
            "BUSINESS_OWNER",
            "BUSINESS_USER",
            "GUEST_USER",
            "IT_ADMIN",
        ]
        | None
    ) = None
    page_size: int | None = None
    start: str | None = None


class RampUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RampUsersListQuery | None = None


class RampVirtualCardCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RampVirtualCardCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idempotency_key: str
    user_id: str
    display_name: str | None = None
    entity_id: str | None = None
    is_temporary: bool | None = None
    spending_restrictions: dict[str, Any] | None = None


class RampVirtualCardCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RampVirtualCardCreateHeader | None = None
    body: RampVirtualCardCreateBody


class RampNamespace:
    """Typed accessor for the `ramp` app. Reach it via `client.apps.ramp`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def cards_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RampCardsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all corporate cards issued on the business account, with optional filtering by cardholder, card program, or business entity. Supports cursor-based pagination. Use this when you need to audit active cards, find a card ID for spending control operations, or review which cards are assigned to which users.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("ramp.cards_list", owner_id, args, account_id=account_id)

    async def transaction_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RampTransactionGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single corporate card transaction by its unique identifier, returning all available details including merchant info, card holder, and accounting breakdown. No data is modified — this is a read-only lookup. Use this when you need the full picture of a specific transaction for reconciliation, dispute handling, or expense verification.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ramp.transaction_get", owner_id, args, account_id=account_id
        )

    async def transactions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RampTransactionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated, filterable list of corporate card transactions across the business account. Supports filtering by state (cleared, pending, declined), date range, specific card, user, or merchant, and supports cursor-based pagination. Use this when you need to review spending activity, reconcile expenses, or search for a specific transaction by merchant or amount.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ramp.transactions_list", owner_id, args, account_id=account_id
        )

    async def user_invite_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RampUserInviteCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Initiates an asynchronous task to invite a new user to the business account by email, assigning them a role, department, and optional manager. The invitee receives an email and can access the account once they accept. Use this when you need to onboard a new team member, grant a role like auditor or admin, or provision access to Ramp's spending platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ramp.user_invite_create", owner_id, args, account_id=account_id
        )

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RampUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all users on the business account, with optional filtering by department, location, or role. Supports cursor-based pagination. Use this when you need to find a user ID, review account membership, or identify who has which role before inviting or managing users.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("ramp.users_list", owner_id, args, account_id=account_id)

    async def virtual_card_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RampVirtualCardCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Initiates an asynchronous task to issue a new virtual corporate card for a specified user, with optional spending restrictions and temporary auto-lock behaviour. The card can spend company funds up to the configured limits. Use this when you need to provision a new virtual card for an employee, contractor, or specific business purpose — the card becomes spendable once the async task completes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ramp.virtual_card_create", owner_id, args, account_id=account_id
        )
