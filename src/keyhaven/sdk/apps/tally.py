# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `tally` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TallyFormCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspaceId: str | None = None
    templateId: str | None = None
    status: str
    blocks: list[Any]
    settings: dict[str, Any] | None = None


class TallyFormCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: TallyFormCreateBody


class TallyFormGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str


class TallyFormGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TallyFormGetPath


class TallyFormSubmissionsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str


class TallyFormSubmissionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    limit: int | None = None
    filter: str | None = None
    startDate: str | None = None
    endDate: str | None = None
    afterId: str | None = None


class TallyFormSubmissionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TallyFormSubmissionsListPath
    query: TallyFormSubmissionsListQuery | None = None


class TallyFormUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str


class TallyFormUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    status: str | None = None
    blocks: list[Any] | None = None
    settings: dict[str, Any] | None = None


class TallyFormUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TallyFormUpdatePath
    body: TallyFormUpdateBody | None = None


class TallyFormsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    limit: int | None = None
    workspaceIds: str | None = None


class TallyFormsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TallyFormsListQuery | None = None


class TallyNamespace:
    """Typed accessor for the `tally` app. Reach it via `client.apps.tally`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def form_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TallyFormCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new form in Tally, optionally starting from a template or targeting a specific workspace. You can specify the initial status (BLANK, DRAFT, or PUBLISHED), define the form's block structure (questions and layout), and set configuration options like language and redirect behaviour. Use when you need to programmatically provision a new form or duplicate an existing one via template. A blank or draft form is not visible to the public.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "tally.form_create", owner_id, args, account_id=account_id
        )

    async def form_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TallyFormGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a single Tally form by its ID, including its full block tree (questions, text blocks, layout) and all configuration settings (status, language, redirect URLs, notifications). Use when you need to inspect the structure of a form before editing it or to validate its configuration. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("tally.form_get", owner_id, args, account_id=account_id)

    async def form_submissions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TallyFormSubmissionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of submissions received for a Tally form, including all answers, submission timestamps, and completion status (completed or partial). Supports filtering by date range and submission status. Each submission contains the respondent's answers which may include personal data. Use to export form results for analysis, reporting, or downstream processing. No data is created, modified, or deleted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "tally.form_submissions_list", owner_id, args, account_id=account_id
        )

    async def form_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TallyFormUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Apply partial updates to an existing Tally form, including changing its name, status (BLANK, DRAFT, PUBLISHED, or DELETED), replacing its block structure, or updating its settings. Changing status to PUBLISHED makes the form live and accessible to respondents via its public URL; changing to DELETED removes the form entirely. Use to modify form configuration after creation. This action can affect respondent-facing content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "tally.form_update", owner_id, args, account_id=account_id
        )

    async def forms_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TallyFormsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of all Tally forms accessible to the authenticated account, with optional workspace filtering. Each form object includes metadata such as id, name, status, and workspace association. Use this to discover available forms before reading their details or submissions. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("tally.forms_list", owner_id, args, account_id=account_id)
