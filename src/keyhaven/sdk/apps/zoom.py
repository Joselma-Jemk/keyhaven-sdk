# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `zoom` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ZoomMeetingCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    userId: str = "me"


class ZoomMeetingCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    topic: str
    type_: int = Field(default=2, alias="type")
    start_time: str | None = None
    duration: int | None = None
    timezone: str | None = None
    agenda: str | None = None
    settings: dict[str, Any] | None = None


class ZoomMeetingCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZoomMeetingCreatePath = Field(default_factory=ZoomMeetingCreatePath)
    body: ZoomMeetingCreateBody


class ZoomMeetingDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    meetingId: int


class ZoomMeetingDeleteQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    schedule_for_reminder: bool | None = None


class ZoomMeetingDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZoomMeetingDeletePath
    query: ZoomMeetingDeleteQuery | None = None


class ZoomMeetingGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    meetingId: int


class ZoomMeetingGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZoomMeetingGetPath


class ZoomMeetingUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    meetingId: int


class ZoomMeetingUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    topic: str | None = None
    start_time: str | None = None
    duration: int | None = None
    agenda: str | None = None
    settings: dict[str, Any] | None = None


class ZoomMeetingUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZoomMeetingUpdatePath
    body: ZoomMeetingUpdateBody | None = None


class ZoomMeetingsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    userId: str = "me"


class ZoomMeetingsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str | None = Field(default=None, alias="type")
    page_size: int = 30
    next_page_token: str | None = None


class ZoomMeetingsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZoomMeetingsListPath = Field(default_factory=ZoomMeetingsListPath)
    query: ZoomMeetingsListQuery | None = None


class ZoomUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str = "active"
    page_size: int = 30
    next_page_token: str | None = None


class ZoomUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZoomUsersListQuery | None = None


class ZoomNamespace:
    """Typed accessor for the `zoom` app. Reach it via `client.apps.zoom`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def meeting_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZoomMeetingCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Zoom meeting for a user with a topic, type (instant, scheduled, or recurring), start time, duration, timezone, agenda, and optional settings like password, waiting room, or recording preferences. A live join link is generated and participants can join at the scheduled time. Use this when an agent needs to schedule a new meeting and distribute the join information to attendees.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoom.meeting_create", owner_id, args, account_id=account_id
        )

    async def meeting_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZoomMeetingDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a Zoom meeting by its meeting ID, with an option to notify hosts and alternative hosts about the cancellation. Once deleted the meeting link becomes invalid and all scheduled occurrences are removed. Use this when an agent needs to cancel a meeting entirely, for example when an event is no longer needed or has been rescheduled to a new link.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoom.meeting_delete", owner_id, args, account_id=account_id
        )

    async def meeting_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZoomMeetingGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single Zoom meeting by its numeric meeting ID, including settings, start time, duration, agenda, and join URL. This read-only operation returns the complete meeting configuration. Use this when an agent needs to inspect a meeting's current settings before updating or cancelling it, or to retrieve the join link for distribution.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("zoom.meeting_get", owner_id, args, account_id=account_id)

    async def meeting_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZoomMeetingUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing Zoom meeting's configuration including its topic, start time, duration, agenda, or advanced settings. Changes are immediately reflected in the meeting details and affect anything participants see. Use this when an agent needs to reschedule, retitle, or reconfigure a meeting that was previously created, such as adjusting the time after a scheduling conflict.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoom.meeting_update", owner_id, args, account_id=account_id
        )

    async def meetings_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZoomMeetingsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of meetings for a specified Zoom user, filterable by meeting type (scheduled, live, upcoming upcoming). Returns meeting summaries including topic, start time, duration, and join URL. Use this when an agent needs to enumerate existing meetings for a user before performing updates, cancellations, or reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoom.meetings_list", owner_id, args, account_id=account_id
        )

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZoomUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of users on the Zoom account, filterable by status (active, inactive, pending). Returns user details including name, email, and account type. Use this when an agent needs to discover users on the account for meeting creation, user management, or to find a specific user's ID for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("zoom.users_list", owner_id, args, account_id=account_id)
