# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `rocketlane` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class RocketlaneAddFieldOptionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldId: int = 1


class RocketlaneAddFieldOptionHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class RocketlaneAddFieldOptionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    optionColor: Literal["RED", "BLUE", "GREEN", "YELLOW", "PURPLE", "ORANGE"] = "RED"
    optionLabel: str


class RocketlaneAddFieldOptionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RocketlaneAddFieldOptionPath = Field(default_factory=RocketlaneAddFieldOptionPath)
    header: RocketlaneAddFieldOptionHeader = Field(default_factory=RocketlaneAddFieldOptionHeader)
    body: RocketlaneAddFieldOptionBody


class RocketlaneDeleteFieldPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldId: int = 1


class RocketlaneDeleteFieldHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class RocketlaneDeleteFieldRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RocketlaneDeleteFieldPath = Field(default_factory=RocketlaneDeleteFieldPath)
    header: RocketlaneDeleteFieldHeader = Field(default_factory=RocketlaneDeleteFieldHeader)


class RocketlaneGetFieldPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldId: int = 1


class RocketlaneGetFieldQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    includeFields: list[Any] | None = None
    includeAllFields: bool = True


class RocketlaneGetFieldHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class RocketlaneGetFieldRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RocketlaneGetFieldPath = Field(default_factory=RocketlaneGetFieldPath)
    query: RocketlaneGetFieldQuery | None = None
    header: RocketlaneGetFieldHeader = Field(default_factory=RocketlaneGetFieldHeader)


class RocketlaneUpdateFieldPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldId: int = 1


class RocketlaneUpdateFieldQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    includeFields: list[Any] | None = None
    includeAllFields: bool = True


class RocketlaneUpdateFieldHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class RocketlaneUpdateFieldBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldLabel: str
    fieldDescription: str | None = None
    enabled: bool = False
    private: bool = False


class RocketlaneUpdateFieldRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RocketlaneUpdateFieldPath = Field(default_factory=RocketlaneUpdateFieldPath)
    query: RocketlaneUpdateFieldQuery | None = None
    header: RocketlaneUpdateFieldHeader = Field(default_factory=RocketlaneUpdateFieldHeader)
    body: RocketlaneUpdateFieldBody


class RocketlaneUpdateFieldOptionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fieldId: int = 1


class RocketlaneUpdateFieldOptionHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    accept: str = "application/json"


class RocketlaneUpdateFieldOptionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    optionValue: int
    optionColor: Literal["RED", "BLUE", "GREEN", "YELLOW", "PURPLE", "ORANGE"] = "RED"
    optionLabel: str


class RocketlaneUpdateFieldOptionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RocketlaneUpdateFieldOptionPath = Field(default_factory=RocketlaneUpdateFieldOptionPath)
    header: RocketlaneUpdateFieldOptionHeader = Field(
        default_factory=RocketlaneUpdateFieldOptionHeader
    )
    body: RocketlaneUpdateFieldOptionBody


class RocketlaneNamespace:
    """Typed accessor for the `rocketlane` app. Reach it via `client.apps.rocketlane`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def add_field_option(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketlaneAddFieldOptionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new selectable option to an existing single-choice or multiple-choice custom field in Rocketlane, specifying its label and color. The option becomes immediately available for selection on forms and records across the organization. Use this to extend dropdown fields with new values as business requirements evolve.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketlane.add_field_option", owner_id, args, account_id=account_id
        )

    async def delete_field(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketlaneDeleteFieldRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a custom field and all its data from Rocketlane by its field ID. This action cannot be undone — any values stored in this field across projects are lost. Use this only when a field is fully deprecated and its data has been migrated or is no longer needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketlane.delete_field", owner_id, args, account_id=account_id
        )

    async def get_field(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketlaneGetFieldRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the configuration details of a specific custom field in Rocketlane by its unique field ID, with optional control over which properties are returned. Fields define project metadata, dropdown options, and visibility rules across workspaces. Use this to inspect field definitions before updating them or to understand the data model of your Rocketlane account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketlane.get_field", owner_id, args, account_id=account_id
        )

    async def update_field(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketlaneUpdateFieldRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the configuration of an existing custom field in Rocketlane, including its label, description, enabled status, and privacy setting. Changing the enabled or private state affects how the field appears across all projects and workspaces. Use this to rename fields, disable unused ones, or adjust visibility — the change is immediately visible to all users.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketlane.update_field", owner_id, args, account_id=account_id
        )

    async def update_field_option(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketlaneUpdateFieldOptionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the label, color, or associated value of an existing option in a single-choice or multiple-choice custom field in Rocketlane, identified by field ID and option value. Changes to the label or color are immediately reflected wherever the option is displayed across all projects. Use this to rename or recolor dropdown options to match updated business terminology or branding.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketlane.update_field_option", owner_id, args, account_id=account_id
        )
