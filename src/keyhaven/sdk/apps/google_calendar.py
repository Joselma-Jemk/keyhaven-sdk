# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `google_calendar` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GoogleCalendarCalendarListListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    maxResults: int = 100
    pageToken: str | None = None
    showHidden: bool = False


class GoogleCalendarCalendarListListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleCalendarCalendarListListQuery | None = None


class GoogleCalendarCalendarsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"


class GoogleCalendarCalendarsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarCalendarsGetPath = Field(default_factory=GoogleCalendarCalendarsGetPath)


class GoogleCalendarEventsDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"
    eventId: str


class GoogleCalendarEventsDeleteQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sendUpdates: Literal["all", "externalOnly", "none"] | None = None


class GoogleCalendarEventsDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarEventsDeletePath
    query: GoogleCalendarEventsDeleteQuery | None = None


class GoogleCalendarEventsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"
    eventId: str


class GoogleCalendarEventsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarEventsGetPath


class GoogleCalendarEventsInsertPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"


class GoogleCalendarEventsInsertBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    summary: str
    description: str | None = None
    location: str | None = None
    start: dict[str, Any]
    end: dict[str, Any]
    attendees: list[Any] | None = None
    reminders: dict[str, Any] | None = None


class GoogleCalendarEventsInsertRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarEventsInsertPath = Field(default_factory=GoogleCalendarEventsInsertPath)
    body: GoogleCalendarEventsInsertBody


class GoogleCalendarEventsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"


class GoogleCalendarEventsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    timeMin: str | None = None
    timeMax: str | None = None
    maxResults: int = 250
    singleEvents: bool = False
    orderBy: Literal["startTime", "updated"] | None = None
    q: str | None = None
    pageToken: str | None = None
    timeZone: str | None = None


class GoogleCalendarEventsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarEventsListPath = Field(default_factory=GoogleCalendarEventsListPath)
    query: GoogleCalendarEventsListQuery | None = None


class GoogleCalendarEventsPatchPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"
    eventId: str


class GoogleCalendarEventsPatchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sendUpdates: Literal["all", "externalOnly", "none"] | None = None


class GoogleCalendarEventsPatchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    summary: str | None = None
    description: str | None = None
    location: str | None = None
    start: dict[str, Any] | None = None
    end: dict[str, Any] | None = None
    attendees: list[Any] | None = None


class GoogleCalendarEventsPatchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarEventsPatchPath
    query: GoogleCalendarEventsPatchQuery | None = None
    body: GoogleCalendarEventsPatchBody = Field(default_factory=GoogleCalendarEventsPatchBody)


class GoogleCalendarEventsQuickAddPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"


class GoogleCalendarEventsQuickAddQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    text: str


class GoogleCalendarEventsQuickAddRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarEventsQuickAddPath = Field(default_factory=GoogleCalendarEventsQuickAddPath)
    query: GoogleCalendarEventsQuickAddQuery


class GoogleCalendarEventsUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    calendarId: str = "primary"
    eventId: str


class GoogleCalendarEventsUpdateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sendUpdates: Literal["all", "externalOnly", "none"] | None = None


class GoogleCalendarEventsUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    summary: str
    description: str | None = None
    location: str | None = None
    start: dict[str, Any]
    end: dict[str, Any]
    attendees: list[Any] | None = None


class GoogleCalendarEventsUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleCalendarEventsUpdatePath
    query: GoogleCalendarEventsUpdateQuery | None = None
    body: GoogleCalendarEventsUpdateBody


class GoogleCalendarFreebusyQueryBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    timeMin: str
    timeMax: str
    items: list[Any]
    timeZone: str | None = None


class GoogleCalendarFreebusyQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: GoogleCalendarFreebusyQueryBody


class GoogleCalendarNamespace:
    """Typed accessor for the `google_calendar` app. Reach it via `client.apps.google_calendar`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def calendar_list_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarCalendarListListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the list of calendars that appear in the authenticated user calendar list, optionally including hidden entries. Each entry includes the calendar summary, access role, and visibility settings. Use this when an agent needs to discover which calendars are available before performing operations such as listing events or checking free/busy information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.calendar_list_list", owner_id, args, account_id=account_id
        )

    async def calendars_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarCalendarsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve metadata for a specific calendar, including its summary (name), description, time zone, and the authenticated user access role (such as free-busy reader, reader, writer, or owner). Use this when an agent needs to verify calendar details or check user permissions before performing write operations on that calendar.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.calendars_get", owner_id, args, account_id=account_id
        )

    async def events_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarEventsDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently remove an event from a specified calendar. All attendee invitations are canceled and the event is no longer visible to anyone. For recurring events, individual instances can be deleted without affecting the rest of the series. Use this when an agent needs to remove a scheduled event that is no longer relevant, while being aware this action cannot be undone.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.events_delete", owner_id, args, account_id=account_id
        )

    async def events_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarEventsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full details of a single calendar event by its event ID, including title, time, description, attendees, reminders, and recurrence information. Use this when an agent needs to inspect a specific event details before deciding whether to update, delete, or respond to it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.events_get", owner_id, args, account_id=account_id
        )

    async def events_insert(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarEventsInsertRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new event on a specified calendar with a title, start and end times, optional attendees, description, location, and custom reminders. The event is immediately visible on the calendar and invitation emails are sent to listed attendees. Use this when an agent needs to schedule meetings, appointments, or reminders — any created event with attendees will notify real people via email.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.events_insert", owner_id, args, account_id=account_id
        )

    async def events_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarEventsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List events from a specified calendar within an optional time window, with support for free-text search, ordering, and pagination. Recurring events can be expanded into individual instances or returned as a single series. Use this when an agent needs to view scheduled events, check availability, or retrieve event IDs for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.events_list", owner_id, args, account_id=account_id
        )

    async def events_patch(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarEventsPatchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Update specific fields of an existing calendar event without replacing the entire resource. Fields not included in the body are left unchanged. Controls whether notification emails are sent to attendees about the change. Use this when an agent needs to make targeted modifications like adjusting the time, updating the description, or modifying the attendee list without resubmitting the full event.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.events_patch", owner_id, args, account_id=account_id
        )

    async def events_quick_add(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarEventsQuickAddRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a calendar event by providing a natural-language text description such as "Lunch with Sarah next Tuesday at 1pm." The Google Calendar server parses the text to extract title, date, time, and potential attendees. Use this when an agent needs to quickly add an event without specifying structured fields — the parsed result may misinterpret ambiguous input, so verification is recommended.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.events_quick_add", owner_id, args, account_id=account_id
        )

    async def events_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarEventsUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Replace an existing calendar event entirely with a new resource — all event fields must be provided in the body. Controls whether notification emails are sent to attendees about the change. Use this when an agent needs to fully reschedule or restructure an existing event, such as changing its time, location, or attendee list.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.events_update", owner_id, args, account_id=account_id
        )

    async def freebusy_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleCalendarFreebusyQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Check the busy time intervals for one or more calendars over a specified time range. Returns time periods where each calendar has scheduled events, enabling availability detection. Use this when an agent needs to find available meeting slots before creating events or to verify scheduling conflicts across multiple calendars.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_calendar.freebusy_query", owner_id, args, account_id=account_id
        )
