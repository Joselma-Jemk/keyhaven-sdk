# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `tavily` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TavilyExtractBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    urls: list[Any]
    api_key: str = "X-API-KEY"


class TavilyExtractRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: TavilyExtractBody


class TavilySearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    api_key: str = "X-API-KEY"
    search_depth: Literal["basic", "advanced"] = "basic"
    topic: Literal["general", "news"] = "general"
    days: int = 3
    max_results: int = 5
    include_images: bool = False
    include_image_descriptions: bool = False
    include_answer: bool = False
    include_raw_content: bool = False
    include_domains: list[Any] | None = None
    exclude_domains: list[Any] | None = None


class TavilySearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: TavilySearchBody


class TavilyNamespace:
    """Typed accessor for the `tavily` app. Reach it via `client.apps.tavily`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def extract(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TavilyExtractRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Scrapes and returns the raw textual content of one or more specified URLs using Tavily's extraction engine. Performs a live HTTP request against Tavily's /extract endpoint, fetching, parsing, and cleaning the HTML of each provided URL. This action is read-only — it retrieves public web content without modifying any source or publishing anything. Use when the agent needs the full body text of specific pages rather than just search snippets.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("tavily.extract", owner_id, args, account_id=account_id)

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TavilySearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes a web search via Tavily's search engine, optimized for LLM consumption. Queries Tavily's /search endpoint and returns structured results including URLs, titles, snippets, and optionally full page content, images, or a summarized answer. This action is read-only — it retrieves information from the web without modifying any data or publishing anything. Use when the agent needs real-time or news information to answer a user query.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("tavily.search", owner_id, args, account_id=account_id)
