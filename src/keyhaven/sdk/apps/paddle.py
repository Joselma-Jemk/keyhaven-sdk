# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `paddle` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PaddleCreateCustomerHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    Paddle_Version: str = Field(default="1", alias="Paddle-Version")


class PaddleCreateCustomerBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str
    name: str | None = None
    locale: str = "en"
    custom_data: dict[str, Any] | None = None


class PaddleCreateCustomerRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PaddleCreateCustomerHeader = Field(default_factory=PaddleCreateCustomerHeader)
    body: PaddleCreateCustomerBody


class PaddleCreateProductHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    Paddle_Version: str = Field(default="1", alias="Paddle-Version")


class PaddleCreateProductBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    tax_category: str = "standard"
    description: str | None = None
    image_url: str | None = None
    custom_data: dict[str, Any] | None = None


class PaddleCreateProductRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PaddleCreateProductHeader = Field(default_factory=PaddleCreateProductHeader)
    body: PaddleCreateProductBody


class PaddleGetCustomerPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer_id: str


class PaddleGetCustomerHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    Paddle_Version: str = Field(default="1", alias="Paddle-Version")


class PaddleGetCustomerRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PaddleGetCustomerPath
    header: PaddleGetCustomerHeader = Field(default_factory=PaddleGetCustomerHeader)


class PaddleListCustomersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    after: str | None = None
    email: list[Any] | None = None
    id: list[Any] | None = None
    order_by: str | None = None
    per_page: int = 50
    search: str | None = None
    status: list[Any] | None = None


class PaddleListCustomersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    Paddle_Version: str = Field(default="1", alias="Paddle-Version")


class PaddleListCustomersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PaddleListCustomersQuery | None = None
    header: PaddleListCustomersHeader = Field(default_factory=PaddleListCustomersHeader)


class PaddleUpdateCustomerPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer_id: str


class PaddleUpdateCustomerHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    Paddle_Version: str = Field(default="1", alias="Paddle-Version")


class PaddleUpdateCustomerBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    name: str | None = None
    locale: str | None = None
    custom_data: dict[str, Any] | None = None


class PaddleUpdateCustomerRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PaddleUpdateCustomerPath
    header: PaddleUpdateCustomerHeader = Field(default_factory=PaddleUpdateCustomerHeader)
    body: PaddleUpdateCustomerBody = Field(default_factory=PaddleUpdateCustomerBody)


class PaddleNamespace:
    """Typed accessor for the `paddle` app. Reach it via `client.apps.paddle`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_customer(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PaddleCreateCustomerRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer entity in the Paddle billing system with an email address (required), optional name, locale, and custom data fields. The customer becomes available immediately for assigning subscriptions, invoices, and transactions. Use this when a new buyer registers or when migrating customer data into Paddle from another billing platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "paddle.create_customer", owner_id, args, account_id=account_id
        )

    async def create_product(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PaddleCreateProductRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new product entity in the Paddle catalog with a name (required), tax category, description, optional image URL, and custom data. The product appears immediately in the Paddle dashboard and can be associated with prices, subscriptions, and one-time charges. Use this when adding new offerings to the product catalog before configuring pricing plans or associating subscription items.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "paddle.create_product", owner_id, args, account_id=account_id
        )

    async def get_customer(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PaddleGetCustomerRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single Paddle customer by their customer ID, including email, name, locale, status, custom data, and creation timestamps. Use this to inspect a customer's profile before performing operations like creating subscriptions or processing transactions. Requires the customer_id path parameter.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "paddle.get_customer", owner_id, args, account_id=account_id
        )

    async def list_customers(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PaddleListCustomersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of customers in the Paddle billing system, filterable by email, customer ID, status (active or archived), and free-text search across id, name, and email fields. Each customer entry includes billing details, status, and creation timestamps. Use this to browse the customer base, locate a specific customer by email, or export customer data for external reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "paddle.list_customers", owner_id, args, account_id=account_id
        )

    async def update_customer(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PaddleUpdateCustomerRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing customer's profile in the Paddle billing system, allowing changes to email, name, locale, and custom data fields. The update takes effect immediately and affects future invoices, communications, and tax calculations tied to this customer. Use this to correct customer information, update contact details, or modify custom metadata after the customer was initially created.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "paddle.update_customer", owner_id, args, account_id=account_id
        )
