# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `outreach` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class OutreachMailingsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_number_: int | None = Field(default=None, alias="page[number]")
    page_size_: int | None = Field(default=None, alias="page[size]")
    sort: str | None = None
    filter_prospect__id_: int | None = Field(default=None, alias="filter[prospect][id]")
    filter_sequence__id_: int | None = Field(default=None, alias="filter[sequence][id]")
    filter_mailbox__id_: int | None = Field(default=None, alias="filter[mailbox][id]")
    filter_user__id_: int | None = Field(default=None, alias="filter[user][id]")
    filter_state_: str | None = Field(default=None, alias="filter[state]")
    include: str | None = None


class OutreachMailingsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OutreachMailingsListQuery | None = None


class OutreachProspectCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any]


class OutreachProspectCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: OutreachProspectCreateBody


class OutreachProspectGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class OutreachProspectGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields_prospect_: str | None = Field(default=None, alias="fields[prospect]")
    include: str | None = None


class OutreachProspectGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: OutreachProspectGetPath
    query: OutreachProspectGetQuery | None = None


class OutreachProspectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_number_: int | None = Field(default=None, alias="page[number]")
    page_size_: int | None = Field(default=None, alias="page[size]")
    sort: str | None = None
    fields_prospect_: str | None = Field(default=None, alias="fields[prospect]")
    filter_firstName_: str | None = Field(default=None, alias="filter[firstName]")
    filter_lastName_: str | None = Field(default=None, alias="filter[lastName]")
    filter_email_: str | None = Field(default=None, alias="filter[email]")
    filter_account__id_: int | None = Field(default=None, alias="filter[account][id]")
    filter_owner__id_: int | None = Field(default=None, alias="filter[owner][id]")
    include: str | None = None


class OutreachProspectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OutreachProspectsListQuery | None = None


class OutreachSequencesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_number_: int | None = Field(default=None, alias="page[number]")
    page_size_: int | None = Field(default=None, alias="page[size]")
    sort: str | None = None
    filter_enabled_: bool | None = Field(default=None, alias="filter[enabled]")
    filter_shareType_: str | None = Field(default=None, alias="filter[shareType]")
    filter_user__id_: int | None = Field(default=None, alias="filter[user][id]")


class OutreachSequencesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: OutreachSequencesListQuery | None = None


class OutreachNamespace:
    """Typed accessor for the `outreach` app. Reach it via `client.apps.outreach`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def mailings_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OutreachMailingsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of email mailings sent through Outreach, with filters for prospect, sequence, mailbox, sender, and delivery state (delivered, opened, clicked, replied, bounced, failed, scheduled, sending). Each mailing record includes timestamps, engagement status, and linked resources. Use this to audit email activity, measure campaign performance, investigate delivery issues, or review a specific prospect's communication history.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "outreach.mailings_list", owner_id, args, account_id=account_id
        )

    async def prospect_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OutreachProspectCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new prospect (contact) record in the Outreach sales engagement platform with attributes like name, email, job title, company, and LinkedIn URL, and optionally links it to an existing account or owner via JSON:API relationships. The prospect is immediately visible in the Outreach UI and available for sequences, mailings, and reporting. Use this to add leads from external sources or manually register contacts that should enter sales workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "outreach.prospect_create", owner_id, args, account_id=account_id
        )

    async def prospect_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OutreachProspectGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single prospect's full profile from Outreach using its numeric ID, including name, email, job title, company, and linked accounts or owner. Use this to view a complete prospect record before taking action such as adding to a sequence or updating contact information. Optionally include related resources like account, owner, and mailing history via the include parameter.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "outreach.prospect_get", owner_id, args, account_id=account_id
        )

    async def prospects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OutreachProspectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of prospects (contacts) from the Outreach sales engagement platform, with support for JSON:API filters on name, email, account, and owner, as well as sorting and sparse fieldsets. Each prospect entry includes contact details, job information, and links to related resources. Use this to browse the prospect database, find contacts matching specific criteria, or synchronize Outreach data with external systems.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "outreach.prospects_list", owner_id, args, account_id=account_id
        )

    async def sequences_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OutreachSequencesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of Outreach sequences (automated multi-step cadences) filterable by enabled status, sharing scope (private, team, company, shared), and owning user. Each sequence entry includes metadata such as step count, creation date, and enabled state. Use this to discover available sequences before enrolling prospects or to audit the sequence library for compliance and coverage analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "outreach.sequences_list", owner_id, args, account_id=account_id
        )
