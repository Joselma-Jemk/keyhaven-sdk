# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `microsoft_teams` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MicrosoftTeamsAddChatMemberPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str


class MicrosoftTeamsAddChatMemberBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    odata_type: str = Field(
        default="#microsoft.graph.aadUserConversationMember", alias="@odata.type"
    )
    roles: list[Any] = ["owner"]
    user_odata_bind: str = Field(alias="user@odata.bind")


class MicrosoftTeamsAddChatMemberRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsAddChatMemberPath
    body: MicrosoftTeamsAddChatMemberBody


class MicrosoftTeamsAddTeamMemberPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsAddTeamMemberBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    odata_type: str = Field(
        default="#microsoft.graph.aadUserConversationMember", alias="@odata.type"
    )
    user_odata_bind: str = Field(alias="user@odata.bind")
    roles: list[Any] = []


class MicrosoftTeamsAddTeamMemberRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsAddTeamMemberPath
    body: MicrosoftTeamsAddTeamMemberBody


class MicrosoftTeamsArchiveTeamPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsArchiveTeamBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shouldSetSpoSiteReadOnlyForMembers: bool = False


class MicrosoftTeamsArchiveTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsArchiveTeamPath
    body: MicrosoftTeamsArchiveTeamBody | None = None


class MicrosoftTeamsCreateChannelPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsCreateChannelBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    displayName: str
    description: str | None = None
    membershipType: Literal["standard", "private"] = "standard"


class MicrosoftTeamsCreateChannelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsCreateChannelPath
    body: MicrosoftTeamsCreateChannelBody


class MicrosoftTeamsCreateChatBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chatType: Literal["oneOnOne", "group"]
    topic: str | None = None
    members: list[Any]


class MicrosoftTeamsCreateChatRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MicrosoftTeamsCreateChatBody


class MicrosoftTeamsCreateTeamBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    template_odata_bind: str = Field(
        default="https://graph.microsoft.com/v1.0/teamsTemplates('standard')",
        alias="template@odata.bind",
    )
    displayName: str
    description: str | None = None
    visibility: Literal["private", "public"] = "private"


class MicrosoftTeamsCreateTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MicrosoftTeamsCreateTeamBody


class MicrosoftTeamsDeleteChannelPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str


class MicrosoftTeamsDeleteChannelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsDeleteChannelPath


class MicrosoftTeamsDeleteTeamPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsDeleteTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsDeleteTeamPath


class MicrosoftTeamsGetChannelPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str


class MicrosoftTeamsGetChannelQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    expand: str | None = Field(default=None, alias="$expand")


class MicrosoftTeamsGetChannelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsGetChannelPath
    query: MicrosoftTeamsGetChannelQuery | None = None


class MicrosoftTeamsGetChatMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str
    message_id: str


class MicrosoftTeamsGetChatMessageQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    expand: str | None = Field(default=None, alias="$expand")


class MicrosoftTeamsGetChatMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsGetChatMessagePath
    query: MicrosoftTeamsGetChatMessageQuery | None = None


class MicrosoftTeamsGetTeamPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsGetTeamQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    expand: str | None = Field(default=None, alias="$expand")


class MicrosoftTeamsGetTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsGetTeamPath
    query: MicrosoftTeamsGetTeamQuery | None = None


class MicrosoftTeamsListChannelMessagesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str


class MicrosoftTeamsListChannelMessagesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    filter: str | None = Field(default=None, alias="$filter")
    orderby: str | None = Field(default=None, alias="$orderby")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")


class MicrosoftTeamsListChannelMessagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsListChannelMessagesPath
    query: MicrosoftTeamsListChannelMessagesQuery | None = None


class MicrosoftTeamsListChatMembersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str


class MicrosoftTeamsListChatMembersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    top: int | None = Field(default=None, alias="$top")


class MicrosoftTeamsListChatMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsListChatMembersPath
    query: MicrosoftTeamsListChatMembersQuery | None = None


class MicrosoftTeamsListChatMessagesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str


class MicrosoftTeamsListChatMessagesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")
    orderby: str | None = Field(default=None, alias="$orderby")


class MicrosoftTeamsListChatMessagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsListChatMessagesPath
    query: MicrosoftTeamsListChatMessagesQuery | None = None


class MicrosoftTeamsListJoinedTeamsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")
    filter: str | None = Field(default=None, alias="$filter")


class MicrosoftTeamsListJoinedTeamsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MicrosoftTeamsListJoinedTeamsQuery | None = None


class MicrosoftTeamsListMessageRepliesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str


class MicrosoftTeamsListMessageRepliesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")


class MicrosoftTeamsListMessageRepliesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsListMessageRepliesPath
    query: MicrosoftTeamsListMessageRepliesQuery | None = None


class MicrosoftTeamsListTeamChannelsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsListTeamChannelsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    filter: str | None = Field(default=None, alias="$filter")
    orderby: str | None = Field(default=None, alias="$orderby")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")


class MicrosoftTeamsListTeamChannelsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsListTeamChannelsPath
    query: MicrosoftTeamsListTeamChannelsQuery | None = None


class MicrosoftTeamsListTeamMembersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsListTeamMembersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    filter: str | None = Field(default=None, alias="$filter")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")


class MicrosoftTeamsListTeamMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsListTeamMembersPath
    query: MicrosoftTeamsListTeamMembersQuery | None = None


class MicrosoftTeamsListUserChatsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    select: str | None = Field(default=None, alias="$select")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")
    filter: str | None = Field(default=None, alias="$filter")
    orderby: str | None = Field(default=None, alias="$orderby")


class MicrosoftTeamsListUserChatsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MicrosoftTeamsListUserChatsQuery | None = None


class MicrosoftTeamsRemoveChatMemberPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str
    member_id: str


class MicrosoftTeamsRemoveChatMemberRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsRemoveChatMemberPath


class MicrosoftTeamsRemoveTeamMemberPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    member_id: str


class MicrosoftTeamsRemoveTeamMemberRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsRemoveTeamMemberPath


class MicrosoftTeamsSendChannelMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str


class MicrosoftTeamsSendChannelMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: dict[str, Any]
    subject: str | None = None


class MicrosoftTeamsSendChannelMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSendChannelMessagePath
    body: MicrosoftTeamsSendChannelMessageBody


class MicrosoftTeamsSendChatMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str


class MicrosoftTeamsSendChatMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: dict[str, Any]


class MicrosoftTeamsSendChatMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSendChatMessagePath
    body: MicrosoftTeamsSendChatMessageBody


class MicrosoftTeamsSendMessageReplyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str


class MicrosoftTeamsSendMessageReplyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: dict[str, Any]


class MicrosoftTeamsSendMessageReplyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSendMessageReplyPath
    body: MicrosoftTeamsSendMessageReplyBody


class MicrosoftTeamsSetChannelMessageReactionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str


class MicrosoftTeamsSetChannelMessageReactionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reactionType: str


class MicrosoftTeamsSetChannelMessageReactionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSetChannelMessageReactionPath
    body: MicrosoftTeamsSetChannelMessageReactionBody


class MicrosoftTeamsSetChatMessageReactionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str
    message_id: str


class MicrosoftTeamsSetChatMessageReactionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reactionType: str


class MicrosoftTeamsSetChatMessageReactionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSetChatMessageReactionPath
    body: MicrosoftTeamsSetChatMessageReactionBody


class MicrosoftTeamsSoftDeleteChannelMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str


class MicrosoftTeamsSoftDeleteChannelMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSoftDeleteChannelMessagePath


class MicrosoftTeamsSoftDeleteChatMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str
    message_id: str


class MicrosoftTeamsSoftDeleteChatMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSoftDeleteChatMessagePath


class MicrosoftTeamsSoftDeleteReplyMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str
    reply_id: str


class MicrosoftTeamsSoftDeleteReplyMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsSoftDeleteReplyMessagePath


class MicrosoftTeamsUnarchiveTeamPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsUnarchiveTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUnarchiveTeamPath


class MicrosoftTeamsUndoSoftDeleteChannelMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str


class MicrosoftTeamsUndoSoftDeleteChannelMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUndoSoftDeleteChannelMessagePath


class MicrosoftTeamsUndoSoftDeleteChatMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str
    message_id: str


class MicrosoftTeamsUndoSoftDeleteChatMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUndoSoftDeleteChatMessagePath


class MicrosoftTeamsUndoSoftDeleteReplyMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str
    reply_id: str


class MicrosoftTeamsUndoSoftDeleteReplyMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUndoSoftDeleteReplyMessagePath


class MicrosoftTeamsUnsetChannelMessageReactionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str


class MicrosoftTeamsUnsetChannelMessageReactionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reactionType: str


class MicrosoftTeamsUnsetChannelMessageReactionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUnsetChannelMessageReactionPath
    body: MicrosoftTeamsUnsetChannelMessageReactionBody


class MicrosoftTeamsUnsetChatMessageReactionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str
    message_id: str


class MicrosoftTeamsUnsetChatMessageReactionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    reactionType: str


class MicrosoftTeamsUnsetChatMessageReactionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUnsetChatMessageReactionPath
    body: MicrosoftTeamsUnsetChatMessageReactionBody


class MicrosoftTeamsUpdateChannelPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str


class MicrosoftTeamsUpdateChannelBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    displayName: str | None = None
    description: str | None = None


class MicrosoftTeamsUpdateChannelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUpdateChannelPath
    body: MicrosoftTeamsUpdateChannelBody = Field(default_factory=MicrosoftTeamsUpdateChannelBody)


class MicrosoftTeamsUpdateChannelMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    channel_id: str
    message_id: str


class MicrosoftTeamsUpdateChannelMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: dict[str, Any]


class MicrosoftTeamsUpdateChannelMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUpdateChannelMessagePath
    body: MicrosoftTeamsUpdateChannelMessageBody


class MicrosoftTeamsUpdateChatMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chat_id: str
    message_id: str


class MicrosoftTeamsUpdateChatMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: dict[str, Any]


class MicrosoftTeamsUpdateChatMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUpdateChatMessagePath
    body: MicrosoftTeamsUpdateChatMessageBody


class MicrosoftTeamsUpdateTeamPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class MicrosoftTeamsUpdateTeamBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    displayName: str | None = None
    description: str | None = None
    visibility: Literal["private", "public"] | None = None


class MicrosoftTeamsUpdateTeamRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUpdateTeamPath
    body: MicrosoftTeamsUpdateTeamBody = Field(default_factory=MicrosoftTeamsUpdateTeamBody)


class MicrosoftTeamsUpdateTeamMemberPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str
    member_id: str


class MicrosoftTeamsUpdateTeamMemberBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    odata_type: str = Field(
        default="#microsoft.graph.aadUserConversationMember", alias="@odata.type"
    )
    roles: list[Any]


class MicrosoftTeamsUpdateTeamMemberRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MicrosoftTeamsUpdateTeamMemberPath
    body: MicrosoftTeamsUpdateTeamMemberBody


class MicrosoftTeamsNamespace:
    """Typed accessor for the `microsoft_teams` app. Reach it via `client.apps.microsoft_teams`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def add_chat_member(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsAddChatMemberRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new participant to an existing group chat by their Azure AD user ID, optionally assigning roles. The new member gains access to the full chat history and all future messages. Use this to include additional people in an ongoing group conversation. This action only applies to group chats, not 1:1 chats. Reversible by removing the member.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.add_chat_member", owner_id, args, account_id=account_id
        )

    async def add_team_member(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsAddTeamMemberRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a user to an existing team by their Azure AD user ID or email, optionally assigning them as an owner, guest, or basic member. The user immediately gains access to the team's channels, files, and conversations according to their role. Use this to onboard new members to a team. Reversible by subsequently removing the member.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.add_team_member", owner_id, args, account_id=account_id
        )

    async def archive_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsArchiveTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Archives a team to make it read-only for all members, preventing new messages, channel creation, or edits while preserving all existing content for reference. Optionally also makes the SharePoint site read-only for members. Archived teams appear in the Teams client with a banner indicating the archived state. Use this to freeze a completed project while retaining access. Reversible via unarchive.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.archive_team", owner_id, args, account_id=account_id
        )

    async def create_channel(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsCreateChannelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new channel within an existing team with a specified display name, optional description, and membership type (standard or private). Standard channels are visible to all team members; private channels are restricted to invited members. The channel appears immediately in the team's channel list. Use this to organize conversations into dedicated topics or workstreams.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.create_channel", owner_id, args, account_id=account_id
        )

    async def create_chat(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsCreateChatRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new 1:1 or group chat with specified participants. For 1:1 chats, exactly two members are required; for group chats, use two or more members and optionally a topic name. The chat appears in all participants' Teams clients immediately. Use this to start a private conversation between users. The chatType and membership are not changeable after creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.create_chat", owner_id, args, account_id=account_id
        )

    async def create_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsCreateTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Microsoft Team along with its underlying Microsoft 365 group, SharePoint site, and OneNote notebook. Requires a display name, optionally a description and visibility setting (defaults to private). The new team appears in all members' Teams clients. Use this when setting up a new collaboration workspace. This is a significant action that creates persistent infrastructure.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.create_team", owner_id, args, account_id=account_id
        )

    async def delete_channel(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsDeleteChannelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a channel and all its messages, files, tabs, and conversation history from a team. The General channel cannot be deleted. For private channels, the associated SharePoint site is also removed. This action cannot be undone through the API (only via SharePoint retention). Use only when a channel and all its content must be permanently removed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.delete_channel", owner_id, args, account_id=account_id
        )

    async def delete_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsDeleteTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a team and its associated Microsoft 365 group, which removes all channels, messages, files, SharePoint site, and calendar. This action triggers the 30-day soft-delete retention period in Entra ID, during which the group can be restored by an administrator. All team members lose access immediately. Use only when a team must be entirely decommissioned. This is a destructive action with wide-ranging impact.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.delete_team", owner_id, args, account_id=account_id
        )

    async def get_channel(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsGetChannelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific channel within a team, including its display name, description, membership type (standard or private), and creation metadata. Use this to inspect a channel's configuration before posting messages, updating it, or checking its moderation settings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.get_channel", owner_id, args, account_id=account_id
        )

    async def get_chat_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsGetChatMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single message from a chat by its chat ID and message ID, returning its full content, sender details, reactions, attachments, and metadata. Use this to retrieve a specific message's details without fetching the entire chat history, or to inspect a message before replying or acting on it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.get_chat_message", owner_id, args, account_id=account_id
        )

    async def get_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsGetTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed properties of a specific team including its display name, description, visibility (public/private), classification, internalId, and special settings like memberAbilities or messagingSettings. Unlike the joined teams list, this returns the full team resource object. Use when you need a team's configuration details or to verify its current state before making changes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.get_team", owner_id, args, account_id=account_id
        )

    async def list_channel_messages(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListChannelMessagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all top-level messages in a channel (not replies), each with sender info, body content, created date, and any attachments or mentions. Use this to read the conversation history of a channel, find a specific message by its content or author, or obtain a message ID to reply to or react to.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_channel_messages", owner_id, args, account_id=account_id
        )

    async def list_chat_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListChatMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all participants in a specified chat conversation (1:1 or group), showing each member's user ID, display name, email, and roles. Use this to see who is in a chat before sending a message, or to find a member's ID for adding or removing participants in group chats.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_chat_members", owner_id, args, account_id=account_id
        )

    async def list_chat_messages(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListChatMessagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all messages in a specific chat conversation (1:1 or group), ordered chronologically. Each message includes sender information, body content, reactions, and attachments. Use this to read the conversation history of a direct message or group chat, or to locate a specific message ID for updating or reacting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_chat_messages", owner_id, args, account_id=account_id
        )

    async def list_joined_teams(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListJoinedTeamsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all Microsoft Teams that the authenticated user is a direct member of. Results include standard, private, and shared channels the user belongs to, but exclude the host team of shared channels where the user only has guest access. Use this to enumerate the user's team memberships and obtain team IDs for subsequent operations like listing channels or members.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_joined_teams", owner_id, args, account_id=account_id
        )

    async def list_message_replies(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListMessageRepliesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all replies to a specific channel message, forming the complete conversation thread. Each reply includes sender info, body content, and timestamps. Use this to read the full discussion under a parent message or to find a specific reply's ID for moderation actions like soft-delete.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_message_replies", owner_id, args, account_id=account_id
        )

    async def list_team_channels(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListTeamChannelsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all channels in a specified team, including standard and private channels the user has access to. Each channel entry includes its display name, description, membership type, and creation date. Use this to discover the channels within a team before reading messages or posting content to a specific channel.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_team_channels", owner_id, args, account_id=account_id
        )

    async def list_team_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListTeamMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists every member of a specified team with their user ID, display name, email, and assigned roles (member, owner, or guest). Use this to audit team membership, verify who has owner privileges, or find a user's member ID before updating or removing them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_team_members", owner_id, args, account_id=account_id
        )

    async def list_user_chats(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsListUserChatsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all chat conversations the authenticated user participates in, including 1:1 DMs, group chats, and meeting chats. Each entry includes the chat type, topic, and creation date. Use this to discover available chats and obtain their IDs before reading messages or sending chat messages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.list_user_chats", owner_id, args, account_id=account_id
        )

    async def remove_chat_member(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsRemoveChatMemberRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Removes a participant from a group chat by their membership ID. The removed user immediately loses access to the chat and its history. Use this to remove someone who no longer needs to be part of a group conversation. This action only applies to group chats, not 1:1 chats. Reversible by re-adding the member.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.remove_chat_member", owner_id, args, account_id=account_id
        )

    async def remove_team_member(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsRemoveTeamMemberRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Removes a member from a team using their membership ID (not user ID). The removed user immediately loses access to all team channels, files, and conversations. Owners cannot remove the last owner. Use this to offboard users from a team or revoke access to team resources. Reversible by re-adding the member.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.remove_team_member", owner_id, args, account_id=account_id
        )

    async def send_channel_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSendChannelMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Posts a new message to a specified channel, visible immediately to all channel members. Supports both plain text and HTML content, with an optional subject line. The message appears under the authenticated user's identity. Use this to broadcast announcements, post updates, or respond to channel conversations. This action is visible to all team members and cannot be undone without an explicit soft-delete.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.send_channel_message", owner_id, args, account_id=account_id
        )

    async def send_chat_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSendChatMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends a message to an existing 1:1 or group chat by its chat ID. Supports plain text or HTML content. The message is delivered privately to chat participants only, not visible in any team channel. Use this for direct or small-group communications that should not appear in a public team channel. The message cannot be unsent without a soft-delete.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.send_chat_message", owner_id, args, account_id=account_id
        )

    async def send_message_reply(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSendMessageReplyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends a threaded reply to an existing channel message, grouped under the parent message in the Teams UI. Supports plain text or HTML content. Replies maintain the conversation thread and are visible to everyone who can see the parent message. Use this to respond to a specific discussion, answer a question, or continue a thread.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.send_message_reply", owner_id, args, account_id=account_id
        )

    async def set_channel_message_reaction(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSetChannelMessageReactionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds an emoji reaction to a channel message, visible to all channel members. The reaction appears immediately under the message with the authenticated user's identity. Use this to acknowledge, endorse, or express emotion on a channel message without sending a text reply. Reactions are publicly attributed and can be removed via the unset reaction action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.set_channel_message_reaction", owner_id, args, account_id=account_id
        )

    async def set_chat_message_reaction(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSetChatMessageReactionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds an emoji reaction to a chat message (1:1 or group), visible to all chat participants. The reaction appears immediately under the message. Use this to quickly respond to a direct message or group chat message without typing a text reply. Reactions are publicly attributed to the reacting user and can be removed via the unset action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.set_chat_message_reaction", owner_id, args, account_id=account_id
        )

    async def soft_delete_channel_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSoftDeleteChannelMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Soft-deletes a channel message, hiding it from all channel members in the Teams UI while retaining the data for recovery. The message can be restored by the undo soft-delete operation. Use this to remove inappropriate, outdated, or mistakenly sent messages from a channel conversation without permanently destroying the content. This is the recommended deletion approach as it is reversible.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.soft_delete_channel_message", owner_id, args, account_id=account_id
        )

    async def soft_delete_chat_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSoftDeleteChatMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Soft-deletes a message in a chat (1:1 or group), hiding it from all participants while retaining the data for recovery. The message can be restored via the undo soft-delete operation. Use this to remove a mistakenly sent or inappropriate chat message without permanent destruction. This is the recommended deletion method as it is reversible.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.soft_delete_chat_message", owner_id, args, account_id=account_id
        )

    async def soft_delete_reply_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsSoftDeleteReplyMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Soft-deletes a reply message in a channel thread, hiding it from all channel members while retaining the data for recovery. The reply can be restored via the undo soft-delete operation. Use this to remove an inappropriate or mistaken reply within a threaded conversation without permanently destroying it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.soft_delete_reply_message", owner_id, args, account_id=account_id
        )

    async def unarchive_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUnarchiveTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously archived team to full read-write status, allowing members to send messages, create channels, and edit content again. The SharePoint site is also restored to its previous permission state. Use this to reactivate a team for a new phase of work or to correct an accidental archive. This is the reversal of the archive action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.unarchive_team", owner_id, args, account_id=account_id
        )

    async def undo_soft_delete_channel_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUndoSoftDeleteChannelMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously soft-deleted channel message, making it visible again to all channel members in the Teams UI. The message reappears in its original position in the conversation thread with all original content intact. Use this to recover a message that was accidentally deleted or to reverse a moderation action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.undo_soft_delete_channel_message",
            owner_id,
            args,
            account_id=account_id,
        )

    async def undo_soft_delete_chat_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUndoSoftDeleteChatMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously soft-deleted chat message, making it visible again to all chat participants with its original content and position in the conversation. Use this to recover a message that was accidentally deleted in a 1:1 or group chat, or to reverse a moderation action.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.undo_soft_delete_chat_message", owner_id, args, account_id=account_id
        )

    async def undo_soft_delete_reply_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUndoSoftDeleteReplyMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously soft-deleted reply in a channel thread, making it visible again to all channel members with its original content and thread position. Use this to recover a reply that was accidentally deleted or to reverse a moderation action on a threaded conversation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.undo_soft_delete_reply_message", owner_id, args, account_id=account_id
        )

    async def unset_channel_message_reaction(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUnsetChannelMessageReactionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Removes a previously added emoji reaction from a channel message. Use this to correct a reaction that was added in error, change your reaction, or when the context of the message has changed. Only removes the reaction from the authenticated user; other users' reactions are unaffected.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.unset_channel_message_reaction", owner_id, args, account_id=account_id
        )

    async def unset_chat_message_reaction(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUnsetChatMessageReactionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Removes a previously added emoji reaction from a chat message. Use this to undo a reaction added by mistake or to change your reaction on a direct or group chat message. Only affects the authenticated user's reaction; other participants' reactions remain unchanged.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.unset_chat_message_reaction", owner_id, args, account_id=account_id
        )

    async def update_channel(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUpdateChannelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies a channel's display name and/or description. Changes are visible to all team members immediately and the channel retains its message history and membership. Use this to rename a channel to reflect its current purpose or update its description with relevant context. Reversible by updating back to the original values.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.update_channel", owner_id, args, account_id=account_id
        )

    async def update_channel_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUpdateChannelMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Edits the content of an existing channel message, replacing its body with new text or HTML. The update timestamp changes but the original author and creation time are preserved. Use this to correct a mistake, update information in a message, or redact content. Teams shows an 'edited' indicator on the message. Reversible by updating back to the original content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.update_channel_message", owner_id, args, account_id=account_id
        )

    async def update_chat_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUpdateChatMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Edits the content of an existing chat message (1:1 or group), replacing its body. The message retains its original author and creation time but shows an edited indicator. Use this to correct errors, update links or information, or redact sensitive content in a private chat. Reversible by updating back to the original content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.update_chat_message", owner_id, args, account_id=account_id
        )

    async def update_team(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUpdateTeamRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies a team's display name, description, or visibility (public/private). Changes are reflected across all team members' Teams clients and in search results. The team's underlying Microsoft 365 group is also updated. Use this to rename a team, update its purpose, or change its discoverability. Reversible by issuing another update.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.update_team", owner_id, args, account_id=account_id
        )

    async def update_team_member(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MicrosoftTeamsUpdateTeamMemberRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Changes a team member's role between basic member, owner, or guest. Promoting a user to owner grants them full administrative control (add/remove members, delete channels, edit team settings). Demoting removes those privileges. Use this to manage team governance, delegate ownership, or adjust guest access. Reversible by issuing another role update.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "microsoft_teams.update_team_member", owner_id, args, account_id=account_id
        )
