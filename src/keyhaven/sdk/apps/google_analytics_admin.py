# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `google_analytics_admin` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GoogleAnalyticsAdminAccountsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int = 50
    pageToken: str | None = None
    showDeleted: bool = False


class GoogleAnalyticsAdminAccountsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleAnalyticsAdminAccountsListQuery | None = None


class GoogleAnalyticsAdminCustomDimensionsCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str


class GoogleAnalyticsAdminCustomDimensionsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parameterName: str
    displayName: str
    description: str | None = None
    scope: Literal["DIMENSION_SCOPE_UNSPECIFIED", "EVENT", "USER", "ITEM"]
    disallowAdsPersonalization: bool | None = None


class GoogleAnalyticsAdminCustomDimensionsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleAnalyticsAdminCustomDimensionsCreatePath
    body: GoogleAnalyticsAdminCustomDimensionsCreateBody


class GoogleAnalyticsAdminDataStreamsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str


class GoogleAnalyticsAdminDataStreamsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int = 50
    pageToken: str | None = None


class GoogleAnalyticsAdminDataStreamsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleAnalyticsAdminDataStreamsListPath
    query: GoogleAnalyticsAdminDataStreamsListQuery | None = None


class GoogleAnalyticsAdminGoogleAdsLinksListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str


class GoogleAnalyticsAdminGoogleAdsLinksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int = 50
    pageToken: str | None = None


class GoogleAnalyticsAdminGoogleAdsLinksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleAnalyticsAdminGoogleAdsLinksListPath
    query: GoogleAnalyticsAdminGoogleAdsLinksListQuery | None = None


class GoogleAnalyticsAdminPropertiesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str
    pageSize: int = 50
    pageToken: str | None = None
    showDeleted: bool = False


class GoogleAnalyticsAdminPropertiesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleAnalyticsAdminPropertiesListQuery


class GoogleAnalyticsAdminNamespace:
    """Typed accessor for the `google_analytics_admin` app. Reach it via `client.apps.google_analytics_admin`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def accounts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleAnalyticsAdminAccountsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all Google Analytics accounts accessible by the authenticated user, including each account's name, ID, display name, and region. Optionally includes soft-deleted accounts when showDeleted is true. Use this to discover account IDs and names before exploring properties and data streams. Supports pagination for accounts with many entries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_analytics_admin.accounts_list", owner_id, args, account_id=account_id
        )

    async def custom_dimensions_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleAnalyticsAdminCustomDimensionsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new custom dimension in a GA4 property to track additional user, event, or item-level data beyond the default dimensions. Requires defining the parameter name, display name, and scope (event, user, or item). This modifies the property's data schema and enables richer reporting and analysis. Once created, the parameter name and scope are immutable, so plan carefully. Use this when you need to extend Google Analytics tracking for business-specific metrics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_analytics_admin.custom_dimensions_create", owner_id, args, account_id=account_id
        )

    async def data_streams_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleAnalyticsAdminDataStreamsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all data streams (web, iOS, or Android) configured under a specific GA4 property. Each stream includes its measurement ID, Firebase app ID, and stream-specific settings. Use this to inspect which tracking integrations are active for a property and to get stream IDs needed for other operations. Requires the property resource name in the format properties/1234.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_analytics_admin.data_streams_list", owner_id, args, account_id=account_id
        )

    async def google_ads_links_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleAnalyticsAdminGoogleAdsLinksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all Google Ads link configurations associated with a specific GA4 property. Each link connects the property to a Google Ads account for advertising measurement, audience sharing, and conversion tracking. Use this to inspect existing ad integrations before modifying or adding new links. Requires the parent property resource name in the format properties/1234.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_analytics_admin.google_ads_links_list", owner_id, args, account_id=account_id
        )

    async def properties_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleAnalyticsAdminPropertiesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all Google Analytics 4 properties matching a given filter expression (typically an account resource name like 'accounts/123'). Optionally includes deleted properties. Use this to discover property IDs, their names, currency, timezone, and current status before accessing data streams or creating custom dimensions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_analytics_admin.properties_list", owner_id, args, account_id=account_id
        )
