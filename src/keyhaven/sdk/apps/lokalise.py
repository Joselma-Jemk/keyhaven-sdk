# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `lokalise` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class LokaliseFileUploadPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class LokaliseFileUploadBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: str
    filename: str | None = None
    lang_iso: str | None = None
    replace_modified: bool | None = None
    convert_placeholders: bool | None = None
    tags: list[Any] | None = None


class LokaliseFileUploadRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LokaliseFileUploadPath
    body: LokaliseFileUploadBody


class LokaliseKeysListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class LokaliseKeysListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    page: int | None = None
    filter_key_name: str | None = None
    filter_tags: str | None = None


class LokaliseKeysListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LokaliseKeysListPath
    query: LokaliseKeysListQuery | None = None


class LokaliseKeysUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class LokaliseKeysUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    keys: list[Any]
    use_automations: bool | None = None


class LokaliseKeysUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LokaliseKeysUpdatePath
    body: LokaliseKeysUpdateBody


class LokaliseProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    page: int | None = None


class LokaliseProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LokaliseProjectsListQuery | None = None


class LokaliseTranslationsExportPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class LokaliseTranslationsExportBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    format: str
    filter: str | None = None
    filter_filenames: list[Any] | None = None
    include_tags: list[Any] | None = None
    exclude_tags: list[Any] | None = None
    original_filenames: bool | None = None


class LokaliseTranslationsExportRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: LokaliseTranslationsExportPath
    body: LokaliseTranslationsExportBody


class LokaliseNamespace:
    """Typed accessor for the `lokalise` app. Reach it via `client.apps.lokalise`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def file_upload(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LokaliseFileUploadRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Uploads a translation file (base64-encoded) to a Lokalise project, creating or updating keys and translations based on the file content. Options control whether modified translations are overwritten, whether placeholders are auto-converted, and which tags to apply to imported keys. Use this to batch-import translations from external sources or to sync files from a code repository into Lokalise.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lokalise.file_upload", owner_id, args, account_id=account_id
        )

    async def keys_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LokaliseKeysListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves translation keys within a specific Lokalise project, with optional filtering by key name or tags and pagination support. Each key object contains its identifier, base name, associated tags, and available translations across languages. Use this to inspect existing translation keys before updating them or to find specific keys by name or tag.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lokalise.keys_list", owner_id, args, account_id=account_id
        )

    async def keys_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LokaliseKeysUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more existing translation keys in a Lokalise project, including their translations and metadata, with an option to trigger automations on modified translations. This directly modifies the translation content that gets exported to production applications. Use this to correct translations, propagate reviewed content, or batch-update key properties across multiple keys at once.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lokalise.keys_update", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LokaliseProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Lokalise projects accessible to the authenticated user, with pagination support via limit and page parameters. Each project entry includes its ID, name, and metadata such as language configuration and team membership. Use this to discover available projects before performing project-specific operations like key management or file uploads.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lokalise.projects_list", owner_id, args, account_id=account_id
        )

    async def translations_export(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LokaliseTranslationsExportRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Exports translations from a Lokalise project into a file of the specified format (JSON, XML, CSV, etc.), with optional filtering by translation status, filenames, or tags. This generates a downloadable file representing the current state of translations in the project. Use this to retrieve translations for deployment to production, back up project content, or share with external translators.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "lokalise.translations_export", owner_id, args, account_id=account_id
        )
