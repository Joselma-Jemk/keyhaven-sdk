# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `klaviyo` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class KlaviyoCampaignListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = None
    include: str | None = None
    fields_campaign_: str | None = Field(default=None, alias="fields[campaign]")
    page_cursor_: str | None = Field(default=None, alias="page[cursor]")
    page_size_: int | None = Field(default=None, alias="page[size]")


class KlaviyoCampaignListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    revision: str


class KlaviyoCampaignListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KlaviyoCampaignListQuery = Field(default_factory=KlaviyoCampaignListQuery)
    header: KlaviyoCampaignListHeader


class KlaviyoEventCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    revision: str


class KlaviyoEventCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(alias="type")
    attributes: dict[str, Any]


class KlaviyoEventCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: KlaviyoEventCreateHeader
    body: KlaviyoEventCreateBody


class KlaviyoEventListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = None
    include: str | None = None
    fields_event_: str | None = Field(default=None, alias="fields[event]")
    fields_profile_: str | None = Field(default=None, alias="fields[profile]")
    fields_metric_: str | None = Field(default=None, alias="fields[metric]")
    page_cursor_: str | None = Field(default=None, alias="page[cursor]")
    page_size_: int | None = Field(default=None, alias="page[size]")
    sort: str | None = None


class KlaviyoEventListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    revision: str


class KlaviyoEventListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KlaviyoEventListQuery = Field(default_factory=KlaviyoEventListQuery)
    header: KlaviyoEventListHeader


class KlaviyoListCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    revision: str


class KlaviyoListCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(alias="type")
    attributes: dict[str, Any]


class KlaviyoListCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: KlaviyoListCreateHeader
    body: KlaviyoListCreateBody


class KlaviyoProfileCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    revision: str


class KlaviyoProfileCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(alias="type")
    attributes: dict[str, Any]


class KlaviyoProfileCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: KlaviyoProfileCreateHeader
    body: KlaviyoProfileCreateBody


class KlaviyoProfileListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = None
    fields_profile_: str | None = Field(default=None, alias="fields[profile]")
    additional_fields_profile_: str | None = Field(default=None, alias="additional-fields[profile]")
    page_cursor_: str | None = Field(default=None, alias="page[cursor]")
    page_size_: int | None = Field(default=None, alias="page[size]")
    include: str | None = None


class KlaviyoProfileListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    revision: str


class KlaviyoProfileListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: KlaviyoProfileListQuery = Field(default_factory=KlaviyoProfileListQuery)
    header: KlaviyoProfileListHeader


class KlaviyoNamespace:
    """Typed accessor for the `klaviyo` app. Reach it via `client.apps.klaviyo`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def campaign_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KlaviyoCampaignListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists marketing campaigns in the Klaviyo account, filterable by channel (email, SMS), status (draft, sent, scheduled), and send time. Use this to review campaign performance, check send status, or find a campaign ID for further analysis. Read-only — no campaigns are created, modified, or sent.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "klaviyo.campaign_list", owner_id, args, account_id=account_id
        )

    async def event_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KlaviyoEventCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Records a custom event (e.g., 'Placed Order', 'Viewed Product') and links it to a customer profile for analytics and flow triggering. The event is ingested asynchronously and can power segmentation, campaign triggers, and real-time reporting. This is write-only data ingestion — it cannot be undone, but events are internal tracking data that do not directly contact customers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "klaviyo.event_create", owner_id, args, account_id=account_id
        )

    async def event_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KlaviyoEventListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches recorded customer events with filters for metric type, profile, and time range. Use this to audit event data, debug flow triggers, or analyze customer behavior over time. The operation is read-only and does not modify any data in Klaviyo.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "klaviyo.event_list", owner_id, args, account_id=account_id
        )

    async def list_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KlaviyoListCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new subscriber list (segment) in Klaviyo that can be used to group profiles for targeted campaigns and flows. The list is immediately available for profile assignment and campaign targeting. This is an internal organizational structure — it does not directly contact customers but enables future marketing actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "klaviyo.list_create", owner_id, args, account_id=account_id
        )

    async def profile_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KlaviyoProfileCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer profile or updates an existing one (upsert based on email or phone) with name, location, and custom properties. The profile becomes part of your Klaviyo audience and can be added to lists, receive campaigns, and trigger flows. This modifies the customer database but is recoverable by editing or deleting the profile.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "klaviyo.profile_create", owner_id, args, account_id=account_id
        )

    async def profile_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: KlaviyoProfileListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists customer profiles in the Klaviyo account with cursor-based pagination and filtering by email, phone, or external ID. Use this to search for existing profiles before creating campaigns, tracking events, or building segments. The operation is read-only — no profiles are created or modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "klaviyo.profile_list", owner_id, args, account_id=account_id
        )
