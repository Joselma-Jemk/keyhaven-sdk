# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `stackexchange` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class StackexchangeBadgesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: str


class StackexchangeBadgesGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order: str | None = None
    max: str | None = None
    min: str | None = None
    sort: str | None = None
    fromdate: int | None = None
    todate: int | None = None
    pagesize: int | None = None
    page: int | None = None
    filter: str | None = None
    callback: str | None = None
    site: str


class StackexchangeBadgesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StackexchangeBadgesGetPath
    query: StackexchangeBadgesGetQuery


class StackexchangeBadgesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    inname: str | None = None
    order: str | None = None
    max: str | None = None
    min: str | None = None
    sort: str | None = None
    fromdate: int | None = None
    todate: int | None = None
    pagesize: int | None = None
    page: int | None = None
    filter: str | None = None
    callback: str | None = None
    site: str


class StackexchangeBadgesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StackexchangeBadgesListQuery


class StackexchangeMeCommentsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    toId: int


class StackexchangeMeCommentsGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order: str | None = None
    max: str | None = None
    min: str | None = None
    sort: str | None = None
    fromdate: int | None = None
    todate: int | None = None
    pagesize: int | None = None
    page: int | None = None
    filter: str | None = None
    callback: str | None = None
    site: str


class StackexchangeMeCommentsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StackexchangeMeCommentsGetPath
    query: StackexchangeMeCommentsGetQuery


class StackexchangeMeListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order: str | None = None
    max: str | None = None
    min: str | None = None
    sort: str | None = None
    fromdate: int | None = None
    todate: int | None = None
    pagesize: int | None = None
    page: int | None = None
    filter: str | None = None
    callback: str | None = None
    site: str


class StackexchangeMeListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StackexchangeMeListQuery


class StackexchangePostsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order: str | None = None
    max: str | None = None
    min: str | None = None
    sort: str | None = None
    fromdate: int | None = None
    todate: int | None = None
    pagesize: int | None = None
    page: int | None = None
    filter: str | None = None
    callback: str | None = None
    site: str


class StackexchangePostsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StackexchangePostsListQuery


class StackexchangeNamespace:
    """Typed accessor for the `stackexchange` app. Reach it via `client.apps.stackexchange`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def badges_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StackexchangeBadgesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve details for one or more specific badges by their IDs on the specified Stack Exchange site, including badge type, description, and award count. This is a read-only operation that returns badge metadata without modifying any resources. Use this function to get precise information about known badges for display, comparison, or analysis of community achievements.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stackexchange.badges_get", owner_id, args, account_id=account_id
        )

    async def badges_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StackexchangeBadgesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of all badges available across the specified Stack Exchange site, optionally filtered by name substring and sorted by various criteria. This is a read-only operation that returns badge names, descriptions, badge types (tag-based, named), and award counts without making any changes. Use this function to explore the badge system of a site, understand achievement criteria, or research community gamification mechanics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stackexchange.badges_list", owner_id, args, account_id=account_id
        )

    async def me_comments_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StackexchangeMeCommentsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve comments made by the authenticated user to a specific target user identified by toId, across a specified Stack Exchange site. This is a read-only operation that returns comment bodies, scores, creation dates, and post associations without altering any data. Use this function to review past interactions between users, audit comment history, or analyze communication patterns on a given site.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stackexchange.me_comments_get", owner_id, args, account_id=account_id
        )

    async def me_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StackexchangeMeListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the Stack Exchange network profile of the currently authenticated user, including reputation, badges, account IDs, and creation dates. This is a read-only operation that returns personal account data without modifying any content. Use this function when an agent needs to verify the authenticated user's identity, check their reputation across sites, or gather profile statistics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stackexchange.me_list", owner_id, args, account_id=account_id
        )

    async def posts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StackexchangePostsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of posts (questions and answers) across the specified Stack Exchange site, with optional date range filtering, sorting by activity or votes, and custom data filters. This is a read-only operation that returns post bodies, scores, view counts, and author information without any side effects. Use this function to discover top content, analyze site activity patterns, or feed data into analytical workflows about community knowledge.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stackexchange.posts_list", owner_id, args, account_id=account_id
        )
