# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `etherscan` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class EtherscanGetAccountBalanceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainid: int = 1
    module: str = "account"
    action: str = "balance"
    address: str
    tag: str = "latest"


class EtherscanGetAccountBalanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EtherscanGetAccountBalanceQuery


class EtherscanGetContractSourceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainid: int = 1
    module: str = "contract"
    action: str = "getsourcecode"
    address: str


class EtherscanGetContractSourceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EtherscanGetContractSourceQuery


class EtherscanGetGasPriceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainid: int = 1
    module: str = "gastracker"
    action: str = "gasoracle"


class EtherscanGetGasPriceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EtherscanGetGasPriceQuery = Field(default_factory=EtherscanGetGasPriceQuery)


class EtherscanGetTokenBalanceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainid: int = 1
    module: str = "account"
    action: str = "tokenbalance"
    contractaddress: str
    address: str
    tag: str = "latest"


class EtherscanGetTokenBalanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EtherscanGetTokenBalanceQuery


class EtherscanGetTransactionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    chainid: int = 1
    module: str = "account"
    action: str = "txlist"
    address: str
    startblock: int = 0
    endblock: int = 99999999
    page: int = 1
    offset: int = 10
    sort: Literal["asc", "desc"] = "desc"


class EtherscanGetTransactionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EtherscanGetTransactionsQuery


class EtherscanNamespace:
    """Typed accessor for the `etherscan` app. Reach it via `client.apps.etherscan`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_account_balance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtherscanGetAccountBalanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the native Ether balance of a specified wallet address on any supported EVM chain (Ethereum, Arbitrum, Optimism, etc.). The balance is returned in Wei. Use this when an agent needs to check an address's ETH holdings for monitoring, financial reporting, or before initiating a transaction — this exposes sensitive financial data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etherscan.get_account_balance", owner_id, args, account_id=account_id
        )

    async def get_contract_source(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtherscanGetContractSourceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the Solidity source code, ABI, compiler version, and constructor arguments of a verified smart contract on any supported EVM chain. Returns the full source files and compilation metadata. Use this when an agent needs to audit a contract, understand its logic, or verify its behaviour — this is public data for verified contracts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etherscan.get_contract_source", owner_id, args, account_id=account_id
        )

    async def get_gas_price(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtherscanGetGasPriceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve current gas price estimates (SafeGasPrice, ProposeGasPrice, FastGasPrice) and base fee from the Ethereum network's gas oracle. Returns real-time fee data in Gwei. Use this when an agent needs to estimate transaction costs or compare gas prices across chains — this is public network data, not user-sensitive.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etherscan.get_gas_price", owner_id, args, account_id=account_id
        )

    async def get_token_balance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtherscanGetTokenBalanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the ERC-20 token balance of a specified wallet address for a specific token contract on any supported EVM chain. Returns the raw balance in the smallest token unit. Use this when an agent needs to check holdings of a specific token across chains — this exposes sensitive financial portfolio data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etherscan.get_token_balance", owner_id, args, account_id=account_id
        )

    async def get_transactions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EtherscanGetTransactionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the transaction history for a wallet address across multiple EVM chains, with pagination (page/offset) and sort ordering (asc/desc). Returns each transaction's hash, block, from/to addresses, value, gas used, and timestamp. Use this when an agent needs to audit or analyse an address's on-chain activity — this exposes sensitive financial history.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "etherscan.get_transactions", owner_id, args, account_id=account_id
        )
