# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `apaleo` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ApaleoCreateBookingHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ApaleoCreateBookingBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    paymentAccount: dict[str, Any] | None = None
    booker: dict[str, Any] | None = None
    reservations: list[Any]
    transactionReference: str | None = None


class ApaleoCreateBookingRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ApaleoCreateBookingHeader = Field(default_factory=ApaleoCreateBookingHeader)
    body: ApaleoCreateBookingBody


class ApaleoGetPropertiesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str | None = None
    includeArchived: bool | None = None
    countryCode: str | None = None
    pageNumber: int | None = None
    expand: str | None = None


class ApaleoGetPropertiesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ApaleoGetPropertiesQuery | None = None


class ApaleoGetRatePlansQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageNumber: int | None = None


class ApaleoGetRatePlansRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ApaleoGetRatePlansQuery | None = None


class ApaleoGetReservationsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageNumber: int | None = None


class ApaleoGetReservationsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ApaleoGetReservationsQuery | None = None


class ApaleoGetUnitsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageNumber: int | None = None


class ApaleoGetUnitsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ApaleoGetUnitsQuery | None = None


class ApaleoNamespace:
    """Typed accessor for the `apaleo` app. Reach it via `client.apps.apaleo`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_booking(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApaleoCreateBookingRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new booking in Apaleo with one or more reservations, including guest details, payment account information, rate plan selections, optional services, and pre-payment amounts. The booking is immediately visible in the property management system, blocks inventory, and triggers any connected channel or notification workflows. Use this when a new guest reservation needs to be recorded and confirmed in the system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "apaleo.create_booking", owner_id, args, account_id=account_id
        )

    async def get_properties(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApaleoGetPropertiesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all properties (hotels, apartments) managed under the Apaleo account, with optional filters for status, country, archived state, and pagination. Results include property names, statuses, locations, and identifiers. Use this to discover available properties before creating bookings or managing inventory.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "apaleo.get_properties", owner_id, args, account_id=account_id
        )

    async def get_rate_plans(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApaleoGetRatePlansRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all rate plans configured across the account, with pagination support. Rate plans define pricing rules, restrictions, availability, and seasonality for different property types and room categories. Use this to discover available rate plan IDs and pricing details needed when creating or modifying bookings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "apaleo.get_rate_plans", owner_id, args, account_id=account_id
        )

    async def get_reservations(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApaleoGetReservationsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all reservations across properties with pagination support. Results include guest personal details (names, contact information), arrival and departure dates, booking status, assigned unit, and linked booking reference. Use this to look up existing reservations, verify guest stays, audit booking data, or find a reservation to modify or cancel.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "apaleo.get_reservations", owner_id, args, account_id=account_id
        )

    async def get_units(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApaleoGetUnitsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all units (individual rooms, apartments, or bookable spaces) across properties, with pagination support. Each unit includes its type, capacity, status, amenities, and assigned property. Use this to check unit availability, verify room specifications, and identify specific unit IDs before creating reservations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("apaleo.get_units", owner_id, args, account_id=account_id)
