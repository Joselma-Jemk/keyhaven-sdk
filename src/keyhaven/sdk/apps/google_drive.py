# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `google_drive` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GoogleDriveFilesCopyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fileId: str


class GoogleDriveFilesCopyQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    supportsAllDrives: bool = False


class GoogleDriveFilesCopyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    parents: list[Any] | None = None


class GoogleDriveFilesCopyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleDriveFilesCopyPath
    query: GoogleDriveFilesCopyQuery | None = None
    body: GoogleDriveFilesCopyBody | None = None


class GoogleDriveFilesCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    supportsAllDrives: bool = False


class GoogleDriveFilesCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    mimeType: str | None = None
    parents: list[Any] | None = None
    description: str | None = None


class GoogleDriveFilesCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleDriveFilesCreateQuery | None = None
    body: GoogleDriveFilesCreateBody | None = None


class GoogleDriveFilesDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fileId: str


class GoogleDriveFilesDeleteQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    supportsAllDrives: bool = False


class GoogleDriveFilesDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleDriveFilesDeletePath
    query: GoogleDriveFilesDeleteQuery | None = None


class GoogleDriveFilesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fileId: str


class GoogleDriveFilesGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    supportsAllDrives: bool = False
    acknowledgeAbuse: bool = False


class GoogleDriveFilesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleDriveFilesGetPath
    query: GoogleDriveFilesGetQuery | None = None


class GoogleDriveFilesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    q: str | None = None
    pageSize: int = 100
    pageToken: str | None = None
    fields: str | None = None
    orderBy: str | None = None
    spaces: str = "drive"
    corpora: str = "user"
    includeItemsFromAllDrives: bool = False
    supportsAllDrives: bool = False


class GoogleDriveFilesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleDriveFilesListQuery | None = None


class GoogleDriveFilesUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fileId: str


class GoogleDriveFilesUpdateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    supportsAllDrives: bool = False


class GoogleDriveFilesUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    mimeType: str | None = None
    description: str | None = None
    trashed: bool | None = None


class GoogleDriveFilesUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleDriveFilesUpdatePath
    query: GoogleDriveFilesUpdateQuery | None = None
    body: GoogleDriveFilesUpdateBody | None = None


class GoogleDrivePermissionsCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fileId: str


class GoogleDrivePermissionsCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sendNotificationEmail: bool = False
    supportsAllDrives: bool = False
    fields: str | None = None


class GoogleDrivePermissionsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    role: str
    type_: str = Field(alias="type")
    emailAddress: str | None = None
    domain: str | None = None


class GoogleDrivePermissionsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleDrivePermissionsCreatePath
    query: GoogleDrivePermissionsCreateQuery | None = None
    body: GoogleDrivePermissionsCreateBody


class GoogleDrivePermissionsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fileId: str


class GoogleDrivePermissionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    pageSize: int = 100
    pageToken: str | None = None
    supportsAllDrives: bool = False


class GoogleDrivePermissionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleDrivePermissionsListPath
    query: GoogleDrivePermissionsListQuery | None = None


class GoogleDriveNamespace:
    """Typed accessor for the `google_drive` app. Reach it via `client.apps.google_drive`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def files_copy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDriveFilesCopyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a duplicate of an existing file in Google Drive, optionally with a new name and a different parent folder. The original file remains unchanged. Use this when an agent needs to duplicate a template, create a backup, or make a modified copy for editing. This action is reversible by deleting the copy.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.files_copy", owner_id, args, account_id=account_id
        )

    async def files_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDriveFilesCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new file or folder in Google Drive with the specified name, MIME type, parent folder, and description. The new item is immediately visible in the Drive UI and accessible to the creator. Use this when an agent needs to save a generated document, create a folder structure, or upload content. This action is reversible by moving the file to trash or deleting it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.files_create", owner_id, args, account_id=account_id
        )

    async def files_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDriveFilesDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a file or folder by its ID, bypassing the trash. The file and all its content are immediately and irreversibly removed from Google Drive. Use this only when the user explicitly confirms permanent deletion. There is no way to recover the file after this operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.files_delete", owner_id, args, account_id=account_id
        )

    async def files_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDriveFilesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the metadata of a specific file or folder by its ID, including name, MIME type, size, creation time, parent folders, and permissions. Optionally acknowledges the risk of downloading potentially abusive content. Use this when an agent needs detailed information about a known file. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.files_get", owner_id, args, account_id=account_id
        )

    async def files_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDriveFilesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists the files and folders in the user's Google Drive that match the specified query criteria, with pagination and field selection. The query supports the Google Drive query language for filtering by name, type, modified time, and other properties. Use this when an agent needs to search for files, browse a folder, or enumerate Drive contents. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.files_list", owner_id, args, account_id=account_id
        )

    async def files_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDriveFilesUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the metadata of an existing file or folder (name, description, MIME type) or moves it to the trash. Changes are immediately reflected in the Drive UI and affect all users with access. Use this when an agent needs to rename files, update descriptions, or clean up by trashing obsolete items. Trashed items can be restored, making this action reversible.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.files_update", owner_id, args, account_id=account_id
        )

    async def permissions_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDrivePermissionsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Grants a new permission on a file or folder, giving access to a user (by email), a group, an entire domain, or anyone with the link. The role can be reader, writer, commenter, or owner. Optionally sends a notification email to the grantee. Use this when an agent needs to share a file or folder with others. This action changes the file's access control and may expose sensitive content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.permissions_create", owner_id, args, account_id=account_id
        )

    async def permissions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleDrivePermissionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all permissions (access grants) on a specific file or folder, showing who has access and what role they have (reader, writer, commenter, owner). Use this when an agent needs to audit sharing settings or check who can access a file. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_drive.permissions_list", owner_id, args, account_id=account_id
        )
