# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `notion_scim` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class NotionScimBlocksChildrenListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class NotionScimBlocksChildrenListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_size: str | None = None


class NotionScimBlocksChildrenListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str | None = Field(default=None, alias="Notion-Version")


class NotionScimBlocksChildrenListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionScimBlocksChildrenListPath
    query: NotionScimBlocksChildrenListQuery | None = None
    header: NotionScimBlocksChildrenListHeader | None = None


class NotionScimBlocksGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class NotionScimBlocksGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str | None = Field(default=None, alias="Notion-Version")


class NotionScimBlocksGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionScimBlocksGetPath
    header: NotionScimBlocksGetHeader | None = None


class NotionScimCommentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    block_id: str | None = None
    page_size: str | None = None


class NotionScimCommentsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str | None = Field(default=None, alias="Notion-Version")


class NotionScimCommentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: NotionScimCommentsListQuery | None = None
    header: NotionScimCommentsListHeader | None = None


class NotionScimPagesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class NotionScimPagesGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str | None = Field(default=None, alias="Notion-Version")


class NotionScimPagesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionScimPagesGetPath
    header: NotionScimPagesGetHeader | None = None


class NotionScimPagesPropertiesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_id: str
    property_id: str


class NotionScimPagesPropertiesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionScimPagesPropertiesGetPath


class NotionScimNamespace:
    """Typed accessor for the `notion_scim` app. Reach it via `client.apps.notion_scim`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def blocks_children_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionScimBlocksChildrenListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the child blocks nested under a parent block in a Notion page, with optional page_size control. Child blocks can include paragraphs, headings, lists, images, databases, and other content types that make up the page structure. Use this to traverse the block hierarchy of a page, read its full content, or discover child blocks before performing other operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion_scim.blocks_children_list", owner_id, args, account_id=account_id
        )

    async def blocks_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionScimBlocksGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single block object from Notion by its block ID, returning its type, properties, and metadata such as creation time and last edited time. Blocks are the fundamental content units in Notion (paragraphs, headings, to-do items, etc.). Use this to inspect a specific block's content and configuration, or to verify a block exists before modifying it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion_scim.blocks_get", owner_id, args, account_id=account_id
        )

    async def comments_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionScimCommentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves comments from a Notion page or block, with optional pagination via page_size. Comments are threaded discussions attached to specific blocks or pages, including their content, author, and creation timestamp. Use this to review discussion history, read feedback left by collaborators, or gather context before responding to or resolving comments.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion_scim.comments_list", owner_id, args, account_id=account_id
        )

    async def pages_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionScimPagesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a Notion page object including its properties (title, status, date, select, etc.) and metadata such as the parent container, icon, and cover image. Pages are top-level containers in Notion that can hold blocks, databases, and other content. Use this to read a page's property values, verify its existence, or gather context before performing page-level operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion_scim.pages_get", owner_id, args, account_id=account_id
        )

    async def pages_properties_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionScimPagesPropertiesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the value of a specific property item on a Notion page, identified by page ID and property ID. Properties are the structured fields on a page (text, number, date, select, relation, formula, etc.). Use this to read individual property values without loading the entire page, which is useful for fetching specific data points from database entries or forms.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion_scim.pages_properties_get", owner_id, args, account_id=account_id
        )
