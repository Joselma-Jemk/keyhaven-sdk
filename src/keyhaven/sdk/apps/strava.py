# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `strava` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class StravaActivitiesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    before: int | None = None
    after: int | None = None
    page: int | None = None
    per_page: int | None = None


class StravaActivitiesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StravaActivitiesListQuery | None = None


class StravaActivityCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    sport_type: str
    start_date_local: str
    elapsed_time: int
    type_: str | None = Field(default=None, alias="type")
    description: str | None = None
    distance: float | None = None
    trainer: int | None = None
    commute: int | None = None


class StravaActivityCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: StravaActivityCreateBody


class StravaActivityGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class StravaActivityGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_all_efforts: bool | None = None


class StravaActivityGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StravaActivityGetPath
    query: StravaActivityGetQuery | None = None


class StravaActivityUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class StravaActivityUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    type_: str | None = Field(default=None, alias="type")
    sport_type: str | None = None
    description: str | None = None
    gear_id: str | None = None
    trainer: int | None = None
    commute: int | None = None
    hide_from_home: bool | None = None


class StravaActivityUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StravaActivityUpdatePath
    body: StravaActivityUpdateBody | None = None


class StravaAthleteGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class StravaNamespace:
    """Typed accessor for the `strava` app. Reach it via `client.apps.strava`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def activities_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StravaActivitiesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of the authenticated athlete's past activities (runs, rides, swims, etc.), optionally filtered by time range with before/after timestamps. This is a read-only operation that returns summaries including distance, duration, elevation, and sport type without modifying any data. Use this function to browse your fitness history, fetch recent workout data, or analyze training patterns over a specific period.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "strava.activities_list", owner_id, args, account_id=account_id
        )

    async def activity_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StravaActivityCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new manually-entered activity (run, ride, swim, etc.) in the authenticated athlete's Strava profile with required name, sport type, start time, and duration. The new activity will appear in the athlete's feed, count toward weekly and monthly totals, and be visible to followers depending on privacy settings. Use this function when you need to log a workout that wasn't recorded by a GPS device, such as a treadmill run or an activity imported from another platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "strava.activity_create", owner_id, args, account_id=account_id
        )

    async def activity_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StravaActivityGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve full details of a specific activity by its ID, including distance, duration, elevation, pace, splits, and optionally all segment efforts. This is a read-only operation that returns comprehensive activity data without making any changes. Use this function when you need in-depth analysis of a single workout, to display a detailed activity view, or to extract segment performance data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "strava.activity_get", owner_id, args, account_id=account_id
        )

    async def activity_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StravaActivityUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modify editable fields of an existing activity owned by the authenticated athlete, such as name, description, sport type, gear, or privacy settings like hiding from the home feed. Changes take effect immediately and are visible to the athlete and their followers based on sharing preferences, and can be reverted by updating again. Use this function to correct activity metadata, update gear attribution, or adjust privacy visibility after a workout has been saved.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "strava.activity_update", owner_id, args, account_id=account_id
        )

    async def athlete_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StravaAthleteGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full profile of the currently authenticated Strava athlete, including name, location, profile pictures, follower counts, and athlete stats like maximum heart rate and weight. This is a read-only operation that returns personal profile data without modifying anything. Use this function when an agent needs the athlete's basic information for display, verification, or as context before performing Strava operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "strava.athlete_get", owner_id, args, account_id=account_id
        )
