# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `algolia` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AlgoliaIndicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    hitsPerPage: int | None = None
    page: int | None = None


class AlgoliaIndicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AlgoliaIndicesListQuery | None = None


class AlgoliaObjectDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    indexName: str
    objectID: str


class AlgoliaObjectDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AlgoliaObjectDeletePath


class AlgoliaObjectsBatchPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    indexName: str


class AlgoliaObjectsBatchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    requests: list[Any]


class AlgoliaObjectsBatchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AlgoliaObjectsBatchPath
    body: AlgoliaObjectsBatchBody


class AlgoliaSearchPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    indexName: str


class AlgoliaSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    params: str


class AlgoliaSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AlgoliaSearchPath
    body: AlgoliaSearchBody


class AlgoliaSettingsUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    indexName: str


class AlgoliaSettingsUpdateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    forwardToReplicas: bool | None = None


class AlgoliaSettingsUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    searchableAttributes: list[Any] | None = None
    attributesForFaceting: list[Any] | None = None
    customRanking: list[Any] | None = None
    ranking: list[Any] | None = None
    hitsPerPage: int | None = None
    typoTolerance: str | None = None
    distinct: int | None = None


class AlgoliaSettingsUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AlgoliaSettingsUpdatePath
    query: AlgoliaSettingsUpdateQuery | None = None
    body: AlgoliaSettingsUpdateBody | None = None


class AlgoliaNamespace:
    """Typed accessor for the `algolia` app. Reach it via `client.apps.algolia`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def indices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AlgoliaIndicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the complete list of Algolia indices for the application. Each index entry includes its name, creation date, record count, and disk size. This is a read-only operation with no side effects on data or configuration. Use it when an agent needs to discover available indices before searching or modifying them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "algolia.indices_list", owner_id, args, account_id=account_id
        )

    async def object_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AlgoliaObjectDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a single record from an Algolia index using its unique object ID. The deleted record immediately disappears from all search results across the application. This action is irreversible — there is no trash or undo mechanism within Algolia. Use it when an agent needs to remove an outdated, duplicate, or deleted-elsewhere record, but only after confirming the objectID is correct.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "algolia.object_delete", owner_id, args, account_id=account_id
        )

    async def objects_batch(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AlgoliaObjectsBatchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Performs a batch of write operations (add, update, delete, or clear) on objects in a specified Algolia index in a single request. Each operation in the batch is defined by an action type and a body. Results are immediately visible in search queries. Use it when an agent needs to index large datasets, bulk-update records, or clear an entire index, but note that operations like delete and clear are destructive and affect all application users relying on that index.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "algolia.objects_batch", owner_id, args, account_id=account_id
        )

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AlgoliaSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes a full-text search against a named Algolia index using query parameters passed as a URL-encoded string. Returns matching records with their relevance score, highlighting, and metadata. This is a read-only operation that does not create, modify, or delete any data. Use it when an agent needs to find records, test search relevance, or retrieve specific objects by query.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("algolia.search", owner_id, args, account_id=account_id)

    async def settings_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AlgoliaSettingsUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Replaces the configuration settings of a named Algolia index, controlling how records are ranked, searched, faceted, and displayed. Changing ranking, searchable attributes, or typo tolerance immediately alters search behaviour and results for all users querying that index. These changes are reversible by submitting previous settings again. Use it when an agent needs to tune search relevance, enable faceting on new attributes, or adjust pagination defaults.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "algolia.settings_update", owner_id, args, account_id=account_id
        )
