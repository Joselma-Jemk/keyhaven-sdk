# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `survey_monkey` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SurveyMonkeySurveyCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    nickname: str | None = None
    language: str | None = None
    folder_id: str | None = None
    category: str | None = None


class SurveyMonkeySurveyCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SurveyMonkeySurveyCreateBody


class SurveyMonkeySurveyGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    survey_id: str


class SurveyMonkeySurveyGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SurveyMonkeySurveyGetPath


class SurveyMonkeySurveyResponsesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    survey_id: str


class SurveyMonkeySurveyResponsesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    custom_variables: str | None = None


class SurveyMonkeySurveyResponsesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SurveyMonkeySurveyResponsesListPath
    query: SurveyMonkeySurveyResponsesListQuery | None = None


class SurveyMonkeySurveysListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None


class SurveyMonkeySurveysListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SurveyMonkeySurveysListQuery | None = None


class SurveyMonkeySurveysSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    page: int | None = None
    per_page: int | None = None
    sort_by: str | None = None
    sort_order: str | None = None
    start_date: str | None = None
    end_date: str | None = None
    date_type: str | None = None


class SurveyMonkeySurveysSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SurveyMonkeySurveysSearchQuery | None = None


class SurveyMonkeyNamespace:
    """Typed accessor for the `survey_monkey` app. Reach it via `client.apps.survey_monkey`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def survey_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SurveyMonkeySurveyCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new blank survey in the authenticated user's account. The survey is created without questions or pages — use the SurveyMonkey web UI or other API calls to add content later. Accepts optional fields like nickname, language, folder_id, and category. Use when you need to programmatically provision a new survey collection instrument. The survey is initially a private draft, not visible to respondents.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "survey_monkey.survey_create", owner_id, args, account_id=account_id
        )

    async def survey_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SurveyMonkeySurveyGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full metadata of a single survey by its unique identifier, including title, language, status (draft/open/closed), category, and folder assignment. Does not include the survey's questions or responses. Use when you need details about a specific survey before modifying it or retrieving its responses. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "survey_monkey.survey_get", owner_id, args, account_id=account_id
        )

    async def survey_responses_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SurveyMonkeySurveyResponsesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of responses collected for a given survey, including respondent answers, IP addresses, timestamps, and any collected custom variables. Each response reflects a completed or partially-completed submission from a real respondent. Use to export survey data for analysis, to verify data collection, or to feed results into downstream systems. Respondents' personal data may be included; access to this data is sensitive.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "survey_monkey.survey_responses_list", owner_id, args, account_id=account_id
        )

    async def surveys_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SurveyMonkeySurveysListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of all surveys owned by or shared with the authenticated user. Each survey object includes metadata such as title, nickname, language, response count, and date created/modified. Use this to discover available surveys before reading their details or responses. No side effects; no data is created, modified, or deleted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "survey_monkey.surveys_list", owner_id, args, account_id=account_id
        )

    async def surveys_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SurveyMonkeySurveysSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search for surveys belonging to the authenticated user using free-text and date-range filters. Supports matching against the survey title and nickname, sorting by date_modified or title, and filtering by creation/modification date window. Use when you need to find a specific survey by name or time period rather than paginating through all surveys. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "survey_monkey.surveys_search", owner_id, args, account_id=account_id
        )
