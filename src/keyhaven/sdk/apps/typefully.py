# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `typefully` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TypefullyCreateDraftHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TypefullyCreateDraftBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content: str
    threadify: bool | None = None
    share: bool | None = None
    schedule_date: str | None = Field(default=None, alias="schedule-date")
    auto_retweet_enabled: bool | None = None
    auto_plug_enabled: bool | None = None


class TypefullyCreateDraftRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: TypefullyCreateDraftHeader = Field(default_factory=TypefullyCreateDraftHeader)
    body: TypefullyCreateDraftBody


class TypefullyGetLatestNotificationsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    kind: Literal["inbox", "activity"] | None = None


class TypefullyGetLatestNotificationsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TypefullyGetLatestNotificationsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TypefullyGetLatestNotificationsQuery | None = None
    header: TypefullyGetLatestNotificationsHeader = Field(
        default_factory=TypefullyGetLatestNotificationsHeader
    )


class TypefullyGetRecentlyPublishedDraftsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content_filter: Literal["threads", "tweets"] | None = None


class TypefullyGetRecentlyPublishedDraftsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TypefullyGetRecentlyPublishedDraftsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TypefullyGetRecentlyPublishedDraftsQuery | None = None
    header: TypefullyGetRecentlyPublishedDraftsHeader = Field(
        default_factory=TypefullyGetRecentlyPublishedDraftsHeader
    )


class TypefullyGetRecentlyScheduledDraftsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content_filter: Literal["threads", "tweets"] | None = None


class TypefullyGetRecentlyScheduledDraftsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TypefullyGetRecentlyScheduledDraftsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TypefullyGetRecentlyScheduledDraftsQuery | None = None
    header: TypefullyGetRecentlyScheduledDraftsHeader = Field(
        default_factory=TypefullyGetRecentlyScheduledDraftsHeader
    )


class TypefullyMarkNotificationsReadHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TypefullyMarkNotificationsReadBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    kind: Literal["inbox", "activity"] | None = None
    username: str | None = None


class TypefullyMarkNotificationsReadRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: TypefullyMarkNotificationsReadHeader = Field(
        default_factory=TypefullyMarkNotificationsReadHeader
    )
    body: TypefullyMarkNotificationsReadBody | None = None


class TypefullyNamespace:
    """Typed accessor for the `typefully` app. Reach it via `client.apps.typefully`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_draft(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TypefullyCreateDraftRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new draft post on Typefully with the provided text content, optionally split into a thread using 4 consecutive newlines between tweets. Supports scheduling for future publication via an ISO date or 'next-free-slot', automatic threadification, and enabling Auto-Retweet or Auto-Plug features per the account's Typefully settings. Use this to compose and save tweets or threads for later review, scheduling, or immediate publication through the Typefully platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "typefully.create_draft", owner_id, args, account_id=account_id
        )

    async def get_latest_notifications(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TypefullyGetLatestNotificationsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the latest notifications from Typefully, optionally filtered by kind: inbox (comments, replies to your posts) or activity (tweet published, scheduled, or other account events). Notifications include the type, timestamp, and related draft or tweet information. Use this to check for engagement on published content or monitor account activity without opening the Typefully dashboard.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "typefully.get_latest_notifications", owner_id, args, account_id=account_id
        )

    async def get_recently_published_drafts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TypefullyGetRecentlyPublishedDraftsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of the most recently published drafts from Typefully, optionally filtered to show only tweets or only threads using the content_filter parameter. Each entry includes the published content, publish timestamp, and engagement metrics. Use this to review what has been posted recently, verify that scheduled content was published, or gather analytics on published content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "typefully.get_recently_published_drafts", owner_id, args, account_id=account_id
        )

    async def get_recently_scheduled_drafts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TypefullyGetRecentlyScheduledDraftsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of the most recently scheduled drafts from Typefully, optionally filtered to show only tweets or only threads using the content_filter parameter. Each entry includes the draft content, scheduled publication time, and scheduling status. Use this to review upcoming posts, verify that scheduling was applied correctly, or manage the content queue before publication.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "typefully.get_recently_scheduled_drafts", owner_id, args, account_id=account_id
        )

    async def mark_notifications_read(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TypefullyMarkNotificationsReadRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Marks all notifications on Typefully as read, optionally scoped to a specific account username and filtered by kind (inbox for comments and replies, or activity for publishing events). This is a UI state change that clears unread indicators on the Typefully dashboard but does not delete or permanently alter any data. Use this to reset notification badges after reviewing new activity.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "typefully.mark_notifications_read", owner_id, args, account_id=account_id
        )
