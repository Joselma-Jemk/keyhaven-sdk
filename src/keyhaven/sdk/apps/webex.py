# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `webex` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class WebexMessageCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    roomId: str | None = None
    parentId: str | None = None
    toPersonId: str | None = None
    toPersonEmail: str | None = None
    text: str | None = None
    markdown: str | None = None
    files: list[Any] | None = None
    attachments: list[Any] | None = None


class WebexMessageCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: WebexMessageCreateBody = Field(default_factory=WebexMessageCreateBody)


class WebexMessageGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    messageId: str


class WebexMessageGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WebexMessageGetPath


class WebexMessagesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    roomId: str
    parentId: str | None = None
    mentionedPeople: str | None = None
    before: str | None = None
    beforeMessage: str | None = None
    max: int = 50


class WebexMessagesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WebexMessagesListQuery


class WebexPeopleListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    displayName: str | None = None
    id: str | None = None
    orgId: str | None = None
    max: int = 100


class WebexPeopleListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WebexPeopleListQuery | None = None


class WebexRoomCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    teamId: str | None = None
    classificationId: str | None = None
    isLocked: bool | None = None
    isPublic: bool | None = None
    description: str | None = None


class WebexRoomCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: WebexRoomCreateBody


class WebexRoomsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    teamId: str | None = None
    type_: Literal["direct", "group"] | None = Field(default=None, alias="type")
    sortBy: Literal["id", "lastactivity", "created"] | None = None
    max: int = 100


class WebexRoomsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WebexRoomsListQuery | None = None


class WebexNamespace:
    """Typed accessor for the `webex` app. Reach it via `client.apps.webex`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def message_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WebexMessageCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Posts a new message to a Webex room, direct 1:1 conversation, or thread reply. Supports plain text, markdown (which overrides text), file attachments via public URLs, and adaptive card attachments. The message is delivered in real-time to all room members or the direct recipient. Use this function when an agent needs to notify users, respond in a conversation thread, or send automated alerts — the message is immediately visible to its audience.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "webex.message_create", owner_id, args, account_id=account_id
        )

    async def message_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WebexMessageGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full content of a single Webex message by its unique identifier, including text, markdown, file attachments, sender ID, and creation timestamp. This is a read-only lookup that returns message metadata and body without modifying anything. Use this function when an agent needs to inspect a specific message's details, such as when processing commands embedded in messages or quoting a previous message.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "webex.message_get", owner_id, args, account_id=account_id
        )

    async def messages_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WebexMessagesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves messages from a given Webex room, with optional filtering by parent message for thread replies, before a timestamp or message ID, and mentioning a specific person. This read-only operation allows agents to review conversations and thread content without altering any data. Use this function to understand conversation context before taking further actions such as replying or creating a new room.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "webex.messages_list", owner_id, args, account_id=account_id
        )

    async def people_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WebexPeopleListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for Webex users by email, display name, or person identifier, optionally scoped to a specific organization. This read-only lookup returns profile information such as name, email, avatar URL, and department. Use this function when an agent needs to find a person's Webex ID before sending them a direct message or adding them to a room — no data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "webex.people_list", owner_id, args, account_id=account_id
        )

    async def room_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WebexRoomCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Webex room (space) with a title, optional team association, classification label, moderation lock, public visibility setting, and description. The room is immediately created and becomes accessible to the creator and the specified team if one is provided. Use this function when an agent needs to set up a dedicated collaboration space for a project or workflow — the room appears in members' space lists right away and can be deleted if needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "webex.room_create", owner_id, args, account_id=account_id
        )

    async def rooms_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WebexRoomsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists rooms the authenticated Webex user belongs to, filterable by team, type (direct or group), and sortable by last activity or creation date. This read-only operation helps agents discover available spaces and their identifiers without modifying any data. Use this function to find a room ID before sending messages or to enumerate the user's active collaboration spaces.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("webex.rooms_list", owner_id, args, account_id=account_id)
