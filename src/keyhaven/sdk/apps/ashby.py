# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `ashby` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AshbyApplicationUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    applicationId: str
    sourceId: str | None = None
    creditedToUserId: str | None = None
    sendNotifications: bool = True


class AshbyApplicationUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AshbyApplicationUpdateBody


class AshbyApplicationsListBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str | None = None
    jobId: str | None = None
    limit: int = 10


class AshbyApplicationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AshbyApplicationsListBody | None = None


class AshbyCandidateCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    email: str
    phoneNumber: str | None = None
    sourceId: str | None = None
    creditedToUserId: str | None = None


class AshbyCandidateCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AshbyCandidateCreateBody


class AshbyCandidatesListBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    limit: int = 10


class AshbyCandidatesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AshbyCandidatesListBody | None = None


class AshbyJobsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 10
    offset: int = 0


class AshbyJobsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AshbyJobsListQuery | None = None


class AshbyNamespace:
    """Typed accessor for the `ashby` app. Reach it via `client.apps.ashby`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def application_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AshbyApplicationUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing application's metadata: changes the credited source or recruiter, and optionally sends email notifications to subscribed users (notifications are sent by default). Use this when a candidate's origin needs correction or when the responsible team member should be reassigned. Note that changing the credited user triggers a notification to subscribed team members unless sendNotifications is explicitly set to false.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ashby.application_update", owner_id, args, account_id=account_id
        )

    async def applications_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AshbyApplicationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of applications with optional filtering by status (Hired, Archived, Active, Lead) or by a specific job ID. Returns application metadata including candidate name, job, stage, and current status. Use this to review or audit applications in the pipeline — a read-only operation that does not alter any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ashby.applications_list", owner_id, args, account_id=account_id
        )

    async def candidate_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AshbyCandidateCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new candidate record in Ashby with basic contact information (name and email are required). Optionally sets the candidate's source and credits the creation to a specific user. Use this when a new person applies or is sourced and needs to be added to the ATS for tracking through the hiring pipeline. This is an internal record-creation action that does not publish anything externally.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ashby.candidate_create", owner_id, args, account_id=account_id
        )

    async def candidates_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AshbyCandidatesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all candidates in the Ashby ATS. Returns candidate profiles with associated metadata such as name, email, phone, source, and current stage. Use this to browse or search candidates when you need to find someone in the pipeline without modifying any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ashby.candidates_list", owner_id, args, account_id=account_id
        )

    async def jobs_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AshbyJobsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists active job postings in the organization with pagination support (limit and offset). Returns basic job metadata such as title, department, location, and posting status. Use this to discover what positions are currently open or to retrieve a job ID for use in other operations — a read-only GET request with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("ashby.jobs_list", owner_id, args, account_id=account_id)
