# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `notte` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class NotteObservePageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    session_id: str | None = None
    url: str
    keep_alive: bool = False
    session_timeout: int = 10


class NotteObservePageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: NotteObservePageBody


class NotteScrapeDataBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    session_id: str | None = None
    url: str | None = None
    keep_alive: bool = False
    session_timeout: int = 10
    screenshot: bool | None = None


class NotteScrapeDataRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: NotteScrapeDataBody = Field(default_factory=NotteScrapeDataBody)


class NotteStartSessionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    session_id: str | None = None
    keep_alive: bool = False
    session_timeout: int = 10
    screenshot: bool = False


class NotteStartSessionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: NotteStartSessionBody = Field(default_factory=NotteStartSessionBody)


class NotteNamespace:
    """Typed accessor for the `notte` app. Reach it via `client.apps.notte`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def observe_page(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotteObservePageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Analyzes a given URL within an active browser session and returns a structured list of all available interactive elements (links, buttons, forms, inputs) that can be acted upon. This is the equivalent of visually inspecting a webpage to understand what actions are possible — it performs no click or navigation itself. An agent should use this before attempting to interact with a page, to discover which elements are available and their selectors for subsequent actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notte.observe_page", owner_id, args, account_id=account_id
        )

    async def scrape_data(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotteScrapeDataRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Extracts and returns the structured text content and data from a specified URL within an active browser session. Unlike observation (which finds interactive elements), scraping retrieves the actual visible text, tables, lists, and other information displayed on the page. An agent should use this when the goal is to collect information from a webpage — such as reading articles, extracting product details, or gathering tabular data — without interacting with its controls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notte.scrape_data", owner_id, args, account_id=account_id
        )

    async def start_session(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotteStartSessionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new remote browser session via the Notte API and returns a unique session ID. This is the mandatory first step before any navigation, observation, or scraping operations can be performed. The session allocates server-side browser resources that automatically expire after the configured timeout unless keep_alive is enabled. An agent should call this at the beginning of any workflow that requires inspecting or extracting data from live web pages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notte.start_session", owner_id, args, account_id=account_id
        )
