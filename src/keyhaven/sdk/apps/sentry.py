# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `sentry` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SentryGetEventsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetEventsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetEventsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetEventsPath
    header: SentryGetEventsHeader = Field(default_factory=SentryGetEventsHeader)


class SentryGetIssuesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetIssuesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetIssuesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetIssuesPath
    header: SentryGetIssuesHeader = Field(default_factory=SentryGetIssuesHeader)


class SentryGetKeysPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetKeysHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetKeysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetKeysPath
    header: SentryGetKeysHeader = Field(default_factory=SentryGetKeysHeader)


class SentryGetMembersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetMembersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetMembersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetMembersPath
    header: SentryGetMembersHeader = Field(default_factory=SentryGetMembersHeader)


class SentryGetReleasesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetReleasesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetReleasesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetReleasesPath
    header: SentryGetReleasesHeader = Field(default_factory=SentryGetReleasesHeader)


class SentryGetTagsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetTagsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetTagsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetTagsPath
    header: SentryGetTagsHeader = Field(default_factory=SentryGetTagsHeader)


class SentryGetTeamsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetTeamsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetTeamsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetTeamsPath
    header: SentryGetTeamsHeader = Field(default_factory=SentryGetTeamsHeader)


class SentryGetUsersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization: str
    project: str


class SentryGetUsersHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class SentryGetUsersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SentryGetUsersPath
    header: SentryGetUsersHeader = Field(default_factory=SentryGetUsersHeader)


class SentryNamespace:
    """Typed accessor for the `sentry` app. Reach it via `client.apps.sentry`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_events(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetEventsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve individual error events for a Sentry project, each representing a single occurrence of an error with its own stack trace, context, and breadcrumbs. Events provide the granular detail needed to debug specific error instances. Use this when an agent needs to examine the details of a particular error beyond the grouped issue view.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sentry.get_events", owner_id, args, account_id=account_id
        )

    async def get_issues(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetIssuesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a list of error issues (grouped events) for a specific Sentry project within an organization. Issues represent grouped errors with metadata like event count, first and last seen timestamps, and assigned team member. Use this when an agent needs to triage bugs, monitor error trends, or identify unresolved problems in an application.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sentry.get_issues", owner_id, args, account_id=account_id
        )

    async def get_keys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetKeysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the client keys (DSNs) configured for a Sentry project, which applications use to send error events to Sentry. Each key includes a public DSN and may include a secret token that controls event ingestion. Use this when an agent needs to audit active credentials or configure a new application to report errors — keys are sensitive credentials that should not be exposed to unauthorized parties.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("sentry.get_keys", owner_id, args, account_id=account_id)

    async def get_members(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetMembersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the list of team members associated with a Sentry project, including their email addresses, roles, and pending invite status. Members control who has access to error data and project settings. Use this when an agent needs to verify team composition, check access permissions, or identify who is responsible for a project.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sentry.get_members", owner_id, args, account_id=account_id
        )

    async def get_releases(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetReleasesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List the software releases associated with a Sentry project, including version identifiers, commit information, and deployment dates. Releases help correlate error spikes to specific code deployments. Use this when an agent needs to investigate whether a recent deploy introduced new errors or track which version is affected by an issue.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sentry.get_releases", owner_id, args, account_id=account_id
        )

    async def get_tags(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetTagsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List the custom tag keys (such as browser, environment, device, or user-defined tags) available for a Sentry project. Tags are key-value pairs attached to events that enable filtering and faceted search. Use this when an agent needs to understand what metadata dimensions are available for drilling into error data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("sentry.get_tags", owner_id, args, account_id=account_id)

    async def get_teams(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetTeamsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List the teams linked to a Sentry project. Teams are groups of members that can be assigned ownership of issues, alerts, and project settings. Use this when an agent needs to understand the organizational structure around error ownership or route an issue to the appropriate team.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("sentry.get_teams", owner_id, args, account_id=account_id)

    async def get_users(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SentryGetUsersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a list of end users who have experienced errors in a Sentry project, identified by their user ID or email as attached to error events. This reveals which real-world users have been affected by bugs and how frequently. Use this when an agent needs to assess the user impact of an issue or identify which users are most affected by a recurring error.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("sentry.get_users", owner_id, args, account_id=account_id)
