# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `mixpanel` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MixpanelEngageQueryBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    distinct_id: str | None = None
    distinct_ids: str | None = None
    where: str | None = None
    output_properties: str | None = None
    session_id: str | None = None
    page: int | None = None
    filter_by_cohort: str | None = None
    include_all_users: bool | None = None


class MixpanelEngageQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MixpanelEngageQueryBody | None = None


class MixpanelEventSegmentationQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    event: str
    from_date: str
    to_date: str
    unit: str | None = None
    interval: int | None = None
    where: str | None = None
    on: str | None = None
    type_: str = Field(alias="type")
    limit: int | None = None
    project_id: int | None = None
    workspace_id: int | None = None


class MixpanelEventSegmentationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MixpanelEventSegmentationQuery


class MixpanelFunnelQueryQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    funnel_id: int | None = None
    from_date: str
    to_date: str
    length: int | None = None
    interval: int | None = None
    unit: str | None = None
    where: str | None = None
    project_id: int
    workspace_id: int | None = None


class MixpanelFunnelQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MixpanelFunnelQueryQuery


class MixpanelProfileSetBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    token: str = Field(alias="$token")
    distinct_id: str = Field(alias="$distinct_id")
    set: dict[str, Any] | None = Field(default=None, alias="$set")
    set_once: dict[str, Any] | None = Field(default=None, alias="$set_once")


class MixpanelProfileSetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MixpanelProfileSetBody


class MixpanelProfileUnionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    token: str = Field(alias="$token")
    distinct_id: str = Field(alias="$distinct_id")
    union: dict[str, Any] = Field(alias="$union")


class MixpanelProfileUnionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MixpanelProfileUnionBody


class MixpanelTrackEventBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    event: str
    properties: dict[str, Any]


class MixpanelTrackEventRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MixpanelTrackEventBody


class MixpanelNamespace:
    """Typed accessor for the `mixpanel` app. Reach it via `client.apps.mixpanel`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def engage_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MixpanelEngageQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Query Mixpanel user profiles with optional filtering, pagination, and property selection. Accepts a single distinct_id, an array of ids, or segmentation expressions (where) to filter profiles. Returns profile data including all set properties, which may contain PII (names, emails) or business-sensitive fields. Use this to look up users, export profile data, or power personalization workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mixpanel.engage_query", owner_id, args, account_id=account_id
        )

    async def event_segmentation(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MixpanelEventSegmentationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Run a segmentation query against Mixpanel event data: returns aggregated counts of a specified event over a date range, with optional filtering (where), grouping (on), and time bucketing (minute, hour, day, week, month). Use this to analyze trends, compare cohorts, or measure feature adoption. This is a read-only analytics query that does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mixpanel.event_segmentation", owner_id, args, account_id=account_id
        )

    async def funnel_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MixpanelFunnelQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Run a funnel analysis query: compute conversion rates and step-by-step drop-off for a sequence of events over a date range. Use this to understand where users abandon a multi-step process (e.g. signup flow, checkout funnel). Results include the number of users who completed each step and the overall conversion percentage. This is a read-only analytics query that does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mixpanel.funnel_query", owner_id, args, account_id=account_id
        )

    async def profile_set(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MixpanelProfileSetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Set or overwrite properties on a Mixpanel user profile, creating the profile if it does not already exist. Properties passed in $set will replace any existing values; properties in $set_once are written only if absent. Use this to update user attributes like plan tier, signup date, or preferences. Profile changes are immediately available for segmentation and queries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mixpanel.profile_set", owner_id, args, account_id=account_id
        )

    async def profile_union(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MixpanelProfileUnionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Append unique values to a list-type property on a Mixpanel user profile (e.g. tags, interests, groups). Duplicate values are automatically discarded per property. If the profile does not exist it is created first. Use this to maintain multi-value attributes that grow over time, such as 'completed_achievements' or 'visited_countries'. Profile changes are immediately available for segmentation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mixpanel.profile_union", owner_id, args, account_id=account_id
        )

    async def track_event(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MixpanelTrackEventRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Send one or more analytics events to Mixpanel's ingestion API. Each event requires a project token, a distinct_id identifying the user, and an event name (e.g. 'Signed Up', 'Purchase Completed'). Additional properties can be attached for segmentation and filtering. Use this to record user actions in real time from server-side or integration contexts. Data appears in Mixpanel reports within seconds. This operation is idempotent when $insert_id is provided.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mixpanel.track_event", owner_id, args, account_id=account_id
        )
