# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `servicem8` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class Servicem8CompaniesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = Field(default=None, alias="$filter")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")


class Servicem8CompaniesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: Servicem8CompaniesListQuery | None = None


class Servicem8JobCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_uuid: str | None = None
    status: str | None = None
    job_address: str | None = None
    job_description: str | None = None
    job_priority: str | None = None
    active: int | None = None


class Servicem8JobCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: Servicem8JobCreateBody = Field(default_factory=Servicem8JobCreateBody)


class Servicem8JobGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    uuid: str


class Servicem8JobGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: Servicem8JobGetPath


class Servicem8JobUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    uuid: str


class Servicem8JobUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: str | None = None
    job_address: str | None = None
    job_description: str | None = None
    job_priority: str | None = None


class Servicem8JobUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: Servicem8JobUpdatePath
    body: Servicem8JobUpdateBody = Field(default_factory=Servicem8JobUpdateBody)


class Servicem8JobsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = Field(default=None, alias="$filter")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")
    orderby: str | None = Field(default=None, alias="$orderby")


class Servicem8JobsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: Servicem8JobsListQuery | None = None


class Servicem8StaffListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = Field(default=None, alias="$filter")
    top: int | None = Field(default=None, alias="$top")
    skip: int | None = Field(default=None, alias="$skip")


class Servicem8StaffListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: Servicem8StaffListQuery | None = None


class Servicem8Namespace:
    """Typed accessor for the `servicem8` app. Reach it via `client.apps.servicem8`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def companies_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Servicem8CompaniesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of customer companies (clients) with OData filtering and pagination. Companies are the client records linked to jobs and invoices in ServiceM8. Agents should use this to find or match client records by name before creating jobs or updating company details. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "servicem8.companies_list", owner_id, args, account_id=account_id
        )

    async def job_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Servicem8JobCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new job record in ServiceM8 linked to a client company, with status, service address, description, and priority. The job appears immediately on the dispatch board and can be assigned to staff for field service. Agents should use this to schedule new work orders, quotes, or follow-up visits for customers. This action is reversible by setting the job as inactive or deleting it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "servicem8.job_create", owner_id, args, account_id=account_id
        )

    async def job_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Servicem8JobGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single service job by its UUID, including its status, service address, description, priority, and assigned company. Agents should use this to inspect a specific job's details for dispatching, customer service follow-up, or before updating the job. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "servicem8.job_get", owner_id, args, account_id=account_id
        )

    async def job_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Servicem8JobUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing job's fields (status, address, description, priority) using its UUID. Changes are reflected immediately on the dispatch board and may affect staff schedules and customer notifications. Agents should use this to modify job details, change statuses (e.g. from Quote to Work Order), or update service information as work progresses. This action is reversible by updating the fields again.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "servicem8.job_update", owner_id, args, account_id=account_id
        )

    async def jobs_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Servicem8JobsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of service jobs with OData-style filtering by status, company, or date, plus pagination and sorting. Jobs represent work orders, quotes, or invoices in the ServiceM8 system. Agents should use this to find jobs by status or client for scheduling, dispatching, or reporting. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "servicem8.jobs_list", owner_id, args, account_id=account_id
        )

    async def staff_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: Servicem8StaffListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of staff members with OData filtering and pagination. Staff are the employees or contractors who are dispatched to perform service jobs. Agents should use this to verify staff availability, find staff identifiers, or review team composition before assigning jobs. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "servicem8.staff_list", owner_id, args, account_id=account_id
        )
