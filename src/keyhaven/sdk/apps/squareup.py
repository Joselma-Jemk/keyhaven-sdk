# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `squareup` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SquareupCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idempotency_key: str | None = None
    given_name: str | None = None
    family_name: str | None = None
    company_name: str | None = None
    nickname: str | None = None
    email_address: str | None = None
    phone_number: str | None = None
    address: dict[str, Any] | None = None
    note: str | None = None


class SquareupCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SquareupCustomerCreateBody = Field(default_factory=SquareupCustomerCreateBody)


class SquareupCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    limit: int | None = None
    sort_field: Literal["DEFAULT", "CREATED_AT"] | None = None
    sort_order: Literal["ASC", "DESC"] | None = None
    email_address: str | None = None
    phone_number: str | None = None


class SquareupCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SquareupCustomersListQuery | None = None


class SquareupLocationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SquareupOrderCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    idempotency_key: str | None = None
    order: dict[str, Any] | None = None


class SquareupOrderCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SquareupOrderCreateBody = Field(default_factory=SquareupOrderCreateBody)


class SquareupOrdersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    location_id: str
    cursor: str | None = None
    limit: int | None = None


class SquareupOrdersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SquareupOrdersListQuery


class SquareupPaymentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    begin_time: str | None = None
    end_time: str | None = None
    location_id: str | None = None
    cursor: str | None = None
    limit: int | None = None


class SquareupPaymentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SquareupPaymentsListQuery | None = None


class SquareupNamespace:
    """Typed accessor for the `squareup` app. Reach it via `client.apps.squareup`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquareupCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer profile in Square with identifying details such as name, email, phone, company, and address. At least one identifying field is required. The idempotency_key prevents duplicate profiles on retry. Use this when a new customer makes a purchase or registers, so their orders and payments can be linked to a persistent profile. The profile can be updated or deleted later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squareup.customer_create", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquareupCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches and lists Square customer profiles with optional filters by email address or phone number, plus cursor-based pagination and sorting. Each profile contains PII such as name, email, phone number, and address. Use this to find an existing customer before creating a new one, or to gather customer data for reporting. Read-only but accesses personal data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squareup.customers_list", owner_id, args, account_id=account_id
        )

    async def locations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquareupLocationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all business locations associated with the authenticated Square account. Each location includes its name, address, timezone, currency, status (active/inactive), and capability flags. Use this to discover available location IDs needed as parameters for orders, payments, and other location-scoped operations. Read-only and does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squareup.locations_list", owner_id, args, account_id=account_id
        )

    async def order_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquareupOrderCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new order in Square for a specific merchant location, with line items, taxes, discounts, and an optional customer association. The order is created in a draft state and does not process any payment by itself. Use this to build a cart before requesting payment, or to create a recorded order for accounting purposes. The idempotency_key guarantees at-most-once creation even on network retries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squareup.order_create", owner_id, args, account_id=account_id
        )

    async def orders_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquareupOrdersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists orders from a specific Square location with cursor-based pagination and an optional page limit. Each order includes line items, totals, taxes, discounts, and fulfillment status. Use this to review order history for a location, find orders by timeframe, or obtain order IDs needed for subsequent operations. Read-only operation that does not alter any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squareup.orders_list", owner_id, args, account_id=account_id
        )

    async def payments_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SquareupPaymentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of payments processed by the Square account within an optional time window, scoped to a specific location if desired. Each payment record includes amount, currency, status, card details, and timestamps. Use this to review transaction history, reconcile settlements, or locate a specific payment by its ID for follow-up actions. The data returned contains financial information and should be handled with care.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "squareup.payments_list", owner_id, args, account_id=account_id
        )
