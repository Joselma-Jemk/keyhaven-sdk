# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `ghost_content` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GhostContentAuthorsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key: str
    limit: int | None = None
    page: int | None = None
    include: str | None = None
    order: str | None = None


class GhostContentAuthorsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GhostContentAuthorsListQuery


class GhostContentPostGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GhostContentPostGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key: str


class GhostContentPostGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GhostContentPostGetPath
    query: GhostContentPostGetQuery


class GhostContentPostsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key: str
    limit: int | None = None
    page: int | None = None
    include: str | None = None
    filter: str | None = None
    order: str | None = None
    fields: str | None = None
    formats: str | None = None


class GhostContentPostsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GhostContentPostsListQuery


class GhostContentSettingsGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key: str


class GhostContentSettingsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GhostContentSettingsGetQuery


class GhostContentTagsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    key: str
    limit: int | None = None
    page: int | None = None
    include: str | None = None
    order: str | None = None


class GhostContentTagsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GhostContentTagsListQuery


class GhostContentNamespace:
    """Typed accessor for the `ghost_content` app. Reach it via `client.apps.ghost_content`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def authors_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GhostContentAuthorsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all author profiles associated with the Ghost site, including each author's name, bio, profile image URL, cover image, and social media links. Use this to display an author directory or to resolve author slugs and IDs when filtering posts. Supports pagination and relation includes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ghost_content.authors_list", owner_id, args, account_id=account_id
        )

    async def post_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GhostContentPostGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of one published post, including HTML or plaintext content, excerpt, feature image URL, metadata, and associated tags and authors. Use this to display or process a specific article on your site. Requires the post's unique ID from the list endpoint.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ghost_content.post_get", owner_id, args, account_id=account_id
        )

    async def posts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GhostContentPostsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of published blog posts from the Ghost site, with support for filtering by tag or featured status, ordering by publication date, and selecting specific fields. Optionally includes related authors and tags. Use this to browse, search, or syndicate published content programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ghost_content.posts_list", owner_id, args, account_id=account_id
        )

    async def settings_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GhostContentSettingsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the public-facing configuration of the Ghost site, such as the site title, description, logo URL, cover image, navigation links, social accounts, and timezone. Use this to initialize site metadata or display consistent branding information across pages. All data returned is already public content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ghost_content.settings_get", owner_id, args, account_id=account_id
        )

    async def tags_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GhostContentTagsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all tags used across the Ghost site, including each tag's name, slug, description, and associated post count. Use this to build tag clouds, filter navigation menus, or discover tag slugs to use in post filtering. Supports pagination and ordering.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "ghost_content.tags_list", owner_id, args, account_id=account_id
        )
