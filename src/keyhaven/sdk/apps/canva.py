# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `canva` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CanvaAutofillCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    brand_template_id: str
    data: dict[str, Any]
    title: str | None = None


class CanvaAutofillCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CanvaAutofillCreateBody


class CanvaBrandTemplatesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    continuation: str | None = None
    ownership: str | None = None
    sort_by: str | None = None
    dataset: str | None = None


class CanvaBrandTemplatesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CanvaBrandTemplatesListQuery | None = None


class CanvaDesignCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    design_type: dict[str, Any] | None = None
    asset_id: str | None = None
    title: str | None = None


class CanvaDesignCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CanvaDesignCreateBody = Field(default_factory=CanvaDesignCreateBody)


class CanvaDesignExportCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    design_id: str
    format: dict[str, Any]


class CanvaDesignExportCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CanvaDesignExportCreateBody


class CanvaDesignGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    designId: str


class CanvaDesignGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CanvaDesignGetPath


class CanvaDesignsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    continuation: str | None = None
    ownership: str | None = None
    sort_by: str | None = None


class CanvaDesignsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CanvaDesignsListQuery | None = None


class CanvaNamespace:
    """Typed accessor for the `canva` app. Reach it via `client.apps.canva`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def autofill_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CanvaAutofillCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Initiates an asynchronous job that takes a brand template and fills its dataset fields with provided text, image asset, or chart data values, producing a new design. The resulting design appears in the user's Canva workspace once the job completes. Use this to bulk-generate personalised designs (e.g. certificates, social cards) from a branded template.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "canva.autofill_create", owner_id, args, account_id=account_id
        )

    async def brand_templates_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CanvaBrandTemplatesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of brand templates the authenticated user has access to, with optional search, ownership filter, and dataset filter. No data is modified. Use this to find a suitable brand template for autofilling into new designs, or to discover which templates support dataset fields.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "canva.brand_templates_list", owner_id, args, account_id=account_id
        )

    async def design_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CanvaDesignCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Canva design from a preset type (e.g. presentation, social media graphic, document) or from a previously uploaded asset as the first page. The design is immediately created in the user's workspace and visible in the Canva UI. Use this to programmatically generate new design documents for subsequent editing, autofilling, or export.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "canva.design_create", owner_id, args, account_id=account_id
        )

    async def design_export_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CanvaDesignExportCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Initiates an asynchronous job that exports an existing design to a downloadable file in the requested format (PDF, JPG, PNG, MP4, GIF, or PPTX). The original design remains unchanged. Use this when you need a rendered, distributable version of a design for sharing, printing, or archival.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "canva.design_export_create", owner_id, args, account_id=account_id
        )

    async def design_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CanvaDesignGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches metadata such as title, type, thumbnail URL, owner, and timestamps for a single design without retrieving its editable contents. No data is modified. Use this to learn about a specific design before deciding to edit, export, or autofill it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("canva.design_get", owner_id, args, account_id=account_id)

    async def designs_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CanvaDesignsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of designs accessible to the authenticated user, with optional search text, ownership filter, and sort order. No designs are created or modified. Use this to browse existing designs, find a design's ID for subsequent operations, or search for a specific design by title.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "canva.designs_list", owner_id, args, account_id=account_id
        )
