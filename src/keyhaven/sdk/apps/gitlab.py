# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `gitlab` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GitlabArchiveProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabArchiveProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabArchiveProjectPath


class GitlabCreateBranchPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabCreateBranchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    branch: str
    ref: str


class GitlabCreateBranchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabCreateBranchPath
    query: GitlabCreateBranchQuery


class GitlabCreateFilePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str
    file_path: str


class GitlabCreateFileBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    branch: str = "main"
    content: str
    commit_message: str
    author_email: str | None = None
    author_name: str | None = None
    encoding: Literal["text", "base64"] = "text"


class GitlabCreateFileRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabCreateFilePath
    body: GitlabCreateFileBody


class GitlabCreateProjectBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    path: str | None = None
    description: str | None = None
    visibility: Literal["private", "internal", "public"] = "private"
    initialize_with_readme: bool = False


class GitlabCreateProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: GitlabCreateProjectBody


class GitlabDeleteProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabDeleteProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabDeleteProjectPath


class GitlabGetCurrentUserRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class GitlabGetFileContentPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str
    file_path: str


class GitlabGetFileContentQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ref: str = "main"


class GitlabGetFileContentRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabGetFileContentPath
    query: GitlabGetFileContentQuery = Field(default_factory=GitlabGetFileContentQuery)


class GitlabGetProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabGetProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabGetProjectPath


class GitlabGetUserByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class GitlabGetUserByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabGetUserByIdPath


class GitlabGetUserByUsernameQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    username: str


class GitlabGetUserByUsernameRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GitlabGetUserByUsernameQuery


class GitlabListBranchesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabListBranchesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    search: str | None = None
    per_page: int = 20
    page: int = 1


class GitlabListBranchesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabListBranchesPath
    query: GitlabListBranchesQuery | None = None


class GitlabListProjectIssuesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabListProjectIssuesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    state: Literal["opened", "closed", "all"] = "opened"
    labels: str | None = None
    milestone: str | None = None
    order_by: Literal[
        "created_at",
        "updated_at",
        "priority",
        "due_date",
        "relative_position",
        "label_priority",
        "milestone_due",
        "popularity",
        "weight",
    ] = "created_at"
    sort: Literal["asc", "desc"] = "desc"
    per_page: int = 20
    page: int = 1


class GitlabListProjectIssuesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabListProjectIssuesPath
    query: GitlabListProjectIssuesQuery | None = None


class GitlabListUserProjectsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: int


class GitlabListUserProjectsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    visibility: Literal["public", "internal", "private"] | None = None
    order_by: Literal["id", "name", "path", "created_at", "updated_at", "last_activity_at"] = (
        "created_at"
    )
    sort: Literal["asc", "desc"] = "desc"
    per_page: int = 20
    page: int = 1


class GitlabListUserProjectsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabListUserProjectsPath
    query: GitlabListUserProjectsQuery | None = None


class GitlabRestoreProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabRestoreProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabRestoreProjectPath


class GitlabUnarchiveProjectPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabUnarchiveProjectRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabUnarchiveProjectPath


class GitlabUpdateFilePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str
    file_path: str


class GitlabUpdateFileBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    branch: str = "main"
    content: str
    commit_message: str
    author_email: str | None = None
    author_name: str | None = None
    encoding: Literal["text", "base64"] = "text"
    last_commit_id: str | None = None


class GitlabUpdateFileRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabUpdateFilePath
    body: GitlabUpdateFileBody


class GitlabNamespace:
    """Typed accessor for the `gitlab` app. Reach it via `client.apps.gitlab`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def archive_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabArchiveProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Archives a GitLab project, making it read-only: no new commits, issues, merge requests, or changes can be made until it is unarchived. The repository remains visible to authorised users. This action requires Owner or administrator role and is reversible via unarchive. Use this when an agent needs to freeze a completed project, prevent further modifications to a stable release, or clean up inactive repositories without deleting them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.archive_project", owner_id, args, account_id=account_id
        )

    async def create_branch(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabCreateBranchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new branch in a GitLab project's repository, branching from a specified existing branch name or commit SHA. The branch appears immediately in the repository and can be used for commits and merge requests. This is a reversible action — branches can be deleted later. Use this when an agent needs to create a feature or fix branch programmatically as part of an automated workflow, without requiring a local git client.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.create_branch", owner_id, args, account_id=account_id
        )

    async def create_file(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabCreateFileRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new file in a GitLab repository by committing it to a specified branch. Requires the file path, content, a commit message, and optionally author details and encoding (text or base64). A new commit is created on the branch, which triggers CI/CD pipelines and appears in the project's activity feed. This action is reversible via deletion or revert commit. Use this when an agent needs to add a new configuration file, documentation, or source file programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.create_file", owner_id, args, account_id=account_id
        )

    async def create_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabCreateProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new GitLab project under the authenticated user's namespace, with configurable name, visibility (private/internal/public), description, and optional initialisation with a README file. The project becomes immediately available for commits, issues, merge requests, and CI/CD pipelines. This action is reversible by deleting the project but will trigger notifications to team members. Use this when an agent needs to bootstrap a new code repository programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.create_project", owner_id, args, account_id=account_id
        )

    async def delete_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabDeleteProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Marks a GitLab project for deletion, including all associated resources (repository, issues, merge requests, wikis, CI/CD pipelines, and container images). By default the project is deleted after a 7-day grace period during which it can be restored via GITLAB__RESTORE_PROJECT. On GitLab 15.11+, if the project is already marked for deletion, it is removed immediately. This is a destructive action with a soft-delete window. Use this only when an agent has explicit authorisation to remove a project permanently.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.delete_project", owner_id, args, account_id=account_id
        )

    async def get_current_user(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabGetCurrentUserRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the profile of the currently authenticated GitLab user, including their ID, username, email, avatar, and account metadata. This is read-only and has no side effects. Use this when an agent needs to confirm its own identity, retrieve the access token's associated user information, or obtain the current user's ID for use in subsequent operations that require an actor reference.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.get_current_user", owner_id, args, account_id=account_id
        )

    async def get_file_content(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabGetFileContentRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the raw content of a single file from a GitLab repository at a specific branch, tag, or commit reference. Returns the file's data along with metadata such as file size and encoding. This is read-only and does not modify the repository. Use this when an agent needs to read configuration files, inspect source code, download assets, or verify file contents before making changes to the repository.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.get_file_content", owner_id, args, account_id=account_id
        )

    async def get_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabGetProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific GitLab project by its ID or URL-encoded path, including the repository URL, visibility, owner, description, default branch, statistics, and CI/CD settings. This is read-only and does not alter the project. Use this when an agent needs to inspect project configuration, verify a project exists before performing operations on it, or retrieve the default branch name for subsequent git operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.get_project", owner_id, args, account_id=account_id
        )

    async def get_user_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabGetUserByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the profile of a GitLab user by their numeric user ID. Returns detailed information such as name, username, avatar, public email, and account creation date. This is read-only and modifies nothing. Use this when an agent already knows a user's numeric ID (e.g., from a prior search or event payload) and needs to retrieve their profile or confirm their identity before performing an action on their behalf.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.get_user_by_id", owner_id, args, account_id=account_id
        )

    async def get_user_by_username(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabGetUserByUsernameRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Looks up a GitLab user by their username using a query parameter on the /users endpoint. Returns the user's profile information including ID, name, avatar URL, and public activity status. This is a read-only operation that does not affect any resources. Use this when an agent needs to resolve a human-readable username into a numeric user ID for subsequent API calls, or to verify that a user exists before interacting with them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.get_user_by_username", owner_id, args, account_id=account_id
        )

    async def list_branches(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabListBranchesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all branches in a GitLab project's repository, with optional search filtering and pagination. Returns branch names, commit references, and whether each branch is the default. This is read-only and does not modify any repository state. Use this when an agent needs to enumerate available branches for checkout, verify branch naming patterns, or check whether a specific branch exists before creating or deleting it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.list_branches", owner_id, args, account_id=account_id
        )

    async def list_project_issues(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabListProjectIssuesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all issues within a GitLab project, with optional filters for state (opened/closed/all), labels, milestone, and ordering by various fields including priority, due date, and weight. Pagination is supported. This is read-only and does not modify any issues. Use this when an agent needs to triage or summarise project issues, find items matching specific labels or milestones, or collect issue data for reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.list_project_issues", owner_id, args, account_id=account_id
        )

    async def list_user_projects(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabListUserProjectsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all GitLab projects accessible to a specific user, with optional filtering by visibility (public/internal/private), ordering, sorting, and pagination. This is read-only and does not modify any project or user state. Use this when an agent needs to discover which projects a user has access to, enumerate repositories for audit or migration, or find a specific project by name among a user's repositories.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.list_user_projects", owner_id, args, account_id=account_id
        )

    async def restore_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabRestoreProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a GitLab project that was previously marked for deletion, cancelling the scheduled removal and returning the project to normal active status. All associated resources (repository, issues, merge requests, pipelines) become fully available again. Use this when an agent needs to recover a project that was deleted accidentally or prematurely, or when the deletion grace period should be halted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.restore_project", owner_id, args, account_id=account_id
        )

    async def unarchive_project(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabUnarchiveProjectRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores an archived GitLab project to its normal active state, allowing commits, issues, merge requests, and other operations to resume. This endpoint is idempotent — unarchiving a project that is not archived leaves it unchanged. Use this when an agent needs to reopen a previously completed project for further development, reactivate a repository for patch releases, or reverse an accidental archival.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.unarchive_project", owner_id, args, account_id=account_id
        )

    async def update_file(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabUpdateFileRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies the content of an existing file in a GitLab repository by creating a new commit on the specified branch. Supports setting the branch, new content, commit message, author details, encoding, and an optional last_commit_id for conflict detection. This action triggers CI/CD pipelines and updates the project's activity feed. The change is reversible via revert or a subsequent update. Use this when an agent needs to programmatically edit configuration files, source code, or documentation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab.update_file", owner_id, args, account_id=account_id
        )
