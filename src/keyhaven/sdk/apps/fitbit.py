# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `fitbit` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FitbitActivityLogCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class FitbitActivityLogCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    activityId: int | None = None
    activityName: str | None = None
    manualCalories: int | None = None
    startTime: str
    durationMillis: int
    date: str
    distance: float | None = None
    distanceUnit: str | None = None


class FitbitActivityLogCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FitbitActivityLogCreatePath
    query: FitbitActivityLogCreateQuery


class FitbitActivityTimeseriesHeartratePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str
    startDate: str
    endDate: str


class FitbitActivityTimeseriesHeartrateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FitbitActivityTimeseriesHeartratePath


class FitbitActivityTimeseriesStepsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str
    startDate: str
    endDate: str


class FitbitActivityTimeseriesStepsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FitbitActivityTimeseriesStepsPath


class FitbitBodyWeightLogCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class FitbitBodyWeightLogCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    weight: float
    date: str
    time: str | None = None


class FitbitBodyWeightLogCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FitbitBodyWeightLogCreatePath
    query: FitbitBodyWeightLogCreateQuery


class FitbitSleepLogListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str
    startDate: str
    endDate: str


class FitbitSleepLogListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FitbitSleepLogListPath


class FitbitUserProfileGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class FitbitUserProfileGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FitbitUserProfileGetPath


class FitbitNamespace:
    """Typed accessor for the `fitbit` app. Reach it via `client.apps.fitbit`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def activity_log_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FitbitActivityLogCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new manual activity log entry on the user's Fitbit account, such as a walk, run, or custom exercise. This writes a record that appears in the user's activity history, contributes to daily step and calorie totals, and may be shared with Fitbit friends depending on privacy settings. Use it when a user completed an exercise their device did not automatically detect, or when backfilling historical activity data. The entry can be deleted later via the Fitbit dashboard.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fitbit.activity_log_create", owner_id, args, account_id=account_id
        )

    async def activity_timeseries_heartrate(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FitbitActivityTimeseriesHeartrateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the daily resting heart rate and heart rate zone data for the specified date range as a time series. Each data point provides the date and the resting heart rate value logged by the device. This read-only operation does not modify any Fitbit data. Use it when monitoring cardiovascular trends, generating health reports, or correlating heart rate with other activity metrics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fitbit.activity_timeseries_heartrate", owner_id, args, account_id=account_id
        )

    async def activity_timeseries_steps(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FitbitActivityTimeseriesStepsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the daily step count totals for the specified user and date range as a time series. Each data point contains the date and total steps recorded by the device for that day. This is a read-only query that does not alter any Fitbit data. Use it when you need historical step trends, progress tracking, or aggregated activity data for reports.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fitbit.activity_timeseries_steps", owner_id, args, account_id=account_id
        )

    async def body_weight_log_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FitbitBodyWeightLogCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Logs a new body weight measurement on the user's Fitbit account with the specified weight, date, and optional time of day. The entry appears in the user's weight dashboard and long-term weight trends. Use this function when recording daily weigh-ins, importing weight data from another source into Fitbit, or setting a starting weight. The log entry is reversible and can be deleted by the user later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fitbit.body_weight_log_create", owner_id, args, account_id=account_id
        )

    async def sleep_log_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FitbitSleepLogListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all sleep log entries for the specified user and date range, including sleep start and end times, total duration, efficiency score, and breakdown by sleep phase (light, deep, REM, wake). The date range is limited to 100 days per request. This is a read-only query that does not modify any Fitbit data. Use it to analyze sleep patterns, generate sleep quality reports, or calculate sleep metrics over time.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fitbit.sleep_log_list", owner_id, args, account_id=account_id
        )

    async def user_profile_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FitbitUserProfileGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the authenticated Fitbit user's profile including locale, unit preferences, timezone, member-since date, avatar URL, and the encoded user ID. This read-only operation does not create or modify any data on Fitbit's servers. Use it when you need to identify the current user, discover their measurement system, or verify account linkage during setup.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fitbit.user_profile_get", owner_id, args, account_id=account_id
        )
