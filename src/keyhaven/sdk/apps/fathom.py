# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `fathom` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FathomMeetingGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class FathomMeetingGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_transcript: bool | None = None
    include_summary: bool | None = None


class FathomMeetingGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FathomMeetingGetPath
    query: FathomMeetingGetQuery | None = None


class FathomMeetingSummaryGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class FathomMeetingSummaryGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FathomMeetingSummaryGetPath


class FathomMeetingsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    created_after: str | None = None
    created_before: str | None = None
    include_transcript: bool | None = None
    include_summary: bool | None = None
    include_action_items: bool | None = None
    calendar_invitees_domains: list[Any] | None = None


class FathomMeetingsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FathomMeetingsListQuery | None = None


class FathomRecordingTranscriptGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    recording_id: str


class FathomRecordingTranscriptGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    destination_url: str | None = None


class FathomRecordingTranscriptGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FathomRecordingTranscriptGetPath
    query: FathomRecordingTranscriptGetQuery | None = None


class FathomTeamMembersListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class FathomTeamMembersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FathomTeamMembersListPath


class FathomTeamsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None


class FathomTeamsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FathomTeamsListQuery | None = None


class FathomNamespace:
    """Typed accessor for the `fathom` app. Reach it via `client.apps.fathom`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def meeting_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FathomMeetingGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single Fathom meeting by its ID with optional inclusion of the transcript, summary, and action items. This returns the full meeting record including participants, duration, and recording links. Use it when an agent needs to inspect a specific meeting's details and content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fathom.meeting_get", owner_id, args, account_id=account_id
        )

    async def meeting_summary_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FathomMeetingSummaryGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the AI-generated summary for a specific Fathom meeting by meeting ID, including key points, decisions, and highlights extracted from the conversation. This is a read-only operation that provides a concise overview without the full transcript detail. Use it when an agent needs a quick understanding of what was discussed in a meeting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fathom.meeting_summary_get", owner_id, args, account_id=account_id
        )

    async def meetings_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FathomMeetingsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists Fathom meetings with optional filters by creation date, calendar invitee domains, and full content inclusion (transcript, summary, action items). Returns meeting metadata such as titles, dates, duration, and invitees. Use it when an agent needs to search or browse recorded meetings for further analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fathom.meetings_list", owner_id, args, account_id=account_id
        )

    async def recording_transcript_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FathomRecordingTranscriptGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full transcript for a specific Fathom meeting recording, with an option to provide a destination URL for asynchronous delivery. The transcript captures the entire conversation with speaker labels and timestamps. Use it when an agent needs to analyze spoken content or extract quotes from a meeting, but be aware transcripts may contain sensitive information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fathom.recording_transcript_get", owner_id, args, account_id=account_id
        )

    async def team_members_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FathomTeamMembersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists members of a specific Fathom team by team ID, returning user details including names and roles within the team. This is a read-only operation that shows who has access to team resources. Use it when an agent needs to verify team composition or identify team members.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fathom.team_members_list", owner_id, args, account_id=account_id
        )

    async def teams_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FathomTeamsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all teams within the authenticated Fathom account with optional pagination. Returns team metadata such as names and identifiers. Use it when an agent needs to discover available teams before listing their members or accessing team-level resources.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fathom.teams_list", owner_id, args, account_id=account_id
        )
