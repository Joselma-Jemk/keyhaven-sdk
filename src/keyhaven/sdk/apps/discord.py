# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `discord` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DiscordGetCurrentUserRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class DiscordNamespace:
    """Typed accessor for the `discord` app. Reach it via `client.apps.discord`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_current_user(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DiscordGetCurrentUserRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the Discord user object for the currently authenticated user. Returns profile details such as the user's unique ID, username, discriminator, avatar hash, banner, accent color, locale, and flags. This is a read-only introspection call — it does not modify anything or notify anyone. Use it when you need to confirm the identity or profile attributes of the logged-in user before proceeding with further actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "discord.get_current_user", owner_id, args, account_id=account_id
        )
