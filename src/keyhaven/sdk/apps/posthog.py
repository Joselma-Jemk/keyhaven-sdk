# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `posthog` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PosthogOrganizationsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PosthogOrganizationsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PosthogOrganizationsHeader = Field(default_factory=PosthogOrganizationsHeader)


class PosthogProjectsCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization_id: str


class PosthogProjectsCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PosthogProjectsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None


class PosthogProjectsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PosthogProjectsCreatePath
    header: PosthogProjectsCreateHeader = Field(default_factory=PosthogProjectsCreateHeader)
    body: PosthogProjectsCreateBody = Field(default_factory=PosthogProjectsCreateBody)


class PosthogProjectsDelPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization_id: str
    id: str


class PosthogProjectsDelHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PosthogProjectsDelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PosthogProjectsDelPath
    header: PosthogProjectsDelHeader = Field(default_factory=PosthogProjectsDelHeader)


class PosthogProjectsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization_id: str


class PosthogProjectsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PosthogProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PosthogProjectsListPath
    header: PosthogProjectsListHeader = Field(default_factory=PosthogProjectsListHeader)


class PosthogProjectsQueryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization_id: str
    id: str


class PosthogProjectsQueryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PosthogProjectsQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PosthogProjectsQueryPath
    header: PosthogProjectsQueryHeader = Field(default_factory=PosthogProjectsQueryHeader)


class PosthogProjectsUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organization_id: str
    id: str


class PosthogProjectsUpdateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PosthogProjectsUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None


class PosthogProjectsUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PosthogProjectsUpdatePath
    header: PosthogProjectsUpdateHeader = Field(default_factory=PosthogProjectsUpdateHeader)
    body: PosthogProjectsUpdateBody = Field(default_factory=PosthogProjectsUpdateBody)


class PosthogNamespace:
    """Typed accessor for the `posthog` app. Reach it via `client.apps.posthog`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def organizations(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PosthogOrganizationsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all PostHog organizations the authenticated account belongs to. Each organization represents a separate workspace with its own projects, dashboards, and analytics data. Use this to discover organization IDs before operating on projects within an organization. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "posthog.organizations", owner_id, args, account_id=account_id
        )

    async def projects_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PosthogProjectsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new PostHog project within the specified organization with a given display name. The new project starts empty and can be used to collect events, run product analytics, manage feature flags, and build dashboards. Use this to set up a workspace for a new product or environment. This action is reversible by deleting the project.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "posthog.projects_create", owner_id, args, account_id=account_id
        )

    async def projects_del(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PosthogProjectsDelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a PostHog project and all of its associated data, including events, persons, feature flags, dashboards, insights, and annotations. This action is irreversible and data cannot be recovered. Use this only when a project is definitively no longer needed and all valuable data has been exported or migrated.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "posthog.projects_del", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PosthogProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all PostHog projects within a specified organization. Projects contain product analytics data such as events, persons, feature flags, and dashboards. Use this to browse available projects or locate a project ID for subsequent analytics queries. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "posthog.projects_list", owner_id, args, account_id=account_id
        )

    async def projects_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PosthogProjectsQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific PostHog project, including its name, configuration, and API tokens. Use this to inspect a project's metadata before querying its analytics data or managing its settings. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "posthog.projects_query", owner_id, args, account_id=account_id
        )

    async def projects_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PosthogProjectsUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the properties of an existing PostHog project, such as its display name. Changes are immediately reflected across the organization workspace and affect all team members. Use this to rename a project or adjust its configuration. This action is reversible by issuing another update.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "posthog.projects_update", owner_id, args, account_id=account_id
        )
