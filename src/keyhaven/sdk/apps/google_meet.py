# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `google_meet` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GoogleMeetConferenceRecordsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class GoogleMeetConferenceRecordsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetConferenceRecordsGetPath


class GoogleMeetConferenceRecordsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int = 25
    pageToken: str | None = None
    filter: str | None = None


class GoogleMeetConferenceRecordsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GoogleMeetConferenceRecordsListQuery | None = None


class GoogleMeetParticipantSessionsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str


class GoogleMeetParticipantSessionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int = 100
    pageToken: str | None = None
    filter: str | None = None


class GoogleMeetParticipantSessionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetParticipantSessionsListPath
    query: GoogleMeetParticipantSessionsListQuery | None = None


class GoogleMeetParticipantsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class GoogleMeetParticipantsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetParticipantsGetPath


class GoogleMeetParticipantsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str


class GoogleMeetParticipantsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int = 100
    pageToken: str | None = None
    filter: str | None = None


class GoogleMeetParticipantsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetParticipantsListPath
    query: GoogleMeetParticipantsListQuery | None = None


class GoogleMeetRecordingsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class GoogleMeetRecordingsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetRecordingsGetPath


class GoogleMeetRecordingsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str


class GoogleMeetRecordingsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int = 25
    pageToken: str | None = None


class GoogleMeetRecordingsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetRecordingsListPath
    query: GoogleMeetRecordingsListQuery | None = None


class GoogleMeetSpacesConfigureSmartNotesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class GoogleMeetSpacesConfigureSmartNotesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    updateMask: str | None = None


class GoogleMeetSpacesConfigureSmartNotesBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    config: dict[str, Any] | None = None


class GoogleMeetSpacesConfigureSmartNotesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetSpacesConfigureSmartNotesPath
    query: GoogleMeetSpacesConfigureSmartNotesQuery | None = None
    body: GoogleMeetSpacesConfigureSmartNotesBody = Field(
        default_factory=GoogleMeetSpacesConfigureSmartNotesBody
    )


class GoogleMeetSpacesCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    config: dict[str, Any] | None = None


class GoogleMeetSpacesCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: GoogleMeetSpacesCreateBody | None = None


class GoogleMeetSpacesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class GoogleMeetSpacesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleMeetSpacesGetPath


class GoogleMeetNamespace:
    """Typed accessor for the `google_meet` app. Reach it via `client.apps.google_meet`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def conference_records_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetConferenceRecordsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve detailed information about a specific Google Meet conference record by its resource name, including its start and end times, participant count, and associated meeting space. Use this when an agent needs details about a past or ongoing meeting for recap purposes or meeting analytics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.conference_records_get", owner_id, args, account_id=account_id
        )

    async def conference_records_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetConferenceRecordsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List past and ongoing Google Meet conference records, with optional filtering by space, meeting code, or time range. Returns metadata such as start time, end time, and linked space for each conference. Use this when an agent needs to browse meeting history, find a specific conference by its code, or retrieve records for analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.conference_records_list", owner_id, args, account_id=account_id
        )

    async def participant_sessions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetParticipantSessionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List the individual join and leave intervals (sessions) for a participant in a conference record. Each session represents a continuous period of attendance with start and end times. Use this when an agent needs to analyze a participant engagement patterns, such as whether they joined late, left early, or experienced disconnections.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.participant_sessions_list", owner_id, args, account_id=account_id
        )

    async def participants_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetParticipantsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve details about a single participant in a Google Meet conference, including their display name, email address (if available), and session timestamps. Use this when an agent needs specific information about a meeting attendee participation, such as when they joined and left.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.participants_get", owner_id, args, account_id=account_id
        )

    async def participants_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetParticipantsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all participants who attended a specific Google Meet conference, with optional filtering to show only currently active participants. Returns participant identifiers and their join or leave times. Use this when an agent needs to audit meeting attendance or determine who was present at a given meeting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.participants_list", owner_id, args, account_id=account_id
        )

    async def recordings_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetRecordingsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve detailed information about a single meeting recording, including its Google Drive URL, file type, file size, and creation timestamp. Use this when an agent needs to access or reference a specific recording for sharing, archiving, or downstream analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.recordings_get", owner_id, args, account_id=account_id
        )

    async def recordings_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetRecordingsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all recordings associated with a specific Google Meet conference record, including their Google Drive links, file names, and processing status. Use this when an agent needs to find available recordings after a meeting has ended or check whether a recording is ready for download.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.recordings_list", owner_id, args, account_id=account_id
        )

    async def spaces_configure_smart_notes(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetSpacesConfigureSmartNotesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Update the smart notes and auto-summary configuration for a meeting space, controlling whether meeting recaps and transcripts are automatically generated after future meetings. This setting affects participant privacy and the availability of post-meeting artifacts. Use this when an agent needs to enable or disable automatic transcription for compliance, privacy, or productivity reasons.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.spaces_configure_smart_notes", owner_id, args, account_id=account_id
        )

    async def spaces_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetSpacesCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new Google Meet meeting space with optional configuration for moderation and automatic smart notes generation. Returns a space resource with a meeting code and URL that can be shared with participants. Use this when an agent needs to generate a meeting link for a scheduled or ad-hoc video conference — no calendar event or notification is created; this just prepares the meeting infrastructure.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.spaces_create", owner_id, args, account_id=account_id
        )

    async def spaces_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleMeetSpacesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve metadata about a Google Meet meeting space, including its meeting code, URL, and configured settings such as moderation mode and artifact generation preferences. Use this when an agent needs to inspect a space configuration before modifying settings or sharing the meeting link with participants.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_meet.spaces_get", owner_id, args, account_id=account_id
        )
