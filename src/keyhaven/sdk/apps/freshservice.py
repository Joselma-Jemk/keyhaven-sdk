# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `freshservice` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FreshserviceRequesterCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshserviceRequesterCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str | None = None
    last_name: str | None = None
    email: str | None = None
    job_title: str | None = None
    work_phone: str | None = None
    mobile_phone: str | None = None
    department_id: int | None = None
    location_id: int | None = None


class FreshserviceRequesterCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshserviceRequesterCreatePath
    body: FreshserviceRequesterCreateBody = Field(default_factory=FreshserviceRequesterCreateBody)


class FreshserviceRequestersListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshserviceRequestersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 30
    email: str | None = None


class FreshserviceRequestersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshserviceRequestersListPath
    query: FreshserviceRequestersListQuery | None = None


class FreshserviceTicketCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshserviceTicketCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    email: str
    subject: str
    description: str
    status: int = 2
    priority: int = 1
    source: int | None = None
    group_id: int | None = None
    responder_id: int | None = None
    department_id: int | None = None
    category: str | None = None
    sub_category: str | None = None
    tags: list[Any] | None = None


class FreshserviceTicketCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshserviceTicketCreatePath
    body: FreshserviceTicketCreateBody


class FreshserviceTicketGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str
    ticket_id: str


class FreshserviceTicketGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include: str | None = None


class FreshserviceTicketGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshserviceTicketGetPath
    query: FreshserviceTicketGetQuery | None = None


class FreshserviceTicketUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str
    ticket_id: str


class FreshserviceTicketUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: int | None = None
    priority: int | None = None
    group_id: int | None = None
    responder_id: int | None = None
    tags: list[Any] | None = None


class FreshserviceTicketUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshserviceTicketUpdatePath
    body: FreshserviceTicketUpdateBody = Field(default_factory=FreshserviceTicketUpdateBody)


class FreshserviceTicketsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshserviceTicketsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 30
    filter: str | None = None
    order_by: str | None = None
    order_type: str = "desc"
    requester_id: str | None = None
    email: str | None = None


class FreshserviceTicketsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshserviceTicketsListPath
    query: FreshserviceTicketsListQuery | None = None


class FreshserviceNamespace:
    """Typed accessor for the `freshservice` app. Reach it via `client.apps.freshservice`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def requester_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshserviceRequesterCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new end-user (requester) profile in Freshservice with optional contact details, department, and location. The email must be unique across all requesters. Use this to onboard new users who will submit tickets via the portal or email. The profile is immediately usable and does not trigger any notifications by itself.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshservice.requester_create", owner_id, args, account_id=account_id
        )

    async def requesters_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshserviceRequestersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of requester (end-user) profiles registered in Freshservice, with an optional email filter. Each result includes the requester's name, email, phone, department, and timestamps. Use this to look up existing users by email before creating a new requester, or to audit the user base.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshservice.requesters_list", owner_id, args, account_id=account_id
        )

    async def ticket_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshserviceTicketCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new support ticket with a requester email, subject, and description, plus optional fields like priority, status, group assignment, category, and tags. A new requester profile is automatically created if the email does not match an existing one. Use this to log incoming issues, escalate customer requests, or automate ticket creation from external triggers. The ticket is immediately visible to agents and triggers any configured Freshservice automations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshservice.ticket_create", owner_id, args, account_id=account_id
        )

    async def ticket_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshserviceTicketGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single support ticket by its numeric ID, with optional embedded resources such as requester details, conversation stats, or custom fields. The full payload includes descriptions, attachments, and audit trail data. Use this when the agent already knows the ticket ID and needs the complete record for context or reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshservice.ticket_get", owner_id, args, account_id=account_id
        )

    async def ticket_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshserviceTicketUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more fields of an existing Freshservice ticket by its numeric ID. Status, priority, group assignment, responder, and tags can all be modified in a single call. Use this to triage tickets: reassign to the right team, escalate urgency, close resolved issues, or synchronize ticket state from external systems. Changes are logged in the ticket's audit trail and visible to all agents.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshservice.ticket_update", owner_id, args, account_id=account_id
        )

    async def tickets_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshserviceTicketsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of support tickets from Freshservice, with optional filters by requester, email, status filter view, sort order, and date range (defaults to last 30 days). Each ticket includes its subject, status, priority, assignee, and timestamps. The agent should use this to discover tickets matching specific criteria before taking further actions such as updating or viewing details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshservice.tickets_list", owner_id, args, account_id=account_id
        )
