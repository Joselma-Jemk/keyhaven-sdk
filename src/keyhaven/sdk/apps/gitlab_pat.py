# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `gitlab_pat` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GitlabPatGroupsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabPatGroupsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabPatGroupsGetPath


class GitlabPatGroupsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    statistics: bool | None = None
    all_available: bool | None = None
    search: str | None = None
    order_by: str | None = None
    sort: str | None = None
    page: int | None = None
    per_page: int | None = None


class GitlabPatGroupsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GitlabPatGroupsListQuery | None = None


class GitlabPatInternalCheckListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class GitlabPatProjectsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class GitlabPatProjectsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GitlabPatProjectsGetPath


class GitlabPatProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order_by: str | None = None
    sort: str | None = None
    archived: bool | None = None
    visibility: str | None = None
    search: str | None = None
    page: int | None = None
    per_page: int | None = None
    simple: bool | None = None


class GitlabPatProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GitlabPatProjectsListQuery | None = None


class GitlabPatNamespace:
    """Typed accessor for the `gitlab_pat` app. Reach it via `client.apps.gitlab_pat`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def groups_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabPatGroupsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a single GitLab group, including its description, visibility level, avatar URL, web URL, and nested projects within the group. Use this to inspect a group's configuration and identify which projects belong to it. Requires the group's ID obtained from the list endpoint.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab_pat.groups_get", owner_id, args, account_id=account_id
        )

    async def groups_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabPatGroupsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all GitLab groups accessible by the authenticated user's personal access token, with detailed metadata including visibility, description, statistics, and project counts. Supports searching, filtering by availability, ordering, and pagination. Use this to discover group IDs and properties before inspecting a specific group's details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab_pat.groups_list", owner_id, args, account_id=account_id
        )

    async def internal_check_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabPatInternalCheckListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Pings the GitLab internal health check endpoint to verify API connectivity and return server status information. Use this to test whether the GitLab instance is reachable and the personal access token is valid before executing other operations. This is a lightweight connectivity check with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab_pat.internal_check_list", owner_id, args, account_id=account_id
        )

    async def projects_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabPatProjectsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about one GitLab project, including its repository URL, default branch, description, visibility level, namespace, owner, and CI/CD configuration settings. Use this to inspect a project's metadata and configuration before cloning, creating merge requests, or managing pipelines. Requires the project's ID.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab_pat.projects_get", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GitlabPatProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all GitLab projects accessible by the authenticated user, with support for searching by name, filtering by visibility level or archive status, and ordering by various fields. Optionally returns simplified project objects to reduce payload size. Use this to discover project IDs, URLs, and metadata before performing repository or CI/CD operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gitlab_pat.projects_list", owner_id, args, account_id=account_id
        )
