# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `mailchimp` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MailchimpCampaignCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(alias="type")
    recipients: dict[str, Any] | None = None
    settings: dict[str, Any] | None = None
    variate_settings: dict[str, Any] | None = None
    tracking: dict[str, Any] | None = None
    rss_opts: dict[str, Any] | None = None
    social_card: dict[str, Any] | None = None
    content_type: str | None = None


class MailchimpCampaignCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MailchimpCampaignCreateBody


class MailchimpCampaignsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    exclude_fields: str | None = None
    count: int | None = None
    offset: int | None = None
    type_: str | None = Field(default=None, alias="type")
    status: str | None = None
    list_id: str | None = None
    since_send_time: str | None = None
    before_send_time: str | None = None
    sort_field: str | None = None
    sort_dir: str | None = None


class MailchimpCampaignsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MailchimpCampaignsListQuery | None = None


class MailchimpListGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list_id: str


class MailchimpListGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    exclude_fields: str | None = None
    include_total_contacts: bool | None = None


class MailchimpListGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailchimpListGetPath
    query: MailchimpListGetQuery | None = None


class MailchimpListMemberAddPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list_id: str


class MailchimpListMemberAddQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    skip_merge_validation: bool | None = None


class MailchimpListMemberAddBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email_address: str
    status: str
    email_type: str | None = None
    merge_fields: dict[str, Any] | None = None
    interests: dict[str, Any] | None = None
    language: str | None = None
    vip: bool | None = None
    tags: list[Any] | None = None


class MailchimpListMemberAddRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailchimpListMemberAddPath
    query: MailchimpListMemberAddQuery | None = None
    body: MailchimpListMemberAddBody


class MailchimpListMemberUpsertPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list_id: str
    subscriber_hash: str


class MailchimpListMemberUpsertQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    skip_merge_validation: bool | None = None


class MailchimpListMemberUpsertBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email_address: str
    status_if_new: str | None = None
    status: str | None = None
    email_type: str | None = None
    merge_fields: dict[str, Any] | None = None
    interests: dict[str, Any] | None = None
    language: str | None = None
    vip: bool | None = None


class MailchimpListMemberUpsertRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailchimpListMemberUpsertPath
    query: MailchimpListMemberUpsertQuery | None = None
    body: MailchimpListMemberUpsertBody


class MailchimpListMembersListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list_id: str


class MailchimpListMembersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    exclude_fields: str | None = None
    count: int | None = None
    offset: int | None = None
    status: str | None = None
    since_timestamp_opt: str | None = None
    before_timestamp_opt: str | None = None
    since_last_changed: str | None = None


class MailchimpListMembersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailchimpListMembersListPath
    query: MailchimpListMembersListQuery | None = None


class MailchimpListsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    exclude_fields: str | None = None
    count: int | None = None
    offset: int | None = None
    email: str | None = None
    before_date_created: str | None = None
    since_date_created: str | None = None
    sort_field: str | None = None
    sort_dir: str | None = None


class MailchimpListsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MailchimpListsListQuery | None = None


class MailchimpNamespace:
    """Typed accessor for the `mailchimp` app. Reach it via `client.apps.mailchimp`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def campaign_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailchimpCampaignCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new email campaign draft with the specified type, recipient audience, settings (subject, from name, reply-to), tracking options, and content type. The campaign is created in a draft (save) state and does not send any emails until explicitly sent or scheduled later. Use this when an agent needs to set up a campaign structure that will later be populated with content and sent. This action is reversible by deleting the draft.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailchimp.campaign_create", owner_id, args, account_id=account_id
        )

    async def campaigns_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailchimpCampaignsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all email campaigns in the Mailchimp account, filterable by type (regular, plaintext, RSS, etc.), status (save, sent, scheduled, etc.), audience, and send time range. Use this when an agent needs to view campaign history, find a specific campaign, or analyse past sends. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailchimp.campaigns_list", owner_id, args, account_id=account_id
        )

    async def list_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailchimpListGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a single audience (list) identified by its unique ID, including stats like member count and campaign defaults. Use this when an agent needs metadata about a specific audience, such as its name, contact settings, or subscribe notifications. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailchimp.list_get", owner_id, args, account_id=account_id
        )

    async def list_member_add(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailchimpListMemberAddRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new subscriber to a specific audience with the given email address and status (subscribed, pending, etc.), along with optional merge fields, interests, tags, and language preference. Once added, the subscriber may start receiving campaign emails based on their status. Use this when a user signs up or is imported into an audience. This action affects the audience composition and may trigger confirmation emails depending on the list settings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailchimp.list_member_add", owner_id, args, account_id=account_id
        )

    async def list_member_upsert(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailchimpListMemberUpsertRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds or updates an audience member identified by the MD5 hash of their lowercase email address. If the member does not exist, they are created with the status specified in `status_if_new`; if they exist, their profile is updated with the provided fields. Use this to synchronise subscriber data from an external source or to update subscriber properties without risking a duplicate. This action changes the subscriber's data and status in the audience.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailchimp.list_member_upsert", owner_id, args, account_id=account_id
        )

    async def list_members_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailchimpListMembersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of subscribers (members) of a specific audience, with optional filters for status, opt-in timestamps, and last changed date. The response includes email addresses, merge fields, and subscription status for each member. Use this when an agent needs to inspect an audience's subscriber base, verify a subscription, or export contact data. This operation is read-only but exposes personal information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailchimp.list_members_list", owner_id, args, account_id=account_id
        )

    async def lists_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailchimpListsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all audiences (lists) in the Mailchimp account, with optional filters for creation date, email address, sort order, and field selection. Use this when an agent needs to discover available audiences, verify an audience exists, or populate a list picker for other operations. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailchimp.lists_list", owner_id, args, account_id=account_id
        )
