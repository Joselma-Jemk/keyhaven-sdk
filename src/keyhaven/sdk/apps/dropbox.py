# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `dropbox` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DropboxFilesCreateFolderBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: str
    autorename: bool | None = None


class DropboxFilesCreateFolderRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxFilesCreateFolderBody


class DropboxFilesDeleteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: str
    parent_rev: str | None = None


class DropboxFilesDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxFilesDeleteBody


class DropboxFilesGetMetadataBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: str
    include_media_info: bool | None = None
    include_deleted: bool | None = None
    include_has_explicit_shared_members: bool | None = None


class DropboxFilesGetMetadataRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxFilesGetMetadataBody


class DropboxFilesListFolderBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: str
    recursive: bool | None = None
    include_media_info: bool | None = None
    include_deleted: bool | None = None
    include_has_explicit_shared_members: bool | None = None
    include_mounted_folders: bool | None = None
    limit: int | None = None


class DropboxFilesListFolderRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxFilesListFolderBody


class DropboxFilesMoveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_path: str
    to_path: str
    allow_shared_folder: bool | None = None
    autorename: bool | None = None
    allow_ownership_transfer: bool | None = None


class DropboxFilesMoveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxFilesMoveBody


class DropboxFilesSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    options: dict[str, Any] | None = None
    match_field_options: dict[str, Any] | None = None
    include_highlights: bool | None = None


class DropboxFilesSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DropboxFilesSearchBody


class DropboxNamespace:
    """Typed accessor for the `dropbox` app. Reach it via `client.apps.dropbox`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def files_create_folder(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxFilesCreateFolderRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new folder at the specified path. The folder is immediately visible in Dropbox. If a folder or file already exists at the path and autorename is enabled, the new folder gets a variant suffix instead of failing. Use this to organise content before uploading files into the new folder.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox.files_create_folder", owner_id, args, account_id=account_id
        )

    async def files_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxFilesDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Delete the file or folder at the given path. If the path points to a folder, all of its contents are deleted recursively. The deleted items are moved to the Dropbox trash where they can be restored within 30 days via the web interface or API. Use with extreme caution as this permanently removes organisational content from active storage.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox.files_delete", owner_id, args, account_id=account_id
        )

    async def files_get_metadata(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxFilesGetMetadataRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Return metadata for a file or folder at the given path, including size, modification time, and sharing information. Optionally includes deleted-item metadata and media info. Use this to inspect a specific item's properties before deciding whether to download, move, or delete it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox.files_get_metadata", owner_id, args, account_id=account_id
        )

    async def files_list_folder(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxFilesListFolderRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List the contents of a Dropbox folder at the specified path, with optional recursive traversal, deleted entries, and shared folder inclusion. Returns file and folder metadata including names, sizes, and modification times. Use this to browse the file tree before reading, downloading, or moving files.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox.files_list_folder", owner_id, args, account_id=account_id
        )

    async def files_move(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxFilesMoveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Move or rename a file or folder from one path to another within the same Dropbox account. Can move items across folders but not across different accounts. If the destination path is already in use and autorename is enabled, the moved item gets a variant suffix. Use to reorganise the file hierarchy.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox.files_move", owner_id, args, account_id=account_id
        )

    async def files_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DropboxFilesSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search files and folders in Dropbox by query string with optional filters for path, file status, file extensions, categories, and result ordering. Returns matching items with metadata and optional match highlights. Use this to find files by name or content before performing operations on them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dropbox.files_search", owner_id, args, account_id=account_id
        )
