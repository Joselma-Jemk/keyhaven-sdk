# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `podium` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PodiumCampaignsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    limit: int = 25
    location_id: str | None = None


class PodiumCampaignsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PodiumCampaignsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PodiumCampaignsListQuery | None = None
    header: PodiumCampaignsListHeader = Field(default_factory=PodiumCampaignsListHeader)


class PodiumContactCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PodiumContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    phone: str | None = None
    email: str | None = None
    location_id: str | None = None
    source: str | None = None


class PodiumContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PodiumContactCreateHeader = Field(default_factory=PodiumContactCreateHeader)
    body: PodiumContactCreateBody


class PodiumContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    limit: int = 25
    location_id: str | None = None


class PodiumContactsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PodiumContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PodiumContactsListQuery | None = None
    header: PodiumContactsListHeader = Field(default_factory=PodiumContactsListHeader)


class PodiumConversationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    limit: int = 25
    location_id: str | None = None


class PodiumConversationsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PodiumConversationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PodiumConversationsListQuery | None = None
    header: PodiumConversationsListHeader = Field(default_factory=PodiumConversationsListHeader)


class PodiumLocationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    limit: int = 25


class PodiumLocationsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PodiumLocationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PodiumLocationsListQuery | None = None
    header: PodiumLocationsListHeader = Field(default_factory=PodiumLocationsListHeader)


class PodiumMessageSendHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class PodiumMessageSendBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    conversation_id: str
    text: str
    message_type: str | None = None


class PodiumMessageSendRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PodiumMessageSendHeader = Field(default_factory=PodiumMessageSendHeader)
    body: PodiumMessageSendBody


class PodiumNamespace:
    """Typed accessor for the `podium` app. Reach it via `client.apps.podium`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def campaigns_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PodiumCampaignsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of marketing campaigns for the Podium organization, optionally filtered by location. Each campaign includes its name, status, type, and performance metrics. Use this to review active or past campaigns before creating a new one or analyzing results. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "podium.campaigns_list", owner_id, args, account_id=account_id
        )

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PodiumContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer contact in the Podium organization with a name, phone number, email, and location association. The contact immediately appears in the contact directory and becomes available for conversations and campaign targeting. Use this to add a customer record after an offline interaction or data import. This action is reversible by deleting the contact.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "podium.contact_create", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PodiumContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of customer contacts across the Podium organization, optionally filtered by location. Each contact record includes personal information such as name, phone number, and email address. Use this to look up customer details or find contact IDs for targeting messages or campaigns. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "podium.contacts_list", owner_id, args, account_id=account_id
        )

    async def conversations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PodiumConversationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of customer conversations across the Podium organization, optionally filtered by location. Each conversation includes metadata such as participant details and latest activity timestamp. Use this to review recent customer interactions or find a conversation ID before sending a reply. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "podium.conversations_list", owner_id, args, account_id=account_id
        )

    async def locations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PodiumLocationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of all business locations associated with the authenticated Podium organization, including address, phone, and status information. Use this to discover available location IDs before querying conversations, contacts, or campaigns for a specific site. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "podium.locations_list", owner_id, args, account_id=account_id
        )

    async def message_send(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PodiumMessageSendRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends a text message to an existing customer conversation over a specified channel (SMS or webchat). The message is immediately delivered to the customer and appears in the conversation history on both sides. Use this to reply to a customer inquiry or send a proactive notification. This action cannot be recalled after delivery.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "podium.message_send", owner_id, args, account_id=account_id
        )
