# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `active_campaign` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ActiveCampaignCreateContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_name: str


class ActiveCampaignCreateContactHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ActiveCampaignCreateContactBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact: dict[str, Any]


class ActiveCampaignCreateContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ActiveCampaignCreateContactPath
    header: ActiveCampaignCreateContactHeader = Field(
        default_factory=ActiveCampaignCreateContactHeader
    )
    body: ActiveCampaignCreateContactBody


class ActiveCampaignCreateListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_name: str


class ActiveCampaignCreateListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ActiveCampaignCreateListBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list: dict[str, Any]


class ActiveCampaignCreateListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ActiveCampaignCreateListPath
    header: ActiveCampaignCreateListHeader = Field(default_factory=ActiveCampaignCreateListHeader)
    body: ActiveCampaignCreateListBody


class ActiveCampaignRetrieveContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_name: str
    contact_id: str


class ActiveCampaignRetrieveContactHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ActiveCampaignRetrieveContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ActiveCampaignRetrieveContactPath
    header: ActiveCampaignRetrieveContactHeader = Field(
        default_factory=ActiveCampaignRetrieveContactHeader
    )


class ActiveCampaignUpdateContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_name: str
    contact_id: str


class ActiveCampaignUpdateContactHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ActiveCampaignUpdateContactBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact: dict[str, Any]


class ActiveCampaignUpdateContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ActiveCampaignUpdateContactPath
    header: ActiveCampaignUpdateContactHeader = Field(
        default_factory=ActiveCampaignUpdateContactHeader
    )
    body: ActiveCampaignUpdateContactBody


class ActiveCampaignUpdateContactListStatusPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    account_name: str


class ActiveCampaignUpdateContactListStatusHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ActiveCampaignUpdateContactListStatusBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contactList: dict[str, Any]


class ActiveCampaignUpdateContactListStatusRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ActiveCampaignUpdateContactListStatusPath
    header: ActiveCampaignUpdateContactListStatusHeader = Field(
        default_factory=ActiveCampaignUpdateContactListStatusHeader
    )
    body: ActiveCampaignUpdateContactListStatusBody


class ActiveCampaignNamespace:
    """Typed accessor for the `active_campaign` app. Reach it via `client.apps.active_campaign`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ActiveCampaignCreateContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new person record in the ActiveCampaign CRM, storing details like email, name, phone, and custom field values. The contact becomes available for segmentation, automation triggers, campaigns, and sales pipelines across the platform. Use this after a signup, manual entry, or import to add a new contact to the database.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "active_campaign.create_contact", owner_id, args, account_id=account_id
        )

    async def create_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ActiveCampaignCreateListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new mailing list in ActiveCampaign with sender identity details — including name, string ID, sender URL, sender reminder, and physical address — that appear in email footers for legal compliance. Once created, contacts can be subscribed to this list to receive campaign emails. Use this when setting up a new audience segment for email marketing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "active_campaign.create_list", owner_id, args, account_id=account_id
        )

    async def retrieve_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ActiveCampaignRetrieveContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single contact's full profile from ActiveCampaign by its unique ID, including email, name, phone, custom fields, and associated metadata. This is a read-only operation with no side effects in the system. Use this to inspect existing contact data before performing an update or to gather information for personalization.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "active_campaign.retrieve_contact", owner_id, args, account_id=account_id
        )

    async def update_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ActiveCampaignUpdateContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing contact's profile fields in ActiveCampaign — such as email, name, phone, or custom field values — identified by contact ID. Changes take effect immediately across automations, segments, and campaigns. Use this when a contact's information needs correction, enrichment, or synchronization from another system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "active_campaign.update_contact", owner_id, args, account_id=account_id
        )

    async def update_contact_list_status(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ActiveCampaignUpdateContactListStatusRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates a contact's subscription status on a specific mailing list — status 1 subscribes the contact (active) and status 2 unsubscribes them. This directly controls whether the contact receives campaign emails sent to that list. Use this to opt contacts into or out of specific email communications based on their consent or preferences.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "active_campaign.update_contact_list_status", owner_id, args, account_id=account_id
        )
