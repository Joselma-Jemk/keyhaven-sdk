# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `youtube` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class YoutubeGetVideoCategoriesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    part: Literal["snippet"] = "snippet"
    id: str | None = None
    regionCode: str | None = None
    hl: str = "en_US"


class YoutubeGetVideoCategoriesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: YoutubeGetVideoCategoriesQuery = Field(default_factory=YoutubeGetVideoCategoriesQuery)


class YoutubeGetVideoDetailsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    part: Literal[
        "contentDetails",
        "fileDetails",
        "id",
        "liveStreamingDetails",
        "localizations",
        "paidProductPlacementDetails",
        "player",
        "processingDetails",
        "recordingDetails",
        "snippet",
        "statistics",
        "status",
        "suggestions",
        "topicDetails",
    ] = "snippet"
    id: str | None = None
    chart: Literal["mostPopular"] = "mostPopular"
    myRating: Literal["like", "dislike"] | None = None
    maxHeight: int | None = None
    maxWidth: int | None = None
    maxResults: int = 5
    pageToken: str | None = None
    regionCode: str | None = None
    videoCategoryId: str = "0"
    hl: str = "en_US"
    onBehalfOfContentOwner: str | None = None


class YoutubeGetVideoDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: YoutubeGetVideoDetailsQuery = Field(default_factory=YoutubeGetVideoDetailsQuery)


class YoutubeRateVideoQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str
    rating: Literal["like", "dislike", "none"] = "like"


class YoutubeRateVideoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: YoutubeRateVideoQuery


class YoutubeSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    part: Literal["id", "snippet"] = "snippet"
    forContentOwner: bool | None = None
    forDeveloper: bool | None = None
    forMine: bool | None = None
    q: str | None = None
    maxResults: int = 5
    pageToken: str | None = None
    type_: Literal[
        "video",
        "channel",
        "playlist",
        "video,channel",
        "video,playlist",
        "channel,playlist",
        "video,channel,playlist",
    ] = Field(default="video,channel,playlist", alias="type")
    videoDefinition: Literal["any", "high", "standard"] = "any"
    videoCategoryId: str | None = None
    order: Literal["date", "rating", "relevance", "title", "videoCount", "viewCount"] = "relevance"
    channelId: str | None = None
    channelType: Literal["any", "show"] = "any"
    eventType: Literal["completed", "live", "upcoming"] | None = None
    location: str | None = None
    locationRadius: str | None = None
    publishedAfter: str | None = None
    publishedBefore: str | None = None
    regionCode: str = "US"
    relevanceLanguage: str | None = None
    safeSearch: Literal["moderate", "none", "strict"] = "moderate"
    videoCaption: Literal["any", "closedCaption", "none"] = "any"
    videoDimension: Literal["2d", "3d", "any"] = "any"
    videoDuration: Literal["any", "long", "medium", "short"] = "any"
    videoEmbeddable: Literal["any", "true"] = "any"
    videoLicense: Literal["any", "creativeCommon", "youtube"] = "any"
    videoSyndicated: Literal["any", "true"] = "any"
    videoType: Literal["any", "episode", "movie"] = "any"


class YoutubeSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: YoutubeSearchQuery = Field(default_factory=YoutubeSearchQuery)


class YoutubeNamespace:
    """Typed accessor for the `youtube` app. Reach it via `client.apps.youtube`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_video_categories(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: YoutubeGetVideoCategoriesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists the video categories available on YouTube, optionally filtered by region or specific category ID, returning localized names and assignable category IDs. This is a read-only lookup that does not affect any resources. Use this action to populate category selection menus or to obtain the correct category ID before uploading or updating a video's metadata.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "youtube.get_video_categories", owner_id, args, account_id=account_id
        )

    async def get_video_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: YoutubeGetVideoDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves comprehensive metadata for one or more specific YouTube videos by their IDs, including statistics (views, likes, comments), content details (duration, definition, captions), status (privacy, embeddability), and snippet information (title, description, tags). This read-only operation fetches the full video resource but makes no changes. Use this action after identifying relevant video IDs (e.g., from a search) to obtain detailed data for analysis, display, or decision-making.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "youtube.get_video_details", owner_id, args, account_id=account_id
        )

    async def rate_video(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: YoutubeRateVideoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Records a rating (like, dislike, or none) on a specified YouTube video on behalf of the authenticated user, updating the video's public like/dislike count accordingly. Setting rating to 'none' removes any previously recorded rating without adding a new one — this action is reversible since you can change or remove the rating at any time, but the like/dislike is immediately visible to the public. Use this action when you need to express user preference for a video or clear a previous rating.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "youtube.rate_video", owner_id, args, account_id=account_id
        )

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: YoutubeSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches YouTube's content index for videos, channels, and playlists matching specified criteria such as keywords, region, language, publication date, content type, and quality filters. Returns a paginated list of search results with metadata like titles, descriptions, thumbnails, and channel information — it does not modify any resources. Use this action when you need to discover relevant YouTube content before retrieving full details or performing further operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("youtube.search", owner_id, args, account_id=account_id)
