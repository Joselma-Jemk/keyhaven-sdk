# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `procore` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ProcoreCompaniesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    include_free_companies: bool | None = None


class ProcoreCompaniesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ProcoreCompaniesListQuery | None = None


class ProcoreCompanyGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: str


class ProcoreCompanyGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ProcoreCompanyGetPath


class ProcoreProjectCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: str


class ProcoreProjectCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Procore_Company_Id: str | None = Field(default=None, alias="Procore-Company-Id")


class ProcoreProjectCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    display_name: str | None = None
    project_number: str | None = None
    address: str | None = None
    city: str | None = None
    state_code: str | None = None
    country_code: str | None = None
    zip: str | None = None
    start_date: str | None = None
    completion_date: str | None = None


class ProcoreProjectCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ProcoreProjectCreatePath
    header: ProcoreProjectCreateHeader | None = None
    body: ProcoreProjectCreateBody


class ProcoreProjectGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class ProcoreProjectGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Procore_Company_Id: str | None = Field(default=None, alias="Procore-Company-Id")


class ProcoreProjectGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ProcoreProjectGetPath
    header: ProcoreProjectGetHeader | None = None


class ProcoreProjectsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: str


class ProcoreProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    filters_stage_: str | None = Field(default=None, alias="filters[stage]")
    filters_status_: str | None = Field(default=None, alias="filters[status]")
    sort: str | None = None


class ProcoreProjectsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Procore_Company_Id: str | None = Field(default=None, alias="Procore-Company-Id")


class ProcoreProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ProcoreProjectsListPath
    query: ProcoreProjectsListQuery | None = None
    header: ProcoreProjectsListHeader | None = None


class ProcoreUsersListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: str


class ProcoreUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    filters_search_: str | None = Field(default=None, alias="filters[search]")


class ProcoreUsersListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Procore_Company_Id: str | None = Field(default=None, alias="Procore-Company-Id")


class ProcoreUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ProcoreUsersListPath
    query: ProcoreUsersListQuery | None = None
    header: ProcoreUsersListHeader | None = None


class ProcoreNamespace:
    """Typed accessor for the `procore` app. Reach it via `client.apps.procore`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def companies_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProcoreCompaniesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Procore companies visible to the authenticated user, with optional pagination and a flag to include free-tier companies. Each company represents a construction firm with its own projects, users, and directory. Use this to discover available company IDs before accessing projects or users. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "procore.companies_list", owner_id, args, account_id=account_id
        )

    async def company_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProcoreCompanyGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a single Procore company by its ID, including its name, address, and account settings. Use this to verify a company's details or inspect its configuration before performing company-scoped operations. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "procore.company_get", owner_id, args, account_id=account_id
        )

    async def project_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProcoreProjectCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new construction project within a Procore company with a name, address, dates, and project number. The project immediately appears in the company's project portfolio and becomes accessible to team members with appropriate permissions. Use this to onboard a new construction project in the system. This action is reversible by deactivating or archiving the project.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "procore.project_create", owner_id, args, account_id=account_id
        )

    async def project_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProcoreProjectGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a single Procore project by its ID, including its name, address, start and completion dates, and current status. Use this to inspect a project's full metadata before managing RFIs, submittals, observations, or other project-specific resources. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "procore.project_get", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProcoreProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of construction projects within a Procore company, with optional filters for stage (Active, Completed, Pending) and status, plus sorting. Each project includes its name, stage, and status. Use this to browse the project portfolio or locate a project ID before performing project-level operations. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "procore.projects_list", owner_id, args, account_id=account_id
        )

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProcoreUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of users in a Procore company directory, with optional search by name or email. Each user record includes name, email address, and permission role. Use this to find user IDs for assigning project roles or reviewing team composition. No data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "procore.users_list", owner_id, args, account_id=account_id
        )
