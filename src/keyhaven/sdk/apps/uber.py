# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `uber` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class UberTripCancelPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    trip_id: str


class UberTripCancelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UberTripCancelPath


class UberTripCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    guest: dict[str, Any]
    pickup: dict[str, Any]
    dropoff: dict[str, Any]
    product_id: str
    fare_id: str | None = None
    scheduling: dict[str, Any] | None = None
    hourly: dict[str, Any] | None = None


class UberTripCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: UberTripCreateBody


class UberTripGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    trip_id: str


class UberTripGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UberTripGetPath


class UberTripReceiptGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    trip_id: str


class UberTripReceiptGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UberTripReceiptGetPath


class UberTripUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    trip_id: str


class UberTripUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pickup: dict[str, Any] | None = None
    dropoff: dict[str, Any] | None = None
    stops: list[Any] | None = None
    product_id: str


class UberTripUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: UberTripUpdatePath
    body: UberTripUpdateBody


class UberNamespace:
    """Typed accessor for the `uber` app. Reach it via `client.apps.uber`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def trip_cancel(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UberTripCancelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Cancels an active guest trip by its trip ID, which stops the ride request, notifies the driver, and may trigger a cancellation fee depending on timing and Uber's policy. This action is irreversible once processed and the trip cannot be resumed. Use this when a rider no longer needs the ride and before the driver has arrived or the trip has begun.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("uber.trip_cancel", owner_id, args, account_id=account_id)

    async def trip_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UberTripCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Books a new ride for a guest rider by specifying pickup and dropoff locations, rider contact information (name, email, phone), and ride type (product_id). Supports on-demand trips, future scheduling via a pickup timestamp, and hourly rentals with duration and distance limits. This action dispatches a driver to the pickup location and commits the rider to potential charges, so use it only with confirmed rider intent and validated location data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("uber.trip_create", owner_id, args, account_id=account_id)

    async def trip_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UberTripGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the current state of a guest trip by its trip ID, including the trip status (e.g., accepted, arrived, in_progress, completed), pickup and dropoff locations, driver details (name, rating, vehicle make and model), and estimated time of arrival. The data is read-only and reflects real-time trip progress. Use this to track a ride's status, verify that a booking was successful, or check driver assignment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("uber.trip_get", owner_id, args, account_id=account_id)

    async def trip_receipt_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UberTripReceiptGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the itemized receipt for a completed guest trip, including base fare, distance and time charges, surge pricing, tips, tolls, taxes, and the total amount charged to the rider. This is read-only financial data tied to a finished ride. Use this to review charges for billing verification, expense reporting, generate invoices, or respond to customer support inquiries about trip costs.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "uber.trip_receipt_get", owner_id, args, account_id=account_id
        )

    async def trip_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: UberTripUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies a currently active guest trip's pickup location, dropoff location, or ordered list of intermediate stops. The ride must not yet be in a final state (completed or cancelled) for the update to apply. Changes are reflected in real time in both the driver's navigation and the rider's trip details. Use this to correct an incorrect location, change the destination mid-trip, or add waypoints before the trip concludes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("uber.trip_update", owner_id, args, account_id=account_id)
