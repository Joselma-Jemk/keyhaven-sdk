# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `close` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CloseContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    lead_id: str
    name: str | None = None
    first_name: str | None = None
    last_name: str | None = None
    title: str | None = None
    emails: list[Any] | None = None
    phones: list[Any] | None = None


class CloseContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CloseContactCreateBody


class CloseContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    sort: str | None = None
    filter: str | None = None


class CloseContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CloseContactsListQuery | None = None


class CloseLeadCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None
    url: str | None = None
    status: str | None = None
    status_id: str | None = None
    contacts: list[Any] | None = None
    custom: dict[str, Any] | None = None


class CloseLeadCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CloseLeadCreateBody = Field(default_factory=CloseLeadCreateBody)


class CloseLeadUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class CloseLeadUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None
    status: str | None = None
    status_id: str | None = None
    custom: dict[str, Any] | None = None


class CloseLeadUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloseLeadUpdatePath
    body: CloseLeadUpdateBody = Field(default_factory=CloseLeadUpdateBody)


class CloseLeadsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = Field(default=None, alias="_limit")
    offset: int | None = Field(default=None, alias="_offset")
    fields: str | None = Field(default=None, alias="_fields")
    query: str | None = None


class CloseLeadsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CloseLeadsListQuery | None = None


class CloseOpportunitiesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = Field(default=None, alias="_limit")
    offset: int | None = Field(default=None, alias="_offset")
    order_by: str | None = Field(default=None, alias="_order_by")


class CloseOpportunitiesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CloseOpportunitiesListQuery | None = None


class CloseNamespace:
    """Typed accessor for the `close` app. Reach it via `client.apps.close`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloseContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new contact record and associate it with an existing lead in Close CRM, providing names, job title, email addresses, and phone numbers. This adds a person entry linked to a prospect account, making them reachable for calls and emails by the sales team. Use when onboarding a new stakeholder at a prospect company or adding decision-makers to a deal.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "close.contact_create", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloseContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of contact records from Close CRM, with optional filtering, sorting, and pagination controls. Returns contact details including names, email addresses, phone numbers, job titles, and the associated lead ID. Use when you need to find a specific person, browse contacts for outreach, or export data for analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "close.contacts_list", owner_id, args, account_id=account_id
        )

    async def lead_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloseLeadCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new lead record in Close CRM with a name, optional description, URL, status assignment, associated contacts, and custom field values. This immediately adds a visible prospect to the CRM pipeline that sales team members can view, qualify, and progress through stages. Use when capturing a new prospect from a form, import, or external source to begin the sales process.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "close.lead_create", owner_id, args, account_id=account_id
        )

    async def lead_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloseLeadUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Update one or more fields of an existing lead in Close CRM by its unique ID, such as name, description, status, or custom data. Changes reflect immediately in the CRM and are visible to all team members. Use when a lead progresses through pipeline stages, its contact details change, or custom field data needs correction.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "close.lead_update", owner_id, args, account_id=account_id
        )

    async def leads_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloseLeadsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of lead records from Close CRM, with optional search query, field filtering, and pagination controls. Returns leads with their contact info, status labels, custom fields, and associated metadata across all pipeline stages. Use when you need to browse, search, or export lead data for analysis, reporting, or to find a specific lead before performing an update.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("close.leads_list", owner_id, args, account_id=account_id)

    async def opportunities_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloseOpportunitiesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of sales opportunities (deals) from Close CRM, with optional pagination and ordering controls. Each opportunity includes its monetary value, pipeline stage assignment, associated lead, and expected close date. Use to review pipeline health, forecast revenue, find specific deals to update, or export opportunity data for reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "close.opportunities_list", owner_id, args, account_id=account_id
        )
