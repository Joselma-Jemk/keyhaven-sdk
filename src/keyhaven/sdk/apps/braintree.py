# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `braintree` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BraintreeCustomerCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    merchant_id: str


class BraintreeCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    firstName: str
    lastName: str
    email: str | None = None
    phone: str | None = None
    paymentMethodNonce: str | None = None
    customFields: dict[str, Any] | None = None


class BraintreeCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BraintreeCustomerCreatePath
    body: BraintreeCustomerCreateBody


class BraintreeCustomerFindPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    merchant_id: str
    id: str


class BraintreeCustomerFindRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BraintreeCustomerFindPath


class BraintreePaymentMethodCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    merchant_id: str


class BraintreePaymentMethodCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customerId: str
    paymentMethodNonce: str
    options: dict[str, Any] | None = None


class BraintreePaymentMethodCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BraintreePaymentMethodCreatePath
    body: BraintreePaymentMethodCreateBody


class BraintreeSubscriptionUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    merchant_id: str
    id: str


class BraintreeSubscriptionUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    paymentMethodToken: str | None = None
    price: str | None = None
    planId: str | None = None
    merchantAccountId: str | None = None


class BraintreeSubscriptionUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BraintreeSubscriptionUpdatePath
    body: BraintreeSubscriptionUpdateBody | None = None


class BraintreeTransactionSalePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    merchant_id: str


class BraintreeTransactionSaleBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    amount: str
    paymentMethodNonce: str | None = None
    customerId: str | None = None
    merchantAccountId: str | None = None
    options: dict[str, Any] | None = None


class BraintreeTransactionSaleRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BraintreeTransactionSalePath
    body: BraintreeTransactionSaleBody


class BraintreeNamespace:
    """Typed accessor for the `braintree` app. Reach it via `client.apps.braintree`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BraintreeCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer record in the Braintree Vault, storing profile details such as name, email, phone, and optionally an initial payment method. The customer becomes available for future transactions, subscriptions, and vault lookups. Use when you need to register a new buyer before processing payments or creating subscriptions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braintree.customer_create", owner_id, args, account_id=account_id
        )

    async def customer_find(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BraintreeCustomerFindRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a customer's profile from the Braintree Vault by their unique customer ID, returning stored details such as name, email, phone, and associated payment methods. This operation does not modify any data. Use when you need to look up customer information or verify their existence before performing transactions or other operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braintree.customer_find", owner_id, args, account_id=account_id
        )

    async def payment_method_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BraintreePaymentMethodCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Vaults a new payment method (credit card, PayPal, etc.) and attaches it to an existing customer record using a payment method nonce from the client SDK. Once stored, the payment method can be reused for future transactions without collecting sensitive details again. Use when a customer provides new payment credentials that need to be saved for recurring or future purchases.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braintree.payment_method_create", owner_id, args, account_id=account_id
        )

    async def subscription_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BraintreeSubscriptionUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing recurring subscription's parameters such as price, plan ID, payment method, or merchant account. Changes take effect on future billing cycles and alter what the customer is charged going forward. Use when a customer changes plans, their payment method expires, or pricing needs adjustment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braintree.subscription_update", owner_id, args, account_id=account_id
        )

    async def transaction_sale(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BraintreeTransactionSaleRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates and processes a financial sale transaction through Braintree, charging the specified amount to the provided payment method or a vaulted customer. Funds are captured from the buyer and queued for settlement to the merchant account — this moves real money. Use when you need to charge a customer for a purchase, one-time payment, or invoice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "braintree.transaction_sale", owner_id, args, account_id=account_id
        )
