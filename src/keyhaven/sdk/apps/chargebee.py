# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `chargebee` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ChargebeeCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str | None = None
    last_name: str | None = None
    email: str | None = None
    phone: str | None = None
    company: str | None = None
    auto_collection: str | None = None
    preferred_currency_code: str | None = None
    locale: str | None = None


class ChargebeeCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ChargebeeCustomerCreateBody = Field(default_factory=ChargebeeCustomerCreateBody)


class ChargebeeCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: str | None = None


class ChargebeeCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ChargebeeCustomersListQuery | None = None


class ChargebeeInvoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: str | None = None
    customer_id: str | None = None


class ChargebeeInvoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ChargebeeInvoicesListQuery | None = None


class ChargebeeSubscriptionCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer_id: str


class ChargebeeSubscriptionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    plan_id: str | None = None
    subscription_items: list[Any] | None = None
    trial_end: int | None = None
    billing_cycles: int | None = None
    invoice_immediately: bool | None = None


class ChargebeeSubscriptionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ChargebeeSubscriptionCreatePath
    body: ChargebeeSubscriptionCreateBody = Field(default_factory=ChargebeeSubscriptionCreateBody)


class ChargebeeSubscriptionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: str | None = None
    item_id: str | None = None


class ChargebeeSubscriptionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ChargebeeSubscriptionsListQuery | None = None


class ChargebeeNamespace:
    """Typed accessor for the `chargebee` app. Reach it via `client.apps.chargebee`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ChargebeeCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer record in Chargebee with billing details such as name, email, phone, company, preferred currency, and locale. This establishes a billing entity in the system but does not create a subscription or charge. Use this when onboarding a new customer who will later be subscribed to a plan.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "chargebee.customer_create", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ChargebeeCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of customers from Chargebee, with optional limit and offset for pagination. Customer records contain billing contact information such as name, email, and company. Use this to browse your customer base, find a customer by reference, or export customer data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "chargebee.customers_list", owner_id, args, account_id=account_id
        )

    async def invoices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ChargebeeInvoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of invoices from Chargebee, optionally filtered by customer ID. Invoices contain amounts, line items, payment status, and dates. Use this to audit billing history, verify payments, or find a specific invoice for reconciliation or customer support.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "chargebee.invoices_list", owner_id, args, account_id=account_id
        )

    async def subscription_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ChargebeeSubscriptionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new subscription for an existing customer in Chargebee, assigning a plan or subscription items with optional trial period and billing cycles. If invoice_immediately is true, an invoice is generated right away. Use this to enroll a customer in a paid plan, which starts the billing cycle and can generate real charges.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "chargebee.subscription_create", owner_id, args, account_id=account_id
        )

    async def subscriptions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ChargebeeSubscriptionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of subscriptions from Chargebee, with optional filters for limit, offset, and item ID. Subscription records contain plan details, status, billing dates, and customer associations. Use this to review active subscriptions, find expired ones, or audit billing status across customers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "chargebee.subscriptions_list", owner_id, args, account_id=account_id
        )
