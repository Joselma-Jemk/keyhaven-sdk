# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `cloudflare` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CloudflareCreateZoneDnsRecordPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    zone_id: str


class CloudflareCreateZoneDnsRecordBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    comment: str | None = None
    content: str | None = None
    name: str | None = None
    proxied: bool | None = None
    ttl: int = 1
    type_: str | None = Field(default=None, alias="type")


class CloudflareCreateZoneDnsRecordRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloudflareCreateZoneDnsRecordPath
    body: CloudflareCreateZoneDnsRecordBody = Field(
        default_factory=CloudflareCreateZoneDnsRecordBody
    )


class CloudflareDeleteZoneDnsRecordPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    zone_id: str
    dns_record_id: str


class CloudflareDeleteZoneDnsRecordRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloudflareDeleteZoneDnsRecordPath


class CloudflareGetZoneDetailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    zone_id: str


class CloudflareGetZoneDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloudflareGetZoneDetailsPath


class CloudflareGetZoneDnsRecordDetailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    zone_id: str
    dns_record_id: str


class CloudflareGetZoneDnsRecordDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloudflareGetZoneDnsRecordDetailsPath


class CloudflareListZoneDnsRecordsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    zone_id: str


class CloudflareListZoneDnsRecordsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloudflareListZoneDnsRecordsPath


class CloudflareListZonesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class CloudflareUpdateZoneDnsRecordPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    zone_id: str
    dns_record_id: str


class CloudflareUpdateZoneDnsRecordBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    comment: str | None = None
    content: str | None = None
    name: str | None = None
    proxied: bool | None = None
    ttl: int | None = None
    type_: str | None = Field(default=None, alias="type")


class CloudflareUpdateZoneDnsRecordRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CloudflareUpdateZoneDnsRecordPath
    body: CloudflareUpdateZoneDnsRecordBody = Field(
        default_factory=CloudflareUpdateZoneDnsRecordBody
    )


class CloudflareNamespace:
    """Typed accessor for the `cloudflare` app. Reach it via `client.apps.cloudflare`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_zone_dns_record(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudflareCreateZoneDnsRecordRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new DNS record in a specified zone with a given type (A, AAAA, CNAME, MX, TXT, etc.), name, content, TTL, and optional Cloudflare proxying. This immediately changes how DNS queries for that name are resolved, affecting live traffic routing. Use this when an agent needs to add a DNS entry, such as pointing a subdomain to a new server or adding email authentication records like SPF or DKIM.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudflare.create_zone_dns_record", owner_id, args, account_id=account_id
        )

    async def delete_zone_dns_record(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudflareDeleteZoneDnsRecordRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a DNS record from a zone by specifying the zone ID and DNS record ID. The record is immediately deleted and the domain's DNS resolution changes accordingly, which can cause websites, email, or other services relying on that record to become unreachable. Use this when an agent needs to clean up stale records or remove entries pointing to decommissioned services.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudflare.delete_zone_dns_record", owner_id, args, account_id=account_id
        )

    async def get_zone_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudflareGetZoneDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a specific zone (domain) by its zone ID, including its plan, assigned name servers, and current status. This read-only operation returns the full zone configuration. Use this when an agent needs to verify a zone's settings or confirm ownership before modifying DNS records or security settings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudflare.get_zone_details", owner_id, args, account_id=account_id
        )

    async def get_zone_dns_record_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudflareGetZoneDnsRecordDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single DNS record within a zone by its zone ID and DNS record ID. Returns the record's type, name, content, TTL, proxied status, and metadata. Use this when an agent needs to inspect a specific record before deciding to update or delete it, or to verify its current configuration matches expectations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudflare.get_zone_dns_record_details", owner_id, args, account_id=account_id
        )

    async def list_zone_dns_records(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudflareListZoneDnsRecordsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all DNS records for a specified zone, with search, sort, and filter capabilities. Returns each record's type, name, content, TTL, and proxy status. Use this when an agent needs to audit existing DNS records before creating, updating, or deleting entries to avoid conflicts, or to troubleshoot DNS resolution issues.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudflare.list_zone_dns_records", owner_id, args, account_id=account_id
        )

    async def list_zones(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudflareListZonesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all zones (domains) on the Cloudflare account with search, sort, and filter capabilities. Returns zone metadata including names, plans, name servers, and current statuses. Use this when an agent needs to discover which domains are managed by Cloudflare before performing DNS or security operations on a specific zone.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudflare.list_zones", owner_id, args, account_id=account_id
        )

    async def update_zone_dns_record(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CloudflareUpdateZoneDnsRecordRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing DNS record in a zone by its zone ID and DNS record ID, modifying its type, name, content, TTL, proxied status, or comments. Changes propagate immediately through Cloudflare's network and affect live DNS resolution for the domain. Use this when an agent needs to change a record's target, such as re-pointing an A record to a new IP address or adjusting proxy settings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cloudflare.update_zone_dns_record", owner_id, args, account_id=account_id
        )
