# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `dynatrace` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DynatraceEntitiesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int | None = None
    nextPageKey: str | None = None
    entitySelector: str | None = None
    fields: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None


class DynatraceEntitiesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DynatraceEntitiesListQuery | None = None


class DynatraceEventsIngestBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    eventType: str
    properties: dict[str, Any] | None = None
    entitySelector: str | None = None
    timeout: int | None = None


class DynatraceEventsIngestRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DynatraceEventsIngestBody


class DynatraceMetricsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int | None = None
    nextPageKey: str | None = None
    metricSelector: str | None = None
    text: str | None = None
    fields: str | None = None


class DynatraceMetricsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DynatraceMetricsListQuery | None = None


class DynatraceMetricsQueryQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    metricSelector: str
    entitySelector: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None
    resolution: str | None = None


class DynatraceMetricsQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DynatraceMetricsQueryQuery


class DynatraceProblemsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int | None = None
    nextPageKey: str | None = None
    problemSelector: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None


class DynatraceProblemsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DynatraceProblemsListQuery | None = None


class DynatraceSyntheticMonitorsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int | None = None
    nextPageKey: str | None = None


class DynatraceSyntheticMonitorsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DynatraceSyntheticMonitorsListQuery | None = None


class DynatraceNamespace:
    """Typed accessor for the `dynatrace` app. Reach it via `client.apps.dynatrace`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def entities_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DynatraceEntitiesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all monitored entities (hosts, services, processes, applications, and more) in the Dynatrace environment, with filtering by entity type, tags, management zone, or time range. Each entity represents a monitored component with its current properties and state. Use this to discover what is being monitored, verify entity coverage, or find a specific entity ID for deeper drill-down.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dynatrace.entities_list", owner_id, args, account_id=account_id
        )

    async def events_ingest(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DynatraceEventsIngestRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Ingests a custom event into Dynatrace and attaches it to specified monitored entities, with optional auto-close timeout and custom properties. The event appears in the problem analysis timeline and can trigger alerting rules, notifications, or automation workflows. Use this to push external signals such as deployments, maintenance windows, configuration changes, or custom alerts into Dynatrace for unified observability.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dynatrace.events_ingest", owner_id, args, account_id=account_id
        )

    async def metrics_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DynatraceMetricsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists metric schemata available in the Dynatrace environment, including their keys, display names, units, dimensions, and supported transformations. Each metric represents an observable data type such as CPU usage, memory consumption, or request latency. Use this to discover which metrics exist before querying their data points or to find a specific metric key for monitoring configuration.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dynatrace.metrics_list", owner_id, args, account_id=account_id
        )

    async def metrics_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DynatraceMetricsQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Queries time-series data points for one or more metrics using a metric selector expression, with optional entity filtering and time range bounds. Returns raw measurement values that can be used for dashboards, ad-hoc analysis, or performance investigations. Use this to retrieve actual numerical data for specific metrics over a given time window, such as host CPU trends or service response times.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dynatrace.metrics_query", owner_id, args, account_id=account_id
        )

    async def problems_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DynatraceProblemsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists detected problems in the Dynatrace environment with optional filtering by status (open, resolved), severity, affected entity, or time range. Each problem entry includes root-cause analysis, impacted entities, and event timeline. Use this to review ongoing incidents, investigate historical problems, or assess the health of monitored infrastructure and applications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dynatrace.problems_list", owner_id, args, account_id=account_id
        )

    async def synthetic_monitors_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DynatraceSyntheticMonitorsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all synthetic browser and API monitors configured in the Dynatrace environment, with pagination support. Synthetic monitors simulate user interactions or API calls to proactively detect availability and performance issues before real users are affected. Use this to review your synthetic monitoring coverage, find a specific monitor ID, or audit which endpoints are being tested.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dynatrace.synthetic_monitors_list", owner_id, args, account_id=account_id
        )
