# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `baserow` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BaserowCreateTeamsRowPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    table_id: int


class BaserowCreateTeamsRowQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_field_names: Literal["y", "yes", "true", "t", "on", "1", ""] | None = None
    before: int | None = None
    send_webhook_events: Literal["y", "yes", "true", "t", "on", "1", ""] = "true"


class BaserowCreateTeamsRowBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Name: str


class BaserowCreateTeamsRowRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BaserowCreateTeamsRowPath
    query: BaserowCreateTeamsRowQuery | None = None
    body: BaserowCreateTeamsRowBody


class BaserowGetTeamsRowPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    table_id: int
    row_id: int


class BaserowGetTeamsRowQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_field_names: Literal["y", "yes", "true", "t", "on", "1", ""] | None = None


class BaserowGetTeamsRowRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BaserowGetTeamsRowPath
    query: BaserowGetTeamsRowQuery | None = None


class BaserowListTeamsFieldsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    table_id: int


class BaserowListTeamsFieldsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BaserowListTeamsFieldsPath


class BaserowListTeamsRowsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    table_id: int


class BaserowListTeamsRowsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    size: int = 100
    user_field_names: Literal["y", "yes", "true", "t", "on", "1", ""] | None = None
    search: str | None = None
    order_by: str | None = None
    filters: str | None = None
    filter_type: Literal["AND", "OR"] = "AND"
    include: str | None = None
    exclude: str | None = None
    view_id: int | None = None
    link_row_field__join: str | None = None
    field_filters: dict[str, Any] | None = None


class BaserowListTeamsRowsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BaserowListTeamsRowsPath
    query: BaserowListTeamsRowsQuery | None = None


class BaserowUpdateTeamsRowPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    table_id: int
    row_id: int


class BaserowUpdateTeamsRowQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_field_names: Literal["y", "yes", "true", "t", "on", "1", ""] | None = None
    send_webhook_events: Literal["y", "yes", "true", "t", "on", "1", ""] = "true"


class BaserowUpdateTeamsRowBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Name: str | None = None


class BaserowUpdateTeamsRowRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BaserowUpdateTeamsRowPath
    query: BaserowUpdateTeamsRowQuery | None = None
    body: BaserowUpdateTeamsRowBody = Field(default_factory=BaserowUpdateTeamsRowBody)


class BaserowNamespace:
    """Typed accessor for the `baserow` app. Reach it via `client.apps.baserow`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_teams_row(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BaserowCreateTeamsRowRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Inserts a new row into the Teams table with the provided field values, making it immediately visible in list queries and the Baserow UI. The row is assigned a unique ID upon creation. An agent should use this to add a new team entry, specifying at minimum the required 'Name' field.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "baserow.create_teams_row", owner_id, args, account_id=account_id
        )

    async def get_teams_row(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BaserowGetTeamsRowRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single row by its unique ID from the Teams table, returning all its field values. This is the most targeted read operation — no pagination or filtering needed. An agent should use this when it already knows the row ID and needs the full record, for example after a create or search operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "baserow.get_teams_row", owner_id, args, account_id=account_id
        )

    async def list_teams_fields(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BaserowListTeamsFieldsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the schema (column definitions) of the Teams table, including field names, types, and constraints. This does not return row data — only the table's structural metadata. An agent should use this to discover what fields exist before reading or writing rows, ensuring parameter correctness for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "baserow.list_teams_fields", owner_id, args, account_id=account_id
        )

    async def list_teams_rows(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BaserowListTeamsRowsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all rows from the Teams table with support for pagination, search, filtering, sorting, and field selection. Returns a paginated response with the matching row records. An agent should use this to query team data, apply filters to find specific teams, or enumerate records for bulk operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "baserow.list_teams_rows", owner_id, args, account_id=account_id
        )

    async def update_teams_row(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BaserowUpdateTeamsRowRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies one or more field values of an existing row in the Teams table using a partial update (PATCH). Only the fields included in the request body are changed; all other fields retain their current values. An agent should use this to correct or update team information without replacing the entire record.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "baserow.update_teams_row", owner_id, args, account_id=account_id
        )
