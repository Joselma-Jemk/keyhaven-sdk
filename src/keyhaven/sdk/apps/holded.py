# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `holded` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class HoldedCreateEmployeeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class HoldedCreateEmployeeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    lastName: str
    email: str
    sendInvite: bool = True


class HoldedCreateEmployeeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: HoldedCreateEmployeeHeader = Field(default_factory=HoldedCreateEmployeeHeader)
    body: HoldedCreateEmployeeBody


class HoldedDeleteEmployeePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employeeId: str


class HoldedDeleteEmployeeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class HoldedDeleteEmployeeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HoldedDeleteEmployeePath
    header: HoldedDeleteEmployeeHeader = Field(default_factory=HoldedDeleteEmployeeHeader)


class HoldedGetEmployeePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employeeId: str


class HoldedGetEmployeeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class HoldedGetEmployeeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HoldedGetEmployeePath
    header: HoldedGetEmployeeHeader = Field(default_factory=HoldedGetEmployeeHeader)


class HoldedListEmployeesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1


class HoldedListEmployeesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class HoldedListEmployeesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HoldedListEmployeesQuery | None = None
    header: HoldedListEmployeesHeader = Field(default_factory=HoldedListEmployeesHeader)


class HoldedUpdateEmployeePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    employeeId: str


class HoldedUpdateEmployeeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class HoldedUpdateEmployeeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    lastName: str | None = None
    mainEmail: str | None = None
    email: str | None = None
    nationality: str | None = None
    phone: str | None = None
    mobile: str | None = None
    dateOfBirth: str | None = None
    gender: Literal["male", "female"] | None = None
    mainLanguage: (
        Literal[
            "English",
            "English(UK)",
            "English(US)",
            "English(Canada)",
            "Spanish",
            "Spanish(Spain)",
            "Spanish(Mexico)",
            "Spanish(Argentina)",
            "Spanish(Colombia)",
            "Portuguese",
            "Portuguese(Portugal)",
            "Portuguese(Brazil)",
            "Catalan",
            "Galician",
            "Euskera",
            "French",
            "German(Deutsch)",
            "Italian",
            "Greek",
            "Swedish",
            "Dutch",
            "Finnish",
            "Irish",
            "Norwegian",
            "Danish",
            "Czech",
            "Croatian",
            "Russian",
            "Polish",
        ]
        | None
    ) = None
    iban: str | None = None
    code: str | None = None
    socialSecurityNum: str | None = None
    address: dict[str, Any] | None = None
    fiscalResidence: bool | None = None
    timeOffPolicyId: str | None = None
    timeOffSupervisors: list[Any] | None = None
    reportingTo: str | None = None
    fiscalAddress: dict[str, Any] | None = None
    workplace: str | None = None
    holdedUserId: str | None = None
    teams: list[Any] | None = None


class HoldedUpdateEmployeeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HoldedUpdateEmployeePath
    header: HoldedUpdateEmployeeHeader = Field(default_factory=HoldedUpdateEmployeeHeader)
    body: HoldedUpdateEmployeeBody = Field(default_factory=HoldedUpdateEmployeeBody)


class HoldedNamespace:
    """Typed accessor for the `holded` app. Reach it via `client.apps.holded`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_employee(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoldedCreateEmployeeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Registers a new employee in the Holded system with name, email, and optionally sends an invitation email. The employee record is created in your workspace and, if sendInvite is true, the person receives an email notification to join. Use this when the agent needs to onboard a new team member into the Holded platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "holded.create_employee", owner_id, args, account_id=account_id
        )

    async def delete_employee(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoldedDeleteEmployeeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes an employee from the Holded system by their employee ID. This action is irreversible — the employee record and all associated data (time-off, payroll history, team assignments) are deleted from the workspace. Use this when the agent needs to offboard a team member and remove their access and records from the system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "holded.delete_employee", owner_id, args, account_id=account_id
        )

    async def get_employee(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoldedGetEmployeeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details for a single employee by their Holded employee ID. Returns personal information, contact details, address, fiscal information, team assignments, time-off policy, and reporting structure. Use this when the agent needs to view or verify a specific employee's comprehensive profile.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "holded.get_employee", owner_id, args, account_id=account_id
        )

    async def list_employees(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoldedListEmployeesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all employees registered in the Holded team workspace. Returns each employee's name, contact information, position, status, and team assignments. Use this when the agent needs to view the workforce directory, find an employee ID for subsequent operations, or audit the team roster.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "holded.list_employees", owner_id, args, account_id=account_id
        )

    async def update_employee(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoldedUpdateEmployeeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more fields of an existing employee's record in Holded, including personal details, contact information, address, fiscal data (IBAN, tax ID, social security number), team assignments, and reporting structure. Changes take effect immediately and affect payroll, time-off, and team management. Use this when the agent needs to modify employee information or correct HR records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "holded.update_employee", owner_id, args, account_id=account_id
        )
