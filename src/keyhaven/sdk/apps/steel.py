# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `steel` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SteelCaptureScreenshotBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    fullPage: bool = False
    delay: float = 0
    useProxy: bool = False


class SteelCaptureScreenshotRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SteelCaptureScreenshotBody


class SteelConvertToPdfBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    delay: float = 0
    useProxy: bool = False


class SteelConvertToPdfRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SteelConvertToPdfBody


class SteelCreateSessionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sessionId: str | None = None
    userAgent: str | None = None
    useProxy: bool = False
    proxyUrl: str | None = None
    blockAds: bool = False
    solveCaptcha: bool = False
    sessionContext: dict[str, Any] | None = None
    timeout: int = 300000
    concurrency: int = 1
    isSelenium: bool = False
    dimensions: dict[str, Any]


class SteelCreateSessionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SteelCreateSessionBody


class SteelGetSessionContextPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SteelGetSessionContextRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SteelGetSessionContextPath


class SteelGetSessionDetailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SteelGetSessionDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SteelGetSessionDetailsPath


class SteelGetSessionEventsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SteelGetSessionEventsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SteelGetSessionEventsPath


class SteelGetSessionLiveDetailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SteelGetSessionLiveDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SteelGetSessionLiveDetailsPath


class SteelListSessionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursorId: str | None = None
    limit: int = 50
    status: Literal["live", "released", "failed"] | None = None


class SteelListSessionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SteelListSessionsQuery | None = None


class SteelReleaseAllSessionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SteelReleaseSessionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SteelReleaseSessionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SteelReleaseSessionPath


class SteelScrapeWebpageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    format: list[Any] | None = None
    screenshot: bool = False
    pdf: bool = False
    delay: float = 0
    useProxy: bool = False


class SteelScrapeWebpageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SteelScrapeWebpageBody


class SteelNamespace:
    """Typed accessor for the `steel` app. Reach it via `client.apps.steel`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def capture_screenshot(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelCaptureScreenshotRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Takes a screenshot of a webpage using a cloud browser, supporting both viewport-only and full-page captures. Optionally routes through a residential proxy. Use this to visually inspect how a page renders, capture a snapshot for reports, or archive the visual state of a website.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.capture_screenshot", owner_id, args, account_id=account_id
        )

    async def convert_to_pdf(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelConvertToPdfRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generates a PDF document from a webpage URL via a cloud browser. Optionally adds a delay for JS-rendered content and supports residential proxy. Use this to create printable documents, generate PDF invoices/receipts from web pages, or archive web content in a standard portable format.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.convert_to_pdf", owner_id, args, account_id=account_id
        )

    async def create_session(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelCreateSessionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Launches a new cloud browser session with full configuration control: user agent, proxy, captcha solving, ad blocking, viewport dimensions, and preloaded cookies/localStorage. The session is billable while live. Use this to set up an automated browser environment for scraping, testing, or monitoring web applications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.create_session", owner_id, args, account_id=account_id
        )

    async def get_session_context(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelGetSessionContextRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the stored context data (cookies and local storage) from a live browser session. This data may include session tokens, authentication state, and user preferences from websites visited during the session. Use this to extract cookies for reuse or to audit the browsing state.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.get_session_context", owner_id, args, account_id=account_id
        )

    async def get_session_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelGetSessionDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches metadata about a specific browser session, including its status (live/released/failed), creation time, timeout configuration, and proxy settings. Use this to inspect the state of a session before deciding to release it or to check its configuration.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.get_session_details", owner_id, args, account_id=account_id
        )

    async def get_session_events(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelGetSessionEventsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the recorded playback events (in RRWeb format) from a browser session, enabling visual replay of everything that happened during the session. This includes mouse movements, clicks, scrolls, and page mutations. Use this to debug automation workflows or audit what occurred during a headless session.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.get_session_events", owner_id, args, account_id=account_id
        )

    async def get_session_live_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelGetSessionLiveDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provides the real-time state of an active browser session, including open pages, tabs, current URLs, and browser status. Unlike GET_SESSION_DETAILS, this returns live runtime information. Use this to monitor what a running browser session is doing or to check page load progress.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.get_session_live_details", owner_id, args, account_id=account_id
        )

    async def list_sessions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelListSessionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all browser sessions for the current Steel.dev organisation, with optional filtering by status (live, released, or failed). Each session includes its ID, status, creation time, and configuration. Use this to monitor active browser sessions or to find a session ID for subsequent detail or release calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.list_sessions", owner_id, args, account_id=account_id
        )

    async def release_all_sessions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelReleaseAllSessionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Immediately terminates every active browser session for the organisation. All in-progress page loads, scrapes, or automations on those sessions will be interrupted. Use this as a cleanup or reset action when sessions have been left running or to halt all browser activity at once.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.release_all_sessions", owner_id, args, account_id=account_id
        )

    async def release_session(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelReleaseSessionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Terminates a specific live browser session by its ID, stopping all page activity and freeing resources. The session will no longer be usable. Use this to clean up individual sessions after completing a scraping or automation task.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.release_session", owner_id, args, account_id=account_id
        )

    async def scrape_webpage(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SteelScrapeWebpageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Extracts content from a single URL using a cloud browser, returning the page body in your chosen format (HTML, cleaned HTML, markdown, or readability). Optionally captures a screenshot or PDF alongside the extraction. Use this to ingest webpage content for analysis, archiving, or data extraction without fetching raw HTML yourself.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "steel.scrape_webpage", owner_id, args, account_id=account_id
        )
