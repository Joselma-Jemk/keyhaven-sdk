# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `productboard` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ProductboardFeatureCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any]


class ProductboardFeatureCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ProductboardFeatureCreateBody


class ProductboardFeatureGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ProductboardFeatureGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None


class ProductboardFeatureGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ProductboardFeatureGetPath
    query: ProductboardFeatureGetQuery | None = None


class ProductboardFeaturesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type__: str | None = Field(default=None, alias="type[]")
    name: str | None = None
    status_name_: str | None = Field(default=None, alias="status[name]")
    status_id_: str | None = Field(default=None, alias="status[id]")
    owner_id_: str | None = Field(default=None, alias="owner[id]")
    owner_email_: str | None = Field(default=None, alias="owner[email]")
    parent_id_: str | None = Field(default=None, alias="parent[id]")
    archived: bool | None = None
    fields: str | None = None
    pageCursor: str | None = None


class ProductboardFeaturesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ProductboardFeaturesListQuery | None = None


class ProductboardNoteCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any]


class ProductboardNoteCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ProductboardNoteCreateBody


class ProductboardNotesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    archived: bool | None = None
    processed: bool | None = None
    owner_id_: str | None = Field(default=None, alias="owner[id]")
    owner_email_: str | None = Field(default=None, alias="owner[email]")
    creator_id_: str | None = Field(default=None, alias="creator[id]")
    metadata_source__system_: str | None = Field(default=None, alias="metadata[source][system]")
    metadata_source__recordId_: str | None = Field(default=None, alias="metadata[source][recordId]")
    createdFrom: str | None = None
    createdTo: str | None = None
    updatedFrom: str | None = None
    updatedTo: str | None = None
    fields: str | None = None
    pageCursor: str | None = None


class ProductboardNotesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ProductboardNotesListQuery | None = None


class ProductboardNamespace:
    """Typed accessor for the `productboard` app. Reach it via `client.apps.productboard`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def feature_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProductboardFeatureCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new Product Management entity in Productboard — such as a feature, initiative, component, objective, or release — with specified fields, an optional metadata source for external-system provenance, and optional initial relationships. The entity appears immediately in the workspace and is visible to all workspace members. It can be edited, archived, or deleted later. Use this when the agent needs to add a new item to the product roadmap from customer feedback, planning sessions, or external system sync.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "productboard.feature_create", owner_id, args, account_id=account_id
        )

    async def feature_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProductboardFeatureGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full details of a single Productboard entity by its UUID, including all configured fields, status, owner, parent/child relationships, and metadata. This is read-only. Use this when the agent needs the complete record of a specific feature, initiative, or component, for example to inspect its description, check its status, or view its linked notes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "productboard.feature_get", owner_id, args, account_id=account_id
        )

    async def features_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProductboardFeaturesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of Product Management entities from Productboard — features, initiatives, components, objectives, releases, and more — with filters by type, name, status, owner, parent, or archived state. Each entity includes its name, description, status, owner, and custom fields. This is read-only. Use this when the agent needs to browse the product roadmap, find a specific feature, or audit entity relationships.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "productboard.features_list", owner_id, args, account_id=account_id
        )

    async def note_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProductboardNoteCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a customer feedback note in Productboard as either a textNote (standalone text) or conversationNote (multi-part conversation), with optional links to a customer (user or company) and to product entities (features, components, etc.). The note appears immediately in the workspace and is visible to workspace members. Notes can be edited, archived, or deleted later. Use this when the agent needs to capture customer feedback from support tickets, interviews, or surveys and tie it to the relevant product area.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "productboard.note_create", owner_id, args, account_id=account_id
        )

    async def notes_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ProductboardNotesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a paginated list of customer feedback notes (textNote or conversationNote) from Productboard, filterable by archived/processed flags, owner, creator, source system, or creation/update date ranges. Each note includes the feedback content, tags, linked entities, and customer information. This is read-only. Use this when the agent needs to review customer feedback, find notes linked to a feature, or audit recent submissions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "productboard.notes_list", owner_id, args, account_id=account_id
        )
