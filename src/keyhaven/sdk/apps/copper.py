# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `copper` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CopperOpportunitiesSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_number: int | None = None
    page_size: int | None = None
    sort_by: str | None = None
    sort_direction: str | None = None
    name: str | None = None
    status: str | None = None
    assignee_ids: list[Any] | None = None
    pipeline_ids: list[Any] | None = None
    pipeline_stage_ids: list[Any] | None = None
    tags: list[Any] | None = None


class CopperOpportunitiesSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CopperOpportunitiesSearchBody = Field(default_factory=CopperOpportunitiesSearchBody)


class CopperOpportunityCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    primary_contact_id: int
    assignee_id: int | None = None
    company_id: int | None = None
    monetary_value: float | None = None
    pipeline_id: int | None = None
    pipeline_stage_id: int | None = None
    close_date: str | None = None
    win_probability: int | None = None
    status: str | None = None
    details: str | None = None
    tags: list[Any] | None = None


class CopperOpportunityCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CopperOpportunityCreateBody


class CopperPeopleSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_number: int | None = None
    page_size: int | None = None
    sort_by: str | None = None
    sort_direction: str | None = None
    name: str | None = None
    emails: list[Any] | None = None
    assignee_ids: list[Any] | None = None
    company_ids: list[Any] | None = None
    tags: list[Any] | None = None


class CopperPeopleSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CopperPeopleSearchBody = Field(default_factory=CopperPeopleSearchBody)


class CopperPersonCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    emails: list[Any] | None = None
    phone_numbers: list[Any] | None = None
    title: str | None = None
    company_id: int | None = None
    assignee_id: int | None = None
    address: dict[str, Any] | None = None
    details: str | None = None
    tags: list[Any] | None = None


class CopperPersonCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CopperPersonCreateBody


class CopperPersonGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    person_id: int


class CopperPersonGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CopperPersonGetPath


class CopperPersonUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    person_id: int


class CopperPersonUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    emails: list[Any] | None = None
    phone_numbers: list[Any] | None = None
    title: str | None = None
    company_id: int | None = None
    assignee_id: int | None = None
    details: str | None = None
    tags: list[Any] | None = None


class CopperPersonUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CopperPersonUpdatePath
    body: CopperPersonUpdateBody = Field(default_factory=CopperPersonUpdateBody)


class CopperNamespace:
    """Typed accessor for the `copper` app. Reach it via `client.apps.copper`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def opportunities_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CopperOpportunitiesSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search for sales opportunities (deals) in Copper CRM using filters such as name, status (Open/Won/Lost/Abandoned), pipeline, pipeline stage, owner, and tags, with configurable pagination and sorting. Returns matching opportunities with their monetary value, pipeline stage, close date, win probability, and associated contacts. Use when analyzing pipeline health, finding specific deals to update, or preparing revenue forecasts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "copper.opportunities_search", owner_id, args, account_id=account_id
        )

    async def opportunity_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CopperOpportunityCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new sales opportunity in Copper CRM with a name, primary contact, monetary value, pipeline and stage assignment, close date, win probability, and status. This adds a tracked deal to the sales pipeline that is visible to the entire team and contributes to forecasting. Use when a qualified lead is ready to enter the sales process with a defined value and expected close timeline.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "copper.opportunity_create", owner_id, args, account_id=account_id
        )

    async def people_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CopperPeopleSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search for person records in Copper CRM using filters such as name, email address, company, assigned owner, and tags, with configurable pagination and sorting. This returns matching person profiles with their full contact details, company association, and custom fields. Use when you need to find a specific contact for outreach, build a contact list for reporting, or locate a person before performing an update.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "copper.people_search", owner_id, args, account_id=account_id
        )

    async def person_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CopperPersonCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new person record in Copper CRM with name, email addresses, phone numbers, job title, company association, assignee, postal address, free-form notes, and tags. This adds a visible contact to the CRM that can be associated with opportunities, activities, and communications. Use when capturing a new lead from a form, importing contacts from another system, or adding stakeholders to an existing account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "copper.person_create", owner_id, args, account_id=account_id
        )

    async def person_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CopperPersonGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full details of a single person record by its ID from Copper CRM, including all contact information, email addresses, phone numbers, job title, company association, assigned owner, tags, custom fields, and activity history. Use to inspect a contact's complete profile before initiating communication, verifying data accuracy, or preparing context for a sales interaction.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "copper.person_get", owner_id, args, account_id=account_id
        )

    async def person_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CopperPersonUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Update one or more fields of an existing person record in Copper CRM by its ID, including name, emails, phone numbers, title, company, assignee, details, and tags. Changes are applied immediately and visible to all CRM users viewing that contact. Use when correcting outdated contact data, updating job changes, reassigning ownership, or enriching a person record with new information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "copper.person_update", owner_id, args, account_id=account_id
        )
