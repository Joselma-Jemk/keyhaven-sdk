# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `harvest` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class HarvestClientCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    is_active: bool | None = None
    address: str | None = None
    currency: str | None = None


class HarvestClientCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: HarvestClientCreateBody


class HarvestClientGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    client_id: str


class HarvestClientGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HarvestClientGetPath


class HarvestClientsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    is_active: bool | None = None
    updated_since: str | None = None
    cursor: str | None = None
    page: int | None = None
    per_page: int | None = None


class HarvestClientsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HarvestClientsListQuery | None = None


class HarvestInvoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    client_id: str | None = None
    project_id: str | None = None
    state: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None
    cursor: str | None = None
    per_page: int | None = None


class HarvestInvoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HarvestInvoicesListQuery | None = None


class HarvestTimeEntriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str | None = None
    client_id: str | None = None
    project_id: str | None = None
    task_id: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None
    is_running: bool | None = None
    cursor: str | None = None
    per_page: int | None = None


class HarvestTimeEntriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HarvestTimeEntriesListQuery | None = None


class HarvestTimeEntryCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str
    task_id: str
    spent_date: str
    hours: float | None = None
    notes: str | None = None
    started_time: str | None = None
    ended_time: str | None = None
    user_id: str | None = None
    external_reference: str | None = None


class HarvestTimeEntryCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: HarvestTimeEntryCreateBody


class HarvestNamespace:
    """Typed accessor for the `harvest` app. Reach it via `client.apps.harvest`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def client_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HarvestClientCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new client to the Harvest account with a name, optional address, currency, and active status. The client immediately appears in reports and can be assigned to projects. Use this to register a new business partner in your time tracking and invoicing system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "harvest.client_create", owner_id, args, account_id=account_id
        )

    async def client_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HarvestClientGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single client record including its name, address, currency, and whether it is active or archived. Use this to get full details about a specific client before billing them or assigning them to a project.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "harvest.client_get", owner_id, args, account_id=account_id
        )

    async def clients_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HarvestClientsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all clients sorted by newest first, with optional filtering by active status or update timestamp. Shows client names, contact details, and currency settings. Use this to browse your client base before creating invoices or time entries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "harvest.clients_list", owner_id, args, account_id=account_id
        )

    async def invoices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HarvestInvoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists invoices with filters by client, project, state, and date range. Each entry shows the invoice number, amount, status, and issue date. This reveals financial billing data. Use this to review outstanding invoices or reconcile payments.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "harvest.invoices_list", owner_id, args, account_id=account_id
        )

    async def time_entries_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HarvestTimeEntriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns time entries across the account, filterable by user, client, project, task, and date range. Can also show currently running timers. This reveals billable hours and team productivity data. Use this to track time logged against projects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "harvest.time_entries_list", owner_id, args, account_id=account_id
        )

    async def time_entry_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HarvestTimeEntryCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Logs hours worked against a specific project and task for a given date, with optional notes, start/end times, and a different user assignment. The entry becomes part of billable records and appears in reports and invoices. Use this to record work completed on a project.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "harvest.time_entry_create", owner_id, args, account_id=account_id
        )
