# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `pinterest` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PinterestBoardCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    privacy: str | None = None


class PinterestBoardCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: PinterestBoardCreateBody


class PinterestBoardGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    board_id: str


class PinterestBoardGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ad_account_id: str | None = None


class PinterestBoardGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PinterestBoardGetPath
    query: PinterestBoardGetQuery | None = None


class PinterestBoardsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    bookmark: str | None = None
    page_size: int | None = None
    privacy: str | None = None
    ad_account_id: str | None = None


class PinterestBoardsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PinterestBoardsListQuery | None = None


class PinterestPinCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    board_id: str
    title: str | None = None
    description: str | None = None
    link: str | None = None
    media_source: dict[str, Any]
    alt_text: str | None = None
    note: str | None = None


class PinterestPinCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: PinterestPinCreateBody


class PinterestPinGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pin_id: str


class PinterestPinGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ad_account_id: str | None = None


class PinterestPinGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PinterestPinGetPath
    query: PinterestPinGetQuery | None = None


class PinterestPinsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    bookmark: str | None = None
    page_size: int | None = None
    pin_filter: str | None = None
    include_protected_pins: bool | None = None
    ad_account_id: str | None = None


class PinterestPinsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PinterestPinsListQuery | None = None


class PinterestNamespace:
    """Typed accessor for the `pinterest` app. Reach it via `client.apps.pinterest`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def board_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PinterestBoardCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new Pinterest board owned by the authenticated user, with a name, optional description, and privacy setting (PUBLIC, PROTECTED, or SECRET). The board appears immediately on the user's profile (or as secret if configured). Use this when an agent needs to organize content into a new topic area. This action is recoverable by deleting the board.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pinterest.board_create", owner_id, args, account_id=account_id
        )

    async def board_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PinterestBoardGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve full details of a single Pinterest board by its numeric ID, including name, description, privacy level, owner, and pin count. Use this to inspect a board's configuration before creating pins on it or changing its settings. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pinterest.board_get", owner_id, args, account_id=account_id
        )

    async def boards_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PinterestBoardsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of Pinterest boards owned by the authenticated user, with optional privacy filtering (PUBLIC, PROTECTED, SECRET) and support for Business Access via ad_account_id. Use this to discover existing boards before creating pins or analyzing board content. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pinterest.boards_list", owner_id, args, account_id=account_id
        )

    async def pin_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PinterestPinCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new Pinterest pin on a specified board, with a title, description, click-through link, and media source (image URL, base64 image, or video ID). The pin becomes visible immediately on the board and may appear in followers' feeds. Use this when an agent needs to publish visual content. This action is reversible by deleting the pin.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pinterest.pin_create", owner_id, args, account_id=account_id
        )

    async def pin_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PinterestPinGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve full details of a single Pinterest pin by its numeric ID, including title, description, link URL, media, board assignment, and metadata. Use this to inspect a pin's content before updating it or to verify its details. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pinterest.pin_get", owner_id, args, account_id=account_id
        )

    async def pins_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PinterestPinsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of Pinterest pins owned by the authenticated user, with optional filtering (native vs. imported pins, protected pins) and Business Access support. Use this to discover existing pins, inspect their content, or find a specific pin ID before performing follow-up operations. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pinterest.pins_list", owner_id, args, account_id=account_id
        )
