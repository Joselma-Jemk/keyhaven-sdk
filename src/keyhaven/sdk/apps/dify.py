# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `dify` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DifyDeleteConversationPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    conversation_id: str


class DifyDeleteConversationHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class DifyDeleteConversationBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str


class DifyDeleteConversationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DifyDeleteConversationPath
    header: DifyDeleteConversationHeader = Field(default_factory=DifyDeleteConversationHeader)
    body: DifyDeleteConversationBody


class DifyGetConversationListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str
    last_id: str | None = None
    limit: int = 20


class DifyGetConversationListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class DifyGetConversationListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DifyGetConversationListQuery
    header: DifyGetConversationListHeader = Field(default_factory=DifyGetConversationListHeader)


class DifyGetMessagesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str
    conversation_id: str | None = None
    first_id: str | None = None
    limit: int = 20


class DifyGetMessagesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class DifyGetMessagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DifyGetMessagesQuery
    header: DifyGetMessagesHeader = Field(default_factory=DifyGetMessagesHeader)


class DifySendChatMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class DifySendChatMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    inputs: dict[str, Any] | None = None
    query: str
    response_mode: str = "streaming"
    conversation_id: str | None = None
    user: str
    files: list[Any] | None = None
    auto_generate_name: bool = True


class DifySendChatMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: DifySendChatMessageHeader = Field(default_factory=DifySendChatMessageHeader)
    body: DifySendChatMessageBody


class DifySendFeedbackPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    message_id: str


class DifySendFeedbackHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class DifySendFeedbackBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    rating: str
    user: str
    content: str | None = None


class DifySendFeedbackRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DifySendFeedbackPath
    header: DifySendFeedbackHeader = Field(default_factory=DifySendFeedbackHeader)
    body: DifySendFeedbackBody


class DifyStopResponsePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    task_id: str


class DifyStopResponseHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class DifyStopResponseBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str


class DifyStopResponseRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DifyStopResponsePath
    header: DifyStopResponseHeader = Field(default_factory=DifyStopResponseHeader)
    body: DifyStopResponseBody


class DifyUpdateConversationNamePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    conversation_id: str


class DifyUpdateConversationNameHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class DifyUpdateConversationNameBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    auto_generate: bool = True
    user: str


class DifyUpdateConversationNameRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DifyUpdateConversationNamePath
    header: DifyUpdateConversationNameHeader = Field(
        default_factory=DifyUpdateConversationNameHeader
    )
    body: DifyUpdateConversationNameBody


class DifyNamespace:
    """Typed accessor for the `dify` app. Reach it via `client.apps.dify`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def delete_conversation(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DifyDeleteConversationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a conversation and all its associated messages from Dify. This action is irreversible — the conversation data cannot be recovered after deletion. Use this only when a user explicitly requests to delete their conversation history or when cleaning up obsolete data according to a retention policy.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dify.delete_conversation", owner_id, args, account_id=account_id
        )

    async def get_conversation_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DifyGetConversationListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all conversations associated with a given user ID, including metadata such as name, creation date, and last message summary. Use this when an agent needs to display the user's conversation history, find a specific conversation to resume, or populate a conversation picker. This operation is read-only and has no side effects on the data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dify.get_conversation_list", owner_id, args, account_id=account_id
        )

    async def get_messages(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DifyGetMessagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of messages within a given conversation, ordered by creation time. Each message includes the user query, the AI response, and metadata such as timestamps and feedback status. Use this when an agent needs to review conversation history, extract previous AI responses, or populate a chat interface. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dify.get_messages", owner_id, args, account_id=account_id
        )

    async def send_chat_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DifySendChatMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends a user query to a Dify AI conversation and returns the AI-generated response. The message is processed by the configured Dify app (chatbot, agent, or workflow), optionally using context variables such as user location or device type. Use this when an agent needs to obtain an AI answer, continue an existing conversation, or trigger a Dify workflow. The response can be returned as streaming chunks or as a single blocking reply.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dify.send_chat_message", owner_id, args, account_id=account_id
        )

    async def send_feedback(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DifySendFeedbackRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submits user feedback (a rating and optional comment) for a specific AI message in a conversation. The feedback is used by Dify to improve response quality and is visible in the Dify analytics dashboard. Use this after a user rates a response as helpful or unhelpful. This action is internal to the Dify app and does not change the message content or conversation state.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dify.send_feedback", owner_id, args, account_id=account_id
        )

    async def stop_response(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DifyStopResponseRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Halts an ongoing AI response generation for a specific task ID, cutting off the stream before it completes. The conversation and all prior messages remain intact and can still be used. Use this when a response is no longer needed (e.g., the user cancelled or a timeout occurred). This action only affects the active stream and does not delete any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dify.stop_response", owner_id, args, account_id=account_id
        )

    async def update_conversation_name(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DifyUpdateConversationNameRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Changes the display name of a conversation to a manually specified name or auto-generates one from the conversation content. This is a cosmetic metadata update that does not affect the messages or the AI's behaviour. Use this when a user wants to rename a conversation for better organisation or when auto-naming is preferred after the first exchange.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "dify.update_conversation_name", owner_id, args, account_id=account_id
        )
