# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `aidbase` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AidbaseGetAllChatbotsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class AidbaseGetAllChatbotsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AidbaseGetAllChatbotsHeader | None = None


class AidbaseGetChatbotByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AidbaseGetChatbotByIdHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class AidbaseGetChatbotByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AidbaseGetChatbotByIdPath
    header: AidbaseGetChatbotByIdHeader | None = None


class AidbaseGetChatbotKnowledgePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AidbaseGetChatbotKnowledgeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class AidbaseGetChatbotKnowledgeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AidbaseGetChatbotKnowledgePath
    header: AidbaseGetChatbotKnowledgeHeader | None = None


class AidbasePostChatbotReplyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AidbasePostChatbotReplyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class AidbasePostChatbotReplyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    message: str
    session_id: str | None = None


class AidbasePostChatbotReplyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AidbasePostChatbotReplyPath
    header: AidbasePostChatbotReplyHeader | None = None
    body: AidbasePostChatbotReplyBody


class AidbasePutChatbotKnowledgePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AidbasePutChatbotKnowledgeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class AidbasePutChatbotKnowledgeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    knowledge_id: str


class AidbasePutChatbotKnowledgeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AidbasePutChatbotKnowledgePath
    header: AidbasePutChatbotKnowledgeHeader | None = None
    body: AidbasePutChatbotKnowledgeBody


class AidbaseRemoveChatbotKnowledgePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AidbaseRemoveChatbotKnowledgeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class AidbaseRemoveChatbotKnowledgeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    knowledge_id: str


class AidbaseRemoveChatbotKnowledgeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AidbaseRemoveChatbotKnowledgePath
    header: AidbaseRemoveChatbotKnowledgeHeader | None = None
    body: AidbaseRemoveChatbotKnowledgeBody


class AidbaseNamespace:
    """Typed accessor for the `aidbase` app. Reach it via `client.apps.aidbase`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_all_chatbots(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AidbaseGetAllChatbotsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all chatbots configured in the user's Aidbase account, returning each chatbot's identifier, name, configuration status, and metadata. This is a read-only listing with no side effects. Use this function to discover available chatbots before sending messages or managing their knowledge bases.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aidbase.get_all_chatbots", owner_id, args, account_id=account_id
        )

    async def get_chatbot_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AidbaseGetChatbotByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a single chatbot by its unique identifier, including its configuration, settings, and current operational state. This is a read-only operation with no side effects. Use this function to inspect a specific chatbot before modifying it or interacting with its knowledge base.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aidbase.get_chatbot_by_id", owner_id, args, account_id=account_id
        )

    async def get_chatbot_knowledge(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AidbaseGetChatbotKnowledgeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all knowledge items associated with a specific chatbot, including documents, FAQs, and other training data with their identifiers and content details. This is a read-only operation with no side effects. Use this function to review what information a chatbot has been trained on before deciding to add or remove knowledge.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aidbase.get_chatbot_knowledge", owner_id, args, account_id=account_id
        )

    async def post_chatbot_reply(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AidbasePostChatbotReplyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends a user message to a specified chatbot and receives the AI-generated reply. An optional session_id can be provided to maintain multi-turn conversation context. The conversation is persisted within the session but not broadcast to external users. Use this function when an agent needs to converse with an Aidbase chatbot on behalf of a user.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aidbase.post_chatbot_reply", owner_id, args, account_id=account_id
        )

    async def put_chatbot_knowledge(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AidbasePutChatbotKnowledgeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Upserts a knowledge item into a chatbot's knowledge base by its identifier, expanding or updating what the AI can reference when generating replies. This modifies the chatbot's training data and will affect all future response quality. Use this function to teach the chatbot new information or update existing knowledge entries to improve accuracy.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aidbase.put_chatbot_knowledge", owner_id, args, account_id=account_id
        )

    async def remove_chatbot_knowledge(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AidbaseRemoveChatbotKnowledgeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a specific knowledge item from a chatbot's knowledge base by its identifier. The chatbot will no longer reference the deleted content in any future replies and this action cannot be undone through the API. Use this function to purge outdated or incorrect information from a chatbot's training data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "aidbase.remove_chatbot_knowledge", owner_id, args, account_id=account_id
        )
