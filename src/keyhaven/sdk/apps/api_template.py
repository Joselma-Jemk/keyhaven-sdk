# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `api_template` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ApiTemplateGetTemplateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    template_id: str


class ApiTemplateGetTemplateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ApiTemplateGetTemplateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ApiTemplateGetTemplateQuery
    header: ApiTemplateGetTemplateHeader = Field(default_factory=ApiTemplateGetTemplateHeader)


class ApiTemplateListTemplatesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: str = "300"
    offset: str = "0"
    format: Literal["PDF", "JPEG"] | None = None
    template_id: str | None = None
    group_name: str | None = None
    with_layer_info: Literal["0", "1"] = "0"


class ApiTemplateListTemplatesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ApiTemplateListTemplatesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ApiTemplateListTemplatesQuery | None = None
    header: ApiTemplateListTemplatesHeader = Field(default_factory=ApiTemplateListTemplatesHeader)


class ApiTemplateUpdateTemplateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ApiTemplateUpdateTemplateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    template_id: str
    css: str


class ApiTemplateUpdateTemplateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ApiTemplateUpdateTemplateHeader = Field(default_factory=ApiTemplateUpdateTemplateHeader)
    body: ApiTemplateUpdateTemplateBody


class ApiTemplateNamespace:
    """Typed accessor for the `api_template` app. Reach it via `client.apps.api_template`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_template(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApiTemplateGetTemplateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details for a single document template by its unique template ID. Returns comprehensive metadata including the template's configuration, format type, group, and associated layer information. This is a read-only operation with no side effects. Use when an agent needs to inspect a specific template's current state before editing, generating a document, or verifying its properties.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "api_template.get_template", owner_id, args, account_id=account_id
        )

    async def list_templates(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApiTemplateListTemplatesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all document templates in your APITemplate.io account with optional filters. Returns template metadata such as IDs, names, formats (PDF/JPEG), and group information. This is a read-only operation that does not create, modify, or delete any resources. Use when an agent needs to discover available templates, find a specific template ID, or audit which templates exist in the account.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "api_template.list_templates", owner_id, args, account_id=account_id
        )

    async def update_template(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ApiTemplateUpdateTemplateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing document template's CSS and HTML content by its template ID. This changes the visual rendering of any future documents (PDFs or JPEGs) generated from this template. The modification persists in your account and can be reversed by sending a subsequent update with the previous content. Use when an agent needs to change a template's layout, branding, or styling — for example, to update a company logo, modify document headers, or fix formatting issues.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "api_template.update_template", owner_id, args, account_id=account_id
        )
