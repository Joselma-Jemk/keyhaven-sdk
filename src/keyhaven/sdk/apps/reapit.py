# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `reapit` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ReapitApplicantsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int | None = None
    pageNumber: int | None = None
    sortBy: str | None = None
    embed: str | None = None
    marketingMode: str | None = None
    negotiatorId: str | None = None
    officeId: str | None = None


class ReapitApplicantsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ReapitApplicantsListQuery | None = None


class ReapitContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    forename: str | None = None
    surname: str | None = None
    dateOfBirth: str | None = None
    active: bool | None = None
    marketingConsent: str | None = None
    homePhone: str | None = None
    workPhone: str | None = None
    mobilePhone: str | None = None
    email: str | None = None
    officeIds: list[Any]
    negotiatorIds: list[Any]
    primaryAddress: dict[str, Any] | None = None
    metadata: dict[str, Any] | None = None


class ReapitContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ReapitContactCreateBody


class ReapitContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ReapitContactGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    embed: str | None = None


class ReapitContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ReapitContactGetPath
    query: ReapitContactGetQuery | None = None


class ReapitContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int | None = None
    pageNumber: int | None = None
    sortBy: str | None = None
    embed: str | None = None
    email: str | None = None
    name: str | None = None
    negotiatorId: str | None = None
    officeId: str | None = None
    active: bool | None = None
    createdFrom: str | None = None
    modifiedFrom: str | None = None


class ReapitContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ReapitContactsListQuery | None = None


class ReapitPropertiesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    pageSize: int | None = None
    pageNumber: int | None = None
    sortBy: str | None = None
    embed: str | None = None
    marketingMode: str | None = None
    negotiatorId: str | None = None
    officeId: str | None = None
    address: str | None = None


class ReapitPropertiesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ReapitPropertiesListQuery | None = None


class ReapitPropertyGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ReapitPropertyGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    embed: str | None = None


class ReapitPropertyGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ReapitPropertyGetPath
    query: ReapitPropertyGetQuery | None = None


class ReapitNamespace:
    """Typed accessor for the `reapit` app. Reach it via `client.apps.reapit`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def applicants_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReapitApplicantsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of applicant records from the agency CRM, with filtering by marketing mode (buying, renting), negotiator, and office. Supports sorting, pagination, and embedding related resources. Use this when you need to search for registered buyers or tenants, review applicant pipelines, or find an applicant ID for follow-up actions like arranging viewings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "reapit.applicants_list", owner_id, args, account_id=account_id
        )

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReapitContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact record in the agency CRM with personal details, contact information, and assignment to offices and negotiators. The contact becomes immediately searchable and available for property matching, viewing schedules, and communications. Use this when you need to register a new person from a web form, import leads, or onboard a new prospect into the agency system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "reapit.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReapitContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single contact record by its unique identifier, returning all stored details including personal info, marketing consent, and assigned negotiator. No data is modified — this is a read-only lookup. Use this when you need a specific contact's full profile for verification, updating, or preparing a communication.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "reapit.contact_get", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReapitContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of contact records from the agency CRM, with rich filtering by email, name, negotiator, office, active status, and creation or modification date. Supports sorting and embedding related resources. Use this when you need to search for a contact, generate a mailing list, or find a contact's ID for a follow-up operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "reapit.contacts_list", owner_id, args, account_id=account_id
        )

    async def properties_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReapitPropertiesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of property records held by the agency, with filtering by marketing mode (selling, letting), negotiator, office, and address. Supports sorting, pagination, and embedding related resources. Use this when you need to browse the agency's property portfolio, find available properties matching a criteria, or retrieve property IDs for detailed lookups.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "reapit.properties_list", owner_id, args, account_id=account_id
        )

    async def property_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReapitPropertyGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single property record by its unique identifier, returning full details including address, marketing mode, valuation, and assigned negotiator. No data is modified — this is a read-only lookup. Use this when you need a property's complete information for a viewing, valuation report, or to verify details before sending to a prospective buyer or tenant.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "reapit.property_get", owner_id, args, account_id=account_id
        )
