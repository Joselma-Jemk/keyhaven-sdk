# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `calendly` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CalendlyGetCurrentUserHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"


class CalendlyGetCurrentUserRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: CalendlyGetCurrentUserHeader = Field(default_factory=CalendlyGetCurrentUserHeader)


class CalendlyGetEventDetailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    uuid: str


class CalendlyGetEventDetailsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"


class CalendlyGetEventDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CalendlyGetEventDetailsPath
    header: CalendlyGetEventDetailsHeader = Field(default_factory=CalendlyGetEventDetailsHeader)


class CalendlyGetUserAvailabilityQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str


class CalendlyGetUserAvailabilityHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"


class CalendlyGetUserAvailabilityRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CalendlyGetUserAvailabilityQuery
    header: CalendlyGetUserAvailabilityHeader = Field(
        default_factory=CalendlyGetUserAvailabilityHeader
    )


class CalendlyListEventTypesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str | None = None
    organization: str | None = None
    group: str | None = None
    page_token: str | None = None
    count: int = 10
    status: Literal["active", "inactive"] | None = None


class CalendlyListEventTypesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"


class CalendlyListEventTypesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CalendlyListEventTypesQuery = Field(default_factory=CalendlyListEventTypesQuery)
    header: CalendlyListEventTypesHeader = Field(default_factory=CalendlyListEventTypesHeader)


class CalendlyListInviteesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    uuid: str


class CalendlyListInviteesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_token: str | None = None
    count: int = 10
    status: Literal["active", "canceled"] | None = None


class CalendlyListInviteesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"


class CalendlyListInviteesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CalendlyListInviteesPath
    query: CalendlyListInviteesQuery | None = None
    header: CalendlyListInviteesHeader = Field(default_factory=CalendlyListInviteesHeader)


class CalendlyListScheduledEventsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user: str | None = None
    organization: str | None = None
    group: str | None = None
    min_start_time: str | None = None
    max_start_time: str | None = None
    status: Literal["active", "canceled"] | None = None
    page_token: str | None = None
    count: int = 10


class CalendlyListScheduledEventsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: Literal["application/json"] = "application/json"


class CalendlyListScheduledEventsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CalendlyListScheduledEventsQuery = Field(
        default_factory=CalendlyListScheduledEventsQuery
    )
    header: CalendlyListScheduledEventsHeader = Field(
        default_factory=CalendlyListScheduledEventsHeader
    )


class CalendlyNamespace:
    """Typed accessor for the `calendly` app. Reach it via `client.apps.calendly`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_current_user(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalendlyGetCurrentUserRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns basic profile information about the authenticated Calendly user, such as name, email, timezone, and organisation URI. No data is modified. Use this to verify the authenticated identity or to discover the user's organisation URI for scoping subsequent queries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "calendly.get_current_user", owner_id, args, account_id=account_id
        )

    async def get_event_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalendlyGetEventDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches complete details for a single Calendly scheduled event by its UUID, including start and end times, location, status, and participant counts. No data is modified. Use this to inspect a specific meeting's full information when you already have the event UUID.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "calendly.get_event_details", owner_id, args, account_id=account_id
        )

    async def get_user_availability(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalendlyGetUserAvailabilityRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a user's availability schedules, which define the time slots during which they can be booked for events. No data is modified. Use this to determine when a user is available before proposing or confirming a meeting time.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "calendly.get_user_availability", owner_id, args, account_id=account_id
        )

    async def list_event_types(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalendlyListEventTypesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of event type configurations (for example '30-minute meeting' or 'Discovery Call') scoped to a user, organization, or group. Event types define the rules for how invitees can book time. No data is modified. Use this to understand what booking options are available before configuring scheduling.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "calendly.list_event_types", owner_id, args, account_id=account_id
        )

    async def list_invitees(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalendlyListInviteesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all invitees for a specific scheduled event, including their name, email, status (active or canceled), and any custom questions they answered during booking. No data is modified but the response contains personal information. Use this to see who has registered for a meeting or to follow up with attendees after an event.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "calendly.list_invitees", owner_id, args, account_id=account_id
        )

    async def list_scheduled_events(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalendlyListScheduledEventsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of scheduled events filtered by user, organization, or group, with optional time range and status filters. No events are created or modified. Use this to view upcoming meetings, audit past events, or find a specific event UUID for subsequent detail lookups.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "calendly.list_scheduled_events", owner_id, args, account_id=account_id
        )
