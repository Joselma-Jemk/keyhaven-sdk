# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `cognito_forms` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CognitoFormsDeleteFormEntryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str
    entryId: str


class CognitoFormsDeleteFormEntryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class CognitoFormsDeleteFormEntryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CognitoFormsDeleteFormEntryPath
    header: CognitoFormsDeleteFormEntryHeader = Field(
        default_factory=CognitoFormsDeleteFormEntryHeader
    )


class CognitoFormsGetFormEntryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str
    entryId: str


class CognitoFormsGetFormEntryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class CognitoFormsGetFormEntryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CognitoFormsGetFormEntryPath
    header: CognitoFormsGetFormEntryHeader = Field(default_factory=CognitoFormsGetFormEntryHeader)


class CognitoFormsGetFormsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = None
    orderby: str | None = None
    top: int | None = None
    skip: int | None = None


class CognitoFormsGetFormsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CognitoFormsGetFormsQuery | None = None


class CognitoFormsPatchFormEntryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str
    entryId: str


class CognitoFormsPatchFormEntryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class CognitoFormsPatchFormEntryBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Name: str | None = None
    Email: str | None = None
    Phone: str | None = None


class CognitoFormsPatchFormEntryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CognitoFormsPatchFormEntryPath
    header: CognitoFormsPatchFormEntryHeader = Field(
        default_factory=CognitoFormsPatchFormEntryHeader
    )
    body: CognitoFormsPatchFormEntryBody = Field(default_factory=CognitoFormsPatchFormEntryBody)


class CognitoFormsPostFormEntriesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str


class CognitoFormsPostFormEntriesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class CognitoFormsPostFormEntriesBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Name: str | None = None
    Email: str | None = None
    Phone: str | None = None


class CognitoFormsPostFormEntriesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CognitoFormsPostFormEntriesPath
    header: CognitoFormsPostFormEntriesHeader = Field(
        default_factory=CognitoFormsPostFormEntriesHeader
    )
    body: CognitoFormsPostFormEntriesBody = Field(default_factory=CognitoFormsPostFormEntriesBody)


class CognitoFormsNamespace:
    """Typed accessor for the `cognito_forms` app. Reach it via `client.apps.cognito_forms`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def delete_form_entry(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CognitoFormsDeleteFormEntryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a single entry from a specified Cognito Form by its ID. This operation is irreversible and deletes the submitted data from the system. An agent should only call this when a user explicitly instructs deletion of a specific form submission.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cognito_forms.delete_form_entry", owner_id, args, account_id=account_id
        )

    async def get_form_entry(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CognitoFormsGetFormEntryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single entry by its ID from a specified Cognito Form, returning all submitted field values. Use when an agent needs to inspect, process, or verify a specific form submission. The returned data includes the field values the submitter provided.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cognito_forms.get_form_entry", owner_id, args, account_id=account_id
        )

    async def get_forms(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CognitoFormsGetFormsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all forms in the authenticated Cognito Forms organization, with OData-style filtering, sorting, and pagination via filter, orderby, top, and skip parameters. Returns metadata such as form ID, name, and status for each form. Use when an agent needs to discover available forms before submitting or reading entries.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cognito_forms.get_forms", owner_id, args, account_id=account_id
        )

    async def patch_form_entry(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CognitoFormsPatchFormEntryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates specific fields of an existing entry in a Cognito Form without affecting other fields. Only the fields included in the request body are modified; omitted fields retain their original values. Use when an agent needs to correct or amend previously submitted form data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cognito_forms.patch_form_entry", owner_id, args, account_id=account_id
        )

    async def post_form_entries(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CognitoFormsPostFormEntriesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submits a new entry to a specified Cognito Form, creating a record in the form's submission data. The request body must match the form's field structure. Use when an agent needs to programmatically submit form data, such as registrations, orders, or contact inquiries. This creates a persistent entry visible in the form's results.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cognito_forms.post_form_entries", owner_id, args, account_id=account_id
        )
