# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `luma` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class LumaEventAddGuestsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    event_id: str


class LumaEventAddGuestsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    guests: list[Any]


class LumaEventAddGuestsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LumaEventAddGuestsQuery
    body: LumaEventAddGuestsBody


class LumaEventCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    start_at: str
    timezone: str
    description_md: str | None = None
    geo_address_json: dict[str, Any] | None = None
    meeting_url: str | None = None
    cover_url: str | None = None
    visibility: str | None = None
    slug: str | None = None


class LumaEventCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: LumaEventCreateBody


class LumaEventGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class LumaEventGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LumaEventGetQuery


class LumaEventsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    before: str | None = None
    after: str | None = None
    pagination_limit: int | None = None
    pagination_cursor: str | None = None
    sort_column: str | None = None
    sort_direction: str | None = None
    status: str | None = None
    platforms: list[Any] | None = None


class LumaEventsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LumaEventsListQuery | None = None


class LumaGuestGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class LumaGuestGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: LumaGuestGetQuery


class LumaNamespace:
    """Typed accessor for the `luma` app. Reach it via `client.apps.luma`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def event_add_guests(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LumaEventAddGuestsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds one or more guests to a specific Luma event with their email, name, and optional ticket type assignments. This may trigger registration confirmation emails to the guests depending on the event's settings. Use this to batch-register attendees from a waitlist, manually add VIPs, or migrate participants from another platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "luma.event_add_guests", owner_id, args, account_id=account_id
        )

    async def event_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LumaEventCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new event on Luma with a name, schedule, timezone, optional description in Markdown, location (physical or virtual), cover image, and visibility setting (public, members-only, or private). Once created, the event appears on the Luma calendar and can be discovered and joined by attendees. Use this to publish new meetups, conferences, or webinars programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "luma.event_create", owner_id, args, account_id=account_id
        )

    async def event_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LumaEventGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed admin information for a specific Luma event by its event ID (usually starting with evt-), including guest list, ticket sales, registration settings, and scheduling details. Use this to inspect an event's full configuration, verify setup, or gather data needed for guest management operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("luma.event_get", owner_id, args, account_id=account_id)

    async def events_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LumaEventsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists events from a Luma calendar with filtering by time range, status (approved/pending), and platform (Luma/external). Supports cursor-based pagination and sorting by start time. Use this to discover upcoming events, monitor pending approvals, or retrieve a calendar overview for reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("luma.events_list", owner_id, args, account_id=account_id)

    async def guest_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: LumaGuestGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves details of a single guest by their guest key (g-...) or ticket key, including registration data, check-in status, and assigned tickets. Useful for on-site check-in verification, confirming registration, or looking up a specific attendee's information before making changes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("luma.guest_get", owner_id, args, account_id=account_id)
