# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `openai` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class OpenaiFilesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    file_id: str


class OpenaiFilesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: OpenaiFilesGetPath


class OpenaiFilesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class OpenaiFineTunesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fine_tune_id: str


class OpenaiFineTunesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: OpenaiFineTunesGetPath


class OpenaiFineTunesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class OpenaiModelsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class OpenaiNamespace:
    """Typed accessor for the `openai` app. Reach it via `client.apps.openai`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def files_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenaiFilesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves metadata about a single file by its file ID, including its filename, size, purpose, status, and processing state. Use this after a file upload to check whether the file has been processed successfully or before referencing it in a fine-tuning or completion workflow. Requires the file_id path parameter obtained from a prior list or upload operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("openai.files_get", owner_id, args, account_id=account_id)

    async def files_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenaiFilesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all files uploaded to the user's OpenAI organization, including training datasets, fine-tune outputs, and search documents. Each file entry includes metadata such as filename, size, purpose, and creation timestamp. Use this to discover existing file IDs needed for file retrieval or fine-tune job creation. No parameters are required; all files visible to the organization are returned.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "openai.files_list", owner_id, args, account_id=account_id
        )

    async def fine_tunes_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenaiFineTunesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a specific fine-tuning job, including its current status, hyperparameters, training file IDs, base model, and the resulting fine-tuned model name upon completion. Use this to check whether a job has finished, inspect training metrics (loss, accuracy), or obtain the model ID for use in completions. Requires the fine_tune_id path parameter.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "openai.fine_tunes_get", owner_id, args, account_id=account_id
        )

    async def fine_tunes_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenaiFineTunesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all fine-tuning jobs created in the user's organization, including their current status (pending, running, succeeded, failed), base model, training file, and creation date. Use this to monitor the progress of ongoing fine-tuning jobs or to retrieve job IDs needed for detailed inspection or cancellation. No parameters are required; all jobs are returned with summary metadata.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "openai.fine_tunes_list", owner_id, args, account_id=account_id
        )

    async def models_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: OpenaiModelsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all OpenAI models accessible to the user's account, including both OpenAI-owned base models (GPT-4, GPT-3.5, DaVinci, etc.) and any fine-tuned models created by the organization. Each model entry includes its model ID, owning organization, and availability status. Use this to discover available model IDs before sending completion, embedding, or moderation requests.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "openai.models_list", owner_id, args, account_id=account_id
        )
