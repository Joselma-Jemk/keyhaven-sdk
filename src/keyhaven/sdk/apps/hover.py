# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `hover` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class HoverDeliverablesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    wallet_id: int


class HoverDeliverablesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HoverDeliverablesListQuery


class HoverJobCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    job: dict[str, Any]


class HoverJobCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: HoverJobCreateBody


class HoverJobGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    job_id: int


class HoverJobGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HoverJobGetPath


class HoverJobUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    job_id: int


class HoverJobUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    job: dict[str, Any]


class HoverJobUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HoverJobUpdatePath
    body: HoverJobUpdateBody


class HoverJobsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    status: str | None = None
    updated_since: str | None = None


class HoverJobsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HoverJobsListQuery | None = None


class HoverWebhooksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None


class HoverWebhooksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HoverWebhooksListQuery | None = None


class HoverNamespace:
    """Typed accessor for the `hover` app. Reach it via `client.apps.hover`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def deliverables_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoverDeliverablesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all available deliverable products scoped to a specific wallet ID, including their types, pricing, and availability. Deliverables determine what Hover will produce (e.g., measured drawings, 3D models, reports). Use this to check which deliverables can be ordered before creating a job, so you match customer needs with available products.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "hover.deliverables_list", owner_id, args, account_id=account_id
        )

    async def job_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoverJobCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submits a new measurement request for a property, specifying customer contact details and the desired deliverable type. This starts Hover's measurement process, which involves photo capture, modeling, and deliverable generation — a paid service that may incur charges. Use this when you need professional exterior measurements for a new construction, renovation, or product installation project.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("hover.job_create", owner_id, args, account_id=account_id)

    async def job_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoverJobGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single Hover job by its ID, including property information, model data, and downloadable deliverable links (such as measured drawings and 3D models). Use this when you need to access the actual measurement results or verify job completion before proceeding with an installation or order.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("hover.job_get", owner_id, args, account_id=account_id)

    async def job_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoverJobUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the metadata of an existing Hover job, such as customer name, email, phone, or property address details. Changes apply to the job record without restarting the measurement process. Use this to correct or refine job information after creation, for example when a customer notifies you of an address or name change.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("hover.job_update", owner_id, args, account_id=account_id)

    async def jobs_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoverJobsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of measurement jobs for the authenticated organization. Each job entry includes status, property address, customer info, and creation date, with optional filtering by status or update timestamp. Use this to browse current and past measurement projects without modifying any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("hover.jobs_list", owner_id, args, account_id=account_id)

    async def webhooks_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HoverWebhooksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all webhook endpoints currently registered for the authenticated organization, including their target URLs and event subscriptions. Webhooks enable real-time notifications when job statuses change or deliverables become available. Use this to audit your integration's notification setup or verify that webhooks are properly configured before relying on event-driven workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "hover.webhooks_list", owner_id, args, account_id=account_id
        )
