# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `wordpress` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class WordpressMediaListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site: str


class WordpressMediaListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    number: int = 20
    offset: int | None = None
    mime_type: str | None = None
    post_ID: int | None = None


class WordpressMediaListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WordpressMediaListPath
    query: WordpressMediaListQuery | None = None


class WordpressPostCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site: str


class WordpressPostCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    content: str | None = None
    excerpt: str | None = None
    slug: str | None = None
    status: Literal["publish", "private", "draft", "pending", "future", "auto-draft"] = "publish"
    sticky: bool | None = None
    password: str | None = None
    categories: str | None = None
    tags: str | None = None
    format: (
        Literal[
            "standard",
            "aside",
            "chat",
            "gallery",
            "link",
            "image",
            "quote",
            "status",
            "video",
            "audio",
        ]
        | None
    ) = None
    date: str | None = None
    featured_image: str | None = None


class WordpressPostCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WordpressPostCreatePath
    body: WordpressPostCreateBody = Field(default_factory=WordpressPostCreateBody)


class WordpressPostGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site: str
    post_id: int


class WordpressPostGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    context: Literal["display", "edit"] = "display"


class WordpressPostGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WordpressPostGetPath
    query: WordpressPostGetQuery | None = None


class WordpressPostUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site: str
    post_id: int


class WordpressPostUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    content: str | None = None
    excerpt: str | None = None
    slug: str | None = None
    status: str | None = None
    categories: str | None = None
    tags: str | None = None
    featured_image: str | None = None


class WordpressPostUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WordpressPostUpdatePath
    body: WordpressPostUpdateBody = Field(default_factory=WordpressPostUpdateBody)


class WordpressPostsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    site: str


class WordpressPostsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    number: int = 20
    offset: int | None = None
    page: int | None = None
    order: Literal["ASC", "DESC"] = "DESC"
    order_by: Literal["date", "modified", "title", "comment_count", "ID"] | None = None
    search: str | None = None
    status: str = "publish"
    type_: str = Field(default="post", alias="type")
    category: str | None = None
    tag: str | None = None
    author: int | None = None


class WordpressPostsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WordpressPostsListPath
    query: WordpressPostsListQuery | None = None


class WordpressSitesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None
    site_visibility: Literal["visible", "hidden", "all"] | None = None
    include_domain_only: bool | None = None


class WordpressSitesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WordpressSitesListQuery | None = None


class WordpressNamespace:
    """Typed accessor for the `wordpress` app. Reach it via `client.apps.wordpress`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def media_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WordpressMediaListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of media items (images, videos, documents) attached to a site, filterable by MIME type and associated post ID. This read-only operation returns file URLs, titles, descriptions, and metadata like dimensions and file sizes. Use this function when an agent needs to find existing media for use in posts or check what files are stored in the site's media library — no data is created, modified, or deleted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wordpress.media_list", owner_id, args, account_id=account_id
        )

    async def post_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WordpressPostCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new post on the specified WordPress.com site with title, HTML content, excerpt, slug, status (publish, draft, pending, etc.), categories, tags, format, publication date, and optional featured image. The post is immediately saved and becomes visible on the site if the status is publish. Use this function when an agent needs to author content programmatically — from drafts for review to published articles that appear on the site's frontend.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wordpress.post_create", owner_id, args, account_id=account_id
        )

    async def post_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WordpressPostGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single post's full content, title, excerpt, metadata, featured image, categories, and tags by its numeric ID and site. The context parameter controls whether the response is for display (rendered content) or edit (includes raw content and additional fields). This is read-only and does not modify anything. Use this function when an agent needs to read an existing post's content or verify its configuration before updating or referencing it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wordpress.post_get", owner_id, args, account_id=account_id
        )

    async def post_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WordpressPostUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing post's title, content, excerpt, slug, status, categories, tags, or featured image on the specified site. Changes are applied immediately and the updated version is served to visitors if the status is publish. Use this function when an agent needs to edit published content, change a post's publication status (e.g., promoting a draft to publish), or update metadata — the modification is live right away.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wordpress.post_update", owner_id, args, account_id=account_id
        )

    async def posts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WordpressPostsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of posts for a given site, filterable by status (publish, draft, pending, etc.), type, category, tag, author, search term, and sortable by date, title, or comment count. This read-only operation lets agents browse content without modifying anything. Use this function to discover published, draft, or scheduled content across a site before performing operations like editing or deleting specific posts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wordpress.posts_list", owner_id, args, account_id=account_id
        )

    async def sites_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WordpressSitesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all WordPress.com sites accessible to the authenticated user, with fields for site ID, URL, name, visibility, and options. This read-only operation returns site metadata without modifying anything. Use this function at the start of a session to discover which sites the agent can manage and obtain their site identifiers for subsequent operations like reading or creating posts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wordpress.sites_list", owner_id, args, account_id=account_id
        )
