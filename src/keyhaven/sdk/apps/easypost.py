# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `easypost` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class EasypostAddressesCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    street1: str
    street2: str | None = None
    city: str
    state: str
    zip: str
    country: str | None = None
    company: str | None = None
    phone: str | None = None
    email: str | None = None


class EasypostAddressesCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: EasypostAddressesCreateBody


class EasypostParcelsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    length: float | None = None
    width: float | None = None
    height: float | None = None
    weight: float
    predefined_package: str | None = None


class EasypostParcelsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: EasypostParcelsCreateBody


class EasypostShipmentGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class EasypostShipmentGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: EasypostShipmentGetPath


class EasypostShipmentsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to_address: dict[str, Any]
    from_address: dict[str, Any]
    parcel: dict[str, Any]
    options: dict[str, Any] | None = None
    carrier_accounts: list[Any] | None = None


class EasypostShipmentsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: EasypostShipmentsCreateBody


class EasypostTrackerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tracking_code: str
    carrier: str


class EasypostTrackerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: EasypostTrackerCreateBody


class EasypostTrackersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_size: int | None = None
    before_id: str | None = None
    after_id: str | None = None
    start_datetime: str | None = None
    end_datetime: str | None = None


class EasypostTrackersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: EasypostTrackersListQuery | None = None


class EasypostNamespace:
    """Typed accessor for the `easypost` app. Reach it via `client.apps.easypost`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def addresses_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EasypostAddressesCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates and verifies a new address record in EasyPost for use as an origin or destination on shipments, labels, and orders. Addresses are stored and can be reused across multiple shipments. Use this when you need to define a sender or recipient location before creating shipments or purchasing postage.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "easypost.addresses_create", owner_id, args, account_id=account_id
        )

    async def parcels_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EasypostParcelsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a parcel record with specified dimensions and weight, which can be reused across shipments for rate quoting and label generation. You can define exact measurements or use a predefined package type like FlatRateEnvelope. Use this to describe the physical package before requesting carrier rates or purchasing shipping labels.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "easypost.parcels_create", owner_id, args, account_id=account_id
        )

    async def shipment_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EasypostShipmentGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a previously created shipment by its unique identifier, including current state, selected rate, purchased label details, and tracking information. Use this to check shipment status, retrieve label URLs or barcodes after purchase, or access tracking data to monitor delivery progress.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "easypost.shipment_get", owner_id, args, account_id=account_id
        )

    async def shipments_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EasypostShipmentsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a shipment object linking origin and destination addresses with a parcel, then retrieves available shipping rates from all configured carrier accounts. The shipment is not purchased at this stage — this step only collects rate quotes for comparison. Use this when you need to compare carrier prices and service levels before deciding which label to buy.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "easypost.shipments_create", owner_id, args, account_id=account_id
        )

    async def tracker_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EasypostTrackerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new package tracker for a specific carrier tracking code, enabling automatic status updates and webhook callbacks as the package moves through delivery. EasyPost begins polling the carrier and recording scan events immediately. Use this when you have a tracking number from a carrier and want to monitor its delivery progress through EasyPost's unified tracking system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "easypost.tracker_create", owner_id, args, account_id=account_id
        )

    async def trackers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: EasypostTrackersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all package trackers in the EasyPost account with optional date range and pagination filters. Each tracker shows the current delivery status, scan history, and estimated delivery date for a tracked package. Use this to review all active tracking records, find a specific tracker by date range, or audit delivery statuses across multiple shipments.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "easypost.trackers_list", owner_id, args, account_id=account_id
        )
