# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `miro` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MiroBoardCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    teamId: str | None = None
    projectId: str | None = None
    policy: dict[str, Any] | None = None


class MiroBoardCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MiroBoardCreateBody


class MiroBoardGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    board_id: str


class MiroBoardGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MiroBoardGetPath


class MiroBoardItemsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    board_id: str


class MiroBoardItemsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    type_: str | None = Field(default=None, alias="type")
    cursor: str | None = None


class MiroBoardItemsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MiroBoardItemsListPath
    query: MiroBoardItemsListQuery | None = None


class MiroBoardUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    board_id: str


class MiroBoardUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None
    teamId: str | None = None
    projectId: str | None = None
    policy: dict[str, Any] | None = None


class MiroBoardUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MiroBoardUpdatePath
    body: MiroBoardUpdateBody = Field(default_factory=MiroBoardUpdateBody)


class MiroBoardsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str | None = None
    project_id: str | None = None
    query: str | None = None
    owner: str | None = None
    limit: int | None = None
    offset: int | None = None
    sort: str | None = None


class MiroBoardsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MiroBoardsListQuery | None = None


class MiroStickyNoteCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    board_id: str


class MiroStickyNoteCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any]
    style: dict[str, Any] | None = None
    position: dict[str, Any] | None = None
    geometry: dict[str, Any] | None = None
    parent: dict[str, Any] | None = None


class MiroStickyNoteCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MiroStickyNoteCreatePath
    body: MiroStickyNoteCreateBody


class MiroNamespace:
    """Typed accessor for the `miro` app. Reach it via `client.apps.miro`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def board_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MiroBoardCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new Miro board in the specified team or project, with a name, description, and optional sharing policy (permissions and sharing settings). The board becomes immediately visible to team members according to the policy. Use this when an agent needs to set up a new collaborative workspace. This action is recoverable by deleting the board.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "miro.board_create", owner_id, args, account_id=account_id
        )

    async def board_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MiroBoardGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve full metadata of a single Miro board by its unique ID, including its name, description, owner, sharing policy, and timestamps. Use this to inspect a board's configuration before updating it or adding items. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("miro.board_get", owner_id, args, account_id=account_id)

    async def board_items_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MiroBoardItemsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of all items on a Miro board (sticky notes, shapes, cards, text, images, frames, embeds, etc.), optionally filtered by item type. Use this to discover existing content on a board before reading or modifying specific items. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "miro.board_items_list", owner_id, args, account_id=account_id
        )

    async def board_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MiroBoardUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modify an existing Miro board's name, description, team or project assignment, or sharing/permissions policy. Changes take effect immediately for all collaborators. Use this to rename a board, move it between projects, or tighten/loosen access controls. Policy changes can affect who can view or edit the board, so review carefully before applying.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "miro.board_update", owner_id, args, account_id=account_id
        )

    async def boards_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MiroBoardsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of all Miro boards accessible to the authenticated user, with optional filtering by team, project, owner, or free-text search, and sorting by recency or alphabetically. Use this to discover available boards before performing operations on a specific one. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("miro.boards_list", owner_id, args, account_id=account_id)

    async def sticky_note_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MiroStickyNoteCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Add a sticky note to a Miro board at a specified position, with custom content (text or HTML), colors, and styling. The sticky note is immediately visible to all board collaborators. Use this to annotate, brainstorm, or leave feedback directly on the board. This action is reversible by deleting the sticky note.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "miro.sticky_note_create", owner_id, args, account_id=account_id
        )
