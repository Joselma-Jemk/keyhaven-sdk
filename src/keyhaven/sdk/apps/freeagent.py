# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `freeagent` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FreeagentContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact: dict[str, Any]


class FreeagentContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: FreeagentContactCreateBody


class FreeagentContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class FreeagentContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreeagentContactGetPath


class FreeagentContactUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class FreeagentContactUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact: dict[str, Any]


class FreeagentContactUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreeagentContactUpdatePath
    body: FreeagentContactUpdateBody


class FreeagentContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    sort: str | None = None
    from_date: str | None = None
    to_date: str | None = None
    updated_since: str | None = None


class FreeagentContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FreeagentContactsListQuery | None = None


class FreeagentInvoiceCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    invoice: dict[str, Any]


class FreeagentInvoiceCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: FreeagentInvoiceCreateBody


class FreeagentInvoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    sort: str | None = None
    from_date: str | None = None
    to_date: str | None = None
    updated_since: str | None = None


class FreeagentInvoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FreeagentInvoicesListQuery | None = None


class FreeagentNamespace:
    """Typed accessor for the `freeagent` app. Reach it via `client.apps.freeagent`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreeagentContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact record in FreeAgent with fields such as name, email, phone, organization name, and address details. The new contact becomes immediately available for invoicing, expense tracking, and other transactions in FreeAgent. This action is reversible by deleting or archiving the contact. Use it when onboarding a new client or supplier, importing contacts from another system, or creating a one-time contact for an invoice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freeagent.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreeagentContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single FreeAgent contact by its unique ID, including all address fields, phone, email, and organization name. This read-only operation does not alter any data in FreeAgent. Use it when you need to inspect a specific contact's information before creating an invoice, verify details, or display contact information in a user interface.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freeagent.contact_get", owner_id, args, account_id=account_id
        )

    async def contact_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreeagentContactUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more fields of an existing FreeAgent contact identified by its unique ID. Changes take effect immediately and affect future invoices, expenses, and documents referencing this contact. This action is reversible by restoring the previous field values. Use it when correcting contact information, updating addresses, or modifying communication preferences.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freeagent.contact_update", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreeagentContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of contacts from FreeAgent, with optional filtering by date range (created from/to), sort order, and last-updated timestamp. Each contact includes name, email, phone, addresses, and organization. This is a read-only query that does not create or modify any FreeAgent data. Use it when searching for existing contacts, populating a contact picker, or syncing contact data to another system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freeagent.contacts_list", owner_id, args, account_id=account_id
        )

    async def invoice_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreeagentInvoiceCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new invoice in FreeAgent with contact, issue date, payment terms, currency, and line items (description, price, quantity). Once created, the invoice appears in FreeAgent's dashboard and can be sent to the contact, recorded as revenue, or exported for accounting. Use it when billing clients for work completed, setting up recurring invoices, or recording sales. The invoice can be amended or credited later if needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freeagent.invoice_create", owner_id, args, account_id=account_id
        )

    async def invoices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreeagentInvoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of invoices from FreeAgent, with optional filtering by date range (created from/to), sort order, and last-updated timestamp. Each invoice includes status, contact, dates, line items, totals, and tax breakdowns. This is a read-only query that does not modify any FreeAgent data. Use it when reviewing outstanding invoices, auditing billing history, or syncing invoice data to external reporting and analytics tools.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freeagent.invoices_list", owner_id, args, account_id=account_id
        )
