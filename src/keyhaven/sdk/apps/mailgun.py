# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `mailgun` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MailgunDomainCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    smtp_password: str | None = None
    spam_action: str | None = None
    wildcard: bool | None = None
    force_dkim_authority: bool | None = None
    dkim_key_size: str | None = None
    ips: str | None = None
    pool_id: str | None = None
    web_scheme: str | None = None


class MailgunDomainCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MailgunDomainCreateBody


class MailgunDomainsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    authority: str | None = None
    state: str | None = None
    limit: int | None = None
    skip: int | None = None


class MailgunDomainsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MailgunDomainsListQuery | None = None


class MailgunEventsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain_name: str


class MailgunEventsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    begin: str | None = None
    end: str | None = None
    ascending: str | None = None
    limit: int | None = None
    event: str | None = None
    list: str | None = None
    from_: str | None = Field(default=None, alias="from")
    to: str | None = None
    subject: str | None = None
    recipient: str | None = None
    message_id: str | None = Field(default=None, alias="message-id")
    tags: str | None = None
    severity: str | None = None


class MailgunEventsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailgunEventsListPath
    query: MailgunEventsListQuery | None = None


class MailgunMailingListCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str
    name: str | None = None
    description: str | None = None
    access_level: str | None = None
    reply_preference: str | None = None


class MailgunMailingListCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MailgunMailingListCreateBody


class MailgunMailingListMemberAddPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    list_address: str


class MailgunMailingListMemberAddBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str
    name: str | None = None
    vars: str | None = None
    subscribed: str | None = None
    upsert: str | None = None


class MailgunMailingListMemberAddRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailgunMailingListMemberAddPath
    body: MailgunMailingListMemberAddBody


class MailgunMailingListsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    address: str | None = None
    limit: int | None = None


class MailgunMailingListsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MailgunMailingListsListQuery | None = None


class MailgunMessageSendPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain_name: str


class MailgunMessageSendBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: str = Field(alias="from")
    to: str
    cc: str | None = None
    bcc: str | None = None
    subject: str
    text: str | None = None
    html: str | None = None
    template: str | None = None
    t_variables: str | None = Field(default=None, alias="t:variables")
    o_tag: str | None = Field(default=None, alias="o:tag")
    o_tracking: str | None = Field(default=None, alias="o:tracking")
    o_deliverytime: str | None = Field(default=None, alias="o:deliverytime")
    recipient_variables: str | None = Field(default=None, alias="recipient-variables")


class MailgunMessageSendRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailgunMessageSendPath
    body: MailgunMessageSendBody


class MailgunNamespace:
    """Typed accessor for the `mailgun` app. Reach it via `client.apps.mailgun`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def domain_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailgunDomainCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Registers a new sending domain in the Mailgun account with optional SMTP password, spam handling action, DKIM key size, and IP pool assignment. After creation, the domain requires DNS configuration (TXT records) outside of this API before it can send emails. Use this to onboard a new sending domain for a project or client. This action has infrastructure and billing implications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailgun.domain_create", owner_id, args, account_id=account_id
        )

    async def domains_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailgunDomainsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of all domains registered with the Mailgun account, with their states (active, unverified, disabled), DNS settings, and metadata. Use this when an agent needs to verify which domains are available for sending, check domain verification status, or select a domain for other operations. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailgun.domains_list", owner_id, args, account_id=account_id
        )

    async def events_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailgunEventsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of tracked email events (accepted, delivered, opened, clicked, failed, unsubscribed, etc.) for a sending domain, filterable by time range, event type, recipient, and other criteria. Use this when an agent needs to audit delivery performance, investigate bounces, or track engagement for sent messages. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailgun.events_list", owner_id, args, account_id=account_id
        )

    async def mailing_list_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailgunMailingListCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new mailing list (a group email address that fans out messages to its members) with a specified address, display name, description, posting access level, and reply preference. The list is initially empty and no messages are sent by this action. Use this when an agent needs to set up a new discussion group or announcement list. This action is reversible by deleting the list.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailgun.mailing_list_create", owner_id, args, account_id=account_id
        )

    async def mailing_list_member_add(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailgunMailingListMemberAddRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new member (subscriber) to a mailing list with their email address, optional display name, custom variables, and subscription preference. If `upsert` is enabled, an existing member's data is updated instead of raising an error. Once added, the member may receive all messages sent to the list. Use this to build a mailing list subscriber base. This action affects who receives list communications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailgun.mailing_list_member_add", owner_id, args, account_id=account_id
        )

    async def mailing_lists_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailgunMailingListsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of all mailing lists configured on the Mailgun account, with their addresses, names, descriptions, and member counts. Use this when an agent needs to find an existing mailing list, verify list existence, or select a list for member management. This operation is read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailgun.mailing_lists_list", owner_id, args, account_id=account_id
        )

    async def message_send(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailgunMessageSendRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends an email message through a configured Mailgun sending domain, supporting plain text, HTML, stored templates, tags, and delivery optimisation options. The email is immediately dispatched to the specified recipients (to, cc, bcc) and cannot be recalled after sending. Use this when an agent needs to send a transactional email, notification, or marketing message. This action has real-world consequences — recipients receive the email and the sender's domain reputation may be affected.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailgun.message_send", owner_id, args, account_id=account_id
        )
