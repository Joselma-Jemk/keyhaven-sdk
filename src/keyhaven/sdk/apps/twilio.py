# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `twilio` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TwilioAccountsAddressesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    AccountSid: str


class TwilioAccountsAddressesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    CustomerName: str | None = None
    FriendlyName: str | None = None
    IsoCountry: str | None = None
    PageSize: int | None = None
    Page: int | None = None
    PageToken: str | None = None


class TwilioAccountsAddressesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwilioAccountsAddressesListPath
    query: TwilioAccountsAddressesListQuery | None = None


class TwilioAccountsBalanceListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    AccountSid: str


class TwilioAccountsBalanceListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwilioAccountsBalanceListPath


class TwilioAccountsCallsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    AccountSid: str
    Sid: str


class TwilioAccountsCallsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwilioAccountsCallsGetPath


class TwilioAccountsCallsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    AccountSid: str


class TwilioAccountsCallsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    To: str | None = None
    From: str | None = None
    ParentCallSid: str | None = None
    Status: str | None = None
    StartTime: str | None = None
    StartTime_: str | None = Field(default=None, alias="StartTime<")
    StartTime__2: str | None = Field(default=None, alias="StartTime>")
    EndTime: str | None = None
    EndTime_: str | None = Field(default=None, alias="EndTime<")
    EndTime__2: str | None = Field(default=None, alias="EndTime>")
    PageSize: int | None = None
    Page: int | None = None
    PageToken: str | None = None


class TwilioAccountsCallsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwilioAccountsCallsListPath
    query: TwilioAccountsCallsListQuery | None = None


class TwilioAccountsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    FriendlyName: str | None = None
    Status: str | None = None
    PageSize: int | None = None
    Page: int | None = None
    PageToken: str | None = None


class TwilioAccountsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwilioAccountsListQuery | None = None


class TwilioNamespace:
    """Typed accessor for the `twilio` app. Reach it via `client.apps.twilio`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def accounts_addresses_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwilioAccountsAddressesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all registered physical addresses associated with a Twilio account, filterable by customer name, friendly name, or ISO country code. These addresses are used for regulatory compliance purposes such as emergency services address registration and may contain street-level personal information. Use this to audit registered locations, verify compliance data across sub-accounts, or find a specific address entry.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twilio.accounts_addresses_list", owner_id, args, account_id=account_id
        )

    async def accounts_balance_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwilioAccountsBalanceListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the current monetary balance and currency for a specified Twilio account, showing the amount of credit available for use. This is read-only billing data that reflects the account's financial standing with Twilio. Use this to check remaining credit before placing calls or sending messages, or to monitor spending thresholds.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twilio.accounts_balance_list", owner_id, args, account_id=account_id
        )

    async def accounts_calls_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwilioAccountsCallsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed metadata for a single phone call on a Twilio account using its unique Call SID, including its status, duration, direction, start and end times, caller and callee phone numbers, and associated cost. The response is read-only and reflects the call's final or current state. Use this to inspect a specific call's full details after identifying it via a list operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twilio.accounts_calls_get", owner_id, args, account_id=account_id
        )

    async def accounts_calls_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwilioAccountsCallsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of phone calls made or received by a Twilio account, filterable by caller number, callee number, call status, parent call, and start or end time ranges. Each result includes the call SID, duration, direction, cost, and timestamps. Use this to audit call history, analyze usage patterns, or locate a specific call for further inspection.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twilio.accounts_calls_list", owner_id, args, account_id=account_id
        )

    async def accounts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwilioAccountsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all sub-accounts associated with the authenticated Twilio main account, filterable by friendly name or status (active, suspended, or closed). Each entry includes the account SID, name, status, type, owner, and creation date. Use this to manage multi-account setups, audit account states, or locate a specific sub-account for administration.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twilio.accounts_list", owner_id, args, account_id=account_id
        )
