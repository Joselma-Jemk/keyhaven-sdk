# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `instagram` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class InstagramLocationsSearchListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    distance: int | None = None
    facebook_places_id: str | None = None
    foursquare_id: str | None = None
    lat: float | None = None
    lng: float | None = None
    foursquare_v2_id: str | None = None


class InstagramLocationsSearchListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: InstagramLocationsSearchListQuery | None = None


class InstagramMediaGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    media_id: str = Field(alias="media-id")


class InstagramMediaGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: InstagramMediaGetPath


class InstagramMediaSearchListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    lat: float
    lng: float
    min_timestamp: int | None = None
    max_timestamp: int | None = None
    distance: int | None = None


class InstagramMediaSearchListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: InstagramMediaSearchListQuery


class InstagramUsersGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str = Field(alias="user-id")


class InstagramUsersGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: InstagramUsersGetPath


class InstagramUsersSearchListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    q: str
    count: int | None = None


class InstagramUsersSearchListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: InstagramUsersSearchListQuery


class InstagramNamespace:
    """Typed accessor for the `instagram` app. Reach it via `client.apps.instagram`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def locations_search_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InstagramLocationsSearchListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for Instagram location place IDs by latitude, longitude, or external identifiers such as Facebook Places ID and Foursquare IDs. Returns matching locations with their names, coordinates, and associated place IDs. This operation is read-only and does not modify any data. Use this to find location IDs for filtering media searches or attaching geographic context to content analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "instagram.locations_search_list", owner_id, args, account_id=account_id
        )

    async def media_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InstagramMediaGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a single Instagram media item (photo or video) by its media ID. Returns metadata such as caption, comments count, like count, filter used, tags, and the media URL. This operation is read-only and does not modify any data. Use this to inspect the full details of a specific post found via a search or user feed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "instagram.media_get", owner_id, args, account_id=account_id
        )

    async def media_search_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InstagramMediaSearchListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for Instagram media (photos and videos) published within a geographic area defined by latitude, longitude, and optional radius in meters. Results can be further narrowed with minimum and maximum UNIX timestamp filters to constrain by posting time. This operation is read-only and does not modify any data. Use this to discover location-based content, such as posts tagged at a specific venue or neighborhood.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "instagram.media_search_list", owner_id, args, account_id=account_id
        )

    async def users_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InstagramUsersGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full profile details of a specific Instagram user by their numeric user ID. Returns metadata including username, full name, biography, profile picture URL, website, and media count. This operation is read-only and does not modify any data. Use this after obtaining a user ID from a search to get complete profile information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "instagram.users_get", owner_id, args, account_id=account_id
        )

    async def users_search_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InstagramUsersSearchListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches Instagram for public user profiles matching a name query. Returns a list of matching profiles with basic metadata such as ID, username, and profile picture URL. This operation is read-only and does not modify any data. Use this to discover Instagram accounts by name before retrieving detailed information on a specific user.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "instagram.users_search_list", owner_id, args, account_id=account_id
        )
