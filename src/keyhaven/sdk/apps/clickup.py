# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `clickup` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ClickupDocsSearchPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspaceId: str


class ClickupDocsSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str | None = None
    creator: float | None = None
    deleted: bool = False
    archived: bool = False
    parent_id: str | None = None
    parent_type: str | None = None
    limit: float = 50
    next_cursor: str | None = None


class ClickupDocsSearchHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ClickupDocsSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClickupDocsSearchPath
    query: ClickupDocsSearchQuery | None = None
    header: ClickupDocsSearchHeader = Field(default_factory=ClickupDocsSearchHeader)


class ClickupGetAuthorizedWorkspacesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ClickupGetAuthorizedWorkspacesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ClickupGetAuthorizedWorkspacesHeader = Field(
        default_factory=ClickupGetAuthorizedWorkspacesHeader
    )


class ClickupGoalsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class ClickupGoalsGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_completed: bool = True


class ClickupGoalsGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ClickupGoalsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClickupGoalsGetPath
    query: ClickupGoalsGetQuery | None = None
    header: ClickupGoalsGetHeader = Field(default_factory=ClickupGoalsGetHeader)


class ClickupSpacesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class ClickupSpacesGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    archived: bool = False


class ClickupSpacesGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ClickupSpacesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClickupSpacesGetPath
    query: ClickupSpacesGetQuery | None = None
    header: ClickupSpacesGetHeader = Field(default_factory=ClickupSpacesGetHeader)


class ClickupWorkspaceSeatsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    team_id: str


class ClickupWorkspaceSeatsGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ClickupWorkspaceSeatsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClickupWorkspaceSeatsGetPath
    header: ClickupWorkspaceSeatsGetHeader = Field(default_factory=ClickupWorkspaceSeatsGetHeader)


class ClickupNamespace:
    """Typed accessor for the `clickup` app. Reach it via `client.apps.clickup`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def docs_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClickupDocsSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for Docs in a specified ClickUp Workspace, with filters for Doc ID, creator, deletion status, archival status, parent, and pagination. Only returns Docs the authenticated user can access. Use this to find specific documentation or to discover all Docs in a Workspace.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clickup.docs_search", owner_id, args, account_id=account_id
        )

    async def get_authorized_workspaces(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClickupGetAuthorizedWorkspacesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the list of ClickUp Workspaces (Teams) that the authenticated user has access to. Each workspace includes its ID, name, and membership role. Use this as the first step to determine which Workspace ID to use in subsequent ClickUp API calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clickup.get_authorized_workspaces", owner_id, args, account_id=account_id
        )

    async def goals_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClickupGoalsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all Goals (OKR-style objectives with key results) in a specified ClickUp Workspace. Can optionally include completed Goals. Goals are organizational targets used for team alignment. Use this to review progress on objectives, find a specific Goal's ID, or generate progress reports.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clickup.goals_get", owner_id, args, account_id=account_id
        )

    async def spaces_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClickupSpacesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all Spaces (top-level organizational folders) in a ClickUp Workspace, with optional filtering for archived Spaces. Private Spaces only show their content to authorized members. Use this to navigate the workspace structure or to find a Space ID before accessing its Lists and Tasks.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clickup.spaces_get", owner_id, args, account_id=account_id
        )

    async def workspace_seats_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClickupWorkspaceSeatsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the seat usage statistics for a ClickUp Workspace: total seats purchased, seats used, and available seats, broken down by members and guests. This is administrative metadata read-only. Use this to check if your Workspace has capacity to add new members or guests.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clickup.workspace_seats_get", owner_id, args, account_id=account_id
        )
