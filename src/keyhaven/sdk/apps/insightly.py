# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `insightly` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class InsightlyContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    FIRST_NAME: str
    LAST_NAME: str
    EMAIL_ADDRESS: str | None = None
    PHONE: str | None = None
    ORGANISATION_ID: int | None = None
    TAGS: list[Any] | None = None
    CUSTOMFIELDS: list[Any] | None = None


class InsightlyContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: InsightlyContactCreateBody


class InsightlyContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class InsightlyContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: InsightlyContactGetPath


class InsightlyContactUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    CONTACT_ID: int
    FIRST_NAME: str | None = None
    LAST_NAME: str | None = None
    EMAIL_ADDRESS: str | None = None
    PHONE: str | None = None
    TAGS: list[Any] | None = None
    CUSTOMFIELDS: list[Any] | None = None


class InsightlyContactUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: InsightlyContactUpdateBody


class InsightlyContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    top: int | None = None
    skip: int | None = None
    count_total: bool | None = None
    brief: str | None = None


class InsightlyContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: InsightlyContactsListQuery | None = None


class InsightlyContactsSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    field_name: str
    field_value: str
    top: int | None = None
    skip: int | None = None


class InsightlyContactsSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: InsightlyContactsSearchQuery


class InsightlyOpportunitiesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    top: int | None = None
    skip: int | None = None
    count_total: bool | None = None
    brief: str | None = None


class InsightlyOpportunitiesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: InsightlyOpportunitiesListQuery | None = None


class InsightlyNamespace:
    """Typed accessor for the `insightly` app. Reach it via `client.apps.insightly`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InsightlyContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new person record to Insightly CRM with required first and last names, plus optional email, phone, organisation association, tags, and custom fields. The contact is immediately visible in the CRM and available for pipeline and project associations. Use this to capture new leads from external sources or to bulk-import contacts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "insightly.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InsightlyContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the complete record of a single contact by its Insightly ID, including all standard fields, custom fields, and associated tags. Use this when you need full context on a specific contact before updating related records, creating opportunities, or sending communications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "insightly.contact_get", owner_id, args, account_id=account_id
        )

    async def contact_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InsightlyContactUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Replaces the fields of an existing Insightly contact using a full PUT operation (all relevant fields should be provided). The updated record is immediately reflected across the CRM, linked opportunities, and projects. Use this to synchronise or correct contact information from an authoritative external source.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "insightly.contact_update", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InsightlyContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of contact records from your Insightly CRM instance, with options to limit results, skip records, or request a brief response with only essential fields. Use this to browse contacts or feed data into external systems without writing any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "insightly.contacts_list", owner_id, args, account_id=account_id
        )

    async def contacts_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InsightlyContactsSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches for contacts by a specified field name and value, enabling lookups by any standard or custom field such as email, phone, or external ID. Returns matching contacts with pagination support. Use this when you need to find a specific contact by a known identifier without knowing their Insightly internal ID.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "insightly.contacts_search", owner_id, args, account_id=account_id
        )

    async def opportunities_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: InsightlyOpportunitiesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of sales opportunities (deals) from Insightly CRM, with optional skip, top, and brief-format controls. Each opportunity includes its pipeline stage, expected close date, and monetary value. Use this to review the sales pipeline, forecast revenue, or find opportunities that need attention.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "insightly.opportunities_list", owner_id, args, account_id=account_id
        )
