# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `readwise` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ReadwiseBooksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    updated__gt: str | None = None


class ReadwiseBooksListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ReadwiseBooksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ReadwiseBooksListQuery | None = None
    header: ReadwiseBooksListHeader = Field(default_factory=ReadwiseBooksListHeader)


class ReadwiseDocumentSaveHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ReadwiseDocumentSaveBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    title: str | None = None
    author: str | None = None
    tags: list[Any] | None = None
    location: str | None = None


class ReadwiseDocumentSaveRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ReadwiseDocumentSaveHeader = Field(default_factory=ReadwiseDocumentSaveHeader)
    body: ReadwiseDocumentSaveBody


class ReadwiseDocumentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    location: str | None = None
    category: str | None = None
    limit: int = 20
    updated__gt: str | None = None


class ReadwiseDocumentsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ReadwiseDocumentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ReadwiseDocumentsListQuery | None = None
    header: ReadwiseDocumentsListHeader = Field(default_factory=ReadwiseDocumentsListHeader)


class ReadwiseHighlightCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ReadwiseHighlightCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    highlights: list[Any]


class ReadwiseHighlightCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ReadwiseHighlightCreateHeader = Field(default_factory=ReadwiseHighlightCreateHeader)
    body: ReadwiseHighlightCreateBody


class ReadwiseHighlightsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    updated__gt: str | None = None
    book_id: int | None = None


class ReadwiseHighlightsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ReadwiseHighlightsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ReadwiseHighlightsListQuery | None = None
    header: ReadwiseHighlightsListHeader = Field(default_factory=ReadwiseHighlightsListHeader)


class ReadwiseNamespace:
    """Typed accessor for the `readwise` app. Reach it via `client.apps.readwise`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def books_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReadwiseBooksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of books and other sources (articles, PDFs, tweets) from the user's Readwise library, including their associated highlights. Results can be filtered by last-updated timestamp using an ISO 8601 date. Use this to browse or search the user's reading collection and retrieve highlights organized by source.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "readwise.books_list", owner_id, args, account_id=account_id
        )

    async def document_save(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReadwiseDocumentSaveRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Saves a new document (article, PDF, or other supported type) to the user's Readwise Reader library by providing its URL, with optional title, author, tags, and target location (new, later, shortlist, archive, feed). The document is fetched and processed by Readwise's Reader service. Use this to add web content for later reading directly from an agent workflow.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "readwise.document_save", owner_id, args, account_id=account_id
        )

    async def documents_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReadwiseDocumentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists documents from the user's Readwise Reader library with optional filters by location (new, later, shortlist, archive, feed), category (article, email, rss, pdf, epub, tweet, video), and last-updated timestamp. Each result includes metadata such as title, author, URL, and reading progress. Use this to discover what is in the Reader library before acting on specific documents.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "readwise.documents_list", owner_id, args, account_id=account_id
        )

    async def highlight_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReadwiseHighlightCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates one or more new highlights in the user's Readwise account by submitting an array of highlight objects, each requiring at minimum a text field. The highlights are associated with the authenticated user and become visible immediately in their Readwise dashboard and integrations. Use this to programmatically import highlights from other reading apps or custom workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "readwise.highlight_create", owner_id, args, account_id=account_id
        )

    async def highlights_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ReadwiseHighlightsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of highlights from the user's Readwise account, with optional filters by book ID or last-updated timestamp. Each highlight includes the original text, source metadata, tags, and any notes. Use this to retrieve reading highlights for analysis, export, or synchronization — it does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "readwise.highlights_list", owner_id, args, account_id=account_id
        )
