# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `zenrows` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ZenrowsDiscoverAmazonProductsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    search_term: str


class ZenrowsDiscoverAmazonProductsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    tld: str = ".com"
    country: str = "us"


class ZenrowsDiscoverAmazonProductsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ZenrowsDiscoverAmazonProductsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZenrowsDiscoverAmazonProductsPath
    query: ZenrowsDiscoverAmazonProductsQuery | None = None
    header: ZenrowsDiscoverAmazonProductsHeader = Field(
        default_factory=ZenrowsDiscoverAmazonProductsHeader
    )


class ZenrowsGoogleSearchResultsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    tld: str = ".com"
    country: str | None = None


class ZenrowsGoogleSearchResultsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")
    Accept: str = "application/json"


class ZenrowsGoogleSearchResultsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZenrowsGoogleSearchResultsQuery
    header: ZenrowsGoogleSearchResultsHeader = Field(
        default_factory=ZenrowsGoogleSearchResultsHeader
    )


class ZenrowsZillowDiscoveryQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str


class ZenrowsZillowDiscoveryHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ZenrowsZillowDiscoveryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZenrowsZillowDiscoveryQuery
    header: ZenrowsZillowDiscoveryHeader = Field(default_factory=ZenrowsZillowDiscoveryHeader)


class ZenrowsZillowPropertyDataPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    zpid: str


class ZenrowsZillowPropertyDataQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str | None = None
    country: str = "us"


class ZenrowsZillowPropertyDataHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ZenrowsZillowPropertyDataRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZenrowsZillowPropertyDataPath
    query: ZenrowsZillowPropertyDataQuery | None = None
    header: ZenrowsZillowPropertyDataHeader = Field(default_factory=ZenrowsZillowPropertyDataHeader)


class ZenrowsNamespace:
    """Typed accessor for the `zenrows` app. Reach it via `client.apps.zenrows`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def discover_amazon_products(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZenrowsDiscoverAmazonProductsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches Amazon product listings using a keyword or search term and returns structured data including product titles, prices, ratings, and detail-page URLs via the ZenRows Amazon Discovery API. The result is parsed JSON extracted from live Amazon search or category pages, letting downstream tools analyse pricing, availability, or product catalogues without manual browsing. An agent should use this to collect e-commerce product intelligence — for example, to compare prices across sellers, monitor product trends, or build a product database.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zenrows.discover_amazon_products", owner_id, args, account_id=account_id
        )

    async def google_search_results(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZenrowsGoogleSearchResultsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches and parses Google search results for a given query URL, returning structured JSON with titles, URLs, snippets, and other organic result metadata via the ZenRows Google Search Results API. The function transparently handles Google's anti-bot protections so the agent can treat it like a standard search endpoint. An agent should use this to retrieve current search engine results — for example, to gather competitive intelligence, research a topic, or validate the discoverability of a web page.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zenrows.google_search_results", owner_id, args, account_id=account_id
        )

    async def zillow_discovery(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZenrowsZillowDiscoveryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches Zillow listings using a full Zillow search URL and returns structured JSON of matching properties, including addresses, prices, and key specifications via the ZenRows Zillow Discovery API. Unlike single-property lookup, this function discovers multiple listings from search results, making it suitable for bulk data collection across neighbourhoods, price ranges, or home types. An agent should use this when it needs to gather a set of properties matching specific criteria — for example, to build a market analysis, compare inventory, or feed a property-tracking pipeline.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zenrows.zillow_discovery", owner_id, args, account_id=account_id
        )

    async def zillow_property_data(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZenrowsZillowPropertyDataRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed property information from Zillow for a specific listing identified by its Zillow Property ID (ZPID), including address, price, bedrooms, bathrooms, square footage, and other listing specifications via the ZenRows Zillow Property API. The function bypasses anti-scraping measures and returns parsed JSON from the live listing page, making single-property lookups reliable and structured. An agent should use this when it needs to inspect a specific property — for example, to assess property value, compile investment research, or enrich a real estate database.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zenrows.zillow_property_data", owner_id, args, account_id=account_id
        )
