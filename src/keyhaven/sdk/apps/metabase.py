# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `metabase` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MetabaseCardCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    database_id: int
    dataset_query: dict[str, Any]
    display: str | None = None
    description: str | None = None
    collection_id: int | None = None
    visualization_settings: dict[str, Any] | None = None


class MetabaseCardCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MetabaseCardCreateBody


class MetabaseCardGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class MetabaseCardGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MetabaseCardGetPath


class MetabaseCardUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class MetabaseCardUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    dataset_query: dict[str, Any] | None = None
    display: str | None = None
    description: str | None = None
    collection_id: int | None = None
    visualization_settings: dict[str, Any] | None = None


class MetabaseCardUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MetabaseCardUpdatePath
    body: MetabaseCardUpdateBody | None = None


class MetabaseCardsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class MetabaseDashboardGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class MetabaseDashboardGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MetabaseDashboardGetPath


class MetabaseDashboardsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class MetabaseNamespace:
    """Typed accessor for the `metabase` app. Reach it via `client.apps.metabase`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def card_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MetabaseCardCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new saved question (card) in Metabase with a required name, database ID, and dataset query, plus optional visualization type, description, collection placement, and visualization settings. The card appears immediately in the specified collection and is available to dashboard builders. Use this when a new analytics question needs to be persisted for reuse in dashboards or for sharing with the team.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "metabase.card_create", owner_id, args, account_id=account_id
        )

    async def card_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MetabaseCardGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full definition of a single card by its ID, including its name, description, query definition (dataset_query), visualization settings, and collection membership. Use this to inspect a card's underlying query before editing it, or to duplicate a card's query logic programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "metabase.card_get", owner_id, args, account_id=account_id
        )

    async def card_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MetabaseCardUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing card's name, query definition, visualization type, description, collection, or visualization settings. Changes are immediately visible to all users who have access to the card. Use this to modify a question after its requirements change, fix a broken query, or reorganize cards between collections.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "metabase.card_update", owner_id, args, account_id=account_id
        )

    async def cards_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MetabaseCardsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a list of all saved questions (cards) in the Metabase instance, including each card's ID, name, description, collection, and visualization type. No parameters are required. Use this to discover available cards before opening a dashboard, to find a card ID for a subsequent get or update call, or to audit which questions exist.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "metabase.cards_list", owner_id, args, account_id=account_id
        )

    async def dashboard_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MetabaseDashboardGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full definition of a single dashboard by its ID, including its name, description, all placed cards (with their sizes and positions), parameters, and collection membership. Use this to inspect a dashboard's layout, understand which cards are on a dashboard, or programmatically replicate a dashboard structure.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "metabase.dashboard_get", owner_id, args, account_id=account_id
        )

    async def dashboards_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MetabaseDashboardsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a list of all dashboards in the Metabase instance, including each dashboard's ID, name, description, collection, and whether it is pinned. No parameters are required. Use this to discover available dashboards before opening one, or to find a dashboard ID for a subsequent detail call.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "metabase.dashboards_list", owner_id, args, account_id=account_id
        )
