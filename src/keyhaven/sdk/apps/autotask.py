# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `autotask` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AutotaskActiontypesQueryListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    search: str


class AutotaskActiontypesQueryListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ApiIntegrationCode: str
    UserName: str
    Secret: str
    ImpersonationResourceId: str | None = None


class AutotaskActiontypesQueryListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AutotaskActiontypesQueryListQuery
    header: AutotaskActiontypesQueryListHeader


class AutotaskV10ActiontypesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class AutotaskV10ActiontypesGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ApiIntegrationCode: str
    UserName: str
    Secret: str
    ImpersonationResourceId: str | None = None


class AutotaskV10ActiontypesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AutotaskV10ActiontypesGetPath
    header: AutotaskV10ActiontypesGetHeader


class AutotaskV10AppointmentsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class AutotaskV10AppointmentsGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ApiIntegrationCode: str
    UserName: str
    Secret: str
    ImpersonationResourceId: str | None = None


class AutotaskV10AppointmentsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AutotaskV10AppointmentsGetPath
    header: AutotaskV10AppointmentsGetHeader


class AutotaskV10ArticleattachmentsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class AutotaskV10ArticleattachmentsGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ApiIntegrationCode: str
    UserName: str
    Secret: str
    ImpersonationResourceId: str | None = None


class AutotaskV10ArticleattachmentsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AutotaskV10ArticleattachmentsGetPath
    header: AutotaskV10ArticleattachmentsGetHeader


class AutotaskV10ArticlenotesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class AutotaskV10ArticlenotesGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ApiIntegrationCode: str
    UserName: str
    Secret: str
    ImpersonationResourceId: str | None = None


class AutotaskV10ArticlenotesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AutotaskV10ArticlenotesGetPath
    header: AutotaskV10ArticlenotesGetHeader


class AutotaskNamespace:
    """Typed accessor for the `autotask` app. Reach it via `client.apps.autotask`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def actiontypes_query_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AutotaskActiontypesQueryListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches and lists action types available in the Autotask system matching a given search string. Action types define categories of work activities (e.g., phone call, email, onsite visit) and are used when logging time entries or associating actions with tickets. Use this to discover available action type IDs before recording work against a ticket.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "autotask.actiontypes_query_list", owner_id, args, account_id=account_id
        )

    async def v1_0_actiontypes_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AutotaskV10ActiontypesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full definition of a single action type by its unique Autotask ID. Returns metadata such as the action type name, description, and whether it is active. Use this when you already know the action type ID and need to inspect its configuration or verify its properties before associating it with a ticket or time entry.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "autotask.v1_0_actiontypes_get", owner_id, args, account_id=account_id
        )

    async def v1_0_appointments_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AutotaskV10AppointmentsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full record of a specific appointment in Autotask by its appointment ID. Returns details such as the subject, start and end times, assigned resource, associated account, and status. Use this to read existing appointment information for display, scheduling validation, or integration with calendar workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "autotask.v1_0_appointments_get", owner_id, args, account_id=account_id
        )

    async def v1_0_articleattachments_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AutotaskV10ArticleattachmentsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Downloads or retrieves the metadata of a single file attachment linked to an article in Autotask by attachment ID. Attachments can include documents, screenshots, PDFs, or other files uploaded to knowledge base or ticket articles. Use this to access or inspect supporting files associated with an article—note that file content may contain sensitive information depending on the article's visibility.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "autotask.v1_0_articleattachments_get", owner_id, args, account_id=account_id
        )

    async def v1_0_articlenotes_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AutotaskV10ArticlenotesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a specific note attached to an article (knowledge base or ticket article) in Autotask by its note ID. Notes are internal annotations that provide additional context, instructions, or commentary on the article's content. Use this to read the full text of a note when you need to review supplementary information tied to a knowledge base or ticket article.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "autotask.v1_0_articlenotes_get", owner_id, args, account_id=account_id
        )
