# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `todoist` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TodoistProjectsCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TodoistProjectsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    parent_id: str | None = None
    color: str | None = None
    is_favorite: bool | None = None
    view_style: Literal["list", "board"] | None = None
    workspace_id: int | None = None


class TodoistProjectsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: TodoistProjectsCreateHeader = Field(default_factory=TodoistProjectsCreateHeader)
    body: TodoistProjectsCreateBody


class TodoistProjectsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str


class TodoistProjectsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TodoistProjectsGetPath


class TodoistProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    folder_id: int | None = None
    workspace_id: int | None = None
    cursor: str | None = None
    limit: int = 50


class TodoistProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TodoistProjectsListQuery | None = None


class TodoistTasksCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TodoistTasksCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content: str
    description: str | None = None
    project_id: str | None = None
    section_id: str | None = None
    parent_id: str | None = None
    order: int | None = None
    labels: list[Any] | None = None
    priority: Literal["p1", "p2", "p3", "p4"] | None = None
    assignee_id: str | None = None
    due_string: str | None = None
    due_date: str | None = None
    due_datetime: str | None = None
    due_lang: str | None = None
    duration: int | None = None
    duration_unit: Literal["minute", "day"] | None = None
    deadline_date: str | None = None


class TodoistTasksCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: TodoistTasksCreateHeader = Field(default_factory=TodoistTasksCreateHeader)
    body: TodoistTasksCreateBody


class TodoistTasksGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    task_id: str


class TodoistTasksGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TodoistTasksGetPath


class TodoistTasksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: str | None = None
    section_id: str | None = None
    parent_id: str | None = None
    label: str | None = None
    ids: str | None = None
    goal_id: str | None = None
    cursor: str | None = None
    limit: int = 50


class TodoistTasksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TodoistTasksListQuery | None = None


class TodoistNamespace:
    """Typed accessor for the `todoist` app. Reach it via `client.apps.todoist`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def projects_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TodoistProjectsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new project in Todoist with a required name and optional description, parent project (for sub-projects), color, favorite flag, view style (list or board), and workspace assignment. The project immediately appears in the user's sidebar. Use this to organize tasks into a new section of the user's workflow. The project can be deleted or archived later without data loss.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "todoist.projects_create", owner_id, args, account_id=account_id
        )

    async def projects_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TodoistProjectsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the full metadata for a single Todoist project by its string ID, including color, view style (list or board), the caller's role, parent project ID, favorite status, and URL. Use this to inspect a project's configuration before creating tasks or sub-projects within it. Read-only and does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "todoist.projects_get", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TodoistProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a cursor-paginated list of active projects for the authenticated Todoist user, with optional filtering by folder or workspace. Each project includes its name, color, view style (list or board), parent project, and favorite status. Use this to browse the user's project hierarchy or find a project ID for task creation. Read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "todoist.projects_list", owner_id, args, account_id=account_id
        )

    async def tasks_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TodoistTasksCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new task in Todoist with a required content (title) and optional description, project, section, parent task, labels, priority (p1-p4), assignee, due date (natural language or structured), duration, and hard deadline. The task appears immediately in the specified project or the user's inbox. Use this to add work items, assign them to collaborators, and schedule them. Tasks can be closed or deleted later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "todoist.tasks_create", owner_id, args, account_id=account_id
        )

    async def tasks_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TodoistTasksGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single active Todoist task by its string ID, including its content, description, due and deadline dates, duration, priority, labels, project and section placement, assignee, and ordering position. Use this to get the complete picture of a task before deciding to update, close, or reassign it. Read-only operation that does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "todoist.tasks_get", owner_id, args, account_id=account_id
        )

    async def tasks_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TodoistTasksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a cursor-paginated list of active tasks for the authenticated Todoist user, filterable by project, section, parent task, label, goal, or a comma-separated list of specific task IDs. Each task includes its content, due/deadline dates, priority, labels, assignee, and section placement. Use this to find tasks matching certain criteria before updating or closing them. Read-only with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "todoist.tasks_list", owner_id, args, account_id=account_id
        )
