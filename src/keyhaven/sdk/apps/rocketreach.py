# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `rocketreach` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class RocketreachAccountInfoHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class RocketreachAccountInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RocketreachAccountInfoHeader = Field(default_factory=RocketreachAccountInfoHeader)


class RocketreachCheckLookupStatusQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: list[Any]


class RocketreachCheckLookupStatusHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class RocketreachCheckLookupStatusRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RocketreachCheckLookupStatusQuery
    header: RocketreachCheckLookupStatusHeader = Field(
        default_factory=RocketreachCheckLookupStatusHeader
    )


class RocketreachCompanyLookupQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str | None = None
    id: int | None = None
    linkedin_url: str | None = None
    name: str | None = None
    ticker: str | None = None


class RocketreachCompanyLookupHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class RocketreachCompanyLookupRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RocketreachCompanyLookupQuery | None = None
    header: RocketreachCompanyLookupHeader = Field(default_factory=RocketreachCompanyLookupHeader)


class RocketreachCompanySearchHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"
    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RocketreachCompanySearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    start: int = 1
    page_size: int = 10
    order_by: Literal["relevance", "popularity"] = "relevance"
    query: dict[str, Any] | None = None


class RocketreachCompanySearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RocketreachCompanySearchHeader = Field(default_factory=RocketreachCompanySearchHeader)
    body: RocketreachCompanySearchBody = Field(default_factory=RocketreachCompanySearchBody)


class RocketreachCreateApiKeyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"
    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RocketreachCreateApiKeyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    api_key: str


class RocketreachCreateApiKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RocketreachCreateApiKeyHeader = Field(default_factory=RocketreachCreateApiKeyHeader)
    body: RocketreachCreateApiKeyBody


class RocketreachPeopleLookupQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    current_employer: str | None = None
    email: str | None = None
    id: int | None = None
    linkedin_ext_url: str | None = None
    linkedin_url: str | None = None
    lookup_type: (
        Literal["standard", "premium", "premium (feeds disabled)", "bulk", "phone", "enrich"] | None
    ) = None
    name: str | None = None
    npi_number: int | None = None
    title: str | None = None


class RocketreachPeopleLookupHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class RocketreachPeopleLookupRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RocketreachPeopleLookupQuery | None = None
    header: RocketreachPeopleLookupHeader = Field(default_factory=RocketreachPeopleLookupHeader)


class RocketreachPeopleSearchHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"
    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RocketreachPeopleSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    start: int = 1
    page_size: int = 10
    order_by: Literal["relevance", "popularity"] = "relevance"
    query: dict[str, Any] | None = None


class RocketreachPeopleSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: RocketreachPeopleSearchHeader = Field(default_factory=RocketreachPeopleSearchHeader)
    body: RocketreachPeopleSearchBody = Field(default_factory=RocketreachPeopleSearchBody)


class RocketreachProfileCompanyLookupQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    current_employer: str | None = None
    email: str | None = None
    id: int | None = None
    linkedin_url: str | None = None
    lookup_type: (
        Literal["standard", "premium", "premium (feeds disabled)", "bulk", "phone", "enrich"] | None
    ) = None
    name: str | None = None
    npi_number: int | None = None
    title: str | None = None


class RocketreachProfileCompanyLookupHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Accept: str = "application/json"


class RocketreachProfileCompanyLookupRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: RocketreachProfileCompanyLookupQuery | None = None
    header: RocketreachProfileCompanyLookupHeader = Field(
        default_factory=RocketreachProfileCompanyLookupHeader
    )


class RocketreachNamespace:
    """Typed accessor for the `rocketreach` app. Reach it via `client.apps.rocketreach`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def account_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachAccountInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the authenticated RocketReach account details, including current usage metrics, plan information, and API limits. The response shows available credits and consumption statistics. Use this when an agent needs to check remaining quota or account status before performing lookups or searches.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.account_info", owner_id, args, account_id=account_id
        )

    async def check_lookup_status(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachCheckLookupStatusRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Check the processing status of one or more person lookup requests by providing their profile IDs. Returns status information such as complete, failed, waiting, or in progress, along with profile data when available. Use this when an agent needs to poll for results of previously submitted asynchronous lookup requests.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.check_lookup_status", owner_id, args, account_id=account_id
        )

    async def company_lookup(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachCompanyLookupRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve detailed metadata for a specific company using its domain, name, LinkedIn URL, RocketReach ID, or stock ticker. Returns comprehensive profile data including industry classification, employee count, location, and financial information. Use this when an agent needs in-depth information about a known company for research, qualification, or CRM enrichment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.company_lookup", owner_id, args, account_id=account_id
        )

    async def company_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachCompanySearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search the RocketReach business database for companies matching criteria such as name, domain, industry, location, employee count, revenue, and funding. Returns a paginated list of matching company profiles with metadata. Use this when an agent needs to identify target companies for market research, lead generation, or competitive analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.company_search", owner_id, args, account_id=account_id
        )

    async def create_api_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachCreateApiKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Regenerate the API key for your RocketReach account by providing the current key. The previous key is immediately invalidated, which disrupts any services or scripts currently using it for authentication. Use this when an agent needs to rotate credentials following a security incident or as part of regular key management.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.create_api_key", owner_id, args, account_id=account_id
        )

    async def people_lookup(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachPeopleLookupRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Look up a single person professional profile by providing identifiers such as name with employer, email, LinkedIn URL, or RocketReach ID. Returns enriched profile data including contact information, current and past roles, and education history. Use this when an agent needs detailed information about a specific individual for lead research or candidate profiling.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.people_lookup", owner_id, args, account_id=account_id
        )

    async def people_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachPeopleSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search the RocketReach database of over 700 million professional profiles using criteria such as name, company, title, location, industry, and skills. Returns a paginated list of matching profiles without direct contact details. Use this when an agent needs to build a list of potential leads, candidates, or contacts matching specific professional attributes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.people_search", owner_id, args, account_id=account_id
        )

    async def profile_company_lookup(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RocketreachProfileCompanyLookupRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve both a person professional profile and their associated company data in a single request. Provides comprehensive information including personal background and organizational details simultaneously. Use this when an agent needs a full picture of both an individual and their employer for deeper research or qualification.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rocketreach.profile_company_lookup", owner_id, args, account_id=account_id
        )
