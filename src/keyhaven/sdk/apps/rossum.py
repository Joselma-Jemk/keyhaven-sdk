# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `rossum` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class RossumGetAnnotationPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organizations: str | None = None
    annotation_id: str


class RossumGetAnnotationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RossumGetAnnotationPath


class RossumGetDocumentPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    document_id: str
    organizations: str


class RossumGetDocumentRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RossumGetDocumentPath


class RossumGetQueuesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organizations: str


class RossumGetQueuesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RossumGetQueuesPath


class RossumLoginPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organizations: str


class RossumLoginBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    username: str
    password: str


class RossumLoginRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RossumLoginPath
    body: RossumLoginBody


class RossumUploadToQueuePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    organizations: str = "example name."
    queue_id: str


class RossumUploadToQueueHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="multipart/form-data", alias="Content-Type")


class RossumUploadToQueueBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content: str
    filename: str | None = None


class RossumUploadToQueueRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RossumUploadToQueuePath
    header: RossumUploadToQueueHeader = Field(default_factory=RossumUploadToQueueHeader)
    body: RossumUploadToQueueBody


class RossumNamespace:
    """Typed accessor for the `rossum` app. Reach it via `client.apps.rossum`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_annotation(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RossumGetAnnotationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a specific annotation in Rossum by its unique annotation ID, including all extracted data fields, their confidence scores, status, and any corrections made by human reviewers. Annotations represent the AI's understanding of a document's content. Use this to access extracted data (e.g., invoice line items, totals, dates) after a document has been processed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rossum.get_annotation", owner_id, args, account_id=account_id
        )

    async def get_document(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RossumGetDocumentRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details and content of a specific document in Rossum by its unique document ID, including its filename, status, upload timestamp, and associated metadata. Documents typically contain invoices, purchase orders, or other business documents being processed by the AI. Use this to inspect a document's content and processing status after upload or during review workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rossum.get_document", owner_id, args, account_id=account_id
        )

    async def get_queues(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RossumGetQueuesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all available queues in the Rossum organization, each representing a document processing pipeline with its own configuration, schema, and team assignments. Each queue entry includes its ID, name, and document count. Use this to discover which queues exist before uploading documents or retrieving documents from a specific queue.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rossum.get_queues", owner_id, args, account_id=account_id
        )

    async def login(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RossumLoginRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Authenticates with the Rossum API using the organization's credentials (username and password), returning a session token required for all subsequent API calls. The token is valid for the session duration and must be included in the Authorization header of further requests. Use this as the first step before any other Rossum operations — it does not modify any business data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("rossum.login", owner_id, args, account_id=account_id)

    async def upload_to_queue(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RossumUploadToQueueRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Uploads a document file (such as an invoice or purchase order PDF) to a specified Rossum processing queue, where the AI document processing engine will analyze and extract data from it. The document enters the queue and becomes available for annotation and review by the team. Use this to feed new documents into the automated processing pipeline — it triggers downstream extraction workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "rossum.upload_to_queue", owner_id, args, account_id=account_id
        )
