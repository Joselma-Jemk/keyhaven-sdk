# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `scrapybara` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ScrapybaraBrowserAuthenticatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraBrowserAuthenticateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    auth_state_id: str


class ScrapybaraBrowserAuthenticateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraBrowserAuthenticatePath
    query: ScrapybaraBrowserAuthenticateQuery


class ScrapybaraBrowserGetCdpUrlPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraBrowserGetCdpUrlRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraBrowserGetCdpUrlPath


class ScrapybaraBrowserGetCurrentUrlPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraBrowserGetCurrentUrlRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraBrowserGetCurrentUrlPath


class ScrapybaraBrowserModifyAuthPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraBrowserModifyAuthQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    auth_state_id: str
    name: str | None = None


class ScrapybaraBrowserModifyAuthRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraBrowserModifyAuthPath
    query: ScrapybaraBrowserModifyAuthQuery


class ScrapybaraBrowserSaveAuthPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraBrowserSaveAuthQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None


class ScrapybaraBrowserSaveAuthRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraBrowserSaveAuthPath
    query: ScrapybaraBrowserSaveAuthQuery | None = None


class ScrapybaraBrowserStartPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraBrowserStartRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraBrowserStartPath


class ScrapybaraBrowserStopPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraBrowserStopRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraBrowserStopPath


class ScrapybaraCodeExecutePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraCodeExecuteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    code: str
    kernel_name: str = "python3"
    timeout: int | None = None


class ScrapybaraCodeExecuteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraCodeExecutePath
    body: ScrapybaraCodeExecuteBody


class ScrapybaraEnvDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraEnvDeleteBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    keys: list[Any]


class ScrapybaraEnvDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraEnvDeletePath
    body: ScrapybaraEnvDeleteBody


class ScrapybaraEnvGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraEnvGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraEnvGetPath


class ScrapybaraEnvSetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraEnvSetBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    variables: dict[str, Any]


class ScrapybaraEnvSetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraEnvSetPath
    body: ScrapybaraEnvSetBody


class ScrapybaraGetInstanceByIdPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraGetInstanceByIdRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraGetInstanceByIdPath


class ScrapybaraGetStreamUrlPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraGetStreamUrlRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraGetStreamUrlPath


class ScrapybaraListAllBrowserAuthStatesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class ScrapybaraListAllInstancesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class ScrapybaraListNotebookKernelsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraListNotebookKernelsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraListNotebookKernelsPath


class ScrapybaraNotebookAddCellPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str
    notebook_id: str


class ScrapybaraNotebookAddCellBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: Literal["code", "markdown", "raw"] = Field(alias="type")
    content: str
    metadata: dict[str, Any] | None = None


class ScrapybaraNotebookAddCellRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraNotebookAddCellPath
    body: ScrapybaraNotebookAddCellBody


class ScrapybaraNotebookCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraNotebookCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    kernel_name: str = "python3"


class ScrapybaraNotebookCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraNotebookCreatePath
    body: ScrapybaraNotebookCreateBody


class ScrapybaraNotebookDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str
    notebook_id: str


class ScrapybaraNotebookDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraNotebookDeletePath


class ScrapybaraNotebookExecuteCellPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str
    notebook_id: str
    cell_id: str


class ScrapybaraNotebookExecuteCellBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    timeout: int | None = None


class ScrapybaraNotebookExecuteCellRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraNotebookExecuteCellPath
    body: ScrapybaraNotebookExecuteCellBody | None = None


class ScrapybaraNotebookGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str
    notebook_id: str


class ScrapybaraNotebookGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraNotebookGetPath


class ScrapybaraPauseInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraPauseInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraPauseInstancePath


class ScrapybaraResumeInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraResumeInstanceQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    timeout_hours: float | None = None


class ScrapybaraResumeInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraResumeInstancePath
    query: ScrapybaraResumeInstanceQuery | None = None


class ScrapybaraRunBashActionsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraRunBashActionsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    command: str | None = None
    restart: bool | None = None


class ScrapybaraRunBashActionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraRunBashActionsPath
    body: ScrapybaraRunBashActionsBody = Field(default_factory=ScrapybaraRunBashActionsBody)


class ScrapybaraRunComputerActionsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraRunComputerActionsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    action: Literal[
        "move_mouse",
        "click_mouse",
        "drag_mouse",
        "scroll",
        "press_key",
        "type_text",
        "wait",
        "take_screenshot",
        "get_cursor_position",
    ]
    coordinates: list[Any] | None = None
    hold_keys: list[Any] | None = None
    button: Literal["left", "right", "middle", "back", "forward"] | None = None
    click_type: Literal["down", "up", "click"] | None = None
    num_clicks: int | None = None
    path: list[Any] | None = None
    delta_x: float | None = None
    delta_y: float | None = None
    keys: list[Any] | None = None
    text: str | None = None
    duration: float | None = None


class ScrapybaraRunComputerActionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraRunComputerActionsPath
    body: ScrapybaraRunComputerActionsBody


class ScrapybaraRunEditActionsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraRunEditActionsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    command: Literal["view", "create", "str_replace", "insert", "undo_edit"]
    path: str
    file_text: str | None = None
    view_range: list[Any] | None = None
    old_str: str | None = None
    new_str: str | None = None
    insert_line: int | None = None


class ScrapybaraRunEditActionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraRunEditActionsPath
    body: ScrapybaraRunEditActionsBody


class ScrapybaraRunFileActionsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraRunFileActionsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    command: Literal["read", "write", "copy", "delete", "list", "find", "move", "search", "view"]
    path: str | None = None
    content: str | None = None
    mode: str | None = None
    encoding: str | None = None
    view_range: list[Any] | None = None
    recursive: bool | None = None
    src: str | None = None
    dst: str | None = None
    old_str: str | None = None
    new_str: str | None = None
    line: int | None = None
    text: str | None = None
    lines: list[Any] | None = None
    all_occurrences: bool | None = None
    pattern: str | None = None
    case_sensitive: bool | None = None
    line_numbers: bool | None = None


class ScrapybaraRunFileActionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraRunFileActionsPath
    body: ScrapybaraRunFileActionsBody


class ScrapybaraStartInstanceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_type: Literal["ubuntu", "browser", "windows"] = "ubuntu"
    timeout_hours: float | None = None
    blocked_domains: list[Any] | None = None


class ScrapybaraStartInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ScrapybaraStartInstanceBody = Field(default_factory=ScrapybaraStartInstanceBody)


class ScrapybaraStopInstancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraStopInstanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraStopInstancePath


class ScrapybaraTakeScreenshotPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    instance_id: str


class ScrapybaraTakeScreenshotRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ScrapybaraTakeScreenshotPath


class ScrapybaraNamespace:
    """Typed accessor for the `scrapybara` app. Reach it via `client.apps.scrapybara`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def browser_authenticate(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraBrowserAuthenticateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Applies a previously saved browser authentication state (cookies, localStorage) to the browser running on a Scrapybara instance, effectively restoring a logged-in session without manual credential entry. The browser must already be started. Use to skip login workflows by replaying a previously saved session onto a fresh or existing browser.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.browser_authenticate", owner_id, args, account_id=account_id
        )

    async def browser_get_cdp_url(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraBrowserGetCdpUrlRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the Chrome DevTools Protocol (CDP) WebSocket URL for the browser running on a Scrapybara instance. This URL allows external CDP clients such as Puppeteer or Playwright to connect for fine-grained browser control beyond what computer actions provide. The browser must already be started. Use when you need programmatic DOM manipulation or network interception.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.browser_get_cdp_url", owner_id, args, account_id=account_id
        )

    async def browser_get_current_url(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraBrowserGetCurrentUrlRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the URL currently displayed in the address bar of the browser on a Scrapybara instance. The browser must already be running. Use to check which page the browser has loaded before deciding the next navigation step or before taking a screenshot for verification.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.browser_get_current_url", owner_id, args, account_id=account_id
        )

    async def browser_modify_auth(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraBrowserModifyAuthRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the display name of an existing saved browser authentication state identified by auth_state_id. Does not alter the underlying cookies or session data — only the label changes. Use to rename saved auth states for better organization and discoverability.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.browser_modify_auth", owner_id, args, account_id=account_id
        )

    async def browser_save_auth(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraBrowserSaveAuthRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Captures and persists the current browser authentication state (cookies, localStorage, session data) from a running Scrapybara instance to a named auth state on the Scrapybara platform. The saved state can later be applied to new browser sessions via BROWSER_AUTHENTICATE. Use to preserve login sessions so they can be reused without re-entering credentials.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.browser_save_auth", owner_id, args, account_id=account_id
        )

    async def browser_start(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraBrowserStartRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Launches a headed browser (typically Chromium) on a running Scrapybara instance, making it visible on the remote desktop. The instance must be running. Use before navigating to URLs, filling forms, taking browser screenshots, or performing any web automation task that requires a graphical browser.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.browser_start", owner_id, args, account_id=account_id
        )

    async def browser_stop(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraBrowserStopRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Terminates the browser process running on a Scrapybara instance. The browser is killed but the underlying instance continues running and remains billable. The instance must be running with an active browser. Use to free CPU and RAM resources on the instance once browser automation is finished.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.browser_stop", owner_id, args, account_id=account_id
        )

    async def code_execute(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraCodeExecuteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes arbitrary code on a running Scrapybara instance using a specified kernel (defaults to python3) without requiring a notebook. Returns the output, errors, and execution metadata. Supports a configurable timeout. Use for quick ad-hoc script execution, data processing, or making API calls from within the instance environment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.code_execute", owner_id, args, account_id=account_id
        )

    async def env_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraEnvDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Deletes specified environment variables from a running Scrapybara instance by providing a list of variable names to remove. If a named variable does not exist, it is silently ignored. The instance must be running. Use to clean up sensitive credentials or reset the environment configuration back to defaults.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.env_delete", owner_id, args, account_id=account_id
        )

    async def env_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraEnvGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all environment variables currently set on a running Scrapybara instance as key-value pairs. May include sensitive values such as API keys or secrets. The instance must be running. Use to inspect the current environment configuration before executing scripts that depend on environment variables.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.env_get", owner_id, args, account_id=account_id
        )

    async def env_set(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraEnvSetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sets one or more environment variables on a running Scrapybara instance by providing an object of key-value pairs. Existing variables with the same keys are overwritten. The instance must be running. Use to configure API keys, database URLs, or other runtime settings that scripts running on the instance will read from the environment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.env_set", owner_id, args, account_id=account_id
        )

    async def get_instance_by_id(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraGetInstanceByIdRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the current status, metadata, and configuration of a specific Scrapybara instance by its unique ID. Returns details such as instance type, creation timestamp, current state (running/paused/stopped), and timeout settings. Use to check whether an instance is alive and ready before sending commands.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.get_instance_by_id", owner_id, args, account_id=account_id
        )

    async def get_stream_url(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraGetStreamUrlRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a temporary VNC/stream URL that provides live, real-time viewing of a running Scrapybara instance desktop. The URL grants viewing access only and expires after a short period. Use when you need to watch automation in real time or debug visual issues interactively.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.get_stream_url", owner_id, args, account_id=account_id
        )

    async def list_all_browser_auth_states(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraListAllBrowserAuthStatesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all saved browser authentication states for your Scrapybara account. Each entry represents a persisted browser session (cookies, localStorage) that can be replayed on new instances via BROWSER_AUTHENTICATE. Use to find the auth_state_id needed before applying a saved login session to a running browser.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.list_all_browser_auth_states", owner_id, args, account_id=account_id
        )

    async def list_all_instances(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraListAllInstancesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists every Scrapybara instance associated with your account, returning an array of instance objects with their IDs, types, states, and metadata. No parameters are required. Use to discover active instances, find an instance ID for subsequent operations, or identify stale instances to clean up.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.list_all_instances", owner_id, args, account_id=account_id
        )

    async def list_notebook_kernels(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraListNotebookKernelsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all available Jupyter notebook kernels installed on a running Scrapybara instance, returning kernel names and their status. The instance must be running. Use to discover which kernels are available before creating a notebook or executing code, especially when a non-default kernel is needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.list_notebook_kernels", owner_id, args, account_id=account_id
        )

    async def notebook_add_cell(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraNotebookAddCellRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new cell (code, markdown, or raw) to an existing Jupyter notebook on a running Scrapybara instance. Optional metadata can be attached to the cell. The notebook must exist and the instance must be running. Use to build up notebook content programmatically cell by cell before execution.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.notebook_add_cell", owner_id, args, account_id=account_id
        )

    async def notebook_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraNotebookCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new named Jupyter notebook on a running Scrapybara instance with a specified kernel (defaults to python3). Returns the notebook ID that is required for subsequent cell operations. Use to organize code into structured, reproducible notebooks with persistent cells and outputs.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.notebook_create", owner_id, args, account_id=account_id
        )

    async def notebook_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraNotebookDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a Jupyter notebook and all its cells from a Scrapybara instance. This action is irreversible — deleted notebooks cannot be recovered. The instance must be running. Use to clean up notebooks that are no longer needed and free storage space.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.notebook_delete", owner_id, args, account_id=account_id
        )

    async def notebook_execute_cell(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraNotebookExecuteCellRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes a specific cell within a Jupyter notebook on a running Scrapybara instance, returning its output, execution count, and any errors. Supports an optional timeout in seconds. The notebook and cell must exist and the instance must be running. Use to run code cells that were previously added to a notebook and collect their results.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.notebook_execute_cell", owner_id, args, account_id=account_id
        )

    async def notebook_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraNotebookGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full content of a Jupyter notebook by its ID from a running Scrapybara instance, including all cells, metadata, and output. The instance must be running. Use to read previously created notebooks, inspect cell results, or clone notebook content for reuse.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.notebook_get", owner_id, args, account_id=account_id
        )

    async def pause_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraPauseInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Suspends a running Scrapybara instance, freezing its processes and state without terminating the environment. The instance can be fully restored later with RESUME_INSTANCE, optionally with a new timeout. Use to temporarily halt work while keeping the environment and all its data intact for later resumption.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.pause_instance", owner_id, args, account_id=account_id
        )

    async def resume_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraResumeInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Restores a previously paused Scrapybara instance back to running state, optionally setting a new timeout_hours to control how long it stays alive. The instance ID must correspond to a currently paused instance. Use to continue work on an existing environment that was temporarily suspended without losing state.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.resume_instance", owner_id, args, account_id=account_id
        )

    async def run_bash_actions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraRunBashActionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes arbitrary bash commands inside a running Scrapybara instance and returns stdout, stderr, and exit code. Optionally restarts the instance after the command completes. The instance must be running. Use to install packages, run scripts, configure the OS environment, or invoke CLI tools that are not exposed through other functions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.run_bash_actions", owner_id, args, account_id=account_id
        )

    async def run_computer_actions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraRunComputerActionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Executes GUI-level input actions on a running Scrapybara instance: move or click the mouse (left/right/middle/back/forward), drag, scroll, press keyboard keys, type text, wait, take an inline screenshot, or get cursor position. Each action simulates real human input on the remote desktop. The instance must be running. Use to automate GUI applications or interact with websites that lack a programmatic API.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.run_computer_actions", owner_id, args, account_id=account_id
        )

    async def run_edit_actions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraRunEditActionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Performs structured file editing on a running Scrapybara instance: view a range of lines, create a new file with text content, replace an exact string (str_replace), insert text at a specific line, or undo the last edit. Designed for precise, surgical code modification rather than raw file I/O. The instance must be running. Use to modify source code or configuration files programmatically with undo support.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.run_edit_actions", owner_id, args, account_id=account_id
        )

    async def run_file_actions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraRunFileActionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Performs general file-system operations on a running Scrapybara instance: read, write, copy, move, delete, list directory contents, search by regex pattern, view line ranges, and find files. Supports encoding selection, recursion, and case-sensitive matching. The instance must be running. Use for comprehensive file management such as transferring data, inspecting logs, or cleaning up output files.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.run_file_actions", owner_id, args, account_id=account_id
        )

    async def start_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraStartInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions and launches a new remote desktop instance on Scrapybara (ubuntu, headed browser, or Windows). Returns a unique instance ID that all other functions use to target this environment. Billing accrues from creation until the instance is stopped. Use as the first step in any scraping or automation workflow.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.start_instance", owner_id, args, account_id=account_id
        )

    async def stop_instance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraStopInstanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Terminates a running Scrapybara instance immediately: all processes are killed, the remote environment is destroyed, and billing stops. This action is irreversible — a stopped instance cannot be restarted or recovered. Use to clean up resources and avoid continued charges once your task is complete.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.stop_instance", owner_id, args, account_id=account_id
        )

    async def take_screenshot(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ScrapybaraTakeScreenshotRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Captures a PNG screenshot of the current display of a running Scrapybara instance, returning the raw image data. The instance must be in running state. Use to visually inspect the remote desktop or browser state during automation, or to capture page renders for debugging.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "scrapybara.take_screenshot", owner_id, args, account_id=account_id
        )
