# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `gmail` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GmailCreateDraftRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to: str
    subject: str
    body: str


class GmailSearchEmailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    max_results: int = 10


class GmailSendEmailRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    to: str
    subject: str
    body: str


class GmailNamespace:
    """Typed accessor for the `gmail` app. Reach it via `client.apps.gmail`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_draft(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GmailCreateDraftRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a Gmail draft. The message is saved to the user's Drafts and is NOT sent — nobody receives it until a human (or a separate send action) sends it. Reversible and private, so the agent can prepare emails freely.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gmail.create_draft", owner_id, args, account_id=account_id
        )

    async def search_emails(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GmailSearchEmailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search the connected user's own Gmail using Gmail query syntax (e.g. 'is:unread from:boss'). Read-only: returns matching message snippets/metadata without modifying anything. The core inbox-reading workflow, safe to run autonomously on the user's own mailbox.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "gmail.search_emails", owner_id, args, account_id=account_id
        )

    async def send_email(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GmailSendEmailRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Send a Gmail message to a single recipient. This delivers the email immediately and irreversibly to an external person — there is no recall. Use only once the recipient, subject and body are confirmed correct.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("gmail.send_email", owner_id, args, account_id=account_id)
