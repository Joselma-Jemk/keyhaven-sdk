# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `pagerduty` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class PagerdutyEscalationPoliciesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    user_ids__: str | None = Field(default=None, alias="user_ids[]")
    team_ids__: str | None = Field(default=None, alias="team_ids[]")
    include__: str | None = Field(default=None, alias="include[]")
    sort_by: str | None = None
    limit: int | None = None
    offset: int | None = None


class PagerdutyEscalationPoliciesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PagerdutyEscalationPoliciesListQuery | None = None


class PagerdutyIncidentCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    From: str | None = None


class PagerdutyIncidentCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    incident: dict[str, Any]


class PagerdutyIncidentCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: PagerdutyIncidentCreateHeader | None = None
    body: PagerdutyIncidentCreateBody


class PagerdutyIncidentGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class PagerdutyIncidentGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include__: str | None = Field(default=None, alias="include[]")


class PagerdutyIncidentGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: PagerdutyIncidentGetPath
    query: PagerdutyIncidentGetQuery | None = None


class PagerdutyIncidentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    statuses__: str | None = Field(default=None, alias="statuses[]")
    urgencies__: str | None = Field(default=None, alias="urgencies[]")
    service_ids__: str | None = Field(default=None, alias="service_ids[]")
    team_ids__: str | None = Field(default=None, alias="team_ids[]")
    user_ids__: str | None = Field(default=None, alias="user_ids[]")
    since: str | None = None
    until: str | None = None
    incident_key: str | None = None
    sort_by: str | None = None
    limit: int | None = None
    offset: int | None = None
    include__: str | None = Field(default=None, alias="include[]")


class PagerdutyIncidentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PagerdutyIncidentsListQuery | None = None


class PagerdutyServicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    team_ids__: str | None = Field(default=None, alias="team_ids[]")
    include__: str | None = Field(default=None, alias="include[]")
    sort_by: str | None = None
    limit: int | None = None
    offset: int | None = None
    total: bool | None = None


class PagerdutyServicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: PagerdutyServicesListQuery | None = None


class PagerdutyNamespace:
    """Typed accessor for the `pagerduty` app. Reach it via `client.apps.pagerduty`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def escalation_policies_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PagerdutyEscalationPoliciesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of PagerDuty escalation policies filterable by involved user, owning team, or full-text name search. Each policy entry includes its escalation rules specifying who gets notified, in what order, and after what delay. Use this to review on-call routing rules, find which escalation policy applies to a given service, or verify escalation chains before modifying incident response configurations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pagerduty.escalation_policies_list", owner_id, args, account_id=account_id
        )

    async def incident_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PagerdutyIncidentCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Triggers a new incident on a specified PagerDuty service, immediately generating alerts and notifications to the on-call responders defined by the service's escalation policy. The incident appears in the PagerDuty dashboard with the given title, urgency, body details, and an optional dedup_key to merge with existing open incidents. Use this to programmatically alert on-call teams from monitoring systems, webhooks, or any external detection source that cannot use a native integration.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pagerduty.incident_create", owner_id, args, account_id=account_id
        )

    async def incident_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PagerdutyIncidentGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single PagerDuty incident by its incident ID (PXXXXXX format), including status, urgency, title, service reference, assigned responders, escalation state, and timestamps. Use this to inspect a specific incident during troubleshooting, review its full timeline and current state, or check acknowledgements and assignments before deciding on a response action. Optionally include related resources like alerts, acknowledgements, and notes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pagerduty.incident_get", owner_id, args, account_id=account_id
        )

    async def incidents_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PagerdutyIncidentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of PagerDuty incidents with filtering by status (triggered, acknowledged, resolved), urgency (high, low), service, team, assigned user, and time range via ISO-8601 since/until parameters. Each incident entry includes title, status, urgency, assigned responders, service, and timestamps. Use this to audit active incidents, review historical incident data, or identify trends across services and teams for reliability analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pagerduty.incidents_list", owner_id, args, account_id=account_id
        )

    async def services_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: PagerdutyServicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of PagerDuty services (monitored applications or components) filterable by team membership, full-text name search, and integration type. Each service entry includes its name, status, escalation policy reference, and integration details. Use this to discover available services before creating incidents, or to audit the service catalog and verify which escalation policy protects each service.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "pagerduty.services_list", owner_id, args, account_id=account_id
        )
