# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `mailjet` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class MailjetContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Email: str
    Name: str | None = None
    IsExcludedFromCampaigns: bool | None = None
    Properties: dict[str, Any] | None = None


class MailjetContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MailjetContactCreateBody


class MailjetContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact_id: str


class MailjetContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: MailjetContactGetPath


class MailjetContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Limit: int | None = None
    Offset: int | None = None
    OrderBy: str | None = None
    Filter: str | None = None


class MailjetContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MailjetContactsListQuery | None = None


class MailjetContactslistCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Name: str
    Label: str | None = None


class MailjetContactslistCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MailjetContactslistCreateBody


class MailjetContactslistsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Limit: int | None = None
    Offset: int | None = None
    OrderBy: str | None = None


class MailjetContactslistsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: MailjetContactslistsListQuery | None = None


class MailjetSendEmailBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Messages: list[Any]
    Globals: dict[str, Any] | None = None


class MailjetSendEmailRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: MailjetSendEmailBody


class MailjetNamespace:
    """Typed accessor for the `mailjet` app. Reach it via `client.apps.mailjet`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailjetContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact in the Mailjet account with the given email address (required), optional display name, campaign-exclusion flag, and custom properties. The contact becomes available immediately for list membership and can receive campaigns unless explicitly excluded. Use this to onboard new subscribers or add leads before assigning them to a contact list.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailjet.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailjetContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single contact's full profile by its Mailjet numeric ID, including email, name, creation date, and any custom properties. Use this when you already know the contact ID (e.g. from a list call) and need the complete record before updating or sending to that contact.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailjet.contact_get", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailjetContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of all contacts in the Mailjet account, optionally filtered by email or ordered by creation date. The response includes contact IDs, email addresses, names, and creation timestamps. Use this to browse contacts, verify if a specific email exists, or feed downstream operations that need contact IDs.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailjet.contacts_list", owner_id, args, account_id=account_id
        )

    async def contactslist_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailjetContactslistCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new empty contact list (segment) with a unique name and optional label. The list is immediately available for contacts to be added to it. Use this when you need a new grouping for a campaign, such as a newsletter segment or a segmented audience for targeted sends.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailjet.contactslist_create", owner_id, args, account_id=account_id
        )

    async def contactslists_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailjetContactslistsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of all contact lists (segments) in the Mailjet account, each with its ID, name, subscriber count, and creation date. Use this to discover available lists before adding or removing contacts, or to retrieve the list ID needed by other list-management endpoints.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailjet.contactslists_list", owner_id, args, account_id=account_id
        )

    async def send_email(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: MailjetSendEmailRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends one or more transactional emails via Mailjet's v3.1 Send API. Each message can specify sender, recipients, subject, HTML/text content, attachments, and template ID. Recipients will receive the email in their inbox almost immediately. Use this for transactional flows such as password resets, order confirmations, or notification emails — not for bulk marketing campaigns, which require a different API endpoint.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "mailjet.send_email", owner_id, args, account_id=account_id
        )
