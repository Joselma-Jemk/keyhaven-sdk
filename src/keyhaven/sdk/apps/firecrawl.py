# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `firecrawl` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FirecrawlBatchScrapeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirecrawlBatchScrapeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    urls: list[Any]
    webhook: dict[str, Any] | None = None
    ignoreInvalidURLs: bool = False
    formats: list[Any]
    onlyMainContent: bool = True
    blockAds: bool = True
    includeTags: list[Any] | None = None
    excludeTags: list[Any] | None = None
    waitFor: int = 0
    mobile: bool = False
    skipTlsVerification: bool = False
    timeout: int = 30000


class FirecrawlBatchScrapeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirecrawlBatchScrapeHeader = Field(default_factory=FirecrawlBatchScrapeHeader)
    body: FirecrawlBatchScrapeBody


class FirecrawlCancelCrawlPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    crawl_id: str


class FirecrawlCancelCrawlRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FirecrawlCancelCrawlPath


class FirecrawlCrawlHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirecrawlCrawlBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    excludePaths: list[Any] | None = None
    includePaths: list[Any] | None = None
    maxDepth: int = 10
    maxDiscoveryDepth: int | None = None
    ignoreSitemap: bool = False
    ignoreQueryParameters: bool = False
    limit: int = 10000
    allowBackwardLinks: bool = False
    allowExternalLinks: bool = False
    webhook: dict[str, Any] | None = None
    scrapeOptions: dict[str, Any]


class FirecrawlCrawlRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirecrawlCrawlHeader = Field(default_factory=FirecrawlCrawlHeader)
    body: FirecrawlCrawlBody


class FirecrawlExtractHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirecrawlExtractBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    urls: list[Any]
    prompt: str
    schema_: dict[str, Any] | None = Field(default=None, alias="schema")
    enableWebSearch: bool = False
    ignoreSitemap: bool = False
    includeSubdomains: bool = True
    showSources: bool = False
    scrapeOptions: dict[str, Any]


class FirecrawlExtractRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirecrawlExtractHeader = Field(default_factory=FirecrawlExtractHeader)
    body: FirecrawlExtractBody


class FirecrawlGetBatchScrapeErrorsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    batch_id: str


class FirecrawlGetBatchScrapeErrorsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FirecrawlGetBatchScrapeErrorsPath


class FirecrawlGetBatchScrapeStatusPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    batch_id: str


class FirecrawlGetBatchScrapeStatusRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FirecrawlGetBatchScrapeStatusPath


class FirecrawlGetCrawlErrorsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    crawl_id: str


class FirecrawlGetCrawlErrorsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FirecrawlGetCrawlErrorsPath


class FirecrawlGetCrawlStatusPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    crawl_id: str


class FirecrawlGetCrawlStatusRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FirecrawlGetCrawlStatusPath


class FirecrawlGetCreditUsageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirecrawlGetCreditUsageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirecrawlGetCreditUsageHeader = Field(default_factory=FirecrawlGetCreditUsageHeader)


class FirecrawlGetExtractStatusPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    extract_id: str


class FirecrawlGetExtractStatusRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FirecrawlGetExtractStatusPath


class FirecrawlMapHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirecrawlMapBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    excludePaths: list[Any] | None = None
    includePaths: list[Any] | None = None
    maxDepth: int = 10
    ignoreSitemap: bool = False
    limit: int = 10000


class FirecrawlMapRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirecrawlMapHeader = Field(default_factory=FirecrawlMapHeader)
    body: FirecrawlMapBody


class FirecrawlScrapeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirecrawlScrapeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    formats: list[Any]
    onlyMainContent: bool = True
    blockAds: bool = True


class FirecrawlScrapeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirecrawlScrapeHeader = Field(default_factory=FirecrawlScrapeHeader)
    body: FirecrawlScrapeBody


class FirecrawlSearchHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FirecrawlSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    limit: int = 10


class FirecrawlSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: FirecrawlSearchHeader = Field(default_factory=FirecrawlSearchHeader)
    body: FirecrawlSearchBody


class FirecrawlNamespace:
    """Typed accessor for the `firecrawl` app. Reach it via `client.apps.firecrawl`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def batch_scrape(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlBatchScrapeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Scrapes multiple URLs in parallel with a single request, supporting configurable formats, tag filtering, mobile emulation, and webhook notifications when all pages are processed. Use this to efficiently extract content from many pages at once instead of calling SCRAPE repeatedly in a loop.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.batch_scrape", owner_id, args, account_id=account_id
        )

    async def cancel_crawl(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlCancelCrawlRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Stops an ongoing crawl operation identified by its crawl ID. Already-scraped pages are preserved but no new pages will be discovered or crawled. Use this to halt a crawl that is taking too long, burning credits, or has gone off-track due to incorrect path filters.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.cancel_crawl", owner_id, args, account_id=account_id
        )

    async def crawl(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlCrawlRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Crawls an entire website starting from a seed URL, following links up to a configurable depth and page limit. Supports path inclusion/exclusion, sitemap integration, and webhook notifications. Each page is scraped according to the specified scrape options. Use this to index a full website, perform site audits, or build a dataset from all pages of a domain.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("firecrawl.crawl", owner_id, args, account_id=account_id)

    async def extract(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlExtractRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Uses an LLM to extract structured data from one or more URLs based on a natural language prompt and optional JSON schema. Supports web search augmentation, subdomain scanning, and source attribution. Use this to extract specific information from web pages (e.g., pricing tables, product specs, contact info) without writing parsing logic.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.extract", owner_id, args, account_id=account_id
        )

    async def get_batch_scrape_errors(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlGetBatchScrapeErrorsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves any errors that occurred during a batch scrape operation, such as unreachable URLs, timeouts, or parsing failures. Returns details per failed URL to help diagnose issues. Use this after a batch scrape completes with failures to understand what went wrong.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.get_batch_scrape_errors", owner_id, args, account_id=account_id
        )

    async def get_batch_scrape_status(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlGetBatchScrapeStatusRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Checks the progress and results of a batch scrape operation by its batch ID. Returns completion percentage, completed pages, and any failures. Use this to poll for results after starting a BATCH_SCRAPE, especially when not using webhooks.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.get_batch_scrape_status", owner_id, args, account_id=account_id
        )

    async def get_crawl_errors(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlGetCrawlErrorsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves any errors encountered during a crawl operation, such as unreachable URLs, timeouts, or invalid responses. Returns per-URL error details. Use this after a crawl completes to diagnose and fix any pages that failed to be scraped.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.get_crawl_errors", owner_id, args, account_id=account_id
        )

    async def get_crawl_status(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlGetCrawlStatusRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the status and results of a crawl operation by its crawl ID, including total pages crawled, pages remaining, and paginated results. Use this to poll for crawl progress after starting a CRAWL, especially when not using webhook notifications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.get_crawl_status", owner_id, args, account_id=account_id
        )

    async def get_credit_usage(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlGetCreditUsageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns your Firecrawl account's current credit consumption, including total credits used, remaining balance, and usage breakdown. This is billing information. Use this to monitor spending and ensure you stay within your plan limits.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.get_credit_usage", owner_id, args, account_id=account_id
        )

    async def get_extract_status(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlGetExtractStatusRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Checks the status and results of an extract operation by its extract ID. Returns whether the extraction is still running, has completed, or has failed, along with the structured data when finished. Use this to poll for results after starting an EXTRACT operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "firecrawl.get_extract_status", owner_id, args, account_id=account_id
        )

    async def map(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlMapRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Discovers all reachable URLs on a website starting from a seed URL, returning a flat list of links with configurable depth and path filters. Unlike CRAWL, this only discovers URLs without scraping page content. Use this to build a site map, verify link structure, or find all pages before deciding which ones to scrape.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("firecrawl.map", owner_id, args, account_id=account_id)

    async def scrape(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlScrapeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Extracts the full content of a single webpage in markdown or JSON format, with options to strip ads and keep only the main content. It loads the page in a headless browser, rendering JavaScript before extraction. Use this to ingest a specific article, documentation page, or landing page for analysis or downstream processing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("firecrawl.scrape", owner_id, args, account_id=account_id)

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FirecrawlSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches the web for a query and returns full page content (not just snippets) for each result. Unlike traditional search APIs, this returns the actual scraped content of the result pages. Use this to gather comprehensive information on a topic, find references, or research competitors without manually visiting each result.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("firecrawl.search", owner_id, args, account_id=account_id)
