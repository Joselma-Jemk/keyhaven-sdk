# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `asana_scim` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AsanaScimAttachmentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: str


class AsanaScimAttachmentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AsanaScimAttachmentsListQuery


class AsanaScimGoalsGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    goal_gid: str


class AsanaScimGoalsGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AsanaScimGoalsGetPath


class AsanaScimGoalsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    portfolio: str | None = None
    project: str | None = None
    is_workspace_level: bool | None = None
    team: str | None = None
    workspace: str | None = None
    time_periods: list[Any] | None = None


class AsanaScimGoalsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AsanaScimGoalsListQuery | None = None


class AsanaScimPortfoliosGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    portfolio_gid: str


class AsanaScimPortfoliosGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AsanaScimPortfoliosGetPath


class AsanaScimPortfoliosListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace: str
    owner: str


class AsanaScimPortfoliosListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AsanaScimPortfoliosListQuery


class AsanaScimNamespace:
    """Typed accessor for the `asana_scim` app. Reach it via `client.apps.asana_scim`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def attachments_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaScimAttachmentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all attachments (files, images, links) attached to a given parent object identified by its GID, such as a task or project. Use this to discover which files are linked to an item before downloading or inspecting them. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana_scim.attachments_list", owner_id, args, account_id=account_id
        )

    async def goals_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaScimGoalsGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single goal by its unique GID, including its name, due date, owner, status, and time period. Use this after listing goals to inspect full details of a specific goal. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana_scim.goals_get", owner_id, args, account_id=account_id
        )

    async def goals_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaScimGoalsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of goals, optionally filtered by portfolio, project, team, workspace, or time period. Use this to browse or discover existing goals before reading or updating one. This is a read-only operation with no side effects on the data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana_scim.goals_list", owner_id, args, account_id=account_id
        )

    async def portfolios_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaScimPortfoliosGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single portfolio by its unique GID, returning details such as name, owner, project members, and color. Use this after listing portfolios to get full metadata on a specific one. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana_scim.portfolios_get", owner_id, args, account_id=account_id
        )

    async def portfolios_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaScimPortfoliosListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a list of portfolios in a given workspace, filtered by owner. Use this to discover existing portfolios before performing operations on a specific one. This is a read-only operation with no side effects on the data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana_scim.portfolios_list", owner_id, args, account_id=account_id
        )
