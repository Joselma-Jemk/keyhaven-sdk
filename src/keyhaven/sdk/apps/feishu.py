# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `feishu` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FeishuEditMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    message_id: str


class FeishuEditMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class FeishuEditMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    msg_type: Literal["text", "post"] = "text"
    content: str


class FeishuEditMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FeishuEditMessagePath
    header: FeishuEditMessageHeader = Field(default_factory=FeishuEditMessageHeader)
    body: FeishuEditMessageBody


class FeishuForwardMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    message_id: str


class FeishuForwardMessageQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    receive_id_type: Literal["open_id", "user_id", "union_id", "email", "chat_id", "thread_id"] = (
        "open_id"
    )
    uuid: str | None = None


class FeishuForwardMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class FeishuForwardMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    receive_id: str


class FeishuForwardMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FeishuForwardMessagePath
    query: FeishuForwardMessageQuery = Field(default_factory=FeishuForwardMessageQuery)
    header: FeishuForwardMessageHeader = Field(default_factory=FeishuForwardMessageHeader)
    body: FeishuForwardMessageBody


class FeishuMergeForwardMessageQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    receive_id_type: Literal["open_id", "user_id", "union_id", "email", "chat_id", "thread_id"] = (
        "open_id"
    )
    uuid: str | None = None


class FeishuMergeForwardMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class FeishuMergeForwardMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    receive_id: str
    message_id_list: list[Any]


class FeishuMergeForwardMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FeishuMergeForwardMessageQuery = Field(default_factory=FeishuMergeForwardMessageQuery)
    header: FeishuMergeForwardMessageHeader = Field(default_factory=FeishuMergeForwardMessageHeader)
    body: FeishuMergeForwardMessageBody


class FeishuReplyMessagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    message_id: str


class FeishuReplyMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json; charset=utf-8", alias="Content-Type")


class FeishuReplyMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    content: str
    msg_type: str
    reply_in_thread: bool = False
    uuid: str | None = None


class FeishuReplyMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FeishuReplyMessagePath
    header: FeishuReplyMessageHeader = Field(default_factory=FeishuReplyMessageHeader)
    body: FeishuReplyMessageBody


class FeishuSendMessageQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    receive_id_type: Literal["open_id", "user_id", "union_id", "email", "chat_id"] = "open_id"


class FeishuSendMessageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class FeishuSendMessageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    receive_id: str
    msg_type: Literal[
        "text", "post", "image", "file", "audio", "media", "sticker", "interactive"
    ] = "text"
    content: str
    uuid: str | None = None


class FeishuSendMessageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FeishuSendMessageQuery = Field(default_factory=FeishuSendMessageQuery)
    header: FeishuSendMessageHeader = Field(default_factory=FeishuSendMessageHeader)
    body: FeishuSendMessageBody


class FeishuNamespace:
    """Typed accessor for the `feishu` app. Reach it via `client.apps.feishu`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def edit_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FeishuEditMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Edit the content of an already sent Feishu message, replacing it with new text or post content. Only text and post message types can be edited after sending. The updated content replaces the original for all chat participants. Use this when an agent needs to correct or update a previously sent message — requires confirmation since it modifies visible communication retroactively.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "feishu.edit_message", owner_id, args, account_id=account_id
        )

    async def forward_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FeishuForwardMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Forward an existing Feishu message to another user or chat, preserving its original content and sender context. The forwarded message appears in the target conversation immediately. Use this when an agent needs to share a specific message with another person or group — requires confirmation since it shares potentially sensitive message content outside the original conversation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "feishu.forward_message", owner_id, args, account_id=account_id
        )

    async def merge_forward_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FeishuMergeForwardMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Combine multiple Feishu messages (up to 100) from the same chat into a single merged message and forward it to another user or chat. All source messages must be from the same conversation. The merged forward appears as a single combined message in the target chat. Use this when an agent needs to share a batch of related messages as a digest — requires confirmation since it can expose large volumes of conversation content externally.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "feishu.merge_forward_message", owner_id, args, account_id=account_id
        )

    async def reply_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FeishuReplyMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Reply to an existing Feishu message in a chat, optionally as a thread reply (reply_in_thread). The reply is immediately visible to all chat participants and cannot be undone. Use this when an agent needs to respond to a specific message in context — requires human confirmation to avoid unintended public replies.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "feishu.reply_message", owner_id, args, account_id=account_id
        )

    async def send_message(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FeishuSendMessageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Send a new message (text, post, image, file, audio, media, sticker, or interactive card) to a Feishu user or group chat using their receive_id (open_id, user_id, union_id, email, or chat_id). The message is delivered immediately and visible to the recipient — this action cannot be undone. Use this when an agent needs to notify, alert, or communicate with a Feishu user or channel. Requires human confirmation to avoid sending unintended content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "feishu.send_message", owner_id, args, account_id=account_id
        )
