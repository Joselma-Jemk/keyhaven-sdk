# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `bullhorn` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BullhornCandidateCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    corpToken: str


class BullhornCandidateCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    BhRestToken: str


class BullhornCandidateCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    firstName: str
    lastName: str
    email1: str | None = None
    phone: str | None = None
    status: str | None = None
    address: dict[str, Any] | None = None


class BullhornCandidateCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BullhornCandidateCreatePath
    query: BullhornCandidateCreateQuery
    body: BullhornCandidateCreateBody


class BullhornEntityQueryPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    corpToken: str
    entityType: str


class BullhornEntityQueryQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    BhRestToken: str
    query: str
    fields: str | None = None
    count: int = 10
    sort: str | None = None


class BullhornEntityQueryRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BullhornEntityQueryPath
    query: BullhornEntityQueryQuery


class BullhornEntitySearchPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    corpToken: str
    entityType: str


class BullhornEntitySearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    BhRestToken: str
    query: str
    fields: str | None = None
    count: int = 10
    sort: str | None = None


class BullhornEntitySearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BullhornEntitySearchPath
    query: BullhornEntitySearchQuery


class BullhornJobOrderCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    corpToken: str


class BullhornJobOrderCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    BhRestToken: str


class BullhornJobOrderCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    clientContact: dict[str, Any]
    clientCorporation: dict[str, Any]
    numOpenings: int | None = None
    salary: float | None = None
    status: str | None = None
    isOpen: bool | None = None


class BullhornJobOrderCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BullhornJobOrderCreatePath
    query: BullhornJobOrderCreateQuery
    body: BullhornJobOrderCreateBody


class BullhornPlacementCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    corpToken: str


class BullhornPlacementCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    BhRestToken: str


class BullhornPlacementCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    candidate: dict[str, Any]
    jobOrder: dict[str, Any]
    dateBegin: str | None = None
    salary: float | None = None


class BullhornPlacementCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BullhornPlacementCreatePath
    query: BullhornPlacementCreateQuery
    body: BullhornPlacementCreateBody


class BullhornNamespace:
    """Typed accessor for the `bullhorn` app. Reach it via `client.apps.bullhorn`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def candidate_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BullhornCandidateCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new candidate profile in the Bullhorn ATS with personal details such as name, email, phone, address, and status. This adds a permanent record to the candidate database that recruiters can view, search, and associate with job orders or placements. Use this function when an agent needs to onboard a new applicant or lead into the system. The operation is reversible by subsequently deleting or marking the candidate inactive.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bullhorn.candidate_create", owner_id, args, account_id=account_id
        )

    async def entity_query(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BullhornEntityQueryRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Queries Bullhorn entities using JPQL (SQL-like) syntax, returning matching records with the requested fields and sorting. This is a read-only operation that does not create or modify any data. Use this function when an agent needs structured database-style queries against entity fields. Results can be sorted and limited for efficient retrieval.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bullhorn.entity_query", owner_id, args, account_id=account_id
        )

    async def entity_search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BullhornEntitySearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches across Bullhorn entity types (Candidate, JobOrder, Placement, ClientContact, etc.) using Lucene query syntax, returning matching records with the requested fields. This is a read-only operation that does not create or modify any data. Use this function when an agent needs to find records by flexible full-text or field-level criteria. Results can be sorted and limited for efficient retrieval.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bullhorn.entity_search", owner_id, args, account_id=account_id
        )

    async def job_order_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BullhornJobOrderCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new job order record in the Bullhorn ATS, capturing the position title, client contact, client corporation, number of openings, salary range, and open/closed status. This makes the job visible to recruiters for candidate searching and placement activities within the system. Use this function when an agent needs to register a new hiring requirement from a client. The record can be updated or closed later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bullhorn.job_order_create", owner_id, args, account_id=account_id
        )

    async def placement_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BullhornPlacementCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a placement record that formally associates a candidate with a job order, including the start date and agreed salary. This represents a successful hire in the ATS and typically triggers downstream business processes such as commission calculations, timesheet setup, and reporting. Use this function when an agent needs to confirm that a candidate has been placed into a job. The placement can be adjusted or terminated if needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "bullhorn.placement_create", owner_id, args, account_id=account_id
        )
