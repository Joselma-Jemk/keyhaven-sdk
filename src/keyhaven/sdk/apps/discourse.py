# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `discourse` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class DiscourseCategoriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_subcategories: bool | None = None
    include_topics: bool | None = None


class DiscourseCategoriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DiscourseCategoriesListQuery | None = None


class DiscoursePostCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    topic_id: int
    raw: str
    reply_to_post_number: int | None = None


class DiscoursePostCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DiscoursePostCreateBody


class DiscourseSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    q: str
    page: int | None = None


class DiscourseSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DiscourseSearchQuery


class DiscourseTopicCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    raw: str
    category: int | None = None
    created_at: str | None = None


class DiscourseTopicCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: DiscourseTopicCreateBody


class DiscourseTopicGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class DiscourseTopicGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    track_visit: bool | None = None
    include_raw: bool | None = None


class DiscourseTopicGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: DiscourseTopicGetPath
    query: DiscourseTopicGetQuery | None = None


class DiscourseTopicsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    order: str | None = None
    ascending: bool | None = None
    page: int | None = None
    category: str | None = None


class DiscourseTopicsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: DiscourseTopicsListQuery | None = None


class DiscourseNamespace:
    """Typed accessor for the `discourse` app. Reach it via `client.apps.discourse`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def categories_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DiscourseCategoriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all categories in the Discourse forum, optionally including their subcategories and associated topics. Categories are the top-level organisational structure of the forum. Use this to discover available categories before creating or sorting topics.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "discourse.categories_list", owner_id, args, account_id=account_id
        )

    async def post_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DiscoursePostCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Post a reply to an existing topic in the forum. The reply appears immediately in the thread and is visible to all users with access. An optional reply_to_post_number allows threading within the conversation. Use to respond to a discussion; the post cannot be recalled once published.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "discourse.post_create", owner_id, args, account_id=account_id
        )

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DiscourseSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search forum content using a query string that supports Discourse's advanced search syntax (categories, tags, users, status filters). Returns matching topics and posts. Use to find relevant discussions before creating a new topic to avoid duplicates.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("discourse.search", owner_id, args, account_id=account_id)

    async def topic_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DiscourseTopicCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new topic (discussion thread) in the forum. The topic is immediately visible to all users with access to the specified category and cannot be recalled, only edited or deleted by moderators. Use when you need to start a new discussion; review title and content carefully as this constitutes a public post.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "discourse.topic_create", owner_id, args, account_id=account_id
        )

    async def topic_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DiscourseTopicGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a single forum topic and its posts by topic ID or slug. Optionally tracks the visit and includes raw post content. Use this to read the full conversation in a topic before replying, or to inspect content for moderation purposes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "discourse.topic_get", owner_id, args, account_id=account_id
        )

    async def topics_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: DiscourseTopicsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List the latest topics from the forum, sorted by activity, views, votes, or creation date. Supports filtering by category and pagination. Use this to discover recent discussions before reading or replying to a specific topic.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "discourse.topics_list", owner_id, args, account_id=account_id
        )
