# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `clerk` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ClerkOrganizationCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    created_by: str
    slug: str | None = None
    public_metadata: dict[str, Any] | None = None
    private_metadata: dict[str, Any] | None = None


class ClerkOrganizationCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ClerkOrganizationCreateBody


class ClerkOrganizationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    includeMembersCount: bool | None = None
    query: str | None = None


class ClerkOrganizationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ClerkOrganizationsListQuery | None = None


class ClerkUserCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email_address: list[Any]
    password: str | None = None
    first_name: str | None = None
    last_name: str | None = None
    public_metadata: dict[str, Any] | None = None
    private_metadata: dict[str, Any] | None = None


class ClerkUserCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ClerkUserCreateBody


class ClerkUserDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class ClerkUserDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClerkUserDeletePath


class ClerkUserGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str


class ClerkUserGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ClerkUserGetPath


class ClerkUsersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    orderBy: str | None = None


class ClerkUsersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ClerkUsersListQuery | None = None


class ClerkNamespace:
    """Typed accessor for the `clerk` app. Reach it via `client.apps.clerk`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def organization_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClerkOrganizationCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new organization with a name, a creator user ID, an optional unique slug, and optional public or private metadata. The organization is immediately available, and the creator is added as its first member. Use this to programmatically set up multi-tenant structures, onboard new teams, or create organisation entities for access control.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clerk.organization_create", owner_id, args, account_id=account_id
        )

    async def organizations_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClerkOrganizationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all organizations in the Clerk application, with optional filtering by ID, name, or slug and optional member count inclusion. No data is modified. Use this to audit organizations, find IDs for subsequent operations, or support multi-tenant admin views.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clerk.organizations_list", owner_id, args, account_id=account_id
        )

    async def user_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClerkUserCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new user account via the Clerk Backend API with email address, password, name, and optional public or private metadata. The user is immediately active and can sign in. Use this to programmatically onboard users, register accounts during testing, or provision users through admin workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clerk.user_create", owner_id, args, account_id=account_id
        )

    async def user_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClerkUserDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a user account and all associated data by their ID. This action is irreversible — the user will lose all access to the application immediately and their data cannot be recovered. Use this only when you intend to fully revoke access, for example after a data deletion request or account termination.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "clerk.user_delete", owner_id, args, account_id=account_id
        )

    async def user_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClerkUserGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full profile of a specific user by their ID, including email addresses, names, and any associated public or private metadata. No data is modified. Use this when you have a user ID and need to inspect their account details or verify their metadata.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("clerk.user_get", owner_id, args, account_id=account_id)

    async def users_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ClerkUsersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all users in the Clerk application, with optional sorting, result limiting, and offset pagination. No user data is modified, but the response contains personally identifiable information. Use this to audit users, locate a specific user ID for subsequent operations, or support admin dashboard views.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("clerk.users_list", owner_id, args, account_id=account_id)
