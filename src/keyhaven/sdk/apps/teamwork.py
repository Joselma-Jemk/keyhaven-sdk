# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `teamwork` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TeamworkProjectCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project: dict[str, Any]


class TeamworkProjectCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: TeamworkProjectCreateBody


class TeamworkProjectGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class TeamworkProjectGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TeamworkProjectGetPath


class TeamworkProjectUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class TeamworkProjectUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project: dict[str, Any]


class TeamworkProjectUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TeamworkProjectUpdatePath
    body: TeamworkProjectUpdateBody


class TeamworkProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    perPage: int | None = None
    status: str | None = None


class TeamworkProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TeamworkProjectsListQuery | None = None


class TeamworkTaskCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    listId: str


class TeamworkTaskCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    todo_item: dict[str, Any] = Field(alias="todo-item")


class TeamworkTaskCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TeamworkTaskCreatePath
    body: TeamworkTaskCreateBody


class TeamworkTasksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    pageSize: int | None = None
    projectId: str | None = None
    todoListId: str | None = None
    filter: str | None = None
    status: str | None = None
    searchTerm: str | None = None


class TeamworkTasksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TeamworkTasksListQuery | None = None


class TeamworkNamespace:
    """Typed accessor for the `teamwork` app. Reach it via `client.apps.teamwork`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def project_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TeamworkProjectCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new project on the Teamwork site with a required name and optional description, dates, category, and company association. The new project immediately appears in the project list and becomes accessible to team members based on their permissions. Use this to set up a new initiative or workstream. The project can be archived or deleted later if needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "teamwork.project_create", owner_id, args, account_id=account_id
        )

    async def project_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TeamworkProjectGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single Teamwork project by its unique identifier, including name, description, dates, status, company, category, owner, and task list summaries. Use this to inspect a project's metadata before creating tasks or updating the project. Read-only and does not modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "teamwork.project_get", owner_id, args, account_id=account_id
        )

    async def project_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TeamworkProjectUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more fields of an existing Teamwork project including its name, description, start and end dates, and status (active, archived, etc.). Changes are immediately visible to all team members with access to the project. Use this to reflect schedule changes, correct project metadata, or change a project's lifecycle status. The update can be undone by reverting the changed fields.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "teamwork.project_update", owner_id, args, account_id=account_id
        )

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TeamworkProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of projects from the Teamwork site, with optional filtering by status (active, archived, deleted, or all). Each project includes its name, description, dates, status, company, and category. Use this to browse all accessible projects, find a project by name, or discover project IDs needed for subsequent operations. Read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "teamwork.projects_list", owner_id, args, account_id=account_id
        )

    async def task_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TeamworkTaskCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new task (todo item) inside a specific task list on Teamwork, with a required content/title and optional due date, start date, priority, and notification flag. The task appears in the task list immediately and is visible to all project members. Use this to assign work items to team members or build out a project's task breakdown. Creating a task is internal and can be deleted or completed later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "teamwork.task_create", owner_id, args, account_id=account_id
        )

    async def tasks_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TeamworkTasksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of tasks across the Teamwork site with optional filters by project, task list, status, predefined filter (today, overdue, completed), and free-text search. Each task includes its content, dates, priority, assignee, and completion status. Use this to find tasks assigned to a user, view overdue items, or search for specific work items. Read-only with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "teamwork.tasks_list", owner_id, args, account_id=account_id
        )
