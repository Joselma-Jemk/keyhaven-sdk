# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `google_sheets` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GoogleSheetsSpreadsheetCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    properties: dict[str, Any]
    sheets: list[Any] | None = None
    namedRanges: list[Any] | None = None
    spreadsheetUrl: str | None = None
    developerMetadata: list[Any] | None = None
    dataSources: list[Any] | None = None
    dataSourceSchedules: list[Any] | None = None


class GoogleSheetsSpreadsheetCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: GoogleSheetsSpreadsheetCreateBody


class GoogleSheetsValuesAppendPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    spreadsheetId: str
    range: str


class GoogleSheetsValuesAppendQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    valueInputOption: Literal["INPUT_VALUE_OPTION_UNSPECIFIED", "RAW", "USER_ENTERED"]
    insertDataOption: Literal["OVERWRITE", "INSERT_ROWS"] = "OVERWRITE"
    includeValuesInResponse: bool | None = None
    responseValueRenderOption: Literal["FORMATTED_VALUE", "UNFORMATTED_VALUE", "FORMULA"] = (
        "FORMATTED_VALUE"
    )
    responseDateTimeRenderOption: Literal["SERIAL_NUMBER", "FORMATTED_STRING"] = "SERIAL_NUMBER"


class GoogleSheetsValuesAppendBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    range: str
    majorDimension: Literal["DIMENSION_UNSPECIFIED", "ROWS", "COLUMNS"] | None = None
    values: list[Any]


class GoogleSheetsValuesAppendRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleSheetsValuesAppendPath
    query: GoogleSheetsValuesAppendQuery
    body: GoogleSheetsValuesAppendBody


class GoogleSheetsValuesBatchUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    spreadsheetId: str


class GoogleSheetsValuesBatchUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    valueInputOption: Literal["INPUT_VALUE_OPTION_UNSPECIFIED", "RAW", "USER_ENTERED"]
    data: list[Any] | None = None
    includeValuesInResponse: bool | None = None
    responseValueRenderOption: Literal["FORMATTED_VALUE", "UNFORMATTED_VALUE", "FORMULA"] = (
        "FORMATTED_VALUE"
    )
    responseDateTimeRenderOption: Literal["SERIAL_NUMBER", "FORMATTED_STRING"] = "SERIAL_NUMBER"


class GoogleSheetsValuesBatchUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleSheetsValuesBatchUpdatePath
    body: GoogleSheetsValuesBatchUpdateBody


class GoogleSheetsValuesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    spreadsheetId: str
    range: str


class GoogleSheetsValuesGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    majorDimension: Literal["ROWS", "COLUMNS"] | None = None
    valueRenderOption: Literal["FORMATTED_VALUE", "UNFORMATTED_VALUE", "FORMULA"] = (
        "FORMATTED_VALUE"
    )
    dateTimeRenderOption: Literal["SERIAL_NUMBER", "FORMATTED_STRING"] = "SERIAL_NUMBER"


class GoogleSheetsValuesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleSheetsValuesGetPath
    query: GoogleSheetsValuesGetQuery | None = None


class GoogleSheetsValuesUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    spreadsheetId: str
    range: str


class GoogleSheetsValuesUpdateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    valueInputOption: Literal["INPUT_VALUE_OPTION_UNSPECIFIED", "RAW", "USER_ENTERED"]
    includeValuesInResponse: bool | None = None
    responseValueRenderOption: Literal["FORMATTED_VALUE", "UNFORMATTED_VALUE", "FORMULA"] = (
        "FORMATTED_VALUE"
    )
    responseDateTimeRenderOption: Literal["SERIAL_NUMBER", "FORMATTED_STRING"] = "SERIAL_NUMBER"


class GoogleSheetsValuesUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    range: str | None = None
    majorDimension: Literal["DIMENSION_UNSPECIFIED", "ROWS", "COLUMNS"] | None = None
    values: list[Any]


class GoogleSheetsValuesUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GoogleSheetsValuesUpdatePath
    query: GoogleSheetsValuesUpdateQuery
    body: GoogleSheetsValuesUpdateBody


class GoogleSheetsNamespace:
    """Typed accessor for the `google_sheets` app. Reach it via `client.apps.google_sheets`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def spreadsheet_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleSheetsSpreadsheetCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new Google Sheet in the authenticated user's Google Drive with a specified title, locale, timezone, and initial sheet configuration. The new spreadsheet appears immediately in the user's Drive and can be shared with collaborators. Use this when the agent needs to create a fresh spreadsheet to store structured data or generate a new report.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_sheets.spreadsheet_create", owner_id, args, account_id=account_id
        )

    async def values_append(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleSheetsValuesAppendRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Appends rows of data to an existing table within a Google Sheet by detecting the table boundaries and adding data after the last row. Can either overwrite existing data or insert new rows into the sheet. The new data is immediately visible and affects the sheet layout for all collaborators. Use this when the agent needs to add records to a growing dataset such as log entries, form submissions, or transaction records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_sheets.values_append", owner_id, args, account_id=account_id
        )

    async def values_batch_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleSheetsValuesBatchUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Writes data to multiple ranges in a Google Sheet in a single atomic request, where either all updates succeed or none are applied. Each range in the batch receives new values independently, enabling coordinated updates across non-contiguous areas. Use this when the agent needs to update several ranges simultaneously while maintaining data consistency across the spreadsheet.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_sheets.values_batch_update", owner_id, args, account_id=account_id
        )

    async def values_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleSheetsValuesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Reads cell values from a specified range in a Google Sheet using A1 or R1C1 notation. Returns data as a 2D array with configurable rendering options for formatted values, raw values, or formulas. Use this when the agent needs to retrieve existing spreadsheet data for analysis, display, or transformation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_sheets.values_get", owner_id, args, account_id=account_id
        )

    async def values_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GoogleSheetsValuesUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Overwrites cell values in a specified range of a Google Sheet, replacing existing content with new data. Values inside the target range are overwritten; values outside the range are preserved. The change is immediately visible to anyone with access and is tracked in the sheet's version history. Use this when the agent needs to replace existing data in a defined range of cells.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "google_sheets.values_update", owner_id, args, account_id=account_id
        )
