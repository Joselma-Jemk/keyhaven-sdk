# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `twitch` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TwitchClipsCreateQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    broadcaster_id: str
    title: str | None = None
    duration: float | None = None


class TwitchClipsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwitchClipsCreateQuery


class TwitchGamesTopQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first: int | None = None
    before: str | None = None
    after: str | None = None


class TwitchGamesTopRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwitchGamesTopQuery | None = None


class TwitchStreamsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_id: str | None = None
    user_login: str | None = None
    game_id: str | None = None
    type_: str | None = Field(default=None, alias="type")
    language: str | None = None
    first: int | None = None
    before: str | None = None
    after: str | None = None


class TwitchStreamsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwitchStreamsListQuery | None = None


class TwitchUsersGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str | None = None
    login: str | None = None


class TwitchUsersGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwitchUsersGetQuery | None = None


class TwitchVideosListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str | None = None
    user_id: str | None = None
    game_id: str | None = None
    type_: str | None = Field(default=None, alias="type")
    language: str | None = None
    period: str | None = None
    sort: str | None = None
    first: int | None = None
    before: str | None = None
    after: str | None = None


class TwitchVideosListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwitchVideosListQuery | None = None


class TwitchNamespace:
    """Typed accessor for the `twitch` app. Reach it via `client.apps.twitch`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def clips_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitchClipsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a short video clip (5–60 seconds) from a specified broadcaster's currently live stream, with an optional title. The clip is published on Twitch and becomes publicly viewable with its own URL, viewable by anyone on the platform. Use this to capture and share memorable moments from a live broadcast; clips can be deleted later by the broadcaster.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitch.clips_create", owner_id, args, account_id=account_id
        )

    async def games_top(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitchGamesTopRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of the most-watched games on Twitch right now, ranked by current viewer count. Each result includes the game's ID, name, box art URL, and viewer count. Use this to identify trending games, discover popular categories, or benchmark a game's current popularity on the platform.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("twitch.games_top", owner_id, args, account_id=account_id)

    async def streams_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitchStreamsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of currently live broadcasts on Twitch, filterable by specific broadcaster IDs, usernames, game ID, language, or stream type. Each result includes the stream's title, viewer count, game name, language, start time, and thumbnail URL. Use this to discover active streams, monitor channel activity, or gather real-time analytics on live content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitch.streams_list", owner_id, args, account_id=account_id
        )

    async def users_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitchUsersGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches profile information for one or more Twitch users by their user ID or login name. The response includes each user's display name, description, profile image URL, offline image URL, and account creation date. Use this to look up broadcaster details, verify user identities, or enrich stream metadata with profile data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("twitch.users_get", owner_id, args, account_id=account_id)

    async def videos_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwitchVideosListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of VODs (archives, highlights, or uploads) for a specified broadcaster, game, or by direct video IDs, with filtering by language, time period, and sort order. Each result includes the video's title, description, duration, view count, creation date, and thumbnail URL. Use this to browse a channel's past broadcasts, find highlights, or analyze video performance.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twitch.videos_list", owner_id, args, account_id=account_id
        )
