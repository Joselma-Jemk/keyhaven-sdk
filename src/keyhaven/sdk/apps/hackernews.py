# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `hackernews` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class HackernewsItemGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class HackernewsItemGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HackernewsItemGetPath


class HackernewsTopStoriesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class HackernewsUserGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class HackernewsUserGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HackernewsUserGetPath


class HackernewsUserReceivedGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class HackernewsUserReceivedGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HackernewsUserReceivedGetPath


class HackernewsUserStoriesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class HackernewsUserStoriesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HackernewsUserStoriesGetPath


class HackernewsNamespace:
    """Typed accessor for the `hackernews` app. Reach it via `client.apps.hackernews`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def item_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HackernewsItemGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single Hacker News item (story, comment, job, poll, or poll option) by its numeric ID. Returns full item data including title, URL, author, score, timestamp, and nested comment IDs for thread traversal. Use this when the agent needs to read the content or metadata of a specific post or explore a discussion thread.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "hackernews.item_get", owner_id, args, account_id=account_id
        )

    async def top_stories_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HackernewsTopStoriesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches an array of story IDs representing the current top stories on Hacker News, ranked by community upvotes. Returns up to 500 IDs that must then be expanded via HACKERNEWS__ITEM_GET to retrieve full details. Use this when the agent needs to discover what is trending on Hacker News.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "hackernews.top_stories_get", owner_id, args, account_id=account_id
        )

    async def user_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HackernewsUserGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a Hacker News user's public profile by their username. Returns the user's creation date, karma score, and lists of recent submissions and comments. Use this when the agent needs to check a user's reputation, activity history, or discover their recent posts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "hackernews.user_get", owner_id, args, account_id=account_id
        )

    async def user_received_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HackernewsUserReceivedGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches comment IDs that a specific Hacker News user has received as replies to their submissions. Returns item IDs that can be expanded with HACKERNEWS__ITEM_GET to read the actual reply content. Use this when the agent needs to find replies directed at a user or monitor engagement on their posts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "hackernews.user_received_get", owner_id, args, account_id=account_id
        )

    async def user_stories_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HackernewsUserStoriesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches story IDs submitted by a specific Hacker News user, ordered by most recent first. Returns IDs that can be expanded with HACKERNEWS__ITEM_GET to read full story content and metadata. Use this when the agent needs to find all submissions by a particular user, analyze their posting history, or monitor their activity.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "hackernews.user_stories_get", owner_id, args, account_id=account_id
        )
