# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `elevenlabs` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ElevenlabsHistoryListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    xi_api_key: str | None = Field(default=None, alias="xi-api-key")


class ElevenlabsHistoryListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ElevenlabsHistoryListHeader | None = None


class ElevenlabsUserListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    xi_api_key: str | None = Field(default=None, alias="xi-api-key")


class ElevenlabsUserListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ElevenlabsUserListHeader | None = None


class ElevenlabsVoicesGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice_id: str


class ElevenlabsVoicesGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    with_settings: bool | None = None


class ElevenlabsVoicesGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    xi_api_key: str | None = Field(default=None, alias="xi-api-key")


class ElevenlabsVoicesGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ElevenlabsVoicesGetPath
    query: ElevenlabsVoicesGetQuery | None = None
    header: ElevenlabsVoicesGetHeader | None = None


class ElevenlabsVoicesListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    xi_api_key: str | None = Field(default=None, alias="xi-api-key")


class ElevenlabsVoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ElevenlabsVoicesListHeader | None = None


class ElevenlabsVoicesSettingsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    voice_id: str


class ElevenlabsVoicesSettingsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    xi_api_key: str | None = Field(default=None, alias="xi-api-key")


class ElevenlabsVoicesSettingsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ElevenlabsVoicesSettingsListPath
    header: ElevenlabsVoicesSettingsListHeader | None = None


class ElevenlabsNamespace:
    """Typed accessor for the `elevenlabs` app. Reach it via `client.apps.elevenlabs`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def history_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenlabsHistoryListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the history of all generated text-to-speech audio items in the ElevenLabs account, including timestamps, the text that was synthesised, the voice used, and character counts. Use this to audit past generations, retrieve previously generated audio IDs, or track API usage against subscription quotas.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "elevenlabs.history_list", owner_id, args, account_id=account_id
        )

    async def user_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenlabsUserListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve information about the authenticated ElevenLabs user, including subscription tier (free, starter, creator, pro), character quota usage and limits, and account settings. Use this when an agent needs to check remaining quota, verify subscription status, or determine whether speech generation is possible before proceeding.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "elevenlabs.user_list", owner_id, args, account_id=account_id
        )

    async def voices_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenlabsVoicesGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve detailed information about a specific ElevenLabs voice by its ID, including name, category, description, preview URL, and optionally its current settings (stability, clarity, style exaggeration, and speed). Use this to inspect a voice's configuration and characteristics before using it for speech generation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "elevenlabs.voices_get", owner_id, args, account_id=account_id
        )

    async def voices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenlabsVoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all available voices in the ElevenLabs account, including both preset (premade) voices and any custom cloned voices. Returns each voice's unique ID, name, category, and a preview URL. Use this when an agent needs to discover which voices are available before selecting one for text-to-speech generation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "elevenlabs.voices_list", owner_id, args, account_id=account_id
        )

    async def voices_settings_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ElevenlabsVoicesSettingsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the current synthesis settings for a specific ElevenLabs voice, including stability, similarity_boost, style, and use_speaker_boost. These parameters control the naturalness, expressiveness, and consistency of generated speech. Use this to inspect a voice's tuning before generating audio or to capture current settings after modification.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "elevenlabs.voices_settings_list", owner_id, args, account_id=account_id
        )
