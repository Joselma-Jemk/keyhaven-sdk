# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `anchor_browser` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AnchorBrowserCreateProfileHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AnchorBrowserCreateProfileBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    source: Literal["session"]
    session_id: str
    store_cache: bool = False


class AnchorBrowserCreateProfileRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AnchorBrowserCreateProfileHeader | None = None
    body: AnchorBrowserCreateProfileBody


class AnchorBrowserGetWebpageContentBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str | None = None
    format: Literal["html", "markdown"] | None = None
    sessionId: str | None = None


class AnchorBrowserGetWebpageContentRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AnchorBrowserGetWebpageContentBody = Field(
        default_factory=AnchorBrowserGetWebpageContentBody
    )


class AnchorBrowserStartSessionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    session: dict[str, Any]
    browser: dict[str, Any]


class AnchorBrowserStartSessionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AnchorBrowserStartSessionBody


class AnchorBrowserTakeScreenshotPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sessionId: str


class AnchorBrowserTakeScreenshotRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AnchorBrowserTakeScreenshotPath


class AnchorBrowserTypeTextPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sessionId: str


class AnchorBrowserTypeTextHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AnchorBrowserTypeTextBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    text: str
    delay: int = 0


class AnchorBrowserTypeTextRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AnchorBrowserTypeTextPath
    header: AnchorBrowserTypeTextHeader | None = None
    body: AnchorBrowserTypeTextBody


class AnchorBrowserNamespace:
    """Typed accessor for the `anchor_browser` app. Reach it via `client.apps.anchor_browser`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_profile(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AnchorBrowserCreateProfileRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Persists the current browser state — cookies, local storage, and optionally cache — from an active session into a named profile that can be reused in future sessions. The profile is saved when the session ends and does not affect any external service. Use this to preserve authenticated sessions or browsing context for later reuse.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "anchor_browser.create_profile", owner_id, args, account_id=account_id
        )

    async def get_webpage_content(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AnchorBrowserGetWebpageContentRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Downloads the content of a given URL and returns it as raw HTML or Markdown, optionally within the context of an active browser session for cookie-aware or authenticated fetching. This is a pure read operation — it does not create or modify any resource on Anchor or the target server. Use this to extract text, metadata, or structured content from web pages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "anchor_browser.get_webpage_content", owner_id, args, account_id=account_id
        )

    async def start_session(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AnchorBrowserStartSessionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates and configures a new cloud-hosted browser session on Anchor Browser, with support for ad-blocking, captcha solving, proxy routing (Anchor residential), recording, live view, and idle timeout controls. The session allocates a remote browser instance that consumes API quota until it expires or is terminated. Use this as the mandatory first step before any browsing, screenshot, or keyboard actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "anchor_browser.start_session", owner_id, args, account_id=account_id
        )

    async def take_screenshot(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AnchorBrowserTakeScreenshotRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Captures a PNG screenshot of the current viewport of an active browser session identified by sessionId. The image reflects exactly what is visible in the browser at the time of capture, including any overlays or dynamic content — it does not modify any state. Use this to visually verify page state, capture results after automation, or debug rendering issues.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "anchor_browser.take_screenshot", owner_id, args, account_id=account_id
        )

    async def type_text(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AnchorBrowserTypeTextRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Simulates keyboard input in an active browser session by typing a specified string character by character, with an optional delay between keystrokes. The text is sent to whatever element currently has focus in the browser — this can fill forms, enter search queries, or otherwise interact with web pages as if a user were typing. Use this to automate text input in web applications programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "anchor_browser.type_text", owner_id, args, account_id=account_id
        )
