# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `wealthbox` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class WealthboxContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str | None = None
    last_name: str | None = None
    middle_name: str | None = None
    prefix: str | None = None
    suffix: str | None = None
    nickname: str | None = None
    company_name: str | None = None
    job_title: str | None = None
    contact_type: str | None = None
    type_: str | None = Field(default=None, alias="type")
    status: str | None = None
    assigned_to: int | None = None
    background_information: str | None = None
    email_addresses: list[Any] | None = None
    phone_numbers: list[Any] | None = None
    street_addresses: list[Any] | None = None
    tags: list[Any] | None = None


class WealthboxContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: WealthboxContactCreateBody = Field(default_factory=WealthboxContactCreateBody)


class WealthboxContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class WealthboxContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WealthboxContactGetPath


class WealthboxContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    name: str | None = None
    email: str | None = None
    phone: str | None = None
    type_: str | None = Field(default=None, alias="type")
    contact_type: str | None = None
    active: bool | None = None
    tags: str | None = None
    order: str | None = None
    updated_since: str | None = None
    updated_before: str | None = None


class WealthboxContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WealthboxContactsListQuery | None = None


class WealthboxTaskCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    due_date: str | None = None
    complete: bool | None = None
    priority: str | None = None
    category: int | None = None
    description: str | None = None
    visible_to: str | None = None
    linked_to: list[Any] | None = None


class WealthboxTaskCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: WealthboxTaskCreateBody


class WealthboxTasksListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    complete: bool | None = None
    assigned_to: int | None = None
    due_before: str | None = None
    due_after: str | None = None


class WealthboxTasksListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WealthboxTasksListQuery | None = None


class WealthboxNamespace:
    """Typed accessor for the `wealthbox` app. Reach it via `client.apps.wealthbox`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WealthboxContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact record in Wealthbox CRM with personal details, employment information, contact type (person, household, organization, or trust), contact category (Client, Lead), assignment, email addresses, phone numbers, street addresses, tags, and background notes. The contact is immediately persisted in the CRM and available for linking to tasks, opportunities, and events. Use when an agent needs to add a new person or organization to the CRM, such as capturing a lead or onboarding a client.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wealthbox.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WealthboxContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full profile of a single CRM contact by its unique ID, including all associated addresses, phone numbers, email addresses, custom fields, and background information stored in Wealthbox. This provides complete visibility into a contact's stored data. Use when an agent needs to view or present all known details about a specific contact, such as during client onboarding or account review.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wealthbox.contact_get", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WealthboxContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of CRM contacts from Wealthbox with comprehensive filtering by name, email, phone, contact type, tags, activity status, and update timestamps. This enables an agent to search for and discover contacts matching specific criteria for follow-up, reporting, or data synchronization. Use when you need to find contacts in the CRM by query fields.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wealthbox.contacts_list", owner_id, args, account_id=account_id
        )

    async def task_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WealthboxTaskCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new task in Wealthbox CRM with a title, due date, priority, category, description, visibility scope, and optional links to contacts or other entities. The task appears in the assignee's task list and can trigger notifications or reminders. Use when an agent needs to schedule a follow-up action, assign work, or log a reminder tied to a specific contact or opportunity.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wealthbox.task_create", owner_id, args, account_id=account_id
        )

    async def tasks_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WealthboxTasksListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of CRM tasks from Wealthbox with filtering by completion status, assignee, and due date range. Tasks represent action items or to-dos within the CRM, often linked to contacts or other entities. Use when an agent needs to view pending or completed tasks for reporting, follow-up reminders, or workload assessment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "wealthbox.tasks_list", owner_id, args, account_id=account_id
        )
