# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `grain` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GrainRecordingGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    recording_id: str


class GrainRecordingGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    includeHighlights: bool | None = None
    includeParticipants: bool | None = None
    includeAiSummary: bool | None = None
    includeCalendarEvent: bool | None = None


class GrainRecordingGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GrainRecordingGetPath
    query: GrainRecordingGetQuery | None = None


class GrainRecordingGetTranscriptPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    recording_id: str


class GrainRecordingGetTranscriptRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GrainRecordingGetTranscriptPath


class GrainRecordingUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    recording_id: str


class GrainRecordingUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    tags: list[Any] | None = None


class GrainRecordingUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GrainRecordingUpdatePath
    body: GrainRecordingUpdateBody | None = None


class GrainRecordingsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cursor: str | None = None
    beforeDatetime: str | None = None
    afterDatetime: str | None = None
    participantScope: str | None = None
    titleSearch: str | None = None
    teamId: str | None = None
    meetingTypeId: str | None = None
    includeHighlights: bool | None = None
    includeParticipants: bool | None = None
    includeAiSummary: bool | None = None


class GrainRecordingsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GrainRecordingsListQuery | None = None


class GrainNamespace:
    """Typed accessor for the `grain` app. Reach it via `client.apps.grain`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def recording_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GrainRecordingGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full metadata for a single Grain recording by its UUID, including title, date, duration, participants, and team. Optionally includes highlights (clipped moments), AI-generated summary, participant list, and linked calendar event data. Use this after obtaining a recording ID from the list endpoint to access the complete record of a specific meeting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "grain.recording_get", owner_id, args, account_id=account_id
        )

    async def recording_get_transcript(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GrainRecordingGetTranscriptRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the complete, time-coded transcript of a Grain recording, with speaker labels and timestamps for every utterance. The transcript contains the full verbatim conversation — potentially including sensitive business discussions, client details, or strategic information. Use this to extract meeting content for analysis, search within conversations, or share text-based records of recorded calls and meetings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "grain.recording_get_transcript", owner_id, args, account_id=account_id
        )

    async def recording_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GrainRecordingUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the title and tags of an existing Grain recording. The new title and assigned tags are immediately visible to all team members who have access to the recording in Grain. Use this to rename poorly-titled meetings after recording, apply consistent organizational tags, or correct metadata without re-recording. The change is reversible by calling this function again with the previous values.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "grain.recording_update", owner_id, args, account_id=account_id
        )

    async def recordings_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GrainRecordingsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists recordings from your Grain workspace with cursor-based pagination and multiple optional filters. Supports filtering by date range, team, meeting type, participant scope (internal/external), and title keyword search. Use this to discover recordings, populate meeting indexes, or find a specific recording by title before retrieving its full details or transcript.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "grain.recordings_list", owner_id, args, account_id=account_id
        )
