# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `greenhouse` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class GreenhouseApplicationAdvancePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    application_id: str


class GreenhouseApplicationAdvanceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_stage_id: str
    to_stage_id: str
    subject: str | None = None
    body: str | None = None
    send_email: bool | None = None


class GreenhouseApplicationAdvanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GreenhouseApplicationAdvancePath
    body: GreenhouseApplicationAdvanceBody


class GreenhouseApplicationsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    job_id: str | None = None
    status: str | None = None
    created_after: str | None = None
    last_activity_after: str | None = None


class GreenhouseApplicationsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GreenhouseApplicationsListQuery | None = None


class GreenhouseCandidateCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str
    last_name: str
    email_addresses: list[Any] | None = None
    phone_numbers: list[Any] | None = None
    social_media_addresses: list[Any] | None = None
    website_addresses: list[Any] | None = None
    addresses: list[Any] | None = None
    educations: list[Any] | None = None
    employments: list[Any] | None = None
    application: dict[str, Any] | None = None


class GreenhouseCandidateCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: GreenhouseCandidateCreateBody


class GreenhouseCandidatesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    email: str | None = None
    job_id: str | None = None
    created_after: str | None = None


class GreenhouseCandidatesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GreenhouseCandidatesListQuery | None = None


class GreenhouseJobGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    job_id: str


class GreenhouseJobGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: GreenhouseJobGetPath


class GreenhouseJobsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int | None = None
    per_page: int | None = None
    created_before: str | None = None
    created_after: str | None = None
    status: str | None = None


class GreenhouseJobsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: GreenhouseJobsListQuery | None = None


class GreenhouseNamespace:
    """Typed accessor for the `greenhouse` app. Reach it via `client.apps.greenhouse`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def application_advance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GreenhouseApplicationAdvanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Moves an application from one pipeline stage to another (e.g. from 'Phone Screen' to 'On-site Interview'), with an optional candidate-facing email notification. This action can advance, reject, or hire a candidate depending on the target stage. Use this to progress candidates through the hiring workflow. Advancing to a final stage (hired/rejected) is a significant HR decision that typically triggers notifications and downstream processes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "greenhouse.application_advance", owner_id, args, account_id=account_id
        )

    async def applications_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GreenhouseApplicationsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of candidate applications and prospect records in Greenhouse, with filters by job ID, status (active, converted, hired, rejected, etc.), and activity date. Each entry includes the candidate, job, current stage, and status history. Application data is personally identifiable (links to candidate PII). Use this to review the candidate pipeline for a specific job or to find applications by status.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "greenhouse.applications_list", owner_id, args, account_id=account_id
        )

    async def candidate_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GreenhouseCandidateCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new candidate record in Greenhouse with personal details (name, emails, phone numbers, social media, addresses, education, employment history) and an optional linked job application. The candidate is added to the ATS immediately. Use this to import candidates from external sources or to manually register a new applicant. The created candidate can later be advanced through hiring stages.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "greenhouse.candidate_create", owner_id, args, account_id=account_id
        )

    async def candidates_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GreenhouseCandidatesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of candidates in Greenhouse, with optional filters by email, job ID, and creation date. Each candidate includes their name, contact details, source, and attached resumes. Candidate data contains personal information (PII) such as email, phone, and address history. Use this to find candidates by email or job application before taking further actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "greenhouse.candidates_list", owner_id, args, account_id=account_id
        )

    async def job_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GreenhouseJobGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a single job record by its internal Greenhouse ID, including its title, department, office, hiring team, custom fields, and current status. Use this when the agent needs detailed information about a specific job posting, such as its requirements, interview plan, or approvals.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "greenhouse.job_get", owner_id, args, account_id=account_id
        )

    async def jobs_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: GreenhouseJobsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all jobs in Greenhouse, with optional filters by status (open, closed, draft) and creation date range. Each job includes its ID, title, department, office locations, and hiring team. Use this to discover open positions, audit job postings, or find a job ID before fetching its details or related applications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "greenhouse.jobs_list", owner_id, args, account_id=account_id
        )
