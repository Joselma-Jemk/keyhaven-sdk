# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `heygen` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class HeygenAvatarsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    avatar_type: str | None = None


class HeygenAvatarsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HeygenAvatarsListQuery | None = None


class HeygenVideoCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: str = Field(alias="type")
    avatar_id: str
    voice_id: str | None = None
    script: str | None = None
    title: str | None = None
    resolution: str | None = None
    aspect_ratio: str | None = None
    callback_url: str | None = None
    engine: dict[str, Any] | None = None
    background: dict[str, Any] | None = None
    voice_settings: dict[str, Any] | None = None


class HeygenVideoCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: HeygenVideoCreateBody


class HeygenVideoDeletePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    video_id: str


class HeygenVideoDeleteRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HeygenVideoDeletePath


class HeygenVideoGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    video_id: str


class HeygenVideoGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: HeygenVideoGetPath


class HeygenVideosListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    token: str | None = None
    folder_id: str | None = None
    title: str | None = None


class HeygenVideosListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HeygenVideosListQuery | None = None


class HeygenVoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    gender: str | None = None
    language: str | None = None


class HeygenVoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: HeygenVoicesListQuery | None = None


class HeygenNamespace:
    """Typed accessor for the `heygen` app. Reach it via `client.apps.heygen`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def avatars_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HeygenAvatarsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all available AI avatars in the account, including their unique IDs, display names, supported rendering engines, and preview images. Each avatar may have multiple looks (outfits or styles). Use this to find an avatar by type before creating a video.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "heygen.avatars_list", owner_id, args, account_id=account_id
        )

    async def video_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HeygenVideoCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Generates a new AI avatar video by selecting an avatar, voice, script text, and output settings like resolution and aspect ratio. This consumes API credits and renders the video asynchronously. Use this to produce talking-head videos from a text script for marketing, training, or content creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "heygen.video_create", owner_id, args, account_id=account_id
        )

    async def video_delete(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HeygenVideoDeleteRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a video from your HeyGen account, including its rendered file and all associated metadata. This action cannot be undone and the video URL will stop working. Use this to clean up unwanted or outdated videos.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "heygen.video_delete", owner_id, args, account_id=account_id
        )

    async def video_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HeygenVideoGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches detailed information about a single AI video including its rendering status, download URL, thumbnail, duration, and creation metadata. Use this to check whether an async video generation has completed and to retrieve the final video file.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("heygen.video_get", owner_id, args, account_id=account_id)

    async def videos_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HeygenVideosListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all previously created AI avatar videos, filterable by folder ID or title substring. Each entry shows the video status, title, thumbnail, and creation date. Use this to browse your video library or find a specific video by name.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "heygen.videos_list", owner_id, args, account_id=account_id
        )

    async def voices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: HeygenVoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the catalog of available text-to-speech voices with their IDs, names, genders, and supported languages. Use this to find an appropriate voice for a particular language or gender before invoking video creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "heygen.voices_list", owner_id, args, account_id=account_id
        )
