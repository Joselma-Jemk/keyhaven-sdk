# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `agent_mail` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AgentMailCreateInboxHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AgentMailCreateInboxBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    username: str
    domain: str | None = None
    display_name: str


class AgentMailCreateInboxRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AgentMailCreateInboxHeader = Field(default_factory=AgentMailCreateInboxHeader)
    body: AgentMailCreateInboxBody


class AgentMailGetInboxPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    inbox_id: str


class AgentMailGetInboxHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AgentMailGetInboxRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AgentMailGetInboxPath
    header: AgentMailGetInboxHeader = Field(default_factory=AgentMailGetInboxHeader)


class AgentMailListMessagesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    inbox_id: str


class AgentMailListMessagesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AgentMailListMessagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AgentMailListMessagesPath
    header: AgentMailListMessagesHeader = Field(default_factory=AgentMailListMessagesHeader)


class AgentMailListThreadsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    inbox_id: str


class AgentMailListThreadsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    last_key: str | None = None
    labels: list[Any] | None = None


class AgentMailListThreadsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AgentMailListThreadsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AgentMailListThreadsPath
    query: AgentMailListThreadsQuery | None = None
    header: AgentMailListThreadsHeader = Field(default_factory=AgentMailListThreadsHeader)


class AgentMailNamespace:
    """Typed accessor for the `agent_mail` app. Reach it via `client.apps.agent_mail`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_inbox(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AgentMailCreateInboxRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Provisions a new email inbox on the Agent Mail service with a specified username (local part), optional domain, and display name. Once created, this inbox is a real email address that can send and receive messages via the API. Use this when you need a dedicated inbox for automated correspondence, such as for a specific project, client, or workflow. The inbox persists until explicitly deleted, making this a reversible operation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "agent_mail.create_inbox", owner_id, args, account_id=account_id
        )

    async def get_inbox(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AgentMailGetInboxRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves metadata and configuration details of a specific email inbox identified by its email address. Use this to verify an inbox exists, check its display name or domain, or obtain the inbox_id needed for other operations like listing threads or messages. This is a read-only endpoint with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "agent_mail.get_inbox", owner_id, args, account_id=account_id
        )

    async def list_messages(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AgentMailListMessagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a list of all email messages from a specific inbox, providing an overview of both received and sent messages. Use this to audit email activity, check for new incoming messages, or obtain message IDs needed for subsequent detailed operations. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "agent_mail.list_messages", owner_id, args, account_id=account_id
        )

    async def list_threads(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AgentMailListThreadsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of email threads from a specific inbox, with optional filtering by labels for targeted searches. Use this to browse conversations, find messages grouped by topic, or monitor incoming communications. Supports pagination via the last_key parameter for iterating through large mailboxes. This is a read-only operation with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "agent_mail.list_threads", owner_id, args, account_id=account_id
        )
