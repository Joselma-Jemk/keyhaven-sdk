# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `akkio` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AkkioAddRowsToDatasetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AkkioAddRowsToDatasetBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str
    rows: list[Any]


class AkkioAddRowsToDatasetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AkkioAddRowsToDatasetHeader = Field(default_factory=AkkioAddRowsToDatasetHeader)
    body: AkkioAddRowsToDatasetBody


class AkkioCreateDatasetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AkkioCreateDatasetBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class AkkioCreateDatasetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AkkioCreateDatasetHeader = Field(default_factory=AkkioCreateDatasetHeader)
    body: AkkioCreateDatasetBody


class AkkioDeleteDatasetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AkkioDeleteDatasetBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AkkioDeleteDatasetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AkkioDeleteDatasetHeader = Field(default_factory=AkkioDeleteDatasetHeader)
    body: AkkioDeleteDatasetBody


class AkkioDeleteModelHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class AkkioDeleteModelBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AkkioDeleteModelRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AkkioDeleteModelHeader = Field(default_factory=AkkioDeleteModelHeader)
    body: AkkioDeleteModelBody


class AkkioListDatasetsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str | None = None


class AkkioListDatasetsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: AkkioListDatasetsQuery | None = None


class AkkioListModelsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class AkkioNamespace:
    """Typed accessor for the `akkio` app. Reach it via `client.apps.akkio`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def add_rows_to_dataset(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AkkioAddRowsToDatasetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Appends an array of data rows to an existing dataset identified by its ID. Each row is represented as an object with field names as keys and their corresponding values. Data added can be removed by deleting the entire dataset. Use this function to populate a dataset with records before training a model or to augment an existing dataset with new observations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "akkio.add_rows_to_dataset", owner_id, args, account_id=account_id
        )

    async def create_dataset(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AkkioCreateDatasetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new empty dataset with a specified name in the organisation. The dataset is initialised with no rows and is immediately ready to receive data. The new dataset can be deleted later if no longer needed. Use this function to set up a fresh data container before populating it with rows for AI model training or prediction workflows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "akkio.create_dataset", owner_id, args, account_id=account_id
        )

    async def delete_dataset(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AkkioDeleteDatasetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a dataset and all its contained rows from the organisation by dataset ID. This action is irreversible through the API and any AI models that were trained on this dataset will no longer function correctly. Use this function only when a dataset is genuinely obsolete and should be entirely purged from the system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "akkio.delete_dataset", owner_id, args, account_id=account_id
        )

    async def delete_model(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AkkioDeleteModelRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes a specific AI model by its identifier, removing it from the organisation entirely. Once deleted, the model is no longer available for predictions or inference and this cannot be undone. Use this function when a model is obsolete, failed validation, or needs to be replaced with a newer version.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "akkio.delete_model", owner_id, args, account_id=account_id
        )

    async def list_datasets(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AkkioListDatasetsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns all datasets in the organisation, or a single dataset if an optional ID filter is provided. Each dataset object includes its name, identifier, schema, row count, and creation timestamp. This is a read-only operation with no side effects. Use this function to discover available datasets before adding rows, training models, or running predictions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "akkio.list_datasets", owner_id, args, account_id=account_id
        )

    async def list_models(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AkkioListModelsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all AI models available in the organisation, including each model's identifier, name, training status, and associated dataset reference. This is a read-only operation with no side effects. Use this function to discover trained models before running predictions, evaluating performance, or managing the model lifecycle.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "akkio.list_models", owner_id, args, account_id=account_id
        )
