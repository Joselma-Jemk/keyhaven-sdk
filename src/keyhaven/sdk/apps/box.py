# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `box` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BoxFileCopyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    file_id: str


class BoxFileCopyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    version: str | None = None
    parent: dict[str, Any]


class BoxFileCopyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BoxFileCopyPath
    body: BoxFileCopyBody


class BoxFileGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    file_id: str


class BoxFileGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    fields: str | None = None


class BoxFileGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BoxFileGetPath
    query: BoxFileGetQuery | None = None


class BoxFileUploadBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    attributes: dict[str, Any]
    file: str
    content_md5: str | None = None


class BoxFileUploadRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: BoxFileUploadBody


class BoxFolderCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    parent: dict[str, Any]
    folder_upload_email: dict[str, Any] | None = None
    is_description_required_for_shared_link: bool | None = None
    access: str | None = None


class BoxFolderCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: BoxFolderCreateBody


class BoxFolderItemsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    folder_id: str


class BoxFolderItemsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    usemarker: bool | None = None
    marker: str | None = None
    fields: str | None = None


class BoxFolderItemsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BoxFolderItemsListPath
    query: BoxFolderItemsListQuery | None = None


class BoxSearchQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    scope: str | None = None
    file_extensions: str | None = None
    created_at_range: str | None = None
    updated_at_range: str | None = None
    size_range: str | None = None
    owner_user_ids: str | None = None
    ancestor_folder_ids: str | None = None
    content_types: str | None = None
    type_: str | None = Field(default=None, alias="type")
    limit: int | None = None
    offset: int | None = None
    fields: str | None = None


class BoxSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: BoxSearchQuery = Field(default_factory=BoxSearchQuery)


class BoxNamespace:
    """Typed accessor for the `box` app. Reach it via `client.apps.box`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def file_copy(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BoxFileCopyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Duplicates an existing file (or a specific version of it) into a destination folder, optionally with a new name. The original file remains completely unchanged; the copy is immediately available to users with access to the destination folder. Use this to reuse or reorganise content across folders without affecting the source.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("box.file_copy", owner_id, args, account_id=account_id)

    async def file_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BoxFileGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches metadata for a single file by its ID, including name, size, extension, parent folder, owner, permissions, and timestamps. The file content itself is not retrieved. Use this to inspect a file's properties before deciding to download, copy, or share it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("box.file_get", owner_id, args, account_id=account_id)

    async def file_upload(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BoxFileUploadRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Uploads a file (up to 50 MB) to a specified Box folder using multipart form-data. The uploaded file immediately appears in the folder for all collaborators with access. Use this to store documents, images, or other binary content into Box storage for sharing, archival, or downstream processing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("box.file_upload", owner_id, args, account_id=account_id)

    async def folder_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BoxFolderCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new empty folder under a specified parent folder (use '0' for the root folder). The folder is immediately visible in the Box web UI and accessible to collaborators based on the parent's sharing settings. Use this to organise content programmatically before uploading files.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "box.folder_create", owner_id, args, account_id=account_id
        )

    async def folder_items_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BoxFolderItemsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated listing of items (files, sub-folders, and web links) inside a specified Box folder. Supports both offset-based and marker-based pagination for navigating large folders. Use this to explore a folder's contents, verify upload results, or build a directory tree.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "box.folder_items_list", owner_id, args, account_id=account_id
        )

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BoxSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches across the user's or enterprise's Box content by name, description, tags, file content, and metadata. Supports filtering by file extension, date ranges, size ranges, owner, and ancestor folder to narrow results. No data is modified. Use this to locate files without knowing their exact folder location.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("box.search", owner_id, args, account_id=account_id)
