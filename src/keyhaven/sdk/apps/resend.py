# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `resend` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ResendCancelEmailPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendCancelEmailHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendCancelEmailRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendCancelEmailPath
    header: ResendCancelEmailHeader = Field(default_factory=ResendCancelEmailHeader)


class ResendCreateApiKeyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendCreateApiKeyBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    permission: Literal["full_access", "sending_access"] | None = None
    domain_id: str | None = None


class ResendCreateApiKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendCreateApiKeyHeader = Field(default_factory=ResendCreateApiKeyHeader)
    body: ResendCreateApiKeyBody


class ResendCreateAudienceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendCreateAudienceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str


class ResendCreateAudienceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendCreateAudienceHeader = Field(default_factory=ResendCreateAudienceHeader)
    body: ResendCreateAudienceBody


class ResendCreateBroadcastHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendCreateBroadcastBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    audience_id: str
    from_: str = Field(alias="from")
    subject: str
    reply_to: Any | None = None
    html: str | None = None
    text: str | None = None
    name: str | None = None


class ResendCreateBroadcastRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendCreateBroadcastHeader = Field(default_factory=ResendCreateBroadcastHeader)
    body: ResendCreateBroadcastBody


class ResendCreateContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    audience_id: str


class ResendCreateContactHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendCreateContactBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str
    first_name: str | None = None
    last_name: str | None = None
    unsubscribed: bool | None = None


class ResendCreateContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendCreateContactPath
    header: ResendCreateContactHeader = Field(default_factory=ResendCreateContactHeader)
    body: ResendCreateContactBody


class ResendCreateDomainHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendCreateDomainBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    region: str | None = None


class ResendCreateDomainRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendCreateDomainHeader = Field(default_factory=ResendCreateDomainHeader)
    body: ResendCreateDomainBody


class ResendDeleteApiKeyPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    api_key_id: str


class ResendDeleteApiKeyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendDeleteApiKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendDeleteApiKeyPath
    header: ResendDeleteApiKeyHeader = Field(default_factory=ResendDeleteApiKeyHeader)


class ResendDeleteAudiencePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendDeleteAudienceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendDeleteAudienceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendDeleteAudiencePath
    header: ResendDeleteAudienceHeader = Field(default_factory=ResendDeleteAudienceHeader)


class ResendDeleteBroadcastPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendDeleteBroadcastHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendDeleteBroadcastRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendDeleteBroadcastPath
    header: ResendDeleteBroadcastHeader = Field(default_factory=ResendDeleteBroadcastHeader)


class ResendDeleteContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    audience_id: str
    id: str


class ResendDeleteContactHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendDeleteContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendDeleteContactPath
    header: ResendDeleteContactHeader = Field(default_factory=ResendDeleteContactHeader)


class ResendDeleteDomainPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendDeleteDomainHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendDeleteDomainRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendDeleteDomainPath
    header: ResendDeleteDomainHeader = Field(default_factory=ResendDeleteDomainHeader)


class ResendListApiKeysHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendListApiKeysRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendListApiKeysHeader = Field(default_factory=ResendListApiKeysHeader)


class ResendListAudiencesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendListAudiencesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendListAudiencesHeader = Field(default_factory=ResendListAudiencesHeader)


class ResendListBroadcastsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendListBroadcastsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendListBroadcastsHeader = Field(default_factory=ResendListBroadcastsHeader)


class ResendListContactsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    audience_id: str


class ResendListContactsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendListContactsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendListContactsPath
    header: ResendListContactsHeader = Field(default_factory=ResendListContactsHeader)


class ResendListDomainsHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendListDomainsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendListDomainsHeader = Field(default_factory=ResendListDomainsHeader)


class ResendRetrieveAudiencePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendRetrieveAudienceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendRetrieveAudienceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendRetrieveAudiencePath
    header: ResendRetrieveAudienceHeader = Field(default_factory=ResendRetrieveAudienceHeader)


class ResendRetrieveBroadcastPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendRetrieveBroadcastHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendRetrieveBroadcastRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendRetrieveBroadcastPath
    header: ResendRetrieveBroadcastHeader = Field(default_factory=ResendRetrieveBroadcastHeader)


class ResendRetrieveContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    audience_id: str
    id: str


class ResendRetrieveContactHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendRetrieveContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendRetrieveContactPath
    header: ResendRetrieveContactHeader = Field(default_factory=ResendRetrieveContactHeader)


class ResendRetrieveDomainPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendRetrieveDomainHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendRetrieveDomainRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendRetrieveDomainPath
    header: ResendRetrieveDomainHeader = Field(default_factory=ResendRetrieveDomainHeader)


class ResendRetriveEmailPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendRetriveEmailHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendRetriveEmailRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendRetriveEmailPath
    header: ResendRetriveEmailHeader = Field(default_factory=ResendRetriveEmailHeader)


class ResendSendBatchEmailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: dict[str, Any]
    body: list[Any]


class ResendSendBroadcastPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendSendBroadcastHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendSendBroadcastBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    scheduled_at: str | None = None


class ResendSendBroadcastRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendSendBroadcastPath
    header: ResendSendBroadcastHeader = Field(default_factory=ResendSendBroadcastHeader)
    body: ResendSendBroadcastBody | None = None


class ResendSendEmailHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendSendEmailBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: str = Field(alias="from")
    to: list[Any]
    subject: str
    bcc: list[Any] | None = None
    cc: list[Any] | None = None
    scheduled_at: str | None = None
    reply_to: Any | None = None
    html: str | None = None
    text: str | None = None
    tags: list[Any] | None = None


class ResendSendEmailRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: ResendSendEmailHeader = Field(default_factory=ResendSendEmailHeader)
    body: ResendSendEmailBody


class ResendUpdateContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    audience_id: str
    id: str


class ResendUpdateContactHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendUpdateContactBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    first_name: str | None = None
    last_name: str | None = None
    unsubscribed: bool | None = None


class ResendUpdateContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendUpdateContactPath
    header: ResendUpdateContactHeader = Field(default_factory=ResendUpdateContactHeader)
    body: ResendUpdateContactBody = Field(default_factory=ResendUpdateContactBody)


class ResendUpdateDomainPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendUpdateDomainHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendUpdateDomainBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    click_tracking: bool | None = None
    open_tracking: bool | None = None


class ResendUpdateDomainRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendUpdateDomainPath
    header: ResendUpdateDomainHeader = Field(default_factory=ResendUpdateDomainHeader)
    body: ResendUpdateDomainBody = Field(default_factory=ResendUpdateDomainBody)


class ResendUpdateEmailPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendUpdateEmailHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendUpdateEmailBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    scheduled_at: str


class ResendUpdateEmailRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendUpdateEmailPath
    header: ResendUpdateEmailHeader = Field(default_factory=ResendUpdateEmailHeader)
    body: ResendUpdateEmailBody


class ResendVerifyDomainPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class ResendVerifyDomainHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class ResendVerifyDomainRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ResendVerifyDomainPath
    header: ResendVerifyDomainHeader = Field(default_factory=ResendVerifyDomainHeader)


class ResendNamespace:
    """Typed accessor for the `resend` app. Reach it via `client.apps.resend`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def cancel_email(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendCancelEmailRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Cancels a scheduled email by its ID, permanently preventing it from being delivered. Only works on emails that were created with a future scheduled_at time; already-sent emails cannot be cancelled. The cancellation is irreversible and the email will never be sent. Use when a scheduled email should be aborted because the triggering event or condition was undone.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.cancel_email", owner_id, args, account_id=account_id
        )

    async def create_api_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendCreateApiKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new API key for authenticating with the Resend API, with a configurable name, permission level (full_access or sending_access), and optional domain restriction. The key value is only returned once at creation time and cannot be retrieved later. Full_access keys authorize all API operations; sending_access keys are limited to email sending. Use when provisioning credentials for a new service, integration, or automated workflow.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.create_api_key", owner_id, args, account_id=account_id
        )

    async def create_audience(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendCreateAudienceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new audience (contact list) on your Resend account for organizing and grouping contacts. Only a name is required; the audience appears immediately with zero contacts. Audiences are the target for broadcast email campaigns. Use when segmenting your contact base for newsletter or marketing campaigns.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.create_audience", owner_id, args, account_id=account_id
        )

    async def create_broadcast(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendCreateBroadcastRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a draft broadcast (mass email campaign) for a target audience, specifying the sender, subject, and content (HTML and/or text). The broadcast is not sent immediately — it remains in draft status until SEND_BROADCAST is called. Use when preparing a newsletter, announcement, or marketing campaign to be sent to an entire audience later.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.create_broadcast", owner_id, args, account_id=account_id
        )

    async def create_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendCreateContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new contact to a specified audience by email address, with optional first name, last name, and unsubscribed status. If the email already exists in the audience, the contact data is updated instead of creating a duplicate. Use when building a contact list for newsletters or importing subscribers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.create_contact", owner_id, args, account_id=account_id
        )

    async def create_domain(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendCreateDomainRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Registers a new sending domain on your Resend account for use as a from-address domain. After creation, you must configure DNS records (SPF, DKIM, DMARC) at your DNS provider and verify the domain before you can reliably send from it. The domain appears in your dashboard immediately with a pending status. Use when onboarding a new domain for sending emails through Resend.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.create_domain", owner_id, args, account_id=account_id
        )

    async def delete_api_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendDeleteApiKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes an API key from your Resend account by its ID. Any service, script, or integration using this key will immediately lose access to the Resend API. This action is irreversible. Use when rotating credentials, revoking access for a compromised key, or removing unused keys.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.delete_api_key", owner_id, args, account_id=account_id
        )

    async def delete_audience(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendDeleteAudienceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently deletes an audience and all contacts within it from your Resend account. All contact data associated with this audience is lost. Any broadcasts targeting this audience will have no recipients after deletion. This action is irreversible. Use when removing an obsolete or duplicate contact list.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.delete_audience", owner_id, args, account_id=account_id
        )

    async def delete_broadcast(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendDeleteBroadcastRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a broadcast from your Resend account by its ID. If the broadcast has not been sent yet, it is simply deleted as a draft. If it was already sent, the broadcast record is removed but the sent emails are not affected. This action is irreversible. Use when cleaning up obsolete draft campaigns.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.delete_broadcast", owner_id, args, account_id=account_id
        )

    async def delete_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendDeleteContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a single contact from an audience by their ID. The contact will no longer receive broadcast emails sent to this audience. This action is irreversible, though the contact can be re-added. Use when removing an individual from a contact list for unsubscribe requests, bounces, or duplicate cleanup.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.delete_contact", owner_id, args, account_id=account_id
        )

    async def delete_domain(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendDeleteDomainRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a sending domain from your Resend account. After deletion, you can no longer send emails from addresses on that domain through Resend, and any scheduled emails using this domain may fail. This action is irreversible and requires re-adding and re-verifying the domain to restore sending. Use when decommissioning a domain or cleaning up unused sending domains.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.delete_domain", owner_id, args, account_id=account_id
        )

    async def list_api_keys(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendListApiKeysRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all API keys associated with your Resend account, showing metadata such as name, ID, permission level, and creation date. Key secret values are not included in the response. No state is changed. Use when auditing existing credentials or finding a key ID for deletion.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.list_api_keys", owner_id, args, account_id=account_id
        )

    async def list_audiences(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendListAudiencesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a list of all audiences in your Resend account, showing each audience ID, name, and creation date. No state is changed. Use when browsing your contact lists or finding an audience ID for subsequent contact management or broadcast operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.list_audiences", owner_id, args, account_id=account_id
        )

    async def list_broadcasts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendListBroadcastsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a list of all broadcasts on your Resend account, including each broadcast ID, name, status (draft or sent), audience, and creation date. No state is changed. Use when reviewing campaign history or finding a broadcast ID for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.list_broadcasts", owner_id, args, account_id=account_id
        )

    async def list_contacts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendListContactsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a list of all contacts within a specific audience, showing each contact ID, email, name, and subscription status. No state is changed. Use when browsing the contacts in an audience or finding a contact ID for subsequent operations like update or delete.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.list_contacts", owner_id, args, account_id=account_id
        )

    async def list_domains(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendListDomainsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a list of all sending domains registered on your Resend account, including each domain name, verification status, creation date, and tracking settings. No state is changed. Use when you need an overview of configured domains or to find a domain ID for subsequent operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.list_domains", owner_id, args, account_id=account_id
        )

    async def retrieve_audience(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendRetrieveAudienceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches details of a single audience by its ID, including its name, creation date, and contact count. No state is changed. Use when inspecting an audience metadata or verifying it exists before adding contacts or creating a broadcast for it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.retrieve_audience", owner_id, args, account_id=account_id
        )

    async def retrieve_broadcast(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendRetrieveBroadcastRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches details of a single broadcast by its ID, including its name, status (draft or sent), targeted audience, sender, subject, and timestamps. No state is changed. Use when inspecting a broadcast configuration or checking whether it has already been sent.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.retrieve_broadcast", owner_id, args, account_id=account_id
        )

    async def retrieve_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendRetrieveContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches details of a single contact within an audience by its ID, including email address, first name, last name, and subscription status. No state is changed. Use when inspecting a specific contact data or verifying a contact exists before updating or deleting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.retrieve_contact", owner_id, args, account_id=account_id
        )

    async def retrieve_domain(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendRetrieveDomainRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches details of a single sending domain by its ID, including verification status, DNS record configuration, and tracking settings. No state is changed. Use when checking a domain verification progress or inspecting its current DNS and tracking configuration.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.retrieve_domain", owner_id, args, account_id=account_id
        )

    async def retrive_email(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendRetriveEmailRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves full details of a previously sent email by its ID, including delivery status, recipient, subject, timestamps, and any error information. No state is modified. Use when checking whether a specific email was delivered, inspecting send errors, or fetching metadata for audit or logging purposes.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.retrive_email", owner_id, args, account_id=account_id
        )

    async def send_batch_emails(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendSendBatchEmailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends up to 100 distinct emails in a single API call by providing an array of email objects, each with its own sender, recipient, subject, content, and scheduling options. Every recipient receives their email independently, and errors are reported per-item rather than failing the entire batch. Use when you need to deliver many personalized transactional emails at once more efficiently than calling SEND_EMAIL repeatedly.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.send_batch_emails", owner_id, args, account_id=account_id
        )

    async def send_broadcast(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendSendBroadcastRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Triggers delivery of a draft broadcast to its entire target audience. Every contact in the audience receives the email immediately (or at the optional scheduled time). Once sent, the broadcast cannot be unsent or recalled. Use when you are ready to launch a campaign — this action dispatches emails to potentially thousands of recipients at once.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.send_broadcast", owner_id, args, account_id=account_id
        )

    async def send_email(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendSendEmailRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sends a single email via the Resend API with full control over sender, recipient, subject, HTML/text content, scheduling, reply-to, tags, CC, and BCC. Recipients receive the email in their inbox immediately (or at the scheduled time if scheduled_at is set). Use when delivering a transactional or notification email to one or more recipients.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.send_email", owner_id, args, account_id=account_id
        )

    async def update_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendUpdateContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates a contact first name, last name, or unsubscribed status within a specific audience. The contact email address cannot be changed. Changes are reflected immediately and affect whether the contact receives future broadcasts. Use when correcting contact details or managing individual subscription preferences.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.update_contact", owner_id, args, account_id=account_id
        )

    async def update_domain(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendUpdateDomainRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Toggles click tracking and/or open tracking for a verified sending domain. Enabling adds tracking pixels and redirect links to emails sent from this domain; disabling removes them. Changes apply globally to all future emails from this domain. Use when you need to enable or disable engagement analytics for a specific sending domain.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.update_domain", owner_id, args, account_id=account_id
        )

    async def update_email(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendUpdateEmailRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates the scheduled send time of a not-yet-sent email identified by its ID. Only the scheduled_at field can be changed, and this only applies to emails originally created with a future send time. The email remains scheduled with the new time; no email is actually sent by this call. Use when you need to postpone or reschedule a pending transactional email.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.update_email", owner_id, args, account_id=account_id
        )

    async def verify_domain(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ResendVerifyDomainRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Triggers an immediate verification check for a domain by its ID. Resend re-reads the DNS records (SPF, DKIM, DMARC) published at your DNS provider and updates the domain status to verified if everything is correctly configured. This does not modify DNS records — it only validates what is already published. Use after adding or updating DNS records to confirm Resend recognizes them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "resend.verify_domain", owner_id, args, account_id=account_id
        )
