# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `all_images` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AllImagesCheckApiKeyHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class AllImagesCheckApiKeyRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AllImagesCheckApiKeyHeader = Field(default_factory=AllImagesCheckApiKeyHeader)


class AllImagesCreateGenerationHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"
    content_type: str = Field(default="application/json", alias="content-type")


class AllImagesCreateGenerationBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    mode: Literal["simple", "advanced"] = "simple"
    prompt: str
    additionalPrompt: str | None = None
    optimizePrompt: bool = False
    processMode: Literal["relax", "fast"] = "fast"
    tags: list[Any] | None = None
    params: list[Any] | None = None


class AllImagesCreateGenerationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AllImagesCreateGenerationHeader = Field(default_factory=AllImagesCreateGenerationHeader)
    body: AllImagesCreateGenerationBody


class AllImagesDownloadImageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"
    content_type: str = Field(default="application/json", alias="content-type")


class AllImagesDownloadImageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class AllImagesDownloadImageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AllImagesDownloadImageHeader = Field(default_factory=AllImagesDownloadImageHeader)
    body: AllImagesDownloadImageBody


class AllImagesGetGenerationPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    generation_id: str


class AllImagesGetGenerationHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"


class AllImagesGetGenerationRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AllImagesGetGenerationPath
    header: AllImagesGetGenerationHeader = Field(default_factory=AllImagesGetGenerationHeader)


class AllImagesSearchImagesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    accept: str = "application/json"
    content_type: str = Field(default="application/json", alias="content-type")


class AllImagesSearchImagesBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    search: str
    limit: int = 10
    offset: int = 0
    filterFree: bool = False
    sort: str | None = None


class AllImagesSearchImagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: AllImagesSearchImagesHeader = Field(default_factory=AllImagesSearchImagesHeader)
    body: AllImagesSearchImagesBody


class AllImagesNamespace:
    """Typed accessor for the `all_images` app. Reach it via `client.apps.all_images`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def check_api_key(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AllImagesCheckApiKeyRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Validates the provided API key against the All-Images.ai service to confirm it is active and authorized. Returns a simple validity status with no side effects on account data. Use this before any authenticated call to verify credentials are still valid.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "all_images.check_api_key", owner_id, args, account_id=account_id
        )

    async def create_generation(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AllImagesCreateGenerationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Submits a new AI image generation task to All-Images.ai with a text prompt and optional style parameters (mode, camera, format, angle, etc.) and organizational tags. The task is queued for processing on the server and will consume generation credits upon completion. Use this to produce custom AI imagery — review the prompt carefully before submitting as the action consumes quota and is not reversible.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "all_images.create_generation", owner_id, args, account_id=account_id
        )

    async def download_image(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AllImagesDownloadImageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Downloads the full image file for a given image ID from the All-Images.ai library. Returns the binary image data or a temporary download URL for use in your application. Use this after searching for images — the download may consume a credit or count toward your usage plan.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "all_images.download_image", owner_id, args, account_id=account_id
        )

    async def get_generation(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AllImagesGetGenerationRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the current status and final result of a previously submitted AI image generation task by its generation ID. Returns progress information while processing and the generated image URLs once complete. Use this to poll for completion after submitting a CREATE_GENERATION request — it is a read-only status check with no side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "all_images.get_generation", owner_id, args, account_id=account_id
        )

    async def search_images(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AllImagesSearchImagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches the All-Images.ai stock image library using a text query with optional filters for free-only images, sorting, and pagination controls. Returns a list of matching image metadata including IDs, preview URLs, and licensing information. Use this to discover images available for download — the search itself does not consume credits or modify any data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "all_images.search_images", owner_id, args, account_id=account_id
        )
