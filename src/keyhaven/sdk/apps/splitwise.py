# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `splitwise` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SplitwiseCurrentUserGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SplitwiseExpenseCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    cost: str
    description: str
    details: str | None = None
    date: str | None = None
    currency_code: str | None = None
    category_id: int | None = None
    group_id: int | None = None
    split_equally: bool | None = None
    users: list[Any] | None = None
    repeat_interval: str | None = None


class SplitwiseExpenseCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SplitwiseExpenseCreateBody


class SplitwiseExpensesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    group_id: int | None = None
    friend_id: int | None = None
    dated_after: str | None = None
    dated_before: str | None = None
    updated_after: str | None = None
    updated_before: str | None = None
    limit: int = 20
    offset: int = 0


class SplitwiseExpensesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SplitwiseExpensesListQuery | None = None


class SplitwiseFriendsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SplitwiseGroupGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class SplitwiseGroupGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SplitwiseGroupGetPath


class SplitwiseGroupsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class SplitwiseNamespace:
    """Typed accessor for the `splitwise` app. Reach it via `client.apps.splitwise`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def current_user_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SplitwiseCurrentUserGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve profile information about the currently authenticated Splitwise user, including name, email, default currency, and registration date. Use this to confirm the authenticated identity or to obtain the user ID needed for other API calls.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "splitwise.current_user_get", owner_id, args, account_id=account_id
        )

    async def expense_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SplitwiseExpenseCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new expense in Splitwise with a cost, description, and distribution among group members or friends. The expense can be split equally or assigned with custom per-user shares. Use this to record a shared purchase or bill; once created, the expense becomes visible to all participants and affects their balances immediately.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "splitwise.expense_create", owner_id, args, account_id=account_id
        )

    async def expenses_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SplitwiseExpensesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve an optionally-filtered, paginated list of expenses visible to the authenticated user. Filters include group, friend, date range, and update time range. Each expense shows the cost, description, shares, and payment status. Use this to review financial activity before reconciling or creating new expenses; the data reveals who owes whom.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "splitwise.expenses_list", owner_id, args, account_id=account_id
        )

    async def friends_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SplitwiseFriendsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve all friends of the authenticated Splitwise user, including their names, emails, and current balance. Friends represent direct peer relationships outside of groups. Use this to find a friend's ID before creating a non-group expense with them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "splitwise.friends_list", owner_id, args, account_id=account_id
        )

    async def group_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SplitwiseGroupGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve detailed information about a single Splitwise group, including its members, their balances, and any simplified debts. Use this to inspect a group's financial state before creating new expenses or settling debts among members.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "splitwise.group_get", owner_id, args, account_id=account_id
        )

    async def groups_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SplitwiseGroupsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch all groups that the authenticated user belongs to, including each group's name, member list, and total balance summary. Groups are the primary container for shared expenses in Splitwise. Use this to discover available groups and obtain their IDs before creating or viewing expenses.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "splitwise.groups_list", owner_id, args, account_id=account_id
        )
