# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `notion` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class NotionAppendBlockChildrenPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    block_id: str


class NotionAppendBlockChildrenHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionAppendBlockChildrenBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    children: list[Any]


class NotionAppendBlockChildrenRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionAppendBlockChildrenPath
    header: NotionAppendBlockChildrenHeader = Field(default_factory=NotionAppendBlockChildrenHeader)
    body: NotionAppendBlockChildrenBody


class NotionCreatePageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionCreatePageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    parent: dict[str, Any]
    properties: dict[str, Any]
    children: list[Any] | None = None
    icon: dict[str, Any] | None = None
    cover: dict[str, Any] | None = None


class NotionCreatePageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: NotionCreatePageHeader = Field(default_factory=NotionCreatePageHeader)
    body: NotionCreatePageBody


class NotionGetBlockChildrenPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    block_id: str


class NotionGetBlockChildrenQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_size: int | None = None
    start_cursor: str | None = None


class NotionGetBlockChildrenHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionGetBlockChildrenRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionGetBlockChildrenPath
    query: NotionGetBlockChildrenQuery | None = None
    header: NotionGetBlockChildrenHeader = Field(default_factory=NotionGetBlockChildrenHeader)


class NotionGetDatabasePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    database_id: str


class NotionGetDatabaseHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionGetDatabaseRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionGetDatabasePath
    header: NotionGetDatabaseHeader = Field(default_factory=NotionGetDatabaseHeader)


class NotionGetPagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_id: str


class NotionGetPageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionGetPageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionGetPagePath
    header: NotionGetPageHeader = Field(default_factory=NotionGetPageHeader)


class NotionQueryDatabasePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    database_id: str


class NotionQueryDatabaseHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionQueryDatabaseBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: dict[str, Any] | None = None
    sorts: list[Any] | None = None
    page_size: int | None = None
    start_cursor: str | None = None


class NotionQueryDatabaseRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionQueryDatabasePath
    header: NotionQueryDatabaseHeader = Field(default_factory=NotionQueryDatabaseHeader)
    body: NotionQueryDatabaseBody | None = None


class NotionSearchPagesHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionSearchPagesBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str | None = None
    page_size: int | None = None
    filter: dict[str, Any] | None = None
    sort: dict[str, Any] | None = None
    start_cursor: str | None = None


class NotionSearchPagesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: NotionSearchPagesHeader = Field(default_factory=NotionSearchPagesHeader)
    body: NotionSearchPagesBody | None = None


class NotionUpdatePagePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page_id: str


class NotionUpdatePageHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Notion_Version: str = Field(default="2022-06-28", alias="Notion-Version")


class NotionUpdatePageBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    properties: dict[str, Any] | None = None
    archived: bool | None = None


class NotionUpdatePageRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: NotionUpdatePagePath
    header: NotionUpdatePageHeader = Field(default_factory=NotionUpdatePageHeader)
    body: NotionUpdatePageBody | None = None


class NotionNamespace:
    """Typed accessor for the `notion` app. Reach it via `client.apps.notion`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def append_block_children(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionAppendBlockChildrenRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Append new block elements (such as paragraphs, headings, bullet lists, images, or code blocks) as children of an existing block or page. The new content is immediately visible to anyone who can view the page. Use this when an agent needs to add content to a Notion page, for example writing meeting notes, appending code snippets, or adding structured information without modifying existing blocks.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion.append_block_children", owner_id, args, account_id=account_id
        )

    async def create_page(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionCreatePageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new page under a specified parent page or database, with optional content blocks, icon, and cover image. The page is immediately visible to anyone with access to the parent workspace and triggers any associated database automations. Use this when an agent needs to add structured content to a Notion workspace, such as meeting notes, task entries, or database records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion.create_page", owner_id, args, account_id=account_id
        )

    async def get_block_children(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionGetBlockChildrenRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List the child block elements such as paragraphs, headings, bullet lists, toggles, images, and code blocks contained within a parent block or page. Each block type, content, and metadata are returned. Use this when an agent needs to read the actual body content of a Notion page or explore a nested block structure.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion.get_block_children", owner_id, args, account_id=account_id
        )

    async def get_database(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionGetDatabaseRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the schema definition and metadata of a Notion database, including all property definitions (such as title, select, date, and relation), database title, and description. The response exposes the database structure without querying its rows. Use this when an agent needs to understand a database schema before constructing a filtered query or creating entries with the correct property types.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion.get_database", owner_id, args, account_id=account_id
        )

    async def get_page(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionGetPageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve the full properties and metadata of a specific Notion page by its unique identifier. The response includes the page title, all property values, and object metadata such as creation and last-edited times. Use this when an agent needs to inspect a page current state before deciding whether to update or archive it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("notion.get_page", owner_id, args, account_id=account_id)

    async def query_database(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionQueryDatabaseRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Query the rows (pages) in a database using optional filters, property-value sorting, and pagination. Results include all property values for every matching row. Use this when an agent needs to find specific database records, extract structured data for reporting, or retrieve entries matching certain criteria before updating or analyzing them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion.query_database", owner_id, args, account_id=account_id
        )

    async def search_pages(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionSearchPagesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search for pages and databases that the integration can access, based on a text query against their titles. Results include matching page and database metadata with object type and last-edited timestamp. Use this when an agent needs to locate a specific Notion page or database before performing further operations like reading, updating, or creating content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion.search_pages", owner_id, args, account_id=account_id
        )

    async def update_page(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: NotionUpdatePageRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modify a page property values such as title, status, or custom database fields, or change its archived state to hide or restore it. Archiving removes the page from most views but is fully reversible via the same endpoint. Use this when an agent needs to update task statuses, edit metadata, or archive or restore a page in a workspace.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "notion.update_page", owner_id, args, account_id=account_id
        )
