# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `stripe_api_key` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class StripeApiKeyCustomersCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeApiKeyCustomersCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    name: str | None = None
    phone: str | None = None
    description: str | None = None


class StripeApiKeyCustomersCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeApiKeyCustomersCreateHeader = Field(
        default_factory=StripeApiKeyCustomersCreateHeader
    )
    body: StripeApiKeyCustomersCreateBody = Field(default_factory=StripeApiKeyCustomersCreateBody)


class StripeApiKeyCustomersGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str


class StripeApiKeyCustomersGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StripeApiKeyCustomersGetPath


class StripeApiKeyCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    created: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeApiKeyCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeApiKeyCustomersListQuery | None = None


class StripeApiKeyPaymentIntentsCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeApiKeyPaymentIntentsCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    amount: int
    currency: str
    customer: str | None = None
    description: str | None = None
    automatic_payment_methods_enabled_: bool = Field(
        default=True, alias="automatic_payment_methods[enabled]"
    )
    confirmation_method: Literal["automatic", "manual"] = "automatic"


class StripeApiKeyPaymentIntentsCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeApiKeyPaymentIntentsCreateHeader = Field(
        default_factory=StripeApiKeyPaymentIntentsCreateHeader
    )
    body: StripeApiKeyPaymentIntentsCreateBody


class StripeApiKeyPaymentIntentsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    created: str | None = None
    customer: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeApiKeyPaymentIntentsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeApiKeyPaymentIntentsListQuery | None = None


class StripeApiKeySubscriptionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str | None = None
    status: (
        Literal[
            "incomplete",
            "incomplete_expired",
            "trialing",
            "active",
            "past_due",
            "canceled",
            "unpaid",
            "paused",
            "all",
        ]
        | None
    ) = None
    price: str | None = None
    created: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeApiKeySubscriptionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeApiKeySubscriptionsListQuery | None = None


class StripeApiKeyNamespace:
    """Typed accessor for the `stripe_api_key` app. Reach it via `client.apps.stripe_api_key`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customers_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeApiKeyCustomersCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer object in Stripe with an optional email, name, phone, and description. The customer appears in the Stripe Dashboard and can be associated with payments, subscriptions, and invoices. Use this when onboarding a new user to attach future transactions to a persistent profile. The operation is reversible by deleting the customer or marking it for deletion.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe_api_key.customers_create", owner_id, args, account_id=account_id
        )

    async def customers_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeApiKeyCustomersGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single Stripe customer by their unique customer ID (cus_xxx). Returns comprehensive profile data including email, name, phone, address, shipping info, default payment method, currency, and metadata. Use this to inspect a customer's complete profile before taking actions like creating a subscription or invoice. The response contains sensitive PII and payment configuration details.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe_api_key.customers_get", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeApiKeyCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of Stripe customers with optional filtering by exact email match, creation date range, or test clock. Each customer record includes email, name, phone, shipping info, and payment method metadata. Use this to search for existing customers before creating duplicates, or to gather customer analytics. The response contains personally identifiable information and should be handled carefully.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe_api_key.customers_list", owner_id, args, account_id=account_id
        )

    async def payment_intents_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeApiKeyPaymentIntentsCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a PaymentIntent that tracks and handles a customer payment lifecycle. Requires an amount (in the smallest currency unit) and a currency code. With automatic confirmation (the default), Stripe will immediately attempt to charge the provided payment method. Use this to accept one-time payments from customers. This action directly involves charging money and is irreversible once the payment succeeds.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe_api_key.payment_intents_create", owner_id, args, account_id=account_id
        )

    async def payment_intents_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeApiKeyPaymentIntentsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a cursor-paginated list of PaymentIntents, filterable by customer ID, creation date range, or amount. Each PaymentIntent shows its current status (requires_payment_method, succeeded, canceled, etc.), amount, currency, and payment method used. Use this to review payment history for a specific customer or to monitor recent transactions. Read-only but exposes financial transaction data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe_api_key.payment_intents_list", owner_id, args, account_id=account_id
        )

    async def subscriptions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeApiKeySubscriptionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists Stripe subscriptions sorted by creation date (most recent first), with filtering by customer, status (active, canceled, past_due, etc.), price ID, and creation date. Each subscription shows its current period, plan details, collection method, and pending updates. Use this to audit active subscriptions, find canceled ones, or check the billing status of a customer. Read-only but exposes recurring revenue and billing data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe_api_key.subscriptions_list", owner_id, args, account_id=account_id
        )
