# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `fortnox` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FortnoxCustomerCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Customer: dict[str, Any]


class FortnoxCustomerCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: FortnoxCustomerCreateBody


class FortnoxCustomerGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    CustomerId: str


class FortnoxCustomerGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FortnoxCustomerGetPath


class FortnoxCustomerUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    CustomerId: str


class FortnoxCustomerUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Customer: dict[str, Any]


class FortnoxCustomerUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FortnoxCustomerUpdatePath
    body: FortnoxCustomerUpdateBody


class FortnoxCustomersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = None
    name: str | None = None
    city: str | None = None
    email: str | None = None
    sortby: str | None = None
    sortorder: str | None = None
    limit: int | None = None
    page: int | None = None
    lastmodified: str | None = None


class FortnoxCustomersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FortnoxCustomersListQuery | None = None


class FortnoxInvoiceCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Invoice: dict[str, Any]


class FortnoxInvoiceCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: FortnoxInvoiceCreateBody


class FortnoxInvoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    filter: str | None = None
    customername: str | None = None
    fromdate: str | None = None
    todate: str | None = None
    sortby: str | None = None
    limit: int | None = None
    page: int | None = None
    lastmodified: str | None = None


class FortnoxInvoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FortnoxInvoicesListQuery | None = None


class FortnoxNamespace:
    """Typed accessor for the `fortnox` app. Reach it via `client.apps.fortnox`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def customer_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FortnoxCustomerCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer record in Fortnox with fields such as name, organization number, email, phone, address, currency, and VAT type. The new customer becomes available immediately for invoicing and other transactions. This action is reversible by deleting or deactivating the customer. Use it when onboarding a new client, importing contacts, or setting up a one-time customer for an invoice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fortnox.customer_create", owner_id, args, account_id=account_id
        )

    async def customer_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FortnoxCustomerGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single Fortnox customer by its customer number, including address, contact info, VAT type, currency, and payment terms. This read-only operation does not alter any data in Fortnox. Use it when you need to inspect a specific customer's configuration before creating an invoice, verify contact details, or display customer information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fortnox.customer_get", owner_id, args, account_id=account_id
        )

    async def customer_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FortnoxCustomerUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more fields of an existing Fortnox customer record identified by its customer number. Changes take effect immediately and affect future invoices, orders, and documents referencing this customer. This action is reversible by updating the fields back to their previous values. Use it when correcting customer contact info, changing payment terms, or updating tax settings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fortnox.customer_update", owner_id, args, account_id=account_id
        )

    async def customers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FortnoxCustomersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of customer records from Fortnox, with optional filtering by name, city, email, status, and last-modified timestamp. You can sort results and control page size (up to 500 per page). This is a read-only query that does not create or modify any data in Fortnox. Use it when you need to search for customers, populate a customer selector, or sync customer data to another system.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fortnox.customers_list", owner_id, args, account_id=account_id
        )

    async def invoice_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FortnoxInvoiceCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new invoice in Fortnox with customer details, invoice date, due date, currency, and invoice rows (articles with description, price, VAT, and discount). Once created, the invoice appears in Fortnox and can be sent to the customer, booked, or processed for payment. Use it when generating invoices for services rendered, recurring billing, or one-time sales. The invoice can be cancelled or credited if needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fortnox.invoice_create", owner_id, args, account_id=account_id
        )

    async def invoices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FortnoxInvoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of invoices from Fortnox, with optional filtering by status (cancelled, fully paid, unpaid, overdue, unbooked), customer name, date range, and last-modified timestamp. Results can be sorted by any field. This is a read-only query that does not modify any invoice data. Use it when reviewing accounts receivable, auditing invoice history, or syncing invoice data to external reporting tools.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fortnox.invoices_list", owner_id, args, account_id=account_id
        )
