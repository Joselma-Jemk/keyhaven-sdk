# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `exa_ai` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ExaAiAnswerBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    stream: bool = False
    text: bool = False
    model: Literal["exa", "exa-pro"] = "exa"


class ExaAiAnswerRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ExaAiAnswerBody


class ExaAiFindsimilarBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    url: str
    numResults: int = 10
    includeDomains: list[Any] | None = None
    excludeDomains: list[Any] | None = None
    startCrawlDate: str | None = None
    endCrawlDate: str | None = None
    startPublishedDate: str | None = None
    endPublishedDate: str | None = None
    includeText: list[Any] | None = None
    excludeText: list[Any] | None = None
    contents: dict[str, Any] | None = None


class ExaAiFindsimilarRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ExaAiFindsimilarBody


class ExaAiGetContentsBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ids: list[Any]
    text: dict[str, Any] | None = None
    highlights: dict[str, Any] | None = None
    summary: dict[str, Any] | None = None


class ExaAiGetContentsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ExaAiGetContentsBody


class ExaAiSearchBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: str
    useAutoprompt: bool = False
    type_: Literal["keyword", "neural", "auto"] = Field(default="neural", alias="type")
    category: (
        Literal[
            "company",
            "research paper",
            "news",
            "linkedin profile",
            "github",
            "tweet",
            "movie",
            "song",
            "personal site",
            "pdf",
        ]
        | None
    ) = None
    numResults: int = 10
    includeDomains: list[Any] | None = None
    excludeDomains: list[Any] | None = None
    startCrawlDate: str | None = None
    endCrawlDate: str | None = None
    startPublishedDate: str | None = None
    endPublishedDate: str | None = None
    includeText: list[Any] | None = None
    excludeText: list[Any] | None = None
    contents: dict[str, Any] | None = None


class ExaAiSearchRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ExaAiSearchBody


class ExaAiNamespace:
    """Typed accessor for the `exa_ai` app. Reach it via `client.apps.exa_ai`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def answer(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ExaAiAnswerRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Answers a question by having an LLM synthesize information from Exa search results, returning a natural-language answer with citations. Supports streaming and configurable models (exa or exa-pro). Use when an agent needs a direct, cited answer to a factual question instead of raw search results.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("exa_ai.answer", owner_id, args, account_id=account_id)

    async def findsimilar(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ExaAiFindsimilarRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Finds web pages similar to a given URL using Exa's embedding-based similarity engine and returns matching URLs with optional text, highlights, or summaries, filterable by domain, date, and text constraints. Use when an agent wants to discover related content on the web starting from a known page.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "exa_ai.findsimilar", owner_id, args, account_id=account_id
        )

    async def get_contents(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ExaAiGetContentsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full text, highlights, or summaries for web documents previously discovered via search or findSimilar, identified by their Exa document IDs. Accepts an array of IDs and returns the parsed page content for each. Use when an agent needs to read the actual content of pages it has already found through search.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "exa_ai.get_contents", owner_id, args, account_id=account_id
        )

    async def search(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ExaAiSearchRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches the web using Exa's neural embedding, keyword, or auto search modes and returns matching URLs with optional text excerpts, highlights, or summaries. Supports filtering by domain, category (company, news, research paper, etc.), and date ranges. Use when an agent needs to find relevant web pages for research, fact-checking, or content discovery.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("exa_ai.search", owner_id, args, account_id=account_id)
