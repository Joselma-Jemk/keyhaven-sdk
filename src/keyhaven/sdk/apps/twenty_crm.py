# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `twenty_crm` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TwentyCrmCompaniesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 10
    offset: int = 0


class TwentyCrmCompaniesListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TwentyCrmCompaniesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwentyCrmCompaniesListQuery | None = None
    header: TwentyCrmCompaniesListHeader = Field(default_factory=TwentyCrmCompaniesListHeader)


class TwentyCrmCompanyCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TwentyCrmCompanyCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    domainName: str | None = None
    address: str | None = None
    city: str | None = None
    employees: int | None = None


class TwentyCrmCompanyCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: TwentyCrmCompanyCreateHeader = Field(default_factory=TwentyCrmCompanyCreateHeader)
    body: TwentyCrmCompanyCreateBody


class TwentyCrmPeopleListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 10
    offset: int = 0


class TwentyCrmPeopleListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TwentyCrmPeopleListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TwentyCrmPeopleListQuery | None = None
    header: TwentyCrmPeopleListHeader = Field(default_factory=TwentyCrmPeopleListHeader)


class TwentyCrmPersonCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TwentyCrmPersonCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    firstName: str
    lastName: str
    email: str
    phone: str | None = None
    jobTitle: str | None = None
    companyId: str | None = None


class TwentyCrmPersonCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: TwentyCrmPersonCreateHeader = Field(default_factory=TwentyCrmPersonCreateHeader)
    body: TwentyCrmPersonCreateBody


class TwentyCrmPersonGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class TwentyCrmPersonGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class TwentyCrmPersonGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TwentyCrmPersonGetPath
    header: TwentyCrmPersonGetHeader = Field(default_factory=TwentyCrmPersonGetHeader)


class TwentyCrmNamespace:
    """Typed accessor for the `twenty_crm` app. Reach it via `client.apps.twenty_crm`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def companies_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwentyCrmCompaniesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of company records from the Twenty CRM. Each record includes company name, domain name, address, city, and employee count. Use to browse your CRM's company directory, find an organization by name, or export company data for analysis. No side effects.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twenty_crm.companies_list", owner_id, args, account_id=account_id
        )

    async def company_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwentyCrmCompanyCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Add a new company (organization) record to the Twenty CRM. Requires a company name; optionally accepts domain name, street address, city, and employee count. Use when onboarding a new organization into your CRM from a lead source or manual entry. The record is stored in the CRM and can be linked to person records. Reversible by deleting the record.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twenty_crm.company_create", owner_id, args, account_id=account_id
        )

    async def people_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwentyCrmPeopleListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetch a paginated list of person records from the Twenty CRM. Each record contains contact information including name, email, phone, job title, and associated company. Use to browse, search, or export your CRM contacts for reporting, enrichment, or downstream workflows. Person records contain personal data such as email addresses and phone numbers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twenty_crm.people_list", owner_id, args, account_id=account_id
        )

    async def person_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwentyCrmPersonCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Add a new person (contact) record to the Twenty CRM. Requires a first name, last name, and email address; optionally accepts phone, job title, and associated company ID. Use when onboarding a new contact into your CRM from a form submission, lead capture, or bulk import. The record is stored in the CRM and becomes visible to all users with access to the people module. Reversible by deleting the record.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twenty_crm.person_create", owner_id, args, account_id=account_id
        )

    async def person_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TwentyCrmPersonGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a single person record from the Twenty CRM by its unique ID. Returns the full person data including name, contact details, job title, and company association. Use to look up a specific contact's information for display, verification, or enrichment purposes. The response contains personal data such as email addresses and phone numbers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "twenty_crm.person_get", owner_id, args, account_id=account_id
        )
