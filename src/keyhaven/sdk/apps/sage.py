# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `sage` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class SageContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contact: dict[str, Any]


class SageContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SageContactCreateBody


class SageContactGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class SageContactGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: SageContactGetPath


class SageContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    items_per_page: int = 20
    page: int = 1
    updated_or_created_since: str | None = None
    search: str | None = None
    contact_type_id: str | None = None


class SageContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SageContactsListQuery | None = None


class SageProductsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    items_per_page: int = 20
    page: int = 1
    active: bool | None = None
    search: str | None = None


class SageProductsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SageProductsListQuery | None = None


class SageSalesInvoiceCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    sales_invoice: dict[str, Any]


class SageSalesInvoiceCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: SageSalesInvoiceCreateBody


class SageSalesInvoicesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    items_per_page: int = 20
    page: int = 1
    updated_or_created_since: str | None = None
    contact_id: str | None = None
    from_date: str | None = None
    to_date: str | None = None
    status_id: str | None = None


class SageSalesInvoicesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: SageSalesInvoicesListQuery | None = None


class SageNamespace:
    """Typed accessor for the `sage` app. Reach it via `client.apps.sage`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SageContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact record in Sage Accounting, configured as either a customer or a supplier with address, contact person, and optional credit limit. The contact appears immediately in the accounting system and can be linked to invoices, quotes, and payments. Agents should use this when onboarding a new client or vendor. This action is reversible by deactivating or deleting the contact.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sage.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SageContactGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single contact (customer or supplier) including addresses, contact persons, credit limit, and communication information. Agents should use this to inspect a specific contact before issuing invoices, checking creditworthiness, or updating contact details. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("sage.contact_get", owner_id, args, account_id=account_id)

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SageContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of all contacts (both customers and suppliers) in the Sage Accounting account. Supports free-text search by name or reference, filtering by contact type, and incremental sync by modification date. Agents should use this to find a contact before performing downstream operations like invoice creation or credit checks. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sage.contacts_list", owner_id, args, account_id=account_id
        )

    async def products_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SageProductsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of products and services registered in the Sage accounting system, with optional search by name or item code and active state filter. Agents should use this to look up product information, prices, stock details, or ledger accounts before creating invoices. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sage.products_list", owner_id, args, account_id=account_id
        )

    async def sales_invoice_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SageSalesInvoiceCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new sales invoice in Sage Accounting with line items, customer reference, dates, and notes. The invoice appears in the accounting ledger and represents a legal claim for payment from the customer. Agents should use this to bill customers for goods or services rendered. This action creates a financial document with legal and accounting implications that is not easily reversed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sage.sales_invoice_create", owner_id, args, account_id=account_id
        )

    async def sales_invoices_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: SageSalesInvoicesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of sales invoices with optional filtering by contact, date range, status, and incremental sync timestamp. Agents should use this to search for existing invoices, check invoice statuses (paid, overdue, draft), or prepare financial reports. No system state is changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "sage.sales_invoices_list", owner_id, args, account_id=account_id
        )
