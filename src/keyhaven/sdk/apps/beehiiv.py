# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `beehiiv` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class BeehiivAutomationJourneyCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    publicationId: str
    automationId: str


class BeehiivAutomationJourneyCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    subscription_id: str | None = None


class BeehiivAutomationJourneyCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BeehiivAutomationJourneyCreatePath
    body: BeehiivAutomationJourneyCreateBody | None = None


class BeehiivPostsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    publicationId: str


class BeehiivPostsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 10
    page: int = 1
    status: str | None = None
    audience: str | None = None
    platform: str | None = None


class BeehiivPostsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BeehiivPostsListPath
    query: BeehiivPostsListQuery | None = None


class BeehiivSubscriptionCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    publicationId: str


class BeehiivSubscriptionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str
    reactivate_existing: bool | None = None
    send_welcome_email: bool | None = None
    utm_source: str | None = None
    utm_medium: str | None = None
    tier: str | None = None
    premium_tiers: list[Any] | None = None


class BeehiivSubscriptionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BeehiivSubscriptionCreatePath
    body: BeehiivSubscriptionCreateBody


class BeehiivSubscriptionGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    publicationId: str
    subscriptionId: str


class BeehiivSubscriptionGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BeehiivSubscriptionGetPath


class BeehiivSubscriptionsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    publicationId: str


class BeehiivSubscriptionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 10
    status: str | None = None
    email: str | None = None
    cursor: str | None = None
    tier: str | None = None


class BeehiivSubscriptionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: BeehiivSubscriptionsListPath
    query: BeehiivSubscriptionsListQuery | None = None


class BeehiivNamespace:
    """Typed accessor for the `beehiiv` app. Reach it via `client.apps.beehiiv`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def automation_journey_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BeehiivAutomationJourneyCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Enrolls an existing subscriber into an automated email journey or sequence configured in the publication. Once enrolled, the subscriber will receive the automated emails defined in that automation workflow (e.g. onboarding series, re-engagement campaign, or drip content). Use this to trigger time-sensitive communication flows for specific subscribers based on their behavior or segment. This action starts an automated outbound email sequence targeted at a real person.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "beehiiv.automation_journey_create", owner_id, args, account_id=account_id
        )

    async def posts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BeehiivPostsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all posts and articles in the publication with optional filters by status (draft/confirmed/archived), audience (free/premium/both), and platform (web/email/both). Use this to browse the content inventory, find a specific post by title, or check which content has been published. Read-only — no data is created, modified, or deleted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "beehiiv.posts_list", owner_id, args, account_id=account_id
        )

    async def subscription_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BeehiivSubscriptionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new email subscriber to the publication, optionally assigning a tier (free/premium) and triggering a welcome email. The subscriber will immediately begin receiving newsletters or premium content based on their tier. If the email previously unsubscribed, set reactivate_existing to true to re-subscribe them instead of leaving them inactive. Use this to grow the publication's audience or manually add known contacts. This action modifies the subscriber list and may send outbound communications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "beehiiv.subscription_create", owner_id, args, account_id=account_id
        )

    async def subscription_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BeehiivSubscriptionGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves detailed information about a single subscriber by their subscription ID, including email, status, tier, and signup metadata. Use this to inspect a specific subscriber's details before taking an action like enrolling them in an automation or changing their tier. Read-only — no data is modified.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "beehiiv.subscription_get", owner_id, args, account_id=account_id
        )

    async def subscriptions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: BeehiivSubscriptionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all subscribers of a Beehiiv publication with optional filters for status, email, tier, and pagination. Use this to browse the subscriber base, verify who is subscribed, or find a specific subscriber by email address. Read-only operation — no data is created, modified, or deleted.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "beehiiv.subscriptions_list", owner_id, args, account_id=account_id
        )
