# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `workable` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class WorkableCandidateCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class WorkableCandidateCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    firstname: str | None = None
    lastname: str | None = None
    email: str
    headline: str | None = None
    phone: str | None = None


class WorkableCandidateCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: WorkableCandidateCreateHeader = Field(default_factory=WorkableCandidateCreateHeader)
    body: WorkableCandidateCreateBody


class WorkableCandidateGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class WorkableCandidateGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class WorkableCandidateGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WorkableCandidateGetPath
    header: WorkableCandidateGetHeader = Field(default_factory=WorkableCandidateGetHeader)


class WorkableCandidatesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 50
    since_id: str | None = None
    updated_after: str | None = None


class WorkableCandidatesListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class WorkableCandidatesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WorkableCandidatesListQuery | None = None
    header: WorkableCandidatesListHeader = Field(default_factory=WorkableCandidatesListHeader)


class WorkableJobGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    shortcode: str


class WorkableJobGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class WorkableJobGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: WorkableJobGetPath
    header: WorkableJobGetHeader = Field(default_factory=WorkableJobGetHeader)


class WorkableJobsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    state: str | None = None
    limit: int = 50
    since_id: str | None = None
    created_after: str | None = None
    updated_after: str | None = None


class WorkableJobsListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class WorkableJobsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: WorkableJobsListQuery | None = None
    header: WorkableJobsListHeader = Field(default_factory=WorkableJobsListHeader)


class WorkableNamespace:
    """Typed accessor for the `workable` app. Reach it via `client.apps.workable`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def candidate_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WorkableCandidateCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new candidate record in Workable with the required name and email, plus optional headline, phone number, first name, and last name. The candidate is added to the account's talent pool and can later be associated with job applications or stages. Use when an agent needs to manually register a sourced candidate, referral, or external applicant in the ATS.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "workable.candidate_create", owner_id, args, account_id=account_id
        )

    async def candidate_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WorkableCandidateGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full profile of a specific candidate by their unique ID, including their name, email, headline, phone number, and associated job applications. This gives an agent complete visibility into a candidate's data within the ATS. Use when you need to review a specific candidate's information during screening, interview preparation, or candidate reporting.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "workable.candidate_get", owner_id, args, account_id=account_id
        )

    async def candidates_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WorkableCandidatesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of candidates in the Workable account, filterable by update recency and pagination cursor. Candidates represent people who have applied or been submitted for positions in the ATS. Use when you need to browse the applicant pool, find recently updated candidates, or extract candidate data for reporting and pipeline analysis.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "workable.candidates_list", owner_id, args, account_id=account_id
        )

    async def job_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WorkableJobGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a specific job posting identified by its unique shortcode, including description, requirements, department, employment type, and hiring team information. This gives an agent complete visibility into a single job's profile within the ATS. Use when you need to review or present full information about a specific job opening for screening, reporting, or candidate matching.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("workable.job_get", owner_id, args, account_id=account_id)

    async def jobs_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: WorkableJobsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all job postings for the Workable account, filterable by state (draft, published, archived, or closed), creation date, and last update date. This provides an agent with a view of the current recruitment pipeline and job statuses. Use when you need to enumerate open positions, review job statuses, or sync job data with external systems.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "workable.jobs_list", owner_id, args, account_id=account_id
        )
