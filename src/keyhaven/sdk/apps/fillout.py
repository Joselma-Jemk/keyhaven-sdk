# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `fillout` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FilloutFormGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str


class FilloutFormGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FilloutFormGetPath


class FilloutFormsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    sort: str | None = None


class FilloutFormsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: FilloutFormsListQuery | None = None


class FilloutSubmissionCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str


class FilloutSubmissionCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    answers: dict[str, Any]
    submittedAt: str | None = None


class FilloutSubmissionCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FilloutSubmissionCreatePath
    body: FilloutSubmissionCreateBody


class FilloutSubmissionGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str
    submissionId: str


class FilloutSubmissionGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FilloutSubmissionGetPath


class FilloutSubmissionsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    formId: str


class FilloutSubmissionsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    sort: str | None = None
    includePreview: bool | None = None


class FilloutSubmissionsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FilloutSubmissionsListPath
    query: FilloutSubmissionsListQuery | None = None


class FilloutNamespace:
    """Typed accessor for the `fillout` app. Reach it via `client.apps.fillout`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def form_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FilloutFormGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full configuration of a single form, including all questions, field types, placeholder text, and validation rules. Use this to inspect a form's structure before creating a submission, so you know which answer keys and value types are expected. Requires the form ID obtained from the list endpoint.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("fillout.form_get", owner_id, args, account_id=account_id)

    async def forms_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FilloutFormsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of all forms configured in the connected Fillout account, each including its ID, title, and metadata. Use this to discover available forms and their IDs before inspecting a specific form's questions or reading submissions. Supports limit and offset pagination to navigate large accounts.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fillout.forms_list", owner_id, args, account_id=account_id
        )

    async def submission_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FilloutSubmissionCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new submission by posting answers as key-value pairs to a specified form. The submitted data becomes visible in the Fillout dashboard and may trigger any webhooks, email notifications, or integrations configured on that form. Use this to programmatically submit form responses on behalf of a user. Because this adds real data that the form owner will see, be careful to send accurate and appropriate content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fillout.submission_create", owner_id, args, account_id=account_id
        )

    async def submission_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FilloutSubmissionGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the complete details of a single form submission, including all answers, status, timestamps, and metadata. Use this to read the full response data for auditing, reporting, or data export. Requires both the form ID and the submission ID (the latter obtainable from the submissions list endpoint).
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fillout.submission_get", owner_id, args, account_id=account_id
        )

    async def submissions_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FilloutSubmissionsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of responses submitted to a specific form, each with status, timestamps, and optionally a preview of the answers. Use this to audit completed submissions, find a particular submission by metadata, or count responses for reporting. Setting includePreview to true returns the first few answer values inline without fetching each submission individually.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "fillout.submissions_list", owner_id, args, account_id=account_id
        )
