# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `stripe` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class StripeAttachPaymentMethodPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    payment_method: str


class StripeAttachPaymentMethodHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeAttachPaymentMethodBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str


class StripeAttachPaymentMethodRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StripeAttachPaymentMethodPath
    header: StripeAttachPaymentMethodHeader = Field(default_factory=StripeAttachPaymentMethodHeader)
    body: StripeAttachPaymentMethodBody


class StripeCancelSubscriptionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class StripeCancelSubscriptionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StripeCancelSubscriptionPath


class StripeCreateCouponHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreateCouponBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    percent_off: float | None = None
    amount_off: int | None = None
    currency: str | None = None
    duration: Literal["forever", "once", "repeating"]
    name: str | None = None


class StripeCreateCouponRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreateCouponHeader = Field(default_factory=StripeCreateCouponHeader)
    body: StripeCreateCouponBody


class StripeCreateCustomerHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreateCustomerBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    name: str | None = None
    phone: str | None = None
    description: str | None = None


class StripeCreateCustomerRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreateCustomerHeader = Field(default_factory=StripeCreateCustomerHeader)
    body: StripeCreateCustomerBody = Field(default_factory=StripeCreateCustomerBody)


class StripeCreateInvoiceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreateInvoiceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str
    auto_advance: bool = True
    description: str | None = None


class StripeCreateInvoiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreateInvoiceHeader = Field(default_factory=StripeCreateInvoiceHeader)
    body: StripeCreateInvoiceBody


class StripeCreateInvoiceItemHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreateInvoiceItemBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str
    amount: int
    currency: str
    description: str | None = None


class StripeCreateInvoiceItemRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreateInvoiceItemHeader = Field(default_factory=StripeCreateInvoiceItemHeader)
    body: StripeCreateInvoiceItemBody


class StripeCreatePaymentLinkHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreatePaymentLinkBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    line_items_0__price_: str = Field(alias="line_items[0][price]")
    line_items_0__quantity_: int = Field(default=1, alias="line_items[0][quantity]")


class StripeCreatePaymentLinkRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreatePaymentLinkHeader = Field(default_factory=StripeCreatePaymentLinkHeader)
    body: StripeCreatePaymentLinkBody


class StripeCreatePaymentMethodHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreatePaymentMethodBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    type_: Literal["card"] = Field(default="card", alias="type")
    card_token_: str = Field(default="tok_visa", alias="card[token]")


class StripeCreatePaymentMethodRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreatePaymentMethodHeader = Field(default_factory=StripeCreatePaymentMethodHeader)
    body: StripeCreatePaymentMethodBody = Field(default_factory=StripeCreatePaymentMethodBody)


class StripeCreatePriceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreatePriceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    currency: str
    product: str
    unit_amount: int | None = None
    recurring: dict[str, Any] | None = None


class StripeCreatePriceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreatePriceHeader = Field(default_factory=StripeCreatePriceHeader)
    body: StripeCreatePriceBody


class StripeCreateProductHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreateProductBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    active: bool = True
    tax_code: str | None = None


class StripeCreateProductRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreateProductHeader = Field(default_factory=StripeCreateProductHeader)
    body: StripeCreateProductBody


class StripeCreateRefundHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreateRefundBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    charge: str | None = None
    payment_intent: str | None = None
    amount: int | None = None
    reason: Literal["duplicate", "fraudulent", "requested_by_customer"] | None = None


class StripeCreateRefundRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreateRefundHeader = Field(default_factory=StripeCreateRefundHeader)
    body: StripeCreateRefundBody = Field(default_factory=StripeCreateRefundBody)


class StripeCreateSubscriptionHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeCreateSubscriptionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str
    items_0__price_: str = Field(alias="items[0][price]")
    items_0__quantity_: int = Field(default=1, alias="items[0][quantity]")
    trial_period_days: int | None = None
    collection_method: Literal["charge_automatically", "send_invoice"] = "charge_automatically"


class StripeCreateSubscriptionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    header: StripeCreateSubscriptionHeader = Field(default_factory=StripeCreateSubscriptionHeader)
    body: StripeCreateSubscriptionBody


class StripeFinalizeInvoicePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    invoice: str


class StripeFinalizeInvoiceHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeFinalizeInvoiceBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    auto_advance: bool | None = None


class StripeFinalizeInvoiceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StripeFinalizeInvoicePath
    header: StripeFinalizeInvoiceHeader = Field(default_factory=StripeFinalizeInvoiceHeader)
    body: StripeFinalizeInvoiceBody = Field(default_factory=StripeFinalizeInvoiceBody)


class StripeGetAccountInfoRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class StripeListCouponsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListCouponsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListCouponsQuery | None = None


class StripeListCustomersQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email: str | None = None
    created: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListCustomersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListCustomersQuery | None = None


class StripeListDisputesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    charge: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListDisputesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListDisputesQuery | None = None


class StripeListInvoicesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str | None = None
    status: Literal["draft", "open", "paid", "uncollectible", "void"] | None = None
    subscription: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListInvoicesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListInvoicesQuery | None = None


class StripeListPaymentIntentsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListPaymentIntentsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListPaymentIntentsQuery | None = None


class StripeListPricesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    active: bool | None = None
    currency: str | None = None
    product: str | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListPricesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListPricesQuery | None = None


class StripeListProductsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    active: bool | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListProductsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListProductsQuery | None = None


class StripeListSubscriptionsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    customer: str | None = None
    price: str | None = None
    status: (
        Literal[
            "incomplete",
            "incomplete_expired",
            "trialing",
            "active",
            "past_due",
            "canceled",
            "unpaid",
            "paused",
        ]
        | None
    ) = None
    collection_method: Literal["charge_automatically", "send_invoice"] | None = None
    limit: int = 10
    starting_after: str | None = None
    ending_before: str | None = None


class StripeListSubscriptionsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: StripeListSubscriptionsQuery | None = None


class StripeRetrieveBalanceRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class StripeUpdateCustomerDefaultPaymentMethodPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class StripeUpdateCustomerDefaultPaymentMethodHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeUpdateCustomerDefaultPaymentMethodBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    invoice_settings_default_payment_method_: str = Field(
        alias="invoice_settings[default_payment_method]"
    )


class StripeUpdateCustomerDefaultPaymentMethodRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StripeUpdateCustomerDefaultPaymentMethodPath
    header: StripeUpdateCustomerDefaultPaymentMethodHeader = Field(
        default_factory=StripeUpdateCustomerDefaultPaymentMethodHeader
    )
    body: StripeUpdateCustomerDefaultPaymentMethodBody


class StripeUpdateDisputePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class StripeUpdateDisputeHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeUpdateDisputeBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    evidence: dict[str, Any] | None = None
    metadata: dict[str, Any] | None = None


class StripeUpdateDisputeRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StripeUpdateDisputePath
    header: StripeUpdateDisputeHeader = Field(default_factory=StripeUpdateDisputeHeader)
    body: StripeUpdateDisputeBody = Field(default_factory=StripeUpdateDisputeBody)


class StripeUpdateSubscriptionPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: str


class StripeUpdateSubscriptionHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/x-www-form-urlencoded", alias="Content-Type")


class StripeUpdateSubscriptionBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    items: list[Any] | None = None
    trial_end: str | None = None
    proration_behavior: Literal["create_prorations", "none", "always_invoice"] | None = None


class StripeUpdateSubscriptionRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: StripeUpdateSubscriptionPath
    header: StripeUpdateSubscriptionHeader = Field(default_factory=StripeUpdateSubscriptionHeader)
    body: StripeUpdateSubscriptionBody = Field(default_factory=StripeUpdateSubscriptionBody)


class StripeNamespace:
    """Typed accessor for the `stripe` app. Reach it via `client.apps.stripe`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def attach_payment_method(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeAttachPaymentMethodRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Attaches an existing payment method to a customer, making it available for future payments and subscriptions. Once attached, the payment method can be used as the customer's default or selected at checkout. Use this to enable customers to pay using saved payment methods. Impacts the customer's ability to make automated payments.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.attach_payment_method", owner_id, args, account_id=account_id
        )

    async def cancel_subscription(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCancelSubscriptionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Cancels an active subscription immediately at period end (the customer retains access until the billing period ends). Use this to end a customer's recurring billing. The subscription status changes to canceled. Customer loses access after the current period. Reversible only by re-subscribing before period end.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.cancel_subscription", owner_id, args, account_id=account_id
        )

    async def create_coupon(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreateCouponRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new discount coupon that can be applied to customers and invoices. Supports percent-off or amount-off discount types with configurable duration. Coupons affect pricing when applied to subscriptions or invoices. Use this to create promotional discounts. Reversible by deleting the coupon or not applying it to customers.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_coupon", owner_id, args, account_id=account_id
        )

    async def create_customer(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreateCustomerRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new customer record in Stripe with optional name, email, phone, and description. Customers are required for subscriptions and invoicing. The customer record can store payment methods and transaction history. Use this to register new customers before charging them. Reversible by deleting the customer.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_customer", owner_id, args, account_id=account_id
        )

    async def create_invoice(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreateInvoiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new draft invoice for a specified customer. The invoice can be customized and will not be charged until finalized. Use this to bill customers for one-time charges. Supports auto_advance for automatic finalization. Draft invoices are non-binding and can be deleted if not needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_invoice", owner_id, args, account_id=account_id
        )

    async def create_invoice_item(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreateInvoiceItemRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a line item to a draft invoice (or to the next invoice for a customer if no invoice is specified). Each item specifies a customer, amount in cents, currency, and optional description. Use this to build up the contents of an invoice before finalizing. Up to 250 items per invoice. Reversible by deleting the invoice item or not finalizing the invoice.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_invoice_item", owner_id, args, account_id=account_id
        )

    async def create_payment_link(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreatePaymentLinkRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a shareable payment link that customers can use to complete a purchase. Requires a price ID and quantity. The link can be distributed via email, website, or any channel. Customers complete payment on Stripe's hosted page. Use this to sell products or services without building a checkout page. Generates a live payment URL.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_payment_link", owner_id, args, account_id=account_id
        )

    async def create_payment_method(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreatePaymentMethodRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a payment method object of a specified type (e.g., card) using a token. The payment method must be attached to a customer before it can be used for payments. Use this to securely store payment information for future charges. Card tokens represent sensitive card data — handle the resulting IDs carefully.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_payment_method", owner_id, args, account_id=account_id
        )

    async def create_price(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreatePriceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new price object for a product, defining how much to charge and whether it's a one-time or recurring (subscription) charge. Prices are referenced by subscriptions, invoices, and payment links. Use this to define pricing before creating subscriptions or selling products. Reversible by deactivating the price, but existing subscriptions using it continue until changed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_price", owner_id, args, account_id=account_id
        )

    async def create_product(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreateProductRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new product record in Stripe with a name, description, and optional tax code. Products are the top-level objects that prices are attached to. Use this to define what you sell before creating prices and subscriptions. Reversible by deactivating the product.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_product", owner_id, args, account_id=account_id
        )

    async def create_refund(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreateRefundRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Issues a full or partial refund for a charge or PaymentIntent, optionally specifying a reason and amount. Refunds return funds to the customer and are subtracted from the Stripe balance. Use this to process returns, cancel incorrect charges, or handle disputes. Financial impact — refunded amounts are deducted from the account balance.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_refund", owner_id, args, account_id=account_id
        )

    async def create_subscription(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeCreateSubscriptionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new recurring subscription for a customer with specified pricing, trial days, and collection method. The customer will be charged according to the billing schedule. Use this to set up recurring revenue. Creates a financial commitment — the customer is billed on the defined schedule. Impacts ongoing billing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.create_subscription", owner_id, args, account_id=account_id
        )

    async def finalize_invoice(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeFinalizeInvoiceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Finalizes a draft invoice, transitioning it to an open state. Once finalized, Stripe can attempt payment (if auto_advance is enabled). This is the step that converts a draft into a real billing document. Use this to trigger the billing process. Finalized invoices affect customer balances and may trigger charges. Reversible by voiding but not by un-finalizing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.finalize_invoice", owner_id, args, account_id=account_id
        )

    async def get_account_info(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeGetAccountInfoRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves details of the connected Stripe account including business profile, country, capabilities, payout settings, and bank accounts. Use this to verify account configuration and capabilities before performing charge or payout operations. Returns sensitive financial information about the account. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.get_account_info", owner_id, args, account_id=account_id
        )

    async def list_coupons(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListCouponsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of discount coupons created on the Stripe account. Use this to browse existing coupons, check their details, or find a coupon ID before applying it. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_coupons", owner_id, args, account_id=account_id
        )

    async def list_customers(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListCustomersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of customers on the Stripe account, filterable by email or creation date. Use this to find customers, verify their existence, or get customer IDs for further operations. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_customers", owner_id, args, account_id=account_id
        )

    async def list_disputes(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListDisputesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of disputes (chargebacks) filed against charges on the Stripe account. Optionally filterable by charge ID. Use this to monitor and review disputes. Dispute data includes amounts, statuses, and evidence requirements. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_disputes", owner_id, args, account_id=account_id
        )

    async def list_invoices(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListInvoicesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of invoices, filterable by customer, status, or subscription. Use this to view billing history, check invoice statuses, or find specific invoices for further operations. Invoice data includes amounts, status, and payment details. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_invoices", owner_id, args, account_id=account_id
        )

    async def list_payment_intents(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListPaymentIntentsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of PaymentIntents, filterable by customer. PaymentIntents track the lifecycle of a payment from creation to completion or failure. Use this to monitor payment activity, check transaction statuses, or reconcile payments. Contains financial transaction data. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_payment_intents", owner_id, args, account_id=account_id
        )

    async def list_prices(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListPricesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of prices, filterable by active status, currency, or product. Use this to browse pricing, find price IDs for products, or check what prices exist. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_prices", owner_id, args, account_id=account_id
        )

    async def list_products(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListProductsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of products, filterable by active status. Use this to browse the product catalog, find product IDs, or check what products exist. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_products", owner_id, args, account_id=account_id
        )

    async def list_subscriptions(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeListSubscriptionsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of subscriptions, filterable by customer, price, status, and collection method. Use this to view all active/inactive subscriptions, check customer signups, or audit billing. Contains customer subscription and billing data. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.list_subscriptions", owner_id, args, account_id=account_id
        )

    async def retrieve_balance(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeRetrieveBalanceRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the current balance of the connected Stripe account, including available and pending amounts by currency. Use this to check account funds before initiating payouts or to verify payment inflows. Contains sensitive financial data. Read-only.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.retrieve_balance", owner_id, args, account_id=account_id
        )

    async def update_customer_default_payment_method(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeUpdateCustomerDefaultPaymentMethodRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Sets a customer's default payment method for all future invoices and subscriptions. This payment method will be used for automatic collection unless another method is specified per-transaction. Use this to ensure billing continuity or update expired cards. Impacts recurring billing.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.update_customer_default_payment_method", owner_id, args, account_id=account_id
        )

    async def update_dispute(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeUpdateDisputeRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates a dispute with evidence (receipt, shipping documentation) and metadata to contest a chargeback. Submitting compelling evidence is critical for winning disputes. Use this to respond to disputes and protect revenue. Directly impacts whether the dispute is resolved in your favor. Involves financial risk.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.update_dispute", owner_id, args, account_id=account_id
        )

    async def update_subscription(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: StripeUpdateSubscriptionRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing subscription by modifying items (price, quantity), trial end, or proration behavior. Changes take effect immediately and may trigger prorated charges or credits. Use this to upgrade/downgrade plans, adjust quantities, or modify trial periods. Impacts customer billing and may result in immediate charges or refunds.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "stripe.update_subscription", owner_id, args, account_id=account_id
        )
