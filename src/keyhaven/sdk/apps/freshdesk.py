# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `freshdesk` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class FreshdeskContactCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshdeskContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    email: str | None = None
    phone: str | None = None
    mobile: str | None = None
    company_id: int | None = None
    job_title: str | None = None
    description: str | None = None
    tags: list[Any] | None = None


class FreshdeskContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshdeskContactCreatePath
    body: FreshdeskContactCreateBody = Field(default_factory=FreshdeskContactCreateBody)


class FreshdeskContactsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshdeskContactsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 30
    email: str | None = None


class FreshdeskContactsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshdeskContactsListPath
    query: FreshdeskContactsListQuery | None = None


class FreshdeskTicketCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshdeskTicketCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    email: str | None = None
    subject: str
    description: str
    status: int = 2
    priority: int = 1
    source: int | None = None
    group_id: int | None = None
    responder_id: int | None = None
    tags: list[Any] | None = None


class FreshdeskTicketCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshdeskTicketCreatePath
    body: FreshdeskTicketCreateBody


class FreshdeskTicketGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str
    ticket_id: str


class FreshdeskTicketGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include: str | None = None


class FreshdeskTicketGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshdeskTicketGetPath
    query: FreshdeskTicketGetQuery | None = None


class FreshdeskTicketUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str
    ticket_id: str


class FreshdeskTicketUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    status: int | None = None
    priority: int | None = None
    group_id: int | None = None
    responder_id: int | None = None
    tags: list[Any] | None = None


class FreshdeskTicketUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshdeskTicketUpdatePath
    body: FreshdeskTicketUpdateBody = Field(default_factory=FreshdeskTicketUpdateBody)


class FreshdeskTicketsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    domain: str


class FreshdeskTicketsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    page: int = 1
    per_page: int = 30
    order_by: str | None = None
    order_type: str = "desc"
    filter: str | None = None


class FreshdeskTicketsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: FreshdeskTicketsListPath
    query: FreshdeskTicketsListQuery | None = None


class FreshdeskNamespace:
    """Typed accessor for the `freshdesk` app. Reach it via `client.apps.freshdesk`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshdeskContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact (end user) in the Freshdesk portal with name, email, phone, mobile, company association, job title, description, and tags. The new contact becomes immediately available for ticket creation and can be linked to tickets, companies, and conversations. The email must be unique within the portal. Use it when onboarding a new customer into the support system, importing contacts from another platform, or creating a contact before submitting a ticket on their behalf.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshdesk.contact_create", owner_id, args, account_id=account_id
        )

    async def contacts_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshdeskContactsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of contacts (end users) in the Freshdesk portal, with optional filtering by exact email address. Each contact includes name, email, phone, company, and other profile fields. This is a read-only query that does not create or modify any contact data. Use it when searching for existing contacts by email, auditing the user base, or populating a contact picker for ticket assignment.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshdesk.contacts_list", owner_id, args, account_id=account_id
        )

    async def ticket_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshdeskTicketCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new support ticket in the Freshdesk portal with subject, description, requester email, priority, status, source, and optional assignment to a group or agent. If the email does not match an existing contact, a new contact is auto-created. The ticket immediately appears in the Freshdesk queue and can trigger automation rules, SLA policies, and email notifications to the assigned agent. Use it when logging a new customer issue, automating ticket creation from external systems, or submitting a support request on behalf of a user.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshdesk.ticket_create", owner_id, args, account_id=account_id
        )

    async def ticket_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshdeskTicketGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single Freshdesk ticket by its numeric ID, including subject, description, status, priority, requester info, assigned agent, group, tags, and optionally embedded resources like requester profile, company, and conversation stats. This read-only operation does not alter any ticket data. Use it when inspecting the full context of a specific support ticket before replying, escalating, or updating its status.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshdesk.ticket_get", owner_id, args, account_id=account_id
        )

    async def ticket_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshdeskTicketUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates one or more fields of an existing Freshdesk ticket, including status, priority, group assignment, responder (agent), and tags. Changing the status to Closed or Resolved can trigger satisfaction surveys and close SLA timers, while reassigning the ticket routes it to a different agent or team. This action is visible to the requester through the portal and email notifications depending on Freshdesk configuration. Use it when triaging tickets, escalating priority, reassigning workloads, or resolving completed issues.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshdesk.ticket_update", owner_id, args, account_id=account_id
        )

    async def tickets_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: FreshdeskTicketsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of support tickets from the Freshdesk portal, with optional filtering by predefined views (new and my open, watching, spam, deleted), sorting by date or priority, and page control. By default, only tickets created or updated in the last 30 days are returned. This is a read-only query that does not modify any ticket data. Use it when reviewing open support requests, monitoring ticket volume, or populating a ticket dashboard.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "freshdesk.tickets_list", owner_id, args, account_id=account_id
        )
