# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `manatal` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ManatalCandidateCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    full_name: str
    email: str | None = None
    phone_number: str | None = None
    source_type: str | None = None
    consent: bool | None = None
    owner: int | None = None
    gender: str | None = None
    birth_date: str | None = None


class ManatalCandidateCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ManatalCandidateCreateBody


class ManatalCandidatesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    full_name__icontains: str | None = None
    email: str | None = None


class ManatalCandidatesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ManatalCandidatesListQuery | None = None


class ManatalJobCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str
    description: str
    status: str | None = None
    location: str | None = None


class ManatalJobCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ManatalJobCreateBody


class ManatalJobGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class ManatalJobGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ManatalJobGetPath


class ManatalJobUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class ManatalJobUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    title: str | None = None
    description: str | None = None
    status: str | None = None


class ManatalJobUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ManatalJobUpdatePath
    body: ManatalJobUpdateBody | None = None


class ManatalJobsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    offset: int | None = None
    status: str | None = None
    sort: str | None = None


class ManatalJobsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ManatalJobsListQuery | None = None


class ManatalNamespace:
    """Typed accessor for the `manatal` app. Reach it via `client.apps.manatal`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def candidate_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ManatalCandidateCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new candidate profile in the Manatal ATS with the required full name, plus optional email, phone, source, consent flag, owner assignment, gender, and birth date. The candidate becomes available for attachment to job applications and searches. Use this when a new person applies, is referred, or is sourced, to record them in the ATS before associating them with a job.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "manatal.candidate_create", owner_id, args, account_id=account_id
        )

    async def candidates_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ManatalCandidatesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of candidate profiles in the Manatal ATS, optionally filtered by name or email. Each result includes the candidate's full name, email, phone, and source. Use this to search for existing candidates by name or email, retrieve candidate IDs for follow-up operations, or audit the candidate pipeline.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "manatal.candidates_list", owner_id, args, account_id=account_id
        )

    async def job_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ManatalJobCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new job posting in the Manatal ATS with a required title and description, plus optional location and initial status. The job will appear in the job board and become visible to recruiters and (depending on configuration) external candidates. Use this when a new position opens and needs to be published in the ATS to start receiving applications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "manatal.job_create", owner_id, args, account_id=account_id
        )

    async def job_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ManatalJobGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a single job posting by its unique ID, including title, description, status, location, and assigned owner. Use this when you need the complete job record — for example, to review the description before reposting or to inspect a job before updating it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("manatal.job_get", owner_id, args, account_id=account_id)

    async def job_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ManatalJobUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing job posting's title, description, or status. Changes are immediately reflected in the ATS and on any public job board the post is published to. Use this when a job's scope changes, the posting needs to be paused (on_hold), or the position has been filled (won) or cancelled (lost).
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "manatal.job_update", owner_id, args, account_id=account_id
        )

    async def jobs_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ManatalJobsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of job postings from the Manatal ATS, with optional filtering by status and sorting. Each result includes the job title, ID, status, location, and creation date. Use this to browse open positions, monitor pipeline state, or find a specific job ID needed for a subsequent detail or update call.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "manatal.jobs_list", owner_id, args, account_id=account_id
        )
