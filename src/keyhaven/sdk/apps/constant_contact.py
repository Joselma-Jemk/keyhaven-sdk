# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `constant_contact` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ConstantContactCampaignActivityUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    campaign_activity_id: str


class ConstantContactCampaignActivityUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    campaign_id: str
    role: str | None = None
    contact_list_ids: list[Any] | None = None
    subject: str | None = None
    from_email: str | None = None
    from_name: str | None = None
    html_content: str | None = None


class ConstantContactCampaignActivityUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ConstantContactCampaignActivityUpdatePath
    body: ConstantContactCampaignActivityUpdateBody


class ConstantContactContactCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    email_address: str
    first_name: str | None = None
    last_name: str | None = None
    job_title: str | None = None
    company_name: str | None = None
    phone_number: str | None = None
    list_memberships: list[Any] | None = None


class ConstantContactContactCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ConstantContactContactCreateBody


class ConstantContactContactListCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    favorite: bool = False


class ConstantContactContactListCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ConstantContactContactListCreateBody


class ConstantContactContactListsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include_count: bool = True
    status: str | None = None
    include_membership_count: str | None = None


class ConstantContactContactListsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ConstantContactContactListsListQuery | None = None


class ConstantContactEmailCampaignCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    format_type: str
    from_email: str | None = None
    from_name: str | None = None
    subject: str | None = None
    html_content: str | None = None
    contact_list_ids: list[Any] | None = None


class ConstantContactEmailCampaignCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ConstantContactEmailCampaignCreateBody


class ConstantContactNamespace:
    """Typed accessor for the `constant_contact` app. Reach it via `client.apps.constant_contact`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def campaign_activity_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ConstantContactCampaignActivityUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing email campaign activity's content, settings, and target lists using the campaign_activity_id. This modifies the campaign draft in place — it can change the subject, sender, HTML body, and recipient lists. Use when an agent needs to revise campaign content or targeting before the campaign is sent.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "constant_contact.campaign_activity_update", owner_id, args, account_id=account_id
        )

    async def contact_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ConstantContactContactCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Adds a new contact to the Constant Contact account with email address, name, company, phone, and list memberships. The contact becomes available for email campaigns and tracking. Use when an agent needs to import or register a new subscriber, lead, or customer into the marketing database.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "constant_contact.contact_create", owner_id, args, account_id=account_id
        )

    async def contact_list_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ConstantContactContactListCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new contact list with a unique name, optional description, and favorite flag. Lists are containers used to organize contacts before sending email campaigns. Use when an agent needs to set up a new segmentation or mailing group for targeting campaigns.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "constant_contact.contact_list_create", owner_id, args, account_id=account_id
        )

    async def contact_lists_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ConstantContactContactListsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all contact lists in the Constant Contact account, with optional filters for status (active, archived, all) and membership counts. Returns list metadata including ID, name, description, and member count. Use when an agent needs to find or select a specific list before adding contacts or targeting an email campaign.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "constant_contact.contact_lists_list", owner_id, args, account_id=account_id
        )

    async def email_campaign_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ConstantContactEmailCampaignCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new email campaign draft with specified HTML content, sender details, subject line, and target contact lists. The campaign is created in draft mode and is not sent automatically. Use when an agent needs to prepare an email blast for review before a human triggers the send.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "constant_contact.email_campaign_create", owner_id, args, account_id=account_id
        )
