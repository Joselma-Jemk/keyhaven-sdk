# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `toggl` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class TogglMeGetQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    with_related_data: bool | None = None


class TogglMeGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TogglMeGetQuery | None = None


class TogglProjectsListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace_id: int


class TogglProjectsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    active: bool | None = None
    name: str | None = None
    client_ids: str | None = None
    page: int = 1
    per_page: int = 50


class TogglProjectsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TogglProjectsListPath
    query: TogglProjectsListQuery | None = None


class TogglTimeEntriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    since: int | None = None
    before: str | None = None
    start_date: str | None = None
    end_date: str | None = None
    meta: bool | None = None


class TogglTimeEntriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: TogglTimeEntriesListQuery | None = None


class TogglTimeEntryCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace_id: int


class TogglTimeEntryCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace_id: int
    created_with: str
    description: str | None = None
    start: str
    stop: str | None = None
    duration: int
    billable: bool = False
    project_id: int | None = None
    task_id: int | None = None
    tags: list[Any] | None = None


class TogglTimeEntryCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TogglTimeEntryCreatePath
    body: TogglTimeEntryCreateBody


class TogglTimeEntryUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace_id: int
    time_entry_id: int


class TogglTimeEntryUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    description: str | None = None
    start: str | None = None
    stop: str | None = None
    duration: int | None = None
    billable: bool | None = None
    project_id: int | None = None
    task_id: int | None = None
    tag_action: Literal["add", "delete"] | None = None
    tags: list[Any] | None = None


class TogglTimeEntryUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: TogglTimeEntryUpdatePath
    body: TogglTimeEntryUpdateBody = Field(default_factory=TogglTimeEntryUpdateBody)


class TogglWorkspacesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class TogglNamespace:
    """Typed accessor for the `toggl` app. Reach it via `client.apps.toggl`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def me_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TogglMeGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the authenticated Toggl Track user's profile including name, email, default workspace, and timezone settings. When with_related_data is true, it also returns workspaces, projects, clients, and tags in a single response. Use this function to verify the caller's identity or fetch user-level configuration before operating on workspaces.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("toggl.me_get", owner_id, args, account_id=account_id)

    async def projects_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TogglProjectsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists the projects in a given workspace, with optional filtering by active status, name substring, and client IDs. Results are paginated via page and per_page parameters. This read-only discovery call does not modify any data. Use this function before assigning time entries to a project or when mapping client work to project identifiers in the workspace.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "toggl.projects_list", owner_id, args, account_id=account_id
        )

    async def time_entries_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TogglTimeEntriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the authenticated user's time entries, optionally narrowed by date range using since, before, start_date, and end_date parameters. This read-only operation shows what time has been logged and can be used to audit billable hours or verify entries before modification. Use this function when you need to inspect time logs for reporting, analysis, or as a prerequisite before updating a specific entry.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "toggl.time_entries_list", owner_id, args, account_id=account_id
        )

    async def time_entry_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TogglTimeEntryCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new time entry in the specified workspace with a description, start/stop times, duration, and optional project, task, or tag associations. The entry can be billable and can represent a completed block of time or a running timer when duration is -1. Use this function when an agent needs to log time on behalf of a user — the entry becomes visible in Toggl reports and dashboards immediately after creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "toggl.time_entry_create", owner_id, args, account_id=account_id
        )

    async def time_entry_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TogglTimeEntryUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Modifies an existing time entry's description, time range, duration, billable flag, project, task, or tags using the tag_action parameter to control add/delete behavior. This overwrites the provided fields and is immediately reflected in Toggl reports and dashboards. Use this function to correct a mis-logged entry, reallocate time to a different project, or adjust billable status after creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "toggl.time_entry_update", owner_id, args, account_id=account_id
        )

    async def workspaces_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: TogglWorkspacesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all workspaces the authenticated user belongs to, with their numeric IDs, names, and permission levels. This is a read-only discovery step that does not modify any data. Use this function before performing workspace-scoped operations to obtain the workspace identifiers needed for other endpoints.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "toggl.workspaces_list", owner_id, args, account_id=account_id
        )
