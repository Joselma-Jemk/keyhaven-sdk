# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `coda` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CodaDeleteRowPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    doc_id: str
    table_id: str
    row_id: str


class CodaDeleteRowRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CodaDeleteRowPath


class CodaGetRowPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    doc_id: str
    table_id: str
    row_id: str


class CodaGetRowQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    valueFormat: Literal["user", "rich"] | None = None


class CodaGetRowRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CodaGetRowPath
    query: CodaGetRowQuery | None = None


class CodaListDocsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    pageToken: str | None = None


class CodaListDocsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: CodaListDocsQuery | None = None


class CodaListRowsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    doc_id: str
    table_id: str


class CodaListRowsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    pageToken: str | None = None
    query: dict[str, Any] | None = None


class CodaListRowsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CodaListRowsPath
    query: CodaListRowsQuery | None = None


class CodaListTablesPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    doc_id: str


class CodaListTablesQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int | None = None
    pageToken: str | None = None


class CodaListTablesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CodaListTablesPath
    query: CodaListTablesQuery | None = None


class CodaNamespace:
    """Typed accessor for the `coda` app. Reach it via `client.apps.coda`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def delete_row(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CodaDeleteRowRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a single row from a specified table in a Coda document using its row ID. This operation is irreversible — the deleted row and its data cannot be recovered through the API. An agent should only call this when a user explicitly instructs deletion of specific data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("coda.delete_row", owner_id, args, account_id=account_id)

    async def get_row(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CodaGetRowRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single row by its ID from a specific table in a Coda document, returning all column values for that row. Supports a valueFormat parameter to choose between plain text ('user') or rich text with formatting and links ('rich'). Use when an agent needs the full detail of a known row, for example to inspect, update, or reference its content.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("coda.get_row", owner_id, args, account_id=account_id)

    async def list_docs(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CodaListDocsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all Coda documents accessible to the authenticated user, returning metadata such as document ID, name, URL, and creation timestamps. Supports pagination via limit and pageToken to handle large workspaces. Use this when an agent needs to discover which documents exist before performing operations on a specific one.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("coda.list_docs", owner_id, args, account_id=account_id)

    async def list_rows(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CodaListRowsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches rows from a specified table in a Coda document, with optional column-value filtering to narrow results. Supports pagination via limit and pageToken. Use this when an agent needs to read, process, or aggregate data stored in a table.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("coda.list_rows", owner_id, args, account_id=account_id)

    async def list_tables(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CodaListTablesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves all tables within a specified Coda document, including their IDs, names, and types (e.g., table, view). Supports pagination through limit and pageToken. Use this to enumerate the structure of a document before querying or modifying rows.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("coda.list_tables", owner_id, args, account_id=account_id)
