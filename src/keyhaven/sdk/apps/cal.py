# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `cal` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class CalCreateScheduleBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    timeZone: str
    availability: list[Any] | None = None
    isDefault: bool = False


class CalCreateScheduleRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: CalCreateScheduleBody


class CalDeleteSchedulePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    schedule_id: str


class CalDeleteScheduleRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CalDeleteSchedulePath


class CalGetSchedulePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    schedule_id: str


class CalGetScheduleRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CalGetSchedulePath


class CalGetSchedulesRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class CalUpdateSchedulePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    schedule_id: str


class CalUpdateScheduleBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    timeZone: str | None = None
    availability: list[Any] | None = None
    overrides: list[Any] | None = None


class CalUpdateScheduleRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: CalUpdateSchedulePath
    body: CalUpdateScheduleBody = Field(default_factory=CalUpdateScheduleBody)


class CalNamespace:
    """Typed accessor for the `cal` app. Reach it via `client.apps.cal`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_schedule(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalCreateScheduleRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new availability schedule with a name, timezone, working days and hours, and optional date overrides. The new schedule can be set as the default, which applies it to all event types without a specific schedule assigned. Use when setting up a new availability pattern for a user or team. This action is reversible — schedules can be updated or deleted after creation.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cal.create_schedule", owner_id, args, account_id=account_id
        )

    async def delete_schedule(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalDeleteScheduleRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a schedule and all its availability configuration from the account. Event types relying on this schedule fall back to the default schedule or become unavailable if no default exists. This action is irreversible — the deleted schedule and its data cannot be recovered. Use only when certain the schedule is no longer needed.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cal.delete_schedule", owner_id, args, account_id=account_id
        )

    async def get_schedule(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalGetScheduleRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a single schedule by its unique identifier, including its full availability configuration: days, start time, end time, timezone, and any date-specific overrides. Use when the agent needs to inspect a specific schedule's details before modifying it, or to verify current settings.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("cal.get_schedule", owner_id, args, account_id=account_id)

    async def get_schedules(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalGetSchedulesRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all availability schedules configured for the authenticated Cal.com account. Returns each schedule's id, name, timezone, default status, and availability windows. Use when the agent needs to enumerate existing schedules before selecting, creating, or modifying one.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cal.get_schedules", owner_id, args, account_id=account_id
        )

    async def update_schedule(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: CalUpdateScheduleRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Updates an existing schedule's name, timezone, availability windows, or date-specific overrides. Changes take effect immediately and affect all event types linked to this schedule. Use when modifying working hours, adding holiday overrides, switching timezones, or adjusting availability. Previous values can be restored by re-updating.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "cal.update_schedule", owner_id, args, account_id=account_id
        )
