# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `recruitee` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class RecruiteeCandidateCreatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: int


class RecruiteeCandidateCreateHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RecruiteeCandidateCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    candidate: dict[str, Any]


class RecruiteeCandidateCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RecruiteeCandidateCreatePath
    header: RecruiteeCandidateCreateHeader = Field(default_factory=RecruiteeCandidateCreateHeader)
    body: RecruiteeCandidateCreateBody


class RecruiteeCandidateGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: int
    candidate_id: int


class RecruiteeCandidateGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RecruiteeCandidateGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RecruiteeCandidateGetPath
    header: RecruiteeCandidateGetHeader = Field(default_factory=RecruiteeCandidateGetHeader)


class RecruiteeCandidatesListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: int


class RecruiteeCandidatesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 25
    offset: int = 0


class RecruiteeCandidatesListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RecruiteeCandidatesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RecruiteeCandidatesListPath
    query: RecruiteeCandidatesListQuery | None = None
    header: RecruiteeCandidatesListHeader = Field(default_factory=RecruiteeCandidatesListHeader)


class RecruiteeOfferGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: int
    offer_id: int


class RecruiteeOfferGetHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RecruiteeOfferGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RecruiteeOfferGetPath
    header: RecruiteeOfferGetHeader = Field(default_factory=RecruiteeOfferGetHeader)


class RecruiteeOffersListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    company_id: int


class RecruiteeOffersListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    limit: int = 25
    offset: int = 0


class RecruiteeOffersListHeader(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    Content_Type: str = Field(default="application/json", alias="Content-Type")


class RecruiteeOffersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: RecruiteeOffersListPath
    query: RecruiteeOffersListQuery | None = None
    header: RecruiteeOffersListHeader = Field(default_factory=RecruiteeOffersListHeader)


class RecruiteeNamespace:
    """Typed accessor for the `recruitee` app. Reach it via `client.apps.recruitee`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def candidate_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RecruiteeCandidateCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new candidate record in the company's Recruitee account by submitting personal details, contact information, and optional offer associations. The candidate appears immediately in the ATS pipeline and can be viewed by recruiters and hiring managers. Use this to add candidates sourced automatically from external platforms or referrals.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recruitee.candidate_create", owner_id, args, account_id=account_id
        )

    async def candidate_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RecruiteeCandidateGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the full details of a specific candidate by their unique ID, including personal information, contact data, tags, custom fields, and associated job offers and applications. The response contains the complete candidate profile as stored in the ATS. Use this when you need to inspect a candidate's complete record before making decisions or updating their data.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recruitee.candidate_get", owner_id, args, account_id=account_id
        )

    async def candidates_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RecruiteeCandidatesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches a paginated list of all candidates in the company's Recruitee account, with configurable page size and offset for efficient browsing. Each candidate record includes personal details, contact information, application history, and associated offers. Use this to search or browse the candidate database — it is read-only and does not alter any records.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recruitee.candidates_list", owner_id, args, account_id=account_id
        )

    async def offer_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RecruiteeOfferGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves the complete details of a specific job offer by its unique ID, including the job description, requirements, compensation range, workflow stage, and associated candidates. This provides all configuration and metadata for an open position. Use this to inspect a job posting's full specification, or to verify offer details before directing candidates to it.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recruitee.offer_get", owner_id, args, account_id=account_id
        )

    async def offers_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: RecruiteeOffersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns a paginated list of all job offers (open positions) in the company's Recruitee account, with configurable limit and offset. Each offer includes the job title, department, location, status, and other settings. Use this to browse open positions, find offer IDs for candidate placement, or analyze the hiring pipeline.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "recruitee.offers_list", owner_id, args, account_id=account_id
        )
