# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `zoho_desk` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ZohoDeskGetContactPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    contactId: str


class ZohoDeskGetContactQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include: str | None = None


class ZohoDeskGetContactRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZohoDeskGetContactPath
    query: ZohoDeskGetContactQuery | None = None


class ZohoDeskGetTicketDetailsPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    ticketId: str


class ZohoDeskGetTicketDetailsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    include: Literal["contacts", "assignee", "tickets", "teams", "creator"] | None = None


class ZohoDeskGetTicketDetailsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ZohoDeskGetTicketDetailsPath
    query: ZohoDeskGetTicketDetailsQuery | None = None


class ZohoDeskListContactsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: int | None = Field(default=None, alias="from")
    limit: int | None = None
    email: str | None = None
    phone: str | None = None
    type_: str | None = Field(default=None, alias="type")
    include: str | None = None
    sortBy: (
        Literal["createdTime", "lastModifiedTime", "email", "firstName", "lastName", "phone"] | None
    ) = None


class ZohoDeskListContactsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZohoDeskListContactsQuery | None = None


class ZohoDeskListDepartmentsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: int | None = Field(default=None, alias="from")
    limit: int | None = None
    isEnabled: bool | None = None
    searchStr: str | None = None
    chatStatus: Literal["AVAILABLE", "DISABLED", "NOT_CREATED", "UNAVAILABLE"] | None = None


class ZohoDeskListDepartmentsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZohoDeskListDepartmentsQuery | None = None


class ZohoDeskListTicketsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: int | None = Field(default=None, alias="from")
    limit: int | None = None
    status: str | None = None
    departmentId: str | None = None
    assignee: str | None = None
    channel: str | None = None
    include: str | None = None
    sortBy: Literal["createdTime", "dueDate", "statusType", "priority"] | None = None


class ZohoDeskListTicketsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZohoDeskListTicketsQuery | None = None


class ZohoDeskSearchContactsQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    from_: int | None = Field(default=None, alias="from")
    limit: int | None = None
    id: str | None = None
    fullName: str | None = None
    firstName: str | None = None
    lastName: str | None = None
    email: str | None = None
    phone: str | None = None
    mobile: str | None = None
    accountName: str | None = None
    all: str | None = Field(default=None, alias="_all")
    customField1: str | None = None
    customField2: str | None = None
    customField3: str | None = None
    customField4: str | None = None
    customField5: str | None = None
    customField6: str | None = None
    customField7: str | None = None
    customField8: str | None = None
    customField9: str | None = None
    customField10: str | None = None
    createdTimeRange: str | None = None
    modifiedTimeRange: str | None = None
    sortBy: (
        Literal[
            "relevance",
            "modifiedTime",
            "createdTime",
            "lastName",
            "firstName",
            "-modifiedTime",
            "-createdTime",
            "-lastName",
            "-firstName",
        ]
        | None
    ) = None


class ZohoDeskSearchContactsRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ZohoDeskSearchContactsQuery | None = None


class ZohoDeskNamespace:
    """Typed accessor for the `zoho_desk` app. Reach it via `client.apps.zoho_desk`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def get_contact(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZohoDeskGetContactRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full profile of a single contact from Zoho Desk by their unique identifier, optionally including associated accounts and products. Returns detailed contact information including name, email, phone, and related records. Use this when an agent needs the complete context of a contact before creating a ticket on their behalf or updating their information.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoho_desk.get_contact", owner_id, args, account_id=account_id
        )

    async def get_ticket_details(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZohoDeskGetTicketDetailsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches the full details of a single support ticket from Zoho Desk by its unique identifier, optionally including associated contacts, assignee, teams, and creator. Returns comprehensive ticket data including conversation history, status, priority, and assignment. Use this when an agent needs the full context of a specific ticket to respond to the customer, update its status, or diagnose an issue.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoho_desk.get_ticket_details", owner_id, args, account_id=account_id
        )

    async def list_contacts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZohoDeskListContactsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of contacts from Zoho Desk, filterable by email, phone, and type, with sorting options and pagination. Returns contact summaries that can be narrowed down with specific search criteria. Use this when an agent needs to find one or more contacts for ticket assignment, reporting, or bulk operations.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoho_desk.list_contacts", owner_id, args, account_id=account_id
        )

    async def list_departments(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZohoDeskListDepartmentsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all departments configured in Zoho Desk, filterable by enabled status, name search, and chat availability. Returns department metadata including name, description, and operational status. Use this when an agent needs to discover available departments for ticket routing, assignment, or to verify a target department exists before creating a ticket.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoho_desk.list_departments", owner_id, args, account_id=account_id
        )

    async def list_tickets(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZohoDeskListTicketsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves a paginated list of support tickets from Zoho Desk, filterable by status, department, assignee, and channel. This read-only operation returns ticket summaries including subject, status, priority, and created time. Use this when an agent needs to find tickets matching specific criteria before performing detailed inspection or bulk actions.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoho_desk.list_tickets", owner_id, args, account_id=account_id
        )

    async def search_contacts(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ZohoDeskSearchContactsRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Searches contacts across the Zoho Desk portal using a wide range of criteria including name, email, phone, mobile, account name, custom fields, and creation or modification date ranges. Returns matching contact profiles with pagination and sorting options, including relevance-based ordering. Use this when an agent needs to locate a specific contact using flexible search terms with wildcard patterns or find contacts matching custom field values.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "zoho_desk.search_contacts", owner_id, args, account_id=account_id
        )
