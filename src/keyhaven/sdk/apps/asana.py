# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `asana` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class AsanaCreateTaskBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    data: dict[str, Any]


class AsanaCreateTaskRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: AsanaCreateTaskBody


class AsanaDeleteTaskPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    task_gid: str


class AsanaDeleteTaskRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AsanaDeleteTaskPath


class AsanaGetTasksFromUserTaskListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_task_list_gid: str


class AsanaGetTasksFromUserTaskListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    completed_since: str | None = None
    opt_fields: str | None = None
    limit: int | None = None


class AsanaGetTasksFromUserTaskListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AsanaGetTasksFromUserTaskListPath
    query: AsanaGetTasksFromUserTaskListQuery | None = None


class AsanaGetUserPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_gid: str = "me"


class AsanaGetUserRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AsanaGetUserPath = Field(default_factory=AsanaGetUserPath)


class AsanaGetUserTaskListPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    user_gid: str = "me"


class AsanaGetUserTaskListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace: str


class AsanaGetUserTaskListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AsanaGetUserTaskListPath = Field(default_factory=AsanaGetUserTaskListPath)
    query: AsanaGetUserTaskListQuery


class AsanaListUsersPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    workspace_gid: str


class AsanaListUsersRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: AsanaListUsersPath


class AsanaNamespace:
    """Typed accessor for the `asana` app. Reach it via `client.apps.asana`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def create_task(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaCreateTaskRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Creates a new task in a specified workspace with a name, description, assignee, project membership, and due date. The task immediately becomes visible to workspace members and appears in the assignee's My Tasks list. Use this to automate task intake from external triggers, create follow-up items, or populate a project backlog programmatically.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana.create_task", owner_id, args, account_id=account_id
        )

    async def delete_task(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaDeleteTaskRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Permanently removes a task and all its associated data (comments, subtasks, attachments, likes) from Asana. This operation cannot be undone through the API — the task and its entire history are gone. Use this only when you are certain the task and all its content is no longer needed, such as cleaning up duplicates or spam.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana.delete_task", owner_id, args, account_id=account_id
        )

    async def get_tasks_from_user_task_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaGetTasksFromUserTaskListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Fetches all tasks belonging to a specific user task list, with optional filtering by completion status and pagination control. You can also select which fields to return using opt_fields. Use this to retrieve someone's personal task queue, inspect what they are working on, or find incomplete tasks assigned to a user.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana.get_tasks_from_user_task_list", owner_id, args, account_id=account_id
        )

    async def get_user(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaGetUserRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieves profile information (name, email, photo, workspaces) for a single user identified by their unique GID. The response includes basic account details such as display name and email address, but not sensitive authentication credentials. Defaults to the authenticated user when user_gid is omitted. Use this to look up who a user is, verify their workspace membership, or fetch their display name for notifications.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("asana.get_user", owner_id, args, account_id=account_id)

    async def get_user_task_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaGetUserTaskListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Returns the task list object for a specific user within a given workspace. The user task list is a container that holds all tasks assigned to that user in that workspace. Use this to obtain the user_task_list_gid, which is then needed as input to fetch the actual tasks from that list.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "asana.get_user_task_list", owner_id, args, account_id=account_id
        )

    async def list_users(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: AsanaListUsersRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Lists all members of a given workspace, returning their profile information such as name and email address. This is a read-only enumeration that reveals who has access to the workspace. Use this to find a user's GID by name, verify team membership, or build a roster of workspace participants.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute("asana.list_users", owner_id, args, account_id=account_id)
