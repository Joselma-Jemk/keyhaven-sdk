# @generated by scripts/gen_sdk.py — DO NOT EDIT.

"""Typed SDK namespace for the `shortcut` app (generated)."""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from keyhaven.sdk._constants import DEFAULT_ACCOUNT_ID

if TYPE_CHECKING:
    from keyhaven.sdk.client import Keyhaven


class ShortcutEpicsListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: int | None = None
    archived: bool | None = None


class ShortcutEpicsListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ShortcutEpicsListQuery | None = None


class ShortcutMembersListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)
    pass


class ShortcutStoriesListQuery(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    project_id: int | None = None
    workflow_id: int | None = None
    owner_id: str | None = None
    archived: bool | None = None
    query: str | None = None
    limit: int | None = None


class ShortcutStoriesListRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    query: ShortcutStoriesListQuery | None = None


class ShortcutStoryCreateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str
    description: str | None = None
    workflow_state_id: int
    project_id: int | None = None
    epic_id: int | None = None
    iteration_id: int | None = None
    owner_id: str | None = None
    story_type: Literal["feature", "bug", "chore"] | None = None
    estimate: int | None = None
    labels: list[Any] | None = None


class ShortcutStoryCreateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    body: ShortcutStoryCreateBody


class ShortcutStoryGetPath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class ShortcutStoryGetRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ShortcutStoryGetPath


class ShortcutStoryUpdatePath(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    id: int


class ShortcutStoryUpdateBody(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    name: str | None = None
    description: str | None = None
    workflow_state_id: int | None = None
    owner_id: str | None = None
    estimate: int | None = None
    story_type: Literal["feature", "bug", "chore"] | None = None
    archived: bool | None = None


class ShortcutStoryUpdateRequest(BaseModel):
    model_config = ConfigDict(extra="ignore", validate_by_name=True)

    path: ShortcutStoryUpdatePath
    body: ShortcutStoryUpdateBody = Field(default_factory=ShortcutStoryUpdateBody)


class ShortcutNamespace:
    """Typed accessor for the `shortcut` app. Reach it via `client.apps.shortcut`."""

    def __init__(self, client: Keyhaven) -> None:
        self._client = client

    async def epics_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShortcutEpicsListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all epics in the Shortcut workspace, optionally filtered by project or archived status. Epics are high-level containers that group related stories together. Use this to discover existing epics before assigning stories to them or to report on epic-level progress.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shortcut.epics_list", owner_id, args, account_id=account_id
        )

    async def members_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShortcutMembersListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        List all members of the authenticated Shortcut workspace, including their profile information such as name, email, and role. Use this to look up member IDs needed for assigning stories or filtering by owner across the workspace.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shortcut.members_list", owner_id, args, account_id=account_id
        )

    async def stories_list(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShortcutStoriesListRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Search and list stories in Shortcut with optional filters by project, workflow state, owner, archived status, or free-text query. Results are paginated with a configurable limit. Use this to find stories matching specific criteria before reading or updating them.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shortcut.stories_list", owner_id, args, account_id=account_id
        )

    async def story_create(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShortcutStoryCreateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Create a new story in Shortcut with a name, workflow state, description, project, epic, labels, and owner. The story becomes immediately visible to all workspace members who have access to the project. Use this to capture new work items such as features, bugs, or chores from an automated pipeline or agent.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shortcut.story_create", owner_id, args, account_id=account_id
        )

    async def story_get(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShortcutStoryGetRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Retrieve a single Shortcut story by its numeric ID, including all fields such as name, description, assignee, workflow state, labels, and comments. Use this when you already know the story ID and need its full details for display, reporting, or decision-making.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shortcut.story_get", owner_id, args, account_id=account_id
        )

    async def story_update(
        self,
        *,
        owner_id: str,
        account_id: str = DEFAULT_ACCOUNT_ID,
        request: ShortcutStoryUpdateRequest | None = None,
        **fields: Any,
    ) -> Any:
        """
        Update one or more fields of an existing Shortcut story, such as its name, description, workflow state, owner, estimate, story type, or archived flag. Only the fields included in the request body are changed; omitted fields stay untouched. Use this to move stories through a workflow, reassign ownership, or archive completed work.
        """
        args = (
            request.model_dump(mode="json", by_alias=True, exclude_none=True)
            if request is not None
            else dict(fields)
        )
        return await self._client.execute(
            "shortcut.story_update", owner_id, args, account_id=account_id
        )
